// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSettingsGetCmd(flags *rootFlags) *cobra.Command {
	var flagDeviceSn string

	cmd := &cobra.Command{
		Use:         "get",
		Short:       "Get persistent settings for a device",
		Example:     "  dreo-pp-cli settings get --device-sn example-value",
		Annotations: map[string]string{"pp:endpoint": "settings.get", "pp:method": "GET", "pp:path": "/api/user-device/setting", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("device-sn") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "device-sn")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/user-device/setting"
			params := map[string]string{}
			if flagDeviceSn != "" {
				params["deviceSn"] = fmt.Sprintf("%v", flagDeviceSn)
			}
			data, err := c.Get(path, params)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDeviceSn, "device-sn", "", "Device serial number")

	return cmd
}
