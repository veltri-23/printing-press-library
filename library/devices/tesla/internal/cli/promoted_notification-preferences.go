// Copyright 2026 Matt Van Horn and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newNotificationPreferencesPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAppVersion string
	var bodyDeviceToken string
	var bodyDeviceType string
	var bodyLocale string
	var bodyNotificationPreferencesAlarm bool
	var bodyNotificationPreferencesAutoparkCompletedSuccess bool
	var bodyNotificationPreferencesAutoparkForwardStarted bool
	var bodyNotificationPreferencesAutoparkUnavailablePluggedIn bool
	var bodyNotificationPreferencesChargingComplete bool
	var bodyNotificationPreferencesChargingInterrupted bool
	var bodyNotificationPreferencesChargingStarted bool
	var bodyNotificationPreferencesGridOutage bool
	var bodyNotificationPreferencesUpdateAvailable bool
	var bodyPlatform string

	cmd := &cobra.Command{
		Use:         "notification-preferences",
		Short:       "Update push notification preferences",
		Long:        "Shortcut for 'notification_preferences create_notification_preferences'. Update push notification preferences",
		Example:     "  tesla-pp-cli notification-preferences --app-version example-value",
		Annotations: map[string]string{"pp:endpoint": "notification_preferences.create_notification_preferences", "pp:method": "POST", "pp:path": "/api/1/notification_preferences"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("app-version") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "app-version")
			}
			if !cmd.Flags().Changed("device-token") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "device-token")
			}
			if !cmd.Flags().Changed("device-type") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "device-type")
			}
			if !cmd.Flags().Changed("locale") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "locale")
			}
			if !cmd.Flags().Changed("notification-preferences-alarm") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "notification-preferences-alarm")
			}
			if !cmd.Flags().Changed("notification-preferences-autopark-completed-success") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "notification-preferences-autopark-completed-success")
			}
			if !cmd.Flags().Changed("notification-preferences-autopark-forward-started") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "notification-preferences-autopark-forward-started")
			}
			if !cmd.Flags().Changed("notification-preferences-autopark-unavailable-plugged-in") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "notification-preferences-autopark-unavailable-plugged-in")
			}
			if !cmd.Flags().Changed("notification-preferences-charging-complete") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "notification-preferences-charging-complete")
			}
			if !cmd.Flags().Changed("notification-preferences-charging-interrupted") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "notification-preferences-charging-interrupted")
			}
			if !cmd.Flags().Changed("notification-preferences-charging-started") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "notification-preferences-charging-started")
			}
			if !cmd.Flags().Changed("notification-preferences-grid-outage") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "notification-preferences-grid-outage")
			}
			if !cmd.Flags().Changed("notification-preferences-update-available") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "notification-preferences-update-available")
			}
			if !cmd.Flags().Changed("platform") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "platform")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/1/notification_preferences"
			params := map[string]string{}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyAppVersion != "" {
				body["app_version"] = bodyAppVersion
			}
			if bodyDeviceToken != "" {
				body["device_token"] = bodyDeviceToken
			}
			if bodyDeviceType != "" {
				body["device_type"] = bodyDeviceType
			}
			if bodyLocale != "" {
				body["locale"] = bodyLocale
			}
			{
				nestedNotificationPreferences := map[string]any{}
				if cmd.Flags().Changed("notification-preferences-alarm") {
					nestedNotificationPreferences["alarm"] = bodyNotificationPreferencesAlarm
				}
				if cmd.Flags().Changed("notification-preferences-autopark-completed-success") {
					nestedNotificationPreferences["autopark_completed_success"] = bodyNotificationPreferencesAutoparkCompletedSuccess
				}
				if cmd.Flags().Changed("notification-preferences-autopark-forward-started") {
					nestedNotificationPreferences["autopark_forward_started"] = bodyNotificationPreferencesAutoparkForwardStarted
				}
				if cmd.Flags().Changed("notification-preferences-autopark-unavailable-plugged-in") {
					nestedNotificationPreferences["autopark_unavailable_plugged_in"] = bodyNotificationPreferencesAutoparkUnavailablePluggedIn
				}
				if cmd.Flags().Changed("notification-preferences-charging-complete") {
					nestedNotificationPreferences["charging_complete"] = bodyNotificationPreferencesChargingComplete
				}
				if cmd.Flags().Changed("notification-preferences-charging-interrupted") {
					nestedNotificationPreferences["charging_interrupted"] = bodyNotificationPreferencesChargingInterrupted
				}
				if cmd.Flags().Changed("notification-preferences-charging-started") {
					nestedNotificationPreferences["charging_started"] = bodyNotificationPreferencesChargingStarted
				}
				if cmd.Flags().Changed("notification-preferences-grid-outage") {
					nestedNotificationPreferences["grid_outage"] = bodyNotificationPreferencesGridOutage
				}
				if cmd.Flags().Changed("notification-preferences-update-available") {
					nestedNotificationPreferences["update_available"] = bodyNotificationPreferencesUpdateAvailable
				}
				if len(nestedNotificationPreferences) > 0 {
					body["notification_preferences"] = nestedNotificationPreferences
				}
			}
			if bodyPlatform != "" {
				body["platform"] = bodyPlatform
			}
			data, _, err := c.PostWithParams(path, params, body)

			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAppVersion, "app-version", "", "")
	cmd.Flags().StringVar(&bodyDeviceToken, "device-token", "", "")
	cmd.Flags().StringVar(&bodyDeviceType, "device-type", "", "")
	cmd.Flags().StringVar(&bodyLocale, "locale", "", "")
	cmd.Flags().BoolVar(&bodyNotificationPreferencesAlarm, "notification-preferences-alarm", false, "")
	cmd.Flags().BoolVar(&bodyNotificationPreferencesAutoparkCompletedSuccess, "notification-preferences-autopark-completed-success", false, "")
	cmd.Flags().BoolVar(&bodyNotificationPreferencesAutoparkForwardStarted, "notification-preferences-autopark-forward-started", false, "")
	cmd.Flags().BoolVar(&bodyNotificationPreferencesAutoparkUnavailablePluggedIn, "notification-preferences-autopark-unavailable-plugged-in", false, "")
	cmd.Flags().BoolVar(&bodyNotificationPreferencesChargingComplete, "notification-preferences-charging-complete", false, "")
	cmd.Flags().BoolVar(&bodyNotificationPreferencesChargingInterrupted, "notification-preferences-charging-interrupted", false, "")
	cmd.Flags().BoolVar(&bodyNotificationPreferencesChargingStarted, "notification-preferences-charging-started", false, "")
	cmd.Flags().BoolVar(&bodyNotificationPreferencesGridOutage, "notification-preferences-grid-outage", false, "")
	cmd.Flags().BoolVar(&bodyNotificationPreferencesUpdateAvailable, "notification-preferences-update-available", false, "")
	cmd.Flags().StringVar(&bodyPlatform, "platform", "", "")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
