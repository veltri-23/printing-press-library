// Copyright 2026 Paul Bockewitz and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelSnapshotSaveHelpWires smoke-tests that the snapshot save command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelSnapshotSaveHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"snapshot", "save", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("snapshot save --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelSnapshotSaveBehavior is the placeholder for table-driven tests of
// the snapshot save command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelSnapshotSaveBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
//
// TestNovelSnapshotSaveBehavior verifies the no-argument invocation falls
// through to help (exit nil) without dialing the router — the heavy SSH capture
// is gated behind a required name argument.
func TestNovelSnapshotSaveBehavior(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"snapshot", "save"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("snapshot save (no name) should fall through to help, got %v", err)
	}
}
