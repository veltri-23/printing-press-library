// Copyright 2026 Paul Bockewitz and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelRpcCallHelpWires smoke-tests that the rpc call command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelRpcCallHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"rpc", "call", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("rpc call --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelRpcCallBehavior is the placeholder for table-driven tests of
// the rpc call command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelRpcCallBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
//
// TestNovelRpcCallBehavior verifies that fewer than two positional arguments
// falls through to help (exit nil) without dialing the router.
func TestNovelRpcCallBehavior(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"rpc", "call", "system"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("rpc call with one arg should fall through to help, got %v", err)
	}
}
