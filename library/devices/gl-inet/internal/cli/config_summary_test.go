// Copyright 2026 Paul Bockewitz and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelConfigSummaryHelpWires smoke-tests that the config summary command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelConfigSummaryHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"config", "summary", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("config summary --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelConfigSummaryBehavior is the placeholder for table-driven tests of
// the config summary command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelConfigSummaryBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
//
// TestNovelConfigSummaryBehavior verifies the --dry-run path returns cleanly
// (exit nil) without dialing the router.
func TestNovelConfigSummaryBehavior(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"config", "summary", "--dry-run"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("config summary --dry-run should exit nil, got %v", err)
	}
}
