// Copyright 2026 Paul Bockewitz and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelWanModeHelpWires smoke-tests that the wan mode command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelWanModeHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"wan", "mode", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("wan mode --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelWanModeBehavior is the placeholder for table-driven tests of
// the wan mode command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelWanModeBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
//
// TestNovelWanModeBehavior verifies an invalid mode is rejected as a usage
// error (exit code 2). The source->netmode mapping is covered by TestMapWanMode.
func TestNovelWanModeBehavior(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"wan", "mode", "bogus"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	err := cmd.Execute()
	if err == nil {
		t.Fatalf("wan mode bogus should error")
	}
	if got := ExitCode(err); got != 2 {
		t.Fatalf("wan mode bogus exit code = %d, want 2", got)
	}
}
