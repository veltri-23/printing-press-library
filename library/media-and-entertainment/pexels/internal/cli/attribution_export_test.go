// Copyright 2026 Vincent Colombo and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"bytes"
	"io"
	"strings"
	"testing"
)

// TestNovelAttributionExportHelpWires smoke-tests that the attribution export command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelAttributionExportHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"attribution", "export", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("attribution export --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelAttributionExportBehavior is the placeholder for table-driven tests of
// the attribution export command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelAttributionExportBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
func TestNovelAttributionExportBehavior(t *testing.T) {
	t.Run("dry-run", func(t *testing.T) {
		var out bytes.Buffer
		cmd := RootCmd()
		cmd.SetArgs([]string{"attribution", "export", "--dry-run"})
		cmd.SetOut(&out)
		cmd.SetErr(&out)
		if err := cmd.Execute(); err != nil {
			t.Fatalf("dry-run error: %v", err)
		}
		if !strings.Contains(out.String(), "would export") {
			t.Fatalf("output %q does not mention export", out.String())
		}
	})

	t.Run("live data source rejected", func(t *testing.T) {
		var out bytes.Buffer
		cmd := RootCmd()
		cmd.SetArgs([]string{"attribution", "export", "--data-source", "live"})
		cmd.SetOut(&out)
		cmd.SetErr(&out)
		if err := cmd.Execute(); err == nil {
			t.Fatal("expected error for --data-source live")
		}
	})
}
