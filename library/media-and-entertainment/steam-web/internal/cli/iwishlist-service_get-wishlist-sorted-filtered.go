// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIwishlistServiceGetWishlistSortedFilteredCmd(flags *rootFlags) *cobra.Command {
	var flagSteamid string
	var flagContext string
	var flagDataRequest string
	var flagSortOrder string
	var flagFilters string
	var flagStartIndex int
	var flagPageSize int
	var flagShareToken string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-wishlist-sorted-filtered",
		Short:       "GetWishlistSortedFiltered operation of IWishlistService",
		Example:     "  steam-web-pp-cli iwishlist-service get-wishlist-sorted-filtered --steamid 42 --context example-value --data-request example-value --filters example-value --share-token your-token-here",
		Annotations: map[string]string{"pp:endpoint": "iwishlist-service.get-wishlist-sorted-filtered", "pp:method": "GET", "pp:path": "/IWishlistService/GetWishlistSortedFiltered/v1", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("steamid") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "steamid")
			}
			if !cmd.Flags().Changed("context") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "context")
			}
			if !cmd.Flags().Changed("data-request") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "data-request")
			}
			if !cmd.Flags().Changed("filters") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "filters")
			}
			if !cmd.Flags().Changed("share-token") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "share-token")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/IWishlistService/GetWishlistSortedFiltered/v1"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "iwishlist-service", path, map[string]string{
				"steamid":      fmt.Sprintf("%v", flagSteamid),
				"context":      fmt.Sprintf("%v", flagContext),
				"data_request": fmt.Sprintf("%v", flagDataRequest),
				"sort_order":   fmt.Sprintf("%v", flagSortOrder),
				"filters":      fmt.Sprintf("%v", flagFilters),
				"start_index":  fmt.Sprintf("%v", flagStartIndex),
				"page_size":    fmt.Sprintf("%v", flagPageSize),
				"share_token":  fmt.Sprintf("%v", flagShareToken),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagSteamid, "steamid", "", "Steamid")
	cmd.Flags().StringVar(&flagContext, "context", "", "Context")
	cmd.Flags().StringVar(&flagDataRequest, "data-request", "", "If passed, item data will be returned")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "", "Sort order")
	cmd.Flags().StringVar(&flagFilters, "filters", "", "Filters")
	cmd.Flags().IntVar(&flagStartIndex, "start-index", 0, "Data in this range will be filled in with StoreBrowse data")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Page size")
	cmd.Flags().StringVar(&flagShareToken, "share-token", "", "The wishlist share token. Determines what items are visible and the effective filters.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
