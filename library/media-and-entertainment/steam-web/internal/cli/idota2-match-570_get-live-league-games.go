// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIdota2Match570GetLiveLeagueGamesCmd(flags *rootFlags) *cobra.Command {
	var flagLeagueId string
	var flagMatchId string
	var flagDpc bool

	cmd := &cobra.Command{
		Use:         "get-live-league-games",
		Aliases:     []string{"list"},
		Short:       "GetLiveLeagueGames operation of IDOTA2Match_570",
		Example:     "  steam-web-pp-cli idota2-match-570 get-live-league-games",
		Annotations: map[string]string{"pp:endpoint": "idota2-match-570.get-live-league-games", "pp:method": "GET", "pp:path": "/IDOTA2Match_570/GetLiveLeagueGames/v1", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/IDOTA2Match_570/GetLiveLeagueGames/v1"
			params := map[string]string{}
			if flagLeagueId != "" {
				params["league_id"] = fmt.Sprintf("%v", flagLeagueId)
			}
			if flagMatchId != "" {
				params["match_id"] = fmt.Sprintf("%v", flagMatchId)
			}
			if flagDpc != false {
				params["dpc"] = fmt.Sprintf("%v", flagDpc)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "idota2-match-570", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagLeagueId, "league-id", "", "Only show matches of the specified league id")
	cmd.Flags().StringVar(&flagMatchId, "match-id", "", "Only show matches of the specified match id")
	cmd.Flags().BoolVar(&flagDpc, "dpc", false, "Only show matches that are part of the DPC")

	return cmd
}
