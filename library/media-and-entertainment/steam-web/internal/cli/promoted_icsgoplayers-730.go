// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIcsgoplayers730PromotedCmd(flags *rootFlags) *cobra.Command {
	var flagSteamid string
	var flagSteamidkey string
	var flagKnowncode string

	cmd := &cobra.Command{
		Use:         "icsgoplayers-730",
		Short:       "GetNextMatchSharingCode operation of ICSGOPlayers_730",
		Long:        "Shortcut for 'icsgoplayers-730 get-next-match-sharing-code'. GetNextMatchSharingCode operation of ICSGOPlayers_730",
		Example:     "  steam-web-pp-cli icsgoplayers-730 --steamid 42 --steamidkey your-token-here --knowncode example-value",
		Annotations: map[string]string{"pp:endpoint": "icsgoplayers-730.get-next-match-sharing-code", "pp:method": "GET", "pp:path": "/ICSGOPlayers_730/GetNextMatchSharingCode/v1", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("steamid") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "steamid")
			}
			if !cmd.Flags().Changed("steamidkey") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "steamidkey")
			}
			if !cmd.Flags().Changed("knowncode") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "knowncode")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ICSGOPlayers_730/GetNextMatchSharingCode/v1"
			params := map[string]string{}
			if flagSteamid != "" {
				params["steamid"] = fmt.Sprintf("%v", flagSteamid)
			}
			if flagSteamidkey != "" {
				params["steamidkey"] = fmt.Sprintf("%v", flagSteamidkey)
			}
			if flagKnowncode != "" {
				params["knowncode"] = fmt.Sprintf("%v", flagKnowncode)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "icsgoplayers-730", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagSteamid, "steamid", "", "The SteamID of the user")
	cmd.Flags().StringVar(&flagSteamidkey, "steamidkey", "", "Authentication obtained from the SteamID")
	cmd.Flags().StringVar(&flagKnowncode, "knowncode", "", "Previously known match sharing code obtained from the SteamID")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
