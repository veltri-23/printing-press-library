// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIdota2Match570GetMatchHistoryCmd(flags *rootFlags) *cobra.Command {
	var flagHeroId string
	var flagGameMode int
	var flagSkill int
	var flagMinPlayers string
	var flagAccountId string
	var flagLeagueId string
	var flagStartAtMatchId string
	var flagMatchesRequested string

	cmd := &cobra.Command{
		Use:         "get-match-history",
		Short:       "GetMatchHistory operation of IDOTA2Match_570",
		Example:     "  steam-web-pp-cli idota2-match-570 get-match-history",
		Annotations: map[string]string{"pp:endpoint": "idota2-match-570.get-match-history", "pp:method": "GET", "pp:path": "/IDOTA2Match_570/GetMatchHistory/v1", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/IDOTA2Match_570/GetMatchHistory/v1"
			params := map[string]string{}
			if flagHeroId != "" {
				params["hero_id"] = fmt.Sprintf("%v", flagHeroId)
			}
			if flagGameMode != 0 {
				params["game_mode"] = fmt.Sprintf("%v", flagGameMode)
			}
			if flagSkill != 0 {
				params["skill"] = fmt.Sprintf("%v", flagSkill)
			}
			if flagMinPlayers != "" {
				params["min_players"] = fmt.Sprintf("%v", flagMinPlayers)
			}
			if flagAccountId != "" {
				params["account_id"] = fmt.Sprintf("%v", flagAccountId)
			}
			if flagLeagueId != "" {
				params["league_id"] = fmt.Sprintf("%v", flagLeagueId)
			}
			if flagStartAtMatchId != "" {
				params["start_at_match_id"] = fmt.Sprintf("%v", flagStartAtMatchId)
			}
			if flagMatchesRequested != "" {
				params["matches_requested"] = fmt.Sprintf("%v", flagMatchesRequested)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "idota2-match-570", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagHeroId, "hero-id", "", "The ID of the hero that must be in the matches being queried")
	cmd.Flags().IntVar(&flagGameMode, "game-mode", 0, "Which game mode to return matches for")
	cmd.Flags().IntVar(&flagSkill, "skill", 0, "The average skill range of the match, these can be [1-3] with lower numbers being lower skill. Ignored if an account...")
	cmd.Flags().StringVar(&flagMinPlayers, "min-players", "", "Minimum number of human players that must be in a match for it to be returned")
	cmd.Flags().StringVar(&flagAccountId, "account-id", "", "An account ID to get matches from. This will fail if the user has their match history hidden")
	cmd.Flags().StringVar(&flagLeagueId, "league-id", "", "The league ID to return games from")
	cmd.Flags().StringVar(&flagStartAtMatchId, "start-at-match-id", "", "The minimum match ID to start from")
	cmd.Flags().StringVar(&flagMatchesRequested, "matches-requested", "", "The number of requested matches to return (maximum 100)")

	return cmd
}
