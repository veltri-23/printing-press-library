// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newItfpromos620GetItemIdCmd(flags *rootFlags) *cobra.Command {
	var flagSteamid string
	var flagPromoID string

	cmd := &cobra.Command{
		Use:         "get-item-id",
		Aliases:     []string{"list"},
		Short:       "GetItemID operation of ITFPromos_620",
		Example:     "  steam-web-pp-cli itfpromos-620 get-item-id --steamid 42 --promo-id 42",
		Annotations: map[string]string{"pp:endpoint": "itfpromos-620.get-item-id", "pp:method": "GET", "pp:path": "/ITFPromos_620/GetItemID/v1", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("steamid") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "steamid")
			}
			if !cmd.Flags().Changed("promo-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "promo-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ITFPromos_620/GetItemID/v1"
			params := map[string]string{}
			if flagSteamid != "" {
				params["steamid"] = fmt.Sprintf("%v", flagSteamid)
			}
			if flagPromoID != "" {
				params["PromoID"] = fmt.Sprintf("%v", flagPromoID)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "itfpromos-620", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagSteamid, "steamid", "", "The Steam ID to fetch items for")
	cmd.Flags().StringVar(&flagPromoID, "promo-id", "", "The promo ID to grant an item for")

	return cmd
}
