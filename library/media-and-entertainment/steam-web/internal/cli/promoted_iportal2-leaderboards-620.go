// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIportal2Leaderboards620PromotedCmd(flags *rootFlags) *cobra.Command {
	var flagLeaderboardName string

	cmd := &cobra.Command{
		Use:         "iportal2-leaderboards-620",
		Short:       "GetBucketizedData operation of IPortal2Leaderboards_620",
		Long:        "Shortcut for 'iportal2-leaderboards-620 get-bucketized-data'. GetBucketizedData operation of IPortal2Leaderboards_620",
		Example:     "  steam-web-pp-cli iportal2-leaderboards-620 --leaderboard-name example-resource",
		Annotations: map[string]string{"pp:endpoint": "iportal2-leaderboards-620.get-bucketized-data", "pp:method": "GET", "pp:path": "/IPortal2Leaderboards_620/GetBucketizedData/v1", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("leaderboard-name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "leaderboard-name")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/IPortal2Leaderboards_620/GetBucketizedData/v1"
			params := map[string]string{}
			if flagLeaderboardName != "" {
				params["leaderboardName"] = fmt.Sprintf("%v", flagLeaderboardName)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "iportal2-leaderboards-620", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagLeaderboardName, "leaderboard-name", "", "The leaderboard name to fetch data for.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
