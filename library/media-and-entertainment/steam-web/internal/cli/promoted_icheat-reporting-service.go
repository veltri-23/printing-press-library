// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIcheatReportingServicePromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAppid int
	var bodyCheatDataDump string
	var bodyCheatParam1 int
	var bodyCheatParam2 int
	var bodyCheatProcessId int
	var bodyCheatname string
	var bodyGameProcessId int
	var bodyKey string
	var bodyPathandfilename string
	var bodySteamid int
	var bodyTimeNow int
	var bodyTimeStarted int
	var bodyTimeStopped int
	var bodyWebcheaturl string

	cmd := &cobra.Command{
		Use:         "icheat-reporting-service",
		Short:       "ReportCheatData operation of ICheatReportingService",
		Long:        "Shortcut for 'icheat-reporting-service report-cheat-data'. ReportCheatData operation of ICheatReportingService",
		Example:     "  steam-web-pp-cli icheat-reporting-service --cheat-data-dump example-value",
		Annotations: map[string]string{"pp:endpoint": "icheat-reporting-service.report-cheat-data", "pp:method": "POST", "pp:path": "/ICheatReportingService/ReportCheatData/v1"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("appid") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "appid")
			}
			if !cmd.Flags().Changed("cheat-data-dump") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "cheat-data-dump")
			}
			if !cmd.Flags().Changed("cheat-param-1") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "cheat-param-1")
			}
			if !cmd.Flags().Changed("cheat-param-2") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "cheat-param-2")
			}
			if !cmd.Flags().Changed("cheat-process-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "cheat-process-id")
			}
			if !cmd.Flags().Changed("cheatname") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "cheatname")
			}
			if !cmd.Flags().Changed("game-process-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "game-process-id")
			}
			if !cmd.Flags().Changed("key") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "key")
			}
			if !cmd.Flags().Changed("pathandfilename") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "pathandfilename")
			}
			if !cmd.Flags().Changed("steamid") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "steamid")
			}
			if !cmd.Flags().Changed("time-now") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "time-now")
			}
			if !cmd.Flags().Changed("time-started") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "time-started")
			}
			if !cmd.Flags().Changed("time-stopped") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "time-stopped")
			}
			if !cmd.Flags().Changed("webcheaturl") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "webcheaturl")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ICheatReportingService/ReportCheatData/v1"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyAppid != 0 {
				body["appid"] = bodyAppid
			}
			if bodyCheatDataDump != "" {
				body["cheat_data_dump"] = bodyCheatDataDump
			}
			if bodyCheatParam1 != 0 {
				body["cheat_param_1"] = bodyCheatParam1
			}
			if bodyCheatParam2 != 0 {
				body["cheat_param_2"] = bodyCheatParam2
			}
			if bodyCheatProcessId != 0 {
				body["cheat_process_id"] = bodyCheatProcessId
			}
			if bodyCheatname != "" {
				body["cheatname"] = bodyCheatname
			}
			if bodyGameProcessId != 0 {
				body["game_process_id"] = bodyGameProcessId
			}
			if bodyKey != "" {
				body["key"] = bodyKey
			}
			if bodyPathandfilename != "" {
				body["pathandfilename"] = bodyPathandfilename
			}
			if bodySteamid != 0 {
				body["steamid"] = bodySteamid
			}
			if bodyTimeNow != 0 {
				body["time_now"] = bodyTimeNow
			}
			if bodyTimeStarted != 0 {
				body["time_started"] = bodyTimeStarted
			}
			if bodyTimeStopped != 0 {
				body["time_stopped"] = bodyTimeStopped
			}
			if bodyWebcheaturl != "" {
				body["webcheaturl"] = bodyWebcheaturl
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyAppid, "appid", 0, "The appid.")
	cmd.Flags().StringVar(&bodyCheatDataDump, "cheat-data-dump", "", "data collection in json format")
	cmd.Flags().IntVar(&bodyCheatParam1, "cheat-param-1", 0, "cheat param 1")
	cmd.Flags().IntVar(&bodyCheatParam2, "cheat-param-2", 0, "cheat param 2")
	cmd.Flags().IntVar(&bodyCheatProcessId, "cheat-process-id", 0, "process ID of the cheat process that ran")
	cmd.Flags().StringVar(&bodyCheatname, "cheatname", "", "descriptive name for the cheat.")
	cmd.Flags().IntVar(&bodyGameProcessId, "game-process-id", 0, "process ID of the running game.")
	cmd.Flags().StringVar(&bodyKey, "key", "", "Access key")
	cmd.Flags().StringVar(&bodyPathandfilename, "pathandfilename", "", "path and file name of the cheat executable.")
	cmd.Flags().IntVar(&bodySteamid, "steamid", 0, "steamid of the user running and reporting the cheat.")
	cmd.Flags().IntVar(&bodyTimeNow, "time-now", 0, "local system time now.")
	cmd.Flags().IntVar(&bodyTimeStarted, "time-started", 0, "local system time when cheat process started. ( 0 if not yet run )")
	cmd.Flags().IntVar(&bodyTimeStopped, "time-stopped", 0, "local system time when cheat process stopped. ( 0 if still running )")
	cmd.Flags().StringVar(&bodyWebcheaturl, "webcheaturl", "", "web url where the cheat was found and downloaded.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
