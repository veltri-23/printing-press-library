// Copyright 2026 horknfbr and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/suno/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/suno/internal/client"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/suno/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/suno/internal/config"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/suno/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/suno/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("billing_eligible-discounts",
			mcplib.WithDescription("Show promotional discounts your account currently qualifies for. Returns the available offers and their terms; use before upgrading or to surface pricing promotions."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/billing/eligible-discounts", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("billing_info",
			mcplib.WithDescription("Show credits, plan, and models. Returns the BillingInfo."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/billing/info/", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("billing_usage-plan",
			mcplib.WithDescription("Show the side-by-side plan comparison (Basic/Pro/Premier): credits, features, and limits per tier. Use to compare what each subscription level includes."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/billing/usage-plan-web-table-comparison/", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("billing_usage-plan-faq",
			mcplib.WithDescription("Show the frequently-asked questions about plans, credits, and usage limits. Use to answer billing or quota questions without leaving the terminal."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/billing/usage-plan-faq/", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clips_attribution",
			mcplib.WithDescription("Show a clip's sample/attribution info. Required: clip_id. Returns the Clip."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/clips/{clip_id}/attribution", true, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "path"}}, []string{"clip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("clips_comments",
			mcplib.WithDescription("List comments on a clip. Required: clip_id. Returns array."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/gen/{clip_id}/comments", true, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "path"}}, []string{"clip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("clips_convert-wav",
			mcplib.WithDescription("Trigger WAV (lossless) conversion for a clip (Pro/Premier). Poll wav-url for the result. Required: clip_id."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/gen/{clip_id}/convert_wav/", false, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "path"}}, []string{"clip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("clips_delete",
			mcplib.WithDescription("Trash (or restore) one or more clips. Required: clip_ids. Optional: undo_trash."),
			mcplib.WithString("clip_ids", mcplib.Required(), mcplib.Description("Clip IDs to trash")),
			mcplib.WithBoolean("undo_trash", mcplib.Description("true restores")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/feed/trash", false, false, nil, []mcpParamBinding{{PublicName: "clip_ids", WireName: "clip_ids", Location: "body"}, {PublicName: "undo_trash", WireName: "undo_trash", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clips_direct-children-count",
			mcplib.WithDescription("Count direct children (extends/covers) of clip(s). Required: clip_ids."),
			mcplib.WithString("clip_ids", mcplib.Required(), mcplib.Description("comma-separated clip IDs")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/clips/direct_children_count", true, false, nil, []mcpParamBinding{{PublicName: "clip_ids", WireName: "clip_ids", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clips_get",
			mcplib.WithDescription("Fetch clip(s) by ID (comma-separated; Suno batches 2 at a time). Required: ids. Returns array of Clip."),
			mcplib.WithString("ids", mcplib.Required(), mcplib.Description("Clip ID(s), comma-separated")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/feed/", true, false, nil, []mcpParamBinding{{PublicName: "ids", WireName: "ids", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clips_list",
			mcplib.WithDescription("List your library (walks the opaque next_cursor; use --all to drain). Optional: cursor, limit (default: 20). Returns array of Clip."),
			mcplib.WithString("cursor", mcplib.Description("Opaque pagination cursor (the next_cursor from the prior page; omit for first page)")),
			mcplib.WithNumber("limit", mcplib.Description("Page size")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/feed/v3", true, false, nil, []mcpParamBinding{{PublicName: "cursor", WireName: "cursor", Location: "body"}, {PublicName: "limit", WireName: "limit", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clips_parent",
			mcplib.WithDescription("Show a clip's parent (the clip it was derived from). Required: clip_id. Returns the Clip."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/clips/parent", true, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clips_publish",
			mcplib.WithDescription("Toggle a clip public or private. Required: clip_id, is_public."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID")),
			mcplib.WithBoolean("is_public", mcplib.Required(), mcplib.Description("true = public, false = private")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/gen/{clip_id}/set_visibility/", false, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "path"}, {PublicName: "is_public", WireName: "is_public", Location: "body"}}, []string{"clip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("clips_set",
			mcplib.WithDescription("Update a clip's title, caption, or lyrics. Required: clip_id. Optional: title, caption, lyrics."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID")),
			mcplib.WithString("title", mcplib.Description("New title")),
			mcplib.WithString("caption", mcplib.Description("New caption")),
			mcplib.WithString("lyrics", mcplib.Description("New lyrics")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/gen/{clip_id}/set_metadata/", false, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "path"}, {PublicName: "title", WireName: "title", Location: "body"}, {PublicName: "caption", WireName: "caption", Location: "body"}, {PublicName: "lyrics", WireName: "lyrics", Location: "body"}}, []string{"clip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("clips_similar",
			mcplib.WithDescription("Find clips similar to a given clip. Required: clip_id. Returns array of Clip."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/clips/get_similar/", true, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clips_stems",
			mcplib.WithDescription("Separate a clip into stems (vocals + instruments). Required: clip_id. Returns the new Clip."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/edit/stems/{clip_id}", false, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "path"}}, []string{"clip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("clips_timed-lyrics",
			mcplib.WithDescription("Word-level timestamped lyrics for a clip. Required: clip_id. Returns the AlignedLyrics."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/gen/{clip_id}/aligned_lyrics/v2/", true, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "path"}}, []string{"clip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("clips_wav-url",
			mcplib.WithDescription("Get the WAV download URL for a clip (null until conversion finishes). Required: clip_id. Returns the WavFile."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/gen/{clip_id}/wav_file/", true, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "path"}}, []string{"clip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("generate_concat",
			mcplib.WithDescription("Finalize/concatenate an extended clip into a full song. Required: clip_id. Returns the new Clip."),
			mcplib.WithString("clip_id", mcplib.Required(), mcplib.Description("Clip ID to finalize")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/generate/concat/v2/", false, false, nil, []mcpParamBinding{{PublicName: "clip_id", WireName: "clip_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("generate_lyrics",
			mcplib.WithDescription("Submit a lyrics-generation job (returns an id to poll). Required: prompt."),
			mcplib.WithString("prompt", mcplib.Required(), mcplib.Description("Description of the lyrics to write")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/generate/lyrics/", false, false, nil, []mcpParamBinding{{PublicName: "prompt", WireName: "prompt", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("generate_lyrics-status",
			mcplib.WithDescription("Poll a lyrics-generation job by id. Required: id. Returns the LyricsResult."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Lyrics job ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/generate/lyrics/{id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("generate_video-status",
			mcplib.WithDescription("Check video-generation status for a clip. Required: id."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Clip/video ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/video/generate/{id}/status/", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("notification_badge-count",
			mcplib.WithDescription("Show the count of unread notifications (the number on the bell badge). Use for a quick check of pending activity without fetching the full list."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/notification/v2/badge-count", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("notification_list",
			mcplib.WithDescription("List your account notifications (likes, comments, follows, system messages), paginated. Use to review recent activity on your tracks and account. Optional: page."),
			mcplib.WithNumber("page", mcplib.Description("Page number")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/notification/v2", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("persona_get",
			mcplib.WithDescription("View a voice persona. Required: persona_id. Optional: page (default: 0). Returns the Persona."),
			mcplib.WithString("persona_id", mcplib.Required(), mcplib.Description("Persona ID")),
			mcplib.WithNumber("page", mcplib.Description("Page of persona clips")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/persona/get-persona-paginated/{persona_id}/", true, false, nil, []mcpParamBinding{{PublicName: "persona_id", WireName: "persona_id", Location: "path"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{"persona_id"}),
	)
	s.AddTool(
		mcplib.NewTool("playlist_list",
			mcplib.WithDescription("List the playlists you've created or saved, paginated. Returns each playlist's id, name, and clip count; use to browse or pick a playlist to inspect. Optional: page (default: 1)."),
			mcplib.WithNumber("page", mcplib.Description("Page number")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/playlist/me", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("project_create",
			mcplib.WithDescription("Create a new workspace. Required: name. Optional: description. Returns the new Project."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Workspace name (max 100 chars)")),
			mcplib.WithString("description", mcplib.Description("Workspace description")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/project", false, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("project_default",
			mcplib.WithDescription("Show your default workspace (project) — the collection new tracks land in by default. Returns the project and its metadata; pair with 'project pinned-clips' for its pinned tracks."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/project/default", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("project_get",
			mcplib.WithDescription("Show a workspace and its clips. Required: project_id. Returns the Project."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("Workspace ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/project/{project_id}", true, false, nil, []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("project_list",
			mcplib.WithDescription("List your workspaces. Optional: page (default: 1), sort (default: created_at), show_trashed (plus 1 more). Returns array of Project."),
			mcplib.WithNumber("page", mcplib.Description("Page number")),
			mcplib.WithString("sort", mcplib.Description("Sort field")),
			mcplib.WithBoolean("show_trashed", mcplib.Description("Include trashed workspaces")),
			mcplib.WithString("query", mcplib.Description("Search workspaces by name")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/project/me", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "show_trashed", WireName: "show_trashed", Location: "query"}, {PublicName: "query", WireName: "query", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("project_pinned-clips",
			mcplib.WithDescription("List clips pinned to your default workspace. Returns array of Clip."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/project/default/pinned-clips", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("project_rename",
			mcplib.WithDescription("Rename a workspace. Required: project_id, name. Optional: description."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("Workspace ID")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("New workspace name (max 100 chars)")),
			mcplib.WithString("description", mcplib.Description("New description")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/project/{project_id}/metadata", false, false, nil, []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("project_trash",
			mcplib.WithDescription("Trash (or restore) a workspace. Required: project_id. Optional: undo_trash."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("Workspace ID")),
			mcplib.WithBoolean("undo_trash", mcplib.Description("true restores instead of trashing")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/project/trash", false, false, nil, []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "body"}, {PublicName: "undo_trash", WireName: "undo_trash", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("trending_list",
			mcplib.WithDescription("Show the clips currently trending on Suno. Returns a ranked list of popular public tracks; use for discovery or to sample what's resonating right now."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/trending/", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("user_config",
			mcplib.WithDescription("Show your account configuration and feature flags. Returns server-side user settings (tier, toggles, preferences); use to inspect how your account is provisioned."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/user/user_config/", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("user_personalization",
			mcplib.WithDescription("Show your personalization settings — the preferences Suno uses to tailor recommendations and generation defaults. Use to review what's been opted into."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/personalization/settings", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("user_personalization-memory",
			mcplib.WithDescription("Show the personalization memory Suno has stored about your tastes and history. Use to inspect the signals driving your recommendations."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/personalization/memory", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export SUNO_TOKEN=<your-key>" +
					"\n      Run 'suno-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export SUNO_TOKEN=<your-key>" +
					"\n      Run 'suno-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export SUNO_TOKEN=<your-key>" +
					"\n      Run 'suno-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "suno-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "suno-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "suno",
		"description": "Every Suno v5.",
		"archetype":   "project-management",
		"tool_count":  37,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion suno-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "SUNO_TOKEN",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
				{
					"name":        "SUNO_JWT",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "billing",
				"description": "Account credits, plan, and available models",
				"endpoints":   []string{"eligible-discounts", "info", "usage-plan", "usage-plan-faq"},
			},
			{
				"name":        "clips",
				"description": "Your Suno songs (clips) — generate, list, fetch, manage",
				"endpoints":   []string{"attribution", "comments", "convert-wav", "delete", "direct-children-count", "get", "list", "parent", "publish", "set", "similar", "stems", "timed-lyrics", "wav-url"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "generate",
				"description": "Music, lyrics, and video generation jobs",
				"endpoints":   []string{"concat", "lyrics", "lyrics-status", "video-status"},
				"searchable":  true,
			},
			{
				"name":        "notification",
				"description": "Your notifications",
				"endpoints":   []string{"badge-count", "list"},
				"syncable":    true,
			},
			{
				"name":        "persona",
				"description": "Voice personas",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
			{
				"name":        "playlist",
				"description": "Your playlists",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "project",
				"description": "Workspaces (Suno 'projects') — organize tracks into collections",
				"endpoints":   []string{"create", "default", "get", "list", "pinned-clips", "rename", "trash"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "trending",
				"description": "Trending clips",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "user",
				"description": "Your account, config, and personalization",
				"endpoints":   []string{"config", "personalization", "personalization-memory"},
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass page parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Lyric-line grep", "command": "grep", "description": "Find clips by remembered lyric, prompt, or tag phrases via local full-text search.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Library analytics", "command": "analytics", "description": "Grouped roll-ups across your whole library: counts, average duration/bpm, total plays and upvotes by any clip field.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Variation lineage", "command": "lineage", "description": "Render the iteration tree of a track: its extends, covers, and remixes, reconstructed from local links.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Top tracks", "command": "top", "description": "Ranked flat list of your best clips by plays, upvotes, or duration, with machine-readable output.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Raw SQL", "command": "sql", "description": "Run read-only SQL directly against your local synced clip store.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Credit throttle report", "command": "credits --forecast", "description": "Remaining credits plus your recent generation volume measured against Suno's captcha-throttle threshold.", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Lyric-line grep", "insight": ""},
			{"topic": "Library analytics", "insight": ""},
			{"topic": "Variation lineage", "insight": ""},
			{"topic": "Top tracks", "insight": ""},
			{"topic": "Raw SQL", "insight": ""},
			{"topic": "Credit throttle report", "insight": ""},
			{"topic": "Finding stale work", "insight": "Use the stale command or sql query to find items not updated recently. More reliable than scanning list results manually."},
			{"topic": "Load analysis", "insight": "When analyzing team workload, filter by assignee and status. Raw counts without status filtering are misleading."},
			{"topic": "Bulk operations", "insight": "For bulk status changes, prefer update endpoints over delete+create. Most PM APIs track history on updates."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
