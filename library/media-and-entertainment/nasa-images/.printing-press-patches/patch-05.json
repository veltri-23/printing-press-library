{
  "schema_version": 2,
  "applied_at": "2026-05-19",
  "base_run_id": "20260519-001814",
  "base_printing_press_version": "4.9.0",
  "file": "internal/cli/channel_workflow.go (additional)",
  "rationale": "Workflow archive --json must emit a single JSON document on stdout. Sync's NDJSON event stream was writing to os.Stdout directly, polluting the document. Swap os.Stdout to /dev/null for the sync loop; restore before the summary JSON encode.",
  "evidence": "live dogfood json_fidelity failure: combined output had 3 sync_* events on stdout preceding the summary JSON."
}
