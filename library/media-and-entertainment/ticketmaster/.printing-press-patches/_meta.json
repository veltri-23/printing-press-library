{
  "schema_version": 2,
  "upstream_tracking": [
    {
      "id": "press-spec-server-url",
      "summary": "apis.guru's Discovery v2 spec ships a protocol-relative server URL (//www.ticketmaster.com/discovery/v2) and no security scheme. This run patches both in-place (spec edit before generation). The generator could optionally accept overrides via --base-url / --auth-scheme flags instead of forcing a spec edit, or treat protocol-relative server URLs as a soft warning rather than rejecting them.",
      "scope": "potential generator-side polish"
    },
    {
      "id": "press-novel-command-stubs",
      "summary": "When research.json declares novel_features, the generator emits a Highlights section in --help but does not stub the corresponding Cobra commands. A future generator could emit `// PATCH-stub:` placeholders that fall through to `cmd.Help()` so a clean build always exits 0 even before hand-authored implementations land.",
      "scope": "generator-side ergonomics"
    },
    {
      "id": "press-ratelimit-toctou",
      "summary": "AdaptiveLimiter.Wait() in internal/cliutil/ratelimit.go has two stacked issues that the generator's template ships unconditionally: (1) the original code reads lastRequest under a lock, releases, sleeps, then re-acquires the lock to update — two critical sections instead of one. (2) Even with the lock held across read+update, setting lastRequest=time.Now() *before* sleeping lets two callers 1ms apart both compute a wake-time at the same wall-clock instant and burst simultaneously. Greptile flagged the first as P2 on PR #444 (ratelimit.go:43-57) and the second as a P1 follow-up (ratelimit.go:68). The correct shape is monotone next-slot: under the lock, advance lastRequest by `delay` (or to now+delay during a quiet period), then sleep until that committed slot. The template needs to ship the monotone shape, not the snapshot shape.",
      "scope": "generator template fix (recurs in every printed CLI)"
    },
    {
      "id": "press-ratelimit-unbounded-ramp",
      "summary": "AdaptiveLimiter.OnSuccess() multiplies the rate by 1.25 every rampAfter successes with no hard upper bound until ceiling is set (which only happens on the first 429). On a long quiet run that never hits 429, the rate ramps to ~16 req/s in ~100 successes regardless of the upstream's documented budget. Greptile flagged on PR #444 (P2). Fixed locally with a `MaxRate = 10.0` constant clamp inside OnSuccess. Defense-in-depth against runaway ramp; the per-API ceiling discovery still drives normal adaptation. Generator template should ship the same constant or accept a per-spec override.",
      "scope": "generator template fix (recurs in every printed CLI)"
    },
    {
      "id": "press-config-auth-header-dead-branch",
      "summary": "Config.AuthHeader() in internal/config/config.go has a duplicate empty-token guard — line 87 checks `if token == \"\"` (returning), and line 90 then re-checks the same field on the original Config. The second check is unreachable. Greptile flagged on PR #444 (P2). Fixed locally by removing the duplicate branch. The generator's auth-template emits this duplicate; should be removed upstream.",
      "scope": "generator template fix (recurs in every printed CLI with api_key auth)"
    },
    {
      "id": "press-config-applyAuthFormat-dead-code",
      "summary": "applyAuthFormat() helper and its `var _ = strings.ReplaceAll` keep-import sentinel are emitted by the generator's auth-template but never wired into the call graph of the printed CLI for query-param auth modes. Greptile flagged on PR #444 (P2). Removed locally along with the now-unused `strings` import. The generator should emit this helper only when an api_key uses Authorization-header format substitution, not unconditionally.",
      "scope": "generator template fix (recurs in every printed CLI)"
    },
    {
      "id": "press-upsert-images-bind-order",
      "summary": "CRITICAL: generator-emitted upsertImagesTx in store.go binds positional `?` arguments out of order with the INSERT column list. Schema is `(id, events_id, data, synced_at)`, args were `(id, string(data), time.Now(), lookupFieldValue(obj, \"events_id\"))`, so the JSON blob ends up in events_id, the timestamp string in data, and events_id in synced_at. Greptile flagged on PR #444 (P0). This bug ships in every printed CLI whose spec has an Image resource (or any resource with a foreign-key column whose generator templating put the FK extraction last). The fix is mechanical: emit bind args in the same order as the column list, then add a column-content assertion to the typed-table dispatch test so the regression is impossible to ship again. Look for any other resource-typed upserts where the generator templated a `lookupFieldValue` extraction call after `string(data)` and `time.Now()` — they're likely affected by the same template bug.",
      "scope": "generator template fix (CRITICAL — every printed CLI with a typed-table resource that has at least one non-id column)"
    }
  ]
}
