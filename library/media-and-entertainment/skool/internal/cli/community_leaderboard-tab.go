// Copyright 2026 Zain Haseeb and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCommunityLeaderboardTabCmd(flags *rootFlags) *cobra.Command {
	var flagCommunity string
	var flagG string
	var flagT string

	cmd := &cobra.Command{
		Use:         "leaderboard-tab",
		Short:       "Leaderboard tab (community page rendered with t=leaderboard)",
		Example:     "  skool-pp-cli community leaderboard-tab --community example-value",
		Annotations: map[string]string{"pp:endpoint": "community.leaderboard-tab", "pp:method": "GET", "pp:path": "/_next/data/{buildId}/{community}.json", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("community") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "community")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/_next/data/{buildId}/{community}.json"
			path = replacePathParam(path, "community", fmt.Sprintf("%v", flagCommunity))
			params := map[string]string{}
			if flagG != "" {
				params["g"] = fmt.Sprintf("%v", flagG)
			}
			if flagT != "" {
				params["t"] = fmt.Sprintf("%v", flagT)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "community", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagCommunity, "community", "", "")
	cmd.Flags().StringVar(&flagG, "g", "", "")
	cmd.Flags().StringVar(&flagT, "t", "leaderboard", "")

	return cmd
}
