// Copyright 2026 Dave Morin and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newV10ResourceSearchSetlistsGetSetlistsGetCmd(flags *rootFlags) *cobra.Command {
	var flagArtistMbid string
	var flagArtistName string
	var flagArtistTmid string
	var flagCityId string
	var flagCityName string
	var flagCountryCode string
	var flagDate string
	var flagLastFm string
	var flagLastUpdated string
	var flagP string
	var flagState string
	var flagStateCode string
	var flagTourName string
	var flagVenueId string
	var flagVenueName string
	var flagYear string

	cmd := &cobra.Command{
		Use:         "resource-search-setlists-get-setlists-get",
		Short:       "Search for setlists.",
		Example:     "  setlist-fm-pp-cli 1-0 resource-search-setlists-get-setlists-get",
		Annotations: map[string]string{"pp:endpoint": "1-0.resource-search-setlists-get-setlists-get", "pp:method": "GET", "pp:path": "/1.0/search/setlists", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/1.0/search/setlists"
			params := map[string]string{}
			if flagArtistMbid != "" {
				params["artistMbid"] = fmt.Sprintf("%v", flagArtistMbid)
			}
			if flagArtistName != "" {
				params["artistName"] = fmt.Sprintf("%v", flagArtistName)
			}
			if flagArtistTmid != "" {
				params["artistTmid"] = fmt.Sprintf("%v", flagArtistTmid)
			}
			if flagCityId != "" {
				params["cityId"] = fmt.Sprintf("%v", flagCityId)
			}
			if flagCityName != "" {
				params["cityName"] = fmt.Sprintf("%v", flagCityName)
			}
			if flagCountryCode != "" {
				params["countryCode"] = fmt.Sprintf("%v", flagCountryCode)
			}
			if flagDate != "" {
				params["date"] = fmt.Sprintf("%v", flagDate)
			}
			if flagLastFm != "" {
				params["lastFm"] = fmt.Sprintf("%v", flagLastFm)
			}
			if flagLastUpdated != "" {
				params["lastUpdated"] = fmt.Sprintf("%v", flagLastUpdated)
			}
			if flagP != "" {
				params["p"] = fmt.Sprintf("%v", flagP)
			}
			if flagState != "" {
				params["state"] = fmt.Sprintf("%v", flagState)
			}
			if flagStateCode != "" {
				params["stateCode"] = fmt.Sprintf("%v", flagStateCode)
			}
			if flagTourName != "" {
				params["tourName"] = fmt.Sprintf("%v", flagTourName)
			}
			if flagVenueId != "" {
				params["venueId"] = fmt.Sprintf("%v", flagVenueId)
			}
			if flagVenueName != "" {
				params["venueName"] = fmt.Sprintf("%v", flagVenueName)
			}
			if flagYear != "" {
				params["year"] = fmt.Sprintf("%v", flagYear)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "1-0", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagArtistMbid, "artist-mbid", "", "the artist's Musicbrainz Identifier (mbid)")
	cmd.Flags().StringVar(&flagArtistName, "artist-name", "", "the artist's name")
	cmd.Flags().StringVar(&flagArtistTmid, "artist-tmid", "", "the artist's Ticketmaster Identifier (tmid) (deprecated)")
	cmd.Flags().StringVar(&flagCityId, "city-id", "", "the city's geoId")
	cmd.Flags().StringVar(&flagCityName, "city-name", "", "the name of the city")
	cmd.Flags().StringVar(&flagCountryCode, "country-code", "", "the country code")
	cmd.Flags().StringVar(&flagDate, "date", "", "the date of the event (format dd-MM-yyyy)")
	cmd.Flags().StringVar(&flagLastFm, "last-fm", "", "the event's Last.fm Event ID (deprecated)")
	cmd.Flags().StringVar(&flagLastUpdated, "last-updated", "", "the date and time (UTC) when this setlist was last updated (format yyyyMMddHHmmss) - either edited or reverted....")
	cmd.Flags().StringVar(&flagP, "p", "", "the number of the result page")
	cmd.Flags().StringVar(&flagState, "state", "", "the state")
	cmd.Flags().StringVar(&flagStateCode, "state-code", "", "the state code")
	cmd.Flags().StringVar(&flagTourName, "tour-name", "", "Tour name")
	cmd.Flags().StringVar(&flagVenueId, "venue-id", "", "the venue id")
	cmd.Flags().StringVar(&flagVenueName, "venue-name", "", "the name of the venue")
	cmd.Flags().StringVar(&flagYear, "year", "", "the year of the event")

	return cmd
}
