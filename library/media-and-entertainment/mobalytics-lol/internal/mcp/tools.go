// Copyright 2026 QuantumGlitch and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/mobalytics-lol/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/mobalytics-lol/internal/client"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/mobalytics-lol/internal/config"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/mobalytics-lol/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/mobalytics-lol/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("cdn_data_get-champion",
			mcplib.WithDescription("Returns the full champion record including abilities (passive + Q/W/E/R with descriptions and tooltips), skins, lore, allytips, enemytips, stats, and image data. The championId is the canonical Riot id (PascalCase, no spaces or apostrophes — e.g., `Aatrox`, `KaiSa`, `MonkeyKing` for Wukong). Required: version (default: latest), locale (default: en_US), championId."),
			mcplib.WithString("version", mcplib.Required(), mcplib.Description("Data Dragon version (e.g., `16.10.1`). Use `listVersions` to get the newest version, or pass...")),
			mcplib.WithString("locale", mcplib.Required(), mcplib.Description("Data locale (`en_US`, `ko_KR`, `ja_JP`, `zh_CN`, etc.). Defaults to `en_US`.")),
			mcplib.WithString("championId", mcplib.Required(), mcplib.Description("Riot champion id in PascalCase (`Aatrox`, `KaiSa`, `MonkeyKing`).")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/cdn/{version}/data/{locale}/champion/{championId}.json", true, false, nil, []mcpParamBinding{{PublicName: "version", WireName: "version", Location: "path"}, {PublicName: "locale", WireName: "locale", Location: "path"}, {PublicName: "championId", WireName: "championId", Location: "path"}}, []string{"version", "locale", "championId"}),
	)
	s.AddTool(
		mcplib.NewTool("cdn_data_list-champions",
			mcplib.WithDescription("Returns champion summary records keyed by champion id. Includes name, title, blurb, tags (roles), info (attack/defense/magic/difficulty), partype (mana/energy/none), and image references. Required: version (default: latest), locale (default: en_US)."),
			mcplib.WithString("version", mcplib.Required(), mcplib.Description("Data Dragon version (e.g., `16.10.1`). Use `listVersions` to get the newest version, or pass...")),
			mcplib.WithString("locale", mcplib.Required(), mcplib.Description("Data locale (`en_US`, `ko_KR`, `ja_JP`, `zh_CN`, etc.). Defaults to `en_US`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/cdn/{version}/data/{locale}/champion.json", true, false, nil, []mcpParamBinding{{PublicName: "version", WireName: "version", Location: "path"}, {PublicName: "locale", WireName: "locale", Location: "path"}}, []string{"version", "locale"}),
	)
	s.AddTool(
		mcplib.NewTool("cdn_data_list-items",
			mcplib.WithDescription("Returns the full item catalog including stats, gold cost, build paths (`from` and `into`), tags, descriptions, and image references. Required: version (default: latest), locale (default: en_US)."),
			mcplib.WithString("version", mcplib.Required(), mcplib.Description("Data Dragon version (e.g., `16.10.1`). Use `listVersions` to get the newest version, or pass...")),
			mcplib.WithString("locale", mcplib.Required(), mcplib.Description("Data locale (`en_US`, `ko_KR`, `ja_JP`, `zh_CN`, etc.). Defaults to `en_US`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/cdn/{version}/data/{locale}/item.json", true, false, nil, []mcpParamBinding{{PublicName: "version", WireName: "version", Location: "path"}, {PublicName: "locale", WireName: "locale", Location: "path"}}, []string{"version", "locale"}),
	)
	s.AddTool(
		mcplib.NewTool("cdn_data_list-runes",
			mcplib.WithDescription("Returns the 5 rune trees (Precision, Domination, Sorcery, Resolve, Inspiration) and each tree's keystone, slot 1/2/3, and shards. Each rune has id, name, icon, shortDesc, longDesc. Required: version (default: latest), locale (default: en_US)."),
			mcplib.WithString("version", mcplib.Required(), mcplib.Description("Data Dragon version (e.g., `16.10.1`). Use `listVersions` to get the newest version, or pass...")),
			mcplib.WithString("locale", mcplib.Required(), mcplib.Description("Data locale (`en_US`, `ko_KR`, `ja_JP`, `zh_CN`, etc.). Defaults to `en_US`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/cdn/{version}/data/{locale}/runesReforged.json", true, false, nil, []mcpParamBinding{{PublicName: "version", WireName: "version", Location: "path"}, {PublicName: "locale", WireName: "locale", Location: "path"}}, []string{"version", "locale"}),
	)
	s.AddTool(
		mcplib.NewTool("cdn_data_list-summoner-spells",
			mcplib.WithDescription("Returns the summoner spell catalog (Flash, Ignite, Teleport, etc.) keyed by spell id, including cooldown, range, modes (CLASSIC, ARAM), description, and image data. Required: version (default: latest), locale (default: en_US)."),
			mcplib.WithString("version", mcplib.Required(), mcplib.Description("Data Dragon version (e.g., `16.10.1`). Use `listVersions` to get the newest version, or pass...")),
			mcplib.WithString("locale", mcplib.Required(), mcplib.Description("Data locale (`en_US`, `ko_KR`, `ja_JP`, `zh_CN`, etc.). Defaults to `en_US`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/cdn/{version}/data/{locale}/summoner.json", true, false, nil, []mcpParamBinding{{PublicName: "version", WireName: "version", Location: "path"}, {PublicName: "locale", WireName: "locale", Location: "path"}}, []string{"version", "locale"}),
	)
	s.AddTool(
		mcplib.NewTool("versions-json_list-versions",
			mcplib.WithDescription("Returns the full list of LoL patch versions known to Data Dragon, newest first. The first element is the current patch."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/versions.json", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      See API docs: https://mobalytics.gg/lol" +
					"\n      Run 'mobalytics-lol-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      See API docs: https://mobalytics.gg/lol" +
					"\n      Run 'mobalytics-lol-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "mobalytics-lol-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 2)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "mobalytics-lol-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "mobalytics-lol",
		"description": "Every League of Legends aggregator, plus a local SQLite for cross-champion queries no single page surfaces.",
		"archetype":   "generic",
		"tool_count":  6,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion mobalytics-lol-pp-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "cdn",
				"description": "Manage cdn",
				"endpoints":   []string{},
			},
			{
				"name":        "versions-json",
				"description": "Manage versions json",
				"endpoints":   []string{"list-versions"},
				"syncable":    true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Counter-pool analysis", "command": "counter-pool", "description": "Given our champion pool and the enemy pool", "rationale": "Mobalytics, u.gg, and op.gg all show counters one champion at a time.", "via": "mcp-command-mirror"},
			{"name": "Patch meta-shift", "command": "meta-shift", "description": "Diff two tier_snapshots and list champions that gained or lost ≥1 tier or ≥2% WR since a prior patch", "rationale": "Every aggregator shows the current patch's tier list. None of them show what moved.", "via": "mcp-command-mirror"},
			{"name": "Head-to-head champion compare", "command": "compare", "description": "Side-by-side join across tier, build, counters, and matchups for two champions — including item-overlap percentage.", "rationale": "No aggregator renders two champions on one page. The query joins three local tables for the chosen patch/role/rank.", "via": "mcp-command-mirror"},
			{"name": "ARAM batch item-set export", "command": "item-set", "description": "Write LoL client item-set JSON for N champions in ARAM mode in one command", "rationale": "Championify did this for Summoner's Rift in 2019 and died; no maintained successor exists for ARAM.", "via": "mcp-command-mirror"},
			{"name": "Duo-finder with candidate-pool filter", "command": "duo-finder", "description": "Best support pairings for a given ADC, restricted to a candidate support pool — coaches' player pools are fixed.", "rationale": "u.gg has a duo-finder but it ranks across all supports; coaches need 'best from these three I can actually run.'", "via": "mcp-command-mirror"},
			{"name": "Personal pool tier digest", "command": "pool-digest", "description": "For each champion in your pool: current tier, WR delta since last patch, top-1 counter", "rationale": "Daily-check-in commands collapse multi-tab morning rituals into one read.", "via": "mcp-command-mirror"},
			{"name": "Power-spike phase filter", "command": "power-spike", "description": "List champions ranked by early/mid/late spike strength for a chosen role — inverts Mobalytics's per-champion data into", "rationale": "Mobalytics shows power-spike phase on each champion page but never aggregates 'who spikes early in jungle.", "via": "mcp-command-mirror"},
			{"name": "Flex-pick detector", "command": "flex", "description": "Champions that are ≥A-tier in 2+ roles for the same patch and rank — the SQL no aggregator surfaces because they index", "rationale": "Aggregators all start with role → champion list.", "via": "mcp-command-mirror"},
			{"name": "Region-split tier compare", "command": "tier-list", "description": "Same patch, same rank — three regions side-by-side.", "rationale": "Every aggregator scopes a tier-list view to one region per page-load.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Counter-pool analysis", "insight": "Mobalytics, u.gg, and op.gg all show counters one champion at a time. The cross-product of two pools is a single SQL join over the local champion_matchups table; no aggregator page exposes it."},
			{"topic": "Patch meta-shift", "insight": "Every aggregator shows the current patch's tier list. None of them show what moved. The diff is trivial against a local store that synced two snapshots."},
			{"topic": "Head-to-head champion compare", "insight": "No aggregator renders two champions on one page. The query joins three local tables for the chosen patch/role/rank."},
			{"topic": "ARAM batch item-set export", "insight": "Championify did this for Summoner's Rift in 2019 and died; no maintained successor exists for ARAM. The build data is already in the local store after sync."},
			{"topic": "Duo-finder with candidate-pool filter", "insight": "u.gg has a duo-finder but it ranks across all supports; coaches need 'best from these three I can actually run.'"},
			{"topic": "Personal pool tier digest", "insight": "Daily-check-in commands collapse multi-tab morning rituals into one read. Composite query over four local tables, no equivalent on any aggregator."},
			{"topic": "Power-spike phase filter", "insight": "Mobalytics shows power-spike phase on each champion page but never aggregates 'who spikes early in jungle.' Inverting it requires the data in a local store."},
			{"topic": "Flex-pick detector", "insight": "Aggregators all start with role → champion list. The inverse (champion → list of roles where they are strong) requires a self-join over tier_snapshots."},
			{"topic": "Region-split tier compare", "insight": "Every aggregator scopes a tier-list view to one region per page-load. The multi-region pivot is one SQL with the regions as columns."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
