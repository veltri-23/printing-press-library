version: 2
project_name: substack-reader-pp-cli
changelog:
  disable: true
builds:
  - id: substack-reader-pp-cli
    main: ./cmd/substack-reader-pp-cli
    binary: substack-reader-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/media-and-entertainment/substack-reader/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: substack-reader-pp-mcp
    main: ./cmd/substack-reader-pp-mcp
    binary: substack-reader-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: substack-reader-pp-cli
    repository:
      owner: maxswinguy
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Read any Substack publication as a local, full-text-searchable corpus — keyless for free posts, your own session for what you subscribe to."
    install: |
      bin.install "substack-reader-pp-cli"
      bin.install "substack-reader-pp-mcp"
