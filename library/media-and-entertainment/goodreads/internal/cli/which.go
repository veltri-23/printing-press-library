// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "amazon-purchases list", Description: "Amazon purchases import/inspection page.", Group: "amazon-purchases"},
	{Command: "book get", Description: "Book detail page. Current page is Next.js-backed.", Group: "book"},
	{Command: "comment get", Description: "User comments/recent posts page.", Group: "comment"},
	{Command: "friend list", Description: "Friends index page.", Group: "friend"},
	{Command: "friend list-requests", Description: "Friend requests page.", Group: "friend"},
	{Command: "genres get", Description: "Genre landing page.", Group: "genres"},
	{Command: "genres list", Description: "Genre index page.", Group: "genres"},
	{Command: "genres list-list", Description: "Alphabetical genre shelves index.", Group: "genres"},
	{Command: "genres list-search", Description: "Genre finder route.", Group: "genres"},
	{Command: "goodreads-web-undocumented-search list", Description: "Canonical book search route from OpenSearch descriptor.", Group: "goodreads-web-undocumented-search"},
	{Command: "list get", Description: "Public Listopia list page.", Group: "list"},
	{Command: "message create", Description: "Batch message folder/read action.", Group: "message"},
	{Command: "message get", Description: "User message folder page.", Group: "message"},
	{Command: "message get-show", Description: "Message detail page.", Group: "message"},
	{Command: "message list", Description: "User message inbox.", Group: "message"},
	{Command: "message list-markallasread", Description: "Mark all visible inbox messages read.", Group: "message"},
	{Command: "notes get", Description: "User Kindle Notes & Highlights index.", Group: "notes"},
	{Command: "notes get-bookslug", Description: "Notes/highlights detail page for one book and user.", Group: "notes"},
	{Command: "notes load-more get", Description: "Paginated notes/highlights index loader.", Group: "notes"},
	{Command: "notes note create", Description: "Discovered from `.js-readingNote[data-note-persist-endpoint]` DOM attributes. Not submitted in this run.\nMethod and payload need a dedicated approved write capture before CLI execution support.", Group: "notes"},
	{Command: "notes share update", Description: "Observed through the \"Make all N visible\" button. This is a real account mutation.\nUse dry-run by default and verify on reload by counting `.js-readingNote[data-visible=true]`.", Group: "notes"},
	{Command: "notifications create", Description: "Notification tracking call emitted by the UI.", Group: "notifications"},
	{Command: "notifications list", Description: "User notifications page.", Group: "notifications"},
	{Command: "opensearch-xml list", Description: "OpenSearch descriptor for Goodreads book search.", Group: "opensearch-xml"},
	{Command: "quotes get", Description: "User quotes widget script.", Group: "quotes"},
	{Command: "quotes list", Description: "Current user's quotes list.", Group: "quotes"},
	{Command: "feed list", Description: "Read the home updates feed (friends' activity).", Group: "feed"},
	{Command: "giveaway list", Description: "Browse the Goodreads Giveaways listing.", Group: "giveaway"},
	{Command: "giveaway show", Description: "Show one giveaway detail page.", Group: "giveaway"},
	{Command: "giveaway enter", Description: "Enter a Goodreads giveaway (requires live capture to finish).", Group: "giveaway"},
	{Command: "rating set", Description: "Rate a book 1-5 stars (RateBook GraphQL mutation).", Group: "rating"},
	{Command: "rating clear", Description: "Clear your rating for a book (UnrateBook GraphQL mutation).", Group: "rating"},
	{Command: "recommendations list", Description: "Personalized recommendations page.", Group: "recommendations"},
	{Command: "recommendations list-tome", Description: "Friends' recommendations page.", Group: "recommendations"},
	{Command: "review create", Description: "Write/update a review for a book, with spoiler and feed-publicize options.", Group: "review"},
	{Command: "review create-update", Description: "Inline review/date/notes field update for one book.", Group: "review"},
	{Command: "review create-updatelist", Description: "Batch update selected reviews/books on a user's shelf table.", Group: "review"},
	{Command: "review get", Description: "Bookshelf list for a user, optionally filtered by shelf.", Group: "review"},
	{Command: "review get-listrss", Description: "This public XML route is the cleanest current read API for shelf exports. Authenticated shelf pages\ncan expose a private keyed RSS URL; do not log or commit that key.", Group: "review"},
	{Command: "review get-stats", Description: "Reading stats page.", Group: "review"},
	{Command: "review list", Description: "Review drafts page.", Group: "review"},
	{Command: "review list-duplicates", Description: "Duplicate books tool.", Group: "review"},
	{Command: "review list-import", Description: "Import/export page.", Group: "review"},
	{Command: "shelf create", Description: "Add or remove one book from a shelf through the shelf chooser helper.", Group: "shelf"},
	{Command: "shelf create-movebatch", Description: "Reorder books/shelves in batch.", Group: "shelf"},
	{Command: "shelf create-movebatch-2", Description: "Persist shelf-table position changes for a user.", Group: "shelf"},
	{Command: "shelf create-movetoposition", Description: "Move one shelf/book row to a specific position.", Group: "shelf"},
	{Command: "shelf create-removebook", Description: "Remove one book from a shelf from the button helper.", Group: "shelf"},
	{Command: "shelf create-update", Description: "Update shelf display/settings.", Group: "shelf"},
	{Command: "shelf get", Description: "Public shelf landing page.", Group: "shelf"},
	{Command: "shelf list", Description: "Top public shelves index.", Group: "shelf"},
	{Command: "tooltips list", Description: "Batch tooltip metadata for book ids shown in shelf/list pages.", Group: "tooltips"},
	{Command: "topic list", Description: "Discussions page, optionally filtered to groups.", Group: "topic"},
	{Command: "user get", Description: "Profile delay-loaded sections.", Group: "user"},
	{Command: "user get-show", Description: "User profile page.", Group: "user"},
	{Command: "user get-yearinbooks", Description: "Year in Books page.", Group: "user"},
	{Command: "user list", Description: "People discovery page for most popular reviewers.", Group: "user"},
	{Command: "user list-topreaders", Description: "People discovery page for top readers.", Group: "user"},
	{Command: "user list-topreviewers", Description: "People discovery page for top reviewers.", Group: "user"},
	{Command: "user-following list", Description: "People discovery page for most-followed users.", Group: "user-following"},
	{Command: "user-shelves create", Description: "Create a custom user shelf.", Group: "user-shelves"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  goodreads-pp-cli which "stale tickets"
  goodreads-pp-cli which "bottleneck"
  goodreads-pp-cli which --limit 1 "send message"
  goodreads-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
