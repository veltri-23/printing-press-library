// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newDiscoverMoviesCmd(flags *rootFlags) *cobra.Command {
	var flagWithGenres string
	var flagPrimaryReleaseYear int
	var flagPrimaryReleaseDateGte string
	var flagPrimaryReleaseDateLte string
	var flagVoteAverageGte float64
	var flagVoteAverageLte float64
	var flagVoteCountGte int
	var flagSortBy string
	var flagWithWatchProviders string
	var flagWatchRegion string
	var flagWithCast string
	var flagWithCrew string
	var flagWithKeywords string
	var flagCertification string
	var flagCertificationCountry string
	var flagPage string

	cmd := &cobra.Command{
		Use:         "movies",
		Short:       "Discover movies by genre, year, rating, certification, cast, crew, streaming provider, and more",
		Example:     "  movie-goat-pp-cli discover movies",
		Annotations: map[string]string{"pp:endpoint": "discover.movies", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/discover/movie"
			params := map[string]string{}
			if flagWithGenres != "" {
				params["with_genres"] = fmt.Sprintf("%v", flagWithGenres)
			}
			if flagPrimaryReleaseYear != 0 {
				params["primary_release_year"] = fmt.Sprintf("%v", flagPrimaryReleaseYear)
			}
			if flagPrimaryReleaseDateGte != "" {
				params["primary_release_date.gte"] = fmt.Sprintf("%v", flagPrimaryReleaseDateGte)
			}
			if flagPrimaryReleaseDateLte != "" {
				params["primary_release_date.lte"] = fmt.Sprintf("%v", flagPrimaryReleaseDateLte)
			}
			if flagVoteAverageGte != 0.0 {
				params["vote_average.gte"] = fmt.Sprintf("%v", flagVoteAverageGte)
			}
			if flagVoteAverageLte != 0.0 {
				params["vote_average.lte"] = fmt.Sprintf("%v", flagVoteAverageLte)
			}
			if flagVoteCountGte != 0 {
				params["vote_count.gte"] = fmt.Sprintf("%v", flagVoteCountGte)
			}
			if flagSortBy != "" {
				params["sort_by"] = fmt.Sprintf("%v", flagSortBy)
			}
			if flagWithWatchProviders != "" {
				params["with_watch_providers"] = fmt.Sprintf("%v", flagWithWatchProviders)
			}
			if flagWatchRegion != "" {
				params["watch_region"] = fmt.Sprintf("%v", flagWatchRegion)
			}
			if flagWithCast != "" {
				params["with_cast"] = fmt.Sprintf("%v", flagWithCast)
			}
			if flagWithCrew != "" {
				params["with_crew"] = fmt.Sprintf("%v", flagWithCrew)
			}
			if flagWithKeywords != "" {
				params["with_keywords"] = fmt.Sprintf("%v", flagWithKeywords)
			}
			if flagCertification != "" {
				params["certification"] = fmt.Sprintf("%v", flagCertification)
			}
			if flagCertificationCountry != "" {
				params["certification_country"] = fmt.Sprintf("%v", flagCertificationCountry)
			}
			if flagPage != "" {
				params["page"] = fmt.Sprintf("%v", flagPage)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "discover", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagWithGenres, "with-genres", "", "Genre IDs (comma-separated, e.g. 28,12 for Action,Adventure)")
	cmd.Flags().IntVar(&flagPrimaryReleaseYear, "primary-release-year", 0, "Exact release year")
	cmd.Flags().StringVar(&flagPrimaryReleaseDateGte, "primary-release-date-gte", "", "Release date from (YYYY-MM-DD)")
	cmd.Flags().StringVar(&flagPrimaryReleaseDateLte, "primary-release-date-lte", "", "Release date to (YYYY-MM-DD)")
	cmd.Flags().Float64Var(&flagVoteAverageGte, "vote-average-gte", 0.0, "Minimum rating (0-10)")
	cmd.Flags().Float64Var(&flagVoteAverageLte, "vote-average-lte", 0.0, "Maximum rating")
	cmd.Flags().IntVar(&flagVoteCountGte, "vote-count-gte", 0, "Minimum number of votes")
	cmd.Flags().StringVar(&flagSortBy, "sort-by", "popularity.desc", "Sort order (popularity.desc, vote_average.desc, release_date.desc, revenue.desc)")
	cmd.Flags().StringVar(&flagWithWatchProviders, "with-watch-providers", "", "Watch provider IDs (comma-separated)")
	cmd.Flags().StringVar(&flagWatchRegion, "watch-region", "", "Region for watch providers (ISO 3166-1, e.g. US)")
	cmd.Flags().StringVar(&flagWithCast, "with-cast", "", "Person IDs to filter by cast")
	cmd.Flags().StringVar(&flagWithCrew, "with-crew", "", "Person IDs to filter by crew")
	cmd.Flags().StringVar(&flagWithKeywords, "with-keywords", "", "Keyword IDs")
	cmd.Flags().StringVar(&flagCertification, "certification", "", "Certification rating (G, PG, PG-13, R)")
	cmd.Flags().StringVar(&flagCertificationCountry, "certification-country", "", "Country for certification")
	cmd.Flags().StringVar(&flagPage, "page", "", "Page number")

	return cmd
}
