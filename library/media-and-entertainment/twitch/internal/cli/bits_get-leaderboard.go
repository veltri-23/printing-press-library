// Copyright 2026 ryanc00per and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newBitsGetLeaderboardCmd(flags *rootFlags) *cobra.Command {
	var flagCount int
	var flagPeriod string
	var flagStartedAt string
	var flagUserId string

	cmd := &cobra.Command{
		Use:         "get-leaderboard",
		Short:       "Gets the Bits leaderboard for the authenticated broadcaster.",
		Example:     "  twitch-pp-cli bits get-leaderboard",
		Annotations: map[string]string{"pp:endpoint": "bits.get-leaderboard", "pp:method": "GET", "pp:path": "/bits/leaderboard", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("period") {
				allowedPeriod := []string{"day", "week", "month", "year", "all"}
				validPeriod := false
				for _, v := range allowedPeriod {
					if flagPeriod == v {
						validPeriod = true
						break
					}
				}
				if !validPeriod {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagPeriod, "period", allowedPeriod)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/bits/leaderboard"
			params := map[string]string{}
			if flagCount != 0 {
				params["count"] = formatCLIParamValue(flagCount)
			}
			if flagPeriod != "" {
				params["period"] = formatCLIParamValue(flagPeriod)
			}
			if flagStartedAt != "" {
				params["started_at"] = formatCLIParamValue(flagStartedAt)
			}
			if flagUserId != "" {
				params["user_id"] = formatCLIParamValue(flagUserId)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "bits", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagCount, "count", 0, "The number of results to return. The minimum count is 1 and the maximum is 100. The default is 10.")
	cmd.Flags().StringVar(&flagPeriod, "period", "", "The time period over which data is aggregated (uses the PST time zone). (one of: day, week, month, year, all)")
	cmd.Flags().StringVar(&flagStartedAt, "started-at", "", "The start date, in RFC3339 format, used for determining the aggregation period.")
	cmd.Flags().StringVar(&flagUserId, "user-id", "", "An ID that identifies a user that cheered bits in the channel.")

	return cmd
}
