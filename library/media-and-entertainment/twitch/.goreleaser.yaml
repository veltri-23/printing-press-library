version: 2
project_name: twitch-pp-cli
changelog:
  disable: true
builds:
  - id: twitch-pp-cli
    main: ./cmd/twitch-pp-cli
    binary: twitch-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/media-and-entertainment/twitch/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: twitch-pp-mcp
    main: ./cmd/twitch-pp-mcp
    binary: twitch-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: twitch-pp-cli
    repository:
      owner: coopdogggs
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Twitch Helix API"
    install: |
      bin.install "twitch-pp-cli"
      bin.install "twitch-pp-mcp"
