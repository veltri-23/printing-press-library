version: 2
project_name: airhint-pp-cli
changelog:
  disable: true
builds:
  - id: airhint-pp-cli
    main: ./cmd/airhint-pp-cli
    binary: airhint-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/travel/airhint/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: airhint-pp-mcp
    main: ./cmd/airhint-pp-mcp
    binary: airhint-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: airhint-pp-cli
    repository:
      owner: jvm
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "AirHint flight price prediction API — buy/wait recommendations for airline tickets"
    install: |
      bin.install "airhint-pp-cli"
      bin.install "airhint-pp-mcp"
