// Copyright 2026 jmbernabotto and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCoverageStopPointsGetCoverageRegionCmd(flags *rootFlags) *cobra.Command {
	var flagDepth int
	var flagForbiddenId string
	var flagForbiddenUris string
	var flagExternalCode string
	var flagHeadsign string
	var flagOdtLevel string
	var flagDataFreshness string
	var flagDistance int
	var flagSince string
	var flagUntil string
	var flagDisableGeojson bool
	var flagDisableDisruption bool
	var flagFilter string
	var flagTags string
	var flagLanguage string
	var flagOriginalId string

	cmd := &cobra.Command{
		Use:         "get-coverage-region <region>",
		Short:       "Get coverage region",
		Example:     "  sncf-connect-pp-cli coverage stop-points get-coverage-region example-value",
		Annotations: map[string]string{"pp:endpoint": "stop-points.get-coverage-region", "pp:method": "GET", "pp:path": "/coverage/{region}/stop_points", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("odt-level") {
				allowedOdtLevel := []string{"all", "scheduled", "with_stops", "zonal"}
				validOdtLevel := false
				for _, v := range allowedOdtLevel {
					if flagOdtLevel == v {
						validOdtLevel = true
						break
					}
				}
				if !validOdtLevel {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "odt-level", flagOdtLevel, allowedOdtLevel)
				}
			}
			if cmd.Flags().Changed("data-freshness") {
				allowedDataFreshness := []string{"base_schedule", "adapted_schedule", "realtime"}
				validDataFreshness := false
				for _, v := range allowedDataFreshness {
					if flagDataFreshness == v {
						validDataFreshness = true
						break
					}
				}
				if !validDataFreshness {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "data-freshness", flagDataFreshness, allowedDataFreshness)
				}
			}
			if cmd.Flags().Changed("language") {
				allowedLanguage := []string{"nl-NL", "en-US", "en-GB", "fr-FR", "de-DE", "hi-IN", "it-IT", "ja-JP", "pt-PT", "ru-RU", "es-ES"}
				validLanguage := false
				for _, v := range allowedLanguage {
					if flagLanguage == v {
						validLanguage = true
						break
					}
				}
				if !validLanguage {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "language", flagLanguage, allowedLanguage)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/coverage/{region}/stop_points"
			path = replacePathParam(path, "region", args[0])
			params := map[string]string{}
			if flagDepth != 0 {
				params["depth"] = fmt.Sprintf("%v", flagDepth)
			}
			if flagForbiddenId != "" {
				params["forbidden_id[]"] = fmt.Sprintf("%v", flagForbiddenId)
			}
			if flagForbiddenUris != "" {
				params["forbidden_uris[]"] = fmt.Sprintf("%v", flagForbiddenUris)
			}
			if flagExternalCode != "" {
				params["external_code"] = fmt.Sprintf("%v", flagExternalCode)
			}
			if flagHeadsign != "" {
				params["headsign"] = fmt.Sprintf("%v", flagHeadsign)
			}
			if flagOdtLevel != "" {
				params["odt_level"] = fmt.Sprintf("%v", flagOdtLevel)
			}
			if flagDataFreshness != "" {
				params["data_freshness"] = fmt.Sprintf("%v", flagDataFreshness)
			}
			if flagDistance != 0 {
				params["distance"] = fmt.Sprintf("%v", flagDistance)
			}
			if flagSince != "" {
				params["since"] = fmt.Sprintf("%v", flagSince)
			}
			if flagUntil != "" {
				params["until"] = fmt.Sprintf("%v", flagUntil)
			}
			if flagDisableGeojson != false {
				params["disable_geojson"] = fmt.Sprintf("%v", flagDisableGeojson)
			}
			if flagDisableDisruption != false {
				params["disable_disruption"] = fmt.Sprintf("%v", flagDisableDisruption)
			}
			if flagFilter != "" {
				params["filter"] = fmt.Sprintf("%v", flagFilter)
			}
			if flagTags != "" {
				params["tags[]"] = fmt.Sprintf("%v", flagTags)
			}
			if flagLanguage != "" {
				params["language"] = fmt.Sprintf("%v", flagLanguage)
			}
			if flagOriginalId != "" {
				params["original_id"] = fmt.Sprintf("%v", flagOriginalId)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "stop-points", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagDepth, "depth", 0, "The depth of your object")
	cmd.Flags().StringVar(&flagForbiddenId, "forbidden-id", "", "DEPRECATED, replaced by `forbidden_uris[]`")
	cmd.Flags().StringVar(&flagForbiddenUris, "forbidden-uris", "", "forbidden uris")
	cmd.Flags().StringVar(&flagExternalCode, "external-code", "", "An external code to query")
	cmd.Flags().StringVar(&flagHeadsign, "headsign", "", "filter vehicle journeys on headsign")
	cmd.Flags().StringVar(&flagOdtLevel, "odt-level", "all", "odt level (one of: all, scheduled, with_stops, zonal)")
	cmd.Flags().StringVar(&flagDataFreshness, "data-freshness", "base_schedule", "Define the freshness of data to use to filter vehicle_journeys along with parameters &since and/or &until . (one of: base_schedule, adapted_schedule, realtime)")
	cmd.Flags().IntVar(&flagDistance, "distance", 0, "Distance range of the query. Used only if a coord is in the query")
	cmd.Flags().StringVar(&flagSince, "since", "", "filters objects not valid before this date")
	cmd.Flags().StringVar(&flagUntil, "until", "", "filters objects not valid after this date")
	cmd.Flags().BoolVar(&flagDisableGeojson, "disable-geojson", false, "remove geojson from the response")
	cmd.Flags().BoolVar(&flagDisableDisruption, "disable-disruption", false, "remove disruptions from the response")
	cmd.Flags().StringVar(&flagFilter, "filter", "", "The filter parameter")
	cmd.Flags().StringVar(&flagTags, "tags", "", "If filled, will restrain the search within the given disruption tags")
	cmd.Flags().StringVar(&flagLanguage, "language", "", "Here, select a specific language for disruption message (one of: nl-NL, en-US, en-GB, fr-FR, de-DE, hi-IN, it-IT, ja-JP, pt-PT, ru-RU, es-ES)")
	cmd.Flags().StringVar(&flagOriginalId, "original-id", "", "original uri of the object you want to query")

	return cmd
}
