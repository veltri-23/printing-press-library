// Copyright 2026 jmbernabotto and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCoverageLineReportsGetCoverageRegionUriCmd(flags *rootFlags) *cobra.Command {
	var flagDepth int
	var flagForbiddenUris string
	var flagDisableGeojson bool
	var flagSince string
	var flagUntil string
	var flagFilterStatus string
	var flagLanguage string

	cmd := &cobra.Command{
		Use:         "get-coverage-region-uri <region> <uri>",
		Short:       "Get coverage region uri",
		Example:     "  sncf-connect-pp-cli coverage line-reports get-coverage-region-uri example-value example-value",
		Annotations: map[string]string{"pp:endpoint": "line-reports.get-coverage-region-uri", "pp:method": "GET", "pp:path": "/coverage/{region}/{uri}/line_reports", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("language") {
				allowedLanguage := []string{"nl-NL", "en-US", "en-GB", "fr-FR", "de-DE", "hi-IN", "it-IT", "ja-JP", "pt-PT", "ru-RU", "es-ES"}
				validLanguage := false
				for _, v := range allowedLanguage {
					if flagLanguage == v {
						validLanguage = true
						break
					}
				}
				if !validLanguage {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "language", flagLanguage, allowedLanguage)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/coverage/{region}/{uri}/line_reports"
			path = replacePathParam(path, "region", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("uri is required\nUsage: %s <%s>", cmd.CommandPath(), "uri"))
			}
			path = replacePathParam(path, "uri", args[1])
			params := map[string]string{}
			if flagDepth != 0 {
				params["depth"] = fmt.Sprintf("%v", flagDepth)
			}
			if flagForbiddenUris != "" {
				params["forbidden_uris[]"] = fmt.Sprintf("%v", flagForbiddenUris)
			}
			if flagDisableGeojson != false {
				params["disable_geojson"] = fmt.Sprintf("%v", flagDisableGeojson)
			}
			if flagSince != "" {
				params["since"] = fmt.Sprintf("%v", flagSince)
			}
			if flagUntil != "" {
				params["until"] = fmt.Sprintf("%v", flagUntil)
			}
			if flagFilterStatus != "" {
				params["filter_status[]"] = fmt.Sprintf("%v", flagFilterStatus)
			}
			if flagLanguage != "" {
				params["language"] = fmt.Sprintf("%v", flagLanguage)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "line-reports", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagDepth, "depth", 0, "The depth of your object")
	cmd.Flags().StringVar(&flagForbiddenUris, "forbidden-uris", "", "forbidden uris")
	cmd.Flags().BoolVar(&flagDisableGeojson, "disable-geojson", false, "remove geojson from the response")
	cmd.Flags().StringVar(&flagSince, "since", "", "use disruptions valid after this date")
	cmd.Flags().StringVar(&flagUntil, "until", "", "use disruptions valid before this date")
	cmd.Flags().StringVar(&flagFilterStatus, "filter-status", "", "filter_status uris")
	cmd.Flags().StringVar(&flagLanguage, "language", "", "Here, select a specific language for disruption message (one of: nl-NL, en-US, en-GB, fr-FR, de-DE, hi-IN, it-IT, ja-JP, pt-PT, ru-RU, es-ES)")

	return cmd
}
