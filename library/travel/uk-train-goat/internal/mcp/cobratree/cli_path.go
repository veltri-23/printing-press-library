// Copyright 2026 Sachin Ahuja and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cobratree

import (
	"os"
	"os/exec"
	"path/filepath"
	"runtime"
)

// SiblingCLIPath resolves the companion CLI via sibling-of-executable,
// UK_TRAIN_GOAT_CLI_PATH env var, then PATH.
func SiblingCLIPath() (string, error) {
	cliName := cliExecutableName(runtime.GOOS)
	if exe, err := os.Executable(); err == nil {
		candidate := filepath.Join(filepath.Dir(exe), cliName)
		if _, err := os.Stat(candidate); err == nil {
			return candidate, nil
		}
	}
	if v := os.Getenv("UK_TRAIN_GOAT_CLI_PATH"); v != "" {
		return v, nil
	}
	return exec.LookPath(cliName)
}

func cliExecutableName(goos string) string {
	name := "uk-train-goat-pp-cli"
	if goos == "windows" {
		return name + ".exe"
	}
	return name
}
