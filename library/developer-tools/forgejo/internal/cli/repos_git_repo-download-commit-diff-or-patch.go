// Copyright 2026 jrimmer. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newReposGitRepoDownloadCommitDiffOrPatchCmd(flags *rootFlags) *cobra.Command {
	var flagDiffType string

	cmd := &cobra.Command{
		Use:         "repo-download-commit-diff-or-patch <owner> <repo> <sha>",
		Short:       "Get a commit's diff or patch",
		Example:     "  forgejo-pp-cli repos git repo-download-commit-diff-or-patch example-value example-value example-value --diff-type diff",
		Annotations: map[string]string{"pp:endpoint": "git.repo-download-commit-diff-or-patch", "pp:method": "GET", "pp:path": "/repos/{owner}/{repo}/git/commits/{sha}.{diffType}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("diff-type") {
				allowedDiffType := []string{"diff", "patch"}
				validDiffType := false
				for _, v := range allowedDiffType {
					if flagDiffType == v {
						validDiffType = true
						break
					}
				}
				if !validDiffType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "diff-type", flagDiffType, allowedDiffType)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/repos/{owner}/{repo}/git/commits/{sha}.{diffType}"
			path = replacePathParam(path, "owner", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("repo is required\nUsage: %s <%s>", cmd.CommandPath(), "repo"))
			}
			path = replacePathParam(path, "repo", args[1])
			if len(args) < 3 {
				return usageErr(fmt.Errorf("sha is required\nUsage: %s <%s>", cmd.CommandPath(), "sha"))
			}
			path = replacePathParam(path, "sha", args[2])
			path = replacePathParam(path, "diffType", fmt.Sprintf("%v", flagDiffType))
			params := map[string]string{}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "git", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDiffType, "diff-type", "diff", "whether the output is diff or patch (one of: diff, patch)")

	return cmd
}
