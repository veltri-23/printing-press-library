// Copyright 2026 davbebawwy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newBuildApisGeneralSettingsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagApiVersion string
	var bodyAuditEnforceSettableVar bool
	var bodyBuildsEnabledForForks bool
	var bodyDisableClassicBuildPipelineCreation bool
	var bodyDisableClassicPipelineCreation bool
	var bodyDisableClassicReleasePipelineCreation bool
	var bodyEnableShellTasksArgsSanitizing bool
	var bodyEnableShellTasksArgsSanitizingAudit bool
	var bodyEnforceJobAuthScope bool
	var bodyEnforceJobAuthScopeForForks bool
	var bodyEnforceJobAuthScopeForReleases bool
	var bodyEnforceNoAccessToSecretsFromForks bool
	var bodyEnforceReferencedRepoScopedToken bool
	var bodyEnforceSettableVar bool
	var bodyForkProtectionEnabled bool
	var bodyIsCommentRequiredForPullRequest bool
	var bodyPublishPipelineMetadata bool
	var bodyRequireCommentsForNonTeamMemberAndNonContributors bool
	var bodyRequireCommentsForNonTeamMembersOnly bool
	var bodyStatusBadgesArePrivate bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "general-settings-update <organization> <project>",
		Short:       "Updates pipeline general settings.",
		Example:     "  azure-devops-pp-cli build-apis general-settings-update example-value example-value --api-version example-value",
		Annotations: map[string]string{"pp:endpoint": "build-apis.general-settings-update", "pp:method": "PATCH", "pp:path": "/{organization}/{project}/_apis/build/generalsettings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("api-version") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "api-version")
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/{organization}/{project}/_apis/build/generalsettings"
			path = replacePathParam(path, "organization", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("project is required\nUsage: %s <%s>", cmd.CommandPath(), "project"))
			}
			path = replacePathParam(path, "project", args[1])
			params := map[string]string{}
			if flagApiVersion != "" {
				params["api-version"] = formatCLIParamValue(flagApiVersion)
			}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("audit-enforce-settable-var") {
					body["auditEnforceSettableVar"] = bodyAuditEnforceSettableVar
				}
				if cmd.Flags().Changed("builds-enabled-for-forks") {
					body["buildsEnabledForForks"] = bodyBuildsEnabledForForks
				}
				if cmd.Flags().Changed("disable-classic-build-pipeline-creation") {
					body["disableClassicBuildPipelineCreation"] = bodyDisableClassicBuildPipelineCreation
				}
				if cmd.Flags().Changed("disable-classic-pipeline-creation") {
					body["disableClassicPipelineCreation"] = bodyDisableClassicPipelineCreation
				}
				if cmd.Flags().Changed("disable-classic-release-pipeline-creation") {
					body["disableClassicReleasePipelineCreation"] = bodyDisableClassicReleasePipelineCreation
				}
				if cmd.Flags().Changed("enable-shell-tasks-args-sanitizing") {
					body["enableShellTasksArgsSanitizing"] = bodyEnableShellTasksArgsSanitizing
				}
				if cmd.Flags().Changed("enable-shell-tasks-args-sanitizing-audit") {
					body["enableShellTasksArgsSanitizingAudit"] = bodyEnableShellTasksArgsSanitizingAudit
				}
				if cmd.Flags().Changed("enforce-job-auth-scope") {
					body["enforceJobAuthScope"] = bodyEnforceJobAuthScope
				}
				if cmd.Flags().Changed("enforce-job-auth-scope-for-forks") {
					body["enforceJobAuthScopeForForks"] = bodyEnforceJobAuthScopeForForks
				}
				if cmd.Flags().Changed("enforce-job-auth-scope-for-releases") {
					body["enforceJobAuthScopeForReleases"] = bodyEnforceJobAuthScopeForReleases
				}
				if cmd.Flags().Changed("enforce-no-access-to-secrets-from-forks") {
					body["enforceNoAccessToSecretsFromForks"] = bodyEnforceNoAccessToSecretsFromForks
				}
				if cmd.Flags().Changed("enforce-referenced-repo-scoped-token") {
					body["enforceReferencedRepoScopedToken"] = bodyEnforceReferencedRepoScopedToken
				}
				if cmd.Flags().Changed("enforce-settable-var") {
					body["enforceSettableVar"] = bodyEnforceSettableVar
				}
				if cmd.Flags().Changed("fork-protection-enabled") {
					body["forkProtectionEnabled"] = bodyForkProtectionEnabled
				}
				if cmd.Flags().Changed("is-comment-required-for-pull-request") {
					body["isCommentRequiredForPullRequest"] = bodyIsCommentRequiredForPullRequest
				}
				if cmd.Flags().Changed("publish-pipeline-metadata") {
					body["publishPipelineMetadata"] = bodyPublishPipelineMetadata
				}
				if cmd.Flags().Changed("require-comments-for-non-team-member-and-non-contributors") {
					body["requireCommentsForNonTeamMemberAndNonContributors"] = bodyRequireCommentsForNonTeamMemberAndNonContributors
				}
				if cmd.Flags().Changed("require-comments-for-non-team-members-only") {
					body["requireCommentsForNonTeamMembersOnly"] = bodyRequireCommentsForNonTeamMembersOnly
				}
				if cmd.Flags().Changed("status-badges-are-private") {
					body["statusBadgesArePrivate"] = bodyStatusBadgesArePrivate
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "build-apis", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "build-apis", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "build-apis", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "build-apis", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "build-apis", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "build-apis",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "build-apis", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "build-apis", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagApiVersion, "api-version", "", "Version of the API to use. This should be set to '7.1' to use this version of the api.")
	cmd.Flags().BoolVar(&bodyAuditEnforceSettableVar, "audit-enforce-settable-var", false, "If enabled")
	cmd.Flags().BoolVar(&bodyBuildsEnabledForForks, "builds-enabled-for-forks", false, "Enable forked repositories to build pull requests.")
	cmd.Flags().BoolVar(&bodyDisableClassicBuildPipelineCreation, "disable-classic-build-pipeline-creation", false, "Disable classic build pipelines creation.")
	cmd.Flags().BoolVar(&bodyDisableClassicPipelineCreation, "disable-classic-pipeline-creation", false, "Disable classic pipelines creation.")
	cmd.Flags().BoolVar(&bodyDisableClassicReleasePipelineCreation, "disable-classic-release-pipeline-creation", false, "Disable classic release pipelines creation.")
	cmd.Flags().BoolVar(&bodyEnableShellTasksArgsSanitizing, "enable-shell-tasks-args-sanitizing", false, "Enable shell tasks args sanitizing.")
	cmd.Flags().BoolVar(&bodyEnableShellTasksArgsSanitizingAudit, "enable-shell-tasks-args-sanitizing-audit", false, "Enable shell tasks args sanitizing preview.")
	cmd.Flags().BoolVar(&bodyEnforceJobAuthScope, "enforce-job-auth-scope", false, "If enabled, scope of access for all non-release pipelines reduces to the current project.")
	cmd.Flags().BoolVar(&bodyEnforceJobAuthScopeForForks, "enforce-job-auth-scope-for-forks", false, "Enforce job auth scope for builds of forked repositories.")
	cmd.Flags().BoolVar(&bodyEnforceJobAuthScopeForReleases, "enforce-job-auth-scope-for-releases", false, "If enabled, scope of access for all release pipelines reduces to the current project.")
	cmd.Flags().BoolVar(&bodyEnforceNoAccessToSecretsFromForks, "enforce-no-access-to-secrets-from-forks", false, "Enforce no access to secrets for builds of forked repositories.")
	cmd.Flags().BoolVar(&bodyEnforceReferencedRepoScopedToken, "enforce-referenced-repo-scoped-token", false, "Restricts the scope of access for all pipelines to only repositories explicitly referenced by the pipeline.")
	cmd.Flags().BoolVar(&bodyEnforceSettableVar, "enforce-settable-var", false, "If enabled, only those variables that are explicitly marked as 'Settable at queue time' can be set at queue time.")
	cmd.Flags().BoolVar(&bodyForkProtectionEnabled, "fork-protection-enabled", false, "Enable settings that enforce certain levels of protection for building pull requests from forks globally.")
	cmd.Flags().BoolVar(&bodyIsCommentRequiredForPullRequest, "is-comment-required-for-pull-request", false, "Make comments required to have builds in all pull requests.")
	cmd.Flags().BoolVar(&bodyPublishPipelineMetadata, "publish-pipeline-metadata", false, "Allows pipelines to record metadata.")
	cmd.Flags().BoolVar(&bodyRequireCommentsForNonTeamMemberAndNonContributors, "require-comments-for-non-team-member-and-non-contributors", false, "Make comments required to have builds in pull requests from non-team members and non-contributors.")
	cmd.Flags().BoolVar(&bodyRequireCommentsForNonTeamMembersOnly, "require-comments-for-non-team-members-only", false, "Make comments required to have builds in pull requests from non-team members.")
	cmd.Flags().BoolVar(&bodyStatusBadgesArePrivate, "status-badges-are-private", false, "Anonymous users can access the status badge API for all pipelines unless this option is enabled.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
