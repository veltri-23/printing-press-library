// Copyright 2026 davbebawwy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newReleaseApisReleasesListCmd(flags *rootFlags) *cobra.Command {
	var flagDefinitionId string
	var flagDefinitionEnvironmentId string
	var flagSearchText string
	var flagCreatedBy string
	var flagStatusFilter string
	var flagEnvironmentStatusFilter int
	var flagMinCreatedTime string
	var flagMaxCreatedTime string
	var flagQueryOrder string
	var flagContinuationToken int
	var flagArtifactTypeId string
	var flagSourceId string
	var flagArtifactVersionId string
	var flagSourceBranchFilter string
	var flagIsDeleted bool
	var flagTagFilter string
	var flagPropertyFilters string
	var flagReleaseIdFilter string
	var flagPath string
	var flagApiVersion string

	cmd := &cobra.Command{
		Use:         "releases-list <organization> <project>",
		Short:       "Get a list of releases",
		Example:     "  azure-devops-pp-cli release-apis releases-list example-value example-value --api-version example-value",
		Annotations: map[string]string{"pp:endpoint": "release-apis.releases-list", "pp:method": "GET", "pp:path": "https://vsrm.dev.azure.com/{organization}/{project}/_apis/release/releases", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("api-version") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "api-version")
			}
			if cmd.Flags().Changed("status-filter") {
				allowedStatusFilter := []string{"undefined", "draft", "active", "abandoned"}
				validStatusFilter := false
				for _, v := range allowedStatusFilter {
					if flagStatusFilter == v {
						validStatusFilter = true
						break
					}
				}
				if !validStatusFilter {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStatusFilter, "status-filter", allowedStatusFilter)
				}
			}
			if cmd.Flags().Changed("query-order") {
				allowedQueryOrder := []string{"descending", "ascending"}
				validQueryOrder := false
				for _, v := range allowedQueryOrder {
					if flagQueryOrder == v {
						validQueryOrder = true
						break
					}
				}
				if !validQueryOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagQueryOrder, "query-order", allowedQueryOrder)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "https://vsrm.dev.azure.com/{organization}/{project}/_apis/release/releases"
			path = replacePathParam(path, "organization", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("project is required\nUsage: %s <%s>", cmd.CommandPath(), "project"))
			}
			path = replacePathParam(path, "project", args[1])
			params := map[string]string{}
			if flagDefinitionId != "" {
				params["definitionId"] = formatCLIParamValue(flagDefinitionId)
			}
			if flagDefinitionEnvironmentId != "" {
				params["definitionEnvironmentId"] = formatCLIParamValue(flagDefinitionEnvironmentId)
			}
			if flagSearchText != "" {
				params["searchText"] = formatCLIParamValue(flagSearchText)
			}
			if flagCreatedBy != "" {
				params["createdBy"] = formatCLIParamValue(flagCreatedBy)
			}
			if flagStatusFilter != "" {
				params["statusFilter"] = formatCLIParamValue(flagStatusFilter)
			}
			if flagEnvironmentStatusFilter != 0 {
				params["environmentStatusFilter"] = formatCLIParamValue(flagEnvironmentStatusFilter)
			}
			if flagMinCreatedTime != "" {
				params["minCreatedTime"] = formatCLIParamValue(flagMinCreatedTime)
			}
			if flagMaxCreatedTime != "" {
				params["maxCreatedTime"] = formatCLIParamValue(flagMaxCreatedTime)
			}
			if flagQueryOrder != "" {
				params["queryOrder"] = formatCLIParamValue(flagQueryOrder)
			}
			if flagContinuationToken != 0 {
				params["continuationToken"] = formatCLIParamValue(flagContinuationToken)
			}
			if flagArtifactTypeId != "" {
				params["artifactTypeId"] = formatCLIParamValue(flagArtifactTypeId)
			}
			if flagSourceId != "" {
				params["sourceId"] = formatCLIParamValue(flagSourceId)
			}
			if flagArtifactVersionId != "" {
				params["artifactVersionId"] = formatCLIParamValue(flagArtifactVersionId)
			}
			if flagSourceBranchFilter != "" {
				params["sourceBranchFilter"] = formatCLIParamValue(flagSourceBranchFilter)
			}
			if flagIsDeleted != false {
				params["isDeleted"] = formatCLIParamValue(flagIsDeleted)
			}
			if flagTagFilter != "" {
				params["tagFilter"] = formatCLIParamValue(flagTagFilter)
			}
			if flagPropertyFilters != "" {
				params["propertyFilters"] = formatCLIParamValue(flagPropertyFilters)
			}
			if flagReleaseIdFilter != "" {
				params["releaseIdFilter"] = formatCLIParamValue(flagReleaseIdFilter)
			}
			if flagPath != "" {
				params["path"] = formatCLIParamValue(flagPath)
			}
			if flagApiVersion != "" {
				params["api-version"] = formatCLIParamValue(flagApiVersion)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "release-apis", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDefinitionId, "definition-id", "", "Releases from this release definition Id.")
	cmd.Flags().StringVar(&flagDefinitionEnvironmentId, "definition-environment-id", "", "Definition environment id")
	cmd.Flags().StringVar(&flagSearchText, "search-text", "", "Releases with names containing searchText.")
	cmd.Flags().StringVar(&flagCreatedBy, "created-by", "", "Releases created by this user.")
	cmd.Flags().StringVar(&flagStatusFilter, "status-filter", "", "Releases that have this status. (one of: undefined, draft, active, abandoned)")
	cmd.Flags().IntVar(&flagEnvironmentStatusFilter, "environment-status-filter", 0, "Environment status filter")
	cmd.Flags().StringVar(&flagMinCreatedTime, "min-created-time", "", "Releases that were created after this time.")
	cmd.Flags().StringVar(&flagMaxCreatedTime, "max-created-time", "", "Releases that were created before this time.")
	cmd.Flags().StringVar(&flagQueryOrder, "query-order", "", "Gets the results in the defined order of created date for releases. Default is descending. (one of: descending, ascending)")
	cmd.Flags().IntVar(&flagContinuationToken, "continuation-token", 0, "Gets the releases after the continuation token provided.")
	cmd.Flags().StringVar(&flagArtifactTypeId, "artifact-type-id", "", "Releases with given artifactTypeId will be returned.")
	cmd.Flags().StringVar(&flagSourceId, "source-id", "", "Unique identifier of the artifact used. e.g.")
	cmd.Flags().StringVar(&flagArtifactVersionId, "artifact-version-id", "", "Releases with given artifactVersionId will be returned. E.g. in case of Build artifactType, it is buildId.")
	cmd.Flags().StringVar(&flagSourceBranchFilter, "source-branch-filter", "", "Releases with given sourceBranchFilter will be returned.")
	cmd.Flags().BoolVar(&flagIsDeleted, "is-deleted", false, "Gets the soft deleted releases, if true.")
	cmd.Flags().StringVar(&flagTagFilter, "tag-filter", "", "A comma-delimited list of tags. Only releases with these tags will be returned.")
	cmd.Flags().StringVar(&flagPropertyFilters, "property-filters", "", "A comma-delimited list of extended properties to be retrieved.")
	cmd.Flags().StringVar(&flagReleaseIdFilter, "release-id-filter", "", "A comma-delimited list of releases Ids. Only releases with these Ids will be returned.")
	cmd.Flags().StringVar(&flagPath, "path", "", "Releases under this folder path will be returned")
	cmd.Flags().StringVar(&flagApiVersion, "api-version", "", "Version of the API to use. This should be set to '7.1' to use this version of the api.")

	return cmd
}
