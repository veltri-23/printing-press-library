// Copyright 2026 davbebawwy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newBuildApisYamlGetCmd(flags *rootFlags) *cobra.Command {
	var flagRevision int
	var flagMinMetricsTime string
	var flagPropertyFilters string
	var flagIncludeLatestBuilds bool
	var flagApiVersion string

	cmd := &cobra.Command{
		Use:         "yaml-get <organization> <project> <definitionId>",
		Short:       "Converts a definition to YAML, optionally at a specific revision.",
		Example:     "  azure-devops-pp-cli build-apis yaml-get example-value example-value 42 --api-version example-value",
		Annotations: map[string]string{"pp:endpoint": "build-apis.yaml-get", "pp:method": "GET", "pp:path": "/{organization}/{project}/_apis/build/definitions/{definitionId}/yaml", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("api-version") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "api-version")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/{organization}/{project}/_apis/build/definitions/{definitionId}/yaml"
			path = replacePathParam(path, "organization", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("project is required\nUsage: %s <%s>", cmd.CommandPath(), "project"))
			}
			path = replacePathParam(path, "project", args[1])
			if len(args) < 3 {
				return usageErr(fmt.Errorf("definitionId is required\nUsage: %s <%s>", cmd.CommandPath(), "definitionId"))
			}
			path = replacePathParam(path, "definitionId", args[2])
			params := map[string]string{}
			if flagRevision != 0 {
				params["revision"] = formatCLIParamValue(flagRevision)
			}
			if flagMinMetricsTime != "" {
				params["minMetricsTime"] = formatCLIParamValue(flagMinMetricsTime)
			}
			if flagPropertyFilters != "" {
				params["propertyFilters"] = formatCLIParamValue(flagPropertyFilters)
			}
			if flagIncludeLatestBuilds != false {
				params["includeLatestBuilds"] = formatCLIParamValue(flagIncludeLatestBuilds)
			}
			if flagApiVersion != "" {
				params["api-version"] = formatCLIParamValue(flagApiVersion)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "build-apis", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagRevision, "revision", 0, "The revision number to retrieve. If this is not specified, the latest version will be returned.")
	cmd.Flags().StringVar(&flagMinMetricsTime, "min-metrics-time", "", "If specified, indicates the date from which metrics should be included.")
	cmd.Flags().StringVar(&flagPropertyFilters, "property-filters", "", "A comma-delimited list of properties to include in the results.")
	cmd.Flags().BoolVar(&flagIncludeLatestBuilds, "include-latest-builds", false, "Include latest builds")
	cmd.Flags().StringVar(&flagApiVersion, "api-version", "", "Version of the API to use. This should be set to '7.1' to use this version of the api.")

	return cmd
}
