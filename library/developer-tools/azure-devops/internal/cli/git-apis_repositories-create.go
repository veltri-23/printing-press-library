// Copyright 2026 davbebawwy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newGitApisRepositoriesCreateCmd(flags *rootFlags) *cobra.Command {
	var flagSourceRef string
	var flagApiVersion string
	var bodyName string
	var bodyParentRepositoryCollectionAvatarUrl string
	var bodyParentRepositoryCollectionId string
	var bodyParentRepositoryCollectionName string
	var bodyParentRepositoryCollectionUrl string
	var bodyParentRepositoryId string
	var bodyParentRepositoryIsFork bool
	var bodyParentRepositoryName string
	var bodyParentRepositoryProjectAbbreviation string
	var bodyParentRepositoryProjectDefaultTeamImageUrl string
	var bodyParentRepositoryProjectDescription string
	var bodyParentRepositoryProjectId string
	var bodyParentRepositoryProjectLastUpdateTime string
	var bodyParentRepositoryProjectName string
	var bodyParentRepositoryProjectRevision int
	var bodyParentRepositoryProjectState string
	var bodyParentRepositoryProjectUrl string
	var bodyParentRepositoryProjectVisibility string
	var bodyParentRepositoryRemoteUrl string
	var bodyParentRepositorySshUrl string
	var bodyParentRepositoryUrl string
	var bodyProject2Abbreviation string
	var bodyProject2DefaultTeamImageUrl string
	var bodyProject2Description string
	var bodyProject2Id string
	var bodyProject2LastUpdateTime string
	var bodyProject2Name string
	var bodyProject2Revision int
	var bodyProject2State string
	var bodyProject2Url string
	var bodyProject2Visibility string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "repositories-create <organization> <project>",
		Short:       "Create a git repository in a team project.",
		Example:     "  azure-devops-pp-cli git-apis repositories-create example-value example-value --api-version example-value",
		Annotations: map[string]string{"pp:endpoint": "git-apis.repositories-create", "pp:method": "POST", "pp:path": "/{organization}/{project}/_apis/git/repositories"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("api-version") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "api-version")
			}
			if cmd.Flags().Changed("source-ref") {
				var parsedSourceRef any
				if err := json.Unmarshal([]byte(flagSourceRef), &parsedSourceRef); err != nil {
					return fmt.Errorf("--source-ref must be valid JSON: %w", err)
				}
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/{organization}/{project}/_apis/git/repositories"
			path = replacePathParam(path, "organization", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("project is required\nUsage: %s <%s>", cmd.CommandPath(), "project"))
			}
			path = replacePathParam(path, "project", args[1])
			params := map[string]string{}
			if flagSourceRef != "" {
				params["sourceRef"] = formatCLIParamValue(flagSourceRef)
			}
			if flagApiVersion != "" {
				params["api-version"] = formatCLIParamValue(flagApiVersion)
			}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyName != "" {
					body["name"] = bodyName
				}
				{
					nestedParentRepository := map[string]any{}
					{
						nestedParentRepositoryCollection := map[string]any{}
						if bodyParentRepositoryCollectionAvatarUrl != "" {
							nestedParentRepositoryCollection["avatarUrl"] = bodyParentRepositoryCollectionAvatarUrl
						}
						if bodyParentRepositoryCollectionId != "" {
							nestedParentRepositoryCollection["id"] = bodyParentRepositoryCollectionId
						}
						if bodyParentRepositoryCollectionName != "" {
							nestedParentRepositoryCollection["name"] = bodyParentRepositoryCollectionName
						}
						if bodyParentRepositoryCollectionUrl != "" {
							nestedParentRepositoryCollection["url"] = bodyParentRepositoryCollectionUrl
						}
						if len(nestedParentRepositoryCollection) > 0 {
							nestedParentRepository["collection"] = nestedParentRepositoryCollection
						}
					}
					if bodyParentRepositoryId != "" {
						nestedParentRepository["id"] = bodyParentRepositoryId
					}
					if cmd.Flags().Changed("parent-repository-is-fork") {
						nestedParentRepository["isFork"] = bodyParentRepositoryIsFork
					}
					if bodyParentRepositoryName != "" {
						nestedParentRepository["name"] = bodyParentRepositoryName
					}
					{
						nestedParentRepositoryProject := map[string]any{}
						if bodyParentRepositoryProjectAbbreviation != "" {
							nestedParentRepositoryProject["abbreviation"] = bodyParentRepositoryProjectAbbreviation
						}
						if bodyParentRepositoryProjectDefaultTeamImageUrl != "" {
							nestedParentRepositoryProject["defaultTeamImageUrl"] = bodyParentRepositoryProjectDefaultTeamImageUrl
						}
						if bodyParentRepositoryProjectDescription != "" {
							nestedParentRepositoryProject["description"] = bodyParentRepositoryProjectDescription
						}
						if bodyParentRepositoryProjectId != "" {
							nestedParentRepositoryProject["id"] = bodyParentRepositoryProjectId
						}
						if bodyParentRepositoryProjectLastUpdateTime != "" {
							nestedParentRepositoryProject["lastUpdateTime"] = bodyParentRepositoryProjectLastUpdateTime
						}
						if bodyParentRepositoryProjectName != "" {
							nestedParentRepositoryProject["name"] = bodyParentRepositoryProjectName
						}
						if bodyParentRepositoryProjectRevision != 0 {
							nestedParentRepositoryProject["revision"] = bodyParentRepositoryProjectRevision
						}
						if bodyParentRepositoryProjectState != "" {
							nestedParentRepositoryProject["state"] = bodyParentRepositoryProjectState
						}
						if bodyParentRepositoryProjectUrl != "" {
							nestedParentRepositoryProject["url"] = bodyParentRepositoryProjectUrl
						}
						if bodyParentRepositoryProjectVisibility != "" {
							nestedParentRepositoryProject["visibility"] = bodyParentRepositoryProjectVisibility
						}
						if len(nestedParentRepositoryProject) > 0 {
							nestedParentRepository["project"] = nestedParentRepositoryProject
						}
					}
					if bodyParentRepositoryRemoteUrl != "" {
						nestedParentRepository["remoteUrl"] = bodyParentRepositoryRemoteUrl
					}
					if bodyParentRepositorySshUrl != "" {
						nestedParentRepository["sshUrl"] = bodyParentRepositorySshUrl
					}
					if bodyParentRepositoryUrl != "" {
						nestedParentRepository["url"] = bodyParentRepositoryUrl
					}
					if len(nestedParentRepository) > 0 {
						body["parentRepository"] = nestedParentRepository
					}
				}
				{
					nestedProject2 := map[string]any{}
					if bodyProject2Abbreviation != "" {
						nestedProject2["abbreviation"] = bodyProject2Abbreviation
					}
					if bodyProject2DefaultTeamImageUrl != "" {
						nestedProject2["defaultTeamImageUrl"] = bodyProject2DefaultTeamImageUrl
					}
					if bodyProject2Description != "" {
						nestedProject2["description"] = bodyProject2Description
					}
					if bodyProject2Id != "" {
						nestedProject2["id"] = bodyProject2Id
					}
					if bodyProject2LastUpdateTime != "" {
						nestedProject2["lastUpdateTime"] = bodyProject2LastUpdateTime
					}
					if bodyProject2Name != "" {
						nestedProject2["name"] = bodyProject2Name
					}
					if bodyProject2Revision != 0 {
						nestedProject2["revision"] = bodyProject2Revision
					}
					if bodyProject2State != "" {
						nestedProject2["state"] = bodyProject2State
					}
					if bodyProject2Url != "" {
						nestedProject2["url"] = bodyProject2Url
					}
					if bodyProject2Visibility != "" {
						nestedProject2["visibility"] = bodyProject2Visibility
					}
					if len(nestedProject2) > 0 {
						body["project"] = nestedProject2
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "git-apis", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "git-apis", data, "validRemoteUrls")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "git-apis", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "git-apis", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "git-apis", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "git-apis",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "git-apis", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "git-apis", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagSourceRef, "source-ref", "", "[optional] Specify the source refs to use while creating a fork repo")
	cmd.Flags().StringVar(&flagApiVersion, "api-version", "", "Version of the API to use. This should be set to '7.1' to use this version of the api.")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyParentRepositoryCollectionAvatarUrl, "parent-repository-collection-avatar-url", "", "Collection avatar Url.")
	cmd.Flags().StringVar(&bodyParentRepositoryCollectionId, "parent-repository-collection-id", "", "Collection Id.")
	cmd.Flags().StringVar(&bodyParentRepositoryCollectionName, "parent-repository-collection-name", "", "Collection Name.")
	cmd.Flags().StringVar(&bodyParentRepositoryCollectionUrl, "parent-repository-collection-url", "", "Collection REST Url.")
	cmd.Flags().StringVar(&bodyParentRepositoryId, "parent-repository-id", "", "Id")
	cmd.Flags().BoolVar(&bodyParentRepositoryIsFork, "parent-repository-is-fork", false, "True if the repository was created as a fork")
	cmd.Flags().StringVar(&bodyParentRepositoryName, "parent-repository-name", "", "Name")
	cmd.Flags().StringVar(&bodyParentRepositoryProjectAbbreviation, "parent-repository-project-abbreviation", "", "Project abbreviation.")
	cmd.Flags().StringVar(&bodyParentRepositoryProjectDefaultTeamImageUrl, "parent-repository-project-default-team-image-url", "", "Url to default team identity image.")
	cmd.Flags().StringVar(&bodyParentRepositoryProjectDescription, "parent-repository-project-description", "", "The project's description (if any).")
	cmd.Flags().StringVar(&bodyParentRepositoryProjectId, "parent-repository-project-id", "", "Project identifier.")
	cmd.Flags().StringVar(&bodyParentRepositoryProjectLastUpdateTime, "parent-repository-project-last-update-time", "", "Project last update time.")
	cmd.Flags().StringVar(&bodyParentRepositoryProjectName, "parent-repository-project-name", "", "Project name.")
	cmd.Flags().IntVar(&bodyParentRepositoryProjectRevision, "parent-repository-project-revision", 0, "Project revision.")
	cmd.Flags().StringVar(&bodyParentRepositoryProjectState, "parent-repository-project-state", "", "Project state.")
	cmd.Flags().StringVar(&bodyParentRepositoryProjectUrl, "parent-repository-project-url", "", "Url to the full version of the object.")
	cmd.Flags().StringVar(&bodyParentRepositoryProjectVisibility, "parent-repository-project-visibility", "", "Project visibility.")
	cmd.Flags().StringVar(&bodyParentRepositoryRemoteUrl, "parent-repository-remote-url", "", "Remote url")
	cmd.Flags().StringVar(&bodyParentRepositorySshUrl, "parent-repository-ssh-url", "", "Ssh url")
	cmd.Flags().StringVar(&bodyParentRepositoryUrl, "parent-repository-url", "", "Url")
	cmd.Flags().StringVar(&bodyProject2Abbreviation, "project-2-abbreviation", "", "Project abbreviation.")
	cmd.Flags().StringVar(&bodyProject2DefaultTeamImageUrl, "project-2-default-team-image-url", "", "Url to default team identity image.")
	cmd.Flags().StringVar(&bodyProject2Description, "project-2-description", "", "The project's description (if any).")
	cmd.Flags().StringVar(&bodyProject2Id, "project-2-id", "", "Project identifier.")
	cmd.Flags().StringVar(&bodyProject2LastUpdateTime, "project-2-last-update-time", "", "Project last update time.")
	cmd.Flags().StringVar(&bodyProject2Name, "project-2-name", "", "Project name.")
	cmd.Flags().IntVar(&bodyProject2Revision, "project-2-revision", 0, "Project revision.")
	cmd.Flags().StringVar(&bodyProject2State, "project-2-state", "", "Project state.")
	cmd.Flags().StringVar(&bodyProject2Url, "project-2-url", "", "Url to the full version of the object.")
	cmd.Flags().StringVar(&bodyProject2Visibility, "project-2-visibility", "", "Project visibility.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
