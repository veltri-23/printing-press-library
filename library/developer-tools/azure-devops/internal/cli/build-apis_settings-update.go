// Copyright 2026 davbebawwy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/developer-tools/azure-devops/internal/cliutil"
	"github.com/spf13/cobra"
)

func newBuildApisSettingsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagApiVersion string
	var bodyDaysToKeepDeletedBuildsBeforeDestroy int
	var bodyDefaultRetentionPolicyArtifactTypesToDelete string
	var bodyDefaultRetentionPolicyArtifacts string
	var bodyDefaultRetentionPolicyBranches string
	var bodyDefaultRetentionPolicyDaysToKeep int
	var bodyDefaultRetentionPolicyDeleteBuildRecord bool
	var bodyDefaultRetentionPolicyDeleteTestResults bool
	var bodyDefaultRetentionPolicyMinimumToKeep int
	var bodyMaximumRetentionPolicyArtifactTypesToDelete string
	var bodyMaximumRetentionPolicyArtifacts string
	var bodyMaximumRetentionPolicyBranches string
	var bodyMaximumRetentionPolicyDaysToKeep int
	var bodyMaximumRetentionPolicyDeleteBuildRecord bool
	var bodyMaximumRetentionPolicyDeleteTestResults bool
	var bodyMaximumRetentionPolicyMinimumToKeep int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "settings-update <organization> <project>",
		Short:       "Updates the build settings.",
		Example:     "  azure-devops-pp-cli build-apis settings-update example-value example-value --api-version example-value",
		Annotations: map[string]string{"pp:endpoint": "build-apis.settings-update", "pp:method": "PATCH", "pp:path": "/{organization}/{project}/_apis/build/settings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("api-version") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "api-version")
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/{organization}/{project}/_apis/build/settings"
			path = replacePathParam(path, "organization", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("project is required\nUsage: %s <%s>", cmd.CommandPath(), "project"))
			}
			path = replacePathParam(path, "project", args[1])
			params := map[string]string{}
			if flagApiVersion != "" {
				params["api-version"] = formatCLIParamValue(flagApiVersion)
			}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyDaysToKeepDeletedBuildsBeforeDestroy != 0 {
					body["daysToKeepDeletedBuildsBeforeDestroy"] = bodyDaysToKeepDeletedBuildsBeforeDestroy
				}
				{
					nestedDefaultRetentionPolicy := map[string]any{}
					if bodyDefaultRetentionPolicyArtifactTypesToDelete != "" {
						nestedDefaultRetentionPolicy["artifactTypesToDelete"] = cliutil.SplitCSV(bodyDefaultRetentionPolicyArtifactTypesToDelete)
					}
					if bodyDefaultRetentionPolicyArtifacts != "" {
						nestedDefaultRetentionPolicy["artifacts"] = cliutil.SplitCSV(bodyDefaultRetentionPolicyArtifacts)
					}
					if bodyDefaultRetentionPolicyBranches != "" {
						nestedDefaultRetentionPolicy["branches"] = cliutil.SplitCSV(bodyDefaultRetentionPolicyBranches)
					}
					if bodyDefaultRetentionPolicyDaysToKeep != 0 {
						nestedDefaultRetentionPolicy["daysToKeep"] = bodyDefaultRetentionPolicyDaysToKeep
					}
					if cmd.Flags().Changed("default-retention-policy-delete-build-record") {
						nestedDefaultRetentionPolicy["deleteBuildRecord"] = bodyDefaultRetentionPolicyDeleteBuildRecord
					}
					if cmd.Flags().Changed("default-retention-policy-delete-test-results") {
						nestedDefaultRetentionPolicy["deleteTestResults"] = bodyDefaultRetentionPolicyDeleteTestResults
					}
					if bodyDefaultRetentionPolicyMinimumToKeep != 0 {
						nestedDefaultRetentionPolicy["minimumToKeep"] = bodyDefaultRetentionPolicyMinimumToKeep
					}
					if len(nestedDefaultRetentionPolicy) > 0 {
						body["defaultRetentionPolicy"] = nestedDefaultRetentionPolicy
					}
				}
				{
					nestedMaximumRetentionPolicy := map[string]any{}
					if bodyMaximumRetentionPolicyArtifactTypesToDelete != "" {
						nestedMaximumRetentionPolicy["artifactTypesToDelete"] = cliutil.SplitCSV(bodyMaximumRetentionPolicyArtifactTypesToDelete)
					}
					if bodyMaximumRetentionPolicyArtifacts != "" {
						nestedMaximumRetentionPolicy["artifacts"] = cliutil.SplitCSV(bodyMaximumRetentionPolicyArtifacts)
					}
					if bodyMaximumRetentionPolicyBranches != "" {
						nestedMaximumRetentionPolicy["branches"] = cliutil.SplitCSV(bodyMaximumRetentionPolicyBranches)
					}
					if bodyMaximumRetentionPolicyDaysToKeep != 0 {
						nestedMaximumRetentionPolicy["daysToKeep"] = bodyMaximumRetentionPolicyDaysToKeep
					}
					if cmd.Flags().Changed("maximum-retention-policy-delete-build-record") {
						nestedMaximumRetentionPolicy["deleteBuildRecord"] = bodyMaximumRetentionPolicyDeleteBuildRecord
					}
					if cmd.Flags().Changed("maximum-retention-policy-delete-test-results") {
						nestedMaximumRetentionPolicy["deleteTestResults"] = bodyMaximumRetentionPolicyDeleteTestResults
					}
					if bodyMaximumRetentionPolicyMinimumToKeep != 0 {
						nestedMaximumRetentionPolicy["minimumToKeep"] = bodyMaximumRetentionPolicyMinimumToKeep
					}
					if len(nestedMaximumRetentionPolicy) > 0 {
						body["maximumRetentionPolicy"] = nestedMaximumRetentionPolicy
					}
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "build-apis", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "build-apis", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "build-apis", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "build-apis", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "build-apis", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "build-apis",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "build-apis", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "build-apis", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagApiVersion, "api-version", "", "Version of the API to use. This should be set to '7.1' to use this version of the api.")
	cmd.Flags().IntVar(&bodyDaysToKeepDeletedBuildsBeforeDestroy, "days-to-keep-deleted-builds-before-destroy", 0, "The number of days to keep records of deleted builds.")
	cmd.Flags().StringVar(&bodyDefaultRetentionPolicyArtifactTypesToDelete, "default-retention-policy-artifact-types-to-delete", "", "Artifact types to delete")
	cmd.Flags().StringVar(&bodyDefaultRetentionPolicyArtifacts, "default-retention-policy-artifacts", "", "Artifacts")
	cmd.Flags().StringVar(&bodyDefaultRetentionPolicyBranches, "default-retention-policy-branches", "", "Branches")
	cmd.Flags().IntVar(&bodyDefaultRetentionPolicyDaysToKeep, "default-retention-policy-days-to-keep", 0, "The number of days to keep builds.")
	cmd.Flags().BoolVar(&bodyDefaultRetentionPolicyDeleteBuildRecord, "default-retention-policy-delete-build-record", false, "Indicates whether the build record itself should be deleted.")
	cmd.Flags().BoolVar(&bodyDefaultRetentionPolicyDeleteTestResults, "default-retention-policy-delete-test-results", false, "Indicates whether to delete test results associated with the build.")
	cmd.Flags().IntVar(&bodyDefaultRetentionPolicyMinimumToKeep, "default-retention-policy-minimum-to-keep", 0, "The minimum number of builds to keep.")
	cmd.Flags().StringVar(&bodyMaximumRetentionPolicyArtifactTypesToDelete, "maximum-retention-policy-artifact-types-to-delete", "", "Artifact types to delete")
	cmd.Flags().StringVar(&bodyMaximumRetentionPolicyArtifacts, "maximum-retention-policy-artifacts", "", "Artifacts")
	cmd.Flags().StringVar(&bodyMaximumRetentionPolicyBranches, "maximum-retention-policy-branches", "", "Branches")
	cmd.Flags().IntVar(&bodyMaximumRetentionPolicyDaysToKeep, "maximum-retention-policy-days-to-keep", 0, "The number of days to keep builds.")
	cmd.Flags().BoolVar(&bodyMaximumRetentionPolicyDeleteBuildRecord, "maximum-retention-policy-delete-build-record", false, "Indicates whether the build record itself should be deleted.")
	cmd.Flags().BoolVar(&bodyMaximumRetentionPolicyDeleteTestResults, "maximum-retention-policy-delete-test-results", false, "Indicates whether to delete test results associated with the build.")
	cmd.Flags().IntVar(&bodyMaximumRetentionPolicyMinimumToKeep, "maximum-retention-policy-minimum-to-keep", 0, "The minimum number of builds to keep.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
