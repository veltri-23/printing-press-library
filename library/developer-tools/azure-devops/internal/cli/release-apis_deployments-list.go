// Copyright 2026 davbebawwy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newReleaseApisDeploymentsListCmd(flags *rootFlags) *cobra.Command {
	var flagDefinitionId string
	var flagDefinitionEnvironmentId string
	var flagCreatedBy string
	var flagMinModifiedTime string
	var flagMaxModifiedTime string
	var flagDeploymentStatus string
	var flagOperationStatus string
	var flagLatestAttemptsOnly bool
	var flagQueryOrder string
	var flagContinuationToken int
	var flagCreatedFor string
	var flagMinStartedTime string
	var flagMaxStartedTime string
	var flagSourceBranch string
	var flagApiVersion string

	cmd := &cobra.Command{
		Use:         "deployments-list <organization> <project>",
		Short:       "Deployments list",
		Example:     "  azure-devops-pp-cli release-apis deployments-list example-value example-value --api-version example-value",
		Annotations: map[string]string{"pp:endpoint": "release-apis.deployments-list", "pp:method": "GET", "pp:path": "https://vsrm.dev.azure.com/{organization}/{project}/_apis/release/deployments", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("api-version") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "api-version")
			}
			if cmd.Flags().Changed("deployment-status") {
				allowedDeploymentStatus := []string{"undefined", "notDeployed", "inProgress", "succeeded", "partiallySucceeded", "failed", "all"}
				validDeploymentStatus := false
				for _, v := range allowedDeploymentStatus {
					if flagDeploymentStatus == v {
						validDeploymentStatus = true
						break
					}
				}
				if !validDeploymentStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagDeploymentStatus, "deployment-status", allowedDeploymentStatus)
				}
			}
			if cmd.Flags().Changed("operation-status") {
				allowedOperationStatus := []string{"undefined", "queued", "scheduled", "pending", "approved", "rejected", "deferred", "queuedForAgent", "phaseInProgress", "phaseSucceeded", "phasePartiallySucceeded", "phaseFailed", "canceled", "phaseCanceled", "manualInterventionPending", "queuedForPipeline", "cancelling", "evaluatingGates", "gateFailed", "all"}
				validOperationStatus := false
				for _, v := range allowedOperationStatus {
					if flagOperationStatus == v {
						validOperationStatus = true
						break
					}
				}
				if !validOperationStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOperationStatus, "operation-status", allowedOperationStatus)
				}
			}
			if cmd.Flags().Changed("query-order") {
				allowedQueryOrder := []string{"descending", "ascending"}
				validQueryOrder := false
				for _, v := range allowedQueryOrder {
					if flagQueryOrder == v {
						validQueryOrder = true
						break
					}
				}
				if !validQueryOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagQueryOrder, "query-order", allowedQueryOrder)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "https://vsrm.dev.azure.com/{organization}/{project}/_apis/release/deployments"
			path = replacePathParam(path, "organization", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("project is required\nUsage: %s <%s>", cmd.CommandPath(), "project"))
			}
			path = replacePathParam(path, "project", args[1])
			params := map[string]string{}
			if flagDefinitionId != "" {
				params["definitionId"] = formatCLIParamValue(flagDefinitionId)
			}
			if flagDefinitionEnvironmentId != "" {
				params["definitionEnvironmentId"] = formatCLIParamValue(flagDefinitionEnvironmentId)
			}
			if flagCreatedBy != "" {
				params["createdBy"] = formatCLIParamValue(flagCreatedBy)
			}
			if flagMinModifiedTime != "" {
				params["minModifiedTime"] = formatCLIParamValue(flagMinModifiedTime)
			}
			if flagMaxModifiedTime != "" {
				params["maxModifiedTime"] = formatCLIParamValue(flagMaxModifiedTime)
			}
			if flagDeploymentStatus != "" {
				params["deploymentStatus"] = formatCLIParamValue(flagDeploymentStatus)
			}
			if flagOperationStatus != "" {
				params["operationStatus"] = formatCLIParamValue(flagOperationStatus)
			}
			if flagLatestAttemptsOnly != false {
				params["latestAttemptsOnly"] = formatCLIParamValue(flagLatestAttemptsOnly)
			}
			if flagQueryOrder != "" {
				params["queryOrder"] = formatCLIParamValue(flagQueryOrder)
			}
			if flagContinuationToken != 0 {
				params["continuationToken"] = formatCLIParamValue(flagContinuationToken)
			}
			if flagCreatedFor != "" {
				params["createdFor"] = formatCLIParamValue(flagCreatedFor)
			}
			if flagMinStartedTime != "" {
				params["minStartedTime"] = formatCLIParamValue(flagMinStartedTime)
			}
			if flagMaxStartedTime != "" {
				params["maxStartedTime"] = formatCLIParamValue(flagMaxStartedTime)
			}
			if flagSourceBranch != "" {
				params["sourceBranch"] = formatCLIParamValue(flagSourceBranch)
			}
			if flagApiVersion != "" {
				params["api-version"] = formatCLIParamValue(flagApiVersion)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "release-apis", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDefinitionId, "definition-id", "", "Definition id")
	cmd.Flags().StringVar(&flagDefinitionEnvironmentId, "definition-environment-id", "", "Definition environment id")
	cmd.Flags().StringVar(&flagCreatedBy, "created-by", "", "Created by")
	cmd.Flags().StringVar(&flagMinModifiedTime, "min-modified-time", "", "Min modified time")
	cmd.Flags().StringVar(&flagMaxModifiedTime, "max-modified-time", "", "Max modified time")
	cmd.Flags().StringVar(&flagDeploymentStatus, "deployment-status", "", "Deployment status (one of: undefined, notDeployed, inProgress, succeeded, partiallySucceeded, failed, all)")
	cmd.Flags().StringVar(&flagOperationStatus, "operation-status", "", "Operation status (one of: undefined, queued, scheduled, pending, approved, rejected, deferred, queuedForAgent, phaseInProgress, phaseSucceeded, phasePartiallySucceeded, phaseFailed, canceled, phaseCanceled, manualInterventionPending, queuedForPipeline, cancelling, evaluatingGates, gateFailed, all)")
	cmd.Flags().BoolVar(&flagLatestAttemptsOnly, "latest-attempts-only", false, "Latest attempts only")
	cmd.Flags().StringVar(&flagQueryOrder, "query-order", "", "Query order (one of: descending, ascending)")
	cmd.Flags().IntVar(&flagContinuationToken, "continuation-token", 0, "Continuation token")
	cmd.Flags().StringVar(&flagCreatedFor, "created-for", "", "Created for")
	cmd.Flags().StringVar(&flagMinStartedTime, "min-started-time", "", "Min started time")
	cmd.Flags().StringVar(&flagMaxStartedTime, "max-started-time", "", "Max started time")
	cmd.Flags().StringVar(&flagSourceBranch, "source-branch", "", "Source branch")
	cmd.Flags().StringVar(&flagApiVersion, "api-version", "", "Version of the API to use. This should be set to '7.1' to use this version of the api.")

	return cmd
}
