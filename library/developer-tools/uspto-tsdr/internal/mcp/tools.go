// Copyright 2026 H179922 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/uspto-tsdr/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/uspto-tsdr/internal/client"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/uspto-tsdr/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/uspto-tsdr/internal/config"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/uspto-tsdr/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/uspto-tsdr/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("case-multi-status_get-list",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/caseMultiStatus/sn?ids=78787878,76767676. Required: ids, type (default: sn). Optional: from, to. Returns the TransactionBag."),
			mcplib.WithString("from", mcplib.Description("from is one of the number types listed above")),
			mcplib.WithString("to", mcplib.Description("to is one of the number types listed above")),
			mcplib.WithString("ids", mcplib.Required(), mcplib.Description("can be serial numbers, ref numbers, reg numbers seperated by commas")),
			mcplib.WithString("type", mcplib.Description("type: * `sn` - serial number(s) * `rn` - registration number(s) * `ref` - reference number(s) * `ir` - Madrid number(s)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/caseMultiStatus/{type}", []mcpParamBinding{{PublicName: "from", WireName: "from", Location: "query"}, {PublicName: "to", WireName: "to", Location: "query"}, {PublicName: "ids", WireName: "ids", Location: "query"}, {PublicName: "type", WireName: "type", Location: "path"}}, []string{"type"}),
	)
	s.AddTool(
		mcplib.NewTool("casedoc_content-pdf_get-document",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casedoc/sn78787878/NOA20070926100613/content.pdf. Required: caseid, docid. Returns the Document."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and...")),
			mcplib.WithString("docid", mcplib.Required(), mcplib.Description("Auto generated Unique Document ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedoc/{caseid}/{docid}/content.pdf", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}, {PublicName: "docid", WireName: "docid", Location: "path"}}, []string{"caseid", "docid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedoc_content-zip_get-document",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casedoc/sn78787878/NOA20070926100613/content.zip. Required: caseid, docid. Returns the Document."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX...")),
			mcplib.WithString("docid", mcplib.Required(), mcplib.Description("Auto generated Unique Document ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedoc/{caseid}/{docid}/content.zip", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}, {PublicName: "docid", WireName: "docid", Location: "path"}}, []string{"caseid", "docid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedoc_download-pdf_get-document-content-pdf-download",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casedoc/sn78787878/NOA20070926100613/download.pdf. Required: caseid, docid. Returns the Document."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithString("docid", mcplib.Required(), mcplib.Description("Auto generated Unique Document ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedoc/{caseid}/{docid}/download.pdf", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}, {PublicName: "docid", WireName: "docid", Location: "path"}}, []string{"caseid", "docid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedoc_download-zip_get-document-content-zip-download",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casedoc/sn78787878/NOA20070926100613/download.zip. Required: caseid, docid. Returns the Document."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithString("docid", mcplib.Required(), mcplib.Description("Auto generated Unique Document ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedoc/{caseid}/{docid}/download.zip", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}, {PublicName: "docid", WireName: "docid", Location: "path"}}, []string{"caseid", "docid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedoc_info_get-document-xml",
			mcplib.WithDescription("Returns the metadata of a Document in XML for case identified by case ID. Required: caseid, docid."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithString("docid", mcplib.Required(), mcplib.Description("Auto generated Unique Document ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedoc/{caseid}/{docid}/info", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}, {PublicName: "docid", WireName: "docid", Location: "path"}}, []string{"caseid", "docid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedoc_media_get-page-content",
			mcplib.WithDescription("Returns the page content as a NATIVE format. Required: caseid, docid, pageid."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithString("docid", mcplib.Required(), mcplib.Description("Auto generated Unique Document ID")),
			mcplib.WithString("pageid", mcplib.Required(), mcplib.Description("Page Number")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedoc/{caseid}/{docid}/{pageid}/media", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}, {PublicName: "docid", WireName: "docid", Location: "path"}, {PublicName: "pageid", WireName: "pageid", Location: "path"}}, []string{"caseid", "docid", "pageid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedocs_get-bundle-info-pdf",
			mcplib.WithDescription("Digits can be entered in one of the first four parameters. rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Examples: https://tsdrapi.uspto.gov/ts/cd/casedocs/bundle.pdf?sn=75757575,78787878 or https://tsdrapi.uspto.gov/ts/cd/casedocs/bundle.pdf?sn=72131351,76515878&type=SPE Documents sent/received on Nov 30th, 2003 for Serial Number 72-131351 as a PDF https://tsdrapi.uspto.gov/ts/cd/casedocs/bundle.pdf?sn=72131351&date=2003-11-30 Note: exactly one of rn,sn,ref,ir must be specified, like sn in the examples. Optional: rn, sn, ref (plus 4 more)."),
			mcplib.WithString("rn", mcplib.Description("comma separated US registration nbr(s)")),
			mcplib.WithString("sn", mcplib.Description("comma separated US serial number(s)")),
			mcplib.WithString("ref", mcplib.Description("comma separated Madrid number(s)")),
			mcplib.WithString("ir", mcplib.Description("comma separated Madrid number(s)")),
			mcplib.WithString("category", mcplib.Description("set to RC to retrieve only registration certificate(s)")),
			mcplib.WithString("type", mcplib.Description("set to SPE to retrieve only specimens")),
			mcplib.WithString("date", mcplib.Description("specific send/receive date")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedocs/bundle.pdf", []mcpParamBinding{{PublicName: "rn", WireName: "rn", Location: "query"}, {PublicName: "sn", WireName: "sn", Location: "query"}, {PublicName: "ref", WireName: "ref", Location: "query"}, {PublicName: "ir", WireName: "ir", Location: "query"}, {PublicName: "category", WireName: "category", Location: "query"}, {PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "date", WireName: "date", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("casedocs_get-bundle-info-xml",
			mcplib.WithDescription("Digits can be entered in one of the first four parameters. rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Examples: https://tsdrapi.uspto.gov/ts/cd/casedocs/bundle.xml?sn=75757575,78787878 Metadata (in XML) about all documents for Serial Number 75008897 sent/received during 2006 https://tsdrapi.uspto.gov/ts/cd/casedocs/bundle.xml?sn=75008897&fromdate=2006-01-01&todate=2006-12-31 Metadata (in XML) about all documents for International Registration Number 0835690 sorted from earliest to latest https://tsdrapi.uspto.gov/ts/cd/casedocs/bundle.xml?ir=0835690&sort=date:A Note: exactly one of rn,sn,ref,ir must be specified, like sn or ir in the examples. Optional: rn, sn, ref (plus 4 more). Returns array of CasedocsGetBundleInfoXmlItem."),
			mcplib.WithString("rn", mcplib.Description("comma separated US registration nbr(s)")),
			mcplib.WithString("sn", mcplib.Description("comma separated US serial number(s)")),
			mcplib.WithString("ref", mcplib.Description("comma separated Madrid number(s)")),
			mcplib.WithString("ir", mcplib.Description("comma separated Madrid number(s)")),
			mcplib.WithString("fromdate", mcplib.Description("start of date range")),
			mcplib.WithString("todate", mcplib.Description("end of date range")),
			mcplib.WithString("sort", mcplib.Description("sort field:[A|D] for ascending or descending")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedocs/bundle.xml", []mcpParamBinding{{PublicName: "rn", WireName: "rn", Location: "query"}, {PublicName: "sn", WireName: "sn", Location: "query"}, {PublicName: "ref", WireName: "ref", Location: "query"}, {PublicName: "ir", WireName: "ir", Location: "query"}, {PublicName: "fromdate", WireName: "fromdate", Location: "query"}, {PublicName: "todate", WireName: "todate", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("casedocs_get-bundle-info-zip",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casedocs/bundle.zip?sn=75757575,78787878 Note: only one parameter can be specified, like sn in the example. Optional: rn, sn, ref (plus 1 more). Returns array of CasedocsGetBundleInfoZipItem."),
			mcplib.WithString("rn", mcplib.Description("comma separated US registration nbr(s)")),
			mcplib.WithString("sn", mcplib.Description("comma separated US serial number(s)")),
			mcplib.WithString("ref", mcplib.Description("comma separated Madrid number(s)")),
			mcplib.WithString("ir", mcplib.Description("comma separated Madrid number(s)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedocs/bundle.zip", []mcpParamBinding{{PublicName: "rn", WireName: "rn", Location: "query"}, {PublicName: "sn", WireName: "sn", Location: "query"}, {PublicName: "ref", WireName: "ref", Location: "query"}, {PublicName: "ir", WireName: "ir", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("casedocs_bundle_get-case-content-xml",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casedocs/sn78787878/bundle. Required: caseid. Returns array of BundleGetCaseContentXmlItem."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedocs/{caseid}/bundle", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedocs_content-pdf_get-case-docs-pdf",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casedocs/sn78787878/content.pdf. Required: caseid. Returns array of ContentPdfGetCaseDocsPdfItem."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedocs/{caseid}/content.pdf", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedocs_content-zip_get-case-docs-zip",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casedocs/sn78787878/content.zip. Required: caseid. Returns array of ContentZipGetCaseDocsZipItem."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedocs/{caseid}/content.zip", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedocs_download-pdf_get-case-docs-pdf-download",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casedocs/sn78787878/download.pdf. Required: caseid. Returns array of DownloadPdfGetCaseDocsPdfDownloadItem."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedocs/{caseid}/download.pdf", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedocs_download-zip_get-case-docs-zip-download",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casedocs/sn78787878/download.zip. Required: caseid. Returns array of DownloadZipGetCaseDocsZipDownloadItem."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedocs/{caseid}/download.zip", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casedocs_info_get-case-docs-xml",
			mcplib.WithDescription("Returns information about all documents in the case in XML format. Required: caseid."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casedocs/{caseid}/info", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casestatus_content-html_get-case-status-info",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casestatus/sn78787878/content.html. Required: caseid. Returns the Transaction."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casestatus/{caseid}/content.html", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casestatus_content-pdf_get-case-status-info",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casestatus/sn78787878/content.pdf. Required: caseid. Returns the Transaction."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casestatus/{caseid}/content.pdf", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casestatus_content-zip_get-case-status-info",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casestatus/sn78787878/content.zip. Required: caseid. Returns the Transaction."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casestatus/{caseid}/content.zip", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casestatus_download-pdf_get-case-status-info-pdf-download",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casestatus/sn78787878/download.pdf. Required: caseid. Returns the Transaction."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casestatus/{caseid}/download.pdf", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casestatus_download-zip_get-case-status-info-zip-download",
			mcplib.WithDescription("Parameters can be one of the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number, refXXXXXXXX and irXXXXXXX for Madrid numbers. Example: https://tsdrapi.uspto.gov/ts/cd/casestatus/sn78787878/download.zip. Required: caseid. Returns the Transaction."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casestatus/{caseid}/download.zip", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casestatus_info_load-xml",
			mcplib.WithDescription("Returns to the user the Trademark data for a specific serial number serialized into ST96 compliant XML. Required: caseid."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casestatus/{caseid}/info", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("casestatus_v1_load-old-xml",
			mcplib.WithDescription("Returns to the user the Trademark data for a specific serial number serialized into ST96 compliant XML. Required: caseid."),
			mcplib.WithString("caseid", mcplib.Required(), mcplib.Description("one the following: rnXXXXXXX for US registration number, snXXXXXXXX for US serial number,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/casestatus/{caseid}/v1/info", []mcpParamBinding{{PublicName: "caseid", WireName: "caseid", Location: "path"}}, []string{"caseid"}),
	)
	s.AddTool(
		mcplib.NewTool("raw-image_get-image",
			mcplib.WithDescription("Parameter is the digits only of the serial number, no leading sn. Example: https://tsdrapi.uspto.gov/ts/cd/rawImage/78787878. Required: serial_number."),
			mcplib.WithString("serial_number", mcplib.Required(), mcplib.Description("digits only")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/rawImage/{serial_number}", []mcpParamBinding{{PublicName: "serial_number", WireName: "serial_number", Location: "path"}}, []string{"serial_number"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export TSDR_APIKEY_HEADER=<your-key>" +
					"\n      Get a key at: https://account.uspto.gov/api-manager" +
					"\n      Run 'tsdr-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export TSDR_APIKEY_HEADER=<your-key>" +
					"\n      Get a key at: https://account.uspto.gov/api-manager" +
					"\n      Run 'tsdr-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export TSDR_APIKEY_HEADER=<your-key>" +
					"\n      Get a key at: https://account.uspto.gov/api-manager" +
					"\n      Run 'tsdr-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "tsdr-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "tsdr-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "tsdr",
		"description": "Beginning on October 2, 2020, you will need an API key to access the TSDR REST API See...",
		"archetype":   "content",
		"tool_count":  24,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion tsdr-pp-cli binary.",
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name":        "TSDR_APIKEY_HEADER",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"key_url": "https://account.uspto.gov/api-manager",
		},
		"resources": []map[string]any{
			{
				"name":        "case-multi-status",
				"description": "Manage case multi status",
				"endpoints":   []string{"get-list"},
				"searchable":  true,
			},
			{
				"name":        "casedoc",
				"description": "Manage casedoc",
				"endpoints":   []string{},
			},
			{
				"name":        "casedocs",
				"description": "Manage casedocs",
				"endpoints":   []string{"get-bundle-info-pdf", "get-bundle-info-xml", "get-bundle-info-zip"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "casestatus",
				"description": "Manage casestatus",
				"endpoints":   []string{},
			},
			{
				"name":        "raw-image",
				"description": "Manage raw image",
				"endpoints":   []string{"get-image"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Trademark One-Look Status", "command": "trademark status", "description": "Full current state of a trademark in one command — mark text, status, owner, classes, filing/registration dates,...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Prosecution Timeline", "command": "trademark timeline", "description": "Every prosecution event in chronological order — office actions, examiner reviews, publication events, and...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Deadline Calculator", "command": "trademark deadlines", "description": "Calculate Section 8, 9, and 15 maintenance deadlines with window-open dates and days-away countdown", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Portfolio Watch", "command": "trademark watch", "description": "Monitor multiple trademarks for status changes — caches previous statuses locally and flags any changes since last...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Batch Status Check", "command": "trademark batch", "description": "Batch status lookup for multiple trademarks using the multi-case endpoint or individual fallback with rate-limit...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Document Listing", "command": "trademark docs", "description": "List all documents in the prosecution file — office actions, responses, specimens, registration certificates —...", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Trademark One-Look Status", "insight": ""},
			{"topic": "Prosecution Timeline", "insight": ""},
			{"topic": "Deadline Calculator", "insight": ""},
			{"topic": "Portfolio Watch", "insight": ""},
			{"topic": "Batch Status Check", "insight": ""},
			{"topic": "Document Listing", "insight": ""},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
