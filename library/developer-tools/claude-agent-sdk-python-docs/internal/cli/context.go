// Copyright 2026 Nik and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"strings"

	"github.com/spf13/cobra"
)

func newNovelContextCmd(flags *rootFlags) *cobra.Command {
	var limit int

	cmd := &cobra.Command{
		Use:         "context <topic>",
		Short:       "Build a compact source-cited docs bundle for one SDK implementation task.",
		Long:        "Use this command to assemble a compact implementation bundle across pages, symbols, examples, and guides. Do NOT use it for raw page reading; use `read` instead.",
		Example:     "  claude-agent-sdk-python-docs-pp-cli context \"custom tools\" --agent --select sections.title,examples.code,citations.url",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// pp:data-source live
			if len(args) == 0 && cmd.Flags().NFlag() == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				fmt.Fprintln(cmd.OutOrStdout(), "would assemble Agent SDK docs context")
				return nil
			}
			if flags.dataSource == "local" {
				return usageErr(fmt.Errorf("--data-source local is not supported by context; omit it to query live Markdown"))
			}
			return runDocsContext(cmd, flags, strings.Join(args, " "), limit)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 5, "maximum sections and examples to include")
	return cmd
}
