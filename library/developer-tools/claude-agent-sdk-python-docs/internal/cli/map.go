// Copyright 2026 Nik and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelMapCmd(flags *rootFlags) *cobra.Command {
	var flagKind string

	cmd := &cobra.Command{
		Use:         "map",
		Short:       "Map functions, classes, types, options, tools, hooks, and message blocks by entity type.",
		Example:     "  claude-agent-sdk-python-docs-pp-cli map --kind classes,types,options --agent",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// pp:data-source live
			if dryRunOK(flags) {
				fmt.Fprintln(cmd.OutOrStdout(), "would map the documented SDK surface")
				return nil
			}
			if flags.dataSource == "local" {
				return usageErr(fmt.Errorf("--data-source local is not supported by map; omit it to query live Markdown"))
			}
			return runDocsMap(cmd, flags, flagKind)
		},
	}
	cmd.Flags().StringVar(&flagKind, "kind", "", "comma-separated entity kinds to include, such as classes,types,options")
	return cmd
}
