// Copyright 2026 Nik and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelVerifyCmd(flags *rootFlags) *cobra.Command {

	cmd := &cobra.Command{
		Use:         "verify <path>",
		Short:       "Check Python code against documented Claude Agent SDK symbols, signatures, and options.",
		Long:        "Use this command for checking local Python code against documented SDK symbols and signatures. Do NOT use it for reading symbol docs directly; use `symbol` instead.",
		Example:     "  claude-agent-sdk-python-docs-pp-cli verify ./src --agent",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// pp:data-source live
			if len(args) == 0 && cmd.Flags().NFlag() == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				fmt.Fprintln(cmd.OutOrStdout(), "would verify Python code against documented SDK symbols")
				return nil
			}
			if flags.dataSource == "local" {
				return usageErr(fmt.Errorf("--data-source local is not supported by verify; omit it to query live Markdown"))
			}
			if len(args) == 0 {
				_ = cmd.Usage()
				return usageErr(fmt.Errorf("path is required"))
			}
			return runDocsVerify(cmd, flags, args[0])
		},
	}
	return cmd
}
