// Copyright 2026 Nik and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelAuditLinksCmd(flags *rootFlags) *cobra.Command {

	cmd := &cobra.Command{
		Use:         "audit-links",
		Short:       "Validate internal anchors, guide links, and section references in the cached docs graph.",
		Example:     "  claude-agent-sdk-python-docs-pp-cli audit-links --agent",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// pp:data-source live
			if dryRunOK(flags) {
				fmt.Fprintln(cmd.OutOrStdout(), "would audit Agent SDK docs links")
				return nil
			}
			if flags.dataSource == "local" {
				return usageErr(fmt.Errorf("--data-source local is not supported by audit-links; omit it to query live Markdown"))
			}
			return runDocsAuditLinks(cmd, flags)
		},
	}
	return cmd
}
