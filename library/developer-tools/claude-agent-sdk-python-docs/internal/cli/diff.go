// Copyright 2026 Nik and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelDiffCmd(flags *rootFlags) *cobra.Command {
	var flagSince string

	cmd := &cobra.Command{
		Use:         "diff",
		Short:       "See which pages, sections, symbols, examples, and links changed between sync snapshots.",
		Example:     "  claude-agent-sdk-python-docs-pp-cli diff --since latest --agent",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// pp:data-source live
			if dryRunOK(flags) {
				fmt.Fprintln(cmd.OutOrStdout(), "would compare current docs hashes with a baseline")
				return nil
			}
			if flags.dataSource == "local" {
				return usageErr(fmt.Errorf("--data-source local is not supported by diff; omit it to query live Markdown"))
			}
			return runDocsDiff(cmd, flags, flagSince)
		},
	}
	cmd.Flags().StringVar(&flagSince, "since", "latest", "baseline identifier or label for the comparison")
	return cmd
}
