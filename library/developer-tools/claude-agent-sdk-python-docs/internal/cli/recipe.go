// Copyright 2026 Nik and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"strings"

	"github.com/spf13/cobra"
)

func newNovelRecipeCmd(flags *rootFlags) *cobra.Command {
	var limit int

	cmd := &cobra.Command{
		Use:         "recipe <topic>",
		Short:       "Compose a deterministic implementation scaffold from documented snippets and exact signatures.",
		Long:        "Use this command when the user wants a stitched implementation scaffold from existing documented snippets. Do NOT use it for listing raw snippets; use `examples` instead.",
		Example:     "  claude-agent-sdk-python-docs-pp-cli recipe \"streaming ClaudeSDKClient\" --agent",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// pp:data-source live
			if len(args) == 0 && cmd.Flags().NFlag() == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				fmt.Fprintln(cmd.OutOrStdout(), "would compose a docs-grounded SDK recipe")
				return nil
			}
			if flags.dataSource == "local" {
				return usageErr(fmt.Errorf("--data-source local is not supported by recipe; omit it to query live Markdown"))
			}
			return runDocsRecipe(cmd, flags, strings.Join(args, " "), limit)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "maximum examples to include")
	return cmd
}
