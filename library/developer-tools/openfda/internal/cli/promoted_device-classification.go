// Copyright 2026 H179922 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newDeviceClassificationPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagDeviceName string
	var flagDeviceClass string
	var flagMedicalSpecialty string
	var flagProductCode string
	var flagRegulationNumber string

	cmd := &cobra.Command{
		Use:         "device-classification",
		Short:       "Search device classifications",
		Long:        "Shortcut for 'device-classification list'. Search device classifications",
		Example:     "  openfda-pp-cli device-classification",
		Annotations: map[string]string{"pp:endpoint": "device-classification.list", "pp:method": "GET", "pp:path": "/device/classification.json", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/device/classification.json"
			params := map[string]string{}
			if flagDeviceName != "" {
				params["device_name"] = fmt.Sprintf("%v", flagDeviceName)
			}
			if flagDeviceClass != "" {
				params["device_class"] = fmt.Sprintf("%v", flagDeviceClass)
			}
			if flagMedicalSpecialty != "" {
				params["medical_specialty"] = fmt.Sprintf("%v", flagMedicalSpecialty)
			}
			if flagProductCode != "" {
				params["product_code"] = fmt.Sprintf("%v", flagProductCode)
			}
			if flagRegulationNumber != "" {
				params["regulation_number"] = fmt.Sprintf("%v", flagRegulationNumber)
			}
			params = buildOpenFDAParams(params)
			data, prov, err := resolveRead(cmd.Context(), c, flags, "device-classification", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDeviceName, "device", "", "Device name")
	cmd.Flags().StringVar(&flagDeviceClass, "class", "", "Device class: 1, 2, or 3")
	cmd.Flags().StringVar(&flagMedicalSpecialty, "specialty", "", "Medical specialty code")
	cmd.Flags().StringVar(&flagProductCode, "code", "", "Product code")
	cmd.Flags().StringVar(&flagRegulationNumber, "regulation", "", "CFR regulation number")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
