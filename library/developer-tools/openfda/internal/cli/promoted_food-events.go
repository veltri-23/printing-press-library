// Copyright 2026 H179922 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newFoodEventsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagProduct string
	var flagReaction string
	var flagOutcome string

	cmd := &cobra.Command{
		Use:         "food-events",
		Short:       "Search food/supplement adverse event reports",
		Long:        "Shortcut for 'food-events list'. Search food/supplement adverse event reports",
		Example:     "  openfda-pp-cli food-events",
		Annotations: map[string]string{"pp:endpoint": "food-events.list", "pp:method": "GET", "pp:path": "/food/event.json", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/food/event.json"
			params := map[string]string{}
			if flagProduct != "" {
				params["product"] = fmt.Sprintf("%v", flagProduct)
			}
			if flagReaction != "" {
				params["reaction"] = fmt.Sprintf("%v", flagReaction)
			}
			if flagOutcome != "" {
				params["outcome"] = fmt.Sprintf("%v", flagOutcome)
			}
			params = buildOpenFDAParams(params)
			data, prov, err := resolveRead(cmd.Context(), c, flags, "food-events", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagProduct, "product", "", "Product name or industry name")
	cmd.Flags().StringVar(&flagReaction, "reaction", "", "Reported reaction/symptom")
	cmd.Flags().StringVar(&flagOutcome, "outcome", "", "Outcome (hospitalization, ER visit, etc.)")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
