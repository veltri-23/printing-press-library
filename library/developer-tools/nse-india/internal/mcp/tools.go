// Copyright 2026 Mayank Lavania and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/nse-india/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/nse-india/internal/client"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/nse-india/internal/config"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/nse-india/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/nse-india/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("corporate_actions",
			mcplib.WithDescription("Corporate actions history — dividends, bonuses, splits, rights with ex-dates and record dates. Required: symbol. Optional: index (default: equities)."),
			mcplib.WithString("index", mcplib.Description("")),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("NSE stock symbol to fetch corporate actions for")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/corporates-corporateActions", []mcpParamBinding{{PublicName: "index", WireName: "index", Location: "query"}, {PublicName: "symbol", WireName: "symbol", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("corporate_announcements",
			mcplib.WithDescription("Exchange filings — board meetings, results, AGM, investor meets, disclosure intimations. Required: symbol. Optional: index (default: equities)."),
			mcplib.WithString("index", mcplib.Description("")),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("NSE stock symbol to fetch announcements for")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/corporate-announcements", []mcpParamBinding{{PublicName: "index", WireName: "index", Location: "query"}, {PublicName: "symbol", WireName: "symbol", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("corporate_annual_reports",
			mcplib.WithDescription("Annual reports with direct PDF download URLs, going back multiple years. Required: symbol. Optional: index (default: equities)."),
			mcplib.WithString("index", mcplib.Description("")),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("NSE stock symbol to fetch annual reports for")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/annual-reports", []mcpParamBinding{{PublicName: "index", WireName: "index", Location: "query"}, {PublicName: "symbol", WireName: "symbol", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("corporate_financial_results",
			mcplib.WithDescription("Quarterly and annual financial result filings with XBRL links. Required: symbol. Optional: index (default: equities), period (default: Quarterly)."),
			mcplib.WithString("index", mcplib.Description("")),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("NSE stock symbol to fetch financial results for")),
			mcplib.WithString("period", mcplib.Description("Period type: 'Quarterly' or 'Annual'")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/corporates-financial-results", []mcpParamBinding{{PublicName: "index", WireName: "index", Location: "query"}, {PublicName: "symbol", WireName: "symbol", Location: "query"}, {PublicName: "period", WireName: "period", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("corporate_insider_trading",
			mcplib.WithDescription("SEBI PIT (Prohibition of Insider Trading) disclosures — promoter/director buy/sell with quantities, values, pre/post holding %. Required: symbol. Optional: issuer, from, to."),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("NSE stock symbol to fetch insider trading disclosures for")),
			mcplib.WithString("issuer", mcplib.Description("")),
			mcplib.WithString("from", mcplib.Description("Start date (DD-MM-YYYY format)")),
			mcplib.WithString("to", mcplib.Description("End date (DD-MM-YYYY format)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/corporates-pit", []mcpParamBinding{{PublicName: "symbol", WireName: "symbol", Location: "query"}, {PublicName: "issuer", WireName: "issuer", Location: "query"}, {PublicName: "from", WireName: "from", Location: "query"}, {PublicName: "to", WireName: "to", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("equity_derivatives",
			mcplib.WithDescription("F&O data — futures (3 expiries with OI, volume, turnover) and options contracts (CE/PE by strike and expiry). Required: functionName (default: getSymbolDerivativesData), symbol. Optional: marketType (default: N), series (default: EQ)."),
			mcplib.WithString("functionName", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("marketType", mcplib.Description("")),
			mcplib.WithString("series", mcplib.Description("")),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("NSE stock symbol eligible for F&O (e.g. ADANIPORTS, RELIANCE, NIFTY)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/NextApi/apiClient/GetQuoteApi", []mcpParamBinding{{PublicName: "functionName", WireName: "functionName", Location: "query"}, {PublicName: "marketType", WireName: "marketType", Location: "query"}, {PublicName: "series", WireName: "series", Location: "query"}, {PublicName: "symbol", WireName: "symbol", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("equity_quote",
			mcplib.WithDescription("Full equity quote — last price, 52w H/L, sector PE, order book (5 bid/ask levels), delivery%, VaR margin, index memberships, pre-open IEP. Required: functionName (default: getSymbolData), symbol. Optional: marketType (default: N), series (default: EQ)."),
			mcplib.WithString("functionName", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("marketType", mcplib.Description("")),
			mcplib.WithString("series", mcplib.Description("")),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("NSE stock symbol (e.g. INFY, ADANIPORTS, RELIANCE)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/NextApi/apiClient/GetQuoteApi", []mcpParamBinding{{PublicName: "functionName", WireName: "functionName", Location: "query"}, {PublicName: "marketType", WireName: "marketType", Location: "query"}, {PublicName: "series", WireName: "series", Location: "query"}, {PublicName: "symbol", WireName: "symbol", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("indices_constituents",
			mcplib.WithDescription("Live prices for all constituent stocks in an index with 52w range, 1Y/30D% change. Required: index."),
			mcplib.WithString("index", mcplib.Required(), mcplib.Description("Index name (e.g. 'NIFTY 50', 'NIFTY BANK', 'NIFTY MIDCAP 100', 'NIFTY IT')")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/equity-stockIndices", []mcpParamBinding{{PublicName: "index", WireName: "index", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("indices_list",
			mcplib.WithDescription("List all available NSE indices with short and long names."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/index-names", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("market_status",
			mcplib.WithDescription("Real-time market status for all segments (Capital Market, Currency, F&O, WDM) with current NIFTY level."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/marketStatus", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("movers_active",
			mcplib.WithDescription("Most active intraday securities ranked by volume or traded value. Optional: index (default: INTRADAY), by (default: volume)."),
			mcplib.WithString("index", mcplib.Description("")),
			mcplib.WithString("by", mcplib.Description("Sort by 'volume' or 'value'")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/live-analysis-most-active-securities", []mcpParamBinding{{PublicName: "index", WireName: "index", Location: "query"}, {PublicName: "by", WireName: "key", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("symbol_lookup_autocomplete",
			mcplib.WithDescription("Search symbols by company name or ticker — returns equity, MF, index matches. Required: query."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Stock name or partial symbol to search (e.g. 'ADANI', 'Infosys')")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/search/autocomplete", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}}, []string{}),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT only). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      Run 'nse-india-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      Run 'nse-india-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "nse-india-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "nse-india-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	// Block write operations
	upper := strings.ToUpper(strings.TrimSpace(query))
	for _, prefix := range []string{"INSERT", "UPDATE", "DELETE", "DROP", "ALTER", "CREATE"} {
		if strings.HasPrefix(upper, prefix) {
			return mcplib.NewToolResultError("only SELECT queries are allowed"), nil
		}
	}

	db, err := store.OpenWithContext(ctx, dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "NSE India",
		"description": "Every NSE equity data point in a single Go binary — live quotes, order book, filings, and a local SQLite store no...",
		"archetype":   "generic",
		"tool_count":  12,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion nse-india-pp-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "corporate",
				"description": "Corporate actions, filings, and financial data",
				"endpoints":   []string{"actions", "announcements", "annual_reports", "financial_results", "insider_trading"},
			},
			{
				"name":        "equity",
				"description": "Comprehensive equity quote data for NSE-listed stocks",
				"endpoints":   []string{"derivatives", "quote"},
			},
			{
				"name":        "indices",
				"description": "NSE index data and constituents",
				"endpoints":   []string{"constituents", "list"},
				"syncable":    true,
			},
			{
				"name":        "market",
				"description": "Market status and operational information",
				"endpoints":   []string{"status"},
			},
			{
				"name":        "movers",
				"description": "Market activity rankings",
				"endpoints":   []string{"active"},
			},
			{
				"name":        "symbol_lookup",
				"description": "Symbol search and autocomplete",
				"endpoints":   []string{"autocomplete"},
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Delivery Spike Detector", "command": "delivery-spike", "description": "Flags stocks where today's delivery-to-traded ratio is significantly above their 20-session rolling average —...", "rationale": "Requires a rolling time series of delivery% in SQLite; a single API call gives one day with no baseline.", "via": "mcp-command-mirror"},
			{"name": "Pre-Market IEP Drift Analyzer", "command": "iep-drift", "description": "Tracks how accurately each stock's pre-market IEP (Indicative Equilibrium Price) predicts the actual opening price...", "rationale": "Requires pre-market IEP and actual open price stored in the same SQLite table across sessions — no single API call...", "via": "mcp-command-mirror"},
			{"name": "Announcement Flood Detector", "command": "announcement-flood", "description": "Surfaces companies whose filing cadence has spiked above their own historical baseline — a leading indicator of...", "rationale": "Compares current filing count against each company's rolling weekly baseline; requires announcement history across...", "via": "mcp-command-mirror"},
			{"name": "Sector Breadth Analyzer", "command": "sector-breadth", "description": "Computes advance/decline ratio, median pChange, and delivery breadth for every constituent of a named index — far...", "rationale": "Two-table join: index_constituents × daily_quotes; impossible from any single NSE API call.", "via": "mcp-command-mirror"},
			{"name": "Index Basket Contributor", "command": "index-driver", "description": "Decomposes any day's index move into per-stock point contributions — identifies the 3-5 stocks driving 80%+ of the...", "rationale": "Attribution joins index constituent weights with daily pChange; the weighted sum decomposition requires data only...", "via": "mcp-command-mirror"},
			{"name": "Portfolio P&L Tracker", "command": "portfolio pnl", "description": "Tracks unrealized P&L, daily delta, and drawdown for a personal holdings file against the synced quote history.", "rationale": "Holdings file × quote history join across dates; reconstructs portfolio value curve from SQLite — no brokerage...", "via": "mcp-command-mirror"},
			{"name": "Portfolio Margin Health Dashboard", "command": "portfolio margin-health", "description": "Aggregates VaR margin, extreme loss margin, and adhoc margin across a full portfolio — shows total margin-at-risk...", "rationale": "SUM(var_margin × qty × price) across all holdings in a single query; requires multi-symbol join impossible from...", "via": "mcp-command-mirror"},
			{"name": "Delivery-Price Divergence Scanner", "command": "delivery-divergence", "description": "Detects when price is rising but delivery % is falling (distribution signal) or price falling while delivery is...", "rationale": "CORR(pChange, delivery_pct) over 10 sessions per symbol; correlation pattern is only visible with a joint time...", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Delivery Spike Detector", "insight": "Requires a rolling time series of delivery% in SQLite; a single API call gives one day with no baseline."},
			{"topic": "Pre-Market IEP Drift Analyzer", "insight": "Requires pre-market IEP and actual open price stored in the same SQLite table across sessions — no single API call can produce this comparison."},
			{"topic": "Announcement Flood Detector", "insight": "Compares current filing count against each company's rolling weekly baseline; requires announcement history across weeks in SQLite."},
			{"topic": "Sector Breadth Analyzer", "insight": "Two-table join: index_constituents × daily_quotes; impossible from any single NSE API call."},
			{"topic": "Index Basket Contributor", "insight": "Attribution joins index constituent weights with daily pChange; the weighted sum decomposition requires data only available after a sync populates both tables."},
			{"topic": "Portfolio P&L Tracker", "insight": "Holdings file × quote history join across dates; reconstructs portfolio value curve from SQLite — no brokerage API needed."},
			{"topic": "Portfolio Margin Health Dashboard", "insight": "SUM(var_margin × qty × price) across all holdings in a single query; requires multi-symbol join impossible from serial API calls."},
			{"topic": "Delivery-Price Divergence Scanner", "insight": "CORR(pChange, delivery_pct) over 10 sessions per symbol; correlation pattern is only visible with a joint time series in SQLite."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
