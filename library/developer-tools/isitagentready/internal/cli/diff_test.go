// Copyright 2026 bobe and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelDiffHelpWires smoke-tests that the diff command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelDiffHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"diff", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("diff --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelDiffBehavior is the placeholder for table-driven tests of
// the diff command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelDiffBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
//
// Behavior is covered by TestDiffBehavior in novel_behavior_test.go
// (two-scan transition table). This verifies wiring and flags.
func TestNovelDiffBehavior(t *testing.T) {
	cmd, _, err := RootCmd().Find([]string{"diff"})
	if err != nil || cmd.Name() != "diff" {
		t.Fatalf("diff command not resolvable: %v", err)
	}
	if cmd.Flags().Lookup("all") == nil {
		t.Fatal("diff is missing --all")
	}
}
