// Copyright 2026 bobe and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelHistoryHelpWires smoke-tests that the history command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelHistoryHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"history", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("history --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelHistoryBehavior is the placeholder for table-driven tests of
// the history command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelHistoryBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
//
// Behavior is covered by TestHistoryBehavior in novel_behavior_test.go
// (timeline + flip detection). This verifies wiring and flags.
func TestNovelHistoryBehavior(t *testing.T) {
	cmd, _, err := RootCmd().Find([]string{"history"})
	if err != nil || cmd.Name() != "history" {
		t.Fatalf("history command not resolvable: %v", err)
	}
	if cmd.Flags().Lookup("limit") == nil || cmd.Flags().Lookup("check") == nil {
		t.Fatal("history is missing --limit or --check")
	}
}
