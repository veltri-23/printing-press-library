// Copyright 2026 bobe and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelOpenAdviceHelpWires smoke-tests that the open-advice command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelOpenAdviceHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"open-advice", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("open-advice --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelOpenAdviceBehavior is the placeholder for table-driven tests of
// the open-advice command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelOpenAdviceBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
//
// Behavior is covered by TestOpenAdviceBehavior in novel_behavior_test.go
// (cross-site backlog + --check filter). This verifies wiring and flags.
func TestNovelOpenAdviceBehavior(t *testing.T) {
	cmd, _, err := RootCmd().Find([]string{"open-advice"})
	if err != nil || cmd.Name() != "open-advice" {
		t.Fatalf("open-advice command not resolvable: %v", err)
	}
	if cmd.Flags().Lookup("site") == nil || cmd.Flags().Lookup("check") == nil {
		t.Fatal("open-advice is missing --site or --check")
	}
}
