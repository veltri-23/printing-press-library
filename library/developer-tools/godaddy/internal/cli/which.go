// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "abuse create-ticket", Description: "Create a new abuse ticket", Group: "abuse"},
	{Command: "abuse create-ticket-v2", Description: "Create a new abuse ticket", Group: "abuse"},
	{Command: "abuse get-ticket-info", Description: "Return the abuse ticket data for a given ticket id", Group: "abuse"},
	{Command: "abuse get-ticket-info-v2", Description: "Return the abuse ticket data for a given ticket id", Group: "abuse"},
	{Command: "abuse get-tickets", Description: "List all abuse tickets ids that match user provided filters", Group: "abuse"},
	{Command: "abuse get-tickets-v2", Description: "List all abuse tickets ids that match user provided filters", Group: "abuse"},
	{Command: "aftermarket add-expiry-listings", Description: "Add expiry listings into GoDaddy Auction", Group: "aftermarket"},
	{Command: "aftermarket delete-listings", Description: "Remove listings from GoDaddy Auction", Group: "aftermarket"},
	{Command: "agents get", Description: "Retrieves detailed information about a registered agent", Group: "agents"},
	{Command: "agents get-events", Description: "Returns a paginated, strictly ordered list of ANS events. When providerId is omitted, the API returns events for all providers; when providerId is provided, only events associated with that provider are returned. Pagination is driven by an opaque cursor token returned in each response.", Group: "agents"},
	{Command: "agents register", Description: "Registers a new agent with the Agent Name Service. Supports three registration flows:\n1. GoDaddy domains with CSRs (synchronous) - Returns 202 immediately with wait instruction\n2. External domains with CSRs (async ACME) or BYOC - Returns 202 with validation requirements\n   Note: BYOC is permitted for server certificates only. Identity certificates are ALWAYS issued by the RA.\n\nThe Registration Authority (RA) validates the agent's identity and submitted\ninformation, then interacts with the CA to issue certificates or validates\nprovided server certificates.", Group: "agents"},
	{Command: "agents resolve-ansname", Description: "Resolves an ANSName to an actionable endpoint reference. The query includes\nthe ANSName of the target agent and can optionally incorporate capability\nfilters to refine the search. The ANS service queries the Agent Registry,\nwhich returns a digitally signed endpoint record if found.", Group: "agents"},
	{Command: "agents search-ansname", Description: "Searches the Agent Name Service registry using flexible criteria such as\npartial agent names, agent host domains, and version ranges. The search\ncan return multiple matching agents along with their metadata and endpoints.\nResults are paginated to handle large datasets efficiently.", Group: "agents"},
	{Command: "agents certificates cancel-server-renewal", Description: "Cancels the pending server certificate renewal for this agent.\n\nUse cases:\n- Client submitted incorrect CSR and wants to retry\n- Client wants to switch from CSR path to BYOC path (or vice versa)\n- Client no longer wishes to complete the renewal\n\nSide effects:\n- If renewal type is SERVER_CSR, marks the associated CSR as REJECTED\n- Removes pending renewal from database\n- Client can immediately submit a new renewal request", Group: "agents"},
	{Command: "agents certificates get-agent-identity-by-agent-id", Description: "Retrieves all identity certificates for the specified agent", Group: "agents"},
	{Command: "agents certificates get-agent-server-by-agent-id", Description: "Retrieves all TLS server certificates for the specified agent", Group: "agents"},
	{Command: "agents certificates get-server-renewal-status", Description: "Returns current renewal status if one exists.\nUsed for:\n- Checking if ACME verification is complete\n- Polling for certificate issuance (CSR path)\n- Retrieving challenges if client lost the POST response\n- Getting TLSA record after completion", Group: "agents"},
	{Command: "agents certificates submit-agent-identity-csr-by-agent-id", Description: "Submits a Certificate Signing Request (CSR) for the agent's identity certificate.\nThe response contains a \"csrId\", that is going to match the same field from CertificateResponse.", Group: "agents"},
	{Command: "agents certificates submit-server-renewal", Description: "Initiates server certificate renewal. Returns ACME challenges; the caller must verify domain control via POST verify-acme.\n\nSupports two paths:\n- CSR path: RA issues a new certificate.\n- BYOC path: Client provides a certificate; RA validates and stores it.\n\nOnly one pending renewal is allowed per agent (409 if one already exists).", Group: "agents"},
	{Command: "agents certificates verify-acme-challenges", Description: "Triggers ACME validation for a pending server certificate renewal.\n\nVerifies:\n- DNS-01 challenge (TXT record at _acme-challenge.{agentHost})\n- HTTP-01 challenge (file at /.well-known/acme-challenge/{token})\n\nResponse depends on renewal type:\n- SERVER_CSR: Returns 202 (asynchronous certificate issuance).\n- SERVER_BYOC: Returns 200 (certificate stored; TLSA record ready for DNS update).", Group: "agents"},
	{Command: "agents csrs get-agent-status-by-agent-id", Description: "Retrieves the current status of a Certificate Signing Request (CSR).\nThis endpoint allows clients to check if a CSR has been signed, is still pending,\nor has been rejected. The failureReason field provides additional context when\na CSR is rejected.", Group: "agents"},
	{Command: "agents revoke agent", Description: "Revokes an active agent or cancels a pending registration.\n\nFor ACTIVE agents: Revokes the agent due to key compromise, decommissioning,\nor other security reasons. The certificate is added to the Certificate\nRevocation List (CRL) and the agent's entry in the registry is flagged.\n\nFor PENDING registrations (PENDING_CERTS or PENDING_DNS status): Cancels\nthe registration attempt after domain validation has been completed. This\nwill cancel any pending certificate issuance jobs and revoke any already-issued\ncertificates.\n\nNote: Registrations in PENDING_VALIDATION status (pre-ACME verification)\nare not cancellable via this API and will automatically expire if the\nACME verification is not completed within specified timeframe.", Group: "agents"},
	{Command: "agents verify-acme validate-registration", Description: "Initiates validation of domain control. The AHP calls this after placing\nthe ACME challenge token at the specified location (DNS or HTTP).\n\nThe RA validates domain control, which is required before issuing both\nserver and identity certificates. A single domain validation is used\nfor both certificates.\n\nThe RA will automatically determine which validation method to use based\non the registration configuration and what is discoverable.", Group: "agents"},
	{Command: "agents verify-dns records", Description: "Verifies that all required DNS records have been configured correctly.\nThe RA knows which records are required based on the registration\nand will check for all four required records (HTTPS, TLSA, _ans, _ra-badge).\nThis is the final step for external domain registration.", Group: "agents"},
	{Command: "agreements get", Description: "Retrieve Legal Agreements for provided agreements keys", Group: "agreements"},
	{Command: "api-customers certificates get-acme-external-account-binding", Description: "Use this endpoint to retrieve a key identifier and Hash-based Message Authentication Code (HMAC) key for Automated Certificate Management Environment (ACME) External Account Binding (EAB). These credentials can be used with an ACME client that supports EAB (ex. CertBot) to automate the issuance request and deployment of DV SSL certificates", Group: "api-customers"},
	{Command: "api-customers certificates get-customer-by-customer-id", Description: "This method can be used to retrieve a list of certificates for a specified customer. <ul><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "api-customers"},
	{Command: "api-customers certificates get-detail-by-cert-identifier", Description: "Once the certificate order has been created, this method can be used to check the status of the certificate. This method can also be used to retrieve details of the certificate. <ul><li>**shopperId** is **not the same** as **customerId**. **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "api-customers"},
	{Command: "api-customers certificates get-domain-details-by-domain", Description: "Retrieve detailed information for supplied domain, including domain verification details and Certificate Authority Authorization (CAA) verification details. <ul><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "api-customers"},
	{Command: "api-customers certificates get-domain-information-by-id", Description: "This method can be used to retrieve the domain verification status for a certificate request.<ul><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>\"", Group: "api-customers"},
	{Command: "auctions-aftermarket place-bids", Description: "Places multiple bids with a single request.", Group: "auctions-aftermarket"},
	{Command: "certificates create", Description: "<p>Creating a certificate order can be a long running asynchronous operation in the PKI workflow. The PKI API supports 2 options for getting the completion stateful actions for this asynchronous operations: 1) by polling operations -- see /v1/certificates/{certificateId}/actions 2) via WebHook style callback -- see '/v1/certificates/{certificateId}/callback'.</p>", Group: "certificates"},
	{Command: "certificates create-endpoint", Description: "<p>Creating a certificate order for a subscription can be a long running asynchronous operation in the PKI workflow. The PKI API supports 2 options for getting the completion stateful actions for this asynchronous operations: 1) by polling operations -- see /v1/certificates/{certificateId}/actions 2) via WebHook style callback -- see '/v1/certificates/{certificateId}/callback'.</p>", Group: "certificates"},
	{Command: "certificates download-entitlement", Description: "Download certificate by entitlement", Group: "certificates"},
	{Command: "certificates get", Description: "Once the certificate order has been created, this method can be used to check the status of the certificate. This method can also be used to retrieve details of the certificate.", Group: "certificates"},
	{Command: "certificates get-entitlement", Description: "Once the certificate order has been created, this method can be used to check the status of the certificate. This method can also be used to retrieve details of the certificates associated to an entitlement.", Group: "certificates"},
	{Command: "certificates retrieve-ssl-by-domain-reseller", Description: "The pagination starts at page 1. Each page contains a page of *subscriptions*, not certificates. This endpoint is meant for paging the subscriptions under the authorized user's account. Each subscription contains a snapshot of certificates contained within the subscription. To fetch further certificates under a subscription, use the /v2/certificates/subscription/{guid} endpoint with the subscription GUID obtained from this call. If any filtering is applied, subscriptions without any certificates will be omitted.", Group: "certificates"},
	{Command: "certificates retrieve-ssl-by-domain-subscription-reseller", Description: "GET a page of certificates for a specific domain product", Group: "certificates"},
	{Command: "certificates validate", Description: "Validate a pending order for certificate", Group: "certificates"},
	{Command: "certificates actions certificate-retrieve", Description: "This method is used to retrieve all stateful actions relating to a certificate lifecycle.", Group: "certificates"},
	{Command: "certificates callback certificate-delete", Description: "Unregister the callback for a particular certificate.", Group: "certificates"},
	{Command: "certificates callback certificate-get", Description: "This method is used to retrieve the registered callback url for a certificate.", Group: "certificates"},
	{Command: "certificates callback certificate-replace", Description: "This method is used to register/replace url for callbacks for stateful actions relating to a certificate lifecycle. The callback url is a Webhook style pattern and will receive POST http requests with json body defined in the CertificateAction model definition for each certificate action.  Only one callback URL is allowed to be registered for each certificateId, so it will replace a previous registration.", Group: "certificates"},
	{Command: "certificates cancel certificate", Description: "Use the cancel call to cancel a pending certificate order.", Group: "certificates"},
	{Command: "certificates download certificate", Description: "Download certificate", Group: "certificates"},
	{Command: "certificates email certificate-alternate-address", Description: "This method adds an alternate email address to a certificate order and re-sends all existing request emails to that address.", Group: "certificates"},
	{Command: "certificates email certificate-history", Description: "This method can be used to retrieve all emails sent for a certificate.", Group: "certificates"},
	{Command: "certificates email certificate-resend", Description: "This method can be used to resend emails by providing the certificate id and the email id", Group: "certificates"},
	{Command: "certificates email certificate-resend-address", Description: "This method can be used to resend emails by providing the certificate id, the email id, and the recipient email address", Group: "certificates"},
	{Command: "certificates reissue certificate", Description: "<p>Rekeying is the process by which the private and public key is changed for a certificate. It is a simplified reissue,where only the CSR is changed. Reissuing is the process by which domain names are added or removed from a certificate.Once a request is validated and approved, the certificate will be reissued with the new common name and sans specified. Unlimited reissues are available during the lifetime of the certificate.New names added to a certificate that do not share the base domain of the common name may take additional time to validate. If this API call is made before a previous pending reissue has been validated and issued, the previous reissue request is automatically rejected and replaced with the current request.</p>", Group: "certificates"},
	{Command: "certificates reissue certificate-certificates", Description: "<p>Rekeying is the process by which the private and public key is changed for a certificate. It is a simplified reissue,where only the CSR is changed. Reissue extends validity of the existing certificate by requesting a new certificate with all the same values as existing issued certificate. Once a request is validated and approved, the certificate will be reissued with the same common name and sans specified from existing certificate. Unlimited reissues are available during the lifetime of the certificate.If this API call is made before a previous pending reissue has been validated and issued, the previous reissue request is automatically rejected and replaced with the current request.</p>", Group: "certificates"},
	{Command: "certificates renew certificate", Description: "Renewal is the process by which the validity of a certificate is extended. Renewal is only available 60 days prior to expiration of the previous certificate and 30 days after the expiration of the previous certificate. The renewal supports modifying a set of the original certificate order information. Once a request is validated and approved, the certificate will be issued with extended validity. Since subject alternative names can be removed during a renewal, we require that you provide the subject alternative names you expect in the renewed certificate. New names added to a certificate that do not share the base domain of the common name may take additional time to validate. </p>", Group: "certificates"},
	{Command: "certificates revoke certificate", Description: "Use revoke call to revoke an active certificate, if the certificate has not been issued a 404 response will be returned.", Group: "certificates"},
	{Command: "certificates site-seal certificate-get", Description: "<p>This method is used to obtain the site seal information for an issued certificate. A site seal is a graphic that the certificate purchaser can embed on their web site to show their visitors information about their SSL certificate. If a web site visitor clicks on the site seal image, a pop-up page is displayed that contains detailed information about the SSL certificate. The site seal token is used to link the site seal graphic image to the appropriate certificate details pop-up page display when a user clicks on the site seal. The site seal images are expected to be static images and hosted on the reseller's website, to minimize delays for customer page load times.</p>", Group: "certificates"},
	{Command: "certificates verify-domain-control certificate", Description: "Domain control is a means for verifying the domain included in the certificate order. This resource is useful for resellers that control the domains for their customers, and can expedite the verification process. See https://www.godaddy.com/help/verifying-your-domain-ownership-for-ssl-certificate-requests-html-or-dns-7452", Group: "certificates"},
	{Command: "countries get", Description: "Retrieves summary country information for the provided marketId and filters", Group: "countries"},
	{Command: "countries get-country", Description: "Retrieves country and summary state information for provided countryKey", Group: "countries"},
	{Command: "customers auctions get-listings", Description: "Get listings from GoDaddy Auctions", Group: "customers"},
	{Command: "domains available", Description: "Determine whether or not the specified domain is available for purchase", Group: "domains"},
	{Command: "domains available-bulk", Description: "Determine whether or not the specified domains are available for purchase", Group: "domains"},
	{Command: "domains cancel", Description: "Cancel a purchased domain", Group: "domains"},
	{Command: "domains contacts-validate", Description: "All contacts specified in request will be validated against all domains specifed in \"domains\". As an alternative, you can also pass in tlds, with the exception of `uk`, which requires full domain names", Group: "domains"},
	{Command: "domains get", Description: "Retrieve details for the specified Domain", Group: "domains"},
	{Command: "domains get-agreement", Description: "Retrieve the legal agreement(s) required to purchase the specified TLD and add-ons", Group: "domains"},
	{Command: "domains get-maintenances", Description: "Retrieve the details for an upcoming system Maintenances", Group: "domains"},
	{Command: "domains get-usage", Description: "Retrieve api usage request counts for a specific year/month.  The data is retained for a period of three months.", Group: "domains"},
	{Command: "domains list", Description: "Retrieve a list of Domains for the specified Shopper", Group: "domains"},
	{Command: "domains list-maintenances", Description: "Retrieve a list of upcoming system Maintenances", Group: "domains"},
	{Command: "domains purchase", Description: "Purchase and register the specified Domain", Group: "domains"},
	{Command: "domains schema", Description: "Retrieve the schema to be submitted when registering a Domain for the specified TLD", Group: "domains"},
	{Command: "domains suggest", Description: "Suggest alternate Domain names based on a seed Domain, a set of keywords, or the shopper's purchase history", Group: "domains"},
	{Command: "domains tlds", Description: "Retrieves a list of TLDs supported and enabled for sale", Group: "domains"},
	{Command: "domains update", Description: "Update details for the specified Domain", Group: "domains"},
	{Command: "domains validate", Description: "Validate the request body using the Domain Purchase Schema for the specified TLD", Group: "domains"},
	{Command: "domains contacts update", Description: "Update domain", Group: "domains"},
	{Command: "domains privacy cancel", Description: "Submit a privacy cancellation request for the given domain", Group: "domains"},
	{Command: "domains privacy purchase", Description: "Purchase privacy for a specified domain", Group: "domains"},
	{Command: "domains records add", Description: "Add the specified DNS Records to the specified Domain", Group: "domains"},
	{Command: "domains records delete-type-name", Description: "Delete all DNS Records for the specified Domain with the specified Type and Name", Group: "domains"},
	{Command: "domains records get", Description: "Retrieve DNS Records for the specified Domain, optionally with the specified Type and/or Name", Group: "domains"},
	{Command: "domains records replace", Description: "Replace all DNS Records for the specified Domain", Group: "domains"},
	{Command: "domains records replace-type", Description: "Replace all DNS Records for the specified Domain with the specified Type", Group: "domains"},
	{Command: "domains records replace-type-name", Description: "Replace all DNS Records for the specified Domain with the specified Type and Name", Group: "domains"},
	{Command: "domains renew renew", Description: "Renew the specified Domain", Group: "domains"},
	{Command: "domains transfer in", Description: "Purchase and start or restart transfer process", Group: "domains"},
	{Command: "domains verify-registrant-email verify-email", Description: "Re-send Contact E-mail Verification for specified Domain", Group: "domains"},
	{Command: "domains-customers domains create", Description: "Purchase and register the specified Domain", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers", Description: "Validate the request body using the Domain Registration Schema for the specified TLD", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-10", Description: "Initiate transfer out to another registrar for a .uk domain.", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-11", Description: "Accept transfer out", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-12", Description: "Reject transfer out", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-13", Description: "Acknowledge a domain notification", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-14", Description: "Validate the request body using the Domain Transfer Schema for the specified TLD", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-2", Description: "Purchase a restore for the given domain to bring it out of redemption", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-3", Description: "Regenerate the auth code for the given domain", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-4", Description: "Renew the specified Domain", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-5", Description: "Purchase and start or restart transfer process", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-6", Description: "Accepts the transfer in", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-7", Description: "Cancels the transfer in", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-8", Description: "Restarts transfer in request from the beginning", Group: "domains-customers"},
	{Command: "domains-customers domains create-customers-9", Description: "Retries the current transfer in request with supplied Authorization code", Group: "domains-customers"},
	{Command: "domains-customers domains delete", Description: "Cancels a pending change of registrant request for a given domain", Group: "domains-customers"},
	{Command: "domains-customers domains delete-customers", Description: "Remove the specifed DNSSEC record from the domain", Group: "domains-customers"},
	{Command: "domains-customers domains delete-customers-2", Description: "Cancel the most recent user action for the specified domain", Group: "domains-customers"},
	{Command: "domains-customers domains forwards-delete", Description: "<strong>Notes:</strong><ul><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "domains-customers"},
	{Command: "domains-customers domains forwards-get", Description: "<strong>Notes:</strong><ul><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "domains-customers"},
	{Command: "domains-customers domains forwards-post", Description: "<strong>Notes:</strong><ul><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "domains-customers"},
	{Command: "domains-customers domains forwards-put", Description: "<strong>Notes:</strong><ul><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "domains-customers"},
	{Command: "domains-customers domains get", Description: "Retrieve the next domain notification", Group: "domains-customers"},
	{Command: "domains-customers domains get-customers", Description: "Retrieve details for the specified Domain", Group: "domains-customers"},
	{Command: "domains-customers domains get-customers-2", Description: "Retrieve a list of notification types that are opted in", Group: "domains-customers"},
	{Command: "domains-customers domains get-customers-3", Description: "Retrieves a list of the most recent actions for the specified domain", Group: "domains-customers"},
	{Command: "domains-customers domains get-customers-4", Description: "Retrieve change of registrant information", Group: "domains-customers"},
	{Command: "domains-customers domains get-customers-5", Description: "Query the current transfer status", Group: "domains-customers"},
	{Command: "domains-customers domains get-customers-6", Description: "Retrieve the schema for the notification data for the specified notification type", Group: "domains-customers"},
	{Command: "domains-customers domains get-customers-7", Description: "Retrieve the schema to be submitted when registering a Domain for the specified TLD", Group: "domains-customers"},
	{Command: "domains-customers domains get-customers-8", Description: "Retrieves the most recent action for the specified domain", Group: "domains-customers"},
	{Command: "domains-customers domains get-customers-9", Description: "Retrieve privacy email forwarding settings showing where emails are delivered", Group: "domains-customers"},
	{Command: "domains-customers domains update", Description: "Opt in to recieve notifications for the submitted notification types", Group: "domains-customers"},
	{Command: "domains-customers domains update-customers", Description: "Update domain contacts", Group: "domains-customers"},
	{Command: "domains-customers domains update-customers-2", Description: "Add the specifed DNSSEC records to the domain", Group: "domains-customers"},
	{Command: "domains-customers domains update-customers-3", Description: "Replaces the existing name servers on the domain.", Group: "domains-customers"},
	{Command: "domains-customers domains update-customers-4", Description: "Update privacy email forwarding settings to determine how emails are delivered", Group: "domains-customers"},
	{Command: "orders get", Description: "<strong>API Resellers</strong><ul><li>This endpoint does not support subaccounts and therefore API Resellers should not supply an X-Shopper-Id header</li></ul>", Group: "orders"},
	{Command: "orders list", Description: "<strong>API Resellers</strong><ul><li>This endpoint does not support subaccounts and therefore API Resellers should not supply an X-Shopper-Id header</li></ul>", Group: "orders"},
	{Command: "parking get-metrics", Description: "Returns a list of parking metrics for the specified customer, using specified filters", Group: "parking"},
	{Command: "parking get-metrics-by-domain", Description: "Returns a list of domain metrics for the specified customer and portfolio, using specified filters", Group: "parking"},
	{Command: "shoppers create-subaccount", Description: "Create a Subaccount owned by the authenticated Reseller", Group: "shoppers"},
	{Command: "shoppers delete", Description: "<strong>Notes:</strong><ul><li>Shopper deletion is not supported in OTE</li><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "shoppers"},
	{Command: "shoppers get", Description: "<strong>Notes:</strong><ul><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "shoppers"},
	{Command: "shoppers update", Description: "<strong>Notes:</strong><ul><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "shoppers"},
	{Command: "shoppers factors change-password", Description: "<strong>Notes:</strong><ul><li>Password set is only supported by API Resellers setting subaccount passwords.</li><li>**shopperId** is **not the same** as **customerId**.  **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "shoppers"},
	{Command: "shoppers status get", Description: "<strong>Notes:</strong><ul><li>**shopperId** is **not the same** as **customerId**. **shopperId** is a number of max length 10 digits (*ex:* 1234567890) whereas **customerId** is a UUIDv4 (*ex:* 295e3bc3-b3b9-4d95-aae5-ede41a994d13)</li></ul>", Group: "shoppers"},
	{Command: "subscriptions cancel", Description: "Cancel the specified Subscription", Group: "subscriptions"},
	{Command: "subscriptions get", Description: "Retrieve details for the specified Subscription", Group: "subscriptions"},
	{Command: "subscriptions list", Description: "Retrieve a list of Subscriptions for the specified Shopper", Group: "subscriptions"},
	{Command: "subscriptions product-groups", Description: "Retrieve a list of ProductGroups for the specified Shopper", Group: "subscriptions"},
	{Command: "subscriptions update", Description: "Only Subscription properties that can be changed without immediate financial impact can be modified via PATCH, whereas some properties can be changed by purchasing a renewal<br/><strong>This endpoint only supports JWT authentication</strong>", Group: "subscriptions"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  godaddy-pp-cli which "stale tickets"
  godaddy-pp-cli which "bottleneck"
  godaddy-pp-cli which --limit 1 "send message"
  godaddy-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
