version: 2
project_name: godaddy-pp-cli
changelog:
  disable: true
builds:
  - id: godaddy-pp-cli
    main: ./cmd/godaddy-pp-cli
    binary: godaddy-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/developer-tools/godaddy/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: godaddy-pp-mcp
    main: ./cmd/godaddy-pp-mcp
    binary: godaddy-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: godaddy-pp-cli
    repository:
      owner: zaydiscold
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Combined CLI for multiple API services"
    install: |
      bin.install "godaddy-pp-cli"
      bin.install "godaddy-pp-mcp"
