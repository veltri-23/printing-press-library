# agentcookie.toml: secrets-bus adoption manifest v2
# See docs/spec-agentcookie-secrets-bus-v2-adoption.md
schema_version = 2
name = "scrape-do-pp-cli"
display_name = "Scrape.do"
description = "The first CLI for Scrape-do with Google SERP scraping plus a credit and concurrency governor"
project_kind = "cli"

[secrets.file]
path = "~/.config/scrape-do-pp-cli/config.toml"

[sync]
default = false

[sync.keys]
SCRAPEDO_API_KEY = true
