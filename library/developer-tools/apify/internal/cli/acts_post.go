// Copyright 2026 Kevin Magnan and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newActsPostCmd(flags *rootFlags) *cobra.Command {
	var bodyActorStandby string
	var bodyCategories string
	var bodyDefaultRunOptionsBuild string
	var bodyDefaultRunOptionsForcePermissionLevel string
	var bodyDefaultRunOptionsMaxItems int
	var bodyDefaultRunOptionsMemoryMbytes int
	var bodyDefaultRunOptionsRestartOnError bool
	var bodyDefaultRunOptionsTimeoutSecs int
	var bodyDescription string
	var bodyExampleRunInput string
	var bodyIsDeprecated bool
	var bodyIsPublic bool
	var bodyName string
	var bodyPricingInfos string
	var bodyRestartOnError bool
	var bodySeoDescription string
	var bodySeoTitle string
	var bodyTitle string
	var bodyVersions string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post",
		Aliases:     []string{"create"},
		Short:       "Creates a new Actor with settings specified in an Actor object passed as JSON in the POST payload. The response is...",
		Example:     "  apify-pp-cli acts post",
		Annotations: map[string]string{"pp:endpoint": "acts.post", "pp:method": "POST", "pp:path": "/v2/acts"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/acts"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyActorStandby != "" {
					body["actorStandby"] = bodyActorStandby
				}
				if bodyCategories != "" {
					var parsedCategories any
					if err := json.Unmarshal([]byte(bodyCategories), &parsedCategories); err != nil {
						return fmt.Errorf("parsing --categories JSON: %w", err)
					}
					body["categories"] = parsedCategories
				}
				{
					nestedDefaultRunOptions := map[string]any{}
					if bodyDefaultRunOptionsBuild != "" {
						nestedDefaultRunOptions["build"] = bodyDefaultRunOptionsBuild
					}
					if bodyDefaultRunOptionsForcePermissionLevel != "" {
						nestedDefaultRunOptions["forcePermissionLevel"] = bodyDefaultRunOptionsForcePermissionLevel
					}
					if bodyDefaultRunOptionsMaxItems != 0 {
						nestedDefaultRunOptions["maxItems"] = bodyDefaultRunOptionsMaxItems
					}
					if bodyDefaultRunOptionsMemoryMbytes != 0 {
						nestedDefaultRunOptions["memoryMbytes"] = bodyDefaultRunOptionsMemoryMbytes
					}
					if bodyDefaultRunOptionsRestartOnError != false {
						nestedDefaultRunOptions["restartOnError"] = bodyDefaultRunOptionsRestartOnError
					}
					if bodyDefaultRunOptionsTimeoutSecs != 0 {
						nestedDefaultRunOptions["timeoutSecs"] = bodyDefaultRunOptionsTimeoutSecs
					}
					if len(nestedDefaultRunOptions) > 0 {
						body["defaultRunOptions"] = nestedDefaultRunOptions
					}
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyExampleRunInput != "" {
					body["exampleRunInput"] = bodyExampleRunInput
				}
				if bodyIsDeprecated != false {
					body["isDeprecated"] = bodyIsDeprecated
				}
				if bodyIsPublic != false {
					body["isPublic"] = bodyIsPublic
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPricingInfos != "" {
					var parsedPricingInfos any
					if err := json.Unmarshal([]byte(bodyPricingInfos), &parsedPricingInfos); err != nil {
						return fmt.Errorf("parsing --pricing-infos JSON: %w", err)
					}
					body["pricingInfos"] = parsedPricingInfos
				}
				if bodyRestartOnError != false {
					body["restartOnError"] = bodyRestartOnError
				}
				if bodySeoDescription != "" {
					body["seoDescription"] = bodySeoDescription
				}
				if bodySeoTitle != "" {
					body["seoTitle"] = bodySeoTitle
				}
				if bodyTitle != "" {
					body["title"] = bodyTitle
				}
				if bodyVersions != "" {
					var parsedVersions any
					if err := json.Unmarshal([]byte(bodyVersions), &parsedVersions); err != nil {
						return fmt.Errorf("parsing --versions JSON: %w", err)
					}
					body["versions"] = parsedVersions
				}
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "acts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyActorStandby, "actor-standby", "", "Actor standby")
	cmd.Flags().StringVar(&bodyCategories, "categories", "", "Categories")
	cmd.Flags().StringVar(&bodyDefaultRunOptionsBuild, "default-run-options-build", "", "Build")
	cmd.Flags().StringVar(&bodyDefaultRunOptionsForcePermissionLevel, "default-run-options-force-permission-level", "", "Force permission level")
	cmd.Flags().IntVar(&bodyDefaultRunOptionsMaxItems, "default-run-options-max-items", 0, "Max items")
	cmd.Flags().IntVar(&bodyDefaultRunOptionsMemoryMbytes, "default-run-options-memory-mbytes", 0, "Memory mbytes")
	cmd.Flags().BoolVar(&bodyDefaultRunOptionsRestartOnError, "default-run-options-restart-on-error", false, "Restart on error")
	cmd.Flags().IntVar(&bodyDefaultRunOptionsTimeoutSecs, "default-run-options-timeout-secs", 0, "Timeout secs")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyExampleRunInput, "example-run-input", "", "Example run input")
	cmd.Flags().BoolVar(&bodyIsDeprecated, "is-deprecated", false, "Is deprecated")
	cmd.Flags().BoolVar(&bodyIsPublic, "is-public", false, "Is public")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyPricingInfos, "pricing-infos", "", "Pricing infos")
	cmd.Flags().BoolVar(&bodyRestartOnError, "restart-on-error", false, "Restart on error")
	cmd.Flags().StringVar(&bodySeoDescription, "seo-description", "", "Seo description")
	cmd.Flags().StringVar(&bodySeoTitle, "seo-title", "", "Seo title")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Title")
	cmd.Flags().StringVar(&bodyVersions, "versions", "", "Versions")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
