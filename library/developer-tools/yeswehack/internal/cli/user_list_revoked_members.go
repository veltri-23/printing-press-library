// Copyright 2026 matt-van-horn. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newUserListRevokedMembersCmd(flags *rootFlags) *cobra.Command {
	var flagPage string
	var flagResultsPerPage int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list-revoked-members",
		Short:       "List revoked program membership records",
		Example:     "  yeswehack-pp-cli user list-revoked-members",
		Annotations: map[string]string{"pp:endpoint": "user.list_revoked_members", "pp:method": "GET", "pp:path": "/user/members/revoked", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/user/members/revoked"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "user-revoked-members", path, map[string]string{
				"page":           fmt.Sprintf("%v", flagPage),
				"resultsPerPage": fmt.Sprintf("%v", flagResultsPerPage),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(extractResponseItems(data), &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(extractResponseItems(data), &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagPage, "page", "", "Page number")
	cmd.Flags().IntVar(&flagResultsPerPage, "results-per-page", 0, "Page size")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
