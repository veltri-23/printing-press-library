// Copyright 2026 sidduHERE and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

var version = "2026.6.1"

// Execute runs the CLI.
func Execute() error {
	rootCmd := &cobra.Command{
		Use:          "agentpool-pp-cli",
		Short:        "Printing Press companion for the official AgentPool CLI",
		SilenceUsage: true,
		// main.go prints Cobra errors while delegated commands keep their own stderr.
		SilenceErrors: true,
		Version:       version,
	}
	rootCmd.SetVersionTemplate("agentpool-pp-cli {{ .Version }}\n")
	rootCmd.AddCommand(newInstallCmd())
	rootCmd.AddCommand(newDoctorCmd())
	rootCmd.AddCommand(newUsageCmd())
	rootCmd.AddCommand(newSkillCmd())
	rootCmd.AddCommand(newMcpConfigCmd())
	rootCmd.AddCommand(newExecCmd())
	rootCmd.AddCommand(newVersionCliCmd())

	return rootCmd.Execute()
}

// ExitCode extracts exit code from an error.
func ExitCode(err error) int {
	if errWithCode, ok := err.(interface{ ExitCode() int }); ok {
		return errWithCode.ExitCode()
	}
	return 1
}

// ShouldPrintError returns false for delegated subprocess exits that already
// wrote their own stderr.
func ShouldPrintError(err error) bool {
	_, ok := err.(commandExitError)
	return !ok
}

func newVersionCliCmd() *cobra.Command {
	return &cobra.Command{
		Use:   "version",
		Short: "Print version",
		Run: func(cmd *cobra.Command, args []string) {
			fmt.Printf("agentpool-pp-cli %s\n", version)
		},
	}
}
