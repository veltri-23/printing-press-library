// Copyright 2026 Adrian Horning and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newLinkedinListSearchCmd(flags *rootFlags) *cobra.Command {
	var flagQuery string
	var flagDatePosted string
	var flagCursor string
	var flagAll bool

	cmd := &cobra.Command{
		Use:   "list-search",
		Short: "Finds public LinkedIn posts, feed updates, and Pulse articles by keyword using Google Search",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  scrape-creators-pp-cli linkedin list-search --query example-value",
		Annotations: map[string]string{"pp:endpoint": "linkedin.list-search", "pp:method": "GET", "pp:path": "/v1/linkedin/search/posts", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("query") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "query")
			}
			if cmd.Flags().Changed("date-posted") {
				allowedDatePosted := []string{"last-hour", "last-day", "last-week", "last-month", "last-year"}
				validDatePosted := false
				for _, v := range allowedDatePosted {
					if flagDatePosted == v {
						validDatePosted = true
						break
					}
				}
				if !validDatePosted {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagDatePosted, "date-posted", allowedDatePosted)
				}
			}
			path := "/v1/linkedin/search/posts"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "linkedin", path, map[string]string{
				"query":       formatCLIParamValue(flagQuery),
				"date_posted": formatCLIParamValue(flagDatePosted),
				"cursor":      formatCLIParamValue(flagCursor),
			}, nil, flagAll, "cursor", "cursor", "", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagQuery, "query", "", "Keyword or phrase to search for in public LinkedIn posts")
	cmd.Flags().StringVar(&flagDatePosted, "date-posted", "", "Date posted filter based on Google-indexed results (one of: last-hour, last-day, last-week, last-month, last-year)")
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "The cursor returned from the previous response")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
