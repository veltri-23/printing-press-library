// Copyright 2026 Adrian Horning and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTwitchListUser2Cmd(flags *rootFlags) *cobra.Command {
	var flagHandle string
	var flagFilterBy string
	var flagSortBy string

	cmd := &cobra.Command{
		Use:         "list-user-2",
		Short:       "Fetches a list of videos (100 max) for a Twitch user, returning each video's id, slug, url, embedURL, title, viewCount",
		Example:     "  scrape-creators-pp-cli twitch list-user-2 --handle ishowspeed",
		Annotations: map[string]string{"pp:endpoint": "twitch.list-user-2", "pp:method": "GET", "pp:path": "/v1/twitch/user/videos", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("handle") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "handle")
			}
			if cmd.Flags().Changed("filter-by") {
				allowedFilterBy := []string{"HIGHLIGHT", "ARCHIVE", "UPLOAD"}
				validFilterBy := false
				for _, v := range allowedFilterBy {
					if flagFilterBy == v {
						validFilterBy = true
						break
					}
				}
				if !validFilterBy {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFilterBy, "filter-by", allowedFilterBy)
				}
			}
			if cmd.Flags().Changed("sort-by") {
				allowedSortBy := []string{"TIME", "VIEWS"}
				validSortBy := false
				for _, v := range allowedSortBy {
					if flagSortBy == v {
						validSortBy = true
						break
					}
				}
				if !validSortBy {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSortBy, "sort-by", allowedSortBy)
				}
			}
			path := "/v1/twitch/user/videos"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagHandle != "" {
				params["handle"] = formatCLIParamValue(flagHandle)
			}
			if flagFilterBy != "" {
				params["filter_by"] = formatCLIParamValue(flagFilterBy)
			}
			if flagSortBy != "" {
				params["sort_by"] = formatCLIParamValue(flagSortBy)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "twitch", false, path, params, nil, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagHandle, "handle", "", "Twitch handle")
	cmd.Flags().StringVar(&flagFilterBy, "filter-by", "", "Filter by (one of: HIGHLIGHT, ARCHIVE, UPLOAD)")
	cmd.Flags().StringVar(&flagSortBy, "sort-by", "", "Sort by (one of: TIME, VIEWS)")

	return cmd
}
