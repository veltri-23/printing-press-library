// Copyright 2026 sdhilip200. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	"github.com/mvanhorn/printing-press-library/library/developer-tools/airflow-admin/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/airflow-admin/internal/client"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/airflow-admin/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/airflow-admin/internal/config"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/airflow-admin/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/airflow-admin/internal/store"
	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("apache-airflow-admin-auth_create-token",
			mcplib.WithDescription("Authenticate with an Airflow username and password and return a JWT access token. Required: password, username. Returns the new AuthTokenResponse. (public)"),
			mcplib.WithString("password", mcplib.Required(), mcplib.Description("Password")),
			mcplib.WithString("username", mcplib.Required(), mcplib.Description("Username")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/auth/token", false, false, nil, []mcpParamBinding{{PublicName: "password", WireName: "password", Location: "body"}, {PublicName: "username", WireName: "username", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apache-airflow-admin-version_get",
			mcplib.WithDescription("Get Airflow version. Returns the VersionInfo."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/version", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("connections_list",
			mcplib.WithDescription("List connections. Optional: limit (default: 100), offset (default: 0). Returns the ConnectionCollection."),
			mcplib.WithNumber("limit", mcplib.Description("Maximum records to return.")),
			mcplib.WithNumber("offset", mcplib.Description("Records to skip.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/connections", true, false, nil, []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dags_get",
			mcplib.WithDescription("Get DAG. Required: dag_id. Returns the Dag."),
			mcplib.WithString("dag_id", mcplib.Required(), mcplib.Description("Dag id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/dags/{dag_id}", true, false, nil, []mcpParamBinding{{PublicName: "dag_id", WireName: "dag_id", Location: "path"}}, []string{"dag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("dags_list",
			mcplib.WithDescription("List DAGs. Optional: limit (default: 100), offset (default: 0), dag_id_pattern (plus 1 more). Returns the DagCollection."),
			mcplib.WithNumber("limit", mcplib.Description("Maximum records to return.")),
			mcplib.WithNumber("offset", mcplib.Description("Records to skip.")),
			mcplib.WithString("dag_id_pattern", mcplib.Description("Optional DAG id pattern filter.")),
			mcplib.WithBoolean("paused", mcplib.Description("Filter DAGs by paused state.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/dags", true, false, nil, []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "dag_id_pattern", WireName: "dag_id_pattern", Location: "query"}, {PublicName: "paused", WireName: "paused", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dags_dag-runs_get",
			mcplib.WithDescription("Get DAG run. Required: dag_id, dag_run_id. Returns the DagRun."),
			mcplib.WithString("dag_id", mcplib.Required(), mcplib.Description("Dag id")),
			mcplib.WithString("dag_run_id", mcplib.Required(), mcplib.Description("Dag run id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/dags/{dag_id}/dagRuns/{dag_run_id}", true, false, nil, []mcpParamBinding{{PublicName: "dag_id", WireName: "dag_id", Location: "path"}, {PublicName: "dag_run_id", WireName: "dag_run_id", Location: "path"}}, []string{"dag_id", "dag_run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("dags_dag-runs_get-task-instance",
			mcplib.WithDescription("Get task instance. Required: dag_id, dag_run_id, task_id. Returns the TaskInstance."),
			mcplib.WithString("dag_id", mcplib.Required(), mcplib.Description("Dag id")),
			mcplib.WithString("dag_run_id", mcplib.Required(), mcplib.Description("Dag run id")),
			mcplib.WithString("task_id", mcplib.Required(), mcplib.Description("Task id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/dags/{dag_id}/dagRuns/{dag_run_id}/taskInstances/{task_id}", true, false, nil, []mcpParamBinding{{PublicName: "dag_id", WireName: "dag_id", Location: "path"}, {PublicName: "dag_run_id", WireName: "dag_run_id", Location: "path"}, {PublicName: "task_id", WireName: "task_id", Location: "path"}}, []string{"dag_id", "dag_run_id", "task_id"}),
	)
	s.AddTool(
		mcplib.NewTool("dags_dag-runs_list",
			mcplib.WithDescription("List DAG runs. Required: dag_id. Optional: limit (default: 100), offset (default: 0), state (plus 3 more). Returns the DagRunCollection."),
			mcplib.WithString("dag_id", mcplib.Required(), mcplib.Description("Dag id")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum records to return.")),
			mcplib.WithNumber("offset", mcplib.Description("Records to skip.")),
			mcplib.WithString("state", mcplib.Description("Filter by one or more DAG run states.")),
			mcplib.WithString("start_date_gte", mcplib.Description("Earliest start date.")),
			mcplib.WithString("start_date_lte", mcplib.Description("Latest start date.")),
			mcplib.WithString("order_by", mcplib.Description("Sort expression supported by Airflow.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/dags/{dag_id}/dagRuns", true, false, nil, []mcpParamBinding{{PublicName: "dag_id", WireName: "dag_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "state", WireName: "state", Location: "query"}, {PublicName: "start_date_gte", WireName: "start_date_gte", Location: "query"}, {PublicName: "start_date_lte", WireName: "start_date_lte", Location: "query"}, {PublicName: "order_by", WireName: "order_by", Location: "query"}}, []string{"dag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("dags_dag-runs_list-task-instances",
			mcplib.WithDescription("List task instances. Required: dag_id, dag_run_id. Optional: limit (default: 100), offset (default: 0), state. Returns the TaskInstanceCollection."),
			mcplib.WithString("dag_id", mcplib.Required(), mcplib.Description("Dag id")),
			mcplib.WithString("dag_run_id", mcplib.Required(), mcplib.Description("Dag run id")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum records to return.")),
			mcplib.WithNumber("offset", mcplib.Description("Records to skip.")),
			mcplib.WithString("state", mcplib.Description("Filter by task instance state.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/dags/{dag_id}/dagRuns/{dag_run_id}/taskInstances", true, false, nil, []mcpParamBinding{{PublicName: "dag_id", WireName: "dag_id", Location: "path"}, {PublicName: "dag_run_id", WireName: "dag_run_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "state", WireName: "state", Location: "query"}}, []string{"dag_id", "dag_run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("dags_details_get-dag",
			mcplib.WithDescription("Get DAG details. Required: dag_id. Returns the DagDetails."),
			mcplib.WithString("dag_id", mcplib.Required(), mcplib.Description("Dag id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/dags/{dag_id}/details", true, false, nil, []mcpParamBinding{{PublicName: "dag_id", WireName: "dag_id", Location: "path"}}, []string{"dag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("dags_tasks_get",
			mcplib.WithDescription("Get DAG task. Required: dag_id, task_id. Returns the Task."),
			mcplib.WithString("dag_id", mcplib.Required(), mcplib.Description("Dag id")),
			mcplib.WithString("task_id", mcplib.Required(), mcplib.Description("Task id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/dags/{dag_id}/tasks/{task_id}", true, false, nil, []mcpParamBinding{{PublicName: "dag_id", WireName: "dag_id", Location: "path"}, {PublicName: "task_id", WireName: "task_id", Location: "path"}}, []string{"dag_id", "task_id"}),
	)
	s.AddTool(
		mcplib.NewTool("dags_tasks_list",
			mcplib.WithDescription("List tasks for DAG. Required: dag_id. Returns the TaskCollection."),
			mcplib.WithString("dag_id", mcplib.Required(), mcplib.Description("Dag id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/dags/{dag_id}/tasks", true, false, nil, []mcpParamBinding{{PublicName: "dag_id", WireName: "dag_id", Location: "path"}}, []string{"dag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("monitor_get-health",
			mcplib.WithDescription("Get health. Returns the HealthInfo."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/monitor/health", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("pools_get",
			mcplib.WithDescription("Get pool. Required: pool_name. Returns the Pool."),
			mcplib.WithString("pool_name", mcplib.Required(), mcplib.Description("Pool name")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/pools/{pool_name}", true, false, nil, []mcpParamBinding{{PublicName: "pool_name", WireName: "pool_name", Location: "path"}}, []string{"pool_name"}),
	)
	s.AddTool(
		mcplib.NewTool("pools_list",
			mcplib.WithDescription("List pools. Optional: limit (default: 100), offset (default: 0). Returns the PoolCollection."),
			mcplib.WithNumber("limit", mcplib.Description("Maximum records to return.")),
			mcplib.WithNumber("offset", mcplib.Description("Records to skip.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/pools", true, false, nil, []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("variables_list",
			mcplib.WithDescription("List variables. Optional: limit (default: 100), offset (default: 0). Returns the VariableCollection."),
			mcplib.WithNumber("limit", mcplib.Description("Maximum records to return.")),
			mcplib.WithNumber("offset", mcplib.Description("Records to skip.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v2/variables", true, false, nil, []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export AIRFLOW_ADMIN_BEARER_AUTH=<your-key>" +
					"\n      Run 'airflow-admin-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export AIRFLOW_ADMIN_BEARER_AUTH=<your-key>" +
					"\n      Run 'airflow-admin-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export AIRFLOW_ADMIN_BEARER_AUTH=<your-key>" +
					"\n      Run 'airflow-admin-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "airflow-admin-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "airflow-admin-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "airflow-admin",
		"description": "Focused read-first API surface for inspecting Apache Airflow DAGs, DAG runs, task instances, pools, variables",
		"archetype":   "communication",
		"tool_count":  16,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion airflow-admin-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "AIRFLOW_ADMIN_BEARER_AUTH",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "apache-airflow-admin-auth",
				"description": "Manage apache airflow admin auth",
				"endpoints":   []string{"create-token"},
				"searchable":  true,
			},
			{
				"name":        "apache-airflow-admin-version",
				"description": "Manage apache airflow admin version",
				"endpoints":   []string{"get"},
			},
			{
				"name":        "connections",
				"description": "Airflow connection metadata.",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "dags",
				"description": "DAG inventory and metadata.",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "monitor",
				"description": "Manage monitor",
				"endpoints":   []string{"get-health"},
			},
			{
				"name":        "pools",
				"description": "Airflow pool capacity and occupancy.",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "variables",
				"description": "Airflow variable metadata.",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass offset parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		"playbook": []map[string]string{
			{"topic": "Message search", "insight": "Use the search tool on synced data rather than paginating through message history. Message APIs often have aggressive rate limits."},
			{"topic": "Channel health", "insight": "When analyzing channel activity, use the channel-health command or sql aggregation on synced messages. Don't iterate individual messages via API."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
