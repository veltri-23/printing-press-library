// Copyright 2026 Giuliano Giacaglia and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProjectsNetworkRestrictionsPatchCmd(flags *rootFlags) *cobra.Command {
	var bodyAddDbAllowedCidrs string
	var bodyAddDbAllowedCidrsV6 string
	var bodyRemoveDbAllowedCidrs string
	var bodyRemoveDbAllowedCidrsV6 string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "patch <ref>",
		Short:       "[Alpha] Updates project's network restrictions by adding or removing CIDRs",
		Example:     "  supabase-pp-cli projects network-restrictions patch example-value",
		Annotations: map[string]string{"pp:endpoint": "network-restrictions.patch", "pp:method": "PATCH", "pp:path": "/v1/projects/{ref}/network-restrictions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/projects/{ref}/network-restrictions"
			path = replacePathParam(path, "ref", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAdd := map[string]any{}
					if bodyAddDbAllowedCidrs != "" {
						var parsedAddDbAllowedCidrs any
						if err := json.Unmarshal([]byte(bodyAddDbAllowedCidrs), &parsedAddDbAllowedCidrs); err != nil {
							return fmt.Errorf("parsing --add-db-allowed-cidrs JSON: %w", err)
						}
						nestedAdd["dbAllowedCidrs"] = parsedAddDbAllowedCidrs
					}
					if bodyAddDbAllowedCidrsV6 != "" {
						var parsedAddDbAllowedCidrsV6 any
						if err := json.Unmarshal([]byte(bodyAddDbAllowedCidrsV6), &parsedAddDbAllowedCidrsV6); err != nil {
							return fmt.Errorf("parsing --add-db-allowed-cidrs-v6 JSON: %w", err)
						}
						nestedAdd["dbAllowedCidrsV6"] = parsedAddDbAllowedCidrsV6
					}
					if len(nestedAdd) > 0 {
						body["add"] = nestedAdd
					}
				}
				{
					nestedRemove := map[string]any{}
					if bodyRemoveDbAllowedCidrs != "" {
						var parsedRemoveDbAllowedCidrs any
						if err := json.Unmarshal([]byte(bodyRemoveDbAllowedCidrs), &parsedRemoveDbAllowedCidrs); err != nil {
							return fmt.Errorf("parsing --remove-db-allowed-cidrs JSON: %w", err)
						}
						nestedRemove["dbAllowedCidrs"] = parsedRemoveDbAllowedCidrs
					}
					if bodyRemoveDbAllowedCidrsV6 != "" {
						var parsedRemoveDbAllowedCidrsV6 any
						if err := json.Unmarshal([]byte(bodyRemoveDbAllowedCidrsV6), &parsedRemoveDbAllowedCidrsV6); err != nil {
							return fmt.Errorf("parsing --remove-db-allowed-cidrs-v6 JSON: %w", err)
						}
						nestedRemove["dbAllowedCidrsV6"] = parsedRemoveDbAllowedCidrsV6
					}
					if len(nestedRemove) > 0 {
						body["remove"] = nestedRemove
					}
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "network-restrictions",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAddDbAllowedCidrs, "add-db-allowed-cidrs", "", "Db allowed cidrs")
	cmd.Flags().StringVar(&bodyAddDbAllowedCidrsV6, "add-db-allowed-cidrs-v6", "", "Db allowed cidrs v6")
	cmd.Flags().StringVar(&bodyRemoveDbAllowedCidrs, "remove-db-allowed-cidrs", "", "Db allowed cidrs")
	cmd.Flags().StringVar(&bodyRemoveDbAllowedCidrsV6, "remove-db-allowed-cidrs-v6", "", "Db allowed cidrs v6")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
