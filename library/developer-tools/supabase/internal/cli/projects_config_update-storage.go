// Copyright 2026 Giuliano Giacaglia and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProjectsConfigUpdateStorageCmd(flags *rootFlags) *cobra.Command {
	var bodyExternalUpstreamTarget string
	var bodyFeaturesIcebergCatalogEnabled bool
	var bodyFeaturesIcebergCatalogMaxCatalogs int
	var bodyFeaturesIcebergCatalogMaxNamespaces int
	var bodyFeaturesIcebergCatalogMaxTables int
	var bodyFeaturesImageTransformationEnabled bool
	var bodyFeaturesS3ProtocolEnabled bool
	var bodyFeaturesVectorBucketsEnabled bool
	var bodyFeaturesVectorBucketsMaxBuckets int
	var bodyFeaturesVectorBucketsMaxIndexes int
	var bodyFileSizeLimit int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update-storage <ref>",
		Short:       "Updates project's storage config",
		Example:     "  supabase-pp-cli projects config update-storage example-value",
		Annotations: map[string]string{"pp:endpoint": "config.update-storage", "pp:method": "PATCH", "pp:path": "/v1/projects/{ref}/config/storage"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("external-upstream-target") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "external-upstream-target")
				}
				if !cmd.Flags().Changed("features-iceberg-catalog-enabled") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "features-iceberg-catalog-enabled")
				}
				if !cmd.Flags().Changed("features-iceberg-catalog-max-catalogs") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "features-iceberg-catalog-max-catalogs")
				}
				if !cmd.Flags().Changed("features-iceberg-catalog-max-namespaces") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "features-iceberg-catalog-max-namespaces")
				}
				if !cmd.Flags().Changed("features-iceberg-catalog-max-tables") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "features-iceberg-catalog-max-tables")
				}
				if !cmd.Flags().Changed("features-image-transformation-enabled") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "features-image-transformation-enabled")
				}
				if !cmd.Flags().Changed("features-s3-protocol-enabled") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "features-s3-protocol-enabled")
				}
				if !cmd.Flags().Changed("features-vector-buckets-enabled") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "features-vector-buckets-enabled")
				}
				if !cmd.Flags().Changed("features-vector-buckets-max-buckets") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "features-vector-buckets-max-buckets")
				}
				if !cmd.Flags().Changed("features-vector-buckets-max-indexes") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "features-vector-buckets-max-indexes")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/projects/{ref}/config/storage"
			path = replacePathParam(path, "ref", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedExternal := map[string]any{}
					if bodyExternalUpstreamTarget != "" {
						nestedExternal["upstreamTarget"] = bodyExternalUpstreamTarget
					}
					if len(nestedExternal) > 0 {
						body["external"] = nestedExternal
					}
				}
				{
					nestedFeatures := map[string]any{}
					{
						nestedFeaturesIcebergCatalog := map[string]any{}
						if bodyFeaturesIcebergCatalogEnabled != false {
							nestedFeaturesIcebergCatalog["enabled"] = bodyFeaturesIcebergCatalogEnabled
						}
						if bodyFeaturesIcebergCatalogMaxCatalogs != 0 {
							nestedFeaturesIcebergCatalog["maxCatalogs"] = bodyFeaturesIcebergCatalogMaxCatalogs
						}
						if bodyFeaturesIcebergCatalogMaxNamespaces != 0 {
							nestedFeaturesIcebergCatalog["maxNamespaces"] = bodyFeaturesIcebergCatalogMaxNamespaces
						}
						if bodyFeaturesIcebergCatalogMaxTables != 0 {
							nestedFeaturesIcebergCatalog["maxTables"] = bodyFeaturesIcebergCatalogMaxTables
						}
						if len(nestedFeaturesIcebergCatalog) > 0 {
							nestedFeatures["icebergCatalog"] = nestedFeaturesIcebergCatalog
						}
					}
					{
						nestedFeaturesImageTransformation := map[string]any{}
						if bodyFeaturesImageTransformationEnabled != false {
							nestedFeaturesImageTransformation["enabled"] = bodyFeaturesImageTransformationEnabled
						}
						if len(nestedFeaturesImageTransformation) > 0 {
							nestedFeatures["imageTransformation"] = nestedFeaturesImageTransformation
						}
					}
					{
						nestedFeaturesS3Protocol := map[string]any{}
						if bodyFeaturesS3ProtocolEnabled != false {
							nestedFeaturesS3Protocol["enabled"] = bodyFeaturesS3ProtocolEnabled
						}
						if len(nestedFeaturesS3Protocol) > 0 {
							nestedFeatures["s3Protocol"] = nestedFeaturesS3Protocol
						}
					}
					{
						nestedFeaturesVectorBuckets := map[string]any{}
						if bodyFeaturesVectorBucketsEnabled != false {
							nestedFeaturesVectorBuckets["enabled"] = bodyFeaturesVectorBucketsEnabled
						}
						if bodyFeaturesVectorBucketsMaxBuckets != 0 {
							nestedFeaturesVectorBuckets["maxBuckets"] = bodyFeaturesVectorBucketsMaxBuckets
						}
						if bodyFeaturesVectorBucketsMaxIndexes != 0 {
							nestedFeaturesVectorBuckets["maxIndexes"] = bodyFeaturesVectorBucketsMaxIndexes
						}
						if len(nestedFeaturesVectorBuckets) > 0 {
							nestedFeatures["vectorBuckets"] = nestedFeaturesVectorBuckets
						}
					}
					if len(nestedFeatures) > 0 {
						body["features"] = nestedFeatures
					}
				}
				if bodyFileSizeLimit != 0 {
					body["fileSizeLimit"] = bodyFileSizeLimit
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "config",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyExternalUpstreamTarget, "external-upstream-target", "", "Upstream target")
	cmd.Flags().BoolVar(&bodyFeaturesIcebergCatalogEnabled, "features-iceberg-catalog-enabled", false, "Enabled")
	cmd.Flags().IntVar(&bodyFeaturesIcebergCatalogMaxCatalogs, "features-iceberg-catalog-max-catalogs", 0, "Max catalogs")
	cmd.Flags().IntVar(&bodyFeaturesIcebergCatalogMaxNamespaces, "features-iceberg-catalog-max-namespaces", 0, "Max namespaces")
	cmd.Flags().IntVar(&bodyFeaturesIcebergCatalogMaxTables, "features-iceberg-catalog-max-tables", 0, "Max tables")
	cmd.Flags().BoolVar(&bodyFeaturesImageTransformationEnabled, "features-image-transformation-enabled", false, "Enabled")
	cmd.Flags().BoolVar(&bodyFeaturesS3ProtocolEnabled, "features-s3-protocol-enabled", false, "Enabled")
	cmd.Flags().BoolVar(&bodyFeaturesVectorBucketsEnabled, "features-vector-buckets-enabled", false, "Enabled")
	cmd.Flags().IntVar(&bodyFeaturesVectorBucketsMaxBuckets, "features-vector-buckets-max-buckets", 0, "Max buckets")
	cmd.Flags().IntVar(&bodyFeaturesVectorBucketsMaxIndexes, "features-vector-buckets-max-indexes", 0, "Max indexes")
	cmd.Flags().IntVar(&bodyFileSizeLimit, "file-size-limit", 0, "File size limit")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
