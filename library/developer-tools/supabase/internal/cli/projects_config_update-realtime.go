// Copyright 2026 Giuliano Giacaglia and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProjectsConfigUpdateRealtimeCmd(flags *rootFlags) *cobra.Command {
	var bodyConnectionPool int
	var bodyMaxBytesPerSecond int
	var bodyMaxChannelsPerClient int
	var bodyMaxConcurrentUsers int
	var bodyMaxEventsPerSecond int
	var bodyMaxJoinsPerSecond int
	var bodyMaxPayloadSizeInKb int
	var bodyMaxPresenceEventsPerSecond int
	var bodyPresenceEnabled bool
	var bodyPrivateOnly bool
	var bodySuspend bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update-realtime <ref>",
		Short:       "Updates realtime configuration",
		Example:     "  supabase-pp-cli projects config update-realtime example-value",
		Annotations: map[string]string{"pp:endpoint": "config.update-realtime", "pp:method": "PATCH", "pp:path": "/v1/projects/{ref}/config/realtime"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/projects/{ref}/config/realtime"
			path = replacePathParam(path, "ref", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyConnectionPool != 0 {
					body["connection_pool"] = bodyConnectionPool
				}
				if bodyMaxBytesPerSecond != 0 {
					body["max_bytes_per_second"] = bodyMaxBytesPerSecond
				}
				if bodyMaxChannelsPerClient != 0 {
					body["max_channels_per_client"] = bodyMaxChannelsPerClient
				}
				if bodyMaxConcurrentUsers != 0 {
					body["max_concurrent_users"] = bodyMaxConcurrentUsers
				}
				if bodyMaxEventsPerSecond != 0 {
					body["max_events_per_second"] = bodyMaxEventsPerSecond
				}
				if bodyMaxJoinsPerSecond != 0 {
					body["max_joins_per_second"] = bodyMaxJoinsPerSecond
				}
				if bodyMaxPayloadSizeInKb != 0 {
					body["max_payload_size_in_kb"] = bodyMaxPayloadSizeInKb
				}
				if bodyMaxPresenceEventsPerSecond != 0 {
					body["max_presence_events_per_second"] = bodyMaxPresenceEventsPerSecond
				}
				if bodyPresenceEnabled != false {
					body["presence_enabled"] = bodyPresenceEnabled
				}
				if bodyPrivateOnly != false {
					body["private_only"] = bodyPrivateOnly
				}
				if bodySuspend != false {
					body["suspend"] = bodySuspend
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "config",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyConnectionPool, "connection-pool", 0, "Sets connection pool size for Realtime Authorization")
	cmd.Flags().IntVar(&bodyMaxBytesPerSecond, "max-bytes-per-second", 0, "Sets maximum number of bytes per second rate per channel limit")
	cmd.Flags().IntVar(&bodyMaxChannelsPerClient, "max-channels-per-client", 0, "Sets maximum number of channels per client rate limit")
	cmd.Flags().IntVar(&bodyMaxConcurrentUsers, "max-concurrent-users", 0, "Sets maximum number of concurrent users rate limit")
	cmd.Flags().IntVar(&bodyMaxEventsPerSecond, "max-events-per-second", 0, "Sets maximum number of events per second rate per channel limit")
	cmd.Flags().IntVar(&bodyMaxJoinsPerSecond, "max-joins-per-second", 0, "Sets maximum number of joins per second rate limit")
	cmd.Flags().IntVar(&bodyMaxPayloadSizeInKb, "max-payload-size-in-kb", 0, "Sets maximum number of payload size in KB rate limit")
	cmd.Flags().IntVar(&bodyMaxPresenceEventsPerSecond, "max-presence-events-per-second", 0, "Sets maximum number of presence events per second rate limit")
	cmd.Flags().BoolVar(&bodyPresenceEnabled, "presence-enabled", false, "Whether to enable presence")
	cmd.Flags().BoolVar(&bodyPrivateOnly, "private-only", false, "Whether to only allow private channels")
	cmd.Flags().BoolVar(&bodySuspend, "suspend", false, "Disables the Realtime service for this project when true. Set to false to re-enable it.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
