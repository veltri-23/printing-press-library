// Copyright 2026 Giuliano Giacaglia and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProjectsConfigUpdatePostgresCmd(flags *rootFlags) *cobra.Command {
	var bodyCheckpointTimeout string
	var bodyEffectiveCacheSize string
	var bodyHotStandbyFeedback bool
	var bodyLogicalDecodingWorkMem string
	var bodyMaintenanceWorkMem string
	var bodyMaxConnections int
	var bodyMaxLocksPerTransaction int
	var bodyMaxParallelMaintenanceWorkers int
	var bodyMaxParallelWorkers int
	var bodyMaxParallelWorkersPerGather int
	var bodyMaxReplicationSlots int
	var bodyMaxSlotWalKeepSize string
	var bodyMaxStandbyArchiveDelay string
	var bodyMaxStandbyStreamingDelay string
	var bodyMaxWalSenders int
	var bodyMaxWalSize string
	var bodyMaxWorkerProcesses int
	var bodyRestartDatabase bool
	var bodySessionReplicationRole string
	var bodySharedBuffers string
	var bodyStatementTimeout string
	var bodyTrackActivityQuerySize string
	var bodyTrackCommitTimestamp bool
	var bodyWalKeepSize string
	var bodyWalSenderTimeout string
	var bodyWorkMem string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update-postgres <ref>",
		Short:       "Updates project's Postgres config",
		Example:     "  supabase-pp-cli projects config update-postgres example-value",
		Annotations: map[string]string{"pp:endpoint": "config.update-postgres", "pp:method": "PUT", "pp:path": "/v1/projects/{ref}/config/database/postgres"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/projects/{ref}/config/database/postgres"
			path = replacePathParam(path, "ref", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCheckpointTimeout != "" {
					body["checkpoint_timeout"] = bodyCheckpointTimeout
				}
				if bodyEffectiveCacheSize != "" {
					body["effective_cache_size"] = bodyEffectiveCacheSize
				}
				if bodyHotStandbyFeedback != false {
					body["hot_standby_feedback"] = bodyHotStandbyFeedback
				}
				if bodyLogicalDecodingWorkMem != "" {
					body["logical_decoding_work_mem"] = bodyLogicalDecodingWorkMem
				}
				if bodyMaintenanceWorkMem != "" {
					body["maintenance_work_mem"] = bodyMaintenanceWorkMem
				}
				if bodyMaxConnections != 0 {
					body["max_connections"] = bodyMaxConnections
				}
				if bodyMaxLocksPerTransaction != 0 {
					body["max_locks_per_transaction"] = bodyMaxLocksPerTransaction
				}
				if bodyMaxParallelMaintenanceWorkers != 0 {
					body["max_parallel_maintenance_workers"] = bodyMaxParallelMaintenanceWorkers
				}
				if bodyMaxParallelWorkers != 0 {
					body["max_parallel_workers"] = bodyMaxParallelWorkers
				}
				if bodyMaxParallelWorkersPerGather != 0 {
					body["max_parallel_workers_per_gather"] = bodyMaxParallelWorkersPerGather
				}
				if bodyMaxReplicationSlots != 0 {
					body["max_replication_slots"] = bodyMaxReplicationSlots
				}
				if bodyMaxSlotWalKeepSize != "" {
					body["max_slot_wal_keep_size"] = bodyMaxSlotWalKeepSize
				}
				if bodyMaxStandbyArchiveDelay != "" {
					body["max_standby_archive_delay"] = bodyMaxStandbyArchiveDelay
				}
				if bodyMaxStandbyStreamingDelay != "" {
					body["max_standby_streaming_delay"] = bodyMaxStandbyStreamingDelay
				}
				if bodyMaxWalSenders != 0 {
					body["max_wal_senders"] = bodyMaxWalSenders
				}
				if bodyMaxWalSize != "" {
					body["max_wal_size"] = bodyMaxWalSize
				}
				if bodyMaxWorkerProcesses != 0 {
					body["max_worker_processes"] = bodyMaxWorkerProcesses
				}
				if bodyRestartDatabase != false {
					body["restart_database"] = bodyRestartDatabase
				}
				if bodySessionReplicationRole != "" {
					body["session_replication_role"] = bodySessionReplicationRole
				}
				if bodySharedBuffers != "" {
					body["shared_buffers"] = bodySharedBuffers
				}
				if bodyStatementTimeout != "" {
					body["statement_timeout"] = bodyStatementTimeout
				}
				if bodyTrackActivityQuerySize != "" {
					body["track_activity_query_size"] = bodyTrackActivityQuerySize
				}
				if bodyTrackCommitTimestamp != false {
					body["track_commit_timestamp"] = bodyTrackCommitTimestamp
				}
				if bodyWalKeepSize != "" {
					body["wal_keep_size"] = bodyWalKeepSize
				}
				if bodyWalSenderTimeout != "" {
					body["wal_sender_timeout"] = bodyWalSenderTimeout
				}
				if bodyWorkMem != "" {
					body["work_mem"] = bodyWorkMem
				}
			}
			data, statusCode, err := c.Put(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "config",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCheckpointTimeout, "checkpoint-timeout", "", "Default unit: s")
	cmd.Flags().StringVar(&bodyEffectiveCacheSize, "effective-cache-size", "", "Effective cache size")
	cmd.Flags().BoolVar(&bodyHotStandbyFeedback, "hot-standby-feedback", false, "Hot standby feedback")
	cmd.Flags().StringVar(&bodyLogicalDecodingWorkMem, "logical-decoding-work-mem", "", "Logical decoding work mem")
	cmd.Flags().StringVar(&bodyMaintenanceWorkMem, "maintenance-work-mem", "", "Maintenance work mem")
	cmd.Flags().IntVar(&bodyMaxConnections, "max-connections", 0, "Max connections")
	cmd.Flags().IntVar(&bodyMaxLocksPerTransaction, "max-locks-per-transaction", 0, "Max locks per transaction")
	cmd.Flags().IntVar(&bodyMaxParallelMaintenanceWorkers, "max-parallel-maintenance-workers", 0, "Max parallel maintenance workers")
	cmd.Flags().IntVar(&bodyMaxParallelWorkers, "max-parallel-workers", 0, "Max parallel workers")
	cmd.Flags().IntVar(&bodyMaxParallelWorkersPerGather, "max-parallel-workers-per-gather", 0, "Max parallel workers per gather")
	cmd.Flags().IntVar(&bodyMaxReplicationSlots, "max-replication-slots", 0, "Max replication slots")
	cmd.Flags().StringVar(&bodyMaxSlotWalKeepSize, "max-slot-wal-keep-size", "", "Max slot wal keep size")
	cmd.Flags().StringVar(&bodyMaxStandbyArchiveDelay, "max-standby-archive-delay", "", "Max standby archive delay")
	cmd.Flags().StringVar(&bodyMaxStandbyStreamingDelay, "max-standby-streaming-delay", "", "Max standby streaming delay")
	cmd.Flags().IntVar(&bodyMaxWalSenders, "max-wal-senders", 0, "Max wal senders")
	cmd.Flags().StringVar(&bodyMaxWalSize, "max-wal-size", "", "Max wal size")
	cmd.Flags().IntVar(&bodyMaxWorkerProcesses, "max-worker-processes", 0, "Max worker processes")
	cmd.Flags().BoolVar(&bodyRestartDatabase, "restart-database", false, "Restart database")
	cmd.Flags().StringVar(&bodySessionReplicationRole, "session-replication-role", "", "Session replication role")
	cmd.Flags().StringVar(&bodySharedBuffers, "shared-buffers", "", "Shared buffers")
	cmd.Flags().StringVar(&bodyStatementTimeout, "statement-timeout", "", "Default unit: ms")
	cmd.Flags().StringVar(&bodyTrackActivityQuerySize, "track-activity-query-size", "", "Track activity query size")
	cmd.Flags().BoolVar(&bodyTrackCommitTimestamp, "track-commit-timestamp", false, "Track commit timestamp")
	cmd.Flags().StringVar(&bodyWalKeepSize, "wal-keep-size", "", "Wal keep size")
	cmd.Flags().StringVar(&bodyWalSenderTimeout, "wal-sender-timeout", "", "Default unit: ms")
	cmd.Flags().StringVar(&bodyWorkMem, "work-mem", "", "Work mem")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
