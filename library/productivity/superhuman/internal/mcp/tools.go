// Copyright 2026 Matt Van Horn and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/productivity/superhuman/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/productivity/superhuman/internal/client"
	"github.com/mvanhorn/printing-press-library/library/productivity/superhuman/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/productivity/superhuman/internal/config"
	"github.com/mvanhorn/printing-press-library/library/productivity/superhuman/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/productivity/superhuman/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("ai_ask",
			mcplib.WithDescription("Ask AI semantic search (SSE stream). Required: query. Returns the new AIResponse."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Natural-language query")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v3/ai.askAIProxy", []mcpParamBinding{{PublicName: "query", WireName: "query", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("attachments_upload",
			mcplib.WithDescription("Upload an attachment for a draft. Required: draft_message_id, thread_id, content_type, content. Returns the new UploadResult."),
			mcplib.WithString("draft_message_id", mcplib.Required(), mcplib.Description("Draft to attach to")),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("Thread ID")),
			mcplib.WithString("content_type", mcplib.Required(), mcplib.Description("MIME type")),
			mcplib.WithString("content", mcplib.Required(), mcplib.Description("Base64-encoded content")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v3/attachments.upload", []mcpParamBinding{{PublicName: "draft_message_id", WireName: "draft_message_id", Location: "body"}, {PublicName: "thread_id", WireName: "thread_id", Location: "body"}, {PublicName: "content_type", WireName: "content_type", Location: "body"}, {PublicName: "content", WireName: "content", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("drafts_list",
			mcplib.WithDescription("List drafts. Optional: limit (default: 50). Returns array of Draft."),
			mcplib.WithString("limit", mcplib.Description("Max drafts")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v3/userdata.getThreads", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("drafts_write",
			mcplib.WithDescription("Create or update a draft (write to draft path). Required: thread_id, draft_id. Optional: subject, body, to (plus 2 more). Returns the new WriteResult."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("Thread ID")),
			mcplib.WithString("draft_id", mcplib.Required(), mcplib.Description("Draft ID (draft00 + 14 hex)")),
			mcplib.WithString("subject", mcplib.Description("Subject")),
			mcplib.WithString("body", mcplib.Description("Body (HTML or plain text)")),
			mcplib.WithString("to", mcplib.Description("To addresses (comma-separated)")),
			mcplib.WithString("cc", mcplib.Description("CC addresses")),
			mcplib.WithString("bcc", mcplib.Description("BCC addresses")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v3/userdata.writeMessage", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "body"}, {PublicName: "draft_id", WireName: "draft_id", Location: "body"}, {PublicName: "subject", WireName: "subject", Location: "body"}, {PublicName: "body", WireName: "body", Location: "body"}, {PublicName: "to", WireName: "to", Location: "body"}, {PublicName: "cc", WireName: "cc", Location: "body"}, {PublicName: "bcc", WireName: "bcc", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("messages_send",
			mcplib.WithDescription("Send a draft (with optional undo delay). Required: draft_message_id, thread_id. Optional: delay (default: 0). Returns the new SendResult."),
			mcplib.WithString("draft_message_id", mcplib.Required(), mcplib.Description("Draft ID (draft00 + 14 hex)")),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("Thread ID for the send")),
			mcplib.WithString("delay", mcplib.Description("Undo window in seconds (0-30)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/messages/send", []mcpParamBinding{{PublicName: "draft_message_id", WireName: "draft_message_id", Location: "body"}, {PublicName: "thread_id", WireName: "thread_id", Location: "body"}, {PublicName: "delay", WireName: "delay", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("reminders_cancel",
			mcplib.WithDescription("Cancel a snooze (un-snooze a thread). Required: reminder_id, thread_id."),
			mcplib.WithString("reminder_id", mcplib.Required(), mcplib.Description("Reminder ID")),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("Thread ID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/reminders/cancel", []mcpParamBinding{{PublicName: "reminder_id", WireName: "reminder_id", Location: "body"}, {PublicName: "thread_id", WireName: "thread_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("reminders_create",
			mcplib.WithDescription("Create a snooze reminder for a thread. Required: thread_id, trigger_at. Returns the new Reminder."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("Thread to snooze")),
			mcplib.WithString("trigger_at", mcplib.Required(), mcplib.Description("ISO timestamp to wake the thread")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/reminders/create", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "body"}, {PublicName: "trigger_at", WireName: "trigger_at", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("teams_suggest",
			mcplib.WithDescription("List teams the user belongs to. Returns array of Team."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v3/teams.suggest?includeSingletons=true", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("threads_get",
			mcplib.WithDescription("Get a thread by ID. Required: thread_id. Returns the new Thread."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("Thread ID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v3/userdata.getThreads", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "query"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_list",
			mcplib.WithDescription("List recent threads. Optional: limit (default: 50), offset (default: 0). Returns array of Thread."),
			mcplib.WithString("limit", mcplib.Description("Max threads to return")),
			mcplib.WithString("offset", mcplib.Description("Pagination offset")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v3/userdata.getThreads", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "body"}, {PublicName: "offset", WireName: "offset", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_achievements",
			mcplib.WithDescription("User achievements / gamification state. Returns the Achievement."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v3/users.achievements", []mcpParamBinding{}, []string{}),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export SUPERHUMAN_JWT=<your-key>" +
					"\n      Run 'superhuman-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export SUPERHUMAN_JWT=<your-key>" +
					"\n      Run 'superhuman-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export SUPERHUMAN_JWT=<your-key>" +
					"\n      Run 'superhuman-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "superhuman-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "superhuman-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "superhuman",
		"description": "Read, draft, and respond to Superhuman email from your terminal or Claude Code. Durable Chrome-session auth, local...",
		"archetype":   "communication",
		"tool_count":  11,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion superhuman-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "SUPERHUMAN_JWT",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "ai",
				"description": "Semantic search via Ask AI",
				"endpoints":   []string{"ask"},
				"searchable":  true,
			},
			{
				"name":        "attachments",
				"description": "Upload, list, download attachments",
				"endpoints":   []string{"upload"},
				"searchable":  true,
			},
			{
				"name":        "drafts",
				"description": "Drafts — create, update, send, delete",
				"endpoints":   []string{"list", "write"},
				"searchable":  true,
			},
			{
				"name":        "messages",
				"description": "Individual messages within threads",
				"endpoints":   []string{"send"},
				"searchable":  true,
			},
			{
				"name":        "reminders",
				"description": "Snooze reminders for threads",
				"endpoints":   []string{"cancel", "create"},
				"searchable":  true,
			},
			{
				"name":        "teams",
				"description": "Team and account info",
				"endpoints":   []string{"suggest"},
				"syncable":    true,
			},
			{
				"name":        "threads",
				"description": "Email threads — read, list, search, archive, label",
				"endpoints":   []string{"get", "list"},
			},
			{
				"name":        "users",
				"description": "User account state",
				"endpoints":   []string{"achievements"},
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Snippets with variables", "command": "snippet use", "description": "Resolve {{name}}, {{company}}, {{today}} placeholders in snippets locally before drafting.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Reminders inbox", "command": "remind list", "description": "Flat queryable list of every reminder across all threads.", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Snippets with variables", "insight": ""},
			{"topic": "Reminders inbox", "insight": ""},
			{"topic": "Message search", "insight": "Use the search tool on synced data rather than paginating through message history. Message APIs often have aggressive rate limits."},
			{"topic": "Channel health", "insight": "When analyzing channel activity, use the channel-health command or sql aggregation on synced messages. Don't iterate individual messages via API."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
