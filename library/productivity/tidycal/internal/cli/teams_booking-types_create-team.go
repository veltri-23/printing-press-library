// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTeamsBookingTypesCreateTeamCmd(flags *rootFlags) *cobra.Command {
	var bodyApprovalRequired bool
	var bodyBookingAvailabilityIntervalMinutes int
	var bodyBookingTypeCategoryId string
	var bodyCurrencyCode string
	var bodyDescription string
	var bodyDisplaySeatsRemaining bool
	var bodyDurationMinutes int
	var bodyLatestAvailabilityDays int
	var bodyMaxBookings int
	var bodyMaxGuestInvitesPerBooker int
	var bodyPaddingMinutes int
	var bodyPaymentPlatform string
	var bodyPrice float64
	var bodyPrivate bool
	var bodyRedirectUrl string
	var bodyTitle string
	var bodyUrlSlug string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-team <team>",
		Aliases:     []string{"create"},
		Short:       "Create a new booking type for a specific team.",
		Example:     "  tidycal-pp-cli teams booking-types create-team 42 --description example-value",
		Annotations: map[string]string{"pp:endpoint": "booking-types.create-team", "pp:method": "POST", "pp:path": "/teams/{team}/booking-types"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "description")
				}
				if !cmd.Flags().Changed("duration-minutes") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "duration-minutes")
				}
				if !cmd.Flags().Changed("title") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "title")
				}
				if !cmd.Flags().Changed("url-slug") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "url-slug")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/teams/{team}/booking-types"
			path = replacePathParam(path, "team", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("approval-required") {
					body["approval_required"] = bodyApprovalRequired
				}
				if bodyBookingAvailabilityIntervalMinutes != 0 {
					body["booking_availability_interval_minutes"] = bodyBookingAvailabilityIntervalMinutes
				}
				if bodyBookingTypeCategoryId != "" {
					body["booking_type_category_id"] = bodyBookingTypeCategoryId
				}
				if bodyCurrencyCode != "" {
					body["currency_code"] = bodyCurrencyCode
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if cmd.Flags().Changed("display-seats-remaining") {
					body["display_seats_remaining"] = bodyDisplaySeatsRemaining
				}
				if bodyDurationMinutes != 0 {
					body["duration_minutes"] = bodyDurationMinutes
				}
				if bodyLatestAvailabilityDays != 0 {
					body["latest_availability_days"] = bodyLatestAvailabilityDays
				}
				if bodyMaxBookings != 0 {
					body["max_bookings"] = bodyMaxBookings
				}
				if bodyMaxGuestInvitesPerBooker != 0 {
					body["max_guest_invites_per_booker"] = bodyMaxGuestInvitesPerBooker
				}
				if bodyPaddingMinutes != 0 {
					body["padding_minutes"] = bodyPaddingMinutes
				}
				if bodyPaymentPlatform != "" {
					body["payment_platform"] = bodyPaymentPlatform
				}
				if bodyPrice != 0.0 {
					body["price"] = bodyPrice
				}
				if cmd.Flags().Changed("private") {
					body["private"] = bodyPrivate
				}
				if bodyRedirectUrl != "" {
					body["redirect_url"] = bodyRedirectUrl
				}
				if bodyTitle != "" {
					body["title"] = bodyTitle
				}
				if bodyUrlSlug != "" {
					body["url_slug"] = bodyUrlSlug
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "booking-types", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "booking-types", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "booking-types", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "booking-types", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "booking-types", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "booking-types",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "booking-types", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "booking-types", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyApprovalRequired, "approval-required", false, "Approval required")
	cmd.Flags().IntVar(&bodyBookingAvailabilityIntervalMinutes, "booking-availability-interval-minutes", 15, "Booking availability interval minutes")
	cmd.Flags().StringVar(&bodyBookingTypeCategoryId, "booking-type-category-id", "", "Booking type category id")
	cmd.Flags().StringVar(&bodyCurrencyCode, "currency-code", "", "ISO 4217 currency code (e.g. USD, EUR, GBP). Defaults to your account currency.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().BoolVar(&bodyDisplaySeatsRemaining, "display-seats-remaining", false, "Display seats remaining")
	cmd.Flags().IntVar(&bodyDurationMinutes, "duration-minutes", 0, "Duration minutes")
	cmd.Flags().IntVar(&bodyLatestAvailabilityDays, "latest-availability-days", 60, "Latest availability days")
	cmd.Flags().IntVar(&bodyMaxBookings, "max-bookings", 1, "Max bookings")
	cmd.Flags().IntVar(&bodyMaxGuestInvitesPerBooker, "max-guest-invites-per-booker", 0, "Max guest invites per booker")
	cmd.Flags().IntVar(&bodyPaddingMinutes, "padding-minutes", 0, "Padding minutes")
	cmd.Flags().StringVar(&bodyPaymentPlatform, "payment-platform", "", "Payment platform to use. The platform must be connected in your account Integrations settings. Required when price > 0.")
	cmd.Flags().Float64Var(&bodyPrice, "price", 0.000000, "Price for the booking type. Set to 0 for free booking types.")
	cmd.Flags().BoolVar(&bodyPrivate, "private", false, "Private")
	cmd.Flags().StringVar(&bodyRedirectUrl, "redirect-url", "", "Redirect url")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Title")
	cmd.Flags().StringVar(&bodyUrlSlug, "url-slug", "", "Url slug")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
