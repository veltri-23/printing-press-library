// Copyright 2026 Giuliano Giacaglia and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newDomainsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyCapabilitiesReceiving string
	var bodyCapabilitiesSending string
	var bodyClickTracking bool
	var bodyCustomReturnPath string
	var bodyName string
	var bodyOpenTracking bool
	var bodyRegion string
	var bodyTls string
	var bodyTrackingSubdomain string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a new domain",
		Example:     "  resend-pp-cli domains create --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "domains.create", "pp:method": "POST", "pp:path": "/domains"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/domains"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedCapabilities := map[string]any{}
					if bodyCapabilitiesReceiving != "" {
						nestedCapabilities["receiving"] = bodyCapabilitiesReceiving
					}
					if bodyCapabilitiesSending != "" {
						nestedCapabilities["sending"] = bodyCapabilitiesSending
					}
					if len(nestedCapabilities) > 0 {
						body["capabilities"] = nestedCapabilities
					}
				}
				if bodyClickTracking != false {
					body["click_tracking"] = bodyClickTracking
				}
				if bodyCustomReturnPath != "" {
					body["custom_return_path"] = bodyCustomReturnPath
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyOpenTracking != false {
					body["open_tracking"] = bodyOpenTracking
				}
				if bodyRegion != "" {
					body["region"] = bodyRegion
				}
				if bodyTls != "" {
					body["tls"] = bodyTls
				}
				if bodyTrackingSubdomain != "" {
					body["tracking_subdomain"] = bodyTrackingSubdomain
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "domains",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCapabilitiesReceiving, "capabilities-receiving", "", "Enable or disable receiving emails to this domain.")
	cmd.Flags().StringVar(&bodyCapabilitiesSending, "capabilities-sending", "", "Enable or disable sending emails from this domain.")
	cmd.Flags().BoolVar(&bodyClickTracking, "click-tracking", false, "Track clicks within the body of each HTML email.")
	cmd.Flags().StringVar(&bodyCustomReturnPath, "custom-return-path", "", "For advanced use cases, choose a subdomain for the Return-Path address. Defaults to 'send' (i.e., send.yourdomain.tld).")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the domain you want to create.")
	cmd.Flags().BoolVar(&bodyOpenTracking, "open-tracking", false, "Track the open rate of each email.")
	cmd.Flags().StringVar(&bodyRegion, "region", "us-east-1", "The region where emails will be sent from. Possible values are us-east-1 | eu-west-1 | sa-east-1 | ap-northeast-1")
	cmd.Flags().StringVar(&bodyTls, "tls", "opportunistic", "TLS mode. Opportunistic attempts secure connection but falls back to unencrypted. Enforced requires TLS or email...")
	cmd.Flags().StringVar(&bodyTrackingSubdomain, "tracking-subdomain", "", "The subdomain to use for click and open tracking.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
