// Copyright 2026 Paul Brennaman and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newMailboxSettingsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAutomaticRepliesSetting string
	var bodyTimeZone string
	var bodyLanguage string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update mailbox settings (automatic replies, timezone, working hours, language)",
		Example:     "  outlook-email-pp-cli mailbox_settings update",
		Annotations: map[string]string{"pp:endpoint": "mailbox_settings.update", "pp:method": "PATCH", "pp:path": "/me/mailboxSettings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/me/mailboxSettings"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAutomaticRepliesSetting != "" {
					var parsedAutomaticRepliesSetting any
					if err := json.Unmarshal([]byte(bodyAutomaticRepliesSetting), &parsedAutomaticRepliesSetting); err != nil {
						return fmt.Errorf("parsing --automatic-replies-setting JSON: %w", err)
					}
					body["automaticRepliesSetting"] = parsedAutomaticRepliesSetting
				}
				if bodyTimeZone != "" {
					body["timeZone"] = bodyTimeZone
				}
				if bodyLanguage != "" {
					var parsedLanguage any
					if err := json.Unmarshal([]byte(bodyLanguage), &parsedLanguage); err != nil {
						return fmt.Errorf("parsing --language JSON: %w", err)
					}
					body["language"] = parsedLanguage
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "mailbox_settings",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAutomaticRepliesSetting, "automatic-replies-setting", "", "Auto-reply struct (status, internalReplyMessage, externalReplyMessage, scheduledStartDateTime, scheduledEndDateTime,...")
	cmd.Flags().StringVar(&bodyTimeZone, "time-zone", "", "Outlook time zone name (e.g. 'Pacific Standard Time')")
	cmd.Flags().StringVar(&bodyLanguage, "language", "", "Locale struct ({locale, displayName})")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
