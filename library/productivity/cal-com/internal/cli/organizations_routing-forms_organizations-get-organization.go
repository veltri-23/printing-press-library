// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsRoutingFormsOrganizationsGetOrganizationCmd(flags *rootFlags) *cobra.Command {
	var flagSkip float64
	var flagTake float64
	var flagSortCreatedAt string
	var flagSortUpdatedAt string
	var flagAfterCreatedAt string
	var flagBeforeCreatedAt string
	var flagAfterUpdatedAt string
	var flagBeforeUpdatedAt string
	var flagRoutedToBookingUid string
	var flagTeamIds string

	cmd := &cobra.Command{
		Use:         "organizations-get-organization <orgId>",
		Aliases:     []string{"get"},
		Short:       "Required membership role: `org admin`. PBAC permission: `routingForm.read`. Learn more about API access control at...",
		Example:     "  cal-com-pp-cli organizations routing-forms organizations-get-organization 42",
		Annotations: map[string]string{"pp:endpoint": "routing-forms.organizations-get-organization", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("sort-created-at") {
				allowedSortCreatedAt := []string{"asc", "desc"}
				validSortCreatedAt := false
				for _, v := range allowedSortCreatedAt {
					if flagSortCreatedAt == v {
						validSortCreatedAt = true
						break
					}
				}
				if !validSortCreatedAt {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-created-at", flagSortCreatedAt, allowedSortCreatedAt)
				}
			}
			if cmd.Flags().Changed("sort-updated-at") {
				allowedSortUpdatedAt := []string{"asc", "desc"}
				validSortUpdatedAt := false
				for _, v := range allowedSortUpdatedAt {
					if flagSortUpdatedAt == v {
						validSortUpdatedAt = true
						break
					}
				}
				if !validSortUpdatedAt {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-updated-at", flagSortUpdatedAt, allowedSortUpdatedAt)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/organizations/{orgId}/routing-forms"
			path = replacePathParam(path, "orgId", args[0])
			params := map[string]string{}
			if flagSkip != 0.0 {
				params["skip"] = fmt.Sprintf("%v", flagSkip)
			}
			if flagTake != 0.0 {
				params["take"] = fmt.Sprintf("%v", flagTake)
			}
			if flagSortCreatedAt != "" {
				params["sortCreatedAt"] = fmt.Sprintf("%v", flagSortCreatedAt)
			}
			if flagSortUpdatedAt != "" {
				params["sortUpdatedAt"] = fmt.Sprintf("%v", flagSortUpdatedAt)
			}
			if flagAfterCreatedAt != "" {
				params["afterCreatedAt"] = fmt.Sprintf("%v", flagAfterCreatedAt)
			}
			if flagBeforeCreatedAt != "" {
				params["beforeCreatedAt"] = fmt.Sprintf("%v", flagBeforeCreatedAt)
			}
			if flagAfterUpdatedAt != "" {
				params["afterUpdatedAt"] = fmt.Sprintf("%v", flagAfterUpdatedAt)
			}
			if flagBeforeUpdatedAt != "" {
				params["beforeUpdatedAt"] = fmt.Sprintf("%v", flagBeforeUpdatedAt)
			}
			if flagRoutedToBookingUid != "" {
				params["routedToBookingUid"] = fmt.Sprintf("%v", flagRoutedToBookingUid)
			}
			if flagTeamIds != "" {
				params["teamIds"] = fmt.Sprintf("%v", flagTeamIds)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "routing-forms", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().Float64Var(&flagSkip, "skip", 0.0, "Number of responses to skip")
	cmd.Flags().Float64Var(&flagTake, "take", 0.0, "Number of responses to take")
	cmd.Flags().StringVar(&flagSortCreatedAt, "sort-created-at", "", "Sort by creation time (one of: asc, desc)")
	cmd.Flags().StringVar(&flagSortUpdatedAt, "sort-updated-at", "", "Sort by update time (one of: asc, desc)")
	cmd.Flags().StringVar(&flagAfterCreatedAt, "after-created-at", "", "Filter by responses created after this date")
	cmd.Flags().StringVar(&flagBeforeCreatedAt, "before-created-at", "", "Filter by responses created before this date")
	cmd.Flags().StringVar(&flagAfterUpdatedAt, "after-updated-at", "", "Filter by responses created after this date")
	cmd.Flags().StringVar(&flagBeforeUpdatedAt, "before-updated-at", "", "Filter by responses updated before this date")
	cmd.Flags().StringVar(&flagRoutedToBookingUid, "routed-to-booking-uid", "", "Filter by responses routed to a specific booking")
	cmd.Flags().StringVar(&flagTeamIds, "team-ids", "", "Filter by teamIds. Team ids must be separated by a comma.")

	return cmd
}
