// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTeamsEventTypesTeamsCreatePhoneCallCmd(flags *rootFlags) *cobra.Command {
	var bodyBeginMessage string
	var bodyCalApiKey string
	var bodyEnabled bool
	var bodyGeneralPrompt string
	var bodyGuestCompany string
	var bodyGuestEmail string
	var bodyGuestName string
	var bodyNumberToCall string
	var bodySchedulerName string
	var bodyTemplateType string
	var bodyYourPhoneNumber string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "teams-create-phone-call <teamId> <eventTypeId>",
		Aliases:     []string{"create"},
		Short:       "If accessed using an OAuth access token, the `TEAM_EVENT_TYPE_WRITE` scope is required.",
		Example:     "  cal-com-pp-cli teams event-types teams-create-phone-call 42 42 --calApiKey your-token-here",
		Annotations: map[string]string{"pp:endpoint": "event-types.teams-create-phone-call"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("cal-api-key") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "cal-api-key")
				}
				if !cmd.Flags().Changed("number-to-call") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "number-to-call")
				}
				if !cmd.Flags().Changed("your-phone-number") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "your-phone-number")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/teams/{teamId}/event-types/{eventTypeId}/create-phone-call"
			path = replacePathParam(path, "teamId", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("eventTypeId is required\nUsage: %s <%s>", cmd.CommandPath(), "eventTypeId"))
			}
			path = replacePathParam(path, "eventTypeId", args[1])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyBeginMessage != "" {
					body["beginMessage"] = bodyBeginMessage
				}
				if bodyCalApiKey != "" {
					body["calApiKey"] = bodyCalApiKey
				}
				if bodyEnabled != false {
					body["enabled"] = bodyEnabled
				}
				if bodyGeneralPrompt != "" {
					body["generalPrompt"] = bodyGeneralPrompt
				}
				if bodyGuestCompany != "" {
					body["guestCompany"] = bodyGuestCompany
				}
				if bodyGuestEmail != "" {
					body["guestEmail"] = bodyGuestEmail
				}
				if bodyGuestName != "" {
					body["guestName"] = bodyGuestName
				}
				if bodyNumberToCall != "" {
					body["numberToCall"] = bodyNumberToCall
				}
				if bodySchedulerName != "" {
					body["schedulerName"] = bodySchedulerName
				}
				if bodyTemplateType != "" {
					body["templateType"] = bodyTemplateType
				}
				if bodyYourPhoneNumber != "" {
					body["yourPhoneNumber"] = bodyYourPhoneNumber
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "event-types",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyBeginMessage, "begin-message", "", "Begin message")
	cmd.Flags().StringVar(&bodyCalApiKey, "cal-api-key", "", "CAL API Key")
	cmd.Flags().BoolVar(&bodyEnabled, "enabled", true, "Enabled status")
	cmd.Flags().StringVar(&bodyGeneralPrompt, "general-prompt", "", "General prompt")
	cmd.Flags().StringVar(&bodyGuestCompany, "guest-company", "", "Guest company")
	cmd.Flags().StringVar(&bodyGuestEmail, "guest-email", "", "Guest email")
	cmd.Flags().StringVar(&bodyGuestName, "guest-name", "", "Guest name")
	cmd.Flags().StringVar(&bodyNumberToCall, "number-to-call", "", "Number to call")
	cmd.Flags().StringVar(&bodySchedulerName, "scheduler-name", "", "Scheduler name")
	cmd.Flags().StringVar(&bodyTemplateType, "template-type", "CUSTOM_TEMPLATE", "Template type")
	cmd.Flags().StringVar(&bodyYourPhoneNumber, "your-phone-number", "", "Your phone number")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
