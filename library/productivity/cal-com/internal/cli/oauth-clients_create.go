// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOauthClientsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAreCalendarEventsEnabled bool
	var bodyAreDefaultEventTypesEnabled bool
	var bodyAreEmailsEnabled bool
	var bodyBookingCancelRedirectUri string
	var bodyBookingRedirectUri string
	var bodyBookingRescheduleRedirectUri string
	var bodyLogo string
	var bodyName string
	var bodyPermissions string
	var bodyRedirectUris string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "<Warning>These endpoints are deprecated and will be removed in the future.</Warning>",
		Example:     "  cal-com-pp-cli oauth-clients create --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "oauth-clients.create"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("permissions") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "permissions")
				}
				if !cmd.Flags().Changed("redirect-uris") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "redirect-uris")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/oauth-clients"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAreCalendarEventsEnabled != false {
					body["areCalendarEventsEnabled"] = bodyAreCalendarEventsEnabled
				}
				if bodyAreDefaultEventTypesEnabled != false {
					body["areDefaultEventTypesEnabled"] = bodyAreDefaultEventTypesEnabled
				}
				if bodyAreEmailsEnabled != false {
					body["areEmailsEnabled"] = bodyAreEmailsEnabled
				}
				if bodyBookingCancelRedirectUri != "" {
					body["bookingCancelRedirectUri"] = bodyBookingCancelRedirectUri
				}
				if bodyBookingRedirectUri != "" {
					body["bookingRedirectUri"] = bodyBookingRedirectUri
				}
				if bodyBookingRescheduleRedirectUri != "" {
					body["bookingRescheduleRedirectUri"] = bodyBookingRescheduleRedirectUri
				}
				if bodyLogo != "" {
					body["logo"] = bodyLogo
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPermissions != "" {
					var parsedPermissions any
					if err := json.Unmarshal([]byte(bodyPermissions), &parsedPermissions); err != nil {
						return fmt.Errorf("parsing --permissions JSON: %w", err)
					}
					body["permissions"] = parsedPermissions
				}
				if bodyRedirectUris != "" {
					var parsedRedirectUris any
					if err := json.Unmarshal([]byte(bodyRedirectUris), &parsedRedirectUris); err != nil {
						return fmt.Errorf("parsing --redirect-uris JSON: %w", err)
					}
					body["redirectUris"] = parsedRedirectUris
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "oauth-clients",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAreCalendarEventsEnabled, "are-calendar-events-enabled", true, "If true and if managed user has calendar connected, calendar events will be created. Disable it if you manually...")
	cmd.Flags().BoolVar(&bodyAreDefaultEventTypesEnabled, "are-default-event-types-enabled", false, "If true, when creating a managed user the managed user will have 4 default event types: 30 and 60 minutes without...")
	cmd.Flags().BoolVar(&bodyAreEmailsEnabled, "are-emails-enabled", false, "Are emails enabled")
	cmd.Flags().StringVar(&bodyBookingCancelRedirectUri, "booking-cancel-redirect-uri", "", "Booking cancel redirect uri")
	cmd.Flags().StringVar(&bodyBookingRedirectUri, "booking-redirect-uri", "", "Booking redirect uri")
	cmd.Flags().StringVar(&bodyBookingRescheduleRedirectUri, "booking-reschedule-redirect-uri", "", "Booking reschedule redirect uri")
	cmd.Flags().StringVar(&bodyLogo, "logo", "", "Logo")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyPermissions, "permissions", "", "Array of permission keys like ['BOOKING_READ', 'BOOKING_WRITE']. Use ['*'] to grant all permissions.")
	cmd.Flags().StringVar(&bodyRedirectUris, "redirect-uris", "", "Redirect uris")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
