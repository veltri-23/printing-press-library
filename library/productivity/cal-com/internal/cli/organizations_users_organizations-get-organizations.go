// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsUsersOrganizationsGetOrganizationsCmd(flags *rootFlags) *cobra.Command {
	var flagTake float64
	var flagSkip float64
	var flagEmails string
	var flagAssignedOptionIds string
	var flagAttributeQueryOperator string
	var flagTeamIds string

	cmd := &cobra.Command{
		Use:         "organizations-get-organizations <orgId>",
		Short:       "Required membership role: `org admin`. PBAC permission: `organization.listMembers`. Learn more about API access...",
		Example:     "  cal-com-pp-cli organizations users organizations-get-organizations 42",
		Annotations: map[string]string{"pp:endpoint": "users.organizations-get-organizations", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("attribute-query-operator") {
				allowedAttributeQueryOperator := []string{"OR", "AND", "NONE"}
				validAttributeQueryOperator := false
				for _, v := range allowedAttributeQueryOperator {
					if flagAttributeQueryOperator == v {
						validAttributeQueryOperator = true
						break
					}
				}
				if !validAttributeQueryOperator {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "attribute-query-operator", flagAttributeQueryOperator, allowedAttributeQueryOperator)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/organizations/{orgId}/users"
			path = replacePathParam(path, "orgId", args[0])
			params := map[string]string{}
			if flagTake != 0.0 {
				params["take"] = fmt.Sprintf("%v", flagTake)
			}
			if flagSkip != 0.0 {
				params["skip"] = fmt.Sprintf("%v", flagSkip)
			}
			if flagEmails != "" {
				params["emails"] = fmt.Sprintf("%v", flagEmails)
			}
			if flagAssignedOptionIds != "" {
				params["assignedOptionIds"] = fmt.Sprintf("%v", flagAssignedOptionIds)
			}
			if flagAttributeQueryOperator != "" {
				params["attributeQueryOperator"] = fmt.Sprintf("%v", flagAttributeQueryOperator)
			}
			if flagTeamIds != "" {
				params["teamIds"] = fmt.Sprintf("%v", flagTeamIds)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "users", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().Float64Var(&flagTake, "take", 0.0, "The number of items to return")
	cmd.Flags().Float64Var(&flagSkip, "skip", 0.0, "The number of items to skip")
	cmd.Flags().StringVar(&flagEmails, "emails", "", "The email address or an array of email addresses to filter by")
	cmd.Flags().StringVar(&flagAssignedOptionIds, "assigned-option-ids", "", "Filter by assigned attribute option ids. ids must be separated by a comma.")
	cmd.Flags().StringVar(&flagAttributeQueryOperator, "attribute-query-operator", "AND", "Query operator used to filter assigned options, AND by default. (one of: OR, AND, NONE)")
	cmd.Flags().StringVar(&flagTeamIds, "team-ids", "", "Filter by teamIds. Team ids must be separated by a comma.")

	return cmd
}
