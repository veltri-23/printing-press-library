// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSelectedCalendarsDeleteCmd(flags *rootFlags) *cobra.Command {
	var flagIntegration string
	var flagExternalId string
	var flagCredentialId string
	var flagDelegationCredentialId string

	cmd := &cobra.Command{
		Use:         "delete",
		Short:       "If accessed using an OAuth access token, the `APPS_WRITE` scope is required.",
		Example:     "  cal-com-pp-cli selected-calendars delete",
		Annotations: map[string]string{"pp:endpoint": "selected-calendars.delete"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("integration") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "integration")
			}
			if !cmd.Flags().Changed("external-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "external-id")
			}
			if !cmd.Flags().Changed("credential-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "credential-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/selected-calendars"
			data, statusCode, err := c.Delete(path)
			if err != nil {
				return classifyDeleteError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "delete",
					"resource": "selected-calendars",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagIntegration, "integration", "", "Integration")
	cmd.Flags().StringVar(&flagExternalId, "external-id", "", "External id")
	cmd.Flags().StringVar(&flagCredentialId, "credential-id", "", "Credential id")
	cmd.Flags().StringVar(&flagDelegationCredentialId, "delegation-credential-id", "", "Delegation credential id")

	return cmd
}
