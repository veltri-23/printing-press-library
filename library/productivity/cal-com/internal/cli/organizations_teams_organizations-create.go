// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsTeamsOrganizationsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAppIconLogo string
	var bodyAppLogo string
	var bodyAutoAcceptCreator bool
	var bodyBannerUrl string
	var bodyBio string
	var bodyBrandColor string
	var bodyCalVideoLogo string
	var bodyDarkBrandColor string
	var bodyHideBookATeamMember bool
	var bodyHideBranding bool
	var bodyIsPrivate bool
	var bodyLogoUrl string
	var bodyName string
	var bodySlug string
	var bodyTheme string
	var bodyTimeFormat float64
	var bodyTimeZone string
	var bodyWeekStart string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "organizations-create <orgId>",
		Short:       "Required membership role: `org admin`. PBAC permission: `team.create`. Learn more about API access control at...",
		Example:     "  cal-com-pp-cli organizations teams organizations-create 42 --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "teams.organizations-create"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/organizations/{orgId}/teams"
			path = replacePathParam(path, "orgId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAppIconLogo != "" {
					body["appIconLogo"] = bodyAppIconLogo
				}
				if bodyAppLogo != "" {
					body["appLogo"] = bodyAppLogo
				}
				if bodyAutoAcceptCreator != false {
					body["autoAcceptCreator"] = bodyAutoAcceptCreator
				}
				if bodyBannerUrl != "" {
					body["bannerUrl"] = bodyBannerUrl
				}
				if bodyBio != "" {
					body["bio"] = bodyBio
				}
				if bodyBrandColor != "" {
					body["brandColor"] = bodyBrandColor
				}
				if bodyCalVideoLogo != "" {
					body["calVideoLogo"] = bodyCalVideoLogo
				}
				if bodyDarkBrandColor != "" {
					body["darkBrandColor"] = bodyDarkBrandColor
				}
				if bodyHideBookATeamMember != false {
					body["hideBookATeamMember"] = bodyHideBookATeamMember
				}
				if bodyHideBranding != false {
					body["hideBranding"] = bodyHideBranding
				}
				if bodyIsPrivate != false {
					body["isPrivate"] = bodyIsPrivate
				}
				if bodyLogoUrl != "" {
					body["logoUrl"] = bodyLogoUrl
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodySlug != "" {
					body["slug"] = bodySlug
				}
				if bodyTheme != "" {
					body["theme"] = bodyTheme
				}
				if bodyTimeFormat != 0.0 {
					body["timeFormat"] = bodyTimeFormat
				}
				if bodyTimeZone != "" {
					body["timeZone"] = bodyTimeZone
				}
				if bodyWeekStart != "" {
					body["weekStart"] = bodyWeekStart
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "teams",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAppIconLogo, "app-icon-logo", "", "App icon logo")
	cmd.Flags().StringVar(&bodyAppLogo, "app-logo", "", "App logo")
	cmd.Flags().BoolVar(&bodyAutoAcceptCreator, "auto-accept-creator", true, "If you are a platform customer, don't pass 'false', because then team creator won't be able to create team event types.")
	cmd.Flags().StringVar(&bodyBannerUrl, "banner-url", "", "URL of the teams banner image which is shown on booker")
	cmd.Flags().StringVar(&bodyBio, "bio", "", "Bio")
	cmd.Flags().StringVar(&bodyBrandColor, "brand-color", "", "Brand color")
	cmd.Flags().StringVar(&bodyCalVideoLogo, "cal-video-logo", "", "Cal video logo")
	cmd.Flags().StringVar(&bodyDarkBrandColor, "dark-brand-color", "", "Dark brand color")
	cmd.Flags().BoolVar(&bodyHideBookATeamMember, "hide-book-a-team-member", false, "Hide book ateam member")
	cmd.Flags().BoolVar(&bodyHideBranding, "hide-branding", false, "Hide branding")
	cmd.Flags().BoolVar(&bodyIsPrivate, "is-private", false, "Is private")
	cmd.Flags().StringVar(&bodyLogoUrl, "logo-url", "", "URL of the teams logo image")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name of the team")
	cmd.Flags().StringVar(&bodySlug, "slug", "", "Team slug in kebab-case - if not provided will be generated automatically based on name.")
	cmd.Flags().StringVar(&bodyTheme, "theme", "", "Theme")
	cmd.Flags().Float64Var(&bodyTimeFormat, "time-format", 0.0, "Time format")
	cmd.Flags().StringVar(&bodyTimeZone, "time-zone", "Europe/London", "Timezone is used to create teams's default schedule from Monday to Friday from 9AM to 5PM. It will default to...")
	cmd.Flags().StringVar(&bodyWeekStart, "week-start", "Sunday", "Week start")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
