// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTeamsBookingsTeamsGetAllTeamCmd(flags *rootFlags) *cobra.Command {
	var flagStatus string
	var flagAttendeeEmail string
	var flagAttendeeName string
	var flagBookingUid string
	var flagEventTypeIds string
	var flagEventTypeId string
	var flagAfterStart string
	var flagBeforeEnd string
	var flagSortStart string
	var flagSortEnd string
	var flagSortCreated string
	var flagTake float64
	var flagSkip float64

	cmd := &cobra.Command{
		Use:         "teams-get-all-team <teamId>",
		Aliases:     []string{"get"},
		Short:       "If accessed using an OAuth access token, the `TEAM_BOOKING_READ` scope is required.",
		Example:     "  cal-com-pp-cli teams bookings teams-get-all-team 42",
		Annotations: map[string]string{"pp:endpoint": "bookings.teams-get-all-team", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("sort-start") {
				allowedSortStart := []string{"asc", "desc"}
				validSortStart := false
				for _, v := range allowedSortStart {
					if flagSortStart == v {
						validSortStart = true
						break
					}
				}
				if !validSortStart {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-start", flagSortStart, allowedSortStart)
				}
			}
			if cmd.Flags().Changed("sort-end") {
				allowedSortEnd := []string{"asc", "desc"}
				validSortEnd := false
				for _, v := range allowedSortEnd {
					if flagSortEnd == v {
						validSortEnd = true
						break
					}
				}
				if !validSortEnd {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-end", flagSortEnd, allowedSortEnd)
				}
			}
			if cmd.Flags().Changed("sort-created") {
				allowedSortCreated := []string{"asc", "desc"}
				validSortCreated := false
				for _, v := range allowedSortCreated {
					if flagSortCreated == v {
						validSortCreated = true
						break
					}
				}
				if !validSortCreated {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-created", flagSortCreated, allowedSortCreated)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/teams/{teamId}/bookings"
			if len(args) < 14 {
				return usageErr(fmt.Errorf("teamId is required\nUsage: %s <%s>", cmd.CommandPath(), "teamId"))
			}
			path = replacePathParam(path, "teamId", args[13])
			params := map[string]string{}
			if flagStatus != "" {
				params["status"] = fmt.Sprintf("%v", flagStatus)
			}
			if flagAttendeeEmail != "" {
				params["attendeeEmail"] = fmt.Sprintf("%v", flagAttendeeEmail)
			}
			if flagAttendeeName != "" {
				params["attendeeName"] = fmt.Sprintf("%v", flagAttendeeName)
			}
			if flagBookingUid != "" {
				params["bookingUid"] = fmt.Sprintf("%v", flagBookingUid)
			}
			if flagEventTypeIds != "" {
				params["eventTypeIds"] = fmt.Sprintf("%v", flagEventTypeIds)
			}
			if flagEventTypeId != "" {
				params["eventTypeId"] = fmt.Sprintf("%v", flagEventTypeId)
			}
			if flagAfterStart != "" {
				params["afterStart"] = fmt.Sprintf("%v", flagAfterStart)
			}
			if flagBeforeEnd != "" {
				params["beforeEnd"] = fmt.Sprintf("%v", flagBeforeEnd)
			}
			if flagSortStart != "" {
				params["sortStart"] = fmt.Sprintf("%v", flagSortStart)
			}
			if flagSortEnd != "" {
				params["sortEnd"] = fmt.Sprintf("%v", flagSortEnd)
			}
			if flagSortCreated != "" {
				params["sortCreated"] = fmt.Sprintf("%v", flagSortCreated)
			}
			if flagTake != 0.0 {
				params["take"] = fmt.Sprintf("%v", flagTake)
			}
			if flagSkip != 0.0 {
				params["skip"] = fmt.Sprintf("%v", flagSkip)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "bookings", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagStatus, "status", "", "Filter bookings by status. If you want to filter by multiple statuses, separate them with a comma.")
	cmd.Flags().StringVar(&flagAttendeeEmail, "attendee-email", "", "Filter bookings by the attendee's email address.")
	cmd.Flags().StringVar(&flagAttendeeName, "attendee-name", "", "Filter bookings by the attendee's name.")
	cmd.Flags().StringVar(&flagBookingUid, "booking-uid", "", "Filter bookings by the booking Uid.")
	cmd.Flags().StringVar(&flagEventTypeIds, "event-type-ids", "", "Filter bookings by event type ids belonging to the team. Event type ids must be separated by a comma.")
	cmd.Flags().StringVar(&flagEventTypeId, "event-type-id", "", "Filter bookings by event type id belonging to the team.")
	cmd.Flags().StringVar(&flagAfterStart, "after-start", "", "Filter bookings with start after this date string.")
	cmd.Flags().StringVar(&flagBeforeEnd, "before-end", "", "Filter bookings with end before this date string.")
	cmd.Flags().StringVar(&flagSortStart, "sort-start", "", "Sort results by their start time in ascending or descending order. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagSortEnd, "sort-end", "", "Sort results by their end time in ascending or descending order. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagSortCreated, "sort-created", "", "Sort results by their creation time (when booking was made) in ascending or descending order. (one of: asc, desc)")
	cmd.Flags().Float64Var(&flagTake, "take", 0.0, "The number of items to return")
	cmd.Flags().Float64Var(&flagSkip, "skip", 0.0, "The number of items to skip")

	return cmd
}
