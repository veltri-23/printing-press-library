// Copyright 2026 salmonumbrella and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMeCreditCardsListCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagIsActive bool
	var flagIsExpired bool
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagUsageLocation float64
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "credit-cards-list",
		Short:       "Credit cards list",
		Example:     "  marianatek-pp-cli me credit-cards-list",
		Annotations: map[string]string{"pp:endpoint": "me.credit-cards-list", "pp:method": "GET", "pp:path": "/me/credit_cards", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/me/credit_cards"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "me", path, map[string]string{
				"id":             fmt.Sprintf("%v", flagId),
				"is_active":      fmt.Sprintf("%v", flagIsActive),
				"is_expired":     fmt.Sprintf("%v", flagIsExpired),
				"ordering":       fmt.Sprintf("%v", flagOrdering),
				"page":           fmt.Sprintf("%v", flagPage),
				"page_size":      fmt.Sprintf("%v", flagPageSize),
				"usage_location": fmt.Sprintf("%v", flagUsageLocation),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().BoolVar(&flagIsActive, "is-active", false, "Is active")
	cmd.Flags().BoolVar(&flagIsExpired, "is-expired", false, "Is expired")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().Float64Var(&flagUsageLocation, "usage-location", 0.0, "Usage location")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
