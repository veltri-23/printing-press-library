// Copyright 2026 Matt Van Horn and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newUsersPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagLimit string
	var flagCursor string

	cmd := &cobra.Command{
		Use:   "users",
		Short: "List all users in the workspace",
		Long:  "Shortcut for 'users list'. List all users in the workspace",
		Example: `  slack-pp-cli users --limit 50
  slack-pp-cli users info --user U0123456789
  slack-pp-cli users lookup_by_email --email alice@example.com`,
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/users.list"
			params := map[string]string{}
			if flagLimit != "" {
				params["limit"] = fmt.Sprintf("%v", flagLimit)
			}
			if flagCursor != "" {
				params["cursor"] = fmt.Sprintf("%v", flagCursor)
			}
			data, prov, err := resolveRead(c, flags, "users", false, path, params)
			if err != nil {
				return classifyAPIError(err)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagLimit, "limit", "", "Maximum number of users to return")
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "Pagination cursor")

	// Wire sibling endpoints and sub-resources as subcommands
	cmd.AddCommand(newUsersGetPresenceCmd(flags))
	cmd.AddCommand(newUsersInfoCmd(flags))
	cmd.AddCommand(newUsersLookupByEmailCmd(flags))
	cmd.AddCommand(newUsersProfileGetCmd(flags))
	cmd.AddCommand(newUsersProfileSetCmd(flags))
	cmd.AddCommand(newUsersSetPresenceCmd(flags))

	return cmd
}
