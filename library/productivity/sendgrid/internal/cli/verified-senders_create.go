// Copyright 2026 Nathan Kettles and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newVerifiedSendersCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAddress string
	var bodyAddress2 string
	var bodyCity string
	var bodyCountry string
	var bodyFromEmail string
	var bodyFromName string
	var bodyNickname string
	var bodyReplyTo string
	var bodyReplyToName string
	var bodyState string
	var bodyZip string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "**This endpoint allows you to create a new Sender Identify**. Upon successful submission of a `POST` request to this...",
		Example:     "  sendgrid-pp-cli verified-senders create --from-email user@example.com",
		Annotations: map[string]string{"pp:endpoint": "verified-senders.create", "pp:method": "POST", "pp:path": "/v3/verified_senders"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("from-email") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "from-email")
				}
				if !cmd.Flags().Changed("nickname") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "nickname")
				}
				if !cmd.Flags().Changed("reply-to") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "reply-to")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v3/verified_senders"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAddress != "" {
					body["address"] = bodyAddress
				}
				if bodyAddress2 != "" {
					body["address2"] = bodyAddress2
				}
				if bodyCity != "" {
					body["city"] = bodyCity
				}
				if bodyCountry != "" {
					body["country"] = bodyCountry
				}
				if bodyFromEmail != "" {
					body["from_email"] = bodyFromEmail
				}
				if bodyFromName != "" {
					body["from_name"] = bodyFromName
				}
				if bodyNickname != "" {
					body["nickname"] = bodyNickname
				}
				if bodyReplyTo != "" {
					body["reply_to"] = bodyReplyTo
				}
				if bodyReplyToName != "" {
					body["reply_to_name"] = bodyReplyToName
				}
				if bodyState != "" {
					body["state"] = bodyState
				}
				if bodyZip != "" {
					body["zip"] = bodyZip
				}
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "verified-senders",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAddress, "address", "", "Address")
	cmd.Flags().StringVar(&bodyAddress2, "address2", "", "Address2")
	cmd.Flags().StringVar(&bodyCity, "city", "", "City")
	cmd.Flags().StringVar(&bodyCountry, "country", "", "Country")
	cmd.Flags().StringVar(&bodyFromEmail, "from-email", "", "From email")
	cmd.Flags().StringVar(&bodyFromName, "from-name", "", "From name")
	cmd.Flags().StringVar(&bodyNickname, "nickname", "", "Nickname")
	cmd.Flags().StringVar(&bodyReplyTo, "reply-to", "", "Reply to")
	cmd.Flags().StringVar(&bodyReplyToName, "reply-to-name", "", "Reply to name")
	cmd.Flags().StringVar(&bodyState, "state", "", "State")
	cmd.Flags().StringVar(&bodyZip, "zip", "", "Zip")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
