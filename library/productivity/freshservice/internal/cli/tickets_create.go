// Copyright 2026 Mark van de Ven and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTicketsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyDescription string
	var bodyDueBy string
	var bodyEmail string
	var bodyGroupId int
	var bodyPriority int
	var bodyRequesterId int
	var bodyResponderId int
	var bodySource int
	var bodyStatus int
	var bodySubject string
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a ticket",
		Example:     "  freshservice-pp-cli tickets create --subject example-value",
		Annotations: map[string]string{"pp:endpoint": "tickets.create", "pp:method": "POST", "pp:path": "/tickets"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("subject") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "subject")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/tickets"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDueBy != "" {
					body["due_by"] = bodyDueBy
				}
				if bodyEmail != "" {
					body["email"] = bodyEmail
				}
				if bodyGroupId != 0 {
					body["group_id"] = bodyGroupId
				}
				if bodyPriority != 0 {
					body["priority"] = bodyPriority
				}
				if bodyRequesterId != 0 {
					body["requester_id"] = bodyRequesterId
				}
				if bodyResponderId != 0 {
					body["responder_id"] = bodyResponderId
				}
				if bodySource != 0 {
					body["source"] = bodySource
				}
				if bodyStatus != 0 {
					body["status"] = bodyStatus
				}
				if bodySubject != "" {
					body["subject"] = bodySubject
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "tickets",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyDueBy, "due-by", "", "Due by")
	cmd.Flags().StringVar(&bodyEmail, "email", "", "Email")
	cmd.Flags().IntVar(&bodyGroupId, "group-id", 0, "Group id")
	cmd.Flags().IntVar(&bodyPriority, "priority", 0, "1=Low 2=Medium 3=High 4=Urgent")
	cmd.Flags().IntVar(&bodyRequesterId, "requester-id", 0, "Requester id")
	cmd.Flags().IntVar(&bodyResponderId, "responder-id", 0, "Responder id")
	cmd.Flags().IntVar(&bodySource, "source", 0, "1=Email 2=Portal 3=Phone 7=Chat 10=Slack 15=Teams")
	cmd.Flags().IntVar(&bodyStatus, "status", 0, "2=Open 3=Pending 4=Resolved 5=Closed")
	cmd.Flags().StringVar(&bodySubject, "subject", "", "Subject")
	cmd.Flags().StringVar(&bodyType, "type", "", "Type")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
