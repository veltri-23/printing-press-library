// Copyright 2026 John McLear and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "anonymize-author using-post", Description: "anonymizes an author across all their edits", Group: "anonymize-author"},
	{Command: "append-chat-message using-post", Description: "appends a chat message", Group: "append-chat-message"},
	{Command: "append-text using-post", Description: "appends text to a pad", Group: "append-text"},
	{Command: "check-token using-post", Description: "returns ok when the current API token is valid", Group: "check-token"},
	{Command: "compact-pad using-post", Description: "compacts a pad's revision history, keeping recent revisions only", Group: "compact-pad"},
	{Command: "copy-pad using-post", Description: "copies a pad with full history and chat", Group: "copy-pad"},
	{Command: "copy-pad-without-history using-post", Description: "copies a pad without history or chat", Group: "copy-pad-without-history"},
	{Command: "create-author using-post", Description: "creates a new author", Group: "create-author"},
	{Command: "create-author-if-not-exists-for using-post", Description: "this functions helps you to map your application author ids to Etherpad author ids", Group: "create-author-if-not-exists-for"},
	{Command: "create-diff-html create-diff-htmlusing-post", Description: "returns an HTML diff between two revisions of a pad", Group: "create-diff-html"},
	{Command: "create-group using-post", Description: "creates a new group", Group: "create-group"},
	{Command: "create-group-if-not-exists-for using-post", Description: "this functions helps you to map your application group ids to Etherpad group ids", Group: "create-group-if-not-exists-for"},
	{Command: "create-group-pad using-post", Description: "creates a new pad in this group", Group: "create-group-pad"},
	{Command: "create-pad using-post", Description: "creates a new (non-group) pad. Note that if you need to create a group Pad, you should call createGroupPad", Group: "create-pad"},
	{Command: "create-session using-post", Description: "creates a new session. validUntil is an unix timestamp in seconds", Group: "create-session"},
	{Command: "delete-group using-post", Description: "deletes a group", Group: "delete-group"},
	{Command: "delete-pad using-post", Description: "deletes a pad", Group: "delete-pad"},
	{Command: "delete-session using-post", Description: "deletes a session", Group: "delete-session"},
	{Command: "get-attribute-pool using-post", Description: "returns the attribute pool of a pad", Group: "get-attribute-pool"},
	{Command: "get-author-name using-post", Description: "Returns the Author Name of the author", Group: "get-author-name"},
	{Command: "get-chat-head using-post", Description: "returns the chatHead (chat-message) of the pad", Group: "get-chat-head"},
	{Command: "get-chat-history using-post", Description: "returns the chat history", Group: "get-chat-history"},
	{Command: "get-html get-htmlusing-post", Description: "returns the text of a pad formatted as HTML", Group: "get-html"},
	{Command: "get-last-edited using-post", Description: "returns the timestamp of the last revision of the pad", Group: "get-last-edited"},
	{Command: "get-pad-id get-pad-idusing-post", Description: "returns the read-write pad ID for a given read-only pad ID", Group: "get-pad-id"},
	{Command: "get-public-status using-post", Description: "return true of false", Group: "get-public-status"},
	{Command: "get-read-only-id get-read-only-idusing-post", Description: "returns the read only link of a pad", Group: "get-read-only-id"},
	{Command: "get-revision-changeset using-post", Description: "returns the changeset at a given revision of a pad", Group: "get-revision-changeset"},
	{Command: "get-revisions-count using-post", Description: "returns the number of revisions of this pad", Group: "get-revisions-count"},
	{Command: "get-saved-revisions-count using-post", Description: "returns the number of saved revisions of a pad", Group: "get-saved-revisions-count"},
	{Command: "get-session-info using-post", Description: "returns information about a session", Group: "get-session-info"},
	{Command: "get-stats using-post", Description: "returns server-wide statistics", Group: "get-stats"},
	{Command: "get-text using-post", Description: "returns the text of a pad", Group: "get-text"},
	{Command: "list-all-groups using-post", Description: "returns the IDs of all groups on this server", Group: "list-all-groups"},
	{Command: "list-all-pads using-post", Description: "list all the pads", Group: "list-all-pads"},
	{Command: "list-authors-of-pad using-post", Description: "returns an array of authors who contributed to this pad", Group: "list-authors-of-pad"},
	{Command: "list-pads using-post", Description: "returns all pads of this group", Group: "list-pads"},
	{Command: "list-pads-of-author using-post", Description: "returns an array of all pads this author contributed to", Group: "list-pads-of-author"},
	{Command: "list-saved-revisions using-post", Description: "returns the list of saved revisions of a pad", Group: "list-saved-revisions"},
	{Command: "list-sessions-of-author using-post", Description: "returns all sessions of an author", Group: "list-sessions-of-author"},
	{Command: "list-sessions-of-group using-post", Description: "returns all sessions of a group", Group: "list-sessions-of-group"},
	{Command: "move-pad using-post", Description: "moves a pad — copy then delete the original", Group: "move-pad"},
	{Command: "pad-users using-post", Description: "returns the list of users that are currently editing this pad", Group: "pad-users"},
	{Command: "pad-users-count using-post", Description: "returns the number of user that are currently editing this pad", Group: "pad-users-count"},
	{Command: "restore-revision using-post", Description: "restores a pad to a specific revision", Group: "restore-revision"},
	{Command: "save-revision using-post", Description: "saves a revision of a pad", Group: "save-revision"},
	{Command: "send-clients-message using-post", Description: "sends a custom message of type msg to the pad", Group: "send-clients-message"},
	{Command: "set-html set-htmlusing-post", Description: "sets the text of a pad with HTML", Group: "set-html"},
	{Command: "set-public-status using-post", Description: "sets a boolean for the public status of a pad", Group: "set-public-status"},
	{Command: "set-text using-post", Description: "sets the text of a pad", Group: "set-text"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  etherpad-pp-cli which "stale tickets"
  etherpad-pp-cli which "bottleneck"
  etherpad-pp-cli which --limit 1 "send message"
  etherpad-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
