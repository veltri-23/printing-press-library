// Copyright 2026 Amitav Khandelwal and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/productivity/zoho-expense/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/productivity/zoho-expense/internal/client"
	"github.com/mvanhorn/printing-press-library/library/productivity/zoho-expense/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/productivity/zoho-expense/internal/config"
	"github.com/mvanhorn/printing-press-library/library/productivity/zoho-expense/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/productivity/zoho-expense/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("currencies_create",
			mcplib.WithDescription("Add a currency to the org. Required: currency_code. Optional: exchange_rate. Returns the new Currency."),
			mcplib.WithString("currency_code", mcplib.Required(), mcplib.Description("ISO currency code (e.g. USD, INR)")),
			mcplib.WithNumber("exchange_rate", mcplib.Description("Initial exchange rate to base currency")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/settings/currencies", false, false, nil, []mcpParamBinding{{PublicName: "currency_code", WireName: "currency_code", Location: "body"}, {PublicName: "exchange_rate", WireName: "exchange_rate", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("currencies_delete",
			mcplib.WithDescription("Permanently delete a currency from the Zoho Expense organization settings. Required: currency_id (path). Destructive and irreversible — the currency must not be in use by any expense, report, or customer record or the API will reject the call. Returns the standard Zoho status payload (code, message). Prefer this only when the currency was created in error; otherwise leave it in place."),
			mcplib.WithString("currency_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/settings/currencies/{currency_id}", false, false, nil, []mcpParamBinding{{PublicName: "currency_id", WireName: "currency_id", Location: "path"}}, []string{"currency_id"}),
	)
	s.AddTool(
		mcplib.NewTool("currencies_get",
			mcplib.WithDescription("Get a currency. Required: currency_id. Returns the Currency."),
			mcplib.WithString("currency_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/settings/currencies/{currency_id}", true, false, nil, []mcpParamBinding{{PublicName: "currency_id", WireName: "currency_id", Location: "path"}}, []string{"currency_id"}),
	)
	s.AddTool(
		mcplib.NewTool("currencies_list",
			mcplib.WithDescription("List currencies configured in the org. Returns array of Currency."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/settings/currencies", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("currencies_update",
			mcplib.WithDescription("Update a currency. Required: currency_id. Optional: exchange_rate."),
			mcplib.WithString("currency_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithNumber("exchange_rate", mcplib.Description("")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/settings/currencies/{currency_id}", false, false, nil, []mcpParamBinding{{PublicName: "currency_id", WireName: "currency_id", Location: "path"}, {PublicName: "exchange_rate", WireName: "exchange_rate", Location: "body"}}, []string{"currency_id"}),
	)
	s.AddTool(
		mcplib.NewTool("customers_create",
			mcplib.WithDescription("Create a customer. Required: contact_name. Optional: company_name, email, phone. Returns the new Customer."),
			mcplib.WithString("contact_name", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("company_name", mcplib.Description("")),
			mcplib.WithString("email", mcplib.Description("")),
			mcplib.WithString("phone", mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/contacts", false, false, nil, []mcpParamBinding{{PublicName: "contact_name", WireName: "contact_name", Location: "body"}, {PublicName: "company_name", WireName: "company_name", Location: "body"}, {PublicName: "email", WireName: "email", Location: "body"}, {PublicName: "phone", WireName: "phone", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("customers_delete",
			mcplib.WithDescription("Permanently delete a customer (contact) record from Zoho Expense. Required: customer_id (path). Destructive — expenses already billed to this customer remain but lose their contact reference. Returns the standard Zoho status payload. Use customers_list to confirm the id first; there is no soft-delete or undo."),
			mcplib.WithString("customer_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/contacts/{customer_id}", false, false, nil, []mcpParamBinding{{PublicName: "customer_id", WireName: "customer_id", Location: "path"}}, []string{"customer_id"}),
	)
	s.AddTool(
		mcplib.NewTool("customers_get",
			mcplib.WithDescription("Get a customer. Required: customer_id. Returns the Customer."),
			mcplib.WithString("customer_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/contacts/{customer_id}", true, false, nil, []mcpParamBinding{{PublicName: "customer_id", WireName: "customer_id", Location: "path"}}, []string{"customer_id"}),
	)
	s.AddTool(
		mcplib.NewTool("customers_list",
			mcplib.WithDescription("List customers. Optional: page, per_page. Returns array of Customer."),
			mcplib.WithNumber("page", mcplib.Description("")),
			mcplib.WithNumber("per_page", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/contacts", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("customers_update",
			mcplib.WithDescription("Update a customer. Required: customer_id. Optional: contact_name, company_name, email (plus 1 more)."),
			mcplib.WithString("customer_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("contact_name", mcplib.Description("")),
			mcplib.WithString("company_name", mcplib.Description("")),
			mcplib.WithString("email", mcplib.Description("")),
			mcplib.WithString("phone", mcplib.Description("")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/contacts/{customer_id}", false, false, nil, []mcpParamBinding{{PublicName: "customer_id", WireName: "customer_id", Location: "path"}, {PublicName: "contact_name", WireName: "contact_name", Location: "body"}, {PublicName: "company_name", WireName: "company_name", Location: "body"}, {PublicName: "email", WireName: "email", Location: "body"}, {PublicName: "phone", WireName: "phone", Location: "body"}}, []string{"customer_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_categories_create",
			mcplib.WithDescription("Create an expense category. Required: name. Optional: description, account_id. Returns the new ExpenseCategory."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("description", mcplib.Description("")),
			mcplib.WithString("account_id", mcplib.Description("Linked accounting account")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/expensecategories", false, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "account_id", WireName: "account_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("expense_categories_delete",
			mcplib.WithDescription("Delete an expense category. Required: category_id. Destructive."),
			mcplib.WithString("category_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/expensecategories/{category_id}", false, false, nil, []mcpParamBinding{{PublicName: "category_id", WireName: "category_id", Location: "path"}}, []string{"category_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_categories_disable",
			mcplib.WithDescription("Hide an expense category so users can no longer pick it on new expenses. Required: category_id (path). Reversible via expense_categories_enable. Existing expenses already tagged to this category are unaffected. Returns the updated category payload. Prefer this over deleting categories you might re-enable later."),
			mcplib.WithString("category_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/expensecategories/{category_id}/hide", false, false, nil, []mcpParamBinding{{PublicName: "category_id", WireName: "category_id", Location: "path"}}, []string{"category_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_categories_enable",
			mcplib.WithDescription("Re-enable a previously hidden expense category so users can pick it on new expenses. Required: category_id (path). Inverse of expense_categories_disable. Returns the updated category payload. Use expense_categories_list with include_hidden to find disabled categories."),
			mcplib.WithString("category_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/expensecategories/{category_id}/show", false, false, nil, []mcpParamBinding{{PublicName: "category_id", WireName: "category_id", Location: "path"}}, []string{"category_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_categories_get",
			mcplib.WithDescription("Get an expense category. Required: category_id. Returns the ExpenseCategory."),
			mcplib.WithString("category_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/expensecategories/{category_id}", true, false, nil, []mcpParamBinding{{PublicName: "category_id", WireName: "category_id", Location: "path"}}, []string{"category_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_categories_list",
			mcplib.WithDescription("List expense categories. Optional: page, per_page. Returns array of ExpenseCategory."),
			mcplib.WithNumber("page", mcplib.Description("")),
			mcplib.WithNumber("per_page", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/expensecategories", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("expense_categories_update",
			mcplib.WithDescription("Update an expense category. Required: category_id. Optional: name, description. Returns the updated ExpenseCategory."),
			mcplib.WithString("category_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("name", mcplib.Description("")),
			mcplib.WithString("description", mcplib.Description("")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/expensecategories/{category_id}", false, false, nil, []mcpParamBinding{{PublicName: "category_id", WireName: "category_id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}}, []string{"category_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_reports_approval_history",
			mcplib.WithDescription("View the approval history of a report. Required: report_id."),
			mcplib.WithString("report_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/expensereports/{report_id}/approvalhistory", true, false, nil, []mcpParamBinding{{PublicName: "report_id", WireName: "report_id", Location: "path"}}, []string{"report_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_reports_approve",
			mcplib.WithDescription("Approve a submitted expense report, moving it from 'submitted' to 'approved' status (next stop: reimbursement). Required: report_id (path). Caller must hold approver permission on the report. Returns the updated expense report payload with new status and approver fields. Use expense_reports_reject for the inverse decision."),
			mcplib.WithString("report_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/expensereports/{report_id}/approve", false, false, nil, []mcpParamBinding{{PublicName: "report_id", WireName: "report_id", Location: "path"}}, []string{"report_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_reports_create",
			mcplib.WithDescription("Create an expense report. Required: report_name. Optional: report_date, business_purpose, expense_ids. Returns the new ExpenseReport."),
			mcplib.WithString("report_name", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("report_date", mcplib.Description("")),
			mcplib.WithString("business_purpose", mcplib.Description("Why the report exists (e.g. 'October 2026 expenses')")),
			mcplib.WithString("expense_ids", mcplib.Description("Expense IDs to attach")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/expensereports", false, false, nil, []mcpParamBinding{{PublicName: "report_name", WireName: "report_name", Location: "body"}, {PublicName: "report_date", WireName: "report_date", Location: "body"}, {PublicName: "business_purpose", WireName: "business_purpose", Location: "body"}, {PublicName: "expense_ids", WireName: "expense_ids", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("expense_reports_get",
			mcplib.WithDescription("Get an expense report (includes attached expenses). Required: report_id. Returns the ExpenseReport."),
			mcplib.WithString("report_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/expensereports/{report_id}", true, false, nil, []mcpParamBinding{{PublicName: "report_id", WireName: "report_id", Location: "path"}}, []string{"report_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_reports_list",
			mcplib.WithDescription("List expense reports. Optional: page, per_page, filter_by. Returns array of ExpenseReport."),
			mcplib.WithNumber("page", mcplib.Description("")),
			mcplib.WithNumber("per_page", mcplib.Description("")),
			mcplib.WithString("filter_by", mcplib.Description("Status filter (e.g. Status.All, Status.Draft, Status.Submitted, Status.Approved)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/expensereports", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "filter_by", WireName: "filter_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("expense_reports_reimburse",
			mcplib.WithDescription("Mark an expense report as reimbursed. Required: report_id."),
			mcplib.WithString("report_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/expensereports/{report_id}/reimburse", false, false, nil, []mcpParamBinding{{PublicName: "report_id", WireName: "report_id", Location: "path"}}, []string{"report_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_reports_reject",
			mcplib.WithDescription("Reject an expense report. Required: report_id. Optional: comments."),
			mcplib.WithString("report_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("comments", mcplib.Description("Rejection reason")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/expensereports/{report_id}/reject", false, false, nil, []mcpParamBinding{{PublicName: "report_id", WireName: "report_id", Location: "path"}, {PublicName: "comments", WireName: "comments", Location: "body"}}, []string{"report_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expense_reports_update",
			mcplib.WithDescription("Update an expense report — used to attach more expenses. Required: report_id. Optional: report_name, business_purpose, expense_ids. Returns the updated ExpenseReport."),
			mcplib.WithString("report_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("report_name", mcplib.Description("")),
			mcplib.WithString("business_purpose", mcplib.Description("")),
			mcplib.WithString("expense_ids", mcplib.Description("Replacement list of attached expense IDs")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/expensereports/{report_id}", false, false, nil, []mcpParamBinding{{PublicName: "report_id", WireName: "report_id", Location: "path"}, {PublicName: "report_name", WireName: "report_name", Location: "body"}, {PublicName: "business_purpose", WireName: "business_purpose", Location: "body"}, {PublicName: "expense_ids", WireName: "expense_ids", Location: "body"}}, []string{"report_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expenses_create",
			mcplib.WithDescription("Create an expense (JSON body — for receipt upload use 'receipt upload'). Required: amount, expense_date. Optional: currency_id, exchange_rate, category_id (plus 11 more). Returns the new Expense."),
			mcplib.WithNumber("amount", mcplib.Required(), mcplib.Description("Expense amount in the expense currency")),
			mcplib.WithString("expense_date", mcplib.Required(), mcplib.Description("Expense date (YYYY-MM-DD)")),
			mcplib.WithString("currency_id", mcplib.Description("Currency ID; omit to use org default")),
			mcplib.WithNumber("exchange_rate", mcplib.Description("Exchange rate to base currency")),
			mcplib.WithString("category_id", mcplib.Description("Expense category ID")),
			mcplib.WithString("merchant_id", mcplib.Description("Merchant ID")),
			mcplib.WithString("merchant_name", mcplib.Description("Merchant name (creates merchant on the fly)")),
			mcplib.WithString("paid_through_account_id", mcplib.Description("Account the expense was paid through")),
			mcplib.WithString("customer_id", mcplib.Description("Customer this expense is for")),
			mcplib.WithString("project_id", mcplib.Description("Project this expense belongs to")),
			mcplib.WithBoolean("is_billable", mcplib.Description("Mark as billable to customer")),
			mcplib.WithBoolean("is_reimbursable", mcplib.Description("Mark as reimbursable to user")),
			mcplib.WithBoolean("is_inclusive_tax", mcplib.Description("Whether amount is tax-inclusive")),
			mcplib.WithString("tax_id", mcplib.Description("Tax ID for this expense")),
			mcplib.WithString("reference_number", mcplib.Description("Reference number / receipt number")),
			mcplib.WithString("description", mcplib.Description("Free-form description / notes")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/expenses", false, false, nil, []mcpParamBinding{{PublicName: "amount", WireName: "amount", Location: "body"}, {PublicName: "expense_date", WireName: "expense_date", Location: "body"}, {PublicName: "currency_id", WireName: "currency_id", Location: "body"}, {PublicName: "exchange_rate", WireName: "exchange_rate", Location: "body"}, {PublicName: "category_id", WireName: "category_id", Location: "body"}, {PublicName: "merchant_id", WireName: "merchant_id", Location: "body"}, {PublicName: "merchant_name", WireName: "merchant_name", Location: "body"}, {PublicName: "paid_through_account_id", WireName: "paid_through_account_id", Location: "body"}, {PublicName: "customer_id", WireName: "customer_id", Location: "body"}, {PublicName: "project_id", WireName: "project_id", Location: "body"}, {PublicName: "is_billable", WireName: "is_billable", Location: "body"}, {PublicName: "is_reimbursable", WireName: "is_reimbursable", Location: "body"}, {PublicName: "is_inclusive_tax", WireName: "is_inclusive_tax", Location: "body"}, {PublicName: "tax_id", WireName: "tax_id", Location: "body"}, {PublicName: "reference_number", WireName: "reference_number", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("expenses_get",
			mcplib.WithDescription("Get a single expense. Required: expense_id. Returns the Expense."),
			mcplib.WithString("expense_id", mcplib.Required(), mcplib.Description("Expense ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/expenses/{expense_id}", true, false, nil, []mcpParamBinding{{PublicName: "expense_id", WireName: "expense_id", Location: "path"}}, []string{"expense_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expenses_list",
			mcplib.WithDescription("List expenses with rich filters (date range, status, user, category, project). Optional: page, per_page, filter_by (plus 7 more). Returns array of Expense."),
			mcplib.WithNumber("page", mcplib.Description("Page number")),
			mcplib.WithNumber("per_page", mcplib.Description("Results per page (default 200, max 200)")),
			mcplib.WithString("filter_by", mcplib.Description("Status filter e.g. Status.All, Status.Unreported, Status.Reported")),
			mcplib.WithString("date_start", mcplib.Description("Earliest expense date (YYYY-MM-DD)")),
			mcplib.WithString("date_end", mcplib.Description("Latest expense date (YYYY-MM-DD)")),
			mcplib.WithString("user_id", mcplib.Description("Filter by user ID")),
			mcplib.WithString("category_id", mcplib.Description("Filter by expense category")),
			mcplib.WithString("customer_id", mcplib.Description("Filter by customer/contact")),
			mcplib.WithString("project_id", mcplib.Description("Filter by project")),
			mcplib.WithString("merchant_id", mcplib.Description("Filter by merchant")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/reports/expensedetails", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "filter_by", WireName: "filter_by", Location: "query"}, {PublicName: "date_start", WireName: "date_start", Location: "query"}, {PublicName: "date_end", WireName: "date_end", Location: "query"}, {PublicName: "user_id", WireName: "user_id", Location: "query"}, {PublicName: "category_id", WireName: "category_id", Location: "query"}, {PublicName: "customer_id", WireName: "customer_id", Location: "query"}, {PublicName: "project_id", WireName: "project_id", Location: "query"}, {PublicName: "merchant_id", WireName: "merchant_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("expenses_merge",
			mcplib.WithDescription("Merge multiple expenses (used to dedupe a scanned-receipt expense with a manual one). Required: expense_id. Optional: expense_ids."),
			mcplib.WithString("expense_id", mcplib.Required(), mcplib.Description("Primary expense ID — others merge into this one")),
			mcplib.WithString("expense_ids", mcplib.Description("Other expense IDs to merge into the primary")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/expenses/{expense_id}/merge", false, false, nil, []mcpParamBinding{{PublicName: "expense_id", WireName: "expense_id", Location: "path"}, {PublicName: "expense_ids", WireName: "expense_ids", Location: "body"}}, []string{"expense_id"}),
	)
	s.AddTool(
		mcplib.NewTool("expenses_update",
			mcplib.WithDescription("Update an expense — used to add category/project/tags after autoscan. Required: expense_id. Optional: amount, expense_date, currency_id (plus 11 more). Returns the updated Expense."),
			mcplib.WithString("expense_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithNumber("amount", mcplib.Description("Expense amount")),
			mcplib.WithString("expense_date", mcplib.Description("")),
			mcplib.WithString("currency_id", mcplib.Description("")),
			mcplib.WithString("category_id", mcplib.Description("")),
			mcplib.WithString("merchant_id", mcplib.Description("")),
			mcplib.WithString("merchant_name", mcplib.Description("")),
			mcplib.WithString("customer_id", mcplib.Description("")),
			mcplib.WithString("project_id", mcplib.Description("")),
			mcplib.WithBoolean("is_billable", mcplib.Description("")),
			mcplib.WithBoolean("is_reimbursable", mcplib.Description("")),
			mcplib.WithBoolean("is_inclusive_tax", mcplib.Description("")),
			mcplib.WithString("tax_id", mcplib.Description("")),
			mcplib.WithString("reference_number", mcplib.Description("")),
			mcplib.WithString("description", mcplib.Description("")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/expenses/{expense_id}", false, false, nil, []mcpParamBinding{{PublicName: "expense_id", WireName: "expense_id", Location: "path"}, {PublicName: "amount", WireName: "amount", Location: "body"}, {PublicName: "expense_date", WireName: "expense_date", Location: "body"}, {PublicName: "currency_id", WireName: "currency_id", Location: "body"}, {PublicName: "category_id", WireName: "category_id", Location: "body"}, {PublicName: "merchant_id", WireName: "merchant_id", Location: "body"}, {PublicName: "merchant_name", WireName: "merchant_name", Location: "body"}, {PublicName: "customer_id", WireName: "customer_id", Location: "body"}, {PublicName: "project_id", WireName: "project_id", Location: "body"}, {PublicName: "is_billable", WireName: "is_billable", Location: "body"}, {PublicName: "is_reimbursable", WireName: "is_reimbursable", Location: "body"}, {PublicName: "is_inclusive_tax", WireName: "is_inclusive_tax", Location: "body"}, {PublicName: "tax_id", WireName: "tax_id", Location: "body"}, {PublicName: "reference_number", WireName: "reference_number", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}}, []string{"expense_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organizations_get",
			mcplib.WithDescription("Get organization details. Required: organization_id. Returns the Organization."),
			mcplib.WithString("organization_id", mcplib.Required(), mcplib.Description("Organization ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organizations/{organization_id}", true, false, nil, []mcpParamBinding{{PublicName: "organization_id", WireName: "organization_id", Location: "path"}}, []string{"organization_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organizations_list",
			mcplib.WithDescription("List organizations accessible to the authenticated user. Returns array of Organization."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organizations", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("projects_activate",
			mcplib.WithDescription("Mark a project as active so it can be assigned to new expenses and trips. Required: project_id (path). Inverse of projects_deactivate. Returns the updated project payload. Common workflow: reactivate seasonal/recurring projects at the start of a new engagement."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/projects/{project_id}/active", false, false, nil, []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_create",
			mcplib.WithDescription("Create a project. Required: project_name. Optional: customer_id, description. Returns the new Project."),
			mcplib.WithString("project_name", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("customer_id", mcplib.Description("Linked customer")),
			mcplib.WithString("description", mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/projects", false, false, nil, []mcpParamBinding{{PublicName: "project_name", WireName: "project_name", Location: "body"}, {PublicName: "customer_id", WireName: "customer_id", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("projects_deactivate",
			mcplib.WithDescription("Mark a project as inactive so it can no longer be assigned to new expenses or trips (existing assignments are unaffected). Required: project_id (path). Reversible via projects_activate. Returns the updated project payload. Prefer this over projects_delete when the project may need to be reactivated later."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/projects/{project_id}/inactive", false, false, nil, []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_delete",
			mcplib.WithDescription("Permanently delete a project from Zoho Expense. Required: project_id (path). Destructive and irreversible — fails if expenses, trips, or invoices reference the project. Returns the standard Zoho status payload. Prefer projects_deactivate for projects with historical assignments; only delete projects created in error."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/projects/{project_id}", false, false, nil, []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_get",
			mcplib.WithDescription("Get a project. Required: project_id. Returns the Project."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/projects/{project_id}", true, false, nil, []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_list",
			mcplib.WithDescription("List projects. Optional: page, per_page. Returns array of Project."),
			mcplib.WithNumber("page", mcplib.Description("")),
			mcplib.WithNumber("per_page", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/projects", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("projects_update",
			mcplib.WithDescription("Update a project. Required: project_id. Optional: project_name, customer_id, description."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("project_name", mcplib.Description("")),
			mcplib.WithString("customer_id", mcplib.Description("")),
			mcplib.WithString("description", mcplib.Description("")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/projects/{project_id}", false, false, nil, []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "project_name", WireName: "project_name", Location: "body"}, {PublicName: "customer_id", WireName: "customer_id", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("receipts_upload",
			mcplib.WithDescription("Upload a receipt file (multipart). Server queues for autoscan; poll `expenses get` for status. Required: receipt."),
			mcplib.WithString("receipt", mcplib.Required(), mcplib.Description("Receipt file (JPG, PNG, PDF; up to 5 MB)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/expenses", false, false, nil, []mcpParamBinding{{PublicName: "receipt", WireName: "receipt", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("reporting_tags_activate",
			mcplib.WithDescription("Activate a reporting tag so it can be assigned to new expenses for cost-center / dimension tracking. Required: tag_id (path). Inverse of reporting_tags_deactivate. Returns the updated tag payload. Reporting tags are the Zoho Expense equivalent of QuickBooks classes."),
			mcplib.WithString("tag_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/reportingtags/{tag_id}/active", false, false, nil, []mcpParamBinding{{PublicName: "tag_id", WireName: "tag_id", Location: "path"}}, []string{"tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("reporting_tags_create",
			mcplib.WithDescription("Create a reporting tag. Required: tag_name. Returns the new ReportingTag."),
			mcplib.WithString("tag_name", mcplib.Required(), mcplib.Description("Tag name (e.g. 'Cost Center')")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/reportingtags", false, false, nil, []mcpParamBinding{{PublicName: "tag_name", WireName: "tag_name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("reporting_tags_deactivate",
			mcplib.WithDescription("Deactivate a reporting tag so it can no longer be assigned to new expenses (existing assignments are unaffected). Required: tag_id (path). Reversible via reporting_tags_activate. Returns the updated tag payload. Prefer this over reporting_tags_delete for tags with historical data."),
			mcplib.WithString("tag_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/reportingtags/{tag_id}/inactive", false, false, nil, []mcpParamBinding{{PublicName: "tag_id", WireName: "tag_id", Location: "path"}}, []string{"tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("reporting_tags_delete",
			mcplib.WithDescription("Permanently delete a reporting tag. Required: tag_id (path). Destructive — fails if any expense currently references the tag. Returns the standard Zoho status payload. Prefer reporting_tags_deactivate over delete for any tag with historical expense assignments."),
			mcplib.WithString("tag_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/reportingtags/{tag_id}", false, false, nil, []mcpParamBinding{{PublicName: "tag_id", WireName: "tag_id", Location: "path"}}, []string{"tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("reporting_tags_get",
			mcplib.WithDescription("Get a reporting tag. Required: tag_id. Returns the ReportingTag."),
			mcplib.WithString("tag_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/reportingtags/{tag_id}", true, false, nil, []mcpParamBinding{{PublicName: "tag_id", WireName: "tag_id", Location: "path"}}, []string{"tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("reporting_tags_list",
			mcplib.WithDescription("List all reporting tags in the Zoho Expense organization (active and inactive). No required parameters. Returns an array of ReportingTag objects (tag_id, tag_name, status, options). Use this to discover tag_ids before calling activate/deactivate/delete, or to populate a tag picker."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/reportingtags", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("reporting_tags_list_options",
			mcplib.WithDescription("List all options for a reporting tag. Required: tag_id."),
			mcplib.WithString("tag_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/reportingtags/{tag_id}/options/all", true, false, nil, []mcpParamBinding{{PublicName: "tag_id", WireName: "tag_id", Location: "path"}}, []string{"tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("reporting_tags_update",
			mcplib.WithDescription("Update a reporting tag. Required: tag_id. Optional: tag_name."),
			mcplib.WithString("tag_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("tag_name", mcplib.Description("")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/reportingtags/{tag_id}", false, false, nil, []mcpParamBinding{{PublicName: "tag_id", WireName: "tag_id", Location: "path"}, {PublicName: "tag_name", WireName: "tag_name", Location: "body"}}, []string{"tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("taxes_create",
			mcplib.WithDescription("Create a tax. Required: tax_name, tax_percentage. Optional: tax_type. Returns the new Tax."),
			mcplib.WithString("tax_name", mcplib.Required(), mcplib.Description("")),
			mcplib.WithNumber("tax_percentage", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("tax_type", mcplib.Description("tax | tax_group")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/settings/taxes", false, false, nil, []mcpParamBinding{{PublicName: "tax_name", WireName: "tax_name", Location: "body"}, {PublicName: "tax_percentage", WireName: "tax_percentage", Location: "body"}, {PublicName: "tax_type", WireName: "tax_type", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("taxes_delete",
			mcplib.WithDescription("Permanently delete a tax rate from organization settings. Required: tax_id (path). Destructive — fails if any expense or invoice currently uses the tax. Returns the standard Zoho status payload. Prefer marking the tax inactive in the Zoho UI if you may need it for historical reporting."),
			mcplib.WithString("tax_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/settings/taxes/{tax_id}", false, false, nil, []mcpParamBinding{{PublicName: "tax_id", WireName: "tax_id", Location: "path"}}, []string{"tax_id"}),
	)
	s.AddTool(
		mcplib.NewTool("taxes_get",
			mcplib.WithDescription("Get a tax. Required: tax_id. Returns the Tax."),
			mcplib.WithString("tax_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/settings/taxes/{tax_id}", true, false, nil, []mcpParamBinding{{PublicName: "tax_id", WireName: "tax_id", Location: "path"}}, []string{"tax_id"}),
	)
	s.AddTool(
		mcplib.NewTool("taxes_list",
			mcplib.WithDescription("List all tax rates configured in the Zoho Expense organization. No required parameters. Returns an array of Tax objects (tax_id, tax_name, tax_percentage, tax_type, is_default). Use this to discover the tax_id needed when creating expenses with GST/VAT."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/settings/taxes", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("taxes_update",
			mcplib.WithDescription("Update a tax. Required: tax_id. Optional: tax_name, tax_percentage."),
			mcplib.WithString("tax_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("tax_name", mcplib.Description("")),
			mcplib.WithNumber("tax_percentage", mcplib.Description("")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/settings/taxes/{tax_id}", false, false, nil, []mcpParamBinding{{PublicName: "tax_id", WireName: "tax_id", Location: "path"}, {PublicName: "tax_name", WireName: "tax_name", Location: "body"}, {PublicName: "tax_percentage", WireName: "tax_percentage", Location: "body"}}, []string{"tax_id"}),
	)
	s.AddTool(
		mcplib.NewTool("trips_approve",
			mcplib.WithDescription("Approve a submitted trip request so the traveller can book travel against it. Required: trip_id (path). Caller must hold approver permission on the trip. Returns the updated trip payload with new status and approver fields. Use trips_reject for the inverse decision."),
			mcplib.WithString("trip_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/trips/{trip_id}/approve", false, false, nil, []mcpParamBinding{{PublicName: "trip_id", WireName: "trip_id", Location: "path"}}, []string{"trip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("trips_cancel",
			mcplib.WithDescription("Cancel an approved or in-progress trip. Required: trip_id (path). Cancellation does NOT auto-refund booked expenses — those must be voided separately. Returns the updated trip payload. Use trips_reject when rejecting a submitted-but-not-yet-approved trip; use cancel after approval."),
			mcplib.WithString("trip_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/trips/{trip_id}/cancel", false, false, nil, []mcpParamBinding{{PublicName: "trip_id", WireName: "trip_id", Location: "path"}}, []string{"trip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("trips_close",
			mcplib.WithDescription("Close a trip (no further expenses can be added). Required: trip_id."),
			mcplib.WithString("trip_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/trips/{trip_id}/close", false, false, nil, []mcpParamBinding{{PublicName: "trip_id", WireName: "trip_id", Location: "path"}}, []string{"trip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("trips_create",
			mcplib.WithDescription("Create a trip. Required: trip_name. Optional: from_date, to_date, destination (plus 1 more). Returns the new Trip."),
			mcplib.WithString("trip_name", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("from_date", mcplib.Description("")),
			mcplib.WithString("to_date", mcplib.Description("")),
			mcplib.WithString("destination", mcplib.Description("")),
			mcplib.WithString("purpose", mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/trips", false, false, nil, []mcpParamBinding{{PublicName: "trip_name", WireName: "trip_name", Location: "body"}, {PublicName: "from_date", WireName: "from_date", Location: "body"}, {PublicName: "to_date", WireName: "to_date", Location: "body"}, {PublicName: "destination", WireName: "destination", Location: "body"}, {PublicName: "purpose", WireName: "purpose", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("trips_delete",
			mcplib.WithDescription("Permanently delete a trip request. Required: trip_id (path). Destructive — only allowed on draft or rejected trips; approved trips with bookings must be cancelled instead. Returns the standard Zoho status payload."),
			mcplib.WithString("trip_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/trips/{trip_id}", false, false, nil, []mcpParamBinding{{PublicName: "trip_id", WireName: "trip_id", Location: "path"}}, []string{"trip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("trips_get",
			mcplib.WithDescription("Get a trip. Required: trip_id. Returns the Trip."),
			mcplib.WithString("trip_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/trips/{trip_id}", true, false, nil, []mcpParamBinding{{PublicName: "trip_id", WireName: "trip_id", Location: "path"}}, []string{"trip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("trips_list",
			mcplib.WithDescription("List trips. Optional: page, per_page. Returns array of Trip."),
			mcplib.WithNumber("page", mcplib.Description("")),
			mcplib.WithNumber("per_page", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/trips", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("trips_reject",
			mcplib.WithDescription("Reject a submitted trip request so the traveller cannot book against it. Required: trip_id (path). Caller must hold approver permission. Returns the updated trip payload with new status and approver fields. Use trips_cancel for an already-approved trip."),
			mcplib.WithString("trip_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/trips/{trip_id}/reject", false, false, nil, []mcpParamBinding{{PublicName: "trip_id", WireName: "trip_id", Location: "path"}}, []string{"trip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("trips_update",
			mcplib.WithDescription("Update a trip. Required: trip_id. Optional: trip_name, from_date, to_date (plus 2 more)."),
			mcplib.WithString("trip_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("trip_name", mcplib.Description("")),
			mcplib.WithString("from_date", mcplib.Description("")),
			mcplib.WithString("to_date", mcplib.Description("")),
			mcplib.WithString("destination", mcplib.Description("")),
			mcplib.WithString("purpose", mcplib.Description("")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/trips/{trip_id}", false, false, nil, []mcpParamBinding{{PublicName: "trip_id", WireName: "trip_id", Location: "path"}, {PublicName: "trip_name", WireName: "trip_name", Location: "body"}, {PublicName: "from_date", WireName: "from_date", Location: "body"}, {PublicName: "to_date", WireName: "to_date", Location: "body"}, {PublicName: "destination", WireName: "destination", Location: "body"}, {PublicName: "purpose", WireName: "purpose", Location: "body"}}, []string{"trip_id"}),
	)
	s.AddTool(
		mcplib.NewTool("users_activate",
			mcplib.WithDescription("Mark a user as active so they can log in and submit expenses again. Required: user_id (path). Inverse of users_deactivate. Returns the updated user payload. Common workflow: reactivate a rehired employee or restore an accidentally-deactivated account."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/users/{user_id}/active", false, false, nil, []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("users_deactivate",
			mcplib.WithDescription("Mark a user as inactive so they can no longer log in or submit expenses (historical expenses and reports remain). Required: user_id (path). Reversible via users_activate. Returns the updated user payload. Prefer this over users_delete when an employee leaves — preserves their expense history for audit."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/users/{user_id}/inactive", false, false, nil, []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("users_delete",
			mcplib.WithDescription("Permanently delete a user from the Zoho Expense organization. Required: user_id (path). Destructive — fails if the user has expenses or reports they own; reassignment to another user is required first. Returns the standard Zoho status payload. Prefer users_deactivate to preserve audit trail."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/users/{user_id}", false, false, nil, []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("users_get",
			mcplib.WithDescription("Get a user. Required: user_id. Returns the User."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("User ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/users/{user_id}", true, false, nil, []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("users_invite",
			mcplib.WithDescription("Invite a user into the org. Required: email_id. Optional: name, role_id. Returns the new User."),
			mcplib.WithString("email_id", mcplib.Required(), mcplib.Description("User email address")),
			mcplib.WithString("name", mcplib.Description("User display name")),
			mcplib.WithString("role_id", mcplib.Description("Role ID to assign")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/users", false, false, nil, []mcpParamBinding{{PublicName: "email_id", WireName: "email_id", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "role_id", WireName: "role_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_list",
			mcplib.WithDescription("List users. Optional: page, per_page. Returns array of User."),
			mcplib.WithNumber("page", mcplib.Description("Page number (default 1)")),
			mcplib.WithNumber("per_page", mcplib.Description("Results per page (default 200, max 200)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/users", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_me",
			mcplib.WithDescription("Get the authenticated user's profile. Returns the User."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/users/me", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_update",
			mcplib.WithDescription("Update a user. Required: user_id. Optional: name, role_id. Returns the updated User."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("name", mcplib.Description("")),
			mcplib.WithString("role_id", mcplib.Description("")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/users/{user_id}", false, false, nil, []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "role_id", WireName: "role_id", Location: "body"}}, []string{"user_id"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export ZOHO_EXPENSE_ORGANIZATION_ID=<your-key>" +
					"\n      Get a key at: https://api-console.zoho.in/" +
					"\n      1. Visit https://api-console.zoho.in/ and create a Self-Client\n2. Set scope to ZohoExpense.fullaccess.ALL and click Create\n3. Copy the 10-min authorization code\n4. Run: zoho-expense-pp-cli auth login --client-id=... --client-secret=... --code=...\n5. The CLI exchanges the code for a refresh token and stores it locally.\n" +
					"\n      Run 'zoho-expense-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your token may have expired. Re-run 'zoho-expense-pp-cli auth login' to re-authenticate" +
					"\n      Set it with: export ZOHO_EXPENSE_ORGANIZATION_ID=<your-key>" +
					"\n      Get a key at: https://api-console.zoho.in/" +
					"\n      1. Visit https://api-console.zoho.in/ and create a Self-Client\n2. Set scope to ZohoExpense.fullaccess.ALL and click Create\n3. Copy the 10-min authorization code\n4. Run: zoho-expense-pp-cli auth login --client-id=... --client-secret=... --code=...\n5. The CLI exchanges the code for a refresh token and stores it locally.\n" +
					"\n      Run 'zoho-expense-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your token may lack required scopes. Re-run 'zoho-expense-pp-cli auth login' to re-authorize" +
					"\n      Set it with: export ZOHO_EXPENSE_ORGANIZATION_ID=<your-key>" +
					"\n      Get a key at: https://api-console.zoho.in/" +
					"\n      1. Visit https://api-console.zoho.in/ and create a Self-Client\n2. Set scope to ZohoExpense.fullaccess.ALL and click Create\n3. Copy the 10-min authorization code\n4. Run: zoho-expense-pp-cli auth login --client-id=... --client-secret=... --code=...\n5. The CLI exchanges the code for a refresh token and stores it locally.\n" +
					"\n      Run 'zoho-expense-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "zoho-expense-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "zoho-expense-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "zoho-expense",
		"description": "Upload receipts, tag expenses, and bundle monthly reports on Zoho Expense from the terminal",
		"archetype":   "project-management",
		"tool_count":  70,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion zoho-expense-pp-cli binary.",
		"auth": map[string]any{
			"type": "oauth2",
			"env_vars": []map[string]any{
				{
					"name":        "ZOHO_EXPENSE_CLIENT_ID",
					"kind":        "auth_flow_input",
					"required":    true,
					"sensitive":   false,
					"description": "OAuth client ID from api-console.zoho.in self-client",
				},
				{
					"name":        "ZOHO_EXPENSE_CLIENT_SECRET",
					"kind":        "auth_flow_input",
					"required":    true,
					"sensitive":   true,
					"description": "Set during initial auth setup.",
				},
				{
					"name":        "ZOHO_EXPENSE_ORGANIZATION_ID",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   false,
					"description": "Zoho Expense organization ID (sent as X-com-zoho-expense-organizationid header on every request)",
				},
			},
			"key_url":      "https://api-console.zoho.in/",
			"instructions": "1. Visit https://api-console.zoho.in/ and create a Self-Client\n2. Set scope to ZohoExpense.fullaccess.ALL and click Create\n3. Copy the 10-min authorization code\n4. Run: zoho-expense-pp-cli auth login --client-id=... --client-secret=... --code=...\n5. The CLI exchanges the code for a refresh token and stores it locally.\n",
		},
		"resources": []map[string]any{
			{
				"name":        "currencies",
				"description": "Currencies and exchange rates",
				"endpoints":   []string{"create", "delete", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "customers",
				"description": "Customers (contacts) expenses can be billed to",
				"endpoints":   []string{"create", "delete", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "expense_categories",
				"description": "Expense categories used to classify expenses",
				"endpoints":   []string{"create", "delete", "disable", "enable", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "expense_reports",
				"description": "Expense reports — bundles of expenses submitted for approval and reimbursement",
				"endpoints":   []string{"approval_history", "approve", "create", "get", "list", "reimburse", "reject", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "expenses",
				"description": "Expenses — the primary entity for an expense management CLI",
				"endpoints":   []string{"create", "get", "list", "merge", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "organizations",
				"description": "Zoho Expense organizations you have access to",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "projects",
				"description": "Projects expenses can be associated with",
				"endpoints":   []string{"activate", "create", "deactivate", "delete", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "receipts",
				"description": "Upload receipts for autoscan",
				"endpoints":   []string{"upload"},
			},
			{
				"name":        "reporting_tags",
				"description": "Reporting tags (custom tagging schema for expenses, e.g. cost center, billable, GST treatment)",
				"endpoints":   []string{"activate", "create", "deactivate", "delete", "get", "list", "list_options", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "taxes",
				"description": "Taxes (GST in India) applied to expenses",
				"endpoints":   []string{"create", "delete", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "trips",
				"description": "Business trips that group related expenses",
				"endpoints":   []string{"approve", "cancel", "close", "create", "delete", "get", "list", "reject", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "users",
				"description": "Manage users in the organization",
				"endpoints":   []string{"activate", "deactivate", "delete", "get", "invite", "list", "me", "update"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass page parameter for subsequent pages.",
			"Control page size with the per_page parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		"playbook": []map[string]string{
			{"topic": "Finding stale work", "insight": "Use the stale command or sql query to find items not updated recently. More reliable than scanning list results manually."},
			{"topic": "Load analysis", "insight": "When analyzing team workload, filter by assignee and status. Raw counts without status filtering are misleading."},
			{"topic": "Bulk operations", "insight": "For bulk status changes, prefer update endpoints over delete+create. Most PM APIs track history on updates."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
