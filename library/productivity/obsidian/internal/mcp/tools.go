// Copyright 2026 Angelo Pullen and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/productivity/obsidian/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/productivity/obsidian/internal/client"
	"github.com/mvanhorn/printing-press-library/library/productivity/obsidian/internal/config"
	"github.com/mvanhorn/printing-press-library/library/productivity/obsidian/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/productivity/obsidian/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("deadends_list",
			mcplib.WithDescription("List notes with no outgoing links. Optional: total, all. Returns array of string."),
			mcplib.WithBoolean("total", mcplib.Description("Total")),
			mcplib.WithBoolean("all", mcplib.Description("All")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/deadends", false, []mcpParamBinding{{PublicName: "total", WireName: "total", Location: "query"}, {PublicName: "all", WireName: "all", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_list",
			mcplib.WithDescription("Wraps `obsidian files`. Filter by folder or extension. Optional: folder, ext, total. Returns array of string."),
			mcplib.WithString("folder", mcplib.Description("Folder")),
			mcplib.WithString("ext", mcplib.Description("Ext")),
			mcplib.WithBoolean("total", mcplib.Description("Total")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/files", false, []mcpParamBinding{{PublicName: "folder", WireName: "folder", Location: "query"}, {PublicName: "ext", WireName: "ext", Location: "query"}, {PublicName: "total", WireName: "total", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("folders_list",
			mcplib.WithDescription("Wraps `obsidian folders`. Optional: folder, total. Returns array of string."),
			mcplib.WithString("folder", mcplib.Description("Filter by parent folder")),
			mcplib.WithBoolean("total", mcplib.Description("Total")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/folders", false, []mcpParamBinding{{PublicName: "folder", WireName: "folder", Location: "query"}, {PublicName: "total", WireName: "total", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("live-search_context",
			mcplib.WithDescription("Wraps `obsidian search:context query=<text>` — body-text search with surrounding lines for each hit. Required: query. Optional: path, limit, case (plus 1 more). Returns array of LiveSearchContextItem."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Query")),
			mcplib.WithString("path", mcplib.Description("Limit to folder")),
			mcplib.WithNumber("limit", mcplib.Description("Limit")),
			mcplib.WithBoolean("case", mcplib.Description("Case")),
			mcplib.WithString("format", mcplib.Description("Output format")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/live-search/context", false, []mcpParamBinding{{PublicName: "query", WireName: "query", Location: "query"}, {PublicName: "path", WireName: "path", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "case", WireName: "case", Location: "query"}, {PublicName: "format", WireName: "format", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("live-search_live_search",
			mcplib.WithDescription("Wraps `obsidian search query=<text>` — dials the running Obsidian process for body-text search. Distinct from the framework `search` command, which queries the local mirror (titles/paths only). limit= bounds output. Required: query. Optional: path, limit, total (plus 2 more). Returns array of LiveSearchLiveSearchItem."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Query")),
			mcplib.WithString("path", mcplib.Description("Limit to folder")),
			mcplib.WithNumber("limit", mcplib.Description("Limit")),
			mcplib.WithBoolean("total", mcplib.Description("Return match count only")),
			mcplib.WithBoolean("case", mcplib.Description("Case sensitive")),
			mcplib.WithString("format", mcplib.Description("Output format")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/live-search", false, []mcpParamBinding{{PublicName: "query", WireName: "query", Location: "query"}, {PublicName: "path", WireName: "path", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "total", WireName: "total", Location: "query"}, {PublicName: "case", WireName: "case", Location: "query"}, {PublicName: "format", WireName: "format", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("notes_read",
			mcplib.WithDescription("Wraps `obsidian read file={name}` (or path= for exact paths). Returns raw file contents. Required: name. Optional: path."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Note name (wikilink-style resolution, maps to obsidian `file=`).")),
			mcplib.WithString("path", mcplib.Description("Exact vault-relative path (maps to obsidian `path=`). Alternative to the {name} path parameter.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/notes/{name}", false, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "path", WireName: "path", Location: "query"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("notes_backlinks_list",
			mcplib.WithDescription("Wraps `obsidian backlinks file={name}`. Required: name. Optional: path, counts, total (plus 1 more). Returns array of BacklinksListItem."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Note name (wikilink-style resolution, maps to obsidian `file=`).")),
			mcplib.WithString("path", mcplib.Description("Exact vault-relative path (maps to obsidian `path=`). Alternative to the {name} path parameter.")),
			mcplib.WithBoolean("counts", mcplib.Description("Counts")),
			mcplib.WithBoolean("total", mcplib.Description("Return backlink count only")),
			mcplib.WithString("format", mcplib.Description("Output format")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/notes/{name}/backlinks", false, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "path", WireName: "path", Location: "query"}, {PublicName: "counts", WireName: "counts", Location: "query"}, {PublicName: "total", WireName: "total", Location: "query"}, {PublicName: "format", WireName: "format", Location: "query"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("notes_links_list",
			mcplib.WithDescription("Wraps `obsidian links file={name}`. Required: name. Optional: path, total. Returns array of LinksListItem."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Note name (wikilink-style resolution, maps to obsidian `file=`).")),
			mcplib.WithString("path", mcplib.Description("Exact vault-relative path (maps to obsidian `path=`). Alternative to the {name} path parameter.")),
			mcplib.WithBoolean("total", mcplib.Description("Return link count only")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/notes/{name}/links", false, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "path", WireName: "path", Location: "query"}, {PublicName: "total", WireName: "total", Location: "query"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("notes_properties_read-property",
			mcplib.WithDescription("Wraps `obsidian property:read name={property} file={name}`. (property:set is V2 — it inherits the markdown-patch corruption bug.). Required: name, property. Optional: path. Returns the PropertiesReadPropertyResponse."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Note name (wikilink-style resolution, maps to obsidian `file=`).")),
			mcplib.WithString("property", mcplib.Required(), mcplib.Description("Frontmatter property name (maps to obsidian `name=` on property:read).")),
			mcplib.WithString("path", mcplib.Description("Exact vault-relative path (maps to obsidian `path=`). Alternative to the {name} path parameter.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/notes/{name}/properties/{property}", false, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "property", WireName: "property", Location: "path"}, {PublicName: "path", WireName: "path", Location: "query"}}, []string{"name", "property"}),
	)
	s.AddTool(
		mcplib.NewTool("tags_list",
			mcplib.WithDescription("Wraps `obsidian tags`. Optionally scope to a file via path= query. Optional: path, counts, total (plus 2 more). Returns array of string."),
			mcplib.WithString("path", mcplib.Description("Scope to a file path")),
			mcplib.WithBoolean("counts", mcplib.Description("Counts")),
			mcplib.WithBoolean("total", mcplib.Description("Total")),
			mcplib.WithString("sort", mcplib.Description("Sort")),
			mcplib.WithString("format", mcplib.Description("Output format")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/tags", false, []mcpParamBinding{{PublicName: "path", WireName: "path", Location: "query"}, {PublicName: "counts", WireName: "counts", Location: "query"}, {PublicName: "total", WireName: "total", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "format", WireName: "format", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("tasks_list",
			mcplib.WithDescription("Wraps `obsidian tasks`. Filter by done/todo/status. (task mutation is V2.). Optional: done, todo, status (plus 2 more). Returns array of string."),
			mcplib.WithBoolean("done", mcplib.Description("Done")),
			mcplib.WithBoolean("todo", mcplib.Description("Todo")),
			mcplib.WithString("status", mcplib.Description("Filter by status character")),
			mcplib.WithBoolean("total", mcplib.Description("Total")),
			mcplib.WithString("format", mcplib.Description("Output format")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/tasks", false, []mcpParamBinding{{PublicName: "done", WireName: "done", Location: "query"}, {PublicName: "todo", WireName: "todo", Location: "query"}, {PublicName: "status", WireName: "status", Location: "query"}, {PublicName: "total", WireName: "total", Location: "query"}, {PublicName: "format", WireName: "format", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("unresolved_list",
			mcplib.WithDescription("Wraps `obsidian unresolved`. Tier-3 `broken` builds on this with source-note context. Optional: counts, total, verbose (plus 1 more). Returns array of string."),
			mcplib.WithBoolean("counts", mcplib.Description("Counts")),
			mcplib.WithBoolean("total", mcplib.Description("Total")),
			mcplib.WithBoolean("verbose", mcplib.Description("Include source files")),
			mcplib.WithString("format", mcplib.Description("Output format")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/unresolved", false, []mcpParamBinding{{PublicName: "counts", WireName: "counts", Location: "query"}, {PublicName: "total", WireName: "total", Location: "query"}, {PublicName: "verbose", WireName: "verbose", Location: "query"}, {PublicName: "format", WireName: "format", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("vault_info",
			mcplib.WithDescription("Wraps `obsidian vault`. info= narrows to one field. Optional: info. Returns the VaultInfoResponse."),
			mcplib.WithString("info", mcplib.Description("Info")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/vault", false, []mcpParamBinding{{PublicName: "info", WireName: "info", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, binaryResponse bool, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if binaryResponse {
			headers = map[string]string{client.BinaryResponseHeader: "true"}
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if binaryResponse {
				data, err = c.GetWithHeaders(path, params, headers)
				break
			}
			data, err = c.Get(path, params)
		case "POST":
			if binaryResponse {
				data, _, err = c.PostWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PostWithParams(path, params, bodyArgs)
		case "PUT":
			if binaryResponse {
				data, _, err = c.PutWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(path, params, bodyArgs)
		case "PATCH":
			if binaryResponse {
				data, _, err = c.PatchWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(path, params, bodyArgs)
		case "DELETE":
			if binaryResponse {
				data, _, err = c.DeleteWithParamsAndHeaders(path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      See API docs: https://github.com/DrDriftwood" +
					"\n      Run 'obsidian-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      See API docs: https://github.com/DrDriftwood" +
					"\n      Run 'obsidian-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "obsidian-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "obsidian-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "obsidian",
		"description": "SYNTHETIC OpenAPI 3.0 spec mapping Obsidian's official command-line interface (v1.12+) to virtual REST endpoints....",
		"archetype":   "project-management",
		"tool_count":  13,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion obsidian-pp-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "deadends",
				"description": "Manage deadends",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "files",
				"description": "Manage files",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "folders",
				"description": "Manage folders",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "live-search",
				"description": "Manage live search",
				"endpoints":   []string{"context", "live_search"},
				"searchable":  true,
			},
			{
				"name":        "notes",
				"description": "Single-note read operations",
				"endpoints":   []string{"read"},
				"searchable":  true,
			},
			{
				"name":        "tags",
				"description": "Manage tags",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "tasks",
				"description": "Manage tasks",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "unresolved",
				"description": "Manage unresolved",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "vault",
				"description": "Vault-level inventory and metadata",
				"endpoints":   []string{"info"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		"playbook": []map[string]string{
			{"topic": "Finding stale work", "insight": "Use the stale command or sql query to find items not updated recently. More reliable than scanning list results manually."},
			{"topic": "Load analysis", "insight": "When analyzing team workload, filter by assignee and status. Raw counts without status filtering are misleading."},
			{"topic": "Bulk operations", "insight": "For bulk status changes, prefer update endpoints over delete+create. Most PM APIs track history on updates."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
