// Copyright 2026 Eric Jung and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/monitoring/adguard-home/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/monitoring/adguard-home/internal/client"
	"github.com/mvanhorn/printing-press-library/library/monitoring/adguard-home/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/monitoring/adguard-home/internal/config"
	"github.com/mvanhorn/printing-press-library/library/monitoring/adguard-home/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/monitoring/adguard-home/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("access_list",
			mcplib.WithDescription("List (dis)allowed clients, blocked hosts, etc. Returns the AccessListResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/access/list", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("access_set",
			mcplib.WithDescription("Set (dis)allowed clients, blocked hosts, etc. Optional: allowed_clients, blocked_hosts, disallowed_clients."),
			mcplib.WithString("allowed_clients", mcplib.Description("The allowlist of clients: IP addresses, CIDRs, or ClientIDs.")),
			mcplib.WithString("blocked_hosts", mcplib.Description("The blocklist of hosts.")),
			mcplib.WithString("disallowed_clients", mcplib.Description("The blocklist of clients: IP addresses, CIDRs, or ClientIDs.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/access/set", false, []mcpParamBinding{{PublicName: "allowed_clients", WireName: "allowed_clients", Location: "body"}, {PublicName: "blocked_hosts", WireName: "blocked_hosts", Location: "body"}, {PublicName: "disallowed_clients", WireName: "disallowed_clients", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("adguard-home-profile_get",
			mcplib.WithDescription("Get. Returns the ProfileInfo."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/profile", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("adguard-home-profile_update",
			mcplib.WithDescription("Updates current user info. Required: language, name, theme."),
			mcplib.WithString("language", mcplib.Required(), mcplib.Description("Language")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithString("theme", mcplib.Required(), mcplib.Description("Interface theme")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/profile/update", false, []mcpParamBinding{{PublicName: "language", WireName: "language", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "theme", WireName: "theme", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apple_mobile-config-do-h",
			mcplib.WithDescription("Get DNS over HTTPS .mobileconfig. Required: host. Optional: client_id."),
			mcplib.WithString("host", mcplib.Required(), mcplib.Description("Host for which the config is generated. If no host is provided, `tls.server_name` from the configuration file is...")),
			mcplib.WithString("client_id", mcplib.Description("ClientID.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apple/doh.mobileconfig", false, []mcpParamBinding{{PublicName: "host", WireName: "host", Location: "query"}, {PublicName: "client_id", WireName: "client_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apple_mobile-config-do-t",
			mcplib.WithDescription("Get DNS over TLS .mobileconfig. Required: host. Optional: client_id."),
			mcplib.WithString("host", mcplib.Required(), mcplib.Description("Host for which the config is generated. If no host is provided, `tls.server_name` from the configuration file is...")),
			mcplib.WithString("client_id", mcplib.Description("ClientID.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apple/dot.mobileconfig", false, []mcpParamBinding{{PublicName: "host", WireName: "host", Location: "query"}, {PublicName: "client_id", WireName: "client_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("blocked-services_all",
			mcplib.WithDescription("Get available services to use for blocking. Returns the BlockedServicesAll."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/blocked_services/all", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("blocked-services_available-services",
			mcplib.WithDescription("Deprecated: Use `GET /blocked_services/all` instead. Returns array of string."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/blocked_services/services", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("blocked-services_list",
			mcplib.WithDescription("Deprecated: Use `GET /blocked_services/get` instead. Returns array of string."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/blocked_services/list", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("blocked-services_schedule",
			mcplib.WithDescription("Get blocked services. Returns the BlockedServicesSchedule."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/blocked_services/get", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("blocked-services_schedule-update",
			mcplib.WithDescription("Update blocked services. Optional: ids, schedule."),
			mcplib.WithString("ids", mcplib.Description("The names of the blocked services.")),
			mcplib.WithString("schedule", mcplib.Description("Sets periods of inactivity for filtering blocked services. The schedule contains 7 days (Sunday to Saturday) and a...")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/blocked_services/update", false, []mcpParamBinding{{PublicName: "ids", WireName: "ids", Location: "body"}, {PublicName: "schedule", WireName: "schedule", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("blocked-services_set",
			mcplib.WithDescription("Deprecated: Use `PUT /blocked_services/update` instead."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/blocked_services/set", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("cache-clear_cache_clear",
			mcplib.WithDescription("Clear DNS cache."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/cache_clear", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_add",
			mcplib.WithDescription("Add a new client. Optional: blocked_services, blocked_services_schedule, filtering_enabled (plus 14 more)."),
			mcplib.WithString("blocked_services", mcplib.Description("Blocked services")),
			mcplib.WithString("blocked_services_schedule", mcplib.Description("Sets periods of inactivity for filtering blocked services. The schedule contains 7 days (Sunday to Saturday) and a...")),
			mcplib.WithBoolean("filtering_enabled", mcplib.Description("Filtering enabled")),
			mcplib.WithString("ids", mcplib.Description("IP, CIDR, MAC, or ClientID.")),
			mcplib.WithBoolean("ignore_querylog", mcplib.Description("NOTE: If `ignore_querylog` is not set in HTTP API `GET /clients/add` request then default value (false) will be...")),
			mcplib.WithBoolean("ignore_statistics", mcplib.Description("NOTE: If `ignore_statistics` is not set in HTTP API `GET /clients/add` request then default value (false) will be...")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithBoolean("parental_enabled", mcplib.Description("Parental enabled")),
			mcplib.WithString("safe_search", mcplib.Description("Safe search settings.")),
			mcplib.WithBoolean("safebrowsing_enabled", mcplib.Description("Safebrowsing enabled")),
			mcplib.WithBoolean("safesearch_enabled", mcplib.Description("Safesearch enabled")),
			mcplib.WithString("tags", mcplib.Description("Tags")),
			mcplib.WithString("upstreams", mcplib.Description("Upstreams")),
			mcplib.WithBoolean("upstreams_cache_enabled", mcplib.Description("NOTE: If `upstreams_cache_enabled` is not set in HTTP API `GET /clients/add` request then default value (false) will...")),
			mcplib.WithNumber("upstreams_cache_size", mcplib.Description("NOTE: If `upstreams_cache_enabled` is not set in HTTP API `GET /clients/update` request then the existing value will...")),
			mcplib.WithBoolean("use_global_blocked_services", mcplib.Description("Use global blocked services")),
			mcplib.WithBoolean("use_global_settings", mcplib.Description("Use global settings")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/clients/add", false, []mcpParamBinding{{PublicName: "blocked_services", WireName: "blocked_services", Location: "body"}, {PublicName: "blocked_services_schedule", WireName: "blocked_services_schedule", Location: "body"}, {PublicName: "filtering_enabled", WireName: "filtering_enabled", Location: "body"}, {PublicName: "ids", WireName: "ids", Location: "body"}, {PublicName: "ignore_querylog", WireName: "ignore_querylog", Location: "body"}, {PublicName: "ignore_statistics", WireName: "ignore_statistics", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "parental_enabled", WireName: "parental_enabled", Location: "body"}, {PublicName: "safe_search", WireName: "safe_search", Location: "body"}, {PublicName: "safebrowsing_enabled", WireName: "safebrowsing_enabled", Location: "body"}, {PublicName: "safesearch_enabled", WireName: "safesearch_enabled", Location: "body"}, {PublicName: "tags", WireName: "tags", Location: "body"}, {PublicName: "upstreams", WireName: "upstreams", Location: "body"}, {PublicName: "upstreams_cache_enabled", WireName: "upstreams_cache_enabled", Location: "body"}, {PublicName: "upstreams_cache_size", WireName: "upstreams_cache_size", Location: "body"}, {PublicName: "use_global_blocked_services", WireName: "use_global_blocked_services", Location: "body"}, {PublicName: "use_global_settings", WireName: "use_global_settings", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_delete",
			mcplib.WithDescription("Remove a client. Optional: name."),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/clients/delete", false, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_find",
			mcplib.WithDescription("Get information about clients by their IP addresses or ClientIDs. Optional: ip0. Returns array of ClientsFindEntry."),
			mcplib.WithString("ip0", mcplib.Description("Filter by IP address or ClientIDs. Parameters with names `ip1`, `ip2`, and so on are also accepted and interpreted...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/clients/find", false, []mcpParamBinding{{PublicName: "ip0", WireName: "ip0", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_search",
			mcplib.WithDescription("Retrieve information about clients by performing an exact match search using IP addresses, CIDRs, MAC addresses, or ClientIDs. Optional: clients. Returns array of ClientsFindEntry."),
			mcplib.WithString("clients", mcplib.Description("Clients")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/clients/search", false, []mcpParamBinding{{PublicName: "clients", WireName: "clients", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_status",
			mcplib.WithDescription("Get information about configured clients. Returns the Clients."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/clients", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_update",
			mcplib.WithDescription("Update client information. Optional: data, name."),
			mcplib.WithString("data", mcplib.Description("Client information.")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/clients/update", false, []mcpParamBinding{{PublicName: "data", WireName: "data", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dhcp_add-static-lease",
			mcplib.WithDescription("Adds a static lease. Required: hostname, ip, mac."),
			mcplib.WithString("hostname", mcplib.Required(), mcplib.Description("Hostname")),
			mcplib.WithString("ip", mcplib.Required(), mcplib.Description("Ip")),
			mcplib.WithString("mac", mcplib.Required(), mcplib.Description("Mac")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/dhcp/add_static_lease", false, []mcpParamBinding{{PublicName: "hostname", WireName: "hostname", Location: "body"}, {PublicName: "ip", WireName: "ip", Location: "body"}, {PublicName: "mac", WireName: "mac", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dhcp_check-active",
			mcplib.WithDescription("Searches for an active DHCP server on the network. Optional: interface. Returns the new DhcpSearchResult."),
			mcplib.WithString("interface", mcplib.Description("The name of the network interface")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/dhcp/find_active_dhcp", false, []mcpParamBinding{{PublicName: "interface", WireName: "interface", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dhcp_interfaces",
			mcplib.WithDescription("Gets the available interfaces. Returns the NetInterfaces."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/dhcp/interfaces", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dhcp_remove-static-lease",
			mcplib.WithDescription("Removes a static lease. Required: hostname, ip, mac."),
			mcplib.WithString("hostname", mcplib.Required(), mcplib.Description("Hostname")),
			mcplib.WithString("ip", mcplib.Required(), mcplib.Description("Ip")),
			mcplib.WithString("mac", mcplib.Required(), mcplib.Description("Mac")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/dhcp/remove_static_lease", false, []mcpParamBinding{{PublicName: "hostname", WireName: "hostname", Location: "body"}, {PublicName: "ip", WireName: "ip", Location: "body"}, {PublicName: "mac", WireName: "mac", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dhcp_reset",
			mcplib.WithDescription("Reset DHCP configuration."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/dhcp/reset", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dhcp_reset-leases",
			mcplib.WithDescription("Reset DHCP leases."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/dhcp/reset_leases", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dhcp_set-config",
			mcplib.WithDescription("Updates the current DHCP server configuration. Optional: enabled, interface_name, v4 (plus 1 more)."),
			mcplib.WithBoolean("enabled", mcplib.Description("Enabled")),
			mcplib.WithString("interface_name", mcplib.Description("Interface name")),
			mcplib.WithString("v4", mcplib.Description("V4")),
			mcplib.WithString("v6", mcplib.Description("V6")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/dhcp/set_config", false, []mcpParamBinding{{PublicName: "enabled", WireName: "enabled", Location: "body"}, {PublicName: "interface_name", WireName: "interface_name", Location: "body"}, {PublicName: "v4", WireName: "v4", Location: "body"}, {PublicName: "v6", WireName: "v6", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dhcp_status",
			mcplib.WithDescription("Gets the current DHCP settings and status. Returns the DhcpStatus."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/dhcp/status", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dhcp_update-static-lease",
			mcplib.WithDescription("Updates IP address, hostname of the static lease. IP version must be the same as previous. Required: hostname, ip, mac."),
			mcplib.WithString("hostname", mcplib.Required(), mcplib.Description("Hostname")),
			mcplib.WithString("ip", mcplib.Required(), mcplib.Description("Ip")),
			mcplib.WithString("mac", mcplib.Required(), mcplib.Description("Mac")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/dhcp/update_static_lease", false, []mcpParamBinding{{PublicName: "hostname", WireName: "hostname", Location: "body"}, {PublicName: "ip", WireName: "ip", Location: "body"}, {PublicName: "mac", WireName: "mac", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dns-config_dns_config",
			mcplib.WithDescription("Set general DNS parameters. Optional: blocked_response_ttl, blocking_ipv4, blocking_ipv6 (plus 26 more)."),
			mcplib.WithNumber("blocked_response_ttl", mcplib.Description("TTL for blocked responses.")),
			mcplib.WithString("blocking_ipv4", mcplib.Description("Blocking ipv4")),
			mcplib.WithString("blocking_ipv6", mcplib.Description("Blocking ipv6")),
			mcplib.WithString("blocking_mode", mcplib.Description("Blocking mode")),
			mcplib.WithString("bootstrap_dns", mcplib.Description("Bootstrap servers, port is optional after colon. Empty value will reset it to default values.")),
			mcplib.WithBoolean("cache_enabled", mcplib.Description("Enables or disables the DNS response cache. If `cache_enabled` is `true`, the companion field `cache_size` must be...")),
			mcplib.WithBoolean("cache_optimistic", mcplib.Description("Cache optimistic")),
			mcplib.WithNumber("cache_size", mcplib.Description("Cache size")),
			mcplib.WithNumber("cache_ttl_max", mcplib.Description("Cache ttl max")),
			mcplib.WithNumber("cache_ttl_min", mcplib.Description("Cache ttl min")),
			mcplib.WithBoolean("disable_ipv6", mcplib.Description("Disable ipv6")),
			mcplib.WithBoolean("dnssec_enabled", mcplib.Description("Dnssec enabled")),
			mcplib.WithString("edns_cs_custom_ip", mcplib.Description("Edns cs custom ip")),
			mcplib.WithBoolean("edns_cs_enabled", mcplib.Description("Edns cs enabled")),
			mcplib.WithBoolean("edns_cs_use_custom", mcplib.Description("Edns cs use custom")),
			mcplib.WithString("fallback_dns", mcplib.Description("List of fallback DNS servers used when upstream DNS servers are not responding. Empty value will clear the list.")),
			mcplib.WithString("local_ptr_upstreams", mcplib.Description("Upstream servers, port is optional after colon. Empty value will reset it to default values.")),
			mcplib.WithString("protection_disabled_until", mcplib.Description("Protection is pause until this time. Nullable.")),
			mcplib.WithBoolean("protection_enabled", mcplib.Description("Protection enabled")),
			mcplib.WithNumber("ratelimit", mcplib.Description("Ratelimit")),
			mcplib.WithNumber("ratelimit_subnet_subnet_len_ipv4", mcplib.Description("Length of the subnet mask for IPv4 addresses.")),
			mcplib.WithNumber("ratelimit_subnet_subnet_len_ipv6", mcplib.Description("Length of the subnet mask for IPv6 addresses.")),
			mcplib.WithString("ratelimit_whitelist", mcplib.Description("List of IP addresses excluded from rate limiting.")),
			mcplib.WithBoolean("resolve_clients", mcplib.Description("Resolve clients")),
			mcplib.WithString("upstream_dns", mcplib.Description("Upstream servers, port is optional after colon. Empty value will reset it to default values.")),
			mcplib.WithString("upstream_dns_file", mcplib.Description("Upstream dns file")),
			mcplib.WithString("upstream_mode", mcplib.Description("Upstream modes enumeration.")),
			mcplib.WithNumber("upstream_timeout", mcplib.Description("The number of seconds to wait for a response from the upstream server")),
			mcplib.WithBoolean("use_private_ptr_resolvers", mcplib.Description("Use private ptr resolvers")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/dns_config", false, []mcpParamBinding{{PublicName: "blocked_response_ttl", WireName: "blocked_response_ttl", Location: "body"}, {PublicName: "blocking_ipv4", WireName: "blocking_ipv4", Location: "body"}, {PublicName: "blocking_ipv6", WireName: "blocking_ipv6", Location: "body"}, {PublicName: "blocking_mode", WireName: "blocking_mode", Location: "body"}, {PublicName: "bootstrap_dns", WireName: "bootstrap_dns", Location: "body"}, {PublicName: "cache_enabled", WireName: "cache_enabled", Location: "body"}, {PublicName: "cache_optimistic", WireName: "cache_optimistic", Location: "body"}, {PublicName: "cache_size", WireName: "cache_size", Location: "body"}, {PublicName: "cache_ttl_max", WireName: "cache_ttl_max", Location: "body"}, {PublicName: "cache_ttl_min", WireName: "cache_ttl_min", Location: "body"}, {PublicName: "disable_ipv6", WireName: "disable_ipv6", Location: "body"}, {PublicName: "dnssec_enabled", WireName: "dnssec_enabled", Location: "body"}, {PublicName: "edns_cs_custom_ip", WireName: "edns_cs_custom_ip", Location: "body"}, {PublicName: "edns_cs_enabled", WireName: "edns_cs_enabled", Location: "body"}, {PublicName: "edns_cs_use_custom", WireName: "edns_cs_use_custom", Location: "body"}, {PublicName: "fallback_dns", WireName: "fallback_dns", Location: "body"}, {PublicName: "local_ptr_upstreams", WireName: "local_ptr_upstreams", Location: "body"}, {PublicName: "protection_disabled_until", WireName: "protection_disabled_until", Location: "body"}, {PublicName: "protection_enabled", WireName: "protection_enabled", Location: "body"}, {PublicName: "ratelimit", WireName: "ratelimit", Location: "body"}, {PublicName: "ratelimit_subnet_subnet_len_ipv4", WireName: "ratelimit_subnet_subnet_len_ipv4", Location: "body"}, {PublicName: "ratelimit_subnet_subnet_len_ipv6", WireName: "ratelimit_subnet_subnet_len_ipv6", Location: "body"}, {PublicName: "ratelimit_whitelist", WireName: "ratelimit_whitelist", Location: "body"}, {PublicName: "resolve_clients", WireName: "resolve_clients", Location: "body"}, {PublicName: "upstream_dns", WireName: "upstream_dns", Location: "body"}, {PublicName: "upstream_dns_file", WireName: "upstream_dns_file", Location: "body"}, {PublicName: "upstream_mode", WireName: "upstream_mode", Location: "body"}, {PublicName: "upstream_timeout", WireName: "upstream_timeout", Location: "body"}, {PublicName: "use_private_ptr_resolvers", WireName: "use_private_ptr_resolvers", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dns-info_dns_info",
			mcplib.WithDescription("Get general DNS parameters. Returns the DnsInfoDnsInfoResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/dns_info", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("filtering_add-url",
			mcplib.WithDescription("Add filter URL or an absolute file path. Optional: name, url, whitelist."),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("url", mcplib.Description("URL or an absolute path to the file containing filtering rules.")),
			mcplib.WithBoolean("whitelist", mcplib.Description("Whitelist")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/filtering/add_url", false, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "url", WireName: "url", Location: "body"}, {PublicName: "whitelist", WireName: "whitelist", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("filtering_check-host",
			mcplib.WithDescription("Check if host name is filtered. Required: name. Optional: client, qtype. Returns the FilterCheckHostResponse."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Filter by host name")),
			mcplib.WithString("client", mcplib.Description("Optional ClientID or client IP address")),
			mcplib.WithString("qtype", mcplib.Description("Optional DNS type")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/filtering/check_host", false, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "query"}, {PublicName: "client", WireName: "client", Location: "query"}, {PublicName: "qtype", WireName: "qtype", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("filtering_config",
			mcplib.WithDescription("Set filtering parameters. Optional: enabled, interval."),
			mcplib.WithBoolean("enabled", mcplib.Description("Enabled")),
			mcplib.WithNumber("interval", mcplib.Description("Interval")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/filtering/config", false, []mcpParamBinding{{PublicName: "enabled", WireName: "enabled", Location: "body"}, {PublicName: "interval", WireName: "interval", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("filtering_refresh",
			mcplib.WithDescription("Reload filtering rules from URLs. This might be needed if new URL was just added and you don't want to wait for automatic refresh to kick in. This API request is ratelimited, so you can call it freely as often as you like, it wont create unnecessary burden on servers that host the URL. This should work as intended, a `force` parameter is offered as last-resort attempt to make filter lists fresh. If you ever find yourself using `force` to make something work that otherwise wont, this is a bug and report it accordingly. Optional: whitelist. Returns the new FilterRefreshResponse."),
			mcplib.WithBoolean("whitelist", mcplib.Description("Whitelist")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/filtering/refresh", false, []mcpParamBinding{{PublicName: "whitelist", WireName: "whitelist", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("filtering_remove-url",
			mcplib.WithDescription("Remove filter URL. Optional: url, whitelist."),
			mcplib.WithString("url", mcplib.Description("Previously added URL containing filtering rules")),
			mcplib.WithBoolean("whitelist", mcplib.Description("Whitelist")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/filtering/remove_url", false, []mcpParamBinding{{PublicName: "url", WireName: "url", Location: "body"}, {PublicName: "whitelist", WireName: "whitelist", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("filtering_set-rules",
			mcplib.WithDescription("Set user-defined filter rules. Optional: rules."),
			mcplib.WithString("rules", mcplib.Description("Rules")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/filtering/set_rules", false, []mcpParamBinding{{PublicName: "rules", WireName: "rules", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("filtering_set-url",
			mcplib.WithDescription("Set URL parameters. Optional: data, url, whitelist."),
			mcplib.WithString("data", mcplib.Description("Filter update data")),
			mcplib.WithString("url", mcplib.Description("Url")),
			mcplib.WithBoolean("whitelist", mcplib.Description("Whitelist")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/filtering/set_url", false, []mcpParamBinding{{PublicName: "data", WireName: "data", Location: "body"}, {PublicName: "url", WireName: "url", Location: "body"}, {PublicName: "whitelist", WireName: "whitelist", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("filtering_status",
			mcplib.WithDescription("Get filtering parameters. Returns the FilterStatus."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/filtering/status", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("i18n_change-language",
			mcplib.WithDescription("Change current language. Argument must be an ISO 639-1 two-letter code. Required: language."),
			mcplib.WithString("language", mcplib.Required(), mcplib.Description("The current language or the language to set.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/i18n/change_language", false, []mcpParamBinding{{PublicName: "language", WireName: "language", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("i18n_current-language",
			mcplib.WithDescription("Get currently set language. Result is ISO 639-1 two-letter code. Empty result means default language. Returns the LanguageSettings."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/i18n/current_language", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("install_check-config",
			mcplib.WithDescription("Checks configuration. Optional: dns, set_static_ip, web. Returns the new CheckConfigResponse."),
			mcplib.WithString("dns", mcplib.Description("Dns")),
			mcplib.WithBoolean("set_static_ip", mcplib.Description("Set static ip")),
			mcplib.WithString("web", mcplib.Description("Web")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/install/check_config", false, []mcpParamBinding{{PublicName: "dns", WireName: "dns", Location: "body"}, {PublicName: "set_static_ip", WireName: "set_static_ip", Location: "body"}, {PublicName: "web", WireName: "web", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("install_configure",
			mcplib.WithDescription("Applies the initial configuration. Required: dns, password, username, web."),
			mcplib.WithString("dns", mcplib.Required(), mcplib.Description("Port information")),
			mcplib.WithString("password", mcplib.Required(), mcplib.Description("Basic auth password")),
			mcplib.WithString("username", mcplib.Required(), mcplib.Description("Basic auth username")),
			mcplib.WithString("web", mcplib.Required(), mcplib.Description("Port information")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/install/configure", false, []mcpParamBinding{{PublicName: "dns", WireName: "dns", Location: "body"}, {PublicName: "password", WireName: "password", Location: "body"}, {PublicName: "username", WireName: "username", Location: "body"}, {PublicName: "web", WireName: "web", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("install_get-addresses",
			mcplib.WithDescription("Gets the network interfaces information. Returns the AddressesInfo."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/install/get_addresses", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("login_login",
			mcplib.WithDescription("Perform administrator log-in. Optional: name, password."),
			mcplib.WithString("name", mcplib.Description("User name")),
			mcplib.WithString("password", mcplib.Description("Password")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/login", false, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "password", WireName: "password", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("logout_logout",
			mcplib.WithDescription("Perform administrator log-out."),
			mcplib.WithReadOnlyHintAnnotation(false),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/logout", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("parental_disable",
			mcplib.WithDescription("Disable parental filtering."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/parental/disable", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("parental_enable",
			mcplib.WithDescription("Enable parental filtering."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/parental/enable", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("parental_status",
			mcplib.WithDescription("Get parental filtering status. Returns the ParentalStatusResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/parental/status", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("protection_set",
			mcplib.WithDescription("Set protection state and duration. Required: enabled. Optional: duration."),
			mcplib.WithNumber("duration", mcplib.Description("Duration of a pause, in milliseconds. Enabled should be false.")),
			mcplib.WithBoolean("enabled", mcplib.Required(), mcplib.Description("Enabled")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/protection", false, []mcpParamBinding{{PublicName: "duration", WireName: "duration", Location: "body"}, {PublicName: "enabled", WireName: "enabled", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("querylog_get-query-log-config",
			mcplib.WithDescription("Get query log parameters. Returns the GetQueryLogConfigResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/querylog/config", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("querylog_put-query-log-config",
			mcplib.WithDescription("Set query log parameters. Required: anonymize_client_ip, enabled, ignored, interval. Optional: ignored_enabled."),
			mcplib.WithBoolean("anonymize_client_ip", mcplib.Required(), mcplib.Description("Anonymize clients' IP addresses")),
			mcplib.WithBoolean("enabled", mcplib.Required(), mcplib.Description("Is query log enabled")),
			mcplib.WithString("ignored", mcplib.Required(), mcplib.Description("List of host names, which should not be written to log")),
			mcplib.WithBoolean("ignored_enabled", mcplib.Description("If true, the host names in the `ignored` array are excluded from the query log.")),
			mcplib.WithNumber("interval", mcplib.Required(), mcplib.Description("Time period for query log rotation in milliseconds.")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/querylog/config/update", false, []mcpParamBinding{{PublicName: "anonymize_client_ip", WireName: "anonymize_client_ip", Location: "body"}, {PublicName: "enabled", WireName: "enabled", Location: "body"}, {PublicName: "ignored", WireName: "ignored", Location: "body"}, {PublicName: "ignored_enabled", WireName: "ignored_enabled", Location: "body"}, {PublicName: "interval", WireName: "interval", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("querylog_query-log",
			mcplib.WithDescription("Get DNS server query log. Optional: older_than, offset, limit (plus 3 more). Returns array of QueryLogItem."),
			mcplib.WithString("older_than", mcplib.Description("Filter by older than")),
			mcplib.WithNumber("offset", mcplib.Description("Specify the ranking number of the first item on the page. Even though it is possible to use 'offset' and...")),
			mcplib.WithNumber("limit", mcplib.Description("Limit the number of records to be returned")),
			mcplib.WithString("search", mcplib.Description("Filter by domain name or client IP")),
			mcplib.WithString("response_status", mcplib.Description("Deprecated: Use 'reason' parameter instead Filter by response status NOTE: This parameter cannot be used with...")),
			mcplib.WithString("reason", mcplib.Description("Filter by response filtering reason. Multiple reasons can be provided. NOTE: This parameter cannot be used with...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/querylog", false, []mcpParamBinding{{PublicName: "older_than", WireName: "older_than", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "response_status", WireName: "response_status", Location: "query"}, {PublicName: "reason", WireName: "reason", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("querylog-clear_querylog_clear",
			mcplib.WithDescription("Clear query log."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/querylog_clear", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("querylog-config_query-log-config",
			mcplib.WithDescription("Deprecated: Use `PUT /querylog/config/update` instead. Optional: anonymize_client_ip, enabled, interval."),
			mcplib.WithBoolean("anonymize_client_ip", mcplib.Description("Anonymize clients' IP addresses")),
			mcplib.WithBoolean("enabled", mcplib.Description("Is query log enabled")),
			mcplib.WithNumber("interval", mcplib.Description("Time period for query log rotation.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/querylog_config", false, []mcpParamBinding{{PublicName: "anonymize_client_ip", WireName: "anonymize_client_ip", Location: "body"}, {PublicName: "enabled", WireName: "enabled", Location: "body"}, {PublicName: "interval", WireName: "interval", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("querylog-info_query-log-info",
			mcplib.WithDescription("Deprecated: Use `GET /querylog/config` instead. NOTE: If `interval` was configured by editing configuration file or new HTTP API call `PUT /querylog/config/update` and it's not equal to previous allowed enum values then it will be equal to `90` days for compatibility reasons. Returns the QueryLogConfig."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/querylog_info", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rewrite_add",
			mcplib.WithDescription("Add a new Rewrite rule. Optional: answer, domain, enabled (default: true)."),
			mcplib.WithString("answer", mcplib.Description("value of A, AAAA or CNAME DNS record")),
			mcplib.WithString("domain", mcplib.Description("Domain name")),
			mcplib.WithBoolean("enabled", mcplib.Description("Optional. If omitted on add, defaults to `true`. On update, omitted preserves previous value.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/rewrite/add", false, []mcpParamBinding{{PublicName: "answer", WireName: "answer", Location: "body"}, {PublicName: "domain", WireName: "domain", Location: "body"}, {PublicName: "enabled", WireName: "enabled", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rewrite_delete",
			mcplib.WithDescription("Remove a Rewrite rule. Optional: answer, domain, enabled (default: true)."),
			mcplib.WithString("answer", mcplib.Description("value of A, AAAA or CNAME DNS record")),
			mcplib.WithString("domain", mcplib.Description("Domain name")),
			mcplib.WithBoolean("enabled", mcplib.Description("Optional. If omitted on add, defaults to `true`. On update, omitted preserves previous value.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/rewrite/delete", false, []mcpParamBinding{{PublicName: "answer", WireName: "answer", Location: "body"}, {PublicName: "domain", WireName: "domain", Location: "body"}, {PublicName: "enabled", WireName: "enabled", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rewrite_list",
			mcplib.WithDescription("Get list of Rewrite rules. Returns array of RewriteEntry."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/rewrite/list", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rewrite_settings-get",
			mcplib.WithDescription("Get rewrite settings. Returns the RewriteSettings."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/rewrite/settings", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rewrite_settings-update",
			mcplib.WithDescription("Update rewrite settings. Required: enabled."),
			mcplib.WithBoolean("enabled", mcplib.Required(), mcplib.Description("indicates whether rewrites are applied")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/rewrite/settings/update", false, []mcpParamBinding{{PublicName: "enabled", WireName: "enabled", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rewrite_update",
			mcplib.WithDescription("Update a Rewrite rule. Optional: target, update."),
			mcplib.WithString("target", mcplib.Description("Rewrite rule")),
			mcplib.WithString("update", mcplib.Description("Rewrite rule")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/rewrite/update", false, []mcpParamBinding{{PublicName: "target", WireName: "target", Location: "body"}, {PublicName: "update", WireName: "update", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("safebrowsing_disable",
			mcplib.WithDescription("Disable safebrowsing."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/safebrowsing/disable", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("safebrowsing_enable",
			mcplib.WithDescription("Enable safebrowsing."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/safebrowsing/enable", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("safebrowsing_status",
			mcplib.WithDescription("Get safebrowsing status. Returns the SafebrowsingStatusResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/safebrowsing/status", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("safesearch_disable",
			mcplib.WithDescription("Disable safesearch."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/safesearch/disable", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("safesearch_enable",
			mcplib.WithDescription("Enable safesearch."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/safesearch/enable", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("safesearch_settings",
			mcplib.WithDescription("Update safesearch settings. Optional: bing, duckduckgo, ecosia (plus 5 more)."),
			mcplib.WithBoolean("bing", mcplib.Description("Bing")),
			mcplib.WithBoolean("duckduckgo", mcplib.Description("Duckduckgo")),
			mcplib.WithBoolean("ecosia", mcplib.Description("Ecosia")),
			mcplib.WithBoolean("enabled", mcplib.Description("Enabled")),
			mcplib.WithBoolean("google", mcplib.Description("Google")),
			mcplib.WithBoolean("pixabay", mcplib.Description("Pixabay")),
			mcplib.WithBoolean("yandex", mcplib.Description("Yandex")),
			mcplib.WithBoolean("youtube", mcplib.Description("Youtube")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/safesearch/settings", false, []mcpParamBinding{{PublicName: "bing", WireName: "bing", Location: "body"}, {PublicName: "duckduckgo", WireName: "duckduckgo", Location: "body"}, {PublicName: "ecosia", WireName: "ecosia", Location: "body"}, {PublicName: "enabled", WireName: "enabled", Location: "body"}, {PublicName: "google", WireName: "google", Location: "body"}, {PublicName: "pixabay", WireName: "pixabay", Location: "body"}, {PublicName: "yandex", WireName: "yandex", Location: "body"}, {PublicName: "youtube", WireName: "youtube", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("safesearch_status",
			mcplib.WithDescription("Get safesearch status. Returns the SafeSearchConfig."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/safesearch/status", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("stats_get-config",
			mcplib.WithDescription("Get statistics parameters. Returns the GetStatsConfigResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/stats/config", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("stats_put-config",
			mcplib.WithDescription("Set statistics parameters. Required: enabled, ignored, interval. Optional: ignored_enabled."),
			mcplib.WithBoolean("enabled", mcplib.Required(), mcplib.Description("Are statistics enabled")),
			mcplib.WithString("ignored", mcplib.Required(), mcplib.Description("List of host names, which should not be counted")),
			mcplib.WithBoolean("ignored_enabled", mcplib.Description("If true, the host names in the `ignored` array are excluded from the statistics.")),
			mcplib.WithNumber("interval", mcplib.Required(), mcplib.Description("Statistics rotation interval in milliseconds")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/stats/config/update", false, []mcpParamBinding{{PublicName: "enabled", WireName: "enabled", Location: "body"}, {PublicName: "ignored", WireName: "ignored", Location: "body"}, {PublicName: "ignored_enabled", WireName: "ignored_enabled", Location: "body"}, {PublicName: "interval", WireName: "interval", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("stats_stats",
			mcplib.WithDescription("Get DNS server statistics. Optional: recent. Returns the Stats."),
			mcplib.WithNumber("recent", mcplib.Description("The lookback period for statistics in milliseconds. The interval must be a multiple of one hour and must not be...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/stats", false, []mcpParamBinding{{PublicName: "recent", WireName: "recent", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("stats-config_stats_config",
			mcplib.WithDescription("Deprecated: Use `PUT /stats/config/update` instead. Optional: interval."),
			mcplib.WithNumber("interval", mcplib.Description("Time period to keep the data. `0` means that the statistics is disabled.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/stats_config", false, []mcpParamBinding{{PublicName: "interval", WireName: "interval", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("stats-info_stats_info",
			mcplib.WithDescription("Deprecated: Use `GET /stats/config` instead. NOTE: If `interval` was configured by editing configuration file or new HTTP API call `PUT /stats/config/update` and it's not equal to previous allowed enum values then it will be equal to `90` days for compatibility reasons. Returns the StatsConfig."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/stats_info", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("stats-reset_stats_reset",
			mcplib.WithDescription("Reset all statistics to zeroes."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/stats_reset", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("status_status",
			mcplib.WithDescription("Get DNS server current status and general settings. Returns the ServerStatus."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/status", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("test-upstream-dns_test_upstream_dns",
			mcplib.WithDescription("Test upstream configuration. Required: bootstrap_dns, upstream_dns. Optional: fallback_dns, private_upstream. Returns the new UpstreamsConfigResponse."),
			mcplib.WithString("bootstrap_dns", mcplib.Required(), mcplib.Description("Bootstrap DNS servers, port is optional after colon.")),
			mcplib.WithString("fallback_dns", mcplib.Description("Fallback DNS servers, port is optional after colon.")),
			mcplib.WithString("private_upstream", mcplib.Description("Local PTR resolvers, port is optional after colon.")),
			mcplib.WithString("upstream_dns", mcplib.Required(), mcplib.Description("Upstream DNS servers, port is optional after colon.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/test_upstream_dns", false, []mcpParamBinding{{PublicName: "bootstrap_dns", WireName: "bootstrap_dns", Location: "body"}, {PublicName: "fallback_dns", WireName: "fallback_dns", Location: "body"}, {PublicName: "private_upstream", WireName: "private_upstream", Location: "body"}, {PublicName: "upstream_dns", WireName: "upstream_dns", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("tls_configure",
			mcplib.WithDescription("Updates current TLS configuration. Optional: certificate_chain, certificate_path, dns_names (plus 20 more). Returns the new TlsConfig."),
			mcplib.WithString("certificate_chain", mcplib.Description("Base64 string with PEM-encoded certificates chain")),
			mcplib.WithString("certificate_path", mcplib.Description("Path to certificate file")),
			mcplib.WithString("dns_names", mcplib.Description("The value of SubjectAltNames field of the first certificate in the chain.")),
			mcplib.WithBoolean("enabled", mcplib.Description("enabled is the encryption (DoT/DoH/HTTPS) status")),
			mcplib.WithBoolean("force_https", mcplib.Description("if true, forces HTTP->HTTPS redirect")),
			mcplib.WithString("issuer", mcplib.Description("The issuer of the first certificate in the chain.")),
			mcplib.WithString("key_type", mcplib.Description("Key type.")),
			mcplib.WithString("not_after", mcplib.Description("The NotAfter field of the first certificate in the chain.")),
			mcplib.WithString("not_before", mcplib.Description("The NotBefore field of the first certificate in the chain.")),
			mcplib.WithNumber("port_dns_over_quic", mcplib.Description("DNS-over-QUIC port. If 0, DoQ will be disabled.")),
			mcplib.WithNumber("port_dns_over_tls", mcplib.Description("DNS-over-TLS port. If 0, DoT will be disabled.")),
			mcplib.WithNumber("port_https", mcplib.Description("HTTPS port. If 0, HTTPS will be disabled.")),
			mcplib.WithString("private_key", mcplib.Description("Base64 string with PEM-encoded private key")),
			mcplib.WithString("private_key_path", mcplib.Description("Path to private key file")),
			mcplib.WithBoolean("private_key_saved", mcplib.Description("Set to true if the user has previously saved a private key as a string. This is used so that the server and the...")),
			mcplib.WithBoolean("serve_plain_dns", mcplib.Description("Set to true if plain DNS is allowed for incoming requests.")),
			mcplib.WithString("server_name", mcplib.Description("server_name is the hostname of your HTTPS/TLS server")),
			mcplib.WithString("subject", mcplib.Description("The subject of the first certificate in the chain.")),
			mcplib.WithBoolean("valid_cert", mcplib.Description("Set to true if the specified certificates chain is a valid chain of X509 certificates.")),
			mcplib.WithBoolean("valid_chain", mcplib.Description("Set to true if the specified certificates chain is verified and issued by a known CA.")),
			mcplib.WithBoolean("valid_key", mcplib.Description("Set to true if the key is a valid private key.")),
			mcplib.WithBoolean("valid_pair", mcplib.Description("Set to true if both certificate and private key are correct.")),
			mcplib.WithString("warning_validation", mcplib.Description("A validation warning message with the issue description.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/tls/configure", false, []mcpParamBinding{{PublicName: "certificate_chain", WireName: "certificate_chain", Location: "body"}, {PublicName: "certificate_path", WireName: "certificate_path", Location: "body"}, {PublicName: "dns_names", WireName: "dns_names", Location: "body"}, {PublicName: "enabled", WireName: "enabled", Location: "body"}, {PublicName: "force_https", WireName: "force_https", Location: "body"}, {PublicName: "issuer", WireName: "issuer", Location: "body"}, {PublicName: "key_type", WireName: "key_type", Location: "body"}, {PublicName: "not_after", WireName: "not_after", Location: "body"}, {PublicName: "not_before", WireName: "not_before", Location: "body"}, {PublicName: "port_dns_over_quic", WireName: "port_dns_over_quic", Location: "body"}, {PublicName: "port_dns_over_tls", WireName: "port_dns_over_tls", Location: "body"}, {PublicName: "port_https", WireName: "port_https", Location: "body"}, {PublicName: "private_key", WireName: "private_key", Location: "body"}, {PublicName: "private_key_path", WireName: "private_key_path", Location: "body"}, {PublicName: "private_key_saved", WireName: "private_key_saved", Location: "body"}, {PublicName: "serve_plain_dns", WireName: "serve_plain_dns", Location: "body"}, {PublicName: "server_name", WireName: "server_name", Location: "body"}, {PublicName: "subject", WireName: "subject", Location: "body"}, {PublicName: "valid_cert", WireName: "valid_cert", Location: "body"}, {PublicName: "valid_chain", WireName: "valid_chain", Location: "body"}, {PublicName: "valid_key", WireName: "valid_key", Location: "body"}, {PublicName: "valid_pair", WireName: "valid_pair", Location: "body"}, {PublicName: "warning_validation", WireName: "warning_validation", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("tls_status",
			mcplib.WithDescription("Returns TLS configuration and its status."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/tls/status", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("tls_validate",
			mcplib.WithDescription("Checks if the current TLS configuration is valid. Optional: certificate_chain, certificate_path, dns_names (plus 20 more). Returns the new TlsConfig."),
			mcplib.WithString("certificate_chain", mcplib.Description("Base64 string with PEM-encoded certificates chain")),
			mcplib.WithString("certificate_path", mcplib.Description("Path to certificate file")),
			mcplib.WithString("dns_names", mcplib.Description("The value of SubjectAltNames field of the first certificate in the chain.")),
			mcplib.WithBoolean("enabled", mcplib.Description("enabled is the encryption (DoT/DoH/HTTPS) status")),
			mcplib.WithBoolean("force_https", mcplib.Description("if true, forces HTTP->HTTPS redirect")),
			mcplib.WithString("issuer", mcplib.Description("The issuer of the first certificate in the chain.")),
			mcplib.WithString("key_type", mcplib.Description("Key type.")),
			mcplib.WithString("not_after", mcplib.Description("The NotAfter field of the first certificate in the chain.")),
			mcplib.WithString("not_before", mcplib.Description("The NotBefore field of the first certificate in the chain.")),
			mcplib.WithNumber("port_dns_over_quic", mcplib.Description("DNS-over-QUIC port. If 0, DoQ will be disabled.")),
			mcplib.WithNumber("port_dns_over_tls", mcplib.Description("DNS-over-TLS port. If 0, DoT will be disabled.")),
			mcplib.WithNumber("port_https", mcplib.Description("HTTPS port. If 0, HTTPS will be disabled.")),
			mcplib.WithString("private_key", mcplib.Description("Base64 string with PEM-encoded private key")),
			mcplib.WithString("private_key_path", mcplib.Description("Path to private key file")),
			mcplib.WithBoolean("private_key_saved", mcplib.Description("Set to true if the user has previously saved a private key as a string. This is used so that the server and the...")),
			mcplib.WithBoolean("serve_plain_dns", mcplib.Description("Set to true if plain DNS is allowed for incoming requests.")),
			mcplib.WithString("server_name", mcplib.Description("server_name is the hostname of your HTTPS/TLS server")),
			mcplib.WithString("subject", mcplib.Description("The subject of the first certificate in the chain.")),
			mcplib.WithBoolean("valid_cert", mcplib.Description("Set to true if the specified certificates chain is a valid chain of X509 certificates.")),
			mcplib.WithBoolean("valid_chain", mcplib.Description("Set to true if the specified certificates chain is verified and issued by a known CA.")),
			mcplib.WithBoolean("valid_key", mcplib.Description("Set to true if the key is a valid private key.")),
			mcplib.WithBoolean("valid_pair", mcplib.Description("Set to true if both certificate and private key are correct.")),
			mcplib.WithString("warning_validation", mcplib.Description("A validation warning message with the issue description.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/tls/validate", false, []mcpParamBinding{{PublicName: "certificate_chain", WireName: "certificate_chain", Location: "body"}, {PublicName: "certificate_path", WireName: "certificate_path", Location: "body"}, {PublicName: "dns_names", WireName: "dns_names", Location: "body"}, {PublicName: "enabled", WireName: "enabled", Location: "body"}, {PublicName: "force_https", WireName: "force_https", Location: "body"}, {PublicName: "issuer", WireName: "issuer", Location: "body"}, {PublicName: "key_type", WireName: "key_type", Location: "body"}, {PublicName: "not_after", WireName: "not_after", Location: "body"}, {PublicName: "not_before", WireName: "not_before", Location: "body"}, {PublicName: "port_dns_over_quic", WireName: "port_dns_over_quic", Location: "body"}, {PublicName: "port_dns_over_tls", WireName: "port_dns_over_tls", Location: "body"}, {PublicName: "port_https", WireName: "port_https", Location: "body"}, {PublicName: "private_key", WireName: "private_key", Location: "body"}, {PublicName: "private_key_path", WireName: "private_key_path", Location: "body"}, {PublicName: "private_key_saved", WireName: "private_key_saved", Location: "body"}, {PublicName: "serve_plain_dns", WireName: "serve_plain_dns", Location: "body"}, {PublicName: "server_name", WireName: "server_name", Location: "body"}, {PublicName: "subject", WireName: "subject", Location: "body"}, {PublicName: "valid_cert", WireName: "valid_cert", Location: "body"}, {PublicName: "valid_chain", WireName: "valid_chain", Location: "body"}, {PublicName: "valid_key", WireName: "valid_key", Location: "body"}, {PublicName: "valid_pair", WireName: "valid_pair", Location: "body"}, {PublicName: "warning_validation", WireName: "warning_validation", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("update_begin",
			mcplib.WithDescription("Begin auto-upgrade procedure."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/update", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("version-json_get",
			mcplib.WithDescription("Gets information about the latest available version of AdGuard. Optional: recheck_now. Returns the new VersionInfo."),
			mcplib.WithBoolean("recheck_now", mcplib.Description("If false, server will check for a new version data only once in several hours.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/version.json", false, []mcpParamBinding{{PublicName: "recheck_now", WireName: "recheck_now", Location: "body"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, binaryResponse bool, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if binaryResponse {
			headers = map[string]string{client.BinaryResponseHeader: "true"}
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if binaryResponse {
				data, err = c.GetWithHeaders(path, params, headers)
				break
			}
			data, err = c.Get(path, params)
		case "POST":
			if binaryResponse {
				data, _, err = c.PostWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PostWithParams(path, params, bodyArgs)
		case "PUT":
			if binaryResponse {
				data, _, err = c.PutWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(path, params, bodyArgs)
		case "PATCH":
			if binaryResponse {
				data, _, err = c.PatchWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(path, params, bodyArgs)
		case "DELETE":
			if binaryResponse {
				data, _, err = c.DeleteWithParamsAndHeaders(path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export ADGUARD_HOME_USERNAME=<your-key>" +
					"\n      See API docs: https://github.com/AdguardTeam/AdGuardHome" +
					"\n      Use your AdGuard Home admin username and password. Set ADGUARD_HOME_URL to your instance URL (e.g. http://10.0.4.86:3001)." +
					"\n      Run 'adguard-home-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export ADGUARD_HOME_USERNAME=<your-key>" +
					"\n      See API docs: https://github.com/AdguardTeam/AdGuardHome" +
					"\n      Use your AdGuard Home admin username and password. Set ADGUARD_HOME_URL to your instance URL (e.g. http://10.0.4.86:3001)." +
					"\n      Run 'adguard-home-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export ADGUARD_HOME_USERNAME=<your-key>" +
					"\n      See API docs: https://github.com/AdguardTeam/AdGuardHome" +
					"\n      Use your AdGuard Home admin username and password. Set ADGUARD_HOME_URL to your instance URL (e.g. http://10.0.4.86:3001)." +
					"\n      Run 'adguard-home-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "adguard-home-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "adguard-home-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "adguard-home",
		"description": "DNS-level ad/tracker/malware blocking via AdGuard Home REST API for self-hosted network DNS management",
		"archetype":   "generic",
		"tool_count":  81,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion adguard-home-pp-cli binary.",
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name":      "ADGUARD_HOME_USERNAME",
					"kind":      "per_call",
					"required":  true,
					"sensitive": false,
				},
				{
					"name":        "ADGUARD_HOME_PASSWORD",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"docs_url":     "https://github.com/AdguardTeam/AdGuardHome",
			"instructions": "Use your AdGuard Home admin username and password. Set ADGUARD_HOME_URL to your instance URL (e.g. http://10.0.4.86:3001).",
		},
		"resources": []map[string]any{
			{
				"name":        "access",
				"description": "Manage access",
				"endpoints":   []string{"list", "set"},
				"syncable":    true,
			},
			{
				"name":        "adguard-home-profile",
				"description": "Manage adguard home profile",
				"endpoints":   []string{"get", "update"},
				"searchable":  true,
			},
			{
				"name":        "apple",
				"description": "Manage apple",
				"endpoints":   []string{"mobile-config-do-h", "mobile-config-do-t"},
				"searchable":  true,
			},
			{
				"name":        "blocked-services",
				"description": "Blocked services controls",
				"endpoints":   []string{"all", "available-services", "list", "schedule", "schedule-update", "set"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "cache-clear",
				"description": "Manage cache clear",
				"endpoints":   []string{"cache_clear"},
			},
			{
				"name":        "clients",
				"description": "Clients list operations",
				"endpoints":   []string{"add", "delete", "find", "search", "status", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "dhcp",
				"description": "Built-in DHCP server controls",
				"endpoints":   []string{"add-static-lease", "check-active", "interfaces", "remove-static-lease", "reset", "reset-leases", "set-config", "status", "update-static-lease"},
				"searchable":  true,
			},
			{
				"name":        "dns-config",
				"description": "Manage dns config",
				"endpoints":   []string{"dns_config"},
				"searchable":  true,
			},
			{
				"name":        "dns-info",
				"description": "Manage dns info",
				"endpoints":   []string{"dns_info"},
				"syncable":    true,
			},
			{
				"name":        "filtering",
				"description": "Rule-based filtering",
				"endpoints":   []string{"add-url", "check-host", "config", "refresh", "remove-url", "set-rules", "set-url", "status"},
				"searchable":  true,
			},
			{
				"name":        "i18n",
				"description": "Application localization",
				"endpoints":   []string{"change-language", "current-language"},
				"searchable":  true,
			},
			{
				"name":        "install",
				"description": "First-time install configuration handlers",
				"endpoints":   []string{"check-config", "configure", "get-addresses"},
				"searchable":  true,
			},
			{
				"name":        "login",
				"description": "Manage login",
				"endpoints":   []string{"login"},
				"searchable":  true,
			},
			{
				"name":        "logout",
				"description": "Manage logout",
				"endpoints":   []string{"logout"},
			},
			{
				"name":        "parental",
				"description": "Blocking adult and explicit materials",
				"endpoints":   []string{"disable", "enable", "status"},
				"syncable":    true,
			},
			{
				"name":        "protection",
				"description": "Manage protection",
				"endpoints":   []string{"set"},
			},
			{
				"name":        "querylog",
				"description": "Manage querylog",
				"endpoints":   []string{"get-query-log-config", "put-query-log-config", "query-log"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "querylog-clear",
				"description": "Manage querylog clear",
				"endpoints":   []string{"querylog_clear"},
			},
			{
				"name":        "querylog-config",
				"description": "Manage querylog config",
				"endpoints":   []string{"query-log-config"},
				"syncable":    true,
			},
			{
				"name":        "querylog-info",
				"description": "Manage querylog info",
				"endpoints":   []string{"query-log-info"},
				"syncable":    true,
			},
			{
				"name":        "rewrite",
				"description": "DNS rewrites",
				"endpoints":   []string{"add", "delete", "list", "settings-get", "settings-update", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "safebrowsing",
				"description": "Blocking malware/phishing sites",
				"endpoints":   []string{"disable", "enable", "status"},
				"syncable":    true,
			},
			{
				"name":        "safesearch",
				"description": "Enforce family-friendly results in search engines",
				"endpoints":   []string{"disable", "enable", "settings", "status"},
			},
			{
				"name":        "stats",
				"description": "AdGuard Home statistics",
				"endpoints":   []string{"get-config", "put-config", "stats"},
				"syncable":    true,
			},
			{
				"name":        "stats-config",
				"description": "Manage stats config",
				"endpoints":   []string{"stats_config"},
			},
			{
				"name":        "stats-info",
				"description": "Manage stats info",
				"endpoints":   []string{"stats_info"},
			},
			{
				"name":        "stats-reset",
				"description": "Manage stats reset",
				"endpoints":   []string{"stats_reset"},
			},
			{
				"name":        "status",
				"description": "Manage status",
				"endpoints":   []string{"status"},
			},
			{
				"name":        "test-upstream-dns",
				"description": "Manage test upstream dns",
				"endpoints":   []string{"test_upstream_dns"},
			},
			{
				"name":        "tls",
				"description": "AdGuard Home HTTPS/DoH/DoQ/DoT settings",
				"endpoints":   []string{"configure", "status", "validate"},
				"searchable":  true,
			},
			{
				"name":        "update",
				"description": "Manage update",
				"endpoints":   []string{"begin"},
			},
			{
				"name":        "version-json",
				"description": "Manage version json",
				"endpoints":   []string{"get"},
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass offset parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
