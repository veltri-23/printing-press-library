// Copyright 2026 Eric Jung and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newDnsConfigPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyBlockedResponseTtl int
	var bodyBlockingIpv4 string
	var bodyBlockingIpv6 string
	var bodyBlockingMode string
	var bodyBootstrapDns string
	var bodyCacheEnabled bool
	var bodyCacheOptimistic bool
	var bodyCacheSize int
	var bodyCacheTtlMax int
	var bodyCacheTtlMin int
	var bodyDisableIpv6 bool
	var bodyDnssecEnabled bool
	var bodyEdnsCsCustomIp string
	var bodyEdnsCsEnabled bool
	var bodyEdnsCsUseCustom bool
	var bodyFallbackDns string
	var bodyLocalPtrUpstreams string
	var bodyProtectionDisabledUntil string
	var bodyProtectionEnabled bool
	var bodyRatelimit int
	var bodyRatelimitSubnetSubnetLenIpv4 int
	var bodyRatelimitSubnetSubnetLenIpv6 int
	var bodyRatelimitWhitelist string
	var bodyResolveClients bool
	var bodyUpstreamDns string
	var bodyUpstreamDnsFile string
	var bodyUpstreamMode string
	var bodyUpstreamTimeout int
	var bodyUsePrivatePtrResolvers bool

	cmd := &cobra.Command{
		Use:         "dns-config",
		Short:       "Set general DNS parameters",
		Long:        "Shortcut for 'dns-config dns_config'. Set general DNS parameters",
		Example:     "  adguard-home-pp-cli dns-config",
		Annotations: map[string]string{"pp:endpoint": "dns-config.dns_config", "pp:method": "POST", "pp:path": "/dns_config"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/dns_config"
			params := map[string]string{}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyBlockedResponseTtl != 0 {
				body["blocked_response_ttl"] = bodyBlockedResponseTtl
			}
			if bodyBlockingIpv4 != "" {
				body["blocking_ipv4"] = bodyBlockingIpv4
			}
			if bodyBlockingIpv6 != "" {
				body["blocking_ipv6"] = bodyBlockingIpv6
			}
			if bodyBlockingMode != "" {
				body["blocking_mode"] = bodyBlockingMode
			}
			if bodyBootstrapDns != "" {
				var parsedBootstrapDns any
				if err := json.Unmarshal([]byte(bodyBootstrapDns), &parsedBootstrapDns); err != nil {
					return fmt.Errorf("parsing --bootstrap-dns JSON: %w", err)
				}
				body["bootstrap_dns"] = parsedBootstrapDns
			}
			if cmd.Flags().Changed("cache-enabled") {
				body["cache_enabled"] = bodyCacheEnabled
			}
			if cmd.Flags().Changed("cache-optimistic") {
				body["cache_optimistic"] = bodyCacheOptimistic
			}
			if bodyCacheSize != 0 {
				body["cache_size"] = bodyCacheSize
			}
			if bodyCacheTtlMax != 0 {
				body["cache_ttl_max"] = bodyCacheTtlMax
			}
			if bodyCacheTtlMin != 0 {
				body["cache_ttl_min"] = bodyCacheTtlMin
			}
			if cmd.Flags().Changed("disable-ipv6") {
				body["disable_ipv6"] = bodyDisableIpv6
			}
			if cmd.Flags().Changed("dnssec-enabled") {
				body["dnssec_enabled"] = bodyDnssecEnabled
			}
			if bodyEdnsCsCustomIp != "" {
				body["edns_cs_custom_ip"] = bodyEdnsCsCustomIp
			}
			if cmd.Flags().Changed("edns-cs-enabled") {
				body["edns_cs_enabled"] = bodyEdnsCsEnabled
			}
			if cmd.Flags().Changed("edns-cs-use-custom") {
				body["edns_cs_use_custom"] = bodyEdnsCsUseCustom
			}
			if bodyFallbackDns != "" {
				var parsedFallbackDns any
				if err := json.Unmarshal([]byte(bodyFallbackDns), &parsedFallbackDns); err != nil {
					return fmt.Errorf("parsing --fallback-dns JSON: %w", err)
				}
				body["fallback_dns"] = parsedFallbackDns
			}
			if bodyLocalPtrUpstreams != "" {
				var parsedLocalPtrUpstreams any
				if err := json.Unmarshal([]byte(bodyLocalPtrUpstreams), &parsedLocalPtrUpstreams); err != nil {
					return fmt.Errorf("parsing --local-ptr-upstreams JSON: %w", err)
				}
				body["local_ptr_upstreams"] = parsedLocalPtrUpstreams
			}
			if bodyProtectionDisabledUntil != "" {
				body["protection_disabled_until"] = bodyProtectionDisabledUntil
			}
			if cmd.Flags().Changed("protection-enabled") {
				body["protection_enabled"] = bodyProtectionEnabled
			}
			if bodyRatelimit != 0 {
				body["ratelimit"] = bodyRatelimit
			}
			if bodyRatelimitSubnetSubnetLenIpv4 != 0 {
				body["ratelimit_subnet_subnet_len_ipv4"] = bodyRatelimitSubnetSubnetLenIpv4
			}
			if bodyRatelimitSubnetSubnetLenIpv6 != 0 {
				body["ratelimit_subnet_subnet_len_ipv6"] = bodyRatelimitSubnetSubnetLenIpv6
			}
			if bodyRatelimitWhitelist != "" {
				var parsedRatelimitWhitelist any
				if err := json.Unmarshal([]byte(bodyRatelimitWhitelist), &parsedRatelimitWhitelist); err != nil {
					return fmt.Errorf("parsing --ratelimit-whitelist JSON: %w", err)
				}
				body["ratelimit_whitelist"] = parsedRatelimitWhitelist
			}
			if cmd.Flags().Changed("resolve-clients") {
				body["resolve_clients"] = bodyResolveClients
			}
			if bodyUpstreamDns != "" {
				var parsedUpstreamDns any
				if err := json.Unmarshal([]byte(bodyUpstreamDns), &parsedUpstreamDns); err != nil {
					return fmt.Errorf("parsing --upstream-dns JSON: %w", err)
				}
				body["upstream_dns"] = parsedUpstreamDns
			}
			if bodyUpstreamDnsFile != "" {
				body["upstream_dns_file"] = bodyUpstreamDnsFile
			}
			if bodyUpstreamMode != "" {
				body["upstream_mode"] = bodyUpstreamMode
			}
			if bodyUpstreamTimeout != 0 {
				body["upstream_timeout"] = bodyUpstreamTimeout
			}
			if cmd.Flags().Changed("use-private-ptr-resolvers") {
				body["use_private_ptr_resolvers"] = bodyUsePrivatePtrResolvers
			}
			data, _, err := c.PostWithParams(path, params, body)

			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyBlockedResponseTtl, "blocked-response-ttl", 0, "TTL for blocked responses.")
	cmd.Flags().StringVar(&bodyBlockingIpv4, "blocking-ipv4", "", "Blocking ipv4")
	cmd.Flags().StringVar(&bodyBlockingIpv6, "blocking-ipv6", "", "Blocking ipv6")
	cmd.Flags().StringVar(&bodyBlockingMode, "blocking-mode", "", "Blocking mode")
	cmd.Flags().StringVar(&bodyBootstrapDns, "bootstrap-dns", "", "Bootstrap servers, port is optional after colon. Empty value will reset it to default values.")
	cmd.Flags().BoolVar(&bodyCacheEnabled, "cache-enabled", false, "Enables or disables the DNS response cache. If `cache_enabled` is `true`, the companion field `cache_size` must be...")
	cmd.Flags().BoolVar(&bodyCacheOptimistic, "cache-optimistic", false, "Cache optimistic")
	cmd.Flags().IntVar(&bodyCacheSize, "cache-size", 0, "Cache size")
	cmd.Flags().IntVar(&bodyCacheTtlMax, "cache-ttl-max", 0, "Cache ttl max")
	cmd.Flags().IntVar(&bodyCacheTtlMin, "cache-ttl-min", 0, "Cache ttl min")
	cmd.Flags().BoolVar(&bodyDisableIpv6, "disable-ipv6", false, "Disable ipv6")
	cmd.Flags().BoolVar(&bodyDnssecEnabled, "dnssec-enabled", false, "Dnssec enabled")
	cmd.Flags().StringVar(&bodyEdnsCsCustomIp, "edns-cs-custom-ip", "", "Edns cs custom ip")
	cmd.Flags().BoolVar(&bodyEdnsCsEnabled, "edns-cs-enabled", false, "Edns cs enabled")
	cmd.Flags().BoolVar(&bodyEdnsCsUseCustom, "edns-cs-use-custom", false, "Edns cs use custom")
	cmd.Flags().StringVar(&bodyFallbackDns, "fallback-dns", "", "List of fallback DNS servers used when upstream DNS servers are not responding. Empty value will clear the list.")
	cmd.Flags().StringVar(&bodyLocalPtrUpstreams, "local-ptr-upstreams", "", "Upstream servers, port is optional after colon. Empty value will reset it to default values.")
	cmd.Flags().StringVar(&bodyProtectionDisabledUntil, "protection-disabled-until", "", "Protection is pause until this time. Nullable.")
	cmd.Flags().BoolVar(&bodyProtectionEnabled, "protection-enabled", false, "Protection enabled")
	cmd.Flags().IntVar(&bodyRatelimit, "ratelimit", 0, "Ratelimit")
	cmd.Flags().IntVar(&bodyRatelimitSubnetSubnetLenIpv4, "ratelimit-subnet-subnet-len-ipv4", 24, "Length of the subnet mask for IPv4 addresses.")
	cmd.Flags().IntVar(&bodyRatelimitSubnetSubnetLenIpv6, "ratelimit-subnet-subnet-len-ipv6", 56, "Length of the subnet mask for IPv6 addresses.")
	cmd.Flags().StringVar(&bodyRatelimitWhitelist, "ratelimit-whitelist", "", "List of IP addresses excluded from rate limiting.")
	cmd.Flags().BoolVar(&bodyResolveClients, "resolve-clients", false, "Resolve clients")
	cmd.Flags().StringVar(&bodyUpstreamDns, "upstream-dns", "", "Upstream servers, port is optional after colon. Empty value will reset it to default values.")
	cmd.Flags().StringVar(&bodyUpstreamDnsFile, "upstream-dns-file", "", "Upstream dns file")
	cmd.Flags().StringVar(&bodyUpstreamMode, "upstream-mode", "", "Upstream modes enumeration.")
	cmd.Flags().IntVar(&bodyUpstreamTimeout, "upstream-timeout", 0, "The number of seconds to wait for a response from the upstream server")
	cmd.Flags().BoolVar(&bodyUsePrivatePtrResolvers, "use-private-ptr-resolvers", false, "Use private ptr resolvers")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
