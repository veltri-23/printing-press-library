// Copyright 2026 Eric Jung and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newClientsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyDataBlockedServices string
	var bodyDataBlockedServicesScheduleTimeZone string
	var bodyDataFilteringEnabled bool
	var bodyDataIds string
	var bodyDataIgnoreQuerylog bool
	var bodyDataIgnoreStatistics bool
	var bodyDataName string
	var bodyDataParentalEnabled bool
	var bodyDataSafeSearchBing bool
	var bodyDataSafeSearchDuckduckgo bool
	var bodyDataSafeSearchEcosia bool
	var bodyDataSafeSearchEnabled bool
	var bodyDataSafeSearchGoogle bool
	var bodyDataSafeSearchPixabay bool
	var bodyDataSafeSearchYandex bool
	var bodyDataSafeSearchYoutube bool
	var bodyDataSafebrowsingEnabled bool
	var bodyDataSafesearchEnabled bool
	var bodyDataTags string
	var bodyDataUpstreams string
	var bodyDataUpstreamsCacheEnabled bool
	var bodyDataUpstreamsCacheSize int
	var bodyDataUseGlobalBlockedServices bool
	var bodyDataUseGlobalSettings bool
	var bodyName string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update client information",
		Example:     "  adguard-home-pp-cli clients update",
		Annotations: map[string]string{"pp:endpoint": "clients.update", "pp:method": "POST", "pp:path": "/clients/update"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/clients/update"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedData := map[string]any{}
					if bodyDataBlockedServices != "" {
						var parsedDataBlockedServices any
						if err := json.Unmarshal([]byte(bodyDataBlockedServices), &parsedDataBlockedServices); err != nil {
							return fmt.Errorf("parsing --data-blocked-services JSON: %w", err)
						}
						nestedData["blocked_services"] = parsedDataBlockedServices
					}
					{
						nestedDataBlockedServicesSchedule := map[string]any{}
						if bodyDataBlockedServicesScheduleTimeZone != "" {
							nestedDataBlockedServicesSchedule["time_zone"] = bodyDataBlockedServicesScheduleTimeZone
						}
						if len(nestedDataBlockedServicesSchedule) > 0 {
							nestedData["blocked_services_schedule"] = nestedDataBlockedServicesSchedule
						}
					}
					if cmd.Flags().Changed("data-filtering-enabled") {
						nestedData["filtering_enabled"] = bodyDataFilteringEnabled
					}
					if bodyDataIds != "" {
						var parsedDataIds any
						if err := json.Unmarshal([]byte(bodyDataIds), &parsedDataIds); err != nil {
							return fmt.Errorf("parsing --data-ids JSON: %w", err)
						}
						nestedData["ids"] = parsedDataIds
					}
					if cmd.Flags().Changed("data-ignore-querylog") {
						nestedData["ignore_querylog"] = bodyDataIgnoreQuerylog
					}
					if cmd.Flags().Changed("data-ignore-statistics") {
						nestedData["ignore_statistics"] = bodyDataIgnoreStatistics
					}
					if bodyDataName != "" {
						nestedData["name"] = bodyDataName
					}
					if cmd.Flags().Changed("data-parental-enabled") {
						nestedData["parental_enabled"] = bodyDataParentalEnabled
					}
					{
						nestedDataSafeSearch := map[string]any{}
						if cmd.Flags().Changed("data-safe-search-bing") {
							nestedDataSafeSearch["bing"] = bodyDataSafeSearchBing
						}
						if cmd.Flags().Changed("data-safe-search-duckduckgo") {
							nestedDataSafeSearch["duckduckgo"] = bodyDataSafeSearchDuckduckgo
						}
						if cmd.Flags().Changed("data-safe-search-ecosia") {
							nestedDataSafeSearch["ecosia"] = bodyDataSafeSearchEcosia
						}
						if cmd.Flags().Changed("data-safe-search-enabled") {
							nestedDataSafeSearch["enabled"] = bodyDataSafeSearchEnabled
						}
						if cmd.Flags().Changed("data-safe-search-google") {
							nestedDataSafeSearch["google"] = bodyDataSafeSearchGoogle
						}
						if cmd.Flags().Changed("data-safe-search-pixabay") {
							nestedDataSafeSearch["pixabay"] = bodyDataSafeSearchPixabay
						}
						if cmd.Flags().Changed("data-safe-search-yandex") {
							nestedDataSafeSearch["yandex"] = bodyDataSafeSearchYandex
						}
						if cmd.Flags().Changed("data-safe-search-youtube") {
							nestedDataSafeSearch["youtube"] = bodyDataSafeSearchYoutube
						}
						if len(nestedDataSafeSearch) > 0 {
							nestedData["safe_search"] = nestedDataSafeSearch
						}
					}
					if cmd.Flags().Changed("data-safebrowsing-enabled") {
						nestedData["safebrowsing_enabled"] = bodyDataSafebrowsingEnabled
					}
					if cmd.Flags().Changed("data-safesearch-enabled") {
						nestedData["safesearch_enabled"] = bodyDataSafesearchEnabled
					}
					if bodyDataTags != "" {
						var parsedDataTags any
						if err := json.Unmarshal([]byte(bodyDataTags), &parsedDataTags); err != nil {
							return fmt.Errorf("parsing --data-tags JSON: %w", err)
						}
						nestedData["tags"] = parsedDataTags
					}
					if bodyDataUpstreams != "" {
						var parsedDataUpstreams any
						if err := json.Unmarshal([]byte(bodyDataUpstreams), &parsedDataUpstreams); err != nil {
							return fmt.Errorf("parsing --data-upstreams JSON: %w", err)
						}
						nestedData["upstreams"] = parsedDataUpstreams
					}
					if cmd.Flags().Changed("data-upstreams-cache-enabled") {
						nestedData["upstreams_cache_enabled"] = bodyDataUpstreamsCacheEnabled
					}
					if bodyDataUpstreamsCacheSize != 0 {
						nestedData["upstreams_cache_size"] = bodyDataUpstreamsCacheSize
					}
					if cmd.Flags().Changed("data-use-global-blocked-services") {
						nestedData["use_global_blocked_services"] = bodyDataUseGlobalBlockedServices
					}
					if cmd.Flags().Changed("data-use-global-settings") {
						nestedData["use_global_settings"] = bodyDataUseGlobalSettings
					}
					if len(nestedData) > 0 {
						body["data"] = nestedData
					}
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "clients",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyDataBlockedServices, "data-blocked-services", "", "Blocked services")
	cmd.Flags().StringVar(&bodyDataBlockedServicesScheduleTimeZone, "data-blocked-services-schedule-time-zone", "", "Time zone name according to IANA time zone database. For example `Europe/Brussels`. `Local` represents the system's...")
	cmd.Flags().BoolVar(&bodyDataFilteringEnabled, "data-filtering-enabled", false, "Filtering enabled")
	cmd.Flags().StringVar(&bodyDataIds, "data-ids", "", "IP, CIDR, MAC, or ClientID.")
	cmd.Flags().BoolVar(&bodyDataIgnoreQuerylog, "data-ignore-querylog", false, "NOTE: If `ignore_querylog` is not set in HTTP API `GET /clients/add` request then default value (false) will be...")
	cmd.Flags().BoolVar(&bodyDataIgnoreStatistics, "data-ignore-statistics", false, "NOTE: If `ignore_statistics` is not set in HTTP API `GET /clients/add` request then default value (false) will be...")
	cmd.Flags().StringVar(&bodyDataName, "data-name", "", "Name")
	cmd.Flags().BoolVar(&bodyDataParentalEnabled, "data-parental-enabled", false, "Parental enabled")
	cmd.Flags().BoolVar(&bodyDataSafeSearchBing, "data-safe-search-bing", false, "Bing")
	cmd.Flags().BoolVar(&bodyDataSafeSearchDuckduckgo, "data-safe-search-duckduckgo", false, "Duckduckgo")
	cmd.Flags().BoolVar(&bodyDataSafeSearchEcosia, "data-safe-search-ecosia", false, "Ecosia")
	cmd.Flags().BoolVar(&bodyDataSafeSearchEnabled, "data-safe-search-enabled", false, "Enabled")
	cmd.Flags().BoolVar(&bodyDataSafeSearchGoogle, "data-safe-search-google", false, "Google")
	cmd.Flags().BoolVar(&bodyDataSafeSearchPixabay, "data-safe-search-pixabay", false, "Pixabay")
	cmd.Flags().BoolVar(&bodyDataSafeSearchYandex, "data-safe-search-yandex", false, "Yandex")
	cmd.Flags().BoolVar(&bodyDataSafeSearchYoutube, "data-safe-search-youtube", false, "Youtube")
	cmd.Flags().BoolVar(&bodyDataSafebrowsingEnabled, "data-safebrowsing-enabled", false, "Safebrowsing enabled")
	cmd.Flags().BoolVar(&bodyDataSafesearchEnabled, "data-safesearch-enabled", false, "Safesearch enabled")
	cmd.Flags().StringVar(&bodyDataTags, "data-tags", "", "Tags")
	cmd.Flags().StringVar(&bodyDataUpstreams, "data-upstreams", "", "Upstreams")
	cmd.Flags().BoolVar(&bodyDataUpstreamsCacheEnabled, "data-upstreams-cache-enabled", false, "NOTE: If `upstreams_cache_enabled` is not set in HTTP API `GET /clients/add` request then default value (false) will...")
	cmd.Flags().IntVar(&bodyDataUpstreamsCacheSize, "data-upstreams-cache-size", 0, "NOTE: If `upstreams_cache_enabled` is not set in HTTP API `GET /clients/update` request then the existing value will...")
	cmd.Flags().BoolVar(&bodyDataUseGlobalBlockedServices, "data-use-global-blocked-services", false, "Use global blocked services")
	cmd.Flags().BoolVar(&bodyDataUseGlobalSettings, "data-use-global-settings", false, "Use global settings")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
