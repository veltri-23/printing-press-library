// Copyright 2026 Eric Jung and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "access list", Description: "List (dis)allowed clients, blocked hosts, etc.", Group: "access"},
	{Command: "access set", Description: "Set (dis)allowed clients, blocked hosts, etc.", Group: "access"},
	{Command: "adguard-home-profile get", Description: "Get", Group: "adguard-home-profile"},
	{Command: "adguard-home-profile update", Description: "Updates current user info", Group: "adguard-home-profile"},
	{Command: "apple mobile-config-do-h", Description: "Get DNS over HTTPS .mobileconfig.", Group: "apple"},
	{Command: "apple mobile-config-do-t", Description: "Get DNS over TLS .mobileconfig.", Group: "apple"},
	{Command: "blocked-services all", Description: "Get available services to use for blocking", Group: "blocked-services"},
	{Command: "blocked-services available-services", Description: "Deprecated: Use `GET /blocked_services/all` instead.", Group: "blocked-services"},
	{Command: "blocked-services list", Description: "Deprecated: Use `GET /blocked_services/get` instead.", Group: "blocked-services"},
	{Command: "blocked-services schedule", Description: "Get blocked services", Group: "blocked-services"},
	{Command: "blocked-services schedule-update", Description: "Update blocked services", Group: "blocked-services"},
	{Command: "blocked-services set", Description: "Deprecated: Use `PUT /blocked_services/update` instead.", Group: "blocked-services"},
	{Command: "cache-clear cache_clear", Description: "Clear DNS cache", Group: "cache-clear"},
	{Command: "clients add", Description: "Add a new client", Group: "clients"},
	{Command: "clients delete", Description: "Remove a client", Group: "clients"},
	{Command: "clients find", Description: "Get information about clients by their IP addresses or ClientIDs.", Group: "clients"},
	{Command: "clients search", Description: "Retrieve information about clients by performing an exact match search using IP addresses, CIDRs, MAC addresses, or ClientIDs.", Group: "clients"},
	{Command: "clients status", Description: "Get information about configured clients", Group: "clients"},
	{Command: "clients update", Description: "Update client information", Group: "clients"},
	{Command: "dhcp add-static-lease", Description: "Adds a static lease", Group: "dhcp"},
	{Command: "dhcp check-active", Description: "Searches for an active DHCP server on the network", Group: "dhcp"},
	{Command: "dhcp interfaces", Description: "Gets the available interfaces", Group: "dhcp"},
	{Command: "dhcp remove-static-lease", Description: "Removes a static lease", Group: "dhcp"},
	{Command: "dhcp reset", Description: "Reset DHCP configuration", Group: "dhcp"},
	{Command: "dhcp reset-leases", Description: "Reset DHCP leases", Group: "dhcp"},
	{Command: "dhcp set-config", Description: "Updates the current DHCP server configuration", Group: "dhcp"},
	{Command: "dhcp status", Description: "Gets the current DHCP settings and status", Group: "dhcp"},
	{Command: "dhcp update-static-lease", Description: "Updates IP address, hostname of the static lease.  IP version must be the same as previous.", Group: "dhcp"},
	{Command: "dns-config dns_config", Description: "Set general DNS parameters", Group: "dns-config"},
	{Command: "dns-info dns_info", Description: "Get general DNS parameters", Group: "dns-info"},
	{Command: "filtering add-url", Description: "Add filter URL or an absolute file path", Group: "filtering"},
	{Command: "filtering check-host", Description: "Check if host name is filtered", Group: "filtering"},
	{Command: "filtering config", Description: "Set filtering parameters", Group: "filtering"},
	{Command: "filtering refresh", Description: "Reload filtering rules from URLs.  This might be needed if new URL was just added and you don't want to wait for automatic refresh to kick in. This API request is ratelimited, so you can call it freely as often as you like, it wont create unnecessary burden on servers that host the URL.  This should work as intended, a `force` parameter is offered as last-resort attempt to make filter lists fresh.  If you ever find yourself using `force` to make something work that otherwise wont, this is a bug and report it accordingly.", Group: "filtering"},
	{Command: "filtering remove-url", Description: "Remove filter URL", Group: "filtering"},
	{Command: "filtering set-rules", Description: "Set user-defined filter rules", Group: "filtering"},
	{Command: "filtering set-url", Description: "Set URL parameters", Group: "filtering"},
	{Command: "filtering status", Description: "Get filtering parameters", Group: "filtering"},
	{Command: "i18n change-language", Description: "Change current language.  Argument must be an ISO 639-1 two-letter code.", Group: "i18n"},
	{Command: "i18n current-language", Description: "Get currently set language.  Result is ISO 639-1 two-letter code.  Empty result means default language.", Group: "i18n"},
	{Command: "install check-config", Description: "Checks configuration", Group: "install"},
	{Command: "install configure", Description: "Applies the initial configuration.", Group: "install"},
	{Command: "install get-addresses", Description: "Gets the network interfaces information.", Group: "install"},
	{Command: "login login", Description: "Perform administrator log-in", Group: "login"},
	{Command: "logout logout", Description: "Perform administrator log-out", Group: "logout"},
	{Command: "parental disable", Description: "Disable parental filtering", Group: "parental"},
	{Command: "parental enable", Description: "Enable parental filtering", Group: "parental"},
	{Command: "parental status", Description: "Get parental filtering status", Group: "parental"},
	{Command: "protection set", Description: "Set protection state and duration", Group: "protection"},
	{Command: "querylog get-query-log-config", Description: "Get query log parameters", Group: "querylog"},
	{Command: "querylog put-query-log-config", Description: "Set query log parameters", Group: "querylog"},
	{Command: "querylog query-log", Description: "Get DNS server query log.", Group: "querylog"},
	{Command: "querylog-clear querylog_clear", Description: "Clear query log", Group: "querylog-clear"},
	{Command: "querylog-config query-log-config", Description: "Deprecated: Use `PUT /querylog/config/update` instead.", Group: "querylog-config"},
	{Command: "querylog-info query-log-info", Description: "Deprecated: Use `GET /querylog/config` instead.\n\nNOTE: If `interval` was configured by editing configuration file or new\nHTTP API call `PUT /querylog/config/update` and it's not equal to\nprevious allowed enum values then it will be equal to `90` days for\ncompatibility reasons.", Group: "querylog-info"},
	{Command: "rewrite add", Description: "Add a new Rewrite rule", Group: "rewrite"},
	{Command: "rewrite delete", Description: "Remove a Rewrite rule", Group: "rewrite"},
	{Command: "rewrite list", Description: "Get list of Rewrite rules", Group: "rewrite"},
	{Command: "rewrite settings-get", Description: "Get rewrite settings", Group: "rewrite"},
	{Command: "rewrite settings-update", Description: "Update rewrite settings", Group: "rewrite"},
	{Command: "rewrite update", Description: "Update a Rewrite rule", Group: "rewrite"},
	{Command: "safebrowsing disable", Description: "Disable safebrowsing", Group: "safebrowsing"},
	{Command: "safebrowsing enable", Description: "Enable safebrowsing", Group: "safebrowsing"},
	{Command: "safebrowsing status", Description: "Get safebrowsing status", Group: "safebrowsing"},
	{Command: "safesearch disable", Description: "Disable safesearch", Group: "safesearch"},
	{Command: "safesearch enable", Description: "Enable safesearch", Group: "safesearch"},
	{Command: "safesearch settings", Description: "Update safesearch settings", Group: "safesearch"},
	{Command: "safesearch status", Description: "Get safesearch status", Group: "safesearch"},
	{Command: "stats get-config", Description: "Get statistics parameters", Group: "stats"},
	{Command: "stats put-config", Description: "Set statistics parameters", Group: "stats"},
	{Command: "stats stats", Description: "Get DNS server statistics", Group: "stats"},
	{Command: "stats-config stats_config", Description: "Deprecated: Use `PUT /stats/config/update` instead.", Group: "stats-config"},
	{Command: "stats-info stats_info", Description: "Deprecated: Use `GET /stats/config` instead.\n\nNOTE: If `interval` was configured by editing configuration file or new\nHTTP API call `PUT /stats/config/update` and it's not equal to\nprevious allowed enum values then it will be equal to `90` days for\ncompatibility reasons.", Group: "stats-info"},
	{Command: "stats-reset stats_reset", Description: "Reset all statistics to zeroes", Group: "stats-reset"},
	{Command: "status status", Description: "Get DNS server current status and general settings", Group: "status"},
	{Command: "test-upstream-dns test_upstream_dns", Description: "Test upstream configuration", Group: "test-upstream-dns"},
	{Command: "tls configure", Description: "Updates current TLS configuration", Group: "tls"},
	{Command: "tls status", Description: "Returns TLS configuration and its status", Group: "tls"},
	{Command: "tls validate", Description: "Checks if the current TLS configuration is valid", Group: "tls"},
	{Command: "update begin", Description: "Begin auto-upgrade procedure", Group: "update"},
	{Command: "version-json get", Description: "Gets information about the latest available version of AdGuard", Group: "version-json"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  adguard-home-pp-cli which "stale tickets"
  adguard-home-pp-cli which "bottleneck"
  adguard-home-pp-cli which --limit 1 "send message"
  adguard-home-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
