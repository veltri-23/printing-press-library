// Copyright 2026 Eric Jung and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newClientsAddCmd(flags *rootFlags) *cobra.Command {
	var bodyBlockedServices string
	var bodyBlockedServicesScheduleFriEnd float64
	var bodyBlockedServicesScheduleFriStart float64
	var bodyBlockedServicesScheduleMonEnd float64
	var bodyBlockedServicesScheduleMonStart float64
	var bodyBlockedServicesScheduleSatEnd float64
	var bodyBlockedServicesScheduleSatStart float64
	var bodyBlockedServicesScheduleSunEnd float64
	var bodyBlockedServicesScheduleSunStart float64
	var bodyBlockedServicesScheduleThuEnd float64
	var bodyBlockedServicesScheduleThuStart float64
	var bodyBlockedServicesScheduleTimeZone string
	var bodyBlockedServicesScheduleTueEnd float64
	var bodyBlockedServicesScheduleTueStart float64
	var bodyBlockedServicesScheduleWedEnd float64
	var bodyBlockedServicesScheduleWedStart float64
	var bodyFilteringEnabled bool
	var bodyIds string
	var bodyIgnoreQuerylog bool
	var bodyIgnoreStatistics bool
	var bodyName string
	var bodyParentalEnabled bool
	var bodySafeSearchBing bool
	var bodySafeSearchDuckduckgo bool
	var bodySafeSearchEcosia bool
	var bodySafeSearchEnabled bool
	var bodySafeSearchGoogle bool
	var bodySafeSearchPixabay bool
	var bodySafeSearchYandex bool
	var bodySafeSearchYoutube bool
	var bodySafebrowsingEnabled bool
	var bodySafesearchEnabled bool
	var bodyTags string
	var bodyUpstreams string
	var bodyUpstreamsCacheEnabled bool
	var bodyUpstreamsCacheSize int
	var bodyUseGlobalBlockedServices bool
	var bodyUseGlobalSettings bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "add",
		Aliases:     []string{"create"},
		Short:       "Add a new client",
		Example:     "  adguard-home-pp-cli clients add",
		Annotations: map[string]string{"pp:endpoint": "clients.add", "pp:method": "POST", "pp:path": "/clients/add"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/clients/add"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyBlockedServices != "" {
					var parsedBlockedServices any
					if err := json.Unmarshal([]byte(bodyBlockedServices), &parsedBlockedServices); err != nil {
						return fmt.Errorf("parsing --blocked-services JSON: %w", err)
					}
					body["blocked_services"] = parsedBlockedServices
				}
				{
					nestedBlockedServicesSchedule := map[string]any{}
					{
						nestedBlockedServicesScheduleFri := map[string]any{}
						if bodyBlockedServicesScheduleFriEnd != 0.0 {
							nestedBlockedServicesScheduleFri["end"] = bodyBlockedServicesScheduleFriEnd
						}
						if bodyBlockedServicesScheduleFriStart != 0.0 {
							nestedBlockedServicesScheduleFri["start"] = bodyBlockedServicesScheduleFriStart
						}
						if len(nestedBlockedServicesScheduleFri) > 0 {
							nestedBlockedServicesSchedule["fri"] = nestedBlockedServicesScheduleFri
						}
					}
					{
						nestedBlockedServicesScheduleMon := map[string]any{}
						if bodyBlockedServicesScheduleMonEnd != 0.0 {
							nestedBlockedServicesScheduleMon["end"] = bodyBlockedServicesScheduleMonEnd
						}
						if bodyBlockedServicesScheduleMonStart != 0.0 {
							nestedBlockedServicesScheduleMon["start"] = bodyBlockedServicesScheduleMonStart
						}
						if len(nestedBlockedServicesScheduleMon) > 0 {
							nestedBlockedServicesSchedule["mon"] = nestedBlockedServicesScheduleMon
						}
					}
					{
						nestedBlockedServicesScheduleSat := map[string]any{}
						if bodyBlockedServicesScheduleSatEnd != 0.0 {
							nestedBlockedServicesScheduleSat["end"] = bodyBlockedServicesScheduleSatEnd
						}
						if bodyBlockedServicesScheduleSatStart != 0.0 {
							nestedBlockedServicesScheduleSat["start"] = bodyBlockedServicesScheduleSatStart
						}
						if len(nestedBlockedServicesScheduleSat) > 0 {
							nestedBlockedServicesSchedule["sat"] = nestedBlockedServicesScheduleSat
						}
					}
					{
						nestedBlockedServicesScheduleSun := map[string]any{}
						if bodyBlockedServicesScheduleSunEnd != 0.0 {
							nestedBlockedServicesScheduleSun["end"] = bodyBlockedServicesScheduleSunEnd
						}
						if bodyBlockedServicesScheduleSunStart != 0.0 {
							nestedBlockedServicesScheduleSun["start"] = bodyBlockedServicesScheduleSunStart
						}
						if len(nestedBlockedServicesScheduleSun) > 0 {
							nestedBlockedServicesSchedule["sun"] = nestedBlockedServicesScheduleSun
						}
					}
					{
						nestedBlockedServicesScheduleThu := map[string]any{}
						if bodyBlockedServicesScheduleThuEnd != 0.0 {
							nestedBlockedServicesScheduleThu["end"] = bodyBlockedServicesScheduleThuEnd
						}
						if bodyBlockedServicesScheduleThuStart != 0.0 {
							nestedBlockedServicesScheduleThu["start"] = bodyBlockedServicesScheduleThuStart
						}
						if len(nestedBlockedServicesScheduleThu) > 0 {
							nestedBlockedServicesSchedule["thu"] = nestedBlockedServicesScheduleThu
						}
					}
					if bodyBlockedServicesScheduleTimeZone != "" {
						nestedBlockedServicesSchedule["time_zone"] = bodyBlockedServicesScheduleTimeZone
					}
					{
						nestedBlockedServicesScheduleTue := map[string]any{}
						if bodyBlockedServicesScheduleTueEnd != 0.0 {
							nestedBlockedServicesScheduleTue["end"] = bodyBlockedServicesScheduleTueEnd
						}
						if bodyBlockedServicesScheduleTueStart != 0.0 {
							nestedBlockedServicesScheduleTue["start"] = bodyBlockedServicesScheduleTueStart
						}
						if len(nestedBlockedServicesScheduleTue) > 0 {
							nestedBlockedServicesSchedule["tue"] = nestedBlockedServicesScheduleTue
						}
					}
					{
						nestedBlockedServicesScheduleWed := map[string]any{}
						if bodyBlockedServicesScheduleWedEnd != 0.0 {
							nestedBlockedServicesScheduleWed["end"] = bodyBlockedServicesScheduleWedEnd
						}
						if bodyBlockedServicesScheduleWedStart != 0.0 {
							nestedBlockedServicesScheduleWed["start"] = bodyBlockedServicesScheduleWedStart
						}
						if len(nestedBlockedServicesScheduleWed) > 0 {
							nestedBlockedServicesSchedule["wed"] = nestedBlockedServicesScheduleWed
						}
					}
					if len(nestedBlockedServicesSchedule) > 0 {
						body["blocked_services_schedule"] = nestedBlockedServicesSchedule
					}
				}
				if cmd.Flags().Changed("filtering-enabled") {
					body["filtering_enabled"] = bodyFilteringEnabled
				}
				if bodyIds != "" {
					var parsedIds any
					if err := json.Unmarshal([]byte(bodyIds), &parsedIds); err != nil {
						return fmt.Errorf("parsing --ids JSON: %w", err)
					}
					body["ids"] = parsedIds
				}
				if cmd.Flags().Changed("ignore-querylog") {
					body["ignore_querylog"] = bodyIgnoreQuerylog
				}
				if cmd.Flags().Changed("ignore-statistics") {
					body["ignore_statistics"] = bodyIgnoreStatistics
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if cmd.Flags().Changed("parental-enabled") {
					body["parental_enabled"] = bodyParentalEnabled
				}
				{
					nestedSafeSearch := map[string]any{}
					if cmd.Flags().Changed("safe-search-bing") {
						nestedSafeSearch["bing"] = bodySafeSearchBing
					}
					if cmd.Flags().Changed("safe-search-duckduckgo") {
						nestedSafeSearch["duckduckgo"] = bodySafeSearchDuckduckgo
					}
					if cmd.Flags().Changed("safe-search-ecosia") {
						nestedSafeSearch["ecosia"] = bodySafeSearchEcosia
					}
					if cmd.Flags().Changed("safe-search-enabled") {
						nestedSafeSearch["enabled"] = bodySafeSearchEnabled
					}
					if cmd.Flags().Changed("safe-search-google") {
						nestedSafeSearch["google"] = bodySafeSearchGoogle
					}
					if cmd.Flags().Changed("safe-search-pixabay") {
						nestedSafeSearch["pixabay"] = bodySafeSearchPixabay
					}
					if cmd.Flags().Changed("safe-search-yandex") {
						nestedSafeSearch["yandex"] = bodySafeSearchYandex
					}
					if cmd.Flags().Changed("safe-search-youtube") {
						nestedSafeSearch["youtube"] = bodySafeSearchYoutube
					}
					if len(nestedSafeSearch) > 0 {
						body["safe_search"] = nestedSafeSearch
					}
				}
				if cmd.Flags().Changed("safebrowsing-enabled") {
					body["safebrowsing_enabled"] = bodySafebrowsingEnabled
				}
				if cmd.Flags().Changed("safesearch-enabled") {
					body["safesearch_enabled"] = bodySafesearchEnabled
				}
				if bodyTags != "" {
					var parsedTags any
					if err := json.Unmarshal([]byte(bodyTags), &parsedTags); err != nil {
						return fmt.Errorf("parsing --tags JSON: %w", err)
					}
					body["tags"] = parsedTags
				}
				if bodyUpstreams != "" {
					var parsedUpstreams any
					if err := json.Unmarshal([]byte(bodyUpstreams), &parsedUpstreams); err != nil {
						return fmt.Errorf("parsing --upstreams JSON: %w", err)
					}
					body["upstreams"] = parsedUpstreams
				}
				if cmd.Flags().Changed("upstreams-cache-enabled") {
					body["upstreams_cache_enabled"] = bodyUpstreamsCacheEnabled
				}
				if bodyUpstreamsCacheSize != 0 {
					body["upstreams_cache_size"] = bodyUpstreamsCacheSize
				}
				if cmd.Flags().Changed("use-global-blocked-services") {
					body["use_global_blocked_services"] = bodyUseGlobalBlockedServices
				}
				if cmd.Flags().Changed("use-global-settings") {
					body["use_global_settings"] = bodyUseGlobalSettings
				}
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "clients",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyBlockedServices, "blocked-services", "", "Blocked services")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleFriEnd, "blocked-services-schedule-fri-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleFriStart, "blocked-services-schedule-fri-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleMonEnd, "blocked-services-schedule-mon-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleMonStart, "blocked-services-schedule-mon-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleSatEnd, "blocked-services-schedule-sat-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleSatStart, "blocked-services-schedule-sat-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleSunEnd, "blocked-services-schedule-sun-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleSunStart, "blocked-services-schedule-sun-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleThuEnd, "blocked-services-schedule-thu-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleThuStart, "blocked-services-schedule-thu-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().StringVar(&bodyBlockedServicesScheduleTimeZone, "blocked-services-schedule-time-zone", "", "Time zone name according to IANA time zone database. For example `Europe/Brussels`. `Local` represents the system's...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleTueEnd, "blocked-services-schedule-tue-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleTueStart, "blocked-services-schedule-tue-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleWedEnd, "blocked-services-schedule-wed-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyBlockedServicesScheduleWedStart, "blocked-services-schedule-wed-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().BoolVar(&bodyFilteringEnabled, "filtering-enabled", false, "Filtering enabled")
	cmd.Flags().StringVar(&bodyIds, "ids", "", "IP, CIDR, MAC, or ClientID.")
	cmd.Flags().BoolVar(&bodyIgnoreQuerylog, "ignore-querylog", false, "NOTE: If `ignore_querylog` is not set in HTTP API `GET /clients/add` request then default value (false) will be...")
	cmd.Flags().BoolVar(&bodyIgnoreStatistics, "ignore-statistics", false, "NOTE: If `ignore_statistics` is not set in HTTP API `GET /clients/add` request then default value (false) will be...")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().BoolVar(&bodyParentalEnabled, "parental-enabled", false, "Parental enabled")
	cmd.Flags().BoolVar(&bodySafeSearchBing, "safe-search-bing", false, "Bing")
	cmd.Flags().BoolVar(&bodySafeSearchDuckduckgo, "safe-search-duckduckgo", false, "Duckduckgo")
	cmd.Flags().BoolVar(&bodySafeSearchEcosia, "safe-search-ecosia", false, "Ecosia")
	cmd.Flags().BoolVar(&bodySafeSearchEnabled, "safe-search-enabled", false, "Enabled")
	cmd.Flags().BoolVar(&bodySafeSearchGoogle, "safe-search-google", false, "Google")
	cmd.Flags().BoolVar(&bodySafeSearchPixabay, "safe-search-pixabay", false, "Pixabay")
	cmd.Flags().BoolVar(&bodySafeSearchYandex, "safe-search-yandex", false, "Yandex")
	cmd.Flags().BoolVar(&bodySafeSearchYoutube, "safe-search-youtube", false, "Youtube")
	cmd.Flags().BoolVar(&bodySafebrowsingEnabled, "safebrowsing-enabled", false, "Safebrowsing enabled")
	cmd.Flags().BoolVar(&bodySafesearchEnabled, "safesearch-enabled", false, "Safesearch enabled")
	cmd.Flags().StringVar(&bodyTags, "tags", "", "Tags")
	cmd.Flags().StringVar(&bodyUpstreams, "upstreams", "", "Upstreams")
	cmd.Flags().BoolVar(&bodyUpstreamsCacheEnabled, "upstreams-cache-enabled", false, "NOTE: If `upstreams_cache_enabled` is not set in HTTP API `GET /clients/add` request then default value (false) will...")
	cmd.Flags().IntVar(&bodyUpstreamsCacheSize, "upstreams-cache-size", 0, "NOTE: If `upstreams_cache_enabled` is not set in HTTP API `GET /clients/update` request then the existing value will...")
	cmd.Flags().BoolVar(&bodyUseGlobalBlockedServices, "use-global-blocked-services", false, "Use global blocked services")
	cmd.Flags().BoolVar(&bodyUseGlobalSettings, "use-global-settings", false, "Use global settings")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
