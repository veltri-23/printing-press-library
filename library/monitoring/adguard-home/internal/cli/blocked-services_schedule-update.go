// Copyright 2026 Eric Jung and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newBlockedServicesScheduleUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyIds string
	var bodyScheduleFriEnd float64
	var bodyScheduleFriStart float64
	var bodyScheduleMonEnd float64
	var bodyScheduleMonStart float64
	var bodyScheduleSatEnd float64
	var bodyScheduleSatStart float64
	var bodyScheduleSunEnd float64
	var bodyScheduleSunStart float64
	var bodyScheduleThuEnd float64
	var bodyScheduleThuStart float64
	var bodyScheduleTimeZone string
	var bodyScheduleTueEnd float64
	var bodyScheduleTueStart float64
	var bodyScheduleWedEnd float64
	var bodyScheduleWedStart float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "schedule-update",
		Aliases:     []string{"update"},
		Short:       "Update blocked services",
		Example:     "  adguard-home-pp-cli blocked-services schedule-update",
		Annotations: map[string]string{"pp:endpoint": "blocked-services.schedule-update", "pp:method": "PUT", "pp:path": "/blocked_services/update"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/blocked_services/update"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyIds != "" {
					var parsedIds any
					if err := json.Unmarshal([]byte(bodyIds), &parsedIds); err != nil {
						return fmt.Errorf("parsing --ids JSON: %w", err)
					}
					body["ids"] = parsedIds
				}
				{
					nestedSchedule := map[string]any{}
					{
						nestedScheduleFri := map[string]any{}
						if bodyScheduleFriEnd != 0.0 {
							nestedScheduleFri["end"] = bodyScheduleFriEnd
						}
						if bodyScheduleFriStart != 0.0 {
							nestedScheduleFri["start"] = bodyScheduleFriStart
						}
						if len(nestedScheduleFri) > 0 {
							nestedSchedule["fri"] = nestedScheduleFri
						}
					}
					{
						nestedScheduleMon := map[string]any{}
						if bodyScheduleMonEnd != 0.0 {
							nestedScheduleMon["end"] = bodyScheduleMonEnd
						}
						if bodyScheduleMonStart != 0.0 {
							nestedScheduleMon["start"] = bodyScheduleMonStart
						}
						if len(nestedScheduleMon) > 0 {
							nestedSchedule["mon"] = nestedScheduleMon
						}
					}
					{
						nestedScheduleSat := map[string]any{}
						if bodyScheduleSatEnd != 0.0 {
							nestedScheduleSat["end"] = bodyScheduleSatEnd
						}
						if bodyScheduleSatStart != 0.0 {
							nestedScheduleSat["start"] = bodyScheduleSatStart
						}
						if len(nestedScheduleSat) > 0 {
							nestedSchedule["sat"] = nestedScheduleSat
						}
					}
					{
						nestedScheduleSun := map[string]any{}
						if bodyScheduleSunEnd != 0.0 {
							nestedScheduleSun["end"] = bodyScheduleSunEnd
						}
						if bodyScheduleSunStart != 0.0 {
							nestedScheduleSun["start"] = bodyScheduleSunStart
						}
						if len(nestedScheduleSun) > 0 {
							nestedSchedule["sun"] = nestedScheduleSun
						}
					}
					{
						nestedScheduleThu := map[string]any{}
						if bodyScheduleThuEnd != 0.0 {
							nestedScheduleThu["end"] = bodyScheduleThuEnd
						}
						if bodyScheduleThuStart != 0.0 {
							nestedScheduleThu["start"] = bodyScheduleThuStart
						}
						if len(nestedScheduleThu) > 0 {
							nestedSchedule["thu"] = nestedScheduleThu
						}
					}
					if bodyScheduleTimeZone != "" {
						nestedSchedule["time_zone"] = bodyScheduleTimeZone
					}
					{
						nestedScheduleTue := map[string]any{}
						if bodyScheduleTueEnd != 0.0 {
							nestedScheduleTue["end"] = bodyScheduleTueEnd
						}
						if bodyScheduleTueStart != 0.0 {
							nestedScheduleTue["start"] = bodyScheduleTueStart
						}
						if len(nestedScheduleTue) > 0 {
							nestedSchedule["tue"] = nestedScheduleTue
						}
					}
					{
						nestedScheduleWed := map[string]any{}
						if bodyScheduleWedEnd != 0.0 {
							nestedScheduleWed["end"] = bodyScheduleWedEnd
						}
						if bodyScheduleWedStart != 0.0 {
							nestedScheduleWed["start"] = bodyScheduleWedStart
						}
						if len(nestedScheduleWed) > 0 {
							nestedSchedule["wed"] = nestedScheduleWed
						}
					}
					if len(nestedSchedule) > 0 {
						body["schedule"] = nestedSchedule
					}
				}
			}
			data, statusCode, err := c.PutWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "blocked-services",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyIds, "ids", "", "The names of the blocked services.")
	cmd.Flags().Float64Var(&bodyScheduleFriEnd, "schedule-fri-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyScheduleFriStart, "schedule-fri-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().Float64Var(&bodyScheduleMonEnd, "schedule-mon-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyScheduleMonStart, "schedule-mon-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().Float64Var(&bodyScheduleSatEnd, "schedule-sat-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyScheduleSatStart, "schedule-sat-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().Float64Var(&bodyScheduleSunEnd, "schedule-sun-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyScheduleSunStart, "schedule-sun-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().Float64Var(&bodyScheduleThuEnd, "schedule-thu-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyScheduleThuStart, "schedule-thu-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().StringVar(&bodyScheduleTimeZone, "schedule-time-zone", "", "Time zone name according to IANA time zone database. For example `Europe/Brussels`. `Local` represents the system's...")
	cmd.Flags().Float64Var(&bodyScheduleTueEnd, "schedule-tue-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyScheduleTueStart, "schedule-tue-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().Float64Var(&bodyScheduleWedEnd, "schedule-wed-end", 0.0, "The number of milliseconds elapsed from the start of a day. It is expected to be rounded to minutes. The maximum...")
	cmd.Flags().Float64Var(&bodyScheduleWedStart, "schedule-wed-start", 0.0, "The number of milliseconds elapsed from the start of a day. It must be less than `end` and is expected to be rounded...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
