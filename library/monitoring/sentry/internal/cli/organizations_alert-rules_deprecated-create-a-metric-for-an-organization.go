// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsAlertRulesDeprecatedCreateAMetricForAnOrganizationCmd(flags *rootFlags) *cobra.Command {
	var bodyAggregate string
	var bodyComparisonDelta int
	var bodyDataset string
	var bodyEnvironment string
	var bodyEventTypes string
	var bodyName string
	var bodyOwner string
	var bodyProjects string
	var bodyQuery string
	var bodyQueryType int
	var bodyResolveThreshold float64
	var bodyThresholdType int
	var bodyTimeWindow int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "deprecated-create-a-metric-for-an-organization <organization_id_or_slug>",
		Aliases:     []string{"create"},
		Short:       "## Deprecated 🚧 Use [Create a Monitor for a Project](/api/monitors/create-a-monitor-for-a-project) and [Create an...",
		Example:     "  sentry-pp-cli organizations alert-rules deprecated-create-a-metric-for-an-organization example-value --aggregate example-value",
		Annotations: map[string]string{"pp:endpoint": "alert-rules.deprecated-create-a-metric-for-an-organization"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("aggregate") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "aggregate")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("projects") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "projects")
				}
				if !cmd.Flags().Changed("query") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "query")
				}
				if !cmd.Flags().Changed("threshold-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "threshold-type")
				}
				if !cmd.Flags().Changed("time-window") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "time-window")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/0/organizations/{organization_id_or_slug}/alert-rules/"
			path = replacePathParam(path, "organization_id_or_slug", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAggregate != "" {
					body["aggregate"] = bodyAggregate
				}
				if bodyComparisonDelta != 0 {
					body["comparisonDelta"] = bodyComparisonDelta
				}
				if bodyDataset != "" {
					body["dataset"] = bodyDataset
				}
				if bodyEnvironment != "" {
					body["environment"] = bodyEnvironment
				}
				if bodyEventTypes != "" {
					var parsedEventTypes any
					if err := json.Unmarshal([]byte(bodyEventTypes), &parsedEventTypes); err != nil {
						return fmt.Errorf("parsing --event-types JSON: %w", err)
					}
					body["eventTypes"] = parsedEventTypes
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyOwner != "" {
					body["owner"] = bodyOwner
				}
				if bodyProjects != "" {
					var parsedProjects any
					if err := json.Unmarshal([]byte(bodyProjects), &parsedProjects); err != nil {
						return fmt.Errorf("parsing --projects JSON: %w", err)
					}
					body["projects"] = parsedProjects
				}
				if bodyQuery != "" {
					body["query"] = bodyQuery
				}
				if bodyQueryType != 0 {
					body["queryType"] = bodyQueryType
				}
				if bodyResolveThreshold != 0.0 {
					body["resolveThreshold"] = bodyResolveThreshold
				}
				if bodyThresholdType != 0 {
					body["thresholdType"] = bodyThresholdType
				}
				if bodyTimeWindow != 0 {
					body["timeWindow"] = bodyTimeWindow
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "alert-rules",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAggregate, "aggregate", "", "A string representing the aggregate function used in this alert rule. Valid aggregate functions are `count`,...")
	cmd.Flags().IntVar(&bodyComparisonDelta, "comparison-delta", 0, "An optional int representing the time delta to use as the comparison period, in minutes. Required when using a...")
	cmd.Flags().StringVar(&bodyDataset, "dataset", "", "The name of the dataset that this query will be executed on. Valid values are `events`, `transactions`, `metrics`,...")
	cmd.Flags().StringVar(&bodyEnvironment, "environment", "", "The name of the environment to filter by. Defaults to all environments.")
	cmd.Flags().StringVar(&bodyEventTypes, "event-types", "", "List of event types that this alert will be related to. Valid values are `default` (events captured using [Capture...")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name for the rule, which has a maximimum length of 256 characters.")
	cmd.Flags().StringVar(&bodyOwner, "owner", "", "The ID of the team or user that owns the rule.")
	cmd.Flags().StringVar(&bodyProjects, "projects", "", "Metric alerts are currently limited to one project. The array should contain a single slug, representing the project...")
	cmd.Flags().StringVar(&bodyQuery, "query", "", "An event search query to subscribe to and monitor for alerts. For example, to filter transactions so that only those...")
	cmd.Flags().IntVar(&bodyQueryType, "query-type", 0, "The type of query. If no value is provided, `queryType` is set to the default for the specified `dataset.` See...")
	cmd.Flags().Float64Var(&bodyResolveThreshold, "resolve-threshold", 0.0, "Optional value that the metric needs to reach to resolve the alert. If no value is provided, this is set...")
	cmd.Flags().IntVar(&bodyThresholdType, "threshold-type", 0, "The comparison operator for the critical and warning thresholds. The comparison operator for the resolved threshold...")
	cmd.Flags().IntVar(&bodyTimeWindow, "time-window", 0, "The time period to aggregate over. * `1` - 1 minute * `5` - 5 minutes * `10` - 10 minutes * `15` - 15 minutes * `30`...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
