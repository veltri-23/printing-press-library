// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsEventsTimeseriesQueryExploreEventsInTimeseriesFormatCmd(flags *rootFlags) *cobra.Command {
	var flagDataset string
	var flagEnd string
	var flagEnvironment string
	var flagProject string
	var flagStart string
	var flagStatsPeriod string
	var flagTopEvents int
	var flagComparisonDelta int
	var flagInterval int
	var flagSort string
	var flagGroupBy string
	var flagYAxis string
	var flagQuery string
	var flagDisableAggregateExtrapolation string
	var flagPreventMetricAggregates string
	var flagExcludeOther string

	cmd := &cobra.Command{
		Use:         "query-explore-events-in-timeseries-format <organization_id_or_slug>",
		Aliases:     []string{"get"},
		Short:       "Retrieves explore data for a given organization as a timeseries. This endpoint can return timeseries for either 1 or...",
		Example:     "  sentry-pp-cli organizations events-timeseries query-explore-events-in-timeseries-format example-value",
		Annotations: map[string]string{"pp:endpoint": "events-timeseries.query-explore-events-in-timeseries-format", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("dataset") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "dataset")
			}
			if cmd.Flags().Changed("dataset") {
				allowedDataset := []string{"logs", "profile_functions", "spans", "uptime_results"}
				validDataset := false
				for _, v := range allowedDataset {
					if flagDataset == v {
						validDataset = true
						break
					}
				}
				if !validDataset {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "dataset", flagDataset, allowedDataset)
				}
			}
			if cmd.Flags().Changed("disable-aggregate-extrapolation") {
				allowedDisableAggregateExtrapolation := []string{"0", "1"}
				validDisableAggregateExtrapolation := false
				for _, v := range allowedDisableAggregateExtrapolation {
					if flagDisableAggregateExtrapolation == v {
						validDisableAggregateExtrapolation = true
						break
					}
				}
				if !validDisableAggregateExtrapolation {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "disable-aggregate-extrapolation", flagDisableAggregateExtrapolation, allowedDisableAggregateExtrapolation)
				}
			}
			if cmd.Flags().Changed("prevent-metric-aggregates") {
				allowedPreventMetricAggregates := []string{"0", "1"}
				validPreventMetricAggregates := false
				for _, v := range allowedPreventMetricAggregates {
					if flagPreventMetricAggregates == v {
						validPreventMetricAggregates = true
						break
					}
				}
				if !validPreventMetricAggregates {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "prevent-metric-aggregates", flagPreventMetricAggregates, allowedPreventMetricAggregates)
				}
			}
			if cmd.Flags().Changed("exclude-other") {
				allowedExcludeOther := []string{"0", "1"}
				validExcludeOther := false
				for _, v := range allowedExcludeOther {
					if flagExcludeOther == v {
						validExcludeOther = true
						break
					}
				}
				if !validExcludeOther {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "exclude-other", flagExcludeOther, allowedExcludeOther)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/0/organizations/{organization_id_or_slug}/events-timeseries/"
			path = replacePathParam(path, "organization_id_or_slug", args[0])
			params := map[string]string{}
			if flagDataset != "" {
				params["dataset"] = fmt.Sprintf("%v", flagDataset)
			}
			if flagEnd != "" {
				params["end"] = fmt.Sprintf("%v", flagEnd)
			}
			if flagEnvironment != "" {
				params["environment"] = fmt.Sprintf("%v", flagEnvironment)
			}
			if flagProject != "" {
				params["project"] = fmt.Sprintf("%v", flagProject)
			}
			if flagStart != "" {
				params["start"] = fmt.Sprintf("%v", flagStart)
			}
			if flagStatsPeriod != "" {
				params["statsPeriod"] = fmt.Sprintf("%v", flagStatsPeriod)
			}
			if flagTopEvents != 0 {
				params["topEvents"] = fmt.Sprintf("%v", flagTopEvents)
			}
			if flagComparisonDelta != 0 {
				params["comparisonDelta"] = fmt.Sprintf("%v", flagComparisonDelta)
			}
			if flagInterval != 0 {
				params["interval"] = fmt.Sprintf("%v", flagInterval)
			}
			if flagSort != "" {
				params["sort"] = fmt.Sprintf("%v", flagSort)
			}
			if flagGroupBy != "" {
				params["groupBy"] = fmt.Sprintf("%v", flagGroupBy)
			}
			if flagYAxis != "" {
				params["yAxis"] = fmt.Sprintf("%v", flagYAxis)
			}
			if flagQuery != "" {
				params["query"] = fmt.Sprintf("%v", flagQuery)
			}
			if flagDisableAggregateExtrapolation != "" {
				params["disableAggregateExtrapolation"] = fmt.Sprintf("%v", flagDisableAggregateExtrapolation)
			}
			if flagPreventMetricAggregates != "" {
				params["preventMetricAggregates"] = fmt.Sprintf("%v", flagPreventMetricAggregates)
			}
			if flagExcludeOther != "" {
				params["excludeOther"] = fmt.Sprintf("%v", flagExcludeOther)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "events-timeseries", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDataset, "dataset", "", "Which dataset to query, changing datasets changes the available fields that can be queried (one of: logs, profile_functions, spans, uptime_results)")
	cmd.Flags().StringVar(&flagEnd, "end", "", "The end of the period of time for the query, expected in ISO-8601 format. For example, `2001-12-14T12:34:56.7890`.")
	cmd.Flags().StringVar(&flagEnvironment, "environment", "", "The name of environments to filter by.")
	cmd.Flags().StringVar(&flagProject, "project", "", "The IDs of projects to filter by. `-1` means all available projects. For example, the following are valid...")
	cmd.Flags().StringVar(&flagStart, "start", "", "The start of the period of time for the query, expected in ISO-8601 format. For example, `2001-12-14T12:34:56.7890`.")
	cmd.Flags().StringVar(&flagStatsPeriod, "stats-period", "", "The period of time for the query, will override the start & end parameters, a number followed by one of: - `d` for...")
	cmd.Flags().IntVar(&flagTopEvents, "top-events", 0, "The number of top event results to return, must be between 1 and 10. When TopEvents is passed, both sort and groupBy...")
	cmd.Flags().IntVar(&flagComparisonDelta, "comparison-delta", 0, "The delta in seconds to return additional offset timeseries by")
	cmd.Flags().IntVar(&flagInterval, "interval", 0, "The size of the bucket for the timeseries to have, must be a value smaller than the window being queried. If the...")
	cmd.Flags().StringVar(&flagSort, "sort", "", "What to order the results of the query by. Must be something in the `field` list, excluding equations.")
	cmd.Flags().StringVar(&flagGroupBy, "group-by", "", "List of fields to group by, *Required* for topEvents queries as this and sort determine what the top events are")
	cmd.Flags().StringVar(&flagYAxis, "y-axis", "", "The aggregate field to create the timeseries for, defaults to `count()` when not included")
	cmd.Flags().StringVar(&flagQuery, "query", "", "Filters results by using [query syntax](/product/sentry-basics/search/). Example: `query=(transaction:foo AND...")
	cmd.Flags().StringVar(&flagDisableAggregateExtrapolation, "disable-aggregate-extrapolation", "", "Whether to disable the use of extrapolation and return the sampled values, due to sampling the number returned may... (one of: 0, 1)")
	cmd.Flags().StringVar(&flagPreventMetricAggregates, "prevent-metric-aggregates", "", "Whether to throw an error when aggregates are passed in the query or groupBy (one of: 0, 1)")
	cmd.Flags().StringVar(&flagExcludeOther, "exclude-other", "", "Only applicable with TopEvents, whether to include the 'other' timeseries which represents all the events that... (one of: 0, 1)")

	return cmd
}
