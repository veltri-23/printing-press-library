// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsUpdateAnCmd(flags *rootFlags) *cobra.Command {
	var bodyAlertsMemberWrite bool
	var bodyAllowJoinRequests bool
	var bodyAllowSharedIssues bool
	var bodyAttachmentsRole string
	var bodyAvatar string
	var bodyAvatarType string
	var bodyCancelDeletion bool
	var bodyCodecovAccess bool
	var bodyDataScrubber bool
	var bodyDataScrubberDefaults bool
	var bodyDebugFilesRole string
	var bodyDefaultRole string
	var bodyEnhancedPrivacy bool
	var bodyEventsMemberAdmin bool
	var bodyHasGranularReplayPermissions bool
	var bodyHideAiFeatures bool
	var bodyIsEarlyAdopter bool
	var bodyIssueAlertsThreadFlag bool
	var bodyMetricAlertsThreadFlag bool
	var bodyName string
	var bodyOpenMembership bool
	var bodyRelayPiiConfig string
	var bodyRequire2FA bool
	var bodySafeFields string
	var bodyScrapeJavaScript bool
	var bodyScrubIPAddresses bool
	var bodySensitiveFields string
	var bodySlug string
	var bodyStoreCrashReports int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update-an <organization_id_or_slug>",
		Aliases:     []string{"update"},
		Short:       "Update various attributes and configurable settings for the given organization.",
		Example:     "  sentry-pp-cli organizations update-an example-value",
		Annotations: map[string]string{"pp:endpoint": "organizations.update-an"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/0/organizations/{organization_id_or_slug}/"
			path = replacePathParam(path, "organization_id_or_slug", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAlertsMemberWrite != false {
					body["alertsMemberWrite"] = bodyAlertsMemberWrite
				}
				if bodyAllowJoinRequests != false {
					body["allowJoinRequests"] = bodyAllowJoinRequests
				}
				if bodyAllowSharedIssues != false {
					body["allowSharedIssues"] = bodyAllowSharedIssues
				}
				if bodyAttachmentsRole != "" {
					body["attachmentsRole"] = bodyAttachmentsRole
				}
				if bodyAvatar != "" {
					body["avatar"] = bodyAvatar
				}
				if bodyAvatarType != "" {
					body["avatarType"] = bodyAvatarType
				}
				if bodyCancelDeletion != false {
					body["cancelDeletion"] = bodyCancelDeletion
				}
				if bodyCodecovAccess != false {
					body["codecovAccess"] = bodyCodecovAccess
				}
				if bodyDataScrubber != false {
					body["dataScrubber"] = bodyDataScrubber
				}
				if bodyDataScrubberDefaults != false {
					body["dataScrubberDefaults"] = bodyDataScrubberDefaults
				}
				if bodyDebugFilesRole != "" {
					body["debugFilesRole"] = bodyDebugFilesRole
				}
				if bodyDefaultRole != "" {
					body["defaultRole"] = bodyDefaultRole
				}
				if bodyEnhancedPrivacy != false {
					body["enhancedPrivacy"] = bodyEnhancedPrivacy
				}
				if bodyEventsMemberAdmin != false {
					body["eventsMemberAdmin"] = bodyEventsMemberAdmin
				}
				if bodyHasGranularReplayPermissions != false {
					body["hasGranularReplayPermissions"] = bodyHasGranularReplayPermissions
				}
				if bodyHideAiFeatures != false {
					body["hideAiFeatures"] = bodyHideAiFeatures
				}
				if bodyIsEarlyAdopter != false {
					body["isEarlyAdopter"] = bodyIsEarlyAdopter
				}
				if bodyIssueAlertsThreadFlag != false {
					body["issueAlertsThreadFlag"] = bodyIssueAlertsThreadFlag
				}
				if bodyMetricAlertsThreadFlag != false {
					body["metricAlertsThreadFlag"] = bodyMetricAlertsThreadFlag
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyOpenMembership != false {
					body["openMembership"] = bodyOpenMembership
				}
				if bodyRelayPiiConfig != "" {
					body["relayPiiConfig"] = bodyRelayPiiConfig
				}
				if bodyRequire2FA != false {
					body["require2FA"] = bodyRequire2FA
				}
				if bodySafeFields != "" {
					var parsedSafeFields any
					if err := json.Unmarshal([]byte(bodySafeFields), &parsedSafeFields); err != nil {
						return fmt.Errorf("parsing --safe-fields JSON: %w", err)
					}
					body["safeFields"] = parsedSafeFields
				}
				if bodyScrapeJavaScript != false {
					body["scrapeJavaScript"] = bodyScrapeJavaScript
				}
				if bodyScrubIPAddresses != false {
					body["scrubIPAddresses"] = bodyScrubIPAddresses
				}
				if bodySensitiveFields != "" {
					var parsedSensitiveFields any
					if err := json.Unmarshal([]byte(bodySensitiveFields), &parsedSensitiveFields); err != nil {
						return fmt.Errorf("parsing --sensitive-fields JSON: %w", err)
					}
					body["sensitiveFields"] = parsedSensitiveFields
				}
				if bodySlug != "" {
					body["slug"] = bodySlug
				}
				if bodyStoreCrashReports != 0 {
					body["storeCrashReports"] = bodyStoreCrashReports
				}
			}
			data, statusCode, err := c.Put(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "organizations",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAlertsMemberWrite, "alerts-member-write", false, "Specify `true` to allow members to create, edit, and delete alert rules by granting them the `alerts:write` scope.")
	cmd.Flags().BoolVar(&bodyAllowJoinRequests, "allow-join-requests", false, "Specify `true` to allow users to request to join your organization.")
	cmd.Flags().BoolVar(&bodyAllowSharedIssues, "allow-shared-issues", false, "Specify `true` to allow sharing of limited details on issues to anonymous users.")
	cmd.Flags().StringVar(&bodyAttachmentsRole, "attachments-role", "", "The role required to download event attachments, such as native crash reports or log files. * `member` - Member *...")
	cmd.Flags().StringVar(&bodyAvatar, "avatar", "", "The image to upload as the organization avatar, in base64. Required if `avatarType` is `upload`.")
	cmd.Flags().StringVar(&bodyAvatarType, "avatar-type", "", "The type of display picture for the organization. * `letter_avatar` - Use initials * `upload` - Upload an image")
	cmd.Flags().BoolVar(&bodyCancelDeletion, "cancel-deletion", false, "Specify `true` to restore an organization that is pending deletion.")
	cmd.Flags().BoolVar(&bodyCodecovAccess, "codecov-access", false, "Specify `true` to enable Code Coverage Insights. This feature is only available for organizations on the Team plan...")
	cmd.Flags().BoolVar(&bodyDataScrubber, "data-scrubber", false, "Specify `true` to require server-side data scrubbing for all projects.")
	cmd.Flags().BoolVar(&bodyDataScrubberDefaults, "data-scrubber-defaults", false, "Specify `true` to apply the default scrubbers to prevent things like passwords and credit cards from being stored...")
	cmd.Flags().StringVar(&bodyDebugFilesRole, "debug-files-role", "", "The role required to download debug information files, ProGuard mappings and source maps. * `member` - Member *...")
	cmd.Flags().StringVar(&bodyDefaultRole, "default-role", "", "The default role new members will receive. * `member` - Member * `admin` - Admin * `manager` - Manager * `owner` - Owner")
	cmd.Flags().BoolVar(&bodyEnhancedPrivacy, "enhanced-privacy", false, "Specify `true` to enable enhanced privacy controls to limit personally identifiable information (PII) as well as...")
	cmd.Flags().BoolVar(&bodyEventsMemberAdmin, "events-member-admin", false, "Specify `true` to allow members to delete events (including the delete & discard action) by granting them the...")
	cmd.Flags().BoolVar(&bodyHasGranularReplayPermissions, "has-granular-replay-permissions", false, "Specify `true` to enable granular replay permissions, allowing per-member access control for replay data.")
	cmd.Flags().BoolVar(&bodyHideAiFeatures, "hide-ai-features", false, "Specify `true` to hide AI features from the organization.")
	cmd.Flags().BoolVar(&bodyIsEarlyAdopter, "is-early-adopter", false, "Specify `true` to opt-in to new features before they're released to the public.")
	cmd.Flags().BoolVar(&bodyIssueAlertsThreadFlag, "issue-alerts-thread-flag", false, "Specify `true` to allow the Sentry Slack integration to post replies in threads for an Issue Alert notification....")
	cmd.Flags().BoolVar(&bodyMetricAlertsThreadFlag, "metric-alerts-thread-flag", false, "Specify `true` to allow the Sentry Slack integration to post replies in threads for a Metric Alert notification....")
	cmd.Flags().StringVar(&bodyName, "name", "", "The new name for the organization.")
	cmd.Flags().BoolVar(&bodyOpenMembership, "open-membership", false, "Specify `true` to allow organization members to freely join any team.")
	cmd.Flags().StringVar(&bodyRelayPiiConfig, "relay-pii-config", "", "Advanced data scrubbing rules that can be configured for each project as a JSON string. The new rules will only...")
	cmd.Flags().BoolVar(&bodyRequire2FA, "require2-fa", false, "Specify `true` to require and enforce two-factor authentication for all members.")
	cmd.Flags().StringVar(&bodySafeFields, "safe-fields", "", "A list of global field names which data scrubbers should ignore.")
	cmd.Flags().BoolVar(&bodyScrapeJavaScript, "scrape-java-script", false, "Specify `true` to allow Sentry to scrape missing JavaScript source context when possible.")
	cmd.Flags().BoolVar(&bodyScrubIPAddresses, "scrub-ip-addresses", false, "Specify `true` to prevent IP addresses from being stored for new events on all projects.")
	cmd.Flags().StringVar(&bodySensitiveFields, "sensitive-fields", "", "A list of additional global field names to match against when scrubbing data for all projects.")
	cmd.Flags().StringVar(&bodySlug, "slug", "", "The new slug for the organization, which needs to be unique.")
	cmd.Flags().IntVar(&bodyStoreCrashReports, "store-crash-reports", 0, "How many native crash reports (such as Minidumps for improved processing and download in issue details) to store per...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
