// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsPreventRetrievesAPaginatedListOfRepositoryTokensForAGivenOwnerCmd(flags *rootFlags) *cobra.Command {
	var flagLimit int
	var flagNavigation string
	var flagCursor string
	var flagSortBy string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "retrieves-a-paginated-list-of-repository-tokens-for-a-given-owner <organization_id_or_slug> <owner>",
		Short:       "Retrieves a paginated list of repository tokens for a given owner.",
		Example:     "  sentry-pp-cli organizations prevent retrieves-a-paginated-list-of-repository-tokens-for-a-given-owner example-value example-value",
		Annotations: map[string]string{"pp:endpoint": "prevent.retrieves-a-paginated-list-of-repository-tokens-for-a-given-owner", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/0/organizations/{organization_id_or_slug}/prevent/owner/{owner}/repositories/tokens/"
			path = replacePathParam(path, "organization_id_or_slug", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("owner is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "owner"))
			}
			path = replacePathParam(path, "owner", args[1])
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "prevent", path, map[string]string{
				"limit":      fmt.Sprintf("%v", flagLimit),
				"navigation": fmt.Sprintf("%v", flagNavigation),
				"cursor":     fmt.Sprintf("%v", flagCursor),
				"sortBy":     fmt.Sprintf("%v", flagSortBy),
			}, nil, flagAll, "cursor", "", "")
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "The number of results to return. If not specified, defaults to 20.")
	cmd.Flags().StringVar(&flagNavigation, "navigation", "", "Whether to get the previous or next page from paginated results. Use `next` for forward pagination after the cursor...")
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "The cursor pointing to a specific position in the result set to start the query from. Results after the cursor will...")
	cmd.Flags().StringVar(&flagSortBy, "sort-by", "", "The property to sort results by. If not specified, the default is `COMMIT_DATE` in descending order. Use `-` for...")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
