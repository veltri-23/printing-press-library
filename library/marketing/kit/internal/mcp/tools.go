// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/marketing/kit/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/marketing/kit/internal/client"
	"github.com/mvanhorn/printing-press-library/library/marketing/kit/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/marketing/kit/internal/config"
	"github.com/mvanhorn/printing-press-library/library/marketing/kit/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/marketing/kit/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("account_list",
			mcplib.WithDescription("Get current account. Returns the AccountListResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/account", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("account_list-colors",
			mcplib.WithDescription("List colors. Returns the AccountListColorsResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/account/colors", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("account_list-creatorprofile",
			mcplib.WithDescription("Get Creator Profile. Returns the AccountListCreatorprofileResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/account/creator_profile", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("account_list-emailstats",
			mcplib.WithDescription("Get email stats. Returns the AccountListEmailstatsResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/account/email_stats", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("account_list-growthstats",
			mcplib.WithDescription("Get growth stats for a specific time period. Defaults to last 90 days.<br/><br/>NOTE: We return your stats in your sending time zone. This endpoint does not return timestamps in UTC. Optional: ending, starting. Returns the AccountListGrowthstatsResponse."),
			mcplib.WithString("ending", mcplib.Description("Get stats for time period ending on this date (format yyyy-mm-dd). Defaults to today.")),
			mcplib.WithString("starting", mcplib.Description("Get stats for time period beginning on this date (format yyyy-mm-dd). Defaults to 90 days ago.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/account/growth_stats", false, []mcpParamBinding{{PublicName: "ending", WireName: "ending", Location: "query"}, {PublicName: "starting", WireName: "starting", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("account_update",
			mcplib.WithDescription("Update colors. Required: colors. Returns the updated AccountUpdateResponse."),
			mcplib.WithString("colors", mcplib.Required(), mcplib.Description("An array of up to 10 color hex codes")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v4/account/colors", false, []mcpParamBinding{{PublicName: "colors", WireName: "colors", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("broadcasts_create",
			mcplib.WithDescription("Draft or schedule to send a broadcast to all or a subset of your subscribers.<br/><br/>To save a draft, set `send_at` to `null`.<br/><br/>To publish to the web, set `public` to `true`.<br/><br/>To schedule the broadcast for sending, provide a `send_at` timestamp. Scheduled broadcasts should contain a subject and your content, at a minimum.<br/><br/>We currently support targeting your subscribers based on segment or tag ids.<aside class='notice'>Starting point templates are not currently supported.</aside>. Required: content, description, preview_text, public, published_at, subject, subscriber_filter. Optional: email_address, email_template_id, send_at (plus 2 more). Returns the new BroadcastsCreateResponse."),
			mcplib.WithString("content", mcplib.Required(), mcplib.Description("The HTML content of the email.")),
			mcplib.WithString("description", mcplib.Required(), mcplib.Description("Description")),
			mcplib.WithString("email_address", mcplib.Description("The sending email address to use. Uses the account's sending email address if not provided.")),
			mcplib.WithNumber("email_template_id", mcplib.Description("Id of the email template to use. Uses the account's default template if not provided. 'Starting point' template is...")),
			mcplib.WithString("preview_text", mcplib.Required(), mcplib.Description("Preview text")),
			mcplib.WithBoolean("public", mcplib.Required(), mcplib.Description("`true` to publish this broadcast to the web. The broadcast will appear in a newsletter feed on your Creator Profile...")),
			mcplib.WithString("published_at", mcplib.Required(), mcplib.Description("The published timestamp to display in ISO8601 format. If no timezone is provided, UTC is assumed.")),
			mcplib.WithString("send_at", mcplib.Description("The scheduled send time for this broadcast in ISO8601 format. If no timezone is provided, UTC is assumed.")),
			mcplib.WithString("subject", mcplib.Required(), mcplib.Description("Subject")),
			mcplib.WithString("subscriber_filter", mcplib.Required(), mcplib.Description("Filters your subscribers. At this time, we only support using only one filter group type via the API (e.g. `all`,...")),
			mcplib.WithString("thumbnail_alt", mcplib.Description("Thumbnail alt")),
			mcplib.WithString("thumbnail_url", mcplib.Description("Thumbnail url")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/broadcasts", false, []mcpParamBinding{{PublicName: "content", WireName: "content", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "email_address", WireName: "email_address", Location: "body"}, {PublicName: "email_template_id", WireName: "email_template_id", Location: "body"}, {PublicName: "preview_text", WireName: "preview_text", Location: "body"}, {PublicName: "public", WireName: "public", Location: "body"}, {PublicName: "published_at", WireName: "published_at", Location: "body"}, {PublicName: "send_at", WireName: "send_at", Location: "body"}, {PublicName: "subject", WireName: "subject", Location: "body"}, {PublicName: "subscriber_filter", WireName: "subscriber_filter", Location: "body"}, {PublicName: "thumbnail_alt", WireName: "thumbnail_alt", Location: "body"}, {PublicName: "thumbnail_url", WireName: "thumbnail_url", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("broadcasts_delete",
			mcplib.WithDescription("Delete a broadcast. Required: id. Destructive."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v4/broadcasts/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("broadcasts_get",
			mcplib.WithDescription("Get a broadcast. Required: id. Returns the BroadcastsGetResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/broadcasts/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("broadcasts_list",
			mcplib.WithDescription("List broadcasts. Optional: after, before, include_total_count (plus 3 more). Returns the BroadcastsListResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithString("sent_after", mcplib.Description("Get broadcasts sent after this date (format yyyy-mm-dd).")),
			mcplib.WithString("sent_before", mcplib.Description("Get broadcasts sent before this date (format yyyy-mm-dd).")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/broadcasts", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "sent_after", WireName: "sent_after", Location: "query"}, {PublicName: "sent_before", WireName: "sent_before", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("broadcasts_list-stats",
			mcplib.WithDescription("Get stats for a list of broadcasts. Optional: after, before, include_total_count (plus 3 more). Returns the BroadcastsListStatsResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithString("sent_after", mcplib.Description("Sent after")),
			mcplib.WithString("sent_before", mcplib.Description("Sent before")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/broadcasts/stats", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "sent_after", WireName: "sent_after", Location: "query"}, {PublicName: "sent_before", WireName: "sent_before", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("broadcasts_update",
			mcplib.WithDescription("Update an existing broadcast. Continue to draft or schedule to send a broadcast to all or a subset of your subscribers.<br/><br/>To save a draft, set `public` to false.<br/><br/>To schedule the broadcast for sending, set `public` to true and provide `send_at`. Scheduled broadcasts should contain a subject and your content, at a minimum.<br/><br/>We currently support targeting your subscribers based on segment or tag ids. Required: id, content, description, email_address, email_template_id, preview_text, public, published_at, send_at, subject, subscriber_filter, thumbnail_alt, thumbnail_url. Returns the updated BroadcastsUpdateResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("content", mcplib.Required(), mcplib.Description("The HTML content of the email.")),
			mcplib.WithString("description", mcplib.Required(), mcplib.Description("Description")),
			mcplib.WithString("email_address", mcplib.Required(), mcplib.Description("The sending email address to use. Uses the account's sending email address if not provided.")),
			mcplib.WithNumber("email_template_id", mcplib.Required(), mcplib.Description("Id of the email template to use. Uses the account's default template if not provided. 'Starting point' template is...")),
			mcplib.WithString("preview_text", mcplib.Required(), mcplib.Description("Preview text")),
			mcplib.WithBoolean("public", mcplib.Required(), mcplib.Description("`true` to publish this broadcast to the web. The broadcast will appear in a newsletter feed on your Creator Profile...")),
			mcplib.WithString("published_at", mcplib.Required(), mcplib.Description("The published timestamp to display in ISO8601 format. If no timezone is provided, UTC is assumed.")),
			mcplib.WithString("send_at", mcplib.Required(), mcplib.Description("The scheduled send time for this broadcast in ISO8601 format. If no timezone is provided, UTC is assumed.")),
			mcplib.WithString("subject", mcplib.Required(), mcplib.Description("Subject")),
			mcplib.WithString("subscriber_filter", mcplib.Required(), mcplib.Description("Filters your subscribers. At this time, we only support using only one filter group type via the API (e.g. `all`,...")),
			mcplib.WithString("thumbnail_alt", mcplib.Required(), mcplib.Description("Thumbnail alt")),
			mcplib.WithString("thumbnail_url", mcplib.Required(), mcplib.Description("Thumbnail url")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v4/broadcasts/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "content", WireName: "content", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "email_address", WireName: "email_address", Location: "body"}, {PublicName: "email_template_id", WireName: "email_template_id", Location: "body"}, {PublicName: "preview_text", WireName: "preview_text", Location: "body"}, {PublicName: "public", WireName: "public", Location: "body"}, {PublicName: "published_at", WireName: "published_at", Location: "body"}, {PublicName: "send_at", WireName: "send_at", Location: "body"}, {PublicName: "subject", WireName: "subject", Location: "body"}, {PublicName: "subscriber_filter", WireName: "subscriber_filter", Location: "body"}, {PublicName: "thumbnail_alt", WireName: "thumbnail_alt", Location: "body"}, {PublicName: "thumbnail_url", WireName: "thumbnail_url", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("broadcasts_clicks_get",
			mcplib.WithDescription("NOTE: Pagination parameters control the list of clicks for the top level broadcast. Required: broadcast_id. Returns the ClicksGetResponse."),
			mcplib.WithNumber("broadcast_id", mcplib.Required(), mcplib.Description("Broadcast id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/broadcasts/{broadcast_id}/clicks", false, []mcpParamBinding{{PublicName: "broadcast_id", WireName: "broadcast_id", Location: "path"}}, []string{"broadcast_id"}),
	)
	s.AddTool(
		mcplib.NewTool("broadcasts_stats_get",
			mcplib.WithDescription("Get stats for a broadcast. Required: broadcast_id. Returns the StatsGetResponse."),
			mcplib.WithNumber("broadcast_id", mcplib.Required(), mcplib.Description("Broadcast id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/broadcasts/{broadcast_id}/stats", false, []mcpParamBinding{{PublicName: "broadcast_id", WireName: "broadcast_id", Location: "path"}}, []string{"broadcast_id"}),
	)
	s.AddTool(
		mcplib.NewTool("bulk_create",
			mcplib.WithDescription("See '[Bulk & async processing](#bulk-amp-async-processing)' for more information. Required: custom_fields. Optional: callback_url. Returns the new BulkCreateResponse."),
			mcplib.WithString("callback_url", mcplib.Description("Callback url")),
			mcplib.WithString("custom_fields", mcplib.Required(), mcplib.Description("Custom fields")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/bulk/custom_fields", false, []mcpParamBinding{{PublicName: "callback_url", WireName: "callback_url", Location: "body"}, {PublicName: "custom_fields", WireName: "custom_fields", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("bulk_create-customfields",
			mcplib.WithDescription("Bulk update subscriber custom field values. Required: callback_url, custom_field_values. Returns the new BulkCreateCustomfieldsResponse."),
			mcplib.WithString("callback_url", mcplib.Required(), mcplib.Description("Callback url")),
			mcplib.WithString("custom_field_values", mcplib.Required(), mcplib.Description("Custom field values")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/bulk/custom_fields/subscribers", false, []mcpParamBinding{{PublicName: "callback_url", WireName: "callback_url", Location: "body"}, {PublicName: "custom_field_values", WireName: "custom_field_values", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("bulk_create-forms",
			mcplib.WithDescription("Adding subscribers to double opt-in forms will trigger sending an Incentive Email. Subscribers already added to the specified form will not receive the Incentive Email again. For more information about double opt-in see '[Double opt-in](#double-opt-in)'. <br/><br/>The subscribers being added to the form must already exist. Subscribers can be created in bulk using the '[Bulk create subscriber](#bulk-create-subscribers)' endpoint.<br/><br/>See '[Bulk & async processing](#bulk-amp-async-processing)' for more information. Required: additions. Optional: callback_url. Returns the new BulkCreateFormsResponse."),
			mcplib.WithString("additions", mcplib.Required(), mcplib.Description("Additions")),
			mcplib.WithString("callback_url", mcplib.Description("Callback url")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/bulk/forms/subscribers", false, []mcpParamBinding{{PublicName: "additions", WireName: "additions", Location: "body"}, {PublicName: "callback_url", WireName: "callback_url", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("bulk_create-subscribers",
			mcplib.WithDescription("See '[Bulk & async processing](#bulk-amp-async-processing)' for more information. Required: subscribers. Optional: callback_url. Returns the new BulkCreateSubscribersResponse."),
			mcplib.WithString("callback_url", mcplib.Description("Callback url")),
			mcplib.WithString("subscribers", mcplib.Required(), mcplib.Description("Subscribers")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/bulk/subscribers", false, []mcpParamBinding{{PublicName: "callback_url", WireName: "callback_url", Location: "body"}, {PublicName: "subscribers", WireName: "subscribers", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("bulk_create-tags",
			mcplib.WithDescription("See '[Bulk & async processing](#bulk-amp-async-processing)' for more information. Required: tags. Optional: callback_url. Returns the new BulkCreateTagsResponse."),
			mcplib.WithString("callback_url", mcplib.Description("Callback url")),
			mcplib.WithString("tags", mcplib.Required(), mcplib.Description("Tags")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/bulk/tags", false, []mcpParamBinding{{PublicName: "callback_url", WireName: "callback_url", Location: "body"}, {PublicName: "tags", WireName: "tags", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("bulk_create-tags-2",
			mcplib.WithDescription("The subscribers being tagged must already exist. Subscribers can be created in bulk using the '[Bulk create subscriber](#bulk-create-subscribers)' endpoint.<br/><br/>See '[Bulk & async processing](#bulk-amp-async-processing)' for more information. Required: taggings. Optional: callback_url. Returns the new BulkCreateTags2Response."),
			mcplib.WithString("callback_url", mcplib.Description("Callback url")),
			mcplib.WithString("taggings", mcplib.Required(), mcplib.Description("Taggings")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/bulk/tags/subscribers", false, []mcpParamBinding{{PublicName: "callback_url", WireName: "callback_url", Location: "body"}, {PublicName: "taggings", WireName: "taggings", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("bulk_delete",
			mcplib.WithDescription("See '[Bulk & async processing](#bulk-amp-async-processing)' for more information. Returns the BulkDeleteResponse. Destructive."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v4/bulk/tags/subscribers", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("custom-fields_create",
			mcplib.WithDescription("Create a custom field for your account. The label field must be unique to your account. Whitespace will be removed from the beginning and the end of your label.<br/><br/>Additionally, a key field and a name field will be generated for you. The key is an ASCII-only, lowercased, underscored representation of your label. This key must be unique to your account. Keys are used in personalization tags in sequences and broadcasts. Names are unique identifiers for use in the HTML of custom forms. They are made up of a combination of ID and the key of the custom field prefixed with 'ck_field'. Required: label. Returns the new CustomFieldsCreateResponse."),
			mcplib.WithString("label", mcplib.Required(), mcplib.Description("Label")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/custom_fields", false, []mcpParamBinding{{PublicName: "label", WireName: "label", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("custom-fields_delete",
			mcplib.WithDescription("This will remove all data in this field from your subscribers. Required: id. Destructive."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v4/custom_fields/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("custom-fields_list",
			mcplib.WithDescription("A custom field allows you to collect subscriber information beyond the standard fields of first name and email address. An example would be a custom field called last name so you can get the full names of your subscribers.<br/><br/>You create a custom field, and then you're able to use that in your forms or emails. Optional: after, before, include_total_count (plus 1 more). Returns the CustomFieldsListResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/custom_fields", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("custom-fields_update",
			mcplib.WithDescription("Updates a custom field label (see [Create a custom field](/api-reference/custom-fields/create-a-custom-field) for more information on labels). Note that the key will change but the name remains the same when the label is updated.<br/><br/><strong>Warning: </strong>An update to a custom field will break all of the liquid personalization tags in emails that reference it - e.g. if you update a `Zip_Code` custom field to `Post_Code`, all liquid tags referencing `{{ subscriber.Zip_Code }}` would no longer work and need to be replaced with `{{ subscriber.Post_Code }}`. Required: id, label. Returns the updated CustomFieldsUpdateResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("label", mcplib.Required(), mcplib.Description("Label")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v4/custom_fields/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "label", WireName: "label", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("email-templates_list",
			mcplib.WithDescription("List email templates. Optional: after, before, include_total_count (plus 1 more). Returns the EmailTemplatesListResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/email_templates", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("forms_list",
			mcplib.WithDescription("List forms. Optional: after, before, include_total_count (plus 3 more). Returns the FormsListResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithString("status", mcplib.Description("Filter forms that have this status (`active`, `archived`, `trashed`, or `all`). Defaults to `active`.")),
			mcplib.WithString("type", mcplib.Description("Filter forms and landing pages by type. Use `embed` for embedded forms. Use `hosted` for landing pages.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/forms", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "status", WireName: "status", Location: "query"}, {PublicName: "type", WireName: "type", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("forms_subscribers_create",
			mcplib.WithDescription("The subscriber being added to the form must already exist. Subscribers can be created using the '[Create a subscriber](#create-a-subscriber)' endpoint. Required: form_id, email_address. Optional: referrer. Returns the new SubscribersCreateResponse."),
			mcplib.WithNumber("form_id", mcplib.Required(), mcplib.Description("Form id")),
			mcplib.WithString("email_address", mcplib.Required(), mcplib.Description("Email address")),
			mcplib.WithString("referrer", mcplib.Description("Referrer")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/forms/{form_id}/subscribers", false, []mcpParamBinding{{PublicName: "form_id", WireName: "form_id", Location: "path"}, {PublicName: "email_address", WireName: "email_address", Location: "body"}, {PublicName: "referrer", WireName: "referrer", Location: "body"}}, []string{"form_id"}),
	)
	s.AddTool(
		mcplib.NewTool("forms_subscribers_create-forms",
			mcplib.WithDescription("The subscriber being added to the form must already exist. Subscribers can be created using the '[Create a subscriber](#create-a-subscriber)' endpoint. Required: form_id, id, referrer. Returns the new SubscribersCreateFormsResponse."),
			mcplib.WithNumber("form_id", mcplib.Required(), mcplib.Description("Form id")),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("referrer", mcplib.Required(), mcplib.Description("Referrer")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/forms/{form_id}/subscribers/{id}", false, []mcpParamBinding{{PublicName: "form_id", WireName: "form_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "referrer", WireName: "referrer", Location: "body"}}, []string{"form_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("forms_subscribers_get",
			mcplib.WithDescription("List subscribers for a form. Required: form_id. Optional: added_after, added_before, after (plus 6 more). Returns the SubscribersGetResponse."),
			mcplib.WithString("added_after", mcplib.Description("Filter subscribers who have been added to the form after this date (format yyyy-mm-dd)")),
			mcplib.WithString("added_before", mcplib.Description("Filter subscribers who have been added to the form before this date (format yyyy-mm-dd)")),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithString("created_after", mcplib.Description("Filter subscribers who have been created after this date (format yyyy-mm-dd)")),
			mcplib.WithString("created_before", mcplib.Description("Filter subscribers who have been created before this date (format yyyy-mm-dd)")),
			mcplib.WithNumber("form_id", mcplib.Required(), mcplib.Description("Form id")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithString("status", mcplib.Description("Filter subscribers who have this status (`active`, `inactive`, `bounced`, `complained`,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/forms/{form_id}/subscribers", false, []mcpParamBinding{{PublicName: "added_after", WireName: "added_after", Location: "query"}, {PublicName: "added_before", WireName: "added_before", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "created_after", WireName: "created_after", Location: "query"}, {PublicName: "created_before", WireName: "created_before", Location: "query"}, {PublicName: "form_id", WireName: "form_id", Location: "path"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "status", WireName: "status", Location: "query"}}, []string{"form_id"}),
	)
	s.AddTool(
		mcplib.NewTool("posts_get",
			mcplib.WithDescription("Get a post. Required: id. Returns the PostsGetResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/posts/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("posts_list",
			mcplib.WithDescription("List posts. Optional: after, before, include_content (plus 2 more). Returns the PostsListResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_content", mcplib.Description("To include the `content` field on each post in the response, use `true`. Omitted by default because it significantly...")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/posts", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_content", WireName: "include_content", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("purchases_create",
			mcplib.WithDescription("Create a purchase. Required: purchase. Returns the new PurchasesCreateResponse."),
			mcplib.WithString("purchase", mcplib.Required(), mcplib.Description("Purchase")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/purchases", false, []mcpParamBinding{{PublicName: "purchase", WireName: "purchase", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("purchases_get",
			mcplib.WithDescription("Get a purchase. Required: id. Returns the PurchasesGetResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/purchases/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("purchases_list",
			mcplib.WithDescription("List purchases. Optional: after, before, include_total_count (plus 1 more). Returns the PurchasesListResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/purchases", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("segments_list",
			mcplib.WithDescription("List segments. Optional: after, before, include_total_count (plus 1 more). Returns the SegmentsListResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/segments", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_create",
			mcplib.WithDescription("Creates an empty sequence — the container that holds sequence emails. After creating the shell, use [Create a sequence email](/api-reference/sequence-emails/create-a-sequence-email) to populate it. Only `name` is required. Every other field has a sensible default: Kit fills in the account's default sending address, a daily send schedule, and the account time zone — and any of these can be tuned later via [Update a sequence](/api-reference/sequences/update-a-sequence). Two behavioural toggles worth flagging up front. `repeat` controls whether a subscriber can re-enter the sequence: by default a subscriber receives the emails once and is marked complete, but with `repeat: true`, re-adding the same subscriber via a Visual Automation, Rule, Bulk Action, or Import resets their position to the start. Filters and exclusions still apply across restarts. `hold` (evergreen) keeps subscribers active in the sequence after they've received every published email — useful when you plan to add more emails later. Without `hold`, subscribers transition to Completed and won't pick up future additions. `exclude_subscriber_sources` lets you exclude subscribers acquired via specific tags, sequences, forms, or segments — they'll skip this sequence entirely. For end-user context, see the help articles on [creating and sending a sequence](https://help.kit.com/en/articles/2502629-creating-and-sending-a-sequence-in-kit), [restarting a sequence](https://help.kit.com/en/articles/5022528-restart-a-sequence), and [holding subscribers in evergreen sequences](https://help.kit.com/en/articles/5192801-how-to-hold-subscribers-in-evergreen-sequences). Optional: active, email_address, email_template_id (plus 7 more). Returns the new SequencesCreateResponse."),
			mcplib.WithBoolean("active", mcplib.Description("`true` to activate the sequence, `false` to deactivate it.")),
			mcplib.WithString("email_address", mcplib.Description("The sending email address to use. Uses the account's sending email address if not provided.")),
			mcplib.WithNumber("email_template_id", mcplib.Description("Id of the email template to use.")),
			mcplib.WithString("exclude_subscriber_sources", mcplib.Description("The subscriber sources to exclude from the sequence.")),
			mcplib.WithBoolean("hold", mcplib.Description("When `true`, subscribers added via Visual Automations stay in the sequence after receiving the last email.")),
			mcplib.WithString("name", mcplib.Description("The name of the sequence.")),
			mcplib.WithBoolean("repeat", mcplib.Description("When `true`, subscribers can restart the sequence multiple times.")),
			mcplib.WithString("send_days", mcplib.Description("The days of the week to send the sequence on. Must be one of: `monday`, `tuesday`, `wednesday`, `thursday`,...")),
			mcplib.WithNumber("send_hour", mcplib.Description("The hour of the day to send the sequence at. Must be an integer between 0 and 23.")),
			mcplib.WithString("time_zone", mcplib.Description("The timezone to use for the sequence. Must be a valid IANA timezone string.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/sequences", false, []mcpParamBinding{{PublicName: "active", WireName: "active", Location: "body"}, {PublicName: "email_address", WireName: "email_address", Location: "body"}, {PublicName: "email_template_id", WireName: "email_template_id", Location: "body"}, {PublicName: "exclude_subscriber_sources", WireName: "exclude_subscriber_sources", Location: "body"}, {PublicName: "hold", WireName: "hold", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "repeat", WireName: "repeat", Location: "body"}, {PublicName: "send_days", WireName: "send_days", Location: "body"}, {PublicName: "send_hour", WireName: "send_hour", Location: "body"}, {PublicName: "time_zone", WireName: "time_zone", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_delete",
			mcplib.WithDescription("Soft-deletes a sequence. The sequence is removed from active delivery immediately, with cleanup of associated state happening in the background. **Warning:** deleting a sequence with active subscribers stops deliveries to those subscribers — they will not receive remaining emails, and any Visual Automations referencing the sequence will need to be updated. Confirm the sequence is not in active use before deleting. If you want to pause rather than delete, use [Update a sequence](/api-reference/sequences/update-a-sequence) with `active: false` instead. Required: id. Destructive."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v4/sequences/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_get",
			mcplib.WithDescription("Fetches a single sequence by `id`. Use this when you need the current schedule, the `active` / `repeat` / `hold` flags, the configured `email_address` and `email_template_id`, or `exclude_subscriber_sources` for a known sequence — for example, to confirm settings before adding subscribers or to render an editor. For the individual emails inside the sequence, use [List sequence emails](/api-reference/sequence-emails/list-sequence-emails). For the sequence model and field semantics, see [Create a sequence](/api-reference/sequences/create-a-sequence). Required: id. Returns the SequencesGetResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/sequences/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_list",
			mcplib.WithDescription("Returns every sequence on the account. A sequence is a self-contained set of automated emails — subscribers join, then receive each email in order, governed by per-email `delay_value` / `delay_unit` and the sequence's overall `send_days`, `send_hour`, and `time_zone` schedule. Each entry carries the schedule defaults plus three behavioural toggles: `active` (whether the sequence is delivering), `repeat` (whether subscribers can re-enter), and `hold` (whether subscribers stay active after receiving every published email — an evergreen pattern). See [Create a sequence](/api-reference/sequences/create-a-sequence) for the full sequence model. Once you have a sequence's `id`, [List sequence emails](/api-reference/sequence-emails/list-sequence-emails) returns the individual emails inside it. For end-user context on how creators build sequences, see the help articles on [creating and sending a sequence](https://help.kit.com/en/articles/2502629-creating-and-sending-a-sequence-in-kit) and [evergreen content](https://help.kit.com/en/articles/2502575-what-is-evergreen-content). Optional: after, before, include_total_count (plus 1 more)."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/sequences", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_update",
			mcplib.WithDescription("Updates any sequence settings — `name`, `email_address`, schedule (`send_days`, `send_hour`, `time_zone`), `email_template_id`, `exclude_subscriber_sources`, or the `active` / `repeat` / `hold` flags. Only fields included in the request body change; everything else is preserved. Some changes have user-visible side effects on subscribers already in the sequence: **Note:** flipping `active` from `false` to `true` resumes delivery for queued subscribers. Flipping it back to `false` pauses the sequence — subscribers stay in their current position but no new emails are sent until it's reactivated. **Note:** changing the schedule (`send_days`, `send_hour`, or `time_zone`) only affects future sends. It does not retroactively reschedule emails already queued for delivery. **Warning:** turning off `repeat` while subscribers are mid-sequence does not stop them from finishing — but they won't be re-eligible to start over after completing. See [Create a sequence](/api-reference/sequences/create-a-sequence) for the full sequence model and what each field controls. Required: id. Optional: active, email_address, email_template_id (plus 7 more). Returns the updated SequencesUpdateResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithBoolean("active", mcplib.Description("`true` to activate the sequence, `false` to deactivate it.")),
			mcplib.WithString("email_address", mcplib.Description("The sending email address to use. Uses the account's sending email address if not provided.")),
			mcplib.WithNumber("email_template_id", mcplib.Description("Id of the email template to use.")),
			mcplib.WithString("exclude_subscriber_sources", mcplib.Description("The subscriber sources to exclude from the sequence.")),
			mcplib.WithBoolean("hold", mcplib.Description("When `true`, subscribers added via Visual Automations stay in the sequence after receiving the last email.")),
			mcplib.WithString("name", mcplib.Description("The name of the sequence.")),
			mcplib.WithBoolean("repeat", mcplib.Description("When `true`, subscribers can restart the sequence multiple times.")),
			mcplib.WithString("send_days", mcplib.Description("The days of the week to send the sequence on. Must be one of: `monday`, `tuesday`, `wednesday`, `thursday`,...")),
			mcplib.WithNumber("send_hour", mcplib.Description("The hour of the day to send the sequence at. Must be an integer between 0 and 23.")),
			mcplib.WithString("time_zone", mcplib.Description("The timezone to use for the sequence. Must be a valid IANA timezone string.")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v4/sequences/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "active", WireName: "active", Location: "body"}, {PublicName: "email_address", WireName: "email_address", Location: "body"}, {PublicName: "email_template_id", WireName: "email_template_id", Location: "body"}, {PublicName: "exclude_subscriber_sources", WireName: "exclude_subscriber_sources", Location: "body"}, {PublicName: "hold", WireName: "hold", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "repeat", WireName: "repeat", Location: "body"}, {PublicName: "send_days", WireName: "send_days", Location: "body"}, {PublicName: "send_hour", WireName: "send_hour", Location: "body"}, {PublicName: "time_zone", WireName: "time_zone", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_emails_create",
			mcplib.WithDescription("Adds a single email to a sequence. Each sequence email represents one step subscribers receive on their journey through the sequence — its `position` determines order, and `delay_value` / `delay_unit` / `send_days` determine when it fires relative to either the previous email or the subscriber's entry. `subject`, `delay_value`, and `delay_unit` are required. **Day-based** emails (`delay_unit: 'days'`) follow the parent sequence's `send_days` and `send_hour`; their per-email `send_days` (e.g. `['monday', 'wednesday']`) overrides the sequence schedule for that one email. **Hour-based** emails (`delay_unit: 'hours'`) ignore the sequence schedule and fire once the delay elapses — Kit checks for hour-delayed sends every 15 minutes — and always return `send_days` as `null`. New emails are created in draft (`published: false`) so they don't go out until you flip the flag. `position` is auto-assigned to the end of the sequence if omitted. `content` accepts HTML and can embed snippets with `{{ snippet.<key> }}` — see [Create a snippet](/api-reference/snippets/create-a-snippet) — which Kit resolves at send time, so editing a snippet later updates every email referencing it without a re-publish. **Note:** only the first email in a sequence can be an immediate send (`delay_value: 0` with `delay_unit: 'days'`). Subsequent emails need a positive delay. **Warning:** publishing an immediate first email, or inserting a new email at a `position` earlier than where existing subscribers sit, processes those subscribers and triggers a send right away. Plan around active sequences carefully. For end-user context on how creators build sequences in the Kit UI, see the help article on [creating and sending a sequence](https://help.kit.com/en/articles/2502629-creating-and-sending-a-sequence-in-kit). Required: sequence_id, delay_unit, delay_value, subject. Optional: content, email_template_id, position (plus 3 more). Returns the new EmailsCreateResponse."),
			mcplib.WithNumber("sequence_id", mcplib.Required(), mcplib.Description("Sequence id")),
			mcplib.WithString("content", mcplib.Description("HTML body content of the email")),
			mcplib.WithString("delay_unit", mcplib.Required(), mcplib.Description("Unit for the send delay. Use `days` for schedule-aware delivery, `hours` for a fixed hourly delay")),
			mcplib.WithNumber("delay_value", mcplib.Required(), mcplib.Description("Number of days or hours to wait before sending this email after the previous one")),
			mcplib.WithNumber("email_template_id", mcplib.Description("ID of the email template to use for layout and styling")),
			mcplib.WithNumber("position", mcplib.Description("Zero-based position of the email in the sequence. Assigned automatically after the last email if omitted")),
			mcplib.WithString("preview_text", mcplib.Description("Preview text shown in email clients before the email is opened")),
			mcplib.WithBoolean("published", mcplib.Description("Whether the email is active and will be sent to subscribers. Defaults to `false` (draft)")),
			mcplib.WithString("send_days", mcplib.Description("Days of the week this email may be sent. Defaults to all 7 days (inherits the sequence schedule). Pass a subset to...")),
			mcplib.WithString("subject", mcplib.Required(), mcplib.Description("Subject line of the email")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/sequences/{sequence_id}/emails", false, []mcpParamBinding{{PublicName: "sequence_id", WireName: "sequence_id", Location: "path"}, {PublicName: "content", WireName: "content", Location: "body"}, {PublicName: "delay_unit", WireName: "delay_unit", Location: "body"}, {PublicName: "delay_value", WireName: "delay_value", Location: "body"}, {PublicName: "email_template_id", WireName: "email_template_id", Location: "body"}, {PublicName: "position", WireName: "position", Location: "body"}, {PublicName: "preview_text", WireName: "preview_text", Location: "body"}, {PublicName: "published", WireName: "published", Location: "body"}, {PublicName: "send_days", WireName: "send_days", Location: "body"}, {PublicName: "subject", WireName: "subject", Location: "body"}}, []string{"sequence_id"}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_emails_delete",
			mcplib.WithDescription("Permanently removes one email from a sequence. **Warning:** subscribers already queued to receive the deleted email will skip it — they'll continue to the next email in sequence order. This is intentional and not retryable. If subscribers need to receive the email's content, send a broadcast or re-add subscribers to the sequence (with `repeat: true`) before deleting. If you want to pause rather than delete, use [Update a sequence email](/api-reference/sequence-emails/update-a-sequence-email) with `published: false` instead. Required: id, sequence_id. Destructive."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithNumber("sequence_id", mcplib.Required(), mcplib.Description("Sequence id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v4/sequences/{sequence_id}/emails/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "sequence_id", WireName: "sequence_id", Location: "path"}}, []string{"id", "sequence_id"}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_emails_get",
			mcplib.WithDescription("Returns every email inside a sequence, ordered by `position` (the order subscribers receive them). Each entry carries timing (`delay_value`, `delay_unit`, `send_days`), publish state (`published`), and metadata (`subject`, `preview_text`, `email_template_id`). **Tip:** the heavier `content` field is omitted by default to keep responses fast on sequences with many emails. Pass `include_content=true` when you need the body — for example, to render a preview, audit Liquid usage, or sync an external draft. For the field semantics — particularly how `delay_unit` and `send_days` interact, and what happens when subscribers hit a `published: false` email — see [Create a sequence email](/api-reference/sequence-emails/create-a-sequence-email). Required: sequence_id. Optional: after, before, include_content (plus 2 more)."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithString("include_content", mcplib.Description("Pass `true` to include the `content` field on each email in the response. Omitted by default because it increases...")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithNumber("sequence_id", mcplib.Required(), mcplib.Description("Sequence id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/sequences/{sequence_id}/emails", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_content", WireName: "include_content", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "sequence_id", WireName: "sequence_id", Location: "path"}}, []string{"sequence_id"}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_emails_get-sequences",
			mcplib.WithDescription("Fetches a single sequence email by `id`. Unlike [List sequence emails](/api-reference/sequence-emails/list-sequence-emails), this endpoint **always returns the full `content`** — no `include_content` flag needed. Use this when you have an email's `id` and need the current body, timing, or publish state — for example, to render a preview before pushing an update via [Update a sequence email](/api-reference/sequence-emails/update-a-sequence-email). For the field semantics, see [Create a sequence email](/api-reference/sequence-emails/create-a-sequence-email). Required: id, sequence_id."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithNumber("sequence_id", mcplib.Required(), mcplib.Description("Sequence id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/sequences/{sequence_id}/emails/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "sequence_id", WireName: "sequence_id", Location: "path"}}, []string{"id", "sequence_id"}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_emails_update",
			mcplib.WithDescription("Updates any field on a sequence email — `subject`, `preview_text`, `content`, `delay_value`, `delay_unit`, `send_days`, `position`, `email_template_id`, or `published`. Only fields included in the request body change; everything else is preserved. **Note:** pass `send_days: null` to revert a per-email schedule override and inherit the parent sequence's `send_days` again. The response will then return all 7 days, indicating no per-email restriction. **Warning:** sending `send_days` on an email with `delay_unit: 'hours'` returns `422` — `send_days` only applies to day-based emails. **Warning:** changing `position` while subscribers are actively progressing through the sequence can cause emails to be sent out of order or skipped. The same caution applies to flipping `published` on a `position: 0` email — it triggers Kit to process all queued subscribers for that email. For the sequence-email model and how `delay_unit` and `send_days` interact, see [Create a sequence email](/api-reference/sequence-emails/create-a-sequence-email). Required: id, sequence_id. Optional: content, delay_unit, delay_value (plus 6 more)."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithNumber("sequence_id", mcplib.Required(), mcplib.Description("Sequence id")),
			mcplib.WithString("content", mcplib.Description("New HTML body content of the email")),
			mcplib.WithString("delay_unit", mcplib.Description("New delay unit. Use `days` for schedule-aware delivery, `hours` for a fixed hourly delay")),
			mcplib.WithNumber("delay_value", mcplib.Description("New delay value")),
			mcplib.WithNumber("email_template_id", mcplib.Description("New email template ID for layout and styling. Pass `null` to clear")),
			mcplib.WithNumber("position", mcplib.Description("New zero-based position of the email in the sequence")),
			mcplib.WithString("preview_text", mcplib.Description("New preview text shown in email clients before the email is opened")),
			mcplib.WithBoolean("published", mcplib.Description("Pass `true` to publish a draft email or `false` to unpublish it")),
			mcplib.WithString("send_days", mcplib.Description("Days of the week this email may be sent. Pass a subset to restrict delivery, or `null` to reset to all days...")),
			mcplib.WithString("subject", mcplib.Description("New subject line for the email")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v4/sequences/{sequence_id}/emails/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "sequence_id", WireName: "sequence_id", Location: "path"}, {PublicName: "content", WireName: "content", Location: "body"}, {PublicName: "delay_unit", WireName: "delay_unit", Location: "body"}, {PublicName: "delay_value", WireName: "delay_value", Location: "body"}, {PublicName: "email_template_id", WireName: "email_template_id", Location: "body"}, {PublicName: "position", WireName: "position", Location: "body"}, {PublicName: "preview_text", WireName: "preview_text", Location: "body"}, {PublicName: "published", WireName: "published", Location: "body"}, {PublicName: "send_days", WireName: "send_days", Location: "body"}, {PublicName: "subject", WireName: "subject", Location: "body"}}, []string{"id", "sequence_id"}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_subscribers_create",
			mcplib.WithDescription("The subscriber being added to the sequence must already exist. Subscribers can be created using the '[Create a subscriber](#create-a-subscriber)' endpoint. Required: sequence_id, email_address. Returns the new SubscribersCreateResponse."),
			mcplib.WithNumber("sequence_id", mcplib.Required(), mcplib.Description("Sequence id")),
			mcplib.WithString("email_address", mcplib.Required(), mcplib.Description("Email address")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/sequences/{sequence_id}/subscribers", false, []mcpParamBinding{{PublicName: "sequence_id", WireName: "sequence_id", Location: "path"}, {PublicName: "email_address", WireName: "email_address", Location: "body"}}, []string{"sequence_id"}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_subscribers_create-sequences",
			mcplib.WithDescription("The subscriber being added to the sequence must already exist. Subscribers can be created using the '[Create a subscriber](#create-a-subscriber)' endpoint. Required: id, sequence_id. Returns the new SubscribersCreateSequencesResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithNumber("sequence_id", mcplib.Required(), mcplib.Description("Sequence id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/sequences/{sequence_id}/subscribers/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "sequence_id", WireName: "sequence_id", Location: "path"}}, []string{"id", "sequence_id"}),
	)
	s.AddTool(
		mcplib.NewTool("sequences_subscribers_get",
			mcplib.WithDescription("List subscribers for a sequence. Required: sequence_id. Optional: added_after, added_before, after (plus 6 more). Returns the SubscribersGetResponse."),
			mcplib.WithString("added_after", mcplib.Description("Filter subscribers who have been added to the form after this date (format yyyy-mm-dd)")),
			mcplib.WithString("added_before", mcplib.Description("Filter subscribers who have been added to the form before this date (format yyyy-mm-dd)")),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithString("created_after", mcplib.Description("Filter subscribers who have been created after this date (format yyyy-mm-dd)")),
			mcplib.WithString("created_before", mcplib.Description("Filter subscribers who have been created before this date (format yyyy-mm-dd)")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithNumber("sequence_id", mcplib.Required(), mcplib.Description("Sequence id")),
			mcplib.WithString("status", mcplib.Description("Filter subscribers who have this status (`active`, `inactive`, `bounced`, `complained`,...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/sequences/{sequence_id}/subscribers", false, []mcpParamBinding{{PublicName: "added_after", WireName: "added_after", Location: "query"}, {PublicName: "added_before", WireName: "added_before", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "created_after", WireName: "created_after", Location: "query"}, {PublicName: "created_before", WireName: "created_before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "sequence_id", WireName: "sequence_id", Location: "path"}, {PublicName: "status", WireName: "status", Location: "query"}}, []string{"sequence_id"}),
	)
	s.AddTool(
		mcplib.NewTool("snippets_create",
			mcplib.WithDescription("Snippets are reusable pieces of email content you can drop into a broadcast or sequence email using Liquid: `{{ snippet.key }}`. Update the snippet once and every email that references it picks up the new content on next send. There are two `snippet_type`s. **`inline`** snippets store plain-text content (with Liquid variable support like `{{ subscriber.first_name }}`) in the `content` field. **`block`** snippets store rich-text HTML — text, lists, images, buttons — in `document_attributes.value_html`. A snippet's type is fixed at creation: it cannot be changed via [Update a snippet](/api-reference/snippets/update-a-snippet). The response includes a `key` field. That's the identifier you use in Liquid — for example, a snippet returned with `'key': 'welcome-message'` is referenced inside a broadcast as `{{ snippet.welcome-message }}`. Keys are derived from the snippet name on creation. **Note:** the API rejects circular references — a snippet cannot reference itself, directly or transitively — with a `422` validation error. For end-user context on how creators build and edit snippets in the Kit UI, see the help articles on [content snippets](https://help.kit.com/en/articles/3812712-creating-and-using-content-snippets-in-your-kit-emails) and [code snippets for custom templates](https://help.kit.com/en/articles/2810398-code-snippets-for-custom-email-templates). Returns the new SnippetsCreateResponse."),
			mcplib.WithString("body_json", mcplib.Description("Request body as a JSON object string (this endpoint accepts a polymorphic schema: oneOf/anyOf)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/snippets", false, []mcpParamBinding{{PublicName: "body_json", WireName: "body_json", Location: "body_json"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("snippets_get",
			mcplib.WithDescription("Fetches a single snippet by `id`. Unlike [List snippets](/api-reference/snippets/list-snippets), this endpoint **always returns the full `content` and `document`** — no `include_content` flag needed. Use this when you have an `id` (e.g. stored from a prior create call) and need the current `key` and body — for example, to preview the resolved HTML or confirm a snippet still exists before referencing it as `{{ snippet.key }}` in a broadcast or sequence email. See [Create a snippet](/api-reference/snippets/create-a-snippet) for the snippet model. Required: id."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/snippets/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("snippets_list",
			mcplib.WithDescription("Returns every snippet on the account. Each snippet's `key` is the identifier used in Liquid — `{{ snippet.key }}` — when creating a broadcast or sequence email. See [Create a snippet](/api-reference/snippets/create-a-snippet) for how snippets work end-to-end. **Tip:** the heavier `content` and `document` fields are omitted by default to keep responses fast. Pass `include_content=true` when you need the body — for example, to render a preview or audit Liquid usage. Filter the result with `snippet_type` (`inline` or `block`) and `archived` (defaults to `false`, set `true` to list only archived snippets). Optional: after, archived, before (plus 4 more)."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("archived", mcplib.Description("When `true`, returns only archived snippets. Defaults to `false`.")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_content", mcplib.Description("When `true`, includes both the `content` and `document` fields for each snippet in the response. Defaults to `false`.")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithString("snippet_type", mcplib.Description("Filter snippets by type. Use `inline` for text snippets or `block` for rich-text block snippets.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/snippets", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "archived", WireName: "archived", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_content", WireName: "include_content", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "snippet_type", WireName: "snippet_type", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("snippets_update",
			mcplib.WithDescription("Rename a snippet, replace its body, or archive/restore it. Updates apply on the next send of any email that references the snippet via `{{ snippet.key }}` — there's no per-email versioning, so a content change ripples to every broadcast or sequence email using that key. The request body must match the existing `snippet_type`. For an **`inline`** snippet, send `content` (and optionally `name`, `archived`). For a **`block`** snippet, send `document_attributes.value_html` (and optionally `name`, `archived`). Pass `archived: true` to archive, `false` to restore. **Warning:** `snippet_type` is immutable. Sending a different value, or sending the body shape for the wrong type, returns a `422` with `snippet_type cannot be changed`. See [Create a snippet](/api-reference/snippets/create-a-snippet) for the full snippet model and how `key` ties into Liquid. Required: id."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("body_json", mcplib.Description("Request body as a JSON object string (this endpoint accepts a polymorphic schema: oneOf/anyOf)")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v4/snippets/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "body_json", WireName: "body_json", Location: "body_json"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_create",
			mcplib.WithDescription("Behaves as an upsert. If a subscriber with the provided email address does not exist, it creates one with the specified first name and state. If a subscriber with the provided email address already exists, it updates the first name.<br/><br/>If you include a custom field key that does not exist on your account, the request returns an error. Use [List custom fields](/api-reference/custom-fields/list-custom-fields) to retrieve existing keys, or [Create a custom field](/api-reference/custom-fields/create-a-custom-field) to add new fields before setting them for subscribers.<br/><br/><strong>NOTE:</strong> Updating the subscriber state with this endpoint is not supported at this time.<br/><strong>NOTE:</strong> We support creating/updating a maximum of 140 custom fields at a time. Required: email_address. Optional: fields, first_name, state."),
			mcplib.WithString("email_address", mcplib.Required(), mcplib.Description("Email address")),
			mcplib.WithString("fields", mcplib.Description("Custom field values keyed by the custom field's `key` (e.g. `last_name`, not `Last Name`). Passing an unknown key...")),
			mcplib.WithString("first_name", mcplib.Description("First name")),
			mcplib.WithString("state", mcplib.Description("Create subscriber in this state (`active`, `bounced`, `cancelled`, `complained` or `inactive`). Defaults to `active`.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/subscribers", false, []mcpParamBinding{{PublicName: "email_address", WireName: "email_address", Location: "body"}, {PublicName: "fields", WireName: "fields", Location: "body"}, {PublicName: "first_name", WireName: "first_name", Location: "body"}, {PublicName: "state", WireName: "state", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_create-filter",
			mcplib.WithDescription("Filter subscribers based on engagement. Required: all. Returns the new SubscribersCreateFilterResponse."),
			mcplib.WithString("all", mcplib.Required(), mcplib.Description("Array of filter conditions where ALL must be met (AND logic)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/subscribers/filter", false, []mcpParamBinding{{PublicName: "all", WireName: "all", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_get",
			mcplib.WithDescription("Get a subscriber. Required: id. Returns the SubscribersGetResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/subscribers/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_list",
			mcplib.WithDescription("List subscribers. Optional: after, before, created_after (plus 11 more). Returns the SubscribersListResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithString("created_after", mcplib.Description("Filter subscribers who have been created after this date (format yyyy-mm-dd)")),
			mcplib.WithString("created_before", mcplib.Description("Filter subscribers who have been created before this date (format yyyy-mm-dd)")),
			mcplib.WithString("email_address", mcplib.Description("Email address")),
			mcplib.WithString("include", mcplib.Description("Comma-separated list of additional fields to include on each subscriber. Valid options: `attribution`, `tags`,...")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithNumber("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithBoolean("slim", mcplib.Description("When `true`, omits expensive optional fields from the response. Produces a faster, smaller response — useful when...")),
			mcplib.WithString("sort_field", mcplib.Description("Sort field")),
			mcplib.WithString("sort_order", mcplib.Description("Sort order")),
			mcplib.WithString("status", mcplib.Description("Filter subscribers who have this status (`active`, `inactive`, `bounced`, `complained`,...")),
			mcplib.WithString("updated_after", mcplib.Description("Filter subscribers who have been updated after this date (format yyyy-mm-dd)")),
			mcplib.WithString("updated_before", mcplib.Description("Filter subscribers who have been updated before this date (format yyyy-mm-dd)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/subscribers", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "created_after", WireName: "created_after", Location: "query"}, {PublicName: "created_before", WireName: "created_before", Location: "query"}, {PublicName: "email_address", WireName: "email_address", Location: "query"}, {PublicName: "include", WireName: "include", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "slim", WireName: "slim", Location: "query"}, {PublicName: "sort_field", WireName: "sort_field", Location: "query"}, {PublicName: "sort_order", WireName: "sort_order", Location: "query"}, {PublicName: "status", WireName: "status", Location: "query"}, {PublicName: "updated_after", WireName: "updated_after", Location: "query"}, {PublicName: "updated_before", WireName: "updated_before", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_update",
			mcplib.WithDescription("If you include a custom field key that does not exist on your account, the request returns an error. Use [List custom fields](/api-reference/custom-fields/list-custom-fields) to retrieve existing keys, or [Create a custom field](/api-reference/custom-fields/create-a-custom-field) to add new fields before setting them for subscribers.<br/><br/><strong>NOTE:</strong> We support creating/updating a maximum of 140 custom fields at a time. Required: id, email_address. Optional: fields, first_name."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("email_address", mcplib.Required(), mcplib.Description("Email address")),
			mcplib.WithString("fields", mcplib.Description("Custom field values keyed by the custom field's `key` (e.g. `last_name`, not `Last Name`). Passing an unknown key...")),
			mcplib.WithString("first_name", mcplib.Description("First name")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v4/subscribers/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "email_address", WireName: "email_address", Location: "body"}, {PublicName: "fields", WireName: "fields", Location: "body"}, {PublicName: "first_name", WireName: "first_name", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_stats_get",
			mcplib.WithDescription("Retrieve email stats for a specific subscriber. You can filter the stats by providing `email_sent_after` and/or `email_sent_before` query parameters to limit the stats to emails sent within a specific date range. Note: this functionality was added in June 2025, so no data for events before that date will be included. Required: subscriber_id. Optional: email_sent_after, email_sent_before. Returns the StatsGetResponse."),
			mcplib.WithString("email_sent_after", mcplib.Description("Filter to stats for emails sent after this date (YYYY-MM-DD)/nNOTE: This functionality was added 2025-06-28 and will...")),
			mcplib.WithString("email_sent_before", mcplib.Description("Filter to stats for emails sent before this date (YYYY-MM-DD)/nNote: this functionality was added in June 2025, so...")),
			mcplib.WithNumber("subscriber_id", mcplib.Required(), mcplib.Description("Subscriber id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/subscribers/{subscriber_id}/stats", false, []mcpParamBinding{{PublicName: "email_sent_after", WireName: "email_sent_after", Location: "query"}, {PublicName: "email_sent_before", WireName: "email_sent_before", Location: "query"}, {PublicName: "subscriber_id", WireName: "subscriber_id", Location: "path"}}, []string{"subscriber_id"}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_tags_get",
			mcplib.WithDescription("List tags for a subscriber. Required: subscriber_id. Optional: after, before, include_total_count (plus 1 more). Returns the TagsGetResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithNumber("subscriber_id", mcplib.Required(), mcplib.Description("Subscriber id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/subscribers/{subscriber_id}/tags", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "subscriber_id", WireName: "subscriber_id", Location: "path"}}, []string{"subscriber_id"}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_unsubscribe_create",
			mcplib.WithDescription("Unsubscribe subscriber. Required: id."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/subscribers/{id}/unsubscribe", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("tags_create",
			mcplib.WithDescription("Create a tag. Required: name. Returns the new TagsCreateResponse."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/tags", false, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("tags_list",
			mcplib.WithDescription("List tags. Optional: after, before, include_total_count (plus 1 more). Returns the TagsListResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/tags", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("tags_update",
			mcplib.WithDescription("Update tag name. Required: id, name. Returns the updated TagsUpdateResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v4/tags/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("tags_subscribers_create",
			mcplib.WithDescription("The subscriber being tagged must already exist. Subscribers can be created using the '[Create a subscriber](#create-a-subscriber)' endpoint. Required: tag_id, email_address. Returns the new SubscribersCreateResponse."),
			mcplib.WithNumber("tag_id", mcplib.Required(), mcplib.Description("Tag id")),
			mcplib.WithString("email_address", mcplib.Required(), mcplib.Description("Email address")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/tags/{tag_id}/subscribers", false, []mcpParamBinding{{PublicName: "tag_id", WireName: "tag_id", Location: "path"}, {PublicName: "email_address", WireName: "email_address", Location: "body"}}, []string{"tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("tags_subscribers_create-tags",
			mcplib.WithDescription("The subscriber being tagged must already exist. Subscribers can be created using the '[Create a subscriber](#create-a-subscriber)' endpoint. Required: id, tag_id. Returns the new SubscribersCreateTagsResponse."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithNumber("tag_id", mcplib.Required(), mcplib.Description("Tag id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/tags/{tag_id}/subscribers/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "tag_id", WireName: "tag_id", Location: "path"}}, []string{"id", "tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("tags_subscribers_delete",
			mcplib.WithDescription("Remove tag from subscriber by email address. Required: tag_id. Destructive."),
			mcplib.WithNumber("tag_id", mcplib.Required(), mcplib.Description("Tag id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v4/tags/{tag_id}/subscribers", false, []mcpParamBinding{{PublicName: "tag_id", WireName: "tag_id", Location: "path"}}, []string{"tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("tags_subscribers_delete-tags",
			mcplib.WithDescription("Remove tag from subscriber. Required: id, tag_id. Destructive."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithNumber("tag_id", mcplib.Required(), mcplib.Description("Tag id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v4/tags/{tag_id}/subscribers/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "tag_id", WireName: "tag_id", Location: "path"}}, []string{"id", "tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("tags_subscribers_get",
			mcplib.WithDescription("List subscribers for a tag. Required: tag_id. Optional: after, before, created_after (plus 6 more). Returns the SubscribersGetResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithString("created_after", mcplib.Description("Filter subscribers who have been created after this date (format yyyy-mm-dd)")),
			mcplib.WithString("created_before", mcplib.Description("Filter subscribers who have been created before this date (format yyyy-mm-dd)")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithString("status", mcplib.Description("Filter subscribers who have this status (`active`, `inactive`, `bounced`, `complained`,...")),
			mcplib.WithNumber("tag_id", mcplib.Required(), mcplib.Description("Tag id")),
			mcplib.WithString("tagged_after", mcplib.Description("Filter subscribers who have been tagged after this date (format yyyy-mm-dd)")),
			mcplib.WithString("tagged_before", mcplib.Description("Filter subscribers who have been tagged before this date (format yyyy-mm-dd)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/tags/{tag_id}/subscribers", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "created_after", WireName: "created_after", Location: "query"}, {PublicName: "created_before", WireName: "created_before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "status", WireName: "status", Location: "query"}, {PublicName: "tag_id", WireName: "tag_id", Location: "path"}, {PublicName: "tagged_after", WireName: "tagged_after", Location: "query"}, {PublicName: "tagged_before", WireName: "tagged_before", Location: "query"}}, []string{"tag_id"}),
	)
	s.AddTool(
		mcplib.NewTool("webhooks_create",
			mcplib.WithDescription("Available event types:<br/>- `subscriber.subscriber_activate`<br/>- `subscriber.subscriber_unsubscribe`<br/>- `subscriber.subscriber_bounce`<br/>- `subscriber.subscriber_complain`<br/>- `subscriber.form_subscribe`, required parameter `form_id` [Integer]<br/>- `subscriber.course_subscribe`, required parameter `sequence_id` [Integer]<br/>- `subscriber.course_complete`, required parameter `sequence_id` [Integer]<br/>- `subscriber.link_click`, required parameter `initiator_value` [String] as a link URL<br/>- `subscriber.product_purchase`, required parameter `product_id` [Integer]<br/>- `subscriber.tag_add`, required parameter `tag_id` [Integer]<br/>- `subscriber.tag_remove`, required parameter `tag_id` [Integer]<br/>- `purchase.purchase_create`<br/>- `custom_field.field_created`<br/>- `custom_field.field_deleted`<br/>- `custom_field.field_value_updated`, required parameter `custom_field_id` [Integer]. Required: event, target_url. Returns the new WebhooksCreateResponse."),
			mcplib.WithString("event", mcplib.Required(), mcplib.Description("Event")),
			mcplib.WithString("target_url", mcplib.Required(), mcplib.Description("Target url")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v4/webhooks", false, []mcpParamBinding{{PublicName: "event", WireName: "event", Location: "body"}, {PublicName: "target_url", WireName: "target_url", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("webhooks_delete",
			mcplib.WithDescription("Delete a webhook. Required: id. Destructive."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v4/webhooks/{id}", false, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("webhooks_list",
			mcplib.WithDescription("Webhooks are automations that will receive subscriber data when a subscriber event is triggered, such as when a subscriber completes a sequence.<br/><br/>When a webhook is triggered, a `POST` request will be made to your URL with a JSON payload. Optional: after, before, include_total_count (plus 1 more). Returns the WebhooksListResponse."),
			mcplib.WithString("after", mcplib.Description("To fetch next page of results, use `?after=<end_cursor>`")),
			mcplib.WithString("before", mcplib.Description("To fetch previous page of results, use `?before=<start_cursor>`")),
			mcplib.WithBoolean("include_total_count", mcplib.Description("Set to `true` to include the `total_count` in the response.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page. Default 500, maximum 1000.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v4/webhooks", false, []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include_total_count", WireName: "include_total_count", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// First-class MCP intent tools (Kit-specific compound workflows).
	// Hand-authored in internal/mcp/intents.go and preserved via
	// .printing-press-patches.json. Registered before the cobratree mirror so
	// the typed intent_* names are present even if a future change to the
	// generator's intent template adopts the same naming convention.
	RegisterIntents(s)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, binaryResponse bool, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if binaryResponse {
			headers = map[string]string{client.BinaryResponseHeader: "true"}
		}
		// bodyJSONOverride holds an opaque body for endpoints whose request
		// schema is a oneOf/anyOf and was wired with a single body_json input.
		// When set, it replaces the bodyArgs map for POST/PUT/PATCH.
		var bodyJSONOverride json.RawMessage
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			case "body_json":
				if s, ok := v.(string); ok && s != "" {
					var parsed any
					if err := json.Unmarshal([]byte(s), &parsed); err != nil {
						return mcplib.NewToolResultError("body_json must be a valid JSON object: " + err.Error()), nil
					}
					if _, isMap := parsed.(map[string]any); !isMap {
						return mcplib.NewToolResultError(fmt.Sprintf("body_json must be a JSON object, got JSON %T", parsed)), nil
					}
					bodyJSONOverride = json.RawMessage(s)
				}
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if binaryResponse {
				data, err = c.GetWithHeaders(path, params, headers)
				break
			}
			data, err = c.Get(path, params)
		case "POST":
			if len(bodyJSONOverride) > 0 {
				if binaryResponse {
					data, _, err = c.PostWithParamsAndHeaders(path, params, bodyJSONOverride, headers)
					break
				}
				data, _, err = c.PostWithParams(path, params, bodyJSONOverride)
				break
			}
			if binaryResponse {
				data, _, err = c.PostWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PostWithParams(path, params, bodyArgs)
		case "PUT":
			if len(bodyJSONOverride) > 0 {
				if binaryResponse {
					data, _, err = c.PutWithParamsAndHeaders(path, params, bodyJSONOverride, headers)
					break
				}
				data, _, err = c.PutWithParams(path, params, bodyJSONOverride)
				break
			}
			if binaryResponse {
				data, _, err = c.PutWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(path, params, bodyArgs)
		case "PATCH":
			if len(bodyJSONOverride) > 0 {
				if binaryResponse {
					data, _, err = c.PatchWithParamsAndHeaders(path, params, bodyJSONOverride, headers)
					break
				}
				data, _, err = c.PatchWithParams(path, params, bodyJSONOverride)
				break
			}
			if binaryResponse {
				data, _, err = c.PatchWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(path, params, bodyArgs)
		case "DELETE":
			if binaryResponse {
				data, _, err = c.DeleteWithParamsAndHeaders(path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export KIT_API_KEY=<your-key>" +
					"\n      Get a key at: https://app.kit.com/account_settings/developer_settings" +
					"\n      Generate a v4 API key in Account Settings → Developer." +
					"\n      Run 'kit-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export KIT_API_KEY=<your-key>" +
					"\n      Get a key at: https://app.kit.com/account_settings/developer_settings" +
					"\n      Generate a v4 API key in Account Settings → Developer." +
					"\n      Run 'kit-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export KIT_API_KEY=<your-key>" +
					"\n      Get a key at: https://app.kit.com/account_settings/developer_settings" +
					"\n      Generate a v4 API key in Account Settings → Developer." +
					"\n      Run 'kit-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "kit-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "kit-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "kit",
		"description": "Email marketing for creators (formerly ConvertKit) — subscribers, broadcasts, sequences, tags",
		"archetype":   "content",
		"tool_count":  72,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion kit-pp-cli binary.",
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name":        "KIT_API_KEY",
					"kind":        "per_call",
					"required":    false,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"key_url":      "https://app.kit.com/account_settings/developer_settings",
			"instructions": "Generate a v4 API key in Account Settings → Developer.",
		},
		"resources": []map[string]any{
			{
				"name":        "account",
				"description": "Manage account",
				"endpoints":   []string{"list", "list-colors", "list-creatorprofile", "list-emailstats", "list-growthstats", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "broadcasts",
				"description": "Manage broadcasts",
				"endpoints":   []string{"create", "delete", "get", "list", "list-stats", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "bulk",
				"description": "Manage bulk",
				"endpoints":   []string{"create", "create-customfields", "create-forms", "create-subscribers", "create-tags", "create-tags-2", "delete"},
				"searchable":  true,
			},
			{
				"name":        "custom-fields",
				"description": "Manage custom fields",
				"endpoints":   []string{"create", "delete", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "email-templates",
				"description": "Manage email templates",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "forms",
				"description": "Manage forms",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "posts",
				"description": "Manage posts",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "purchases",
				"description": "Manage purchases",
				"endpoints":   []string{"create", "get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "segments",
				"description": "Manage segments",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "sequences",
				"description": "Manage sequences",
				"endpoints":   []string{"create", "delete", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "snippets",
				"description": "Manage snippets",
				"endpoints":   []string{"create", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "subscribers",
				"description": "Manage subscribers",
				"endpoints":   []string{"create", "create-filter", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "tags",
				"description": "Manage tags",
				"endpoints":   []string{"create", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "webhooks",
				"description": "Manage webhooks",
				"endpoints":   []string{"create", "delete", "list"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the per_page parameter (default 100).",
			"Use updated_after for incremental fetches (filter by modification time).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Intent capabilities — typed MCP tools that compose multiple endpoint
		// calls. Exposed under intent_workflow_* names alongside the cobratree
		// runtime mirror so agents can pick the typed schema while humans keep
		// the shell-out reach.
		"intent_capabilities": []map[string]string{
			{"name": "Creator Snapshot", "intent_tool": "intent_workflow_creator_snapshot", "command": "workflow creator-snapshot", "description": "One-call read-only operating snapshot for Kit account, growth, audience, content, webhooks, and broadcast stats.", "via": "mcp-intent-tool"},
			{"name": "Audience Health", "intent_tool": "intent_workflow_audience_health", "command": "workflow audience-health", "description": "Read-only subscriber status counts, recent growth stats, and largest tags by subscriber count.", "via": "mcp-intent-tool"},
			{"name": "Content Inventory", "intent_tool": "intent_workflow_content_inventory", "command": "workflow content-inventory", "description": "Read-only inventory of sequences, sequence emails, snippets, forms, templates, and recent broadcast stats.", "via": "mcp-intent-tool"},
			{"name": "Subscriber Lookup", "intent_tool": "intent_workflow_subscriber_lookup", "command": "workflow subscriber-lookup", "description": "Read-only subscriber dossier by email or id with profile, custom fields, tags, attribution, and email stats.", "via": "mcp-intent-tool"},
		},
		"playbook": []map[string]string{
			{"topic": "Creator Snapshot", "insight": ""},
			{"topic": "Audience Health", "insight": ""},
			{"topic": "Content Inventory", "insight": ""},
			{"topic": "Subscriber Lookup", "insight": ""},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
