// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/marketing/ahrefs/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/marketing/ahrefs/internal/client"
	"github.com/mvanhorn/printing-press-library/library/marketing/ahrefs/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/marketing/ahrefs/internal/config"
	"github.com/mvanhorn/printing-press-library/library/marketing/ahrefs/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/marketing/ahrefs/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("keywords_explorer_matching_terms",
			mcplib.WithDescription("Matching terms. Required: select, country. Optional: timeout, limit (default: 1000), order_by (plus 6 more). Returns array of KeywordsExplorerMatchingTermsData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See the response schema for valid column identifiers, except for `volume_monthly`,...")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("search_engine", mcplib.Description("[Deprecated on 5 Aug 2024].")),
			mcplib.WithString("keywords", mcplib.Description("A comma-separated list of keywords to show metrics for.")),
			mcplib.WithNumber("keyword_list_id", mcplib.Description("The id of an existing keyword list to show metrics for.")),
			mcplib.WithString("match_mode", mcplib.Description("Keyword ideas contain the words from your query in any order (terms mode) or in the exact order they are written...")),
			mcplib.WithString("terms", mcplib.Description("All keywords ideas or keywords ideas phrased as questions.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords-explorer/matching-terms", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_explorer_overview",
			mcplib.WithDescription("Overview. Required: select, country. Optional: timeout, limit (default: 1000), order_by (plus 9 more). Returns array of KeywordsExplorerOverviewData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See the response schema for valid column identifiers, except for `volume_monthly`,...")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("volume_monthly_date_to", mcplib.Description("The end date in YYYY-MM-DD format for retrieving historical monthly search volumes in the `volume_monthly_history`...")),
			mcplib.WithString("volume_monthly_date_from", mcplib.Description("The start date in YYYY-MM-DD format for retrieving historical monthly search volumes in the `volume_monthly_history`...")),
			mcplib.WithString("target_mode", mcplib.Description("The scope of the target URL you specified.")),
			mcplib.WithString("target", mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("target_position", mcplib.Description("Filters keywords based on the ranking position of the specified `target`.")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("search_engine", mcplib.Description("[Deprecated on 5 Aug 2024].")),
			mcplib.WithString("keywords", mcplib.Description("A comma-separated list of keywords to show metrics for.")),
			mcplib.WithNumber("keyword_list_id", mcplib.Description("The id of an existing keyword list to show metrics for.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords-explorer/overview", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_explorer_related_terms",
			mcplib.WithDescription("Related terms. Required: select, country. Optional: timeout, limit (default: 1000), order_by (plus 5 more). Returns array of KeywordsExplorerRelatedTermsData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See the response schema for valid column identifiers, except for `volume_monthly`,...")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("keywords", mcplib.Description("A comma-separated list of keywords to show metrics for.")),
			mcplib.WithNumber("keyword_list_id", mcplib.Description("The id of an existing keyword list to show metrics for.")),
			mcplib.WithString("view_for", mcplib.Description("View keywords for the top 10 or top 100 ranking pages.")),
			mcplib.WithString("terms", mcplib.Description("Related keywords which top-ranking pages also rank for (`also_rank_for`), additional keywords frequently mentioned...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords-explorer/related-terms", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_explorer_search_suggestions",
			mcplib.WithDescription("Search suggestions. Required: select, country. Optional: timeout, limit (default: 1000), order_by (plus 4 more). Returns array of KeywordsExplorerSearchSuggestionsData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See the response schema for valid column identifiers, except for `volume_monthly`,...")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("search_engine", mcplib.Description("[Deprecated on 5 Aug 2024].")),
			mcplib.WithString("keywords", mcplib.Description("A comma-separated list of keywords to show metrics for.")),
			mcplib.WithNumber("keyword_list_id", mcplib.Description("The id of an existing keyword list to show metrics for.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords-explorer/search-suggestions", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_explorer_volume_by_country",
			mcplib.WithDescription("Volume by country. Required: keyword. Optional: limit, search_engine (default: google). Returns array of KeywordsExplorerVolumeByCountryData."),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("search_engine", mcplib.Description("[Deprecated on 5 Aug 2024].")),
			mcplib.WithString("keyword", mcplib.Required(), mcplib.Description("The keyword to show metrics for.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords-explorer/volume-by-country", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_explorer_volume_history",
			mcplib.WithDescription("Time-series. Volume history. Required: country, keyword. Optional: date_to, date_from. Returns array of KeywordsExplorerVolumeHistoryData."),
			mcplib.WithString("date_to", mcplib.Description("The end date of the historical period in YYYY-MM-DD format.")),
			mcplib.WithString("date_from", mcplib.Description("The start date of the historical period in YYYY-MM-DD format.")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("keyword", mcplib.Required(), mcplib.Description("The keyword to show metrics for.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords-explorer/volume-history", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("public_crawler_ip_ranges",
			mcplib.WithDescription("Crawler IP ranges. Returns array of PublicCrawlerIpRangesData. (public)"),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/public/crawler-ip-ranges", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("public_crawler_ips",
			mcplib.WithDescription("Crawler IP addresses. Returns array of PublicCrawlerIpsData. (public)"),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/public/crawler-ips", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rank_tracker_competitors_overview",
			mcplib.WithDescription("Competitors overview. Required: select, date, device, project_id. Optional: timeout, limit (default: 1000), order_by (plus 3 more). Returns array of RankTrackerCompetitorsOverviewData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See response schema for valid column identifiers.")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("date_compared", mcplib.Description("A date to compare metrics with in YYYY-MM-DD format.")),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("A date to report metrics on in YYYY-MM-DD format.")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Choose between mobile and desktop rankings.")),
			mcplib.WithNumber("project_id", mcplib.Required(), mcplib.Description("The unique identifier of the project. You can find it in the URL of your Rank Tracker project in Ahrefs:...")),
			mcplib.WithString("volume_mode", mcplib.Description("The search volume calculation mode: monthly or average. It affects volume, traffic, and traffic value.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/rank-tracker/competitors-overview", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rank_tracker_overview",
			mcplib.WithDescription("Overview. Required: select, date, device, project_id. Optional: timeout, limit (default: 1000), order_by (plus 3 more). Returns array of RankTrackerOverviewData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See response schema for valid column identifiers.")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("date_compared", mcplib.Description("A date to compare metrics with in YYYY-MM-DD format.")),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("A date to report metrics on in YYYY-MM-DD format.")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Choose between mobile and desktop rankings.")),
			mcplib.WithNumber("project_id", mcplib.Required(), mcplib.Description("The unique identifier of the project. You can find it in the URL of your Rank Tracker project in Ahrefs:...")),
			mcplib.WithString("volume_mode", mcplib.Description("The search volume calculation mode: monthly or average. It affects volume, traffic, and traffic value.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/rank-tracker/overview", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rank_tracker_serp_overview",
			mcplib.WithDescription("SERP Overview. Required: device, country, keyword, project_id. Optional: top_positions, date, location_id (plus 1 more). Returns array of RankTrackerSerpOverviewData."),
			mcplib.WithNumber("top_positions", mcplib.Description("The number of top organic SERP positions to return. If not specified, all available positions will be returned.")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Choose between mobile and desktop rankings.")),
			mcplib.WithString("date", mcplib.Description("A timestamp on which the last available SERP Overview is returned in YYYY-MM-DDThh:mm:ss format. If it is not...")),
			mcplib.WithNumber("location_id", mcplib.Description("The location ID of a tracked keyword.You can use the `management/project-keywords` endpoint to get country codes,...")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("language_code", mcplib.Description("The language code of a tracked keyword.You can use the `management/project-keywords` endpoint to get country codes,...")),
			mcplib.WithString("keyword", mcplib.Required(), mcplib.Description("The keyword to return SERP Overview for.")),
			mcplib.WithNumber("project_id", mcplib.Required(), mcplib.Description("The unique identifier of the project. You can find it in the URL of your Rank Tracker project in Ahrefs:...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/rank-tracker/serp-overview", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("serp_overview_serp_overview",
			mcplib.WithDescription("SERP Overview. Required: select, country, keyword. Optional: top_positions, date. Returns array of SerpOverviewData."),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithNumber("top_positions", mcplib.Description("The number of top organic SERP positions to return. If not specified, all available positions will be returned.")),
			mcplib.WithString("date", mcplib.Description("A timestamp on which the last available SERP Overview is returned in YYYY-MM-DDThh:mm:ss format. If it is not...")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("keyword", mcplib.Required(), mcplib.Description("The keyword to return SERP Overview for.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/serp-overview/serp-overview", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_audit_issues",
			mcplib.WithDescription("Project Issues. Required: project_id. Optional: date_compared, date. Returns array of SiteAuditIssuesData."),
			mcplib.WithString("date_compared", mcplib.Description("A timestamp in `YYYY-MM-DDThh:mm:ss` format specifying the crawl date to compare metrics with. Follows the same...")),
			mcplib.WithString("date", mcplib.Description("A timestamp in `YYYY-MM-DDThh:mm:ss` format specifying the crawl date to retrieve metrics from. Defaults to the most...")),
			mcplib.WithNumber("project_id", mcplib.Required(), mcplib.Description("The unique identifier of the project. You can find it in the URL of your Site Audit project in Ahrefs:...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-audit/issues", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_audit_page_content",
			mcplib.WithDescription("Page content. Required: select, target_url, project_id. Optional: date. Returns the SiteAuditPageContentData."),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("date", mcplib.Description("A timestamp in `YYYY-MM-DDThh:mm:ss` format specifying the crawl date to retrieve metrics from. Defaults to the most...")),
			mcplib.WithString("target_url", mcplib.Required(), mcplib.Description("The URL of the page to retrieve content for.")),
			mcplib.WithNumber("project_id", mcplib.Required(), mcplib.Description("The unique identifier of the project. Only projects with verified ownership are supported. You can find the project...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-audit/page-content", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_audit_page_explorer",
			mcplib.WithDescription("Page explorer. Required: project_id. Optional: timeout, limit (default: 1000), order_by (plus 6 more). Returns array of SiteAuditPageExplorerData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See response schema for valid column identifiers.")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("filter_mode", mcplib.Description("Indicates which pages to return compared to the previous crawl. If not specified, all URLs that match your filter...")),
			mcplib.WithString("issue_id", mcplib.Description("The unique identifier of an issue. When specified, only URLs affected by this issue are returned. You can get issue...")),
			mcplib.WithString("date_compared", mcplib.Description("A timestamp in `YYYY-MM-DDThh:mm:ss` format specifying the crawl date to compare metrics with. Follows the same...")),
			mcplib.WithString("date", mcplib.Description("A timestamp in `YYYY-MM-DDThh:mm:ss` format specifying the crawl date to retrieve metrics from. Defaults to the most...")),
			mcplib.WithNumber("project_id", mcplib.Required(), mcplib.Description("The unique identifier of the project. Only projects with verified ownership are supported. You can find the project...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-audit/page-explorer", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_audit_projects",
			mcplib.WithDescription("Project Health Scores. Optional: project_url, project_name, date (plus 1 more). Returns array of SiteAuditProjectsData."),
			mcplib.WithString("project_url", mcplib.Description("Filters projects by target URL (ignores protocol and trailing slash).")),
			mcplib.WithString("project_name", mcplib.Description("Filters projects by name.")),
			mcplib.WithString("date", mcplib.Description("A timestamp in `YYYY-MM-DDThh:mm:ss` format specifying the crawl date to retrieve metrics from. Defaults to the most...")),
			mcplib.WithNumber("project_id", mcplib.Description("The unique identifier of the project. You can find it in the URL of your Site Audit project in Ahrefs:...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-audit/projects", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_all_backlinks",
			mcplib.WithDescription("Backlinks. Required: select, target. Optional: timeout, limit (default: 1000), order_by (plus 5 more). Returns array of SiteExplorerAllBacklinksData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See the response schema for valid column identifiers, except for `link_group_count`,...")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("mode", mcplib.Description("The scope of the search based on the target you entered.")),
			mcplib.WithString("aggregation", mcplib.Description("The backlinks grouping mode.")),
			mcplib.WithString("history", mcplib.Description("A time frame to add lost backlinks to the report. Choose between `live` (no history), `since:<date>` (history since...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/all-backlinks", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_backlinks_stats",
			mcplib.WithDescription("Backlinks stats. Required: target, date. Optional: protocol (default: both), mode (default: subdomains). Returns the SiteExplorerBacklinksStatsData."),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("mode", mcplib.Description("The scope of the search based on the target you entered.")),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("A date to report metrics on in YYYY-MM-DD format.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/backlinks-stats", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_broken_backlinks",
			mcplib.WithDescription("Broken Backlinks. Required: select, target. Optional: timeout, limit (default: 1000), order_by (plus 4 more). Returns array of SiteExplorerBrokenBacklinksData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See the response schema for valid column identifiers, except for `http_code_target`,...")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("mode", mcplib.Description("The scope of the search based on the target you entered.")),
			mcplib.WithString("aggregation", mcplib.Description("The backlinks grouping mode.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/broken-backlinks", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_domain_rating",
			mcplib.WithDescription("Point-in-time snapshot. Domain rating. Required: target, date. Optional: protocol (default: both). Returns the SiteExplorerDomainRatingData."),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("A date to report metrics on in YYYY-MM-DD format.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/domain-rating", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_domain_rating_history",
			mcplib.WithDescription("Time-series. Domain Rating history. Required: date_from, target. Optional: history_grouping (default: monthly), date_to. Returns array of SiteExplorerDomainRatingHistoryData."),
			mcplib.WithString("history_grouping", mcplib.Description("The time interval used to group historical data.")),
			mcplib.WithString("date_to", mcplib.Description("The end date of the historical period in YYYY-MM-DD format.")),
			mcplib.WithString("date_from", mcplib.Required(), mcplib.Description("The start date of the historical period in YYYY-MM-DD format.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/domain-rating-history", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_metrics",
			mcplib.WithDescription("Point-in-time snapshot. Metrics. Required: target, date. Optional: volume_mode (default: monthly), country, protocol (default: both) (plus 1 more). Returns the SiteExplorerMetricsData."),
			mcplib.WithString("volume_mode", mcplib.Description("The search volume calculation mode: monthly or average. It affects volume, traffic, and traffic value.")),
			mcplib.WithString("country", mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("mode", mcplib.Description("The scope of the search based on the target you entered.")),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("A date to report metrics on in YYYY-MM-DD format.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/metrics", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_metrics_by_country",
			mcplib.WithDescription("Metrics by country. Required: target, date. Optional: select, volume_mode (default: monthly), protocol (default: both) (plus 1 more). Returns array of SiteExplorerMetricsByCountryData."),
			mcplib.WithString("select", mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("volume_mode", mcplib.Description("The search volume calculation mode: monthly or average. It affects volume, traffic, and traffic value.")),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("mode", mcplib.Description("The scope of the search based on the target you entered.")),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("A date to report metrics on in YYYY-MM-DD format.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/metrics-by-country", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_organic_competitors",
			mcplib.WithDescription("Organic competitors. Required: select, target, country, date. Optional: timeout, limit (default: 1000), order_by (plus 5 more). Returns array of SiteExplorerOrganicCompetitorsData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See response schema for valid column identifiers.")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("mode", mcplib.Description("The scope of the search based on the target you entered.")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("date_compared", mcplib.Description("A date to compare metrics with in YYYY-MM-DD format.")),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("A date to report metrics on in YYYY-MM-DD format.")),
			mcplib.WithString("volume_mode", mcplib.Description("The search volume calculation mode: monthly or average. It affects volume, traffic, and traffic value.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/organic-competitors", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_organic_keywords",
			mcplib.WithDescription("Organic keywords. Required: select, target, date. Optional: timeout, limit (default: 1000), order_by (plus 6 more). Returns array of SiteExplorerOrganicKeywordsData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See response schema for valid column identifiers.")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("mode", mcplib.Description("The scope of the search based on the target you entered.")),
			mcplib.WithString("country", mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("date_compared", mcplib.Description("A date to compare metrics with in YYYY-MM-DD format.")),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("A date to report metrics on in YYYY-MM-DD format.")),
			mcplib.WithString("volume_mode", mcplib.Description("The search volume calculation mode: monthly or average. It affects volume, traffic, and traffic value.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/organic-keywords", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_pages_by_traffic",
			mcplib.WithDescription("Pages by traffic. Required: target. Optional: volume_mode (default: monthly), country, protocol (default: both) (plus 1 more). Returns the SiteExplorerPagesByTrafficData."),
			mcplib.WithString("volume_mode", mcplib.Description("The search volume calculation mode: monthly or average. It affects volume, traffic, and traffic value.")),
			mcplib.WithString("country", mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("mode", mcplib.Description("The scope of the search based on the target you entered.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/pages-by-traffic", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_refdomains_history",
			mcplib.WithDescription("Time-series. Refdomains history. Required: date_from, target. Optional: history_grouping (default: monthly), date_to, protocol (default: both) (plus 1 more). Returns array of SiteExplorerRefdomainsHistoryData."),
			mcplib.WithString("history_grouping", mcplib.Description("The time interval used to group historical data.")),
			mcplib.WithString("date_to", mcplib.Description("The end date of the historical period in YYYY-MM-DD format.")),
			mcplib.WithString("date_from", mcplib.Required(), mcplib.Description("The start date of the historical period in YYYY-MM-DD format.")),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("mode", mcplib.Description("The scope of the search based on the target you entered.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/refdomains-history", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_explorer_top_pages",
			mcplib.WithDescription("Top pages. Required: select, target, date. Optional: timeout, limit (default: 1000), order_by (plus 6 more). Returns array of SiteExplorerTopPagesData."),
			mcplib.WithNumber("timeout", mcplib.Description("A manual timeout duration in seconds.")),
			mcplib.WithNumber("limit", mcplib.Description("The number of results to return.")),
			mcplib.WithString("order_by", mcplib.Description("A column to order results by. See response schema for valid column identifiers.")),
			mcplib.WithString("where", mcplib.Description("Filter expression. See filter-syntax.md for syntax.")),
			mcplib.WithString("select", mcplib.Required(), mcplib.Description("A comma-separated list of columns to return. See response schema for valid column identifiers.")),
			mcplib.WithString("protocol", mcplib.Description("The protocol of your target.")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("The target of the search: a domain or a URL.")),
			mcplib.WithString("mode", mcplib.Description("The scope of the search based on the target you entered.")),
			mcplib.WithString("country", mcplib.Description("A two-letter country code (ISO 3166-1 alpha-2).")),
			mcplib.WithString("date_compared", mcplib.Description("A date to compare metrics with in YYYY-MM-DD format.")),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("A date to report metrics on in YYYY-MM-DD format.")),
			mcplib.WithString("volume_mode", mcplib.Description("The search volume calculation mode: monthly or average. It affects volume, traffic, and traffic value.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/site-explorer/top-pages", []string{}),
	)
	s.AddTool(
		mcplib.NewTool("subscription_info_limits_and_usage",
			mcplib.WithDescription("Limits and usage. Returns the SubscriptionInfoLimitsAndUsageData."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/subscription-info/limits-and-usage", []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT only). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// Build path by substituting positional params
		path := pathTemplate
		for _, p := range positionalParams {
			if v, ok := args[p]; ok {
				path = strings.Replace(path, "{"+p+"}", fmt.Sprintf("%v", v), 1)
			}
		}

		// Collect non-positional params as query params
		params := make(map[string]string)
		for k, v := range args {
			isPositional := false
			for _, p := range positionalParams {
				if k == p {
					isPositional = true
					break
				}
			}
			if !isPositional {
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(args)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(args)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(args)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export AHREFS_API_KEY=<your-key>" +
					"\n      Get a key at: https://app.ahrefs.com/api" +
					"\n      Run 'ahrefs-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export AHREFS_API_KEY=<your-key>" +
					"\n      Get a key at: https://app.ahrefs.com/api" +
					"\n      Run 'ahrefs-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export AHREFS_API_KEY=<your-key>" +
					"\n      Get a key at: https://app.ahrefs.com/api" +
					"\n      Run 'ahrefs-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "ahrefs-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a DELETE/PATCH
	// followed by a GET would otherwise return the pre-mutation snapshot for up
	// to the cache TTL. Skip the cache for the MCP path; the interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "ahrefs-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenWithContext(ctx, dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	// Block write operations
	upper := strings.ToUpper(strings.TrimSpace(query))
	for _, prefix := range []string{"INSERT", "UPDATE", "DELETE", "DROP", "ALTER", "CREATE"} {
		if strings.HasPrefix(upper, prefix) {
			return mcplib.NewToolResultError("only SELECT queries are allowed"), nil
		}
	}

	db, err := store.OpenWithContext(ctx, dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "ahrefs",
		"description": "SEO and competitive intelligence API for backlinks, keywords, rank tracking, site audit, and SERP data.",
		"archetype":   "payments",
		"tool_count":  29,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion ahrefs-pp-cli binary.",
		"auth": map[string]any{
			"type":     "api_key",
			"env_vars": []string{"AHREFS_API_KEY"},
			"key_url":  "https://app.ahrefs.com/api",
		},
		"resources": []map[string]any{
			{
				"name":        "keywords_explorer",
				"description": "Keywords Explorer endpoints.",
				"endpoints":   []string{"matching_terms", "overview", "related_terms", "search_suggestions", "volume_by_country", "volume_history"},
				"searchable":  true,
			},
			{
				"name":        "public",
				"description": "Public endpoints.",
				"endpoints":   []string{"crawler_ip_ranges", "crawler_ips"},
				"syncable":    true,
			},
			{
				"name":        "rank_tracker",
				"description": "Rank Tracker endpoints.",
				"endpoints":   []string{"competitors_overview", "overview", "serp_overview"},
				"searchable":  true,
			},
			{
				"name":        "serp_overview",
				"description": "Serp Overview endpoints.",
				"endpoints":   []string{"serp_overview"},
				"searchable":  true,
			},
			{
				"name":        "site_audit",
				"description": "Site Audit endpoints.",
				"endpoints":   []string{"issues", "page_content", "page_explorer", "projects"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "site_explorer",
				"description": "Site Explorer endpoints.",
				"endpoints":   []string{"all_backlinks", "backlinks_stats", "broken_backlinks", "domain_rating", "domain_rating_history", "metrics", "metrics_by_country", "organic_competitors", "organic_keywords", "pages_by_traffic", "refdomains_history", "top_pages"},
				"searchable":  true,
			},
			{
				"name":        "subscription_info",
				"description": "Subscription Info endpoints.",
				"endpoints":   []string{"limits_and_usage"},
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		"playbook": []map[string]string{
			{"topic": "Financial data", "insight": "Always use read-only operations for financial queries. Never use create/update tools for payment data without explicit user confirmation."},
			{"topic": "Reconciliation", "insight": "For reconciliation tasks, sync first then use sql for cross-referencing. API pagination over financial records is slow and rate-limited."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
