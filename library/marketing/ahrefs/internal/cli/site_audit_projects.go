// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSiteAuditProjectsCmd(flags *rootFlags) *cobra.Command {
	var flagProjectUrl string
	var flagProjectName string
	var flagDate string
	var flagProjectId string

	cmd := &cobra.Command{
		Use:         "projects",
		Short:       "Project Health Scores",
		Example:     "  ahrefs-pp-cli site_audit projects",
		Annotations: map[string]string{"pp:endpoint": "site_audit.projects", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/site-audit/projects"
			params := map[string]string{}
			if flagProjectUrl != "" {
				params["project_url"] = fmt.Sprintf("%v", flagProjectUrl)
			}
			if flagProjectName != "" {
				params["project_name"] = fmt.Sprintf("%v", flagProjectName)
			}
			if flagDate != "" {
				params["date"] = fmt.Sprintf("%v", flagDate)
			}
			if flagProjectId != "" {
				params["project_id"] = fmt.Sprintf("%v", flagProjectId)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "site_audit", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagProjectUrl, "project-url", "", "Filters projects by target URL (ignores protocol and trailing slash).")
	cmd.Flags().StringVar(&flagProjectName, "project-name", "", "Filters projects by name.")
	cmd.Flags().StringVar(&flagDate, "date", "", "A timestamp in `YYYY-MM-DDThh:mm:ss` format specifying the crawl date to retrieve metrics from. Defaults to the most...")
	cmd.Flags().StringVar(&flagProjectId, "project-id", "", "The unique identifier of the project. You can find it in the URL of your Site Audit project in Ahrefs:...")

	return cmd
}
