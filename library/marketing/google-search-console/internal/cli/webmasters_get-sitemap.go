// Copyright 2026 Matt and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newWebmastersGetSitemapCmd(flags *rootFlags) *cobra.Command {

	cmd := &cobra.Command{
		Use:         "get-sitemap <siteUrl> <feedpath>",
		Short:       "Retrieves information about a specific sitemap, including warnings, errors, last submitted/downloaded times, and...",
		Example:     "  google-search-console-pp-cli webmasters get-sitemap https://example.com/resource example-value",
		Annotations: map[string]string{"pp:endpoint": "webmasters.get-sitemap", "pp:method": "GET", "pp:path": "/webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}"
			path = replacePathParam(path, "siteUrl", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("feedpath is required\nUsage: %s <%s>", cmd.CommandPath(), "feedpath"))
			}
			path = replacePathParam(path, "feedpath", args[1])
			params := map[string]string{}
			data, err := c.Get(path, params)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}

	return cmd
}
