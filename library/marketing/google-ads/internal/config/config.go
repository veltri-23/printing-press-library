// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package config

import (
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	"github.com/pelletier/go-toml/v2"
)

type Config struct {
	BaseURL                  string    `toml:"base_url"`
	AuthHeaderVal            string    `toml:"auth_header"`
	AuthSource               string    `toml:"-"`
	AccessToken              string    `toml:"access_token"`
	RefreshToken             string    `toml:"refresh_token"`
	TokenExpiry              time.Time `toml:"token_expiry"`
	ClientID                 string    `toml:"client_id"`
	ClientSecret             string    `toml:"client_secret"`
	Path                     string    `toml:"-"`
	GoogleAdsAccessToken     string    `toml:"ads_access_token"`
	GoogleAdsDeveloperToken  string    `toml:"ads_developer_token"`
	GoogleAdsLoginCustomerId string    `toml:"ads_login_customer_id"`
}

func Load(configPath string) (*Config, error) {
	cfg := &Config{
		BaseURL: "https://googleads.googleapis.com",
	}

	// Resolve config path
	path := configPath
	if path == "" {
		path = os.Getenv("GOOGLE_ADS_CONFIG")
	}
	if path == "" {
		home, _ := os.UserHomeDir()
		path = filepath.Join(home, ".config", "google-ads-pp-cli", "config.toml")
	}
	cfg.Path = path

	// Try to load config file
	data, err := os.ReadFile(path)
	if err == nil {
		if err := toml.Unmarshal(data, cfg); err != nil {
			return nil, fmt.Errorf("parsing config %s: %w", path, err)
		}
	}

	// Env var overrides
	if v := os.Getenv("GOOGLE_ADS_ACCESS_TOKEN"); v != "" {
		cfg.GoogleAdsAccessToken = v
		cfg.AuthSource = "env:GOOGLE_ADS_ACCESS_TOKEN"
	}
	if v := os.Getenv("GOOGLE_ADS_DEVELOPER_TOKEN"); v != "" {
		cfg.GoogleAdsDeveloperToken = v
		cfg.AuthSource = "env:GOOGLE_ADS_DEVELOPER_TOKEN"
	}
	if v := os.Getenv("GOOGLE_ADS_LOGIN_CUSTOMER_ID"); v != "" {
		cfg.GoogleAdsLoginCustomerId = v
		cfg.AuthSource = "env:GOOGLE_ADS_LOGIN_CUSTOMER_ID"
	}

	// Base URL override (used by printing-press verify to point at mock/test servers)
	if v := os.Getenv("GOOGLE_ADS_BASE_URL"); v != "" {
		cfg.BaseURL = v
	}
	return cfg, nil
}

func (c *Config) AuthHeader() string {
	if c.AuthHeaderVal != "" {
		return c.AuthHeaderVal
	}
	// Env-var token wins over file-stored AccessToken (env > config convention).
	if c.GoogleAdsAccessToken != "" {
		c.AuthSource = "env:GOOGLE_ADS_ACCESS_TOKEN"
		return applyAuthFormat("Bearer {token}", map[string]string{
			"ads_access_token":        c.GoogleAdsAccessToken,
			"GOOGLE_ADS_ACCESS_TOKEN": c.GoogleAdsAccessToken,
			"token":                   c.GoogleAdsAccessToken,
		})
	}
	if c.AccessToken != "" {
		c.AuthSource = "oauth2"
		return applyAuthFormat("Bearer {token}", map[string]string{"access_token": c.AccessToken, "token": c.AccessToken})
	}
	return ""
}

func applyAuthFormat(format string, replacements map[string]string) string {
	if format == "" {
		return ""
	}
	for key, value := range replacements {
		format = strings.ReplaceAll(format, "{"+key+"}", value)
	}
	if strings.Contains(format, "{") {
		return ""
	}
	return format
}

func (c *Config) SaveTokens(clientID, clientSecret, accessToken, refreshToken string, expiry time.Time) error {
	c.ClientID = clientID
	c.ClientSecret = clientSecret
	c.AccessToken = accessToken
	c.RefreshToken = refreshToken
	c.TokenExpiry = expiry
	return c.save()
}

func (c *Config) ClearTokens() error {
	c.AccessToken = ""
	c.RefreshToken = ""
	c.TokenExpiry = time.Time{}
	return c.save()
}

func (c *Config) save() error {
	dir := filepath.Dir(c.Path)
	if err := os.MkdirAll(dir, 0o700); err != nil {
		return fmt.Errorf("creating config dir: %w", err)
	}
	data, err := toml.Marshal(c)
	if err != nil {
		return fmt.Errorf("marshaling config: %w", err)
	}
	return os.WriteFile(c.Path, data, 0o600)
}

// Ensure strings import is used
var _ = strings.ReplaceAll
