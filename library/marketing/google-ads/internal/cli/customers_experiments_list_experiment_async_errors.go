// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCustomersExperimentsListExperimentAsyncErrorsCmd(flags *rootFlags) *cobra.Command {
	var flagPageSize int
	var flagPageToken string

	cmd := &cobra.Command{
		Use:         "list-experiment-async-errors <resourceName>",
		Short:       "Returns all errors that occurred during the last Experiment update (either scheduling or promotion). Supports...",
		Example:     "  google-ads-pp-cli customers-experiments list-experiment-async-errors example-resource",
		Annotations: map[string]string{"pp:endpoint": "customers_experiments.list_experiment_async_errors", "pp:method": "GET", "pp:path": "/v22/{resourceName}:listExperimentAsyncErrors", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v22/{resourceName}:listExperimentAsyncErrors"
			if len(args) < 2 {
				return usageErr(fmt.Errorf("resourceName is required\nUsage: %s <%s>", cmd.CommandPath(), "resourceName"))
			}
			path = replacePathParam(path, "resourceName", args[1])
			params := map[string]string{}
			if flagPageSize != 0 {
				params["pageSize"] = fmt.Sprintf("%v", flagPageSize)
			}
			if flagPageToken != "" {
				params["pageToken"] = fmt.Sprintf("%v", flagPageToken)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "customers_experiments", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of elements to retrieve in a single page. When a page request is too large, the server may decide to further...")
	cmd.Flags().StringVar(&flagPageToken, "page-token", "", "Token of the page to retrieve. If not specified, the first page of results will be returned. Use the value obtained...")

	return cmd
}
