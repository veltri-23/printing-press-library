// Copyright 2026 Greg Stellato and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/marketing/google-ad-manager/internal/cliutil"
	"github.com/spf13/cobra"
)

func newAdUnitsCreateCmd(flags *rootFlags) *cobra.Command {
	var flagAlt string
	var flagPrettyPrint bool
	var bodyAdUnitCode string
	var bodyAdUnitId string
	var bodyAdUnitSizes string
	var bodyAppliedAdsenseEnabled bool
	var bodyAppliedLabelFrequencyCaps string
	var bodyAppliedLabels string
	var bodyAppliedTargetWindow string
	var bodyAppliedTeams string
	var bodyDescription string
	var bodyDisplayName string
	var bodyEffectiveAdsenseEnabled bool
	var bodyEffectiveAppliedLabels string
	var bodyEffectiveLabelFrequencyCaps string
	var bodyEffectiveTargetWindow string
	var bodyExplicitlyTargeted bool
	var bodyExternalSetTopBoxChannelId string
	var bodyHasChildren bool
	var bodyName string
	var bodyParentAdUnit string
	var bodyParentPath string
	var bodyRefreshDelay string
	var bodySmartSizeMode string
	var bodyStatus string
	var bodyTeams string
	var bodyUpdateTime string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create <parent>",
		Short:       "API to create an `AdUnit` object.",
		Example:     "  google-ad-manager-pp-cli ad-units create example-value",
		Annotations: map[string]string{"pp:endpoint": "ad-units.create", "pp:method": "POST", "pp:path": "/v1/{parent}/adUnits"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("alt") {
				allowedAlt := []string{"json", "media", "proto"}
				validAlt := false
				for _, v := range allowedAlt {
					if flagAlt == v {
						validAlt = true
						break
					}
				}
				if !validAlt {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAlt, "alt", allowedAlt)
				}
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/{parent}/adUnits"
			path = replacePathParam(path, "parent", args[0])
			params := map[string]string{}
			if flagAlt != "" {
				params["alt"] = formatCLIParamValue(flagAlt)
			}
			if flagPrettyPrint != false {
				params["prettyPrint"] = formatCLIParamValue(flagPrettyPrint)
			}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAdUnitCode != "" {
					body["adUnitCode"] = bodyAdUnitCode
				}
				if bodyAdUnitId != "" {
					body["adUnitId"] = bodyAdUnitId
				}
				if bodyAdUnitSizes != "" {
					var parsedAdUnitSizes any
					if err := json.Unmarshal([]byte(bodyAdUnitSizes), &parsedAdUnitSizes); err != nil {
						return fmt.Errorf("parsing --ad-unit-sizes JSON: %w", err)
					}
					body["adUnitSizes"] = parsedAdUnitSizes
				}
				if cmd.Flags().Changed("applied-adsense-enabled") {
					body["appliedAdsenseEnabled"] = bodyAppliedAdsenseEnabled
				}
				if bodyAppliedLabelFrequencyCaps != "" {
					var parsedAppliedLabelFrequencyCaps any
					if err := json.Unmarshal([]byte(bodyAppliedLabelFrequencyCaps), &parsedAppliedLabelFrequencyCaps); err != nil {
						return fmt.Errorf("parsing --applied-label-frequency-caps JSON: %w", err)
					}
					body["appliedLabelFrequencyCaps"] = parsedAppliedLabelFrequencyCaps
				}
				if bodyAppliedLabels != "" {
					var parsedAppliedLabels any
					if err := json.Unmarshal([]byte(bodyAppliedLabels), &parsedAppliedLabels); err != nil {
						return fmt.Errorf("parsing --applied-labels JSON: %w", err)
					}
					body["appliedLabels"] = parsedAppliedLabels
				}
				if bodyAppliedTargetWindow != "" {
					body["appliedTargetWindow"] = bodyAppliedTargetWindow
				}
				if bodyAppliedTeams != "" {
					body["appliedTeams"] = cliutil.SplitCSV(bodyAppliedTeams)
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDisplayName != "" {
					body["displayName"] = bodyDisplayName
				}
				if cmd.Flags().Changed("effective-adsense-enabled") {
					body["effectiveAdsenseEnabled"] = bodyEffectiveAdsenseEnabled
				}
				if bodyEffectiveAppliedLabels != "" {
					var parsedEffectiveAppliedLabels any
					if err := json.Unmarshal([]byte(bodyEffectiveAppliedLabels), &parsedEffectiveAppliedLabels); err != nil {
						return fmt.Errorf("parsing --effective-applied-labels JSON: %w", err)
					}
					body["effectiveAppliedLabels"] = parsedEffectiveAppliedLabels
				}
				if bodyEffectiveLabelFrequencyCaps != "" {
					var parsedEffectiveLabelFrequencyCaps any
					if err := json.Unmarshal([]byte(bodyEffectiveLabelFrequencyCaps), &parsedEffectiveLabelFrequencyCaps); err != nil {
						return fmt.Errorf("parsing --effective-label-frequency-caps JSON: %w", err)
					}
					body["effectiveLabelFrequencyCaps"] = parsedEffectiveLabelFrequencyCaps
				}
				if bodyEffectiveTargetWindow != "" {
					body["effectiveTargetWindow"] = bodyEffectiveTargetWindow
				}
				if cmd.Flags().Changed("explicitly-targeted") {
					body["explicitlyTargeted"] = bodyExplicitlyTargeted
				}
				if bodyExternalSetTopBoxChannelId != "" {
					body["externalSetTopBoxChannelId"] = bodyExternalSetTopBoxChannelId
				}
				if cmd.Flags().Changed("has-children") {
					body["hasChildren"] = bodyHasChildren
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyParentAdUnit != "" {
					body["parentAdUnit"] = bodyParentAdUnit
				}
				if bodyParentPath != "" {
					var parsedParentPath any
					if err := json.Unmarshal([]byte(bodyParentPath), &parsedParentPath); err != nil {
						return fmt.Errorf("parsing --parent-path JSON: %w", err)
					}
					body["parentPath"] = parsedParentPath
				}
				if bodyRefreshDelay != "" {
					body["refreshDelay"] = bodyRefreshDelay
				}
				if bodySmartSizeMode != "" {
					body["smartSizeMode"] = bodySmartSizeMode
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyTeams != "" {
					body["teams"] = cliutil.SplitCSV(bodyTeams)
				}
				if bodyUpdateTime != "" {
					body["updateTime"] = bodyUpdateTime
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "ad-units", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "ad-units", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ad-units", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ad-units", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ad-units", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "ad-units",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ad-units", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ad-units", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagAlt, "alt", "json", "Data format for response. (one of: json, media, proto)")
	cmd.Flags().BoolVar(&flagPrettyPrint, "pretty-print", true, "Returns response with indentations and line breaks.")
	cmd.Flags().StringVar(&bodyAdUnitCode, "ad-unit-code", "", "Optional. Immutable. A string used to uniquely identify the ad unit for the purposes of serving the ad.")
	cmd.Flags().StringVar(&bodyAdUnitId, "ad-unit-id", "", "Output only. Deprecated: AdUnit ID.")
	cmd.Flags().StringVar(&bodyAdUnitSizes, "ad-unit-sizes", "", "Optional. The sizes that can be served inside this ad unit.")
	cmd.Flags().BoolVar(&bodyAppliedAdsenseEnabled, "applied-adsense-enabled", false, "Optional. The value of AdSense enabled directly applied to this ad unit.")
	cmd.Flags().StringVar(&bodyAppliedLabelFrequencyCaps, "applied-label-frequency-caps", "", "Optional. The set of label frequency caps applied directly to this ad unit.")
	cmd.Flags().StringVar(&bodyAppliedLabels, "applied-labels", "", "Optional. The set of labels applied directly to this ad unit.")
	cmd.Flags().StringVar(&bodyAppliedTargetWindow, "applied-target-window", "", "Optional. The target window directly applied to this AdUnit.")
	cmd.Flags().StringVar(&bodyAppliedTeams, "applied-teams", "", "Optional. The resource names of Teams directly applied to this AdUnit. Format: 'networks/{network_code}/teams/{team_id}'")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Optional. A description of the ad unit. The maximum length is 65,535 characters.")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "Required. The display name of the ad unit. Its maximum length is 255 characters.")
	cmd.Flags().BoolVar(&bodyEffectiveAdsenseEnabled, "effective-adsense-enabled", false, "Output only. Specifies whether or not the AdUnit is enabled for serving ads from the AdSense content network.")
	cmd.Flags().StringVar(&bodyEffectiveAppliedLabels, "effective-applied-labels", "", "Output only.")
	cmd.Flags().StringVar(&bodyEffectiveLabelFrequencyCaps, "effective-label-frequency-caps", "", "Output only. The label frequency caps applied directly to the ad unit as well as those inherited from parent ad units.")
	cmd.Flags().StringVar(&bodyEffectiveTargetWindow, "effective-target-window", "", "Output only. Non-empty default. The target window of this AdUnit.")
	cmd.Flags().BoolVar(&bodyExplicitlyTargeted, "explicitly-targeted", false, "Optional. If this field is set to true, then the AdUnit will not be implicitly targeted when its parent is.")
	cmd.Flags().StringVar(&bodyExternalSetTopBoxChannelId, "external-set-top-box-channel-id", "", "Optional.")
	cmd.Flags().BoolVar(&bodyHasChildren, "has-children", false, "Output only. This field is set to true if the ad unit has any children.")
	cmd.Flags().StringVar(&bodyName, "name", "", "Identifier. The resource name of the AdUnit. Format: `networks/{network_code}/adUnits/{ad_unit_id}`")
	cmd.Flags().StringVar(&bodyParentAdUnit, "parent-ad-unit", "", "Required. Immutable. The AdUnit's parent.")
	cmd.Flags().StringVar(&bodyParentPath, "parent-path", "", "Output only.")
	cmd.Flags().StringVar(&bodyRefreshDelay, "refresh-delay", "", "Optional. The duration after which an Ad Unit will automatically refresh.")
	cmd.Flags().StringVar(&bodySmartSizeMode, "smart-size-mode", "", "Optional. Non-empty default. The smart size mode for this ad unit. This attribute defaults to SmartSizeMode.")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Output only. The status of this ad unit. It defaults to ACTIVE.")
	cmd.Flags().StringVar(&bodyTeams, "teams", "", "Output only. The resource names of all Teams that this AdUnit is on as well as those inherited from parent AdUnits.")
	cmd.Flags().StringVar(&bodyUpdateTime, "update-time", "", "Output only. The time this AdUnit was last modified.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
