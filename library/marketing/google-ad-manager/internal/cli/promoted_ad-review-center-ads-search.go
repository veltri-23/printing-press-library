// Copyright 2026 Greg Stellato and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAdReviewCenterAdsSearchPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagAlt string
	var flagPrettyPrint bool
	var flagAdReviewCenterAdId string
	var flagBuyerAccountId string
	var flagDateTimeRangeEndTime string
	var flagDateTimeRangeStartTime string
	var flagManualReviewStatus string
	var flagPageSize int
	var flagPageToken string
	var flagSearchText string
	var flagStatus string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "ad-review-center-ads-search <parent>",
		Short:       "API to search for AdReviewCenterAds.",
		Long:        "API to search for AdReviewCenterAds.",
		Example:     "  google-ad-manager-pp-cli ad-review-center-ads-search example-value",
		Annotations: map[string]string{"pp:endpoint": "ad-review-center-ads-search.admanager-networks-web-properties", "pp:method": "GET", "pp:path": "/v1/{parent}/adReviewCenterAds:search", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("alt") {
				allowedAlt := []string{"json", "media", "proto"}
				validAlt := false
				for _, v := range allowedAlt {
					if flagAlt == v {
						validAlt = true
						break
					}
				}
				if !validAlt {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAlt, "alt", allowedAlt)
				}
			}
			if cmd.Flags().Changed("manual-review-status") {
				allowedManualReviewStatus := []string{"MANUAL_AD_REVIEW_CENTER_AD_STATUS_UNSPECIFIED", "ALLOWED", "BLOCKED", "ARCHIVED", "PENDING", "SERVING"}
				validManualReviewStatus := false
				for _, v := range allowedManualReviewStatus {
					if flagManualReviewStatus == v {
						validManualReviewStatus = true
						break
					}
				}
				if !validManualReviewStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagManualReviewStatus, "manual-review-status", allowedManualReviewStatus)
				}
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"AD_REVIEW_CENTER_AD_STATUS_UNSPECIFIED", "ALLOWED", "BLOCKED", "UNREVIEWED"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStatus, "status", allowedStatus)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/{parent}/adReviewCenterAds:search"
			if len(args) < 1 {
				// JSON envelope: {error, usage}. Written first; the
				// usageErr return preserves exit code 2 across modes.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "parent is required",
						"usage": fmt.Sprintf("%s <%s>", cmd.CommandPath(), "parent"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("parent is required\nUsage: %s <%s>", cmd.CommandPath(), "parent"))
			}
			path = replacePathParam(path, "parent", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "ad-review-center-ads-search", path, map[string]string{
				"alt":                     formatCLIParamValue(flagAlt),
				"prettyPrint":             formatCLIParamValue(flagPrettyPrint),
				"adReviewCenterAdId":      formatCLIParamValue(flagAdReviewCenterAdId),
				"buyerAccountId":          formatCLIParamValue(flagBuyerAccountId),
				"dateTimeRange.endTime":   formatCLIParamValue(flagDateTimeRangeEndTime),
				"dateTimeRange.startTime": formatCLIParamValue(flagDateTimeRangeStartTime),
				"manualReviewStatus":      formatCLIParamValue(flagManualReviewStatus),
				"pageSize":                formatCLIParamValue(flagPageSize),
				"pageToken":               formatCLIParamValue(flagPageToken),
				"searchText":              formatCLIParamValue(flagSearchText),
				"status":                  formatCLIParamValue(flagStatus),
			}, nil, flagAll, "pageToken", "page_token", "pageSize", "nextPageToken", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAlt, "alt", "json", "Data format for response. (one of: json, media, proto)")
	cmd.Flags().BoolVar(&flagPrettyPrint, "pretty-print", true, "Returns response with indentations and line breaks.")
	cmd.Flags().StringVar(&flagAdReviewCenterAdId, "ad-review-center-ad-id", "", "Optional. Only return ads with the given AdReviewCenterAd IDs.")
	cmd.Flags().StringVar(&flagBuyerAccountId, "buyer-account-id", "", "Optional. If provided, restrict the search to creatives belonging to one of the given Adx buyer account IDs.")
	cmd.Flags().StringVar(&flagDateTimeRangeEndTime, "date-time-range-end-time", "", "Optional. Exclusive end of the interval.")
	cmd.Flags().StringVar(&flagDateTimeRangeStartTime, "date-time-range-start-time", "", "Optional. Inclusive start of the interval.")
	cmd.Flags().StringVar(&flagManualReviewStatus, "manual-review-status", "", "Optional. Only return ads with the given manual review status. (one of: MANUAL_AD_REVIEW_CENTER_AD_STATUS_UNSPECIFIED, ALLOWED, BLOCKED, ARCHIVED, PENDING, SERVING)")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Optional. The maximum number of AdReviewCenterAds to return. The service may return fewer than this value.")
	cmd.Flags().StringVar(&flagPageToken, "page-token", "", "Optional. The page token to fetch the next page of AdReviewCenterAds.")
	cmd.Flags().StringVar(&flagSearchText, "search-text", "", "Optional.")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Optional. Only return ads with the given status. (one of: AD_REVIEW_CENTER_AD_STATUS_UNSPECIFIED, ALLOWED, BLOCKED, UNREVIEWED)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
