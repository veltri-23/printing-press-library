// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newEventsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagEvent string
	var flagDomain string
	var flagKey string
	var flagLinkId string
	var flagExternalId string
	var flagTenantId string
	var flagTagId string
	var flagFolderId string
	var flagGroupId string
	var flagPartnerId string
	var flagCustomerId string
	var flagInterval string
	var flagStart string
	var flagEnd string
	var flagTimezone string
	var flagCountry string
	var flagCity string
	var flagRegion string
	var flagContinent string
	var flagDevice string
	var flagBrowser string
	var flagOs string
	var flagTrigger string
	var flagReferer string
	var flagRefererUrl string
	var flagUrl string
	var flagUtmSource string
	var flagUtmMedium string
	var flagUtmCampaign string
	var flagUtmTerm string
	var flagUtmContent string
	var flagRoot bool
	var flagSaleType string
	var flagQuery string
	var flagProgramId string
	var flagTagIds string
	var flagQr bool
	var flagPage string
	var flagLimit float64
	var flagSortOrder string
	var flagSortBy string
	var flagOrder string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "events",
		Short:       "Retrieve a paginated list of events for the authenticated workspace.",
		Long:        "Shortcut for 'events list'. Retrieve a paginated list of events for the authenticated workspace.",
		Example:     "  dub-pp-cli events",
		Annotations: map[string]string{"pp:endpoint": "events.list", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("event") {
				allowedEvent := []string{"clicks", "leads", "sales"}
				validEvent := false
				for _, v := range allowedEvent {
					if flagEvent == v {
						validEvent = true
						break
					}
				}
				if !validEvent {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "event", flagEvent, allowedEvent)
				}
			}
			if cmd.Flags().Changed("interval") {
				allowedInterval := []string{"24h", "7d", "30d", "90d", "1y", "mtd", "qtd", "ytd", "all"}
				validInterval := false
				for _, v := range allowedInterval {
					if flagInterval == v {
						validInterval = true
						break
					}
				}
				if !validInterval {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "interval", flagInterval, allowedInterval)
				}
			}
			if cmd.Flags().Changed("sale-type") {
				allowedSaleType := []string{"new", "recurring"}
				validSaleType := false
				for _, v := range allowedSaleType {
					if flagSaleType == v {
						validSaleType = true
						break
					}
				}
				if !validSaleType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sale-type", flagSaleType, allowedSaleType)
				}
			}
			if cmd.Flags().Changed("sort-order") {
				allowedSortOrder := []string{"asc", "desc"}
				validSortOrder := false
				for _, v := range allowedSortOrder {
					if flagSortOrder == v {
						validSortOrder = true
						break
					}
				}
				if !validSortOrder {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-order", flagSortOrder, allowedSortOrder)
				}
			}
			if cmd.Flags().Changed("sort-by") {
				allowedSortBy := []string{"timestamp"}
				validSortBy := false
				for _, v := range allowedSortBy {
					if flagSortBy == v {
						validSortBy = true
						break
					}
				}
				if !validSortBy {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-by", flagSortBy, allowedSortBy)
				}
			}
			if cmd.Flags().Changed("order") {
				allowedOrder := []string{"asc", "desc"}
				validOrder := false
				for _, v := range allowedOrder {
					if flagOrder == v {
						validOrder = true
						break
					}
				}
				if !validOrder {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "order", flagOrder, allowedOrder)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/events"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "events", path, map[string]string{
				"event":        fmt.Sprintf("%v", flagEvent),
				"domain":       fmt.Sprintf("%v", flagDomain),
				"key":          fmt.Sprintf("%v", flagKey),
				"linkId":       fmt.Sprintf("%v", flagLinkId),
				"externalId":   fmt.Sprintf("%v", flagExternalId),
				"tenantId":     fmt.Sprintf("%v", flagTenantId),
				"tagId":        fmt.Sprintf("%v", flagTagId),
				"folderId":     fmt.Sprintf("%v", flagFolderId),
				"groupId":      fmt.Sprintf("%v", flagGroupId),
				"partnerId":    fmt.Sprintf("%v", flagPartnerId),
				"customerId":   fmt.Sprintf("%v", flagCustomerId),
				"interval":     fmt.Sprintf("%v", flagInterval),
				"start":        fmt.Sprintf("%v", flagStart),
				"end":          fmt.Sprintf("%v", flagEnd),
				"timezone":     fmt.Sprintf("%v", flagTimezone),
				"country":      fmt.Sprintf("%v", flagCountry),
				"city":         fmt.Sprintf("%v", flagCity),
				"region":       fmt.Sprintf("%v", flagRegion),
				"continent":    fmt.Sprintf("%v", flagContinent),
				"device":       fmt.Sprintf("%v", flagDevice),
				"browser":      fmt.Sprintf("%v", flagBrowser),
				"os":           fmt.Sprintf("%v", flagOs),
				"trigger":      fmt.Sprintf("%v", flagTrigger),
				"referer":      fmt.Sprintf("%v", flagReferer),
				"refererUrl":   fmt.Sprintf("%v", flagRefererUrl),
				"url":          fmt.Sprintf("%v", flagUrl),
				"utm_source":   fmt.Sprintf("%v", flagUtmSource),
				"utm_medium":   fmt.Sprintf("%v", flagUtmMedium),
				"utm_campaign": fmt.Sprintf("%v", flagUtmCampaign),
				"utm_term":     fmt.Sprintf("%v", flagUtmTerm),
				"utm_content":  fmt.Sprintf("%v", flagUtmContent),
				"root":         fmt.Sprintf("%v", flagRoot),
				"saleType":     fmt.Sprintf("%v", flagSaleType),
				"query":        fmt.Sprintf("%v", flagQuery),
				"programId":    fmt.Sprintf("%v", flagProgramId),
				"tagIds":       fmt.Sprintf("%v", flagTagIds),
				"qr":           fmt.Sprintf("%v", flagQr),
				"page":         fmt.Sprintf("%v", flagPage),
				"limit":        fmt.Sprintf("%v", flagLimit),
				"sortOrder":    fmt.Sprintf("%v", flagSortOrder),
				"sortBy":       fmt.Sprintf("%v", flagSortBy),
				"order":        fmt.Sprintf("%v", flagOrder),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagEvent, "event", "clicks", "The type of event to retrieve analytics for. Defaults to 'clicks'. (one of: clicks, leads, sales)")
	cmd.Flags().StringVar(&flagDomain, "domain", "", "The domain to filter analytics for. Supports advanced filtering: single value, multiple values (comma-separated), or...")
	cmd.Flags().StringVar(&flagKey, "key", "", "The slug of the short link to retrieve analytics for. Must be used along with the corresponding `domain` of the...")
	cmd.Flags().StringVar(&flagLinkId, "link-id", "", "The unique ID of the link to retrieve analytics for.Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagExternalId, "external-id", "", "The ID of the link in the your database. Must be prefixed with 'ext_' when passed as a query parameter.")
	cmd.Flags().StringVar(&flagTenantId, "tenant-id", "", "The ID of the tenant that created the link inside your system. Supports advanced filtering: single value, multiple...")
	cmd.Flags().StringVar(&flagTagId, "tag-id", "", "The tag ID to retrieve analytics for. Supports advanced filtering: single value, multiple values (comma-separated),...")
	cmd.Flags().StringVar(&flagFolderId, "folder-id", "", "The folder ID to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagGroupId, "group-id", "", "The group ID to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagPartnerId, "partner-id", "", "The ID of the partner to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagCustomerId, "customer-id", "", "The ID of the customer to retrieve analytics for.")
	cmd.Flags().StringVar(&flagInterval, "interval", "", "The interval to retrieve analytics for. If undefined, defaults to 24h. (one of: 24h, 7d, 30d, 90d, 1y, mtd, qtd, ytd, all)")
	cmd.Flags().StringVar(&flagStart, "start", "", "The start date and time when to retrieve analytics from. If set, takes precedence over `interval`.")
	cmd.Flags().StringVar(&flagEnd, "end", "", "The end date and time when to retrieve analytics from. If not provided, defaults to the current date. If set along...")
	cmd.Flags().StringVar(&flagTimezone, "timezone", "UTC", "The IANA time zone code for aligning timeseries granularity (e.g. America/New_York). Defaults to UTC.")
	cmd.Flags().StringVar(&flagCountry, "country", "", "The country to retrieve analytics for. Must be passed as a 2-letter ISO 3166-1 country code (see https://d.to/geo)....")
	cmd.Flags().StringVar(&flagCity, "city", "", "The city to retrieve analytics for. Supports advanced filtering: single value, multiple values (comma-separated), or...")
	cmd.Flags().StringVar(&flagRegion, "region", "", "The ISO 3166-2 region code to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagContinent, "continent", "", "The continent to retrieve analytics for. Valid values: AF, AN, AS, EU, NA, OC, SA. Supports advanced filtering:...")
	cmd.Flags().StringVar(&flagDevice, "device", "", "The device to retrieve analytics for. Supports advanced filtering: single value, multiple values (comma-separated),...")
	cmd.Flags().StringVar(&flagBrowser, "browser", "", "The browser to retrieve analytics for. Supports advanced filtering: single value, multiple values (comma-separated),...")
	cmd.Flags().StringVar(&flagOs, "os", "", "The OS to retrieve analytics for. Supports advanced filtering: single value, multiple values (comma-separated), or...")
	cmd.Flags().StringVar(&flagTrigger, "trigger", "", "The trigger to retrieve analytics for. Valid values: qr, link, pageview. Supports advanced filtering: single value,...")
	cmd.Flags().StringVar(&flagReferer, "referer", "", "The referer hostname to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagRefererUrl, "referer-url", "", "The full referer URL to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagUrl, "url", "", "The destination URL to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagUtmSource, "utm-source", "", "The UTM source to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagUtmMedium, "utm-medium", "", "The UTM medium to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagUtmCampaign, "utm-campaign", "", "The UTM campaign to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagUtmTerm, "utm-term", "", "The UTM term to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().StringVar(&flagUtmContent, "utm-content", "", "The UTM content to retrieve analytics for. Supports advanced filtering: single value, multiple values...")
	cmd.Flags().BoolVar(&flagRoot, "root", false, "Filter for root domains. If true, filter for domains only. If false, filter for links only. If undefined, return both.")
	cmd.Flags().StringVar(&flagSaleType, "sale-type", "", "Filter sales by type: 'new' for first-time purchases, 'recurring' for repeat purchases. If undefined, returns both. (one of: new, recurring)")
	cmd.Flags().StringVar(&flagQuery, "query", "", "Search the events by a custom metadata value. Only available for lead and sale events. Examples:...")
	cmd.Flags().StringVar(&flagProgramId, "program-id", "", "Deprecated: This is automatically inferred from your workspace's defaultProgramId. The ID of the program to retrieve...")
	cmd.Flags().StringVar(&flagTagIds, "tag-ids", "", "Deprecated: Use `tagId` instead. The tag IDs to retrieve analytics for.")
	cmd.Flags().BoolVar(&flagQr, "qr", false, "Deprecated: Use the `trigger` field instead. Filter for QR code scans. If true, filter for QR codes only. If false,...")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Page")
	cmd.Flags().Float64Var(&flagLimit, "limit", 100.000000, "Limit")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "desc", "The sort order. The default is `desc`. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagSortBy, "sort-by", "timestamp", "The field to sort the events by. The default is `timestamp`. (one of: timestamp)")
	cmd.Flags().StringVar(&flagOrder, "order", "desc", "DEPRECATED. Use `sortOrder` instead. (one of: asc, desc)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
