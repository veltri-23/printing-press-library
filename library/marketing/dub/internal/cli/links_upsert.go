// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newLinksUpsertCmd(flags *rootFlags) *cobra.Command {
	var bodyAndroid string
	var bodyArchived bool
	var bodyComments string
	var bodyDescription string
	var bodyDoIndex bool
	var bodyDomain string
	var bodyExpiredUrl string
	var bodyExpiresAt string
	var bodyExternalId string
	var bodyFolderId string
	var bodyImage string
	var bodyIos string
	var bodyKey string
	var bodyKeyLength float64
	var bodyPartnerId string
	var bodyPassword string
	var bodyPrefix string
	var bodyProgramId string
	var bodyProxy bool
	var bodyPublicStats bool
	var bodyRef string
	var bodyRewrite bool
	var bodyTagId string
	var bodyTagIds string
	var bodyTagNames string
	var bodyTenantId string
	var bodyTestCompletedAt string
	var bodyTestStartedAt string
	var bodyTitle string
	var bodyTrackConversion bool
	var bodyUrl string
	var bodyUtmCampaign string
	var bodyUtmContent string
	var bodyUtmMedium string
	var bodyUtmSource string
	var bodyUtmTerm string
	var bodyVideo string
	var bodyWebhookIds string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "upsert",
		Short:       "Upsert a link for the authenticated workspace by its URL. If a link with the same URL already exists, return it (or...",
		Example:     "  dub-pp-cli links upsert --url https://example.com/resource",
		Annotations: map[string]string{"pp:endpoint": "links.upsert"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("url") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "url")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/links/upsert"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAndroid != "" {
					body["android"] = bodyAndroid
				}
				if bodyArchived != false {
					body["archived"] = bodyArchived
				}
				if bodyComments != "" {
					body["comments"] = bodyComments
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDoIndex != false {
					body["doIndex"] = bodyDoIndex
				}
				if bodyDomain != "" {
					body["domain"] = bodyDomain
				}
				if bodyExpiredUrl != "" {
					body["expiredUrl"] = bodyExpiredUrl
				}
				if bodyExpiresAt != "" {
					body["expiresAt"] = bodyExpiresAt
				}
				if bodyExternalId != "" {
					body["externalId"] = bodyExternalId
				}
				if bodyFolderId != "" {
					body["folderId"] = bodyFolderId
				}
				if bodyImage != "" {
					body["image"] = bodyImage
				}
				if bodyIos != "" {
					body["ios"] = bodyIos
				}
				if bodyKey != "" {
					body["key"] = bodyKey
				}
				if bodyKeyLength != 0.0 {
					body["keyLength"] = bodyKeyLength
				}
				if bodyPartnerId != "" {
					body["partnerId"] = bodyPartnerId
				}
				if bodyPassword != "" {
					body["password"] = bodyPassword
				}
				if bodyPrefix != "" {
					body["prefix"] = bodyPrefix
				}
				if bodyProgramId != "" {
					body["programId"] = bodyProgramId
				}
				if bodyProxy != false {
					body["proxy"] = bodyProxy
				}
				if bodyPublicStats != false {
					body["publicStats"] = bodyPublicStats
				}
				if bodyRef != "" {
					body["ref"] = bodyRef
				}
				if bodyRewrite != false {
					body["rewrite"] = bodyRewrite
				}
				if bodyTagId != "" {
					body["tagId"] = bodyTagId
				}
				if bodyTagIds != "" {
					body["tagIds"] = bodyTagIds
				}
				if bodyTagNames != "" {
					body["tagNames"] = bodyTagNames
				}
				if bodyTenantId != "" {
					body["tenantId"] = bodyTenantId
				}
				if bodyTestCompletedAt != "" {
					body["testCompletedAt"] = bodyTestCompletedAt
				}
				if bodyTestStartedAt != "" {
					body["testStartedAt"] = bodyTestStartedAt
				}
				if bodyTitle != "" {
					body["title"] = bodyTitle
				}
				if bodyTrackConversion != false {
					body["trackConversion"] = bodyTrackConversion
				}
				if bodyUrl != "" {
					body["url"] = bodyUrl
				}
				if bodyUtmCampaign != "" {
					body["utm_campaign"] = bodyUtmCampaign
				}
				if bodyUtmContent != "" {
					body["utm_content"] = bodyUtmContent
				}
				if bodyUtmMedium != "" {
					body["utm_medium"] = bodyUtmMedium
				}
				if bodyUtmSource != "" {
					body["utm_source"] = bodyUtmSource
				}
				if bodyUtmTerm != "" {
					body["utm_term"] = bodyUtmTerm
				}
				if bodyVideo != "" {
					body["video"] = bodyVideo
				}
				if bodyWebhookIds != "" {
					var parsedWebhookIds any
					if err := json.Unmarshal([]byte(bodyWebhookIds), &parsedWebhookIds); err != nil {
						return fmt.Errorf("parsing --webhook-ids JSON: %w", err)
					}
					body["webhookIds"] = parsedWebhookIds
				}
			}
			data, statusCode, err := c.Put(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "links",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAndroid, "android", "", "The Android destination URL for the short link for Android device targeting.")
	cmd.Flags().BoolVar(&bodyArchived, "archived", false, "Whether the short link is archived. Defaults to `false` if not provided.")
	cmd.Flags().StringVar(&bodyComments, "comments", "", "The comments for the short link.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "The custom link preview description (og:description). Will be used for Custom Link Previews if `proxy` is true....")
	cmd.Flags().BoolVar(&bodyDoIndex, "do-index", false, "Allow search engines to index your short link. Defaults to `false` if not provided. Learn more: https://d.to/noindex")
	cmd.Flags().StringVar(&bodyDomain, "domain", "", "The domain of the short link (without protocol). If not provided, the primary domain for the workspace will be used...")
	cmd.Flags().StringVar(&bodyExpiredUrl, "expired-url", "", "The URL to redirect to when the short link has expired.")
	cmd.Flags().StringVar(&bodyExpiresAt, "expires-at", "", "The date and time when the short link will expire at.")
	cmd.Flags().StringVar(&bodyExternalId, "external-id", "", "The ID of the link in your database. If set, it can be used to identify the link in future API requests (must be...")
	cmd.Flags().StringVar(&bodyFolderId, "folder-id", "", "The unique ID existing folder to assign the short link to.")
	cmd.Flags().StringVar(&bodyImage, "image", "", "The custom link preview image (og:image). Will be used for Custom Link Previews if `proxy` is true. Learn more:...")
	cmd.Flags().StringVar(&bodyIos, "ios", "", "The iOS destination URL for the short link for iOS device targeting.")
	cmd.Flags().StringVar(&bodyKey, "key", "", "The short link slug. If not provided, a random 7-character slug will be generated.")
	cmd.Flags().Float64Var(&bodyKeyLength, "key-length", 0.0, "The length of the short link slug. Defaults to 7 if not provided. When used with `prefix`, the total length of the...")
	cmd.Flags().StringVar(&bodyPartnerId, "partner-id", "", "The ID of the partner the short link is associated with.")
	cmd.Flags().StringVar(&bodyPassword, "password", "", "The password required to access the destination URL of the short link.")
	cmd.Flags().StringVar(&bodyPrefix, "prefix", "", "The prefix of the short link slug for randomly-generated keys (e.g. if prefix is `/c/`, generated keys will be in...")
	cmd.Flags().StringVar(&bodyProgramId, "program-id", "", "The ID of the program the short link is associated with.")
	cmd.Flags().BoolVar(&bodyProxy, "proxy", false, "Whether the short link uses Custom Link Previews feature. Defaults to `false` if not provided.")
	cmd.Flags().BoolVar(&bodyPublicStats, "public-stats", false, "Deprecated: Use `dashboard` instead. Whether the short link's stats are publicly accessible. Defaults to `false` if...")
	cmd.Flags().StringVar(&bodyRef, "ref", "", "The referral tag of the short link. If set, this will populate or override the `ref` query parameter in the...")
	cmd.Flags().BoolVar(&bodyRewrite, "rewrite", false, "Whether the short link uses link cloaking. Defaults to `false` if not provided.")
	cmd.Flags().StringVar(&bodyTagId, "tag-id", "", "Deprecated: Use `tagIds` instead. The unique ID of the tag assigned to the short link.")
	cmd.Flags().StringVar(&bodyTagIds, "tag-ids", "", "The unique IDs of the tags assigned to the short link.")
	cmd.Flags().StringVar(&bodyTagNames, "tag-names", "", "The unique name of the tags assigned to the short link (case insensitive).")
	cmd.Flags().StringVar(&bodyTenantId, "tenant-id", "", "The ID of the tenant that created the link inside your system. If set, it can be used to fetch all links for a tenant.")
	cmd.Flags().StringVar(&bodyTestCompletedAt, "test-completed-at", "", "The date and time when the tests were or will be completed.")
	cmd.Flags().StringVar(&bodyTestStartedAt, "test-started-at", "", "The date and time when the tests started.")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "The custom link preview title (og:title). Will be used for Custom Link Previews if `proxy` is true. Learn more:...")
	cmd.Flags().BoolVar(&bodyTrackConversion, "track-conversion", false, "Whether to track conversions for the short link. Defaults to `false` if not provided.")
	cmd.Flags().StringVar(&bodyUrl, "url", "", "The destination URL of the short link.")
	cmd.Flags().StringVar(&bodyUtmCampaign, "utm-campaign", "", "The UTM campaign of the short link. If set, this will populate or override the UTM campaign in the destination URL.")
	cmd.Flags().StringVar(&bodyUtmContent, "utm-content", "", "The UTM content of the short link. If set, this will populate or override the UTM content in the destination URL.")
	cmd.Flags().StringVar(&bodyUtmMedium, "utm-medium", "", "The UTM medium of the short link. If set, this will populate or override the UTM medium in the destination URL.")
	cmd.Flags().StringVar(&bodyUtmSource, "utm-source", "", "The UTM source of the short link. If set, this will populate or override the UTM source in the destination URL.")
	cmd.Flags().StringVar(&bodyUtmTerm, "utm-term", "", "The UTM term of the short link. If set, this will populate or override the UTM term in the destination URL.")
	cmd.Flags().StringVar(&bodyVideo, "video", "", "The custom link preview video (og:video). Will be used for Custom Link Previews if `proxy` is true. Learn more:...")
	cmd.Flags().StringVar(&bodyWebhookIds, "webhook-ids", "", "An array of webhook IDs to trigger when the link is clicked. These webhooks will receive click event data.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
