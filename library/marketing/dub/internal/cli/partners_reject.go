// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newPartnersRejectCmd(flags *rootFlags) *cobra.Command {
	var bodyAllowImmediateReapply bool
	var bodyPartnerId string
	var bodyRejectionNote string
	var bodyRejectionReason string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "reject",
		Short:       "Reject a pending partner application to your program. The partner will be notified via email that their application...",
		Example:     "  dub-pp-cli partners reject --partnerId example-value",
		Annotations: map[string]string{"pp:endpoint": "partners.reject"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("partner-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "partner-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/partners/applications/reject"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAllowImmediateReapply != false {
					body["allowImmediateReapply"] = bodyAllowImmediateReapply
				}
				if bodyPartnerId != "" {
					body["partnerId"] = bodyPartnerId
				}
				if bodyRejectionNote != "" {
					body["rejectionNote"] = bodyRejectionNote
				}
				if bodyRejectionReason != "" {
					body["rejectionReason"] = bodyRejectionReason
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "partners",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAllowImmediateReapply, "allow-immediate-reapply", false, "When true, pending enrollment is removed so the partner can submit a new application immediately.")
	cmd.Flags().StringVar(&bodyPartnerId, "partner-id", "", "The ID of the partner to reject.")
	cmd.Flags().StringVar(&bodyRejectionNote, "rejection-note", "", "Additional details about the rejection. This will be shared with the partner via email.")
	cmd.Flags().StringVar(&bodyRejectionReason, "rejection-reason", "", "The reason for rejecting the partner application. This will be shared with the partner via email.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
