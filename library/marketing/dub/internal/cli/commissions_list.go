// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCommissionsListCmd(flags *rootFlags) *cobra.Command {
	var flagType string
	var flagCustomerId string
	var flagPayoutId string
	var flagPartnerId string
	var flagTenantId string
	var flagGroupId string
	var flagInvoiceId string
	var flagStatus string
	var flagSortBy string
	var flagSortOrder string
	var flagInterval string
	var flagStart string
	var flagEnd string
	var flagTimezone string
	var flagEndingBefore string
	var flagStartingAfter string
	var flagPage string
	var flagPageSize float64
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Retrieve a paginated list of commissions for your partner program.",
		Example:     "  dub-pp-cli commissions list",
		Annotations: map[string]string{"pp:endpoint": "commissions.list", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("type") {
				allowedType := []string{"click", "lead", "sale", "custom"}
				validType := false
				for _, v := range allowedType {
					if flagType == v {
						validType = true
						break
					}
				}
				if !validType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "type", flagType, allowedType)
				}
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"pending", "processed", "paid", "refunded", "duplicate", "fraud", "canceled"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "status", flagStatus, allowedStatus)
				}
			}
			if cmd.Flags().Changed("sort-by") {
				allowedSortBy := []string{"createdAt", "amount"}
				validSortBy := false
				for _, v := range allowedSortBy {
					if flagSortBy == v {
						validSortBy = true
						break
					}
				}
				if !validSortBy {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-by", flagSortBy, allowedSortBy)
				}
			}
			if cmd.Flags().Changed("sort-order") {
				allowedSortOrder := []string{"asc", "desc"}
				validSortOrder := false
				for _, v := range allowedSortOrder {
					if flagSortOrder == v {
						validSortOrder = true
						break
					}
				}
				if !validSortOrder {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-order", flagSortOrder, allowedSortOrder)
				}
			}
			if cmd.Flags().Changed("interval") {
				allowedInterval := []string{"24h", "7d", "30d", "90d", "1y", "mtd", "qtd", "ytd", "all"}
				validInterval := false
				for _, v := range allowedInterval {
					if flagInterval == v {
						validInterval = true
						break
					}
				}
				if !validInterval {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "interval", flagInterval, allowedInterval)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/commissions"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "commissions", path, map[string]string{
				"type":          fmt.Sprintf("%v", flagType),
				"customerId":    fmt.Sprintf("%v", flagCustomerId),
				"payoutId":      fmt.Sprintf("%v", flagPayoutId),
				"partnerId":     fmt.Sprintf("%v", flagPartnerId),
				"tenantId":      fmt.Sprintf("%v", flagTenantId),
				"groupId":       fmt.Sprintf("%v", flagGroupId),
				"invoiceId":     fmt.Sprintf("%v", flagInvoiceId),
				"status":        fmt.Sprintf("%v", flagStatus),
				"sortBy":        fmt.Sprintf("%v", flagSortBy),
				"sortOrder":     fmt.Sprintf("%v", flagSortOrder),
				"interval":      fmt.Sprintf("%v", flagInterval),
				"start":         fmt.Sprintf("%v", flagStart),
				"end":           fmt.Sprintf("%v", flagEnd),
				"timezone":      fmt.Sprintf("%v", flagTimezone),
				"endingBefore":  fmt.Sprintf("%v", flagEndingBefore),
				"startingAfter": fmt.Sprintf("%v", flagStartingAfter),
				"page":          fmt.Sprintf("%v", flagPage),
				"pageSize":      fmt.Sprintf("%v", flagPageSize),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagType, "type", "", "Type (one of: click, lead, sale, custom)")
	cmd.Flags().StringVar(&flagCustomerId, "customer-id", "", "Filter the list of commissions by the associated customer.")
	cmd.Flags().StringVar(&flagPayoutId, "payout-id", "", "Filter the list of commissions by the associated payout.")
	cmd.Flags().StringVar(&flagPartnerId, "partner-id", "", "Filter the list of commissions by the associated partner. When specified, takes precedence over `tenantId`.")
	cmd.Flags().StringVar(&flagTenantId, "tenant-id", "", "Filter the list of commissions by the associated partner's `tenantId` (their unique ID within your database).")
	cmd.Flags().StringVar(&flagGroupId, "group-id", "", "Filter the list of commissions by the associated partner group.")
	cmd.Flags().StringVar(&flagInvoiceId, "invoice-id", "", "Filter the list of commissions by the associated invoice. Since invoiceId is unique on a per-program basis, this...")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Filter the list of commissions by their corresponding status. (one of: pending, processed, paid, refunded, duplicate, fraud, canceled)")
	cmd.Flags().StringVar(&flagSortBy, "sort-by", "createdAt", "The field to sort the list of commissions by. (one of: createdAt, amount)")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "desc", "The sort order for the list of commissions. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagInterval, "interval", "all", "The interval to retrieve commissions for. (one of: 24h, 7d, 30d, 90d, 1y, mtd, qtd, ytd, all)")
	cmd.Flags().StringVar(&flagStart, "start", "", "The start date of the date range to filter the commissions by.")
	cmd.Flags().StringVar(&flagEnd, "end", "", "The end date of the date range to filter the commissions by.")
	cmd.Flags().StringVar(&flagTimezone, "timezone", "", "Timezone")
	cmd.Flags().StringVar(&flagEndingBefore, "ending-before", "", "If specified, the query only searches for results before this cursor. Mutually exclusive with `startingAfter`.")
	cmd.Flags().StringVar(&flagStartingAfter, "starting-after", "", "If specified, the query only searches for results after this cursor. Mutually exclusive with `endingBefore`.")
	cmd.Flags().StringVar(&flagPage, "page", "", "DEPRECATED. Use `startingAfter` instead.")
	cmd.Flags().Float64Var(&flagPageSize, "page-size", 100.000000, "The number of items per page.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
