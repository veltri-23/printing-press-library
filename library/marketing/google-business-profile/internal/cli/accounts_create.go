// Copyright 2026 Raj Yaligar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/marketing/google-business-profile/internal/cliutil"
	"github.com/spf13/cobra"
)

func newAccountsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountName string
	var bodyAccountNumber string
	var bodyName string
	var bodyOrganizationInfoAddressAddressLines string
	var bodyOrganizationInfoAddressAdministrativeArea string
	var bodyOrganizationInfoAddressLanguageCode string
	var bodyOrganizationInfoAddressLocality string
	var bodyOrganizationInfoAddressOrganization string
	var bodyOrganizationInfoAddressPostalCode string
	var bodyOrganizationInfoAddressRecipients string
	var bodyOrganizationInfoAddressRegionCode string
	var bodyOrganizationInfoAddressRevision int
	var bodyOrganizationInfoAddressSortingCode string
	var bodyOrganizationInfoAddressSublocality string
	var bodyOrganizationInfoPhoneNumber string
	var bodyOrganizationInfoRegisteredDomain string
	var bodyPermissionLevel string
	var bodyPrimaryOwner string
	var bodyRole string
	var bodyType string
	var bodyVerificationState string
	var bodyVettedState string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Creates an account with the specified name and type under the given parent.",
		Example:     "  google-business-profile-pp-cli accounts create",
		Annotations: map[string]string{"pp:endpoint": "accounts.create", "pp:method": "POST", "pp:path": "/v1/accounts"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/accounts"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccountName != "" {
					body["accountName"] = bodyAccountName
				}
				if bodyAccountNumber != "" {
					body["accountNumber"] = bodyAccountNumber
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				{
					nestedOrganizationInfo := map[string]any{}
					{
						nestedOrganizationInfoAddress := map[string]any{}
						if bodyOrganizationInfoAddressAddressLines != "" {
							nestedOrganizationInfoAddress["addressLines"] = cliutil.SplitCSV(bodyOrganizationInfoAddressAddressLines)
						}
						if bodyOrganizationInfoAddressAdministrativeArea != "" {
							nestedOrganizationInfoAddress["administrativeArea"] = bodyOrganizationInfoAddressAdministrativeArea
						}
						if bodyOrganizationInfoAddressLanguageCode != "" {
							nestedOrganizationInfoAddress["languageCode"] = bodyOrganizationInfoAddressLanguageCode
						}
						if bodyOrganizationInfoAddressLocality != "" {
							nestedOrganizationInfoAddress["locality"] = bodyOrganizationInfoAddressLocality
						}
						if bodyOrganizationInfoAddressOrganization != "" {
							nestedOrganizationInfoAddress["organization"] = bodyOrganizationInfoAddressOrganization
						}
						if bodyOrganizationInfoAddressPostalCode != "" {
							nestedOrganizationInfoAddress["postalCode"] = bodyOrganizationInfoAddressPostalCode
						}
						if bodyOrganizationInfoAddressRecipients != "" {
							nestedOrganizationInfoAddress["recipients"] = cliutil.SplitCSV(bodyOrganizationInfoAddressRecipients)
						}
						if bodyOrganizationInfoAddressRegionCode != "" {
							nestedOrganizationInfoAddress["regionCode"] = bodyOrganizationInfoAddressRegionCode
						}
						if bodyOrganizationInfoAddressRevision != 0 {
							nestedOrganizationInfoAddress["revision"] = bodyOrganizationInfoAddressRevision
						}
						if bodyOrganizationInfoAddressSortingCode != "" {
							nestedOrganizationInfoAddress["sortingCode"] = bodyOrganizationInfoAddressSortingCode
						}
						if bodyOrganizationInfoAddressSublocality != "" {
							nestedOrganizationInfoAddress["sublocality"] = bodyOrganizationInfoAddressSublocality
						}
						if len(nestedOrganizationInfoAddress) > 0 {
							nestedOrganizationInfo["address"] = nestedOrganizationInfoAddress
						}
					}
					if bodyOrganizationInfoPhoneNumber != "" {
						nestedOrganizationInfo["phoneNumber"] = bodyOrganizationInfoPhoneNumber
					}
					if bodyOrganizationInfoRegisteredDomain != "" {
						nestedOrganizationInfo["registeredDomain"] = bodyOrganizationInfoRegisteredDomain
					}
					if len(nestedOrganizationInfo) > 0 {
						body["organizationInfo"] = nestedOrganizationInfo
					}
				}
				if bodyPermissionLevel != "" {
					body["permissionLevel"] = bodyPermissionLevel
				}
				if bodyPrimaryOwner != "" {
					body["primaryOwner"] = bodyPrimaryOwner
				}
				if bodyRole != "" {
					body["role"] = bodyRole
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
				if bodyVerificationState != "" {
					body["verificationState"] = bodyVerificationState
				}
				if bodyVettedState != "" {
					body["vettedState"] = bodyVettedState
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "accounts", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "accounts", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "accounts", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "accounts", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "accounts", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "accounts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "accounts", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "accounts", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAccountName, "account-name", "", "Required. The name of the account.")
	cmd.Flags().StringVar(&bodyAccountNumber, "account-number", "", "Output only. Account reference number if provisioned.")
	cmd.Flags().StringVar(&bodyName, "name", "", "Immutable. The resource name, in the format `accounts/{account_id}`.")
	cmd.Flags().StringVar(&bodyOrganizationInfoAddressAddressLines, "organization-info-address-address-lines", "", "Unstructured address lines describing the lower levels of an address.")
	cmd.Flags().StringVar(&bodyOrganizationInfoAddressAdministrativeArea, "organization-info-address-administrative-area", "", "Optional. Highest administrative subdivision which is used for postal addresses of a country or region.")
	cmd.Flags().StringVar(&bodyOrganizationInfoAddressLanguageCode, "organization-info-address-language-code", "", "Optional. BCP-47 language code of the contents of this address (if known).")
	cmd.Flags().StringVar(&bodyOrganizationInfoAddressLocality, "organization-info-address-locality", "", "Optional. Generally refers to the city or town portion of the address. Examples: US city, IT comune, UK post town.")
	cmd.Flags().StringVar(&bodyOrganizationInfoAddressOrganization, "organization-info-address-organization", "", "Optional. The name of the organization at the address.")
	cmd.Flags().StringVar(&bodyOrganizationInfoAddressPostalCode, "organization-info-address-postal-code", "", "Optional. Postal code of the address.")
	cmd.Flags().StringVar(&bodyOrganizationInfoAddressRecipients, "organization-info-address-recipients", "", "Optional. The recipient at the address. This field may, under certain circumstances, contain multiline information.")
	cmd.Flags().StringVar(&bodyOrganizationInfoAddressRegionCode, "organization-info-address-region-code", "", "Required. CLDR region code of the country/region of the address.")
	cmd.Flags().IntVar(&bodyOrganizationInfoAddressRevision, "organization-info-address-revision", 0, "The schema revision of the `PostalAddress`. This must be set to 0, which is the latest revision.")
	cmd.Flags().StringVar(&bodyOrganizationInfoAddressSortingCode, "organization-info-address-sorting-code", "", "Optional. Additional, country-specific, sorting code. This is not used in most regions.")
	cmd.Flags().StringVar(&bodyOrganizationInfoAddressSublocality, "organization-info-address-sublocality", "", "Optional. Sublocality of the address. For example, this can be a neighborhood, borough, or district.")
	cmd.Flags().StringVar(&bodyOrganizationInfoPhoneNumber, "organization-info-phone-number", "", "Output only. The contact number for the organization.")
	cmd.Flags().StringVar(&bodyOrganizationInfoRegisteredDomain, "organization-info-registered-domain", "", "Output only. The registered domain for the account.")
	cmd.Flags().StringVar(&bodyPermissionLevel, "permission-level", "", "Output only. Specifies the permission level the user has for this account.")
	cmd.Flags().StringVar(&bodyPrimaryOwner, "primary-owner", "", "Required. Input only. The resource name of the account which will be the primary owner of the account being created.")
	cmd.Flags().StringVar(&bodyRole, "role", "", "Output only. Specifies the AccountRole of this account.")
	cmd.Flags().StringVar(&bodyType, "type", "", "Required. Contains the type of account. Accounts of type PERSONAL and ORGANIZATION cannot be created using this API.")
	cmd.Flags().StringVar(&bodyVerificationState, "verification-state", "", "Output only.")
	cmd.Flags().StringVar(&bodyVettedState, "vetted-state", "", "Output only. Indicates whether the account is vetted by Google.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
