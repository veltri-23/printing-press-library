// Copyright 2026 Raj Yaligar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/marketing/google-business-profile/internal/cliutil"
	"github.com/spf13/cobra"
)

func newMyBusinessVerificationsLocationsFetchVerificationOptionsCmd(flags *rootFlags) *cobra.Command {
	var bodyContextAddressAddressLines string
	var bodyContextAddressAdministrativeArea string
	var bodyContextAddressLanguageCode string
	var bodyContextAddressLocality string
	var bodyContextAddressOrganization string
	var bodyContextAddressPostalCode string
	var bodyContextAddressRecipients string
	var bodyContextAddressRegionCode string
	var bodyContextAddressRevision int
	var bodyContextAddressSortingCode string
	var bodyContextAddressSublocality string
	var bodyLanguageCode string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "fetch-verification-options <location>",
		Short:       "Reports all eligible verification options for a location in a specific language.",
		Example:     "  google-business-profile-pp-cli my-business-verifications-locations fetch-verification-options example-value",
		Annotations: map[string]string{"pp:endpoint": "my-business-verifications-locations.fetch-verification-options", "pp:method": "POST", "pp:path": "https://mybusinessverifications.googleapis.com/v1/{location}:fetchVerificationOptions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "https://mybusinessverifications.googleapis.com/v1/{location}:fetchVerificationOptions"
			path = replacePathParam(path, "location", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedContext := map[string]any{}
					{
						nestedContextAddress := map[string]any{}
						if bodyContextAddressAddressLines != "" {
							nestedContextAddress["addressLines"] = cliutil.SplitCSV(bodyContextAddressAddressLines)
						}
						if bodyContextAddressAdministrativeArea != "" {
							nestedContextAddress["administrativeArea"] = bodyContextAddressAdministrativeArea
						}
						if bodyContextAddressLanguageCode != "" {
							nestedContextAddress["languageCode"] = bodyContextAddressLanguageCode
						}
						if bodyContextAddressLocality != "" {
							nestedContextAddress["locality"] = bodyContextAddressLocality
						}
						if bodyContextAddressOrganization != "" {
							nestedContextAddress["organization"] = bodyContextAddressOrganization
						}
						if bodyContextAddressPostalCode != "" {
							nestedContextAddress["postalCode"] = bodyContextAddressPostalCode
						}
						if bodyContextAddressRecipients != "" {
							nestedContextAddress["recipients"] = cliutil.SplitCSV(bodyContextAddressRecipients)
						}
						if bodyContextAddressRegionCode != "" {
							nestedContextAddress["regionCode"] = bodyContextAddressRegionCode
						}
						if bodyContextAddressRevision != 0 {
							nestedContextAddress["revision"] = bodyContextAddressRevision
						}
						if bodyContextAddressSortingCode != "" {
							nestedContextAddress["sortingCode"] = bodyContextAddressSortingCode
						}
						if bodyContextAddressSublocality != "" {
							nestedContextAddress["sublocality"] = bodyContextAddressSublocality
						}
						if len(nestedContextAddress) > 0 {
							nestedContext["address"] = nestedContextAddress
						}
					}
					if len(nestedContext) > 0 {
						body["context"] = nestedContext
					}
				}
				if bodyLanguageCode != "" {
					body["languageCode"] = bodyLanguageCode
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "my-business-verifications-locations", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "my-business-verifications-locations", data, "options")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "my-business-verifications-locations", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "my-business-verifications-locations", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "my-business-verifications-locations", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "my-business-verifications-locations",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "my-business-verifications-locations", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "my-business-verifications-locations", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyContextAddressAddressLines, "context-address-address-lines", "", "Unstructured address lines describing the lower levels of an address.")
	cmd.Flags().StringVar(&bodyContextAddressAdministrativeArea, "context-address-administrative-area", "", "Optional. Highest administrative subdivision which is used for postal addresses of a country or region.")
	cmd.Flags().StringVar(&bodyContextAddressLanguageCode, "context-address-language-code", "", "Optional. BCP-47 language code of the contents of this address (if known).")
	cmd.Flags().StringVar(&bodyContextAddressLocality, "context-address-locality", "", "Optional. Generally refers to the city or town portion of the address. Examples: US city, IT comune, UK post town.")
	cmd.Flags().StringVar(&bodyContextAddressOrganization, "context-address-organization", "", "Optional. The name of the organization at the address.")
	cmd.Flags().StringVar(&bodyContextAddressPostalCode, "context-address-postal-code", "", "Optional. Postal code of the address.")
	cmd.Flags().StringVar(&bodyContextAddressRecipients, "context-address-recipients", "", "Optional. The recipient at the address. This field may, under certain circumstances, contain multiline information.")
	cmd.Flags().StringVar(&bodyContextAddressRegionCode, "context-address-region-code", "", "Required. CLDR region code of the country/region of the address.")
	cmd.Flags().IntVar(&bodyContextAddressRevision, "context-address-revision", 0, "The schema revision of the `PostalAddress`. This must be set to 0, which is the latest revision.")
	cmd.Flags().StringVar(&bodyContextAddressSortingCode, "context-address-sorting-code", "", "Optional. Additional, country-specific, sorting code. This is not used in most regions.")
	cmd.Flags().StringVar(&bodyContextAddressSublocality, "context-address-sublocality", "", "Optional. Sublocality of the address. For example, this can be a neighborhood, borough, or district.")
	cmd.Flags().StringVar(&bodyLanguageCode, "language-code", "", "Required. The BCP 47 language code representing the language that is to be used for the verification process.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
