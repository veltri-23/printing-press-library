// Copyright 2026 Raj Yaligar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCategoriesBatchGetCmd(flags *rootFlags) *cobra.Command {
	var flagLanguageCode string
	var flagView string
	var flagNames string
	var flagRegionCode string

	cmd := &cobra.Command{
		Use:         "batch-get",
		Short:       "Returns a list of business categories for the provided language and GConcept ids.",
		Example:     "  google-business-profile-pp-cli categories batch-get --names categories/gcid:school --region-code CA --language-code en --view BASIC",
		Annotations: map[string]string{"pp:endpoint": "categories.batch-get", "pp:method": "GET", "pp:path": "https://mybusinessbusinessinformation.googleapis.com/v1/categories:batchGet", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("view") {
				allowedView := []string{"CATEGORY_VIEW_UNSPECIFIED", "BASIC", "FULL"}
				validView := false
				for _, v := range allowedView {
					if flagView == v {
						validView = true
						break
					}
				}
				if !validView {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagView, "view", allowedView)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "https://mybusinessbusinessinformation.googleapis.com/v1/categories:batchGet"
			params := map[string]string{}
			if flagLanguageCode != "" {
				params["languageCode"] = fmt.Sprintf("%v", flagLanguageCode)
			}
			if flagView != "" {
				params["view"] = fmt.Sprintf("%v", flagView)
			}
			if flagNames != "" {
				params["names"] = fmt.Sprintf("%v", flagNames)
			}
			if flagRegionCode != "" {
				params["regionCode"] = fmt.Sprintf("%v", flagRegionCode)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "categories", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagLanguageCode, "language-code", "", "Required. The BCP 47 code of language that the category names should be returned in.")
	cmd.Flags().StringVar(&flagView, "view", "", "Required. Specifies which parts to the Category resource should be returned in the response. (one of: CATEGORY_VIEW_UNSPECIFIED, BASIC, FULL)")
	cmd.Flags().StringVar(&flagNames, "names", "", "Required. At least one name must be set. The GConcept ids the localized category names should be returned for.")
	cmd.Flags().StringVar(&flagRegionCode, "region-code", "", "Optional. The ISO 3166-1 alpha-2 country code used to infer non-standard language.")

	return cmd
}
