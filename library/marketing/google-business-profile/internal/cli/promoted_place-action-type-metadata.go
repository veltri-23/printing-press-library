// Copyright 2026 Raj Yaligar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newPlaceActionTypeMetadataPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagFilter string
	var flagPageToken string
	var flagLanguageCode string
	var flagPageSize int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "place-action-type-metadata",
		Short:       "Returns the list of available place action types for a location or country.",
		Long:        "Returns the list of available place action types for a location or country.",
		Example:     "  google-business-profile-pp-cli place-action-type-metadata",
		Annotations: map[string]string{"pp:endpoint": "place_action_type_metadata.list", "pp:method": "GET", "pp:path": "https://mybusinessplaceactions.googleapis.com/v1/placeActionTypeMetadata", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "https://mybusinessplaceactions.googleapis.com/v1/placeActionTypeMetadata"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "place_action_type_metadata", path, map[string]string{
				"filter":       fmt.Sprintf("%v", flagFilter),
				"pageToken":    fmt.Sprintf("%v", flagPageToken),
				"languageCode": fmt.Sprintf("%v", flagLanguageCode),
				"pageSize":     fmt.Sprintf("%v", flagPageSize),
			}, nil, flagAll, "pageToken", "page_token", "pageSize", "nextPageToken", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagFilter, "filter", "", "Optional. A filter constraining the place action types to return metadata for.")
	cmd.Flags().StringVar(&flagPageToken, "page-token", "", "Optional. If specified, the next page of place action type metadata is retrieved.")
	cmd.Flags().StringVar(&flagLanguageCode, "language-code", "", "Optional. The IETF BCP-47 code of language to get display names in.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Optional. How many action types to include per page. Default is 10, minimum is 1.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
