// Copyright 2026 Raj Yaligar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMyBusinessBusinessAccountsListCmd(flags *rootFlags) *cobra.Command {
	var flagPageToken string
	var flagPageSize int
	var flagOrderBy string
	var flagFilter string
	var flagReadMask string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list <parent>",
		Aliases:     []string{"get"},
		Short:       "Lists the locations for the specified account.",
		Example:     "  google-business-profile-pp-cli my-business-business-accounts list example-value",
		Annotations: map[string]string{"pp:endpoint": "my-business-business-accounts.list", "pp:method": "GET", "pp:path": "https://mybusinessbusinessinformation.googleapis.com/v1/{parent}/locations", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "https://mybusinessbusinessinformation.googleapis.com/v1/{parent}/locations"
			path = replacePathParam(path, "parent", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "my-business-business-accounts", path, map[string]string{
				"pageToken": fmt.Sprintf("%v", flagPageToken),
				"pageSize":  fmt.Sprintf("%v", flagPageSize),
				"orderBy":   fmt.Sprintf("%v", flagOrderBy),
				"filter":    fmt.Sprintf("%v", flagFilter),
				"readMask":  fmt.Sprintf("%v", flagReadMask),
			}, nil, flagAll, "pageToken", "page_token", "pageSize", "nextPageToken", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagPageToken, "page-token", "", "Optional. If specified, it fetches the next `page` of locations.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Optional. How many locations to fetch per page. Default value is 10 if not set.")
	cmd.Flags().StringVar(&flagOrderBy, "order-by", "", "Optional. Sorting order for the request. Multiple fields should be comma-separated, following SQL syntax.")
	cmd.Flags().StringVar(&flagFilter, "filter", "", "Optional. A filter constraining the locations to return. The response includes only entries that match the filter.")
	cmd.Flags().StringVar(&flagReadMask, "read-mask", "", "Required. Read mask to specify what fields will be returned in the response.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
