// Copyright 2026 Raj Yaligar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "accounts accept", Description: "Accepts the specified invitation.", Group: "accounts"},
	{Command: "accounts create", Description: "Creates an account with the specified name and type under the given parent. - Personal accounts and Organizations cannot be created. - User Groups cannot be created with a Personal account as primary owner. - Location Groups cannot be created with a primary owner of a Personal account if the Personal account is in an Organization. - Location Groups cannot own Location Groups.", Group: "accounts"},
	{Command: "accounts create-admins", Description: "Invites the specified user to become an administrator for the specified account. The invitee must accept the invitation in order to be granted access to the account. See AcceptInvitation to programmatically accept an invitation.", Group: "accounts"},
	{Command: "accounts decline", Description: "Declines the specified invitation.", Group: "accounts"},
	{Command: "accounts delete", Description: "Removes the specified admin from the specified account.", Group: "accounts"},
	{Command: "accounts get", Description: "Gets the specified account. Returns `NOT_FOUND` if the account does not exist or if the caller does not have access rights to it.", Group: "accounts"},
	{Command: "accounts list", Description: "Lists all of the accounts for the authenticated user. This includes all accounts that the user owns, as well as any accounts for which the user has management rights.", Group: "accounts"},
	{Command: "accounts list-admins", Description: "Lists the admins for the specified account.", Group: "accounts"},
	{Command: "accounts list-invitations", Description: "Lists pending invitations for the specified account.", Group: "accounts"},
	{Command: "accounts patch", Description: "Updates the Admin for the specified Account Admin.", Group: "accounts"},
	{Command: "attributes list", Description: "Returns the list of attributes that would be available for a location with the given primary category and country.", Group: "attributes"},
	{Command: "business-profile-performance-locations fetch-multi-daily-metrics-time-series", Description: "Returns the values for each date from a given time range that are associated with the specific daily metrics. Note: Only daily data is available. Hourly metrics are not supported. Example request: `GET https://businessprofileperformance.googleapis.com/v1/locations/12345:fetchMultiDailyMetricsTimeSeries?dailyMetrics=WEBSITE_CLICKS&dailyMetrics=CALL_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31`", Group: "business-profile-performance-locations"},
	{Command: "business-profile-performance-locations get-daily-metrics-time-series", Description: "Returns the values for each date from a given time range that are associated with the specific daily metric. Note: Only daily data is available. Hourly metrics are not supported. Example request: `GET https://businessprofileperformance.googleapis.com/v1/locations/12345:getDailyMetricsTimeSeries?dailyMetric=WEBSITE_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31`", Group: "business-profile-performance-locations"},
	{Command: "business-profile-performance-locations list", Description: "Returns the search keywords used to find a business in search or maps. Each search keyword is accompanied by impressions which are aggregated on a monthly basis. Example request: `GET https://businessprofileperformance.googleapis.com/v1/locations/12345/searchkeywords/impressions/monthly?monthly_range.start_month.year=2022&monthly_range.start_month.month=1&monthly_range.end_month.year=2022&monthly_range.end_month.month=3`", Group: "business-profile-performance-locations"},
	{Command: "categories batch-get", Description: "Returns a list of business categories for the provided language and GConcept ids.", Group: "categories"},
	{Command: "categories list", Description: "Returns a list of business categories. Search will match the category name but not the category ID. Search only matches the front of a category name (that is, 'food' may return 'Food Court' but not 'Fast Food Restaurant').", Group: "categories"},
	{Command: "chains get", Description: "Gets the specified chain. Returns `NOT_FOUND` if the chain does not exist.", Group: "chains"},
	{Command: "chains search", Description: "Searches the chain based on chain name.", Group: "chains"},
	{Command: "google_locations search", Description: "Search all of the possible locations that are a match to the specified request.", Group: "google_locations"},
	{Command: "locations transfer", Description: "Moves a location from an account that the user owns to another account that the same user administers. The user must be an owner of the account the location is currently associated with and must also be at least a manager of the destination account.", Group: "locations"},
	{Command: "my-business-business-accounts create", Description: "Creates a new Location that will be owned by the logged in user.", Group: "my-business-business-accounts"},
	{Command: "my-business-business-accounts list", Description: "Lists the locations for the specified account.", Group: "my-business-business-accounts"},
	{Command: "my-business-business-locations delete", Description: "Deletes a location. If this location cannot be deleted using the API and it is marked so in the `google.mybusiness.businessinformation.v1.LocationState`, use the [Google Business Profile](https://business.google.com/manage/) website.", Group: "my-business-business-locations"},
	{Command: "my-business-business-locations get-google-updated", Description: "Retrieves attributes for a location as they appear live on Google Maps and Search. This consumer-facing view may have been updated by Google or user-generated content and may differ from the merchant's version.", Group: "my-business-business-locations"},
	{Command: "my-business-business-locations patch", Description: "Updates the specified location.", Group: "my-business-business-locations"},
	{Command: "my-business-lodging-locations get-google-updated", Description: "Returns the Google updated Lodging of a specific location.", Group: "my-business-lodging-locations"},
	{Command: "my-business-lodging-locations get-lodging", Description: "Returns the Lodging of a specific location.", Group: "my-business-lodging-locations"},
	{Command: "my-business-lodging-locations update-lodging", Description: "Updates the Lodging of a specific location.", Group: "my-business-lodging-locations"},
	{Command: "my-business-notifications-accounts get-notification-setting", Description: "Returns the pubsub notification settings for the account.", Group: "my-business-notifications-accounts"},
	{Command: "my-business-notifications-accounts update-notification-setting", Description: "Sets the pubsub notification setting for the account informing Google which topic to send pubsub notifications for. Use the notification_types field within notification_setting to manipulate the events an account wants to subscribe to. An account will only have one notification setting resource, and only one pubsub topic can be set. To delete the setting, update with an empty notification_types", Group: "my-business-notifications-accounts"},
	{Command: "my-business-place-locations create", Description: "Creates a place action link associated with the specified location, and returns it. The request is considered duplicate if the `parent`, `place_action_link.uri` and `place_action_link.place_action_type` are the same as a previous request.", Group: "my-business-place-locations"},
	{Command: "my-business-place-locations delete", Description: "Deletes a place action link from the specified location.", Group: "my-business-place-locations"},
	{Command: "my-business-place-locations get", Description: "Gets the specified place action link.", Group: "my-business-place-locations"},
	{Command: "my-business-place-locations list", Description: "Lists the place action links for the specified location.", Group: "my-business-place-locations"},
	{Command: "my-business-place-locations patch", Description: "Updates the specified place action link and returns it.", Group: "my-business-place-locations"},
	{Command: "my-business-q-locations create", Description: "Adds a question for the specified location.", Group: "my-business-q-locations"},
	{Command: "my-business-q-locations delete", Description: "Deletes a specific question written by the current user.", Group: "my-business-q-locations"},
	{Command: "my-business-q-locations delete-answersdelete", Description: "Deletes the answer written by the current user to a question.", Group: "my-business-q-locations"},
	{Command: "my-business-q-locations list", Description: "Returns the paginated list of questions and some of its answers for a specified location. This operation is only valid if the specified location is verified.", Group: "my-business-q-locations"},
	{Command: "my-business-q-locations list-answers", Description: "Returns the paginated list of answers for a specified question.", Group: "my-business-q-locations"},
	{Command: "my-business-q-locations patch", Description: "Updates a specific question written by the current user.", Group: "my-business-q-locations"},
	{Command: "my-business-q-locations upsert", Description: "Creates an answer or updates the existing answer written by the user for the specified question. A user can only create one answer per question.", Group: "my-business-q-locations"},
	{Command: "my-business-verifications-locations complete", Description: "Completes a `PENDING` verification. It is only necessary for non `AUTO` verification methods. `AUTO` verification request is instantly `VERIFIED` upon creation.", Group: "my-business-verifications-locations"},
	{Command: "my-business-verifications-locations fetch-verification-options", Description: "Reports all eligible verification options for a location in a specific language.", Group: "my-business-verifications-locations"},
	{Command: "my-business-verifications-locations get-voice-of-merchant-state", Description: "Gets the VoiceOfMerchant state.", Group: "my-business-verifications-locations"},
	{Command: "my-business-verifications-locations list", Description: "List verifications of a location, ordered by create time.", Group: "my-business-verifications-locations"},
	{Command: "my-business-verifications-locations verify", Description: "Starts the verification process for a location.", Group: "my-business-verifications-locations"},
	{Command: "place_action_type_metadata list", Description: "Returns the list of available place action types for a location or country.", Group: "place_action_type_metadata"},
	{Command: "verification_tokens generate", Description: "Generate a token for the provided location data to verify the location.", Group: "verification_tokens"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  google-business-profile-pp-cli which "stale tickets"
  google-business-profile-pp-cli which "bottleneck"
  google-business-profile-pp-cli which --limit 1 "send message"
  google-business-profile-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
