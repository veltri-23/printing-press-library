// Copyright 2026 Raj Yaligar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newBusinessProfilePerformanceLocationsGetDailyMetricsTimeSeriesCmd(flags *rootFlags) *cobra.Command {
	var flagDailySubEntityTypeTimeOfDaySeconds int
	var flagDailyMetric string
	var flagDailyRangeEndDateYear int
	var flagDailySubEntityTypeTimeOfDayNanos int
	var flagDailySubEntityTypeTimeOfDayMinutes int
	var flagDailyRangeStartDateYear int
	var flagDailyRangeStartDateDay int
	var flagDailyRangeStartDateMonth int
	var flagDailyRangeEndDateMonth int
	var flagDailyRangeEndDateDay int
	var flagDailySubEntityTypeDayOfWeek string
	var flagDailySubEntityTypeTimeOfDayHours int

	cmd := &cobra.Command{
		Use:         "get-daily-metrics-time-series <name>",
		Short:       "Returns the values for each date from a given time range that are associated with the specific daily metric.",
		Example:     "  google-business-profile-pp-cli business-profile-performance-locations get-daily-metrics-time-series example-resource",
		Annotations: map[string]string{"pp:endpoint": "business-profile-performance-locations.get-daily-metrics-time-series", "pp:method": "GET", "pp:path": "https://businessprofileperformance.googleapis.com/v1/{name}:getDailyMetricsTimeSeries", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("daily-metric") {
				allowedDailyMetric := []string{"DAILY_METRIC_UNKNOWN", "BUSINESS_IMPRESSIONS_DESKTOP_MAPS", "BUSINESS_IMPRESSIONS_DESKTOP_SEARCH", "BUSINESS_IMPRESSIONS_MOBILE_MAPS", "BUSINESS_IMPRESSIONS_MOBILE_SEARCH", "BUSINESS_CONVERSATIONS", "BUSINESS_DIRECTION_REQUESTS", "CALL_CLICKS", "WEBSITE_CLICKS", "BUSINESS_BOOKINGS", "BUSINESS_FOOD_ORDERS", "BUSINESS_FOOD_MENU_CLICKS"}
				validDailyMetric := false
				for _, v := range allowedDailyMetric {
					if flagDailyMetric == v {
						validDailyMetric = true
						break
					}
				}
				if !validDailyMetric {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagDailyMetric, "daily-metric", allowedDailyMetric)
				}
			}
			if cmd.Flags().Changed("daily-sub-entity-type-day-of-week") {
				allowedDailySubEntityTypeDayOfWeek := []string{"DAY_OF_WEEK_UNSPECIFIED", "MONDAY", "TUESDAY", "WEDNESDAY", "THURSDAY", "FRIDAY", "SATURDAY", "SUNDAY"}
				validDailySubEntityTypeDayOfWeek := false
				for _, v := range allowedDailySubEntityTypeDayOfWeek {
					if flagDailySubEntityTypeDayOfWeek == v {
						validDailySubEntityTypeDayOfWeek = true
						break
					}
				}
				if !validDailySubEntityTypeDayOfWeek {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagDailySubEntityTypeDayOfWeek, "daily-sub-entity-type-day-of-week", allowedDailySubEntityTypeDayOfWeek)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "https://businessprofileperformance.googleapis.com/v1/{name}:getDailyMetricsTimeSeries"
			path = replacePathParam(path, "name", args[0])
			params := map[string]string{}
			if flagDailySubEntityTypeTimeOfDaySeconds != 0 {
				params["dailySubEntityType.timeOfDay.seconds"] = fmt.Sprintf("%v", flagDailySubEntityTypeTimeOfDaySeconds)
			}
			if flagDailyMetric != "" {
				params["dailyMetric"] = fmt.Sprintf("%v", flagDailyMetric)
			}
			if flagDailyRangeEndDateYear != 0 {
				params["dailyRange.endDate.year"] = fmt.Sprintf("%v", flagDailyRangeEndDateYear)
			}
			if flagDailySubEntityTypeTimeOfDayNanos != 0 {
				params["dailySubEntityType.timeOfDay.nanos"] = fmt.Sprintf("%v", flagDailySubEntityTypeTimeOfDayNanos)
			}
			if flagDailySubEntityTypeTimeOfDayMinutes != 0 {
				params["dailySubEntityType.timeOfDay.minutes"] = fmt.Sprintf("%v", flagDailySubEntityTypeTimeOfDayMinutes)
			}
			if flagDailyRangeStartDateYear != 0 {
				params["dailyRange.startDate.year"] = fmt.Sprintf("%v", flagDailyRangeStartDateYear)
			}
			if flagDailyRangeStartDateDay != 0 {
				params["dailyRange.startDate.day"] = fmt.Sprintf("%v", flagDailyRangeStartDateDay)
			}
			if flagDailyRangeStartDateMonth != 0 {
				params["dailyRange.startDate.month"] = fmt.Sprintf("%v", flagDailyRangeStartDateMonth)
			}
			if flagDailyRangeEndDateMonth != 0 {
				params["dailyRange.endDate.month"] = fmt.Sprintf("%v", flagDailyRangeEndDateMonth)
			}
			if flagDailyRangeEndDateDay != 0 {
				params["dailyRange.endDate.day"] = fmt.Sprintf("%v", flagDailyRangeEndDateDay)
			}
			if flagDailySubEntityTypeDayOfWeek != "" {
				params["dailySubEntityType.dayOfWeek"] = fmt.Sprintf("%v", flagDailySubEntityTypeDayOfWeek)
			}
			if flagDailySubEntityTypeTimeOfDayHours != 0 {
				params["dailySubEntityType.timeOfDay.hours"] = fmt.Sprintf("%v", flagDailySubEntityTypeTimeOfDayHours)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "business-profile-performance-locations", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagDailySubEntityTypeTimeOfDaySeconds, "daily-sub-entity-type-time-of-day-seconds", 0, "Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59.")
	cmd.Flags().StringVar(&flagDailyMetric, "daily-metric", "", "Required. The metric to retrieve time series. (one of: DAILY_METRIC_UNKNOWN, BUSINESS_IMPRESSIONS_DESKTOP_MAPS, BUSINESS_IMPRESSIONS_DESKTOP_SEARCH, BUSINESS_IMPRESSIONS_MOBILE_MAPS, BUSINESS_IMPRESSIONS_MOBILE_SEARCH, BUSINESS_CONVERSATIONS, BUSINESS_DIRECTION_REQUESTS, CALL_CLICKS, WEBSITE_CLICKS, BUSINESS_BOOKINGS, BUSINESS_FOOD_ORDERS, BUSINESS_FOOD_MENU_CLICKS)")
	cmd.Flags().IntVar(&flagDailyRangeEndDateYear, "daily-range-end-date-year", 0, "Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year.")
	cmd.Flags().IntVar(&flagDailySubEntityTypeTimeOfDayNanos, "daily-sub-entity-type-time-of-day-nanos", 0, "Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999.")
	cmd.Flags().IntVar(&flagDailySubEntityTypeTimeOfDayMinutes, "daily-sub-entity-type-time-of-day-minutes", 0, "Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59.")
	cmd.Flags().IntVar(&flagDailyRangeStartDateYear, "daily-range-start-date-year", 0, "Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year.")
	cmd.Flags().IntVar(&flagDailyRangeStartDateDay, "daily-range-start-date-day", 0, "Day of a month.")
	cmd.Flags().IntVar(&flagDailyRangeStartDateMonth, "daily-range-start-date-month", 0, "Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day.")
	cmd.Flags().IntVar(&flagDailyRangeEndDateMonth, "daily-range-end-date-month", 0, "Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day.")
	cmd.Flags().IntVar(&flagDailyRangeEndDateDay, "daily-range-end-date-day", 0, "Day of a month.")
	cmd.Flags().StringVar(&flagDailySubEntityTypeDayOfWeek, "daily-sub-entity-type-day-of-week", "", "Represents the day of the week. Eg: MONDAY. Currently supported DailyMetrics = NONE. (one of: DAY_OF_WEEK_UNSPECIFIED, MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY)")
	cmd.Flags().IntVar(&flagDailySubEntityTypeTimeOfDayHours, "daily-sub-entity-type-time-of-day-hours", 0, "Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23.")

	return cmd
}
