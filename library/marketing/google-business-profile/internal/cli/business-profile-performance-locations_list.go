// Copyright 2026 Raj Yaligar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newBusinessProfilePerformanceLocationsListCmd(flags *rootFlags) *cobra.Command {
	var flagMonthlyRangeStartMonthDay int
	var flagMonthlyRangeStartMonthMonth int
	var flagMonthlyRangeEndMonthMonth int
	var flagPageSize int
	var flagPageToken string
	var flagMonthlyRangeEndMonthDay int
	var flagMonthlyRangeEndMonthYear int
	var flagMonthlyRangeStartMonthYear int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list <parent>",
		Short:       "Returns the search keywords used to find a business in search or maps.",
		Example:     "  google-business-profile-pp-cli business-profile-performance-locations list example-value",
		Annotations: map[string]string{"pp:endpoint": "business-profile-performance-locations.list", "pp:method": "GET", "pp:path": "https://businessprofileperformance.googleapis.com/v1/{parent}/searchkeywords/impressions/monthly", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "https://businessprofileperformance.googleapis.com/v1/{parent}/searchkeywords/impressions/monthly"
			path = replacePathParam(path, "parent", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "business-profile-performance-locations", path, map[string]string{
				"monthlyRange.startMonth.day":   fmt.Sprintf("%v", flagMonthlyRangeStartMonthDay),
				"monthlyRange.startMonth.month": fmt.Sprintf("%v", flagMonthlyRangeStartMonthMonth),
				"monthlyRange.endMonth.month":   fmt.Sprintf("%v", flagMonthlyRangeEndMonthMonth),
				"pageSize":                      fmt.Sprintf("%v", flagPageSize),
				"pageToken":                     fmt.Sprintf("%v", flagPageToken),
				"monthlyRange.endMonth.day":     fmt.Sprintf("%v", flagMonthlyRangeEndMonthDay),
				"monthlyRange.endMonth.year":    fmt.Sprintf("%v", flagMonthlyRangeEndMonthYear),
				"monthlyRange.startMonth.year":  fmt.Sprintf("%v", flagMonthlyRangeStartMonthYear),
			}, nil, flagAll, "pageToken", "page_token", "pageSize", "nextPageToken", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagMonthlyRangeStartMonthDay, "monthly-range-start-month-day", 0, "Day of a month.")
	cmd.Flags().IntVar(&flagMonthlyRangeStartMonthMonth, "monthly-range-start-month-month", 0, "Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day.")
	cmd.Flags().IntVar(&flagMonthlyRangeEndMonthMonth, "monthly-range-end-month-month", 0, "Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Optional. The number of results requested. The default page size is 100. Page size can be set to a maximum of 100.")
	cmd.Flags().StringVar(&flagPageToken, "page-token", "", "Optional. A token indicating the next paginated result to be returned.")
	cmd.Flags().IntVar(&flagMonthlyRangeEndMonthDay, "monthly-range-end-month-day", 0, "Day of a month.")
	cmd.Flags().IntVar(&flagMonthlyRangeEndMonthYear, "monthly-range-end-month-year", 0, "Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year.")
	cmd.Flags().IntVar(&flagMonthlyRangeStartMonthYear, "monthly-range-start-month-year", 0, "Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
