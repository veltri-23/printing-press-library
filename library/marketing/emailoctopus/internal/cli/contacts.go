// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"
)

// newContactsCmd is the novel contacts parent that groups cross-list operations
// computed against the local store or paced rate-limited mutations. Distinct
// from `lists contacts`, which mirrors the per-list contacts endpoint.
func newContactsCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "contacts",
		Short: "Cross-list contact operations: engagement scoring, dedupe, CSV sync, paced bulk delete",
		RunE:  parentNoSubcommandRunE(flags),
	}

	cmd.AddCommand(newContactsEngagementCmd(flags))
	cmd.AddCommand(newContactsDedupeCmd(flags))
	cmd.AddCommand(newContactsSyncCSVCmd(flags))
	cmd.AddCommand(newContactsBulkDeleteCmd(flags))
	return cmd
}
