// Copyright 2026 horknfbr. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newManagementModalsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagLastResetAt string
	var flagShowManageKeywordModal bool
	var flagShowManageListingModal bool
	var flagShowManageShopModal bool

	cmd := &cobra.Command{
		Use:         "management-modals",
		Short:       "GET /management_modals",
		Long:        "Shortcut for 'management_modals list_management_modals'. GET /management_modals",
		Example:     "  everbee-pp-cli management-modals --last-reset-at 2026-01-15T09:00:00Z --show-manage-keyword-modal your-token-here --show-manage-listing-modal true --show-manage-shop-modal true",
		Annotations: map[string]string{"pp:endpoint": "management_modals.list_management_modals", "pp:method": "GET", "pp:path": "/management_modals", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("last-reset-at") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "last-reset-at")
			}
			if !cmd.Flags().Changed("show-manage-keyword-modal") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "show-manage-keyword-modal")
			}
			if !cmd.Flags().Changed("show-manage-listing-modal") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "show-manage-listing-modal")
			}
			if !cmd.Flags().Changed("show-manage-shop-modal") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "show-manage-shop-modal")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/management_modals"
			params := map[string]string{}
			if flagLastResetAt != "" {
				params["last_reset_at"] = fmt.Sprintf("%v", flagLastResetAt)
			}
			if flagShowManageKeywordModal != false {
				params["show_manage_keyword_modal"] = fmt.Sprintf("%v", flagShowManageKeywordModal)
			}
			if flagShowManageListingModal != false {
				params["show_manage_listing_modal"] = fmt.Sprintf("%v", flagShowManageListingModal)
			}
			if flagShowManageShopModal != false {
				params["show_manage_shop_modal"] = fmt.Sprintf("%v", flagShowManageShopModal)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "management_modals", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagLastResetAt, "last-reset-at", "", "")
	cmd.Flags().BoolVar(&flagShowManageKeywordModal, "show-manage-keyword-modal", false, "")
	cmd.Flags().BoolVar(&flagShowManageListingModal, "show-manage-listing-modal", false, "")
	cmd.Flags().BoolVar(&flagShowManageShopModal, "show-manage-shop-modal", false, "")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
