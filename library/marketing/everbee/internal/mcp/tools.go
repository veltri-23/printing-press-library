// Copyright 2026 horknfbr. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/marketing/everbee/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/marketing/everbee/internal/client"
	"github.com/mvanhorn/printing-press-library/library/marketing/everbee/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/marketing/everbee/internal/config"
	"github.com/mvanhorn/printing-press-library/library/marketing/everbee/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/marketing/everbee/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("folders_list_folders",
			mcplib.WithDescription("GET /folders. Optional: type. Returns the folders."),
			mcplib.WithString("type", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/folders", false, nil, []mcpParamBinding{{PublicName: "type", WireName: "type", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keyword_research_list_default_keyword_suggestion",
			mcplib.WithDescription("GET /keyword_research/default_keyword_suggestion. Optional: order_by, order_direction, page (plus 1 more). Returns the default_keyword_suggestion."),
			mcplib.WithString("order_by", mcplib.Description("")),
			mcplib.WithString("order_direction", mcplib.Description("")),
			mcplib.WithNumber("page", mcplib.Description("")),
			mcplib.WithString("type_of_search", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keyword_research/default_keyword_suggestion", false, nil, []mcpParamBinding{{PublicName: "order_by", WireName: "order_by", Location: "query"}, {PublicName: "order_direction", WireName: "order_direction", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "type_of_search", WireName: "type_of_search", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("management_modals_list_management_modals",
			mcplib.WithDescription("GET /management_modals. Required: last_reset_at, show_manage_keyword_modal, show_manage_listing_modal, show_manage_shop_modal. Returns the management_modals."),
			mcplib.WithString("last_reset_at", mcplib.Required(), mcplib.Description("")),
			mcplib.WithBoolean("show_manage_keyword_modal", mcplib.Required(), mcplib.Description("")),
			mcplib.WithBoolean("show_manage_listing_modal", mcplib.Required(), mcplib.Description("")),
			mcplib.WithBoolean("show_manage_shop_modal", mcplib.Required(), mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/management_modals", false, nil, []mcpParamBinding{{PublicName: "last_reset_at", WireName: "last_reset_at", Location: "query"}, {PublicName: "show_manage_keyword_modal", WireName: "show_manage_keyword_modal", Location: "query"}, {PublicName: "show_manage_listing_modal", WireName: "show_manage_listing_modal", Location: "query"}, {PublicName: "show_manage_shop_modal", WireName: "show_manage_shop_modal", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("product_analytics_list_default_product_analytics",
			mcplib.WithDescription("GET /product_analytics/default_product_analytics. Optional: order_by, order_direction, page (plus 2 more). Returns the default_product_analytics."),
			mcplib.WithString("order_by", mcplib.Description("")),
			mcplib.WithString("order_direction", mcplib.Description("")),
			mcplib.WithNumber("page", mcplib.Description("")),
			mcplib.WithNumber("per_page", mcplib.Description("")),
			mcplib.WithString("time_range", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/product_analytics/default_product_analytics", false, nil, []mcpParamBinding{{PublicName: "order_by", WireName: "order_by", Location: "query"}, {PublicName: "order_direction", WireName: "order_direction", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "time_range", WireName: "time_range", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("shops_list_shops",
			mcplib.WithDescription("GET /shops. Optional: order_by, order_direction, page (plus 1 more). Returns the shops."),
			mcplib.WithString("order_by", mcplib.Description("")),
			mcplib.WithString("order_direction", mcplib.Description("")),
			mcplib.WithNumber("page", mcplib.Description("")),
			mcplib.WithNumber("per_page", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/shops", false, nil, []mcpParamBinding{{PublicName: "order_by", WireName: "order_by", Location: "query"}, {PublicName: "order_direction", WireName: "order_direction", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Search EverBee live data for the requested query, cache returned records locally, and fall back to local FTS only when live access is unavailable."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("EverBee search query")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on cached data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against the local cache/snapshots. Use after targeted live pulls for offline analysis, aggregations, history, and joins."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export EVERBEE_ACCESS_TOKEN=<your-key>" +
					"\n      See API docs: https://api.everbee.com" +
					"\n      Run 'everbee-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export EVERBEE_ACCESS_TOKEN=<your-key>" +
					"\n      See API docs: https://api.everbee.com" +
					"\n      Run 'everbee-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export EVERBEE_ACCESS_TOKEN=<your-key>" +
					"\n      See API docs: https://api.everbee.com" +
					"\n      Run 'everbee-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "github.com/mvanhorn/printing-press-library/library/marketing/everbee", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 2)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "github.com/mvanhorn/printing-press-library/library/marketing/everbee", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "everbee",
		"description": "Research Etsy products, shops, and keywords from EverBee in a repeatable agent-ready workflow.",
		"archetype":   "generic",
		"tool_count":  5,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion everbee-pp-cli binary.",
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name":        "EVERBEE_ACCESS_TOKEN",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"docs_url": "https://api.everbee.com",
		},
		"resources": []map[string]any{
			{
				"name":        "folders",
				"description": "Operations on folders",
				"endpoints":   []string{"list_folders"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "keyword_research",
				"description": "Operations on default_keyword_suggestion",
				"endpoints":   []string{"list_default_keyword_suggestion"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "management_modals",
				"description": "Operations on management_modals",
				"endpoints":   []string{"list_management_modals"},
				"searchable":  true,
			},
			{
				"name":        "product_analytics",
				"description": "Operations on default_product_analytics",
				"endpoints":   []string{"list_default_product_analytics"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "shops",
				"description": "Operations on shops",
				"endpoints":   []string{"list_shops"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass page parameter for subsequent pages.",
			"Control page size with the per_page parameter (default 100).",
			"Use typed endpoint tools and command-mirror research commands for current EverBee data; they perform targeted live pulls instead of requiring a bulk sync.",
			"Use the search tool for live EverBee keyword search. It caches returned records and falls back to local FTS only when live access is unavailable.",
			"Use the sql tool for repeated manipulation, joins, and historical analysis over cached research/snapshots after targeted live pulls.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Opportunity Shortlist", "command": "opportunity shortlist", "description": "Rank Etsy product opportunities by combining product analytics, keyword demand, competition, and trend history.", "rationale": "Uses targeted EverBee refreshes and cached research slices to join product, keyword, tag, and snapshot evidence without bulk-syncing all records.", "via": "mcp-command-mirror"},
			{"name": "Niche Score", "command": "niche score", "description": "Score a niche by weighing search demand, competition, product saturation, pricing, and trend movement.", "rationale": "Refreshes the relevant keyword/product slice from EverBee when stale, then reuses cached snapshots for scoring and history.", "via": "mcp-command-mirror"},
			{"name": "Shop Gaps", "command": "shop gaps", "description": "Find competitor shop openings from product mix, pricing bands, tags, and keyword coverage.", "rationale": "Correlates targeted shop analyzer output with cached product and keyword research for the requested shop.", "via": "mcp-command-mirror"},
			{"name": "Tag Gap", "command": "tags gap", "description": "Compare winning listing tags against a target shop or keyword set to reveal missing SEO coverage.", "rationale": "Matches current EverBee product tags and keyword scores with cached research history for the requested query.", "via": "mcp-command-mirror"},
			{"name": "Keyword Clusters", "command": "keywords cluster", "description": "Group related keyword suggestions by term overlap, demand, competition, and opportunity score.", "rationale": "Pulls the relevant keyword suggestions from EverBee when needed, then clusters the cached result set for repeated analysis.", "via": "mcp-command-mirror"},
			{"name": "Trend Diff", "command": "trends diff", "description": "Compare saved research snapshots to show which products, shops, or keywords moved over time.", "rationale": "Requires local historical snapshots; current UI tables cannot produce agent-ready diffs alone.", "via": "mcp-command-mirror"},
			{"name": "Competitor Watch", "command": "competitors watch", "description": "Detect competitor changes in top products, price bands, and tags across saved shop snapshots.", "rationale": "Requires repeated shop analyzer snapshots stored locally.", "via": "mcp-command-mirror"},
			{"name": "Listing Audit", "command": "listing audit", "description": "Audit a listing's keyword and tag fit using EverBee-derived product and keyword context.", "rationale": "Refreshes listing/product/keyword context for the target listing when stale, then uses cached evidence for repeat checks.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Opportunity Shortlist", "insight": "Uses targeted live EverBee refreshes plus cached research slices; no bulk sync required."},
			{"topic": "Niche Score", "insight": "Refreshes stale keyword/product evidence for the requested niche, then reuses cached snapshots for history."},
			{"topic": "Shop Gaps", "insight": "Correlates targeted shop analyzer output with cached product and keyword research for the requested shop."},
			{"topic": "Tag Gap", "insight": "Matches current product tags and keyword scores with cached research history for the requested query."},
			{"topic": "Keyword Clusters", "insight": "Clusters targeted keyword suggestions pulled from EverBee and cached for repeated analysis."},
			{"topic": "Trend Diff", "insight": "Requires local historical snapshots; current UI tables cannot produce agent-ready diffs alone."},
			{"topic": "Competitor Watch", "insight": "Requires repeated shop analyzer snapshots stored locally."},
			{"topic": "Listing Audit", "insight": "Refreshes stale listing/product/keyword evidence for the target listing, then reuses cached context."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
