// Copyright 2026 horknfbr and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import "testing"

func TestNovelTagsCommandIsExecutable(t *testing.T) {
	cmd := newNovelTagsCmd(&rootFlags{})
	if cmd.Use == "" || cmd.RunE == nil {
		t.Fatalf("command is not executable: %#v", cmd)
	}
	found, _, err := cmd.Find([]string{"consensus"})
	if err != nil {
		t.Fatalf("Find(consensus) error = %v", err)
	}
	if found == nil || found.Name() != "consensus" || found.Use == "" || found.RunE == nil {
		t.Fatalf("consensus subcommand is not executable: %#v", found)
	}
}

func TestNovelTagsConsensusCommandIsExecutable(t *testing.T) {
	cmd := newNovelTagsConsensusCmd(&rootFlags{})
	if cmd.Use == "" || cmd.RunE == nil {
		t.Fatalf("command is not executable: %#v", cmd)
	}
}

func TestNovelTagsConsensusDuplicateTagsMerge(t *testing.T) {
	signals := keywordSignals{
		TopListings: []map[string]any{{"tags": []any{"Dad-Gift", "dad gift"}}},
		EtsyTags:    []map[string]any{{"tag": " dad gift "}},
	}
	got := rankConsensusTags(signals, 2)
	if len(got) != 1 {
		t.Fatalf("rankConsensusTags() = %#v, want one merged tag", got)
	}
	if got[0].Tag != "dad gift" || got[0].Count != 2 {
		t.Fatalf("rankConsensusTags() = %#v, want merged dad gift count 2", got)
	}
}
