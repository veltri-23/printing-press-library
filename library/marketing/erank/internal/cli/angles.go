// Copyright 2026 horknfbr and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"strings"

	"github.com/spf13/cobra"
)

func newNovelAnglesCmd(flags *rootFlags) *cobra.Command {
	var source, country string
	var limit int
	cmd := &cobra.Command{
		Use:     "angles [keyword]",
		Short:   "Find evidence-backed product angles from eRank keyword data",
		Example: "  erank-pp-cli angles \"dad mug\" --source etsy --country USA --limit 10 --json",
		Annotations: map[string]string{
			"mcp:read-only": "true",
		},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				return nil
			}
			signals, err := fetchKeywordSignals(cmd.Context(), flags, strings.Join(args, " "), source, country)
			if err != nil {
				return err
			}
			result := map[string]any{
				"keyword":  signals.Keyword,
				"angles":   buildAngles(signals, limit),
				"warnings": signals.Warnings,
			}
			return printJSONFiltered(cmd.OutOrStdout(), result, flags)
		},
	}
	cmd.Flags().StringVar(&source, "source", "etsy", "Marketplace source to query")
	cmd.Flags().StringVar(&country, "country", "USA", "Country code for eRank keyword data")
	cmd.Flags().IntVar(&limit, "limit", 10, "Maximum product angles to return")
	return cmd
}
