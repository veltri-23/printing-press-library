// Copyright 2026 Abe Diaz (@abe238) and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"strings"

	"github.com/spf13/cobra"
)

// nhcGratitude is the thank-you to the people of the National Hurricane Center.
// Wording follows build-decisions.md "Tone & the NHC gratitude note".
const nhcGratitude = "Thank you to the people of the National Hurricane Center: the forecasters, " +
	"meteorologists, hurricane hunters, and support staff who sacrifice so much for the benefit of so many."

// nhcDisclaimer is the unofficial-tool disclaimer that accompanies the
// gratitude note everywhere it appears. Wording follows build-decisions.md.
const nhcDisclaimer = "This is an unofficial tool. The NHC / NWS is the authoritative source. " +
	"In an emergency, follow the official watches, warnings, and evacuation orders from your local authorities."

func newNovelCreditsCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "credits",
		Short: "Thanks the people of the National Hurricane Center and states plainly that this is an unofficial tool and NHC/NWS is",
		Long: "Thanks the people of the National Hurricane Center and states plainly that this is an " +
			"unofficial tool and NHC/NWS is authoritative.",
		Example:     "  nhc-pp-cli credits",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if dryRunOK(flags) {
				return nil
			}
			if flags.asJSON {
				return emitData(cmd, flags, map[string]any{
					"gratitude":  nhcGratitude,
					"disclaimer": nhcDisclaimer,
				})
			}
			var b strings.Builder
			fmt.Fprintln(&b, nhcGratitude)
			fmt.Fprintln(&b)
			fmt.Fprintln(&b, nhcDisclaimer)
			fmt.Fprint(cmd.OutOrStdout(), b.String())
			return nil
		},
	}
	return cmd
}
