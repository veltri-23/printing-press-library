# DefiLlama Absorb Manifest

## Shipped shape

- Full endpoint mirror for the public DefiLlama API surface.
- Agent-native JSON output and MCP bundle support.
- Local datastore support through `sync`, `search`, `analytics`, `export`, and `import`.
- Compound `workflow` command for multi-step API operations.

## Novel features recorded

1. `workflow` — compound multi-operation workflows.
2. `analytics` — local synced-data analysis.
3. `search` — live/local search across DefiLlama data.
