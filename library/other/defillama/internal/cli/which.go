// Copyright 2026 kierandotai and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "batch-historical list", Description: "Strings accepted by period and searchWidth:\nCan use regular chart candle notion like ‘4h’ etc where:\nW = week, D = day, H = hour, M = minute (not case sensitive)", Group: "batch-historical"},
	{Command: "block get", Description: "Runs binary search over a blockchain's blocks to get the closest one to a timestamp.\nEvery time this is run we add new data to our database, so each query permanently speeds up future queries.", Group: "block"},
	{Command: "bridges get", Description: "Get summary of bridge volume and volume breakdown by chain", Group: "bridges"},
	{Command: "bridges get-bridgedaystats", Description: "Get a 24hr token and address volume breakdown for a bridge", Group: "bridges"},
	{Command: "bridges get-bridgevolume", Description: "Get historical volumes for a bridge, chain, or bridge on a particular chain", Group: "bridges"},
	{Command: "bridges get-transactions", Description: "Get all transactions for a bridge within a date range", Group: "bridges"},
	{Command: "bridges list", Description: "List all bridges along with summaries of recent bridge volumes.", Group: "bridges"},
	{Command: "categories list", Description: "Overview of all categories accross all protocols", Group: "categories"},
	{Command: "chain-assets list", Description: "Get assets of all chains", Group: "chain-assets"},
	{Command: "chains list", Description: "Get current TVL of all chains", Group: "chains"},
	{Command: "chart get", Description: "Strings accepted by period and searchWidth:\nCan use regular chart candle notion like ‘4h’ etc where:\nW = week, D = day, H = hour, M = minute (not case sensitive)", Group: "chart"},
	{Command: "chart get-fork", Description: "Returns an array of [timestamp, value] pairs representing TVL over time for all forks of a specific protocol.", Group: "chart"},
	{Command: "chart get-metric", Description: "Returns an array of [timestamp, value] pairs representing the total metric value over time.", Group: "chart"},
	{Command: "chart get-oracle", Description: "Returns an array of [timestamp, value] pairs representing oracle TVL over time for a specific chain.", Group: "chart"},
	{Command: "chart get-oracle-2", Description: "Returns an array of [timestamp, value] pairs representing TVL over time for a specific oracle/protocol.", Group: "chart"},
	{Command: "chart get-oracle-3", Description: "Returns an array of objects with a timestamp and TVL values broken down by oracle/protocol for a specific chain.", Group: "chart"},
	{Command: "chart get-oracle-4", Description: "Returns an array of objects with a timestamp and TVL values broken down by chain for a specific oracle/protocol.", Group: "chart"},
	{Command: "chart get-pool", Description: "Get historical APY and TVL of a pool", Group: "chart"},
	{Command: "chart get-treasury", Description: "Returns an array of [timestamp, value] pairs representing the protocol's treasury value over time. By default excludes the protocol's own tokens (OwnTokens).", Group: "chart"},
	{Command: "chart get-treasury-2", Description: "Returns an array of [timestamp, { chain: value }] pairs showing treasury value per chain over time. By default excludes the protocol's own tokens.", Group: "chart"},
	{Command: "chart get-treasury-3", Description: "Returns an array of [timestamp, { token: value }] pairs showing treasury value per token over time. By default excludes OwnTokens and values are in USD. Use key and currency params to customize.", Group: "chart"},
	{Command: "chart get-tvl", Description: "Returns an array of [timestamp, value] pairs representing the protocol's TVL over time. By default returns the base TVL metric. Use the `key` parameter to select a different metric or aggregate all metrics.", Group: "chart"},
	{Command: "chart get-tvl-2", Description: "Returns an array of [timestamp, { chain: value }] pairs showing the selected metric per chain over time.", Group: "chart"},
	{Command: "chart get-tvl-3", Description: "Returns an array of [timestamp, { token: value }] pairs showing TVL per token over time. Values are in USD by default, set currency=tokens for raw token amounts.", Group: "chart"},
	{Command: "chart list", Description: "Returns an array of [timestamp, value] pairs representing total TVL across all oracles over time.", Group: "chart"},
	{Command: "chart list-fork", Description: "Returns an array of objects with a timestamp and TVL values broken down by fork protocol.", Group: "chart"},
	{Command: "chart list-oracle", Description: "Returns an array of objects with a timestamp and TVL values broken down by chain.", Group: "chart"},
	{Command: "chart list-oracle-2", Description: "Returns an array of objects with a timestamp and TVL values broken down by oracle/protocol.", Group: "chart"},
	{Command: "chart category get", Description: "Returns an array of [timestamp, value] pairs for the metric filtered by a specific category.", Group: "chart"},
	{Command: "chart category get-chart", Description: "Returns an array of [timestamp, {chain: value}] pairs showing the metric for a specific category broken down by chain.", Group: "chart"},
	{Command: "chart category get-chart-2", Description: "Returns an array of [timestamp, {protocol: value}] pairs showing the metric for a specific category broken down by protocol.", Group: "chart"},
	{Command: "chart category get-chart-3", Description: "Returns an array of [timestamp, value] pairs for the metric filtered by both category and chain.", Group: "chart"},
	{Command: "chart category get-chart-4", Description: "Returns an array of [timestamp, {protocol: value}] pairs showing the metric filtered by both category and chain, broken down by protocol.", Group: "chart"},
	{Command: "chart chain get", Description: "Returns an array of [timestamp, value] pairs for the metric filtered by a specific chain.", Group: "chart"},
	{Command: "chart chain get-chart", Description: "Returns an array of [timestamp, {protocol: value}] pairs showing the metric on a specific chain broken down by protocol.", Group: "chart"},
	{Command: "chart chain-breakdown get", Description: "Returns an array of [timestamp, {chain: value}] pairs showing the metric broken down by chain over time.", Group: "chart"},
	{Command: "chart protocol get", Description: "Returns an array of [timestamp, value] pairs for the metric of a specific protocol.", Group: "chart"},
	{Command: "chart protocol get-chart", Description: "Returns an array of [timestamp, {chain: value}] pairs showing the metric for a specific protocol broken down by chain.", Group: "chart"},
	{Command: "chart protocol get-chart-2", Description: "Returns an array of [timestamp, {label: value}] pairs showing the metric for a specific protocol broken down by label (e.g. Borrow Interest, Flash Loans). Currently only supported for metric=fees.", Group: "chart"},
	{Command: "chart protocol get-chart-3", Description: "Returns an array of [timestamp, {version: value}] pairs showing the metric for a specific protocol broken down by version (e.g. Aave V2, Aave V3).", Group: "chart"},
	{Command: "chart protocol-breakdown get", Description: "Returns an array of [timestamp, {protocol: value}] pairs showing the metric broken down by protocol over time.", Group: "chart"},
	{Command: "dat get", Description: "Returns detailed data for a specific institution, including mNAV calculations (realized, realistic, maximum) as described in the [DAT Methodology](https://docs.llama.fi/analysts/dat-methodology)", Group: "dat"},
	{Command: "dat list", Description: "Returns comprehensive data about institutions holding digital assets, including mNAV calculations (realized, realistic, maximum) as described in the [DAT Methodology](https://docs.llama.fi/analysts/dat-methodology)", Group: "dat"},
	{Command: "emission get", Description: "Unlocks data for a given token/protocol. You can find a list of available slugs to query by querying /emissions and then extracting the key `gecko_id`", Group: "emission"},
	{Command: "emissions list", Description: "List of all tokens along with basic info for each", Group: "emissions"},
	{Command: "entities list", Description: "List all entities", Group: "entities"},
	{Command: "equities list", Description: "Returns a list of all publicly traded companies tracked by DefiLlama, with current market summary data for each, sorted by market capitalization (largest first)", Group: "equities"},
	{Command: "equities list-v1", Description: "Returns a list of SEC filings (10-K, 10-Q, etc.) for the given ticker, sorted by filing date descending (newest first).", Group: "equities"},
	{Command: "equities list-v1-2", Description: "Returns daily OHLCV bars as six-number arrays: Unix timestamp in seconds (UTC), open, high, low, close, volume. Sorted by time descending (newest first). Optional `timeframe` filters how far back data goes; omit or empty for full history (`MAX`).", Group: "equities"},
	{Command: "equities list-v1-3", Description: "Returns daily closing prices as two-element arrays: ISO 8601 date-time string, then numeric price. Sorted by date descending (newest first).", Group: "equities"},
	{Command: "equities list-v1-4", Description: "Returns income statement, balance sheet, and cash flow statement for the given ticker, broken down by quarterly and annual periods.", Group: "equities"},
	{Command: "equities list-v1-5", Description: "Returns current market data for a single ticker. This is a compact snapshot (no `ticker` / `name` fields); use `GET /equities/v1/companies` for the list shape that includes company identity and balance-sheet highlights.", Group: "equities"},
	{Command: "etfs list", Description: "Historical Flows at the Asset Level", Group: "etfs"},
	{Command: "etfs list-snapshot", Description: "Get ETFs and their metrics (aum, flows, fees...)", Group: "etfs"},
	{Command: "fdv get", Description: "Get chart of narratives based on category performance (with individual coins weighted by mcap)", Group: "fdv"},
	{Command: "forks list", Description: "Overview of all forks accross all protocols", Group: "forks"},
	{Command: "hacks list", Description: "Overview of all hacks on our Hacks dashboard", Group: "hacks"},
	{Command: "historical-chain-tvl get", Description: "Get historical TVL (excludes liquid staking and double counted tvl) of a chain", Group: "historical-chain-tvl"},
	{Command: "historical-chain-tvl list", Description: "Get historical TVL (excludes liquid staking and double counted tvl) of DeFi on all chains", Group: "historical-chain-tvl"},
	{Command: "historical-liquidity get", Description: "Provides the name of contracts on a determined chain", Group: "historical-liquidity"},
	{Command: "inflows get", Description: "Lists the amount of inflows and outflows for a protocol at a given date", Group: "inflows"},
	{Command: "metrics get", Description: "Returns aggregate metrics for the specified dimension including totals and percentage changes across different time periods.", Group: "metrics"},
	{Command: "metrics get-financialstatement", Description: "Returns protocol metadata, methodology details, and aggregated financial statement data (yearly, quarterly, monthly). Each period contains line items such as Gross Protocol Revenue, Cost Of Revenue, Gross Profit, Token Holder Net Income, Incentives, and Earnings, with values and optional label breakdowns.\n\nWhen querying a parent protocol (e.g. `aave`), the response includes a `childProtocols` array with per-version methodology. When querying a child protocol (e.g. `aave-v3`), methodology and breakdownMethodology are at the top level.", Group: "metrics"},
	{Command: "metrics get-treasury", Description: "Returns protocol metadata along with current treasury figures and chain breakdowns.", Group: "metrics"},
	{Command: "metrics get-tvl", Description: "Returns protocol metadata along with current TVL figures, chain breakdowns, and other aggregate metrics.", Group: "metrics"},
	{Command: "metrics list", Description: "Returns an object mapping fork names to arrays of protocol names that are forks of each protocol.", Group: "metrics"},
	{Command: "metrics list-oracle", Description: "Returns an object mapping oracle names to arrays of protocol names that use each oracle.", Group: "metrics"},
	{Command: "metrics category get", Description: "Returns aggregate metrics for the specified dimension filtered by a specific category.", Group: "metrics"},
	{Command: "metrics category get-metrics", Description: "Returns aggregate metrics for the specified dimension filtered by both category and chain.", Group: "metrics"},
	{Command: "metrics chain get", Description: "Returns aggregate metrics for the specified dimension filtered by a specific chain.", Group: "metrics"},
	{Command: "metrics protocol get", Description: "Returns protocol metadata along with aggregate metrics for the specified dimension.", Group: "metrics"},
	{Command: "oracles list", Description: "Overview of all oracles accross all protocols", Group: "oracles"},
	{Command: "overview get", Description: "List all dexs along with summaries of their volumes and dataType history data filtering by chain", Group: "overview"},
	{Command: "overview get-fees", Description: "List all protocols along with summaries of their fees and revenue and dataType history data by chain", Group: "overview"},
	{Command: "overview get-options", Description: "List all options dexs along with summaries of their volumes and dataType history data filtering by chain", Group: "overview"},
	{Command: "overview list", Description: "List all dexs along with summaries of their volumes and dataType history data", Group: "overview"},
	{Command: "overview list-derivatives", Description: "Lists all derivatives along summaries of their volumes filtering by chain", Group: "overview"},
	{Command: "overview list-fees", Description: "List all protocols along with summaries of their fees and revenue and dataType history data", Group: "overview"},
	{Command: "overview list-openinterest", Description: "List all open interest dex exchanges along with summaries of their open interest", Group: "overview"},
	{Command: "overview list-options", Description: "List all options dexs along with summaries of their volumes and dataType history data", Group: "overview"},
	{Command: "percentage get", Description: "Strings accepted by period:\nCan use regular chart candle notion like ‘4h’ etc where:\nW = week, D = day, H = hour, M = minute (not case sensitive)", Group: "percentage"},
	{Command: "pools list", Description: "Retrieve the latest data for all pools, including enriched information such as predictions", Group: "pools"},
	{Command: "prices get", Description: "The goal of this API is to price as many tokens as possible, including exotic ones that never get traded, which makes them impossible to price by looking at markets.\n\nThe base of our data are prices pulled from coingecko, which is then extended through multiple means:\n- We price all bridged tokens by using the price of the token in it's original chain, so we fetch all bridged versions of USDC on arbitrum, fantom, avax... and price all them using the price for the token on Ethereum, which we know. Right now we support 10 different bridging protocols.\n- We have multiple adapters to price specialized sets of tokens by running custom code:\n  - We price yearn's yToken LPs by checking how much underlying token can be withdrawn for each LP\n  - Aave, compound and euler LP tokens are also priced based on their relationship against underlying tokens\n  - Uniswap, curve, balancer and stargate LPs are priced using the underlying tokens in each pair\n  - GMX's GLP token is priced based on the value of tokens given on withdrawal (which includes calculations based on trader's PnL)\n  \n  - Synthetix tokens are priced using forex prices of the coin they are pegged to\n- For tokens that we haven't been able to price in any other way, we find the pool with most liquidity for each on uniswap, curve and serum and then use the prices provided on those exchanges.\n  \n  Unlike all the other tokens, we can't confirm that these prices are correct, so we only ingest the ones that have sufficient liquidity and, even in that case, we attach a `confidence` value to them that is related to the depth of liquidity and which represents our confidence in the quality of each price. API consumers can choose to filter out prices with low confidence values.\n  \n Our API server is fully open source and we are constantly adding more pricing adapters, extending the amount of tokens we support.\n\n  \nTokens are queried using {chain}:{address}, where chain is an identifier such as ethereum, bsc, polygon, avax... You can also get tokens by coingecko id by setting `coingecko` as the chain, eg: coingecko:ethereum, coingecko:bitcoin. Examples:\n  - ethereum:0xdF574c24545E5FfEcb9a659c229253D4111d87e1\n  - bsc:0x762539b45a1dcce3d36d080f74d1aed37844b878\n  - coingecko:ethereum\n  - arbitrum:0x4277f8f2c384827b5273592ff7cebd9f2c1ac258", Group: "prices"},
	{Command: "prices get-first", Description: "Get earliest timestamp price record for coins", Group: "prices"},
	{Command: "prices get-historical", Description: "See /prices/current for explanation on how prices are sourced.", Group: "prices"},
	{Command: "protocol get", Description: "Get historical TVL of a protocol and breakdowns by token and chain", Group: "protocol"},
	{Command: "protocols list", Description: "List all protocols on defillama along with their tvl", Group: "protocols"},
	{Command: "raises list", Description: "Overview of all raises on our Raises dashboard", Group: "raises"},
	{Command: "rwa get", Description: "Returns current RWA assets that have onchain market cap, active market cap, or DeFi active TVL on the requested chain.", Group: "rwa"},
	{Command: "rwa get-chart", Description: "Returns historical onchain market cap, active market cap, and DeFi active TVL totals for a chain.", Group: "rwa"},
	{Command: "rwa list", Description: "Returns current Real World Asset rows with per-chain onchain market cap, active market cap, and DeFi active TVL maps.", Group: "rwa"},
	{Command: "rwa list-chart", Description: "Returns time series rows with one column per chain for the selected RWA metric.", Group: "rwa"},
	{Command: "rwa list-list", Description: "Returns lightweight RWA lists used for discovery, search, and filters.", Group: "rwa"},
	{Command: "rwa list-stats", Description: "Returns RWA aggregates. For the default per-chain table, read byChain[chain].base: assetIssuers.length, assetCount, activeMcap, onChainMcap, and defiActiveTvl. Add stablecoinsOnly, governanceOnly, and stablecoinsAndGovernance when those buckets should be included.", Group: "rwa"},
	{Command: "stablecoin get", Description: "Get historical mcap and historical chain distribution of a stablecoin", Group: "stablecoin"},
	{Command: "stablecoinchains list", Description: "Get current mcap sum of all stablecoins on each chain", Group: "stablecoinchains"},
	{Command: "stablecoincharts get", Description: "Get historical mcap sum of all stablecoins in a chain", Group: "stablecoincharts"},
	{Command: "stablecoincharts list", Description: "Get historical mcap sum of all stablecoins", Group: "stablecoincharts"},
	{Command: "stablecoinprices list", Description: "Get historical prices of all stablecoins", Group: "stablecoinprices"},
	{Command: "stablecoins get", Description: "Get stablecoin dominance per chain along with the info about the larges coin in a chain", Group: "stablecoins"},
	{Command: "stablecoins list", Description: "List all stablecoins along with their circulating amounts", Group: "stablecoins"},
	{Command: "summary get", Description: "Get summary of dex volume with historical data", Group: "summary"},
	{Command: "summary get-derivatives", Description: "Volume Details about a specific perp protocol", Group: "summary"},
	{Command: "summary get-fees", Description: "Get summary of protocol fees and revenue with historical data", Group: "summary"},
	{Command: "summary get-options", Description: "Get summary of options dex volume with historical data", Group: "summary"},
	{Command: "token-protocols get", Description: "Lists the amount of a certain token within all protocols. Data for the Token Usage page", Group: "token-protocols"},
	{Command: "treasuries list", Description: "List all protocols on our Treasuries dashboard", Group: "treasuries"},
	{Command: "tvl get", Description: "Simplified endpoint that only returns a number, the current TVL of a protocol", Group: "tvl"},
	{Command: "usage list", Description: "Get amount of credits left in the api key, these reset on the 1st of each month", Group: "usage"},
	{Command: "yields get", Description: "Historical borrow cost APY from a pool on a lending market, pool ids should be obtained from /poolsBorrow", Group: "yields"},
	{Command: "yields list", Description: "APY rates of multiple LSDs", Group: "yields"},
	{Command: "yields list-perps", Description: "Funding rates and Open Interest of perps across exchanges, including both Decentralized and Centralized", Group: "yields"},
	{Command: "yields list-poolsborrow", Description: "Borrow costs APY of assets from lending markets", Group: "yields"},
	{Command: "yields list-poolsold", Description: "Same as /pools but it also includes a new parameter `pool_old` which usually contains pool address (but not guaranteed)", Group: "yields"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  defillama-pp-cli which "stale tickets"
  defillama-pp-cli which "bottleneck"
  defillama-pp-cli which --limit 1 "send message"
  defillama-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
