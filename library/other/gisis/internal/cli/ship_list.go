// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelShipListCmd(flags *rootFlags) *cobra.Command {
	var flagOwner string
	var flagType string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Browse vessels you have already fetched, with filters on flag/owner/type and full-text search on name/owner.",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				return nil
			}
			return fmt.Errorf("TODO: implement novel feature %q", "ship list")
		},
	}
	cmd.Flags().StringVar(&flagOwner, "owner", "", "TODO: describe --owner")
	cmd.Flags().StringVar(&flagType, "type", "", "TODO: describe --type")
	return cmd
}
