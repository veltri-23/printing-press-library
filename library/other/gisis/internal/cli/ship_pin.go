// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelShipPinCmd(flags *rootFlags) *cobra.Command {
	var flagLabel []string
	var flagPinned bool
	var flagOlderThan string

	cmd := &cobra.Command{
		Use:         "pin <imo>",
		Short:       "Pin vessels for an active deal or story, then refresh only the pinned ones at a chosen cadence.",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				return nil
			}
			return fmt.Errorf("TODO: implement novel feature %q", "ship pin")
		},
	}
	cmd.Flags().StringSliceVar(&flagLabel, "label", nil, "TODO: describe --label")
	cmd.Flags().BoolVar(&flagPinned, "pinned", false, "TODO: describe --pinned")
	cmd.Flags().StringVar(&flagOlderThan, "older-than", "", "TODO: describe --older-than")
	return cmd
}
