// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newSyncCmd(flags *rootFlags) *cobra.Command {
	return &cobra.Command{
		Use:   "sync",
		Short: "Sync the source into the local store (NOT IMPLEMENTED for GISIS).",
		Long: "GISIS is an HTML page-mode source with no list endpoint, so the generic\n" +
			"spec-driven sync template does not fit. Use the page-mode caching commands\n" +
			"instead: 'ship batch --imos ...' (fetch + cache many vessels), 'ship list'\n" +
			"(browse cache), 'ship pin <imo>' (watchlist), and 'ship refresh --pinned'\n" +
			"(re-fetch all pinned).",
		Example: "  # sync is intentionally not implemented for GISIS — use ship batch instead\n" +
			"  gisis-pp-cli ship batch --imos 9866641,9123456 --json\n" +
			"  gisis-pp-cli ship list --json\n" +
			"  gisis-pp-cli ship refresh --pinned",
		Annotations: map[string]string{
			"mcp:hidden":        "true",
			"pp:requires-tier":  "paid",
		},
		RunE: func(cmd *cobra.Command, args []string) error {
			return fmt.Errorf("sync is not implemented for this CLI; use 'ship batch --imos ...' / 'ship refresh --pinned' instead (GISIS is an HTML page-mode source with no list endpoint)")
		},
	}
}
