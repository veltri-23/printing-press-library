version: 2
project_name: ufo-goat-pp-cli
changelog:
  disable: true
builds:
  - id: ufo-goat-pp-cli
    main: ./cmd/ufo-goat-pp-cli
    binary: ufo-goat-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/other/ufo-goat/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: ufo-goat-pp-mcp
    main: ./cmd/ufo-goat-pp-mcp
    binary: ufo-goat-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: ufo-goat-pp-cli
    repository:
      owner: dave-morin
      name: homebrew-tap
    homepage: "https://github.com/dave-morin/ufo-goat-pp-cli"
    description: "Browse, search, and download declassified UAP files from the War.gov PURSUE archive"
    install: |
      bin.install "ufo-goat-pp-cli"
      bin.install "ufo-goat-pp-mcp"
