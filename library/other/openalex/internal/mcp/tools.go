// Copyright 2026 Hiten Shah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/other/openalex/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/other/openalex/internal/client"
	"github.com/mvanhorn/printing-press-library/library/other/openalex/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/other/openalex/internal/config"
	"github.com/mvanhorn/printing-press-library/library/other/openalex/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/other/openalex/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("authors_get",
			mcplib.WithDescription("Retrieve a single author by OpenAlex ID or ORCID. Required: id. Returns the Author."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("OpenAlex ID (e.g., A5023888391) or ORCID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/authors/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("authors_list",
			mcplib.WithDescription("Get a list of authors with optional filtering, searching, sorting, and pagination. Optional: filter, sort, group_by (plus 5 more). Returns the AuthorsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter authors. Examples: `has_orcid:true`, `last_known_institutions.id:I27837315`, `works_count:>100`,...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`, `summary_stats.h_index`, `summary_stats.i10_index`. Prefix with `-` for...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `last_known_institutions.country_code`, `last_known_institutions.type`, `has_orcid` See [all Author...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/authors", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("autocomplete_autocomplete",
			mcplib.WithDescription("Fast typeahead search returning up to 10 results. Use for search-as-you-type interfaces. Required: entity_type (default: works), q. Optional: filter. Returns the AutocompleteResponse."),
			mcplib.WithString("entity_type", mcplib.Description("Type of entity to search")),
			mcplib.WithString("q", mcplib.Required(), mcplib.Description("Search query string")),
			mcplib.WithString("filter", mcplib.Description("Filter results using field:value syntax. Multiple filters can be combined with commas (AND) or pipes (OR). Available...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/autocomplete/{entity_type}", []mcpParamBinding{{PublicName: "entity_type", WireName: "entity_type", Location: "path"}, {PublicName: "q", WireName: "q", Location: "query"}, {PublicName: "filter", WireName: "filter", Location: "query"}}, []string{"entity_type"}),
	)
	s.AddTool(
		mcplib.NewTool("awards_get",
			mcplib.WithDescription("Retrieve a single award by its OpenAlex ID. Required: id. Returns the AwardFull."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("OpenAlex ID (e.g., G5859020988)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/awards/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("awards_list",
			mcplib.WithDescription("Get a list of research grants and funding awards. Optional: filter, sort, group_by (plus 5 more). Returns the AwardsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter awards. Examples: `funder.id:F4320306076`, `start_year:2023`, `funded_outputs_count:>10` See [all Award...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `funded_outputs_count`, `amount`, `start_year`. Prefix with `-` for descending. See [all Award...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `funder.id`, `start_year`, `currency` See [all Award...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/awards", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("changefiles_get",
			mcplib.WithDescription("Get details for a specific date's changefile, including which entity types changed, how many records, and download links in JSONL and Parquet formats. For a full guide on using changefiles to keep your local data current, see [Download Changefiles](/download/changefiles). **Requires a paid plan** — contact sales@openalex.org or see [pricing](https://openalex.org/pricing). Required: date. Returns the ChangefilesGetResponse."),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("The changefile date in YYYY-MM-DD format")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/changefiles/{date}", []mcpParamBinding{{PublicName: "date", WireName: "date", Location: "path"}}, []string{"date"}),
	)
	s.AddTool(
		mcplib.NewTool("changefiles_list",
			mcplib.WithDescription("List all available changefile dates. Each date has downloadable files containing every entity record that was created or modified on that day. For a full guide on using changefiles to keep your local data current, see [Download Changefiles](/download/changefiles). **Requires a paid plan** — contact sales@openalex.org or see [pricing](https://openalex.org/pricing). Returns the ChangefilesListResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/changefiles", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("concepts_get",
			mcplib.WithDescription("**DEPRECATED:** Use Topics instead. Retrieve a single concept by OpenAlex ID. Required: id. Returns the Concept."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("OpenAlex ID (e.g., C41008148 for Computer Science)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/concepts/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("concepts_list",
			mcplib.WithDescription("**DEPRECATED:** Use Topics instead. Get a list of concepts from the legacy taxonomy. Optional: filter, sort, group_by (plus 5 more). Returns the ConceptsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter concepts. Examples: `level:0` (top-level), `ancestors.id:C41008148`, `wikidata_id:Q21198` See [all Concept...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`, `level`. Prefix with `-` for descending. See [all Concept...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `level` See [all Concept fields](/api-reference/concepts#available-filter-sort-and-group_by-fields).")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/concepts", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("continents_get",
			mcplib.WithDescription("Retrieve a single continent by its Wikidata Q-ID. Required: id. Returns the Continent."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Wikidata Q-ID (e.g., Q49 for North America)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/continents/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("continents_list",
			mcplib.WithDescription("Get a list of continents (7 total). Optional: filter, sort, search (plus 5 more). Returns the ContinentsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter continents. Examples: `id:Q46` (Europe), `works_count:>10000000` See [all Continent...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending. See [all Continent...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithString("group_by", mcplib.Description("Group results by a field and return counts. Available fields vary by entity type. See [all Continent...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/continents", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("countries_get-country",
			mcplib.WithDescription("Retrieve a single country by its ISO 3166-1 alpha-2 code. Required: id. Returns the Country."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("ISO 3166-1 alpha-2 country code (e.g., US, GB, CN)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/countries/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("countries_list",
			mcplib.WithDescription("Get a list of countries. Useful for filtering works by author affiliation country. Optional: filter, sort, search (plus 5 more). Returns the CountriesListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter countries. Examples: `id:US`, `works_count:>1000000` See [all Country...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending. See [all Country...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithString("group_by", mcplib.Description("Group results by a field and return counts. Available fields vary by entity type. See [all Country...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/countries", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("domains_get",
			mcplib.WithDescription("Retrieve a single domain by its ID (1-4). Required: id. Returns the Domain."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Domain ID (1-4)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/domains/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("domains_list",
			mcplib.WithDescription("Get a list of domains (top-level topic categories). There are only 4 domains: Life Sciences, Social Sciences, Physical Sciences, and Health Sciences. Optional: filter, sort, search (plus 5 more). Returns the DomainsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter domains. Examples: `id:1`, `works_count:>10000000` See [all Domain...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending. See [all Domain...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithString("group_by", mcplib.Description("Group results by a field and return counts. Available fields vary by entity type. See [all Domain...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/domains", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("fields_get",
			mcplib.WithDescription("Retrieve a single field by its ID. Required: id. Returns the Field."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Field ID (e.g., 27 for Medicine)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/fields/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("fields_list",
			mcplib.WithDescription("Get a list of fields (second-level topic categories). There are 26 fields spread across 4 domains. Optional: filter, sort, group_by (plus 5 more). Returns the FieldsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter fields. Examples: `domain.id:1`, `works_count:>1000000` See [all Field...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending. See [all Field...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `domain.id` See [all Field fields](/api-reference/fields#available-filter-sort-and-group_by-fields).")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/fields", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("funders_get",
			mcplib.WithDescription("Retrieve a single funder by OpenAlex ID or Crossref Funder ID. Required: id. Returns the Funder."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("OpenAlex ID (e.g., F4320306076) or Crossref Funder ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/funders/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("funders_list",
			mcplib.WithDescription("Get a list of funders with optional filtering, searching, sorting, and pagination. Optional: filter, sort, group_by (plus 5 more). Returns the FundersListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter funders. Examples: `country_code:US`, `is_global_south:true`, `continent:Q46` See [all Funder...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`, `awards_count`. Prefix with `-` for descending. See [all Funder...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `country_code`, `continent`, `is_global_south` See [all Funder...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/funders", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("institution-types_list",
			mcplib.WithDescription("Get a list of institution types (education, healthcare, company, etc.). Optional: filter, sort, search (plus 5 more). Returns the InstitutionTypesListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter institution types. Examples: `id:education`, `works_count:>1000000`")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending.")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithString("group_by", mcplib.Description("Group results by a field and return counts. Available fields vary by entity type.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/institution-types", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("institutions_get",
			mcplib.WithDescription("Retrieve a single institution by OpenAlex ID or ROR. Required: id. Returns the Institution."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("OpenAlex ID (e.g., I27837315) or ROR")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/institutions/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("institutions_list",
			mcplib.WithDescription("Get a list of institutions with optional filtering, searching, sorting, and pagination. Optional: filter, sort, group_by (plus 5 more). Returns the InstitutionsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter institutions. Examples: `country_code:US`, `type:education`, `is_global_south:true`, `continent:Q46`,...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`, `summary_stats.h_index`. Prefix with `-` for descending. See [all...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `country_code`, `type`, `continent`, `is_global_south` See [all Institution...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/institutions", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_get",
			mcplib.WithDescription("Retrieve a single keyword by OpenAlex ID. Required: id. Returns the Keyword."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("OpenAlex ID (e.g., K12345)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_list",
			mcplib.WithDescription("Get a list of keywords with optional filtering, searching, sorting, and pagination. Optional: filter, sort, search (plus 5 more). Returns the KeywordsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter keywords. Examples: `works_count:>100`, `cited_by_count:>1000` See [all Keyword...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending. See [all Keyword...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithString("group_by", mcplib.Description("Group results by a field and return counts. Available fields vary by entity type. See [all Keyword...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("languages_get",
			mcplib.WithDescription("Retrieve a single language by its ISO 639-1 code. Required: id. Returns the Language."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("ISO 639-1 language code (e.g., en, zh, es)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/languages/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("languages_list",
			mcplib.WithDescription("Get a list of languages used in scholarly works. Optional: filter, sort, search (plus 5 more). Returns the LanguagesListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter languages. Examples: `id:en` (English), `works_count:>100000` See [all Language...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending. See [all Language...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithString("group_by", mcplib.Description("Group results by a field and return counts. Available fields vary by entity type. See [all Language...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/languages", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("licenses_list",
			mcplib.WithDescription("Get a list of open access licenses (CC BY, CC BY-SA, etc.). Optional: filter, sort, search (plus 5 more). Returns the LicensesListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter licenses. Examples: `id:cc-by`, `works_count:>100000`")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending.")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithString("group_by", mcplib.Description("Group results by a field and return counts. Available fields vary by entity type.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/licenses", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("publishers_get",
			mcplib.WithDescription("Retrieve a single publisher by OpenAlex ID. Required: id. Returns the Publisher."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("OpenAlex ID (e.g., P4310319965)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/publishers/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("publishers_list",
			mcplib.WithDescription("Get a list of publishers with optional filtering, searching, sorting, and pagination. Optional: filter, sort, group_by (plus 5 more). Returns the PublishersListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter publishers. Examples: `country_codes:US`, `hierarchy_level:0`, `lineage:P4310319965` See [all Publisher...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`, `summary_stats.h_index`. Prefix with `-` for descending. See [all...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `country_codes`, `hierarchy_level` See [all Publisher...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/publishers", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rate-limit_get",
			mcplib.WithDescription("Check your current rate limit status including usage and remaining allowance. Returns the RateLimitResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/rate-limit", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sdgs_get",
			mcplib.WithDescription("Retrieve a single Sustainable Development Goal by its ID (1-17). Required: id. Returns the Sdg."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("SDG ID (1-17)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/sdgs/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("sdgs_list",
			mcplib.WithDescription("Get a list of UN Sustainable Development Goals. There are 17 SDGs. Optional: filter, sort, search (plus 5 more). Returns the SdgsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter SDGs. Examples: `id:3` (Good Health), `works_count:>100000` See [all SDG...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending. See [all SDG...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithString("group_by", mcplib.Description("Group results by a field and return counts. Available fields vary by entity type. See [all SDG...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/sdgs", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("source-types_list",
			mcplib.WithDescription("Get a list of source types (journal, repository, conference, etc.). Optional: filter, sort, search (plus 5 more). Returns the SourceTypesListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter source types. Examples: `id:journal`, `works_count:>100000`")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending.")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithString("group_by", mcplib.Description("Group results by a field and return counts. Available fields vary by entity type.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/source-types", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sources_get",
			mcplib.WithDescription("Retrieve a single source by OpenAlex ID or ISSN. Required: id. Returns the Source."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("OpenAlex ID (e.g., S1983995261) or ISSN")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/sources/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("sources_list",
			mcplib.WithDescription("Get a list of sources (journals, repositories, conferences) with optional filtering, searching, sorting, and pagination. Optional: filter, sort, group_by (plus 5 more). Returns the SourcesListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter sources. Examples: `is_oa:true`, `is_in_doaj:true`, `type:journal`, `country_code:US`,...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`, `summary_stats.h_index`. Prefix with `-` for descending. See [all Source...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `type`, `country_code`, `is_oa`, `is_in_doaj` See [all Source...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/sources", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("subfields_get",
			mcplib.WithDescription("Retrieve a single subfield by its ID. Required: id. Returns the Subfield."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Subfield ID (e.g., 1702 for Artificial Intelligence)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/subfields/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("subfields_list",
			mcplib.WithDescription("Get a list of subfields (third-level topic categories). There are 254 subfields spread across 26 fields. Optional: filter, sort, group_by (plus 5 more). Returns the SubfieldsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter subfields. Examples: `field.id:17`, `domain.id:3`, `works_count:>100000` See [all Subfield...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending. See [all Subfield...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `field.id`, `domain.id` See [all Subfield fields](/api-reference/subfields#available-filter-sort-and-group_...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/subfields", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("text_classify",
			mcplib.WithDescription("**DEPRECATED:** This endpoint is deprecated and not recommended for new projects. It will not receive updates or support. Classify arbitrary text to find relevant OpenAlex topics. Costs $0.01 per request. Optional: title, abstract. Returns the TextClassificationResponse."),
			mcplib.WithString("title", mcplib.Description("Title text to classify")),
			mcplib.WithString("abstract", mcplib.Description("Abstract text to classify")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/text/topics", []mcpParamBinding{{PublicName: "title", WireName: "title", Location: "query"}, {PublicName: "abstract", WireName: "abstract", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("topics_get",
			mcplib.WithDescription("Retrieve a single topic by OpenAlex ID. Required: id. Returns the Topic."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("OpenAlex ID (e.g., T12419)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/topics/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("topics_list",
			mcplib.WithDescription("Get a list of topics with optional filtering, searching, sorting, and pagination. Topics are research areas automatically assigned to works. Optional: filter, sort, group_by (plus 5 more). Returns the TopicsListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter topics. Examples: `domain.id:1`, `field.id:17`, `subfield.id:1702`, `works_count:>1000` See [all Topic...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending. See [all Topic...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `domain.id`, `field.id`, `subfield.id` See [all Topic...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/topics", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("work-types_list",
			mcplib.WithDescription("Get a list of work types (article, book, dataset, etc.). Optional: filter, sort, search (plus 5 more). Returns the WorkTypesListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter work types. Examples: `id:article`, `works_count:>1000000`")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `works_count`, `cited_by_count`. Prefix with `-` for descending.")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithString("group_by", mcplib.Description("Group results by a field and return counts. Available fields vary by entity type.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/work-types", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("works_get",
			mcplib.WithDescription("Retrieve a single work by its OpenAlex ID or external ID (DOI, PMID, PMCID, MAG ID). External IDs can be passed as full URLs or URN format (e.g., `doi:10.1234/example` or `pmid:12345678`). Required: id. Returns the Work."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("OpenAlex ID (e.g., W2741809807) or external ID (DOI, PMID, PMCID, MAG)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/works/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("works_list",
			mcplib.WithDescription("Get a list of scholarly works with optional filtering, searching, sorting, and pagination. Works include journal articles, books, datasets, theses, and more. Optional: filter, sort, group_by (plus 5 more). Returns the WorksListResponse."),
			mcplib.WithString("filter", mcplib.Description("Filter works. Examples: `publication_year:2023`, `type:article`, `open_access.is_oa:true`, `author.id:A5023888391`,...")),
			mcplib.WithString("sort", mcplib.Description("Sort by: `cited_by_count`, `publication_date`, `relevance_score` (with search). Prefix with `-` for descending. See...")),
			mcplib.WithString("group_by", mcplib.Description("Group by: `publication_year`, `type`, `open_access.is_oa`, `authorships.institutions.country_code` See [all Work...")),
			mcplib.WithString("search", mcplib.Description("Full-text search across titles, abstracts, and other text fields.")),
			mcplib.WithString("per_page", mcplib.Description("Number of results per page (1-100, default 25)")),
			mcplib.WithString("page", mcplib.Description("Page number for pagination. Use cursor for deep pagination beyond 10,000 results.")),
			mcplib.WithString("cursor", mcplib.Description("Cursor for deep pagination. Use `cursor=*` to start, then use the `next_cursor` from the response.")),
			mcplib.WithString("sample", mcplib.Description("Return a random sample of N results (max 10,000). Cannot be used with sort or page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/works", []mcpParamBinding{{PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "search", WireName: "search", Location: "query"}, {PublicName: "per_page", WireName: "per_page", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "sample", WireName: "sample", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export OPENALEX_API_KEY=<your-key>" +
					"\n      Run 'openalex-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export OPENALEX_API_KEY=<your-key>" +
					"\n      Run 'openalex-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export OPENALEX_API_KEY=<your-key>" +
					"\n      Run 'openalex-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "openalex-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "openalex-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "openalex",
		"description": "The OpenAlex API provides access to a comprehensive catalog of scholarly works, authors, sources, institutions,...",
		"archetype":   "generic",
		"tool_count":  43,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion openalex-pp-cli binary.",
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name":        "OPENALEX_API_KEY",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "authors",
				"description": "People who create scholarly works",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "autocomplete",
				"description": "Fast typeahead search for any entity type",
				"endpoints":   []string{"autocomplete"},
				"searchable":  true,
			},
			{
				"name":        "awards",
				"description": "Research grants and funding awards",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "changefiles",
				"description": "Manage changefiles",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "concepts",
				"description": "Legacy taxonomy of research areas (deprecated - use Topics instead)",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "continents",
				"description": "Geographic continents (7 total)",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "countries",
				"description": "Geographic countries for filtering research by location",
				"endpoints":   []string{"get-country", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "domains",
				"description": "Top-level categories in the topic hierarchy (4 total)",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "fields",
				"description": "Second-level categories in the topic hierarchy (26 total)",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "funders",
				"description": "Organizations that fund research",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "institution-types",
				"description": "Types of institutions (education, healthcare, company, etc.)",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "institutions",
				"description": "Universities, research organizations, and other affiliations",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "keywords",
				"description": "Short phrases identified from works' topics",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "languages",
				"description": "Languages of scholarly works",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "licenses",
				"description": "Open access licenses (CC BY, CC BY-SA, etc.)",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "publishers",
				"description": "Companies and organizations that publish scholarly works",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "rate-limit",
				"description": "Manage rate limit",
				"endpoints":   []string{"get"},
				"syncable":    true,
			},
			{
				"name":        "sdgs",
				"description": "UN Sustainable Development Goals (17 total)",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "source-types",
				"description": "Types of sources (journal, repository, conference, etc.)",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "sources",
				"description": "Journals, repositories, and other venues where works are hosted",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "subfields",
				"description": "Third-level categories in the topic hierarchy (254 total)",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "text",
				"description": "Manage text",
				"endpoints":   []string{"classify"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "topics",
				"description": "Research topics automatically assigned to works",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "work-types",
				"description": "Types of scholarly works (article, book, dataset, etc.)",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "works",
				"description": "Scholarly documents like journal articles, books, datasets, and theses",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass cursor parameter for subsequent pages.",
			"Control page size with the per_page parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Agent-ready works search", "command": "works list", "description": "Search scholarly works with compact JSON, field selection, filters, sorting, and live API fallback.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Author discovery", "command": "authors list", "description": "Search authors and retrieve canonical OpenAlex author records for citation and collaborator research.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Institution discovery", "command": "institutions list", "description": "Search institutions with country/type metadata for affiliation and ecosystem analysis.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Source discovery", "command": "sources list", "description": "Search journals, repositories, and venues with compact agent-oriented output.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Topic taxonomy lookup", "command": "topics list", "description": "Explore OpenAlex topics for research-area discovery and downstream filtering.", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Agent-ready works search", "insight": ""},
			{"topic": "Author discovery", "insight": ""},
			{"topic": "Institution discovery", "insight": ""},
			{"topic": "Source discovery", "insight": ""},
			{"topic": "Topic taxonomy lookup", "insight": ""},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
