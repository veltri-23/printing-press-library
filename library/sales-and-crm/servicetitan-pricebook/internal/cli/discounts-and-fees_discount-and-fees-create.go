// Copyright 2026 Pierce and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newDiscountsAndFeesDiscountAndFeesCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAccount string
	var bodyActive bool
	var bodyAmount float64
	var bodyAmountType string
	var bodyAssets string
	var bodyBonus float64
	var bodyBudgetCostCode string
	var bodyBudgetCostType string
	var bodyCategories string
	var bodyCode string
	var bodyCommissionBonus float64
	var bodyCrossSaleGroup string
	var bodyDescription string
	var bodyDisplayName string
	var bodyExcludeFromPayroll bool
	var bodyExternalData string
	var bodyHours float64
	var bodyIntacctGlGroupAccount string
	var bodyLimit float64
	var bodyPaysCommission bool
	var bodyTaxable bool
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "discount-and-fees-create <tenant>",
		Aliases:     []string{"create"},
		Short:       "Post to add a new discount or fee to your pricebook",
		Example:     "  servicetitan-pricebook-pp-cli discounts-and-fees discount-and-fees-create 42 --amount-type example-value",
		Annotations: map[string]string{"pp:endpoint": "discounts-and-fees.discount-and-fees-create", "pp:method": "POST", "pp:path": "/tenant/{tenant}/discounts-and-fees"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("amount") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "amount")
				}
				if !cmd.Flags().Changed("amount-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "amount-type")
				}
				if !cmd.Flags().Changed("code") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "code")
				}
				if !cmd.Flags().Changed("description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "description")
				}
				if !cmd.Flags().Changed("type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/tenant/{tenant}/discounts-and-fees"
			path = replacePathParam(path, "tenant", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccount != "" {
					body["account"] = bodyAccount
				}
				if bodyActive != false {
					body["active"] = bodyActive
				}
				if bodyAmount != 0.0 {
					body["amount"] = bodyAmount
				}
				if bodyAmountType != "" {
					body["amountType"] = bodyAmountType
				}
				if bodyAssets != "" {
					var parsedAssets any
					if err := json.Unmarshal([]byte(bodyAssets), &parsedAssets); err != nil {
						return fmt.Errorf("parsing --assets JSON: %w", err)
					}
					body["assets"] = parsedAssets
				}
				if bodyBonus != 0.0 {
					body["bonus"] = bodyBonus
				}
				if bodyBudgetCostCode != "" {
					body["budgetCostCode"] = bodyBudgetCostCode
				}
				if bodyBudgetCostType != "" {
					body["budgetCostType"] = bodyBudgetCostType
				}
				if bodyCategories != "" {
					var parsedCategories any
					if err := json.Unmarshal([]byte(bodyCategories), &parsedCategories); err != nil {
						return fmt.Errorf("parsing --categories JSON: %w", err)
					}
					body["categories"] = parsedCategories
				}
				if bodyCode != "" {
					body["code"] = bodyCode
				}
				if bodyCommissionBonus != 0.0 {
					body["commissionBonus"] = bodyCommissionBonus
				}
				if bodyCrossSaleGroup != "" {
					body["crossSaleGroup"] = bodyCrossSaleGroup
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDisplayName != "" {
					body["displayName"] = bodyDisplayName
				}
				if bodyExcludeFromPayroll != false {
					body["excludeFromPayroll"] = bodyExcludeFromPayroll
				}
				if bodyExternalData != "" {
					body["externalData"] = bodyExternalData
				}
				if bodyHours != 0.0 {
					body["hours"] = bodyHours
				}
				if bodyIntacctGlGroupAccount != "" {
					body["intacctGlGroupAccount"] = bodyIntacctGlGroupAccount
				}
				if bodyLimit != 0.0 {
					body["limit"] = bodyLimit
				}
				if bodyPaysCommission != false {
					body["paysCommission"] = bodyPaysCommission
				}
				if bodyTaxable != false {
					body["taxable"] = bodyTaxable
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "discounts-and-fees",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAccount, "account", "", "The accounting account assigned to the SKU")
	cmd.Flags().BoolVar(&bodyActive, "active", false, "Active shows if this item is currently active")
	cmd.Flags().Float64Var(&bodyAmount, "amount", 0.0, "Amount is either the flat amount or percentage you want discounted/added")
	cmd.Flags().StringVar(&bodyAmountType, "amount-type", "", "The amount type takes either 'percentage' or 'flat ' and apply the 'amount' field accordingly")
	cmd.Flags().StringVar(&bodyAssets, "assets", "", "Images, videos or PDFs attached to SKU")
	cmd.Flags().Float64Var(&bodyBonus, "bonus", 0.0, "Flat rate bonus paid for this item")
	cmd.Flags().StringVar(&bodyBudgetCostCode, "budget-cost-code", "", "The Budget CostCode segment for this entity. (Note: BudgetCostType should also be provided)")
	cmd.Flags().StringVar(&bodyBudgetCostType, "budget-cost-type", "", "The Budget CostType segment for this entity (Note: BudgetCostCode should also be provided)")
	cmd.Flags().StringVar(&bodyCategories, "categories", "", "The category technicians will use to find the item")
	cmd.Flags().StringVar(&bodyCode, "code", "", "Code used for the discount or fee")
	cmd.Flags().Float64Var(&bodyCommissionBonus, "commission-bonus", 0.0, "Percentage rate bonus paid for this item")
	cmd.Flags().StringVar(&bodyCrossSaleGroup, "cross-sale-group", "", "A grouping of similar items that you'll then be able to track as a separate columns on the Technical Performance Board.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description for the item")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "Name for the discount of fee")
	cmd.Flags().BoolVar(&bodyExcludeFromPayroll, "exclude-from-payroll", false, "Exclude from payroll")
	cmd.Flags().StringVar(&bodyExternalData, "external-data", "", "Optional model that contains a list of external data items that should be attached to this entity.")
	cmd.Flags().Float64Var(&bodyHours, "hours", 0.0, "The number of hours associated with the SKU")
	cmd.Flags().StringVar(&bodyIntacctGlGroupAccount, "intacct-gl-group-account", "", "Intacct GL Group Name")
	cmd.Flags().Float64Var(&bodyLimit, "limit", 0.0, "The maximum amount that can be applied for this item")
	cmd.Flags().BoolVar(&bodyPaysCommission, "pays-commission", false, "PaysCommissions shows if for this item commission payed")
	cmd.Flags().BoolVar(&bodyTaxable, "taxable", false, "Should tax be applied when the item is added on an estimate or invoice")
	cmd.Flags().StringVar(&bodyType, "type", "", "Type should be entered as Discount or Fee to show the type")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
