// Copyright 2026 bossriceshark and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newEmailAccountsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyBcc string
	var bodyClientId int
	var bodyCustomTrackingUrl string
	var bodyDailyRampup int
	var bodyFromEmail string
	var bodyFromName string
	var bodyImapHost string
	var bodyImapPort int
	var bodyMaxEmailPerDay int
	var bodyPassword string
	var bodyReplyRatePercentage int
	var bodySignature string
	var bodySmtpHost string
	var bodySmtpPort int
	var bodyTotalWarmupPerDay int
	var bodyUserName string
	var bodyWarmupEnabled bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Add a new email sender account",
		Example:     "  smartlead-pp-cli email-accounts create --from-email user@example.com",
		Annotations: map[string]string{"pp:endpoint": "email-accounts.create", "pp:method": "POST", "pp:path": "/email-accounts"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("from-email") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "from-email")
				}
				if !cmd.Flags().Changed("from-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "from-name")
				}
				if !cmd.Flags().Changed("imap-host") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "imap-host")
				}
				if !cmd.Flags().Changed("imap-port") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "imap-port")
				}
				if !cmd.Flags().Changed("password") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "password")
				}
				if !cmd.Flags().Changed("smtp-host") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "smtp-host")
				}
				if !cmd.Flags().Changed("smtp-port") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "smtp-port")
				}
				if !cmd.Flags().Changed("user-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "user-name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/email-accounts"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyBcc != "" {
					body["bcc"] = bodyBcc
				}
				if bodyClientId != 0 {
					body["client_id"] = bodyClientId
				}
				if bodyCustomTrackingUrl != "" {
					body["custom_tracking_url"] = bodyCustomTrackingUrl
				}
				if bodyDailyRampup != 0 {
					body["daily_rampup"] = bodyDailyRampup
				}
				if bodyFromEmail != "" {
					body["from_email"] = bodyFromEmail
				}
				if bodyFromName != "" {
					body["from_name"] = bodyFromName
				}
				if bodyImapHost != "" {
					body["imap_host"] = bodyImapHost
				}
				if bodyImapPort != 0 {
					body["imap_port"] = bodyImapPort
				}
				if bodyMaxEmailPerDay != 0 {
					body["max_email_per_day"] = bodyMaxEmailPerDay
				}
				if bodyPassword != "" {
					body["password"] = bodyPassword
				}
				if bodyReplyRatePercentage != 0 {
					body["reply_rate_percentage"] = bodyReplyRatePercentage
				}
				if bodySignature != "" {
					body["signature"] = bodySignature
				}
				if bodySmtpHost != "" {
					body["smtp_host"] = bodySmtpHost
				}
				if bodySmtpPort != 0 {
					body["smtp_port"] = bodySmtpPort
				}
				if bodyTotalWarmupPerDay != 0 {
					body["total_warmup_per_day"] = bodyTotalWarmupPerDay
				}
				if bodyUserName != "" {
					body["user_name"] = bodyUserName
				}
				if bodyWarmupEnabled != false {
					body["warmup_enabled"] = bodyWarmupEnabled
				}
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "email-accounts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyBcc, "bcc", "", "BCC address for all sends")
	cmd.Flags().IntVar(&bodyClientId, "client-id", 0, "Optional client/whitelabel ID")
	cmd.Flags().StringVar(&bodyCustomTrackingUrl, "custom-tracking-url", "", "Custom open/click tracking domain")
	cmd.Flags().IntVar(&bodyDailyRampup, "daily-rampup", 0, "Warmup ramp-up increment per day")
	cmd.Flags().StringVar(&bodyFromEmail, "from-email", "", "Sender email address")
	cmd.Flags().StringVar(&bodyFromName, "from-name", "", "Display name on outgoing mail")
	cmd.Flags().StringVar(&bodyImapHost, "imap-host", "", "IMAP server host")
	cmd.Flags().IntVar(&bodyImapPort, "imap-port", 0, "IMAP server port")
	cmd.Flags().IntVar(&bodyMaxEmailPerDay, "max-email-per-day", 0, "Daily send cap")
	cmd.Flags().StringVar(&bodyPassword, "password", "", "SMTP/IMAP password")
	cmd.Flags().IntVar(&bodyReplyRatePercentage, "reply-rate-percentage", 0, "Warmup reply rate target")
	cmd.Flags().StringVar(&bodySignature, "signature", "", "Email signature HTML")
	cmd.Flags().StringVar(&bodySmtpHost, "smtp-host", "", "SMTP server host")
	cmd.Flags().IntVar(&bodySmtpPort, "smtp-port", 0, "SMTP server port")
	cmd.Flags().IntVar(&bodyTotalWarmupPerDay, "total-warmup-per-day", 0, "Warmup emails per day")
	cmd.Flags().StringVar(&bodyUserName, "user-name", "", "SMTP/IMAP login username")
	cmd.Flags().BoolVar(&bodyWarmupEnabled, "warmup-enabled", false, "Enable inbox warmup")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
