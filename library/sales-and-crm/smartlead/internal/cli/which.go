// Copyright 2026 bossriceshark and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "campaigns create", Description: "Create a new campaign", Group: "campaigns"},
	{Command: "campaigns delete", Description: "Delete a campaign", Group: "campaigns"},
	{Command: "campaigns get", Description: "Get a campaign by ID", Group: "campaigns"},
	{Command: "campaigns list", Description: "Retrieves every email campaign for the authenticated SmartLead account.", Group: "campaigns"},
	{Command: "campaigns analytics get-campaign", Description: "Get top-level analytics for a campaign", Group: "campaigns"},
	{Command: "campaigns analytics-by-date get-campaign", Description: "Get campaign analytics for a date range", Group: "campaigns"},
	{Command: "campaigns email-accounts add-campaign", Description: "Attach email accounts to a campaign", Group: "campaigns"},
	{Command: "campaigns email-accounts get-campaign", Description: "List email accounts attached to a campaign", Group: "campaigns"},
	{Command: "campaigns email-accounts remove-campaign", Description: "Detach email accounts from a campaign", Group: "campaigns"},
	{Command: "campaigns leads add-to-campaign", Description: "Add leads to a campaign", Group: "campaigns"},
	{Command: "campaigns leads delete-from-campaign", Description: "Remove a lead from a campaign", Group: "campaigns"},
	{Command: "campaigns leads get-message-history", Description: "Get the full email thread for a lead in a campaign", Group: "campaigns"},
	{Command: "campaigns leads list-campaign", Description: "List leads in a campaign", Group: "campaigns"},
	{Command: "campaigns leads pause-in-campaign", Description: "Stop further sends and follow-ups to one lead without deleting the lead or its metrics. Reversible with resume-lead.", Group: "campaigns"},
	{Command: "campaigns leads resume-in-campaign", Description: "Resume sends to a lead previously paused. Fails when the lead has already reached the end of the sequence.", Group: "campaigns"},
	{Command: "campaigns leads unsubscribe-from-campaign", Description: "Unsubscribe a lead from a single campaign", Group: "campaigns"},
	{Command: "campaigns leads update-category", Description: "Set the category of a lead within a campaign", Group: "campaigns"},
	{Command: "campaigns leads update-in-campaign", Description: "Update custom_fields or contact fields for one lead in a specific campaign. SmartLead matches the lead by email together with the lead_id path parameter, so email is required even for partial updates.", Group: "campaigns"},
	{Command: "campaigns leads-export export-campaign-leads", Description: "Export all leads in a campaign as CSV data", Group: "campaigns"},
	{Command: "campaigns reply-email-thread reply-to-lead-thread", Description: "Send a reply within an existing campaign email thread, preserving the original sending account. Requires the stats ID and message ID of the email being replied to, both available from the lead's message-history.", Group: "campaigns"},
	{Command: "campaigns schedule update-campaign", Description: "Update a campaign sending schedule", Group: "campaigns"},
	{Command: "campaigns sequences get", Description: "Get the email sequence steps for a campaign", Group: "campaigns"},
	{Command: "campaigns sequences save", Description: "Save the email sequence steps for a campaign", Group: "campaigns"},
	{Command: "campaigns settings update-campaign", Description: "Update campaign tracking and stop-condition settings", Group: "campaigns"},
	{Command: "campaigns statistics get-campaign", Description: "Get per-lead send statistics for a campaign", Group: "campaigns"},
	{Command: "campaigns status update-campaign", Description: "Start, pause, or stop a campaign", Group: "campaigns"},
	{Command: "campaigns webhooks delete-campaign", Description: "Delete a campaign webhook", Group: "campaigns"},
	{Command: "campaigns webhooks list-campaign", Description: "List webhooks registered on a campaign", Group: "campaigns"},
	{Command: "campaigns webhooks upsert-campaign", Description: "Create or update a campaign webhook", Group: "campaigns"},
	{Command: "client create", Description: "Create a whitelabel client", Group: "client"},
	{Command: "client list", Description: "List all whitelabel clients", Group: "client"},
	{Command: "email-accounts create", Description: "Add a new email sender account", Group: "email-accounts"},
	{Command: "email-accounts list", Description: "List all email sender accounts", Group: "email-accounts"},
	{Command: "email-accounts reconnect-failed", Description: "Trigger a reconnect of all disconnected email accounts", Group: "email-accounts"},
	{Command: "email-accounts update", Description: "Update an email sender account", Group: "email-accounts"},
	{Command: "email-accounts warmup set-settings", Description: "Configure inbox warmup for an email account", Group: "email-accounts"},
	{Command: "email-accounts warmup-stats get", Description: "Get warmup performance stats for an email account", Group: "email-accounts"},
	{Command: "email-campaigns send-email-thread", Description: "Send a reply through the master inbox using only the stats ID of the email being replied to. Use as a fallback when the reply message ID is not available for reply-email-thread.", Group: "email-campaigns"},
	{Command: "leads add-domain-block-list", Description: "Add domains to the account-wide block list", Group: "leads"},
	{Command: "leads get-by-email", Description: "Look up a lead by email address", Group: "leads"},
	{Command: "leads list-categories", Description: "List all lead categories for the account", Group: "leads"},
	{Command: "leads update", Description: "Update a lead's fields", Group: "leads"},
	{Command: "leads campaigns get-lead", Description: "List every campaign a lead belongs to", Group: "leads"},
	{Command: "leads unsubscribe lead-from-all", Description: "Unsubscribe a lead from every campaign", Group: "leads"},
	{Command: "health", Description: "One-shot scorecard for every campaign — bounce rate, reply rate, silent-lead count, sender count, and a stale flag — without clicking through the dashboard.", Group: "Local state that compounds", WhyItMatters: "Reach for this first when an agent needs to know which campaigns are healthy before drilling into any one of them."},
	{Command: "silent", Description: "Finds leads that were emailed but have not replied within N days — the exact set to follow up with or retire.", Group: "Local state that compounds", WhyItMatters: "Use this to build a follow-up list instead of paging the whole lead set and diffing timestamps by hand."},
	{Command: "dupes", Description: "Scans the whole lead mirror for emails or domains that appear in two or more campaigns; --domain prints the full pitch ledger for one site.", Group: "Local state that compounds", WhyItMatters: "Run this before adding leads to a campaign to avoid double-contacting a prospect already in flight."},
	{Command: "sender-health", Description: "Ranks every email sender account by a composite of warmup performance, bounce rate, and reply rate, with bounce concentration by recipient domain.", Group: "Deliverability intelligence", WhyItMatters: "Reach for this to find which sender accounts are dragging deliverability before they tank a campaign."},
	{Command: "warmup-gate", Description: "Applies warmup thresholds to local warmup-stats and returns a typed pass/fail exit code per account — a scriptable launch gate.", Group: "Deliverability intelligence", WhyItMatters: "Call this in a launch script to block attaching a sender account that is not warmed up yet."},
	{Command: "drift", Description: "Computes week-over-week reply, open, and bounce deltas for a campaign from successive synced statistics snapshots.", Group: "Local state that compounds", WhyItMatters: "Use this to catch a campaign decaying over time rather than judging it from a single point-in-time stat."},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  smartlead-pp-cli which "stale tickets"
  smartlead-pp-cli which "bottleneck"
  smartlead-pp-cli which --limit 1 "send message"
  smartlead-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
