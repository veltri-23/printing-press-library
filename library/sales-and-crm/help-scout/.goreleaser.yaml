version: 2
project_name: help-scout-pp-cli
changelog:
  disable: true
builds:
  - id: help-scout-pp-cli
    main: ./cmd/help-scout-pp-cli
    binary: help-scout-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X help-scout-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: help-scout-pp-mcp
    main: ./cmd/help-scout-pp-mcp
    binary: help-scout-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: help-scout-pp-cli
    repository:
      owner: debgotwired
      name: homebrew-tap
    homepage: "https://github.com/debgotwired/help-scout-pp-cli"
    description: "Read-first Help Scout Inbox API surface for support operations and agent workflows."
    install: |
      bin.install "help-scout-pp-cli"
      bin.install "help-scout-pp-mcp"
