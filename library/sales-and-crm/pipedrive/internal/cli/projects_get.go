// Copyright 2026 Deb Mukherjee and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newProjectsGetCmd(flags *rootFlags) *cobra.Command {
	var flagCursor string
	var flagLimit int
	var flagFilterId string
	var flagStatus string
	var flagPhaseId string
	var flagIncludeArchived bool
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get",
		Aliases:     []string{"list"},
		Short:       "Returns all projects. This is a cursor-paginated endpoint.",
		Example:     "  pipedrive-pp-cli projects get",
		Annotations: map[string]string{"pp:endpoint": "projects.get", "pp:method": "GET", "pp:path": "/projects", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/projects"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "projects", path, map[string]string{
				"cursor":           formatCLIParamValue(flagCursor),
				"limit":            formatCLIParamValue(flagLimit),
				"filter_id":        formatCLIParamValue(flagFilterId),
				"status":           formatCLIParamValue(flagStatus),
				"phase_id":         formatCLIParamValue(flagPhaseId),
				"include_archived": formatCLIParamValue(flagIncludeArchived),
			}, nil, flagAll, "cursor", "cursor", "limit", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "For pagination, the marker (an opaque string value) representing the first item on the next page")
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "For pagination, the limit of entries to be returned. If not provided, 100 items will be returned.")
	cmd.Flags().StringVar(&flagFilterId, "filter-id", "", "The ID of the filter to use")
	cmd.Flags().StringVar(&flagStatus, "status", "", "If supplied, includes only projects with the specified statuses.")
	cmd.Flags().StringVar(&flagPhaseId, "phase-id", "", "If supplied, only projects in specified phase are returned")
	cmd.Flags().BoolVar(&flagIncludeArchived, "include-archived", false, "If supplied with `true` then archived projects are also included in the response.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
