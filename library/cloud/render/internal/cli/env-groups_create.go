// Copyright 2026 Giuliano Giacaglia and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newEnvGroupsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyEnvVars string
	var bodyEnvironmentId string
	var bodyName string
	var bodyOwnerId string
	var bodySecretFiles string
	var bodyServiceIds string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a new environment group.",
		Example:     "  render-pp-cli env-groups create --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "env-groups.create", "pp:method": "POST", "pp:path": "/env-groups"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("env-vars") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "env-vars")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("owner-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "owner-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/env-groups"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyEnvVars != "" {
					var parsedEnvVars any
					if err := json.Unmarshal([]byte(bodyEnvVars), &parsedEnvVars); err != nil {
						return fmt.Errorf("parsing --env-vars JSON: %w", err)
					}
					body["envVars"] = parsedEnvVars
				}
				if bodyEnvironmentId != "" {
					body["environmentId"] = bodyEnvironmentId
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyOwnerId != "" {
					body["ownerId"] = bodyOwnerId
				}
				if bodySecretFiles != "" {
					var parsedSecretFiles any
					if err := json.Unmarshal([]byte(bodySecretFiles), &parsedSecretFiles); err != nil {
						return fmt.Errorf("parsing --secret-files JSON: %w", err)
					}
					body["secretFiles"] = parsedSecretFiles
				}
				if bodyServiceIds != "" {
					var parsedServiceIds any
					if err := json.Unmarshal([]byte(bodyServiceIds), &parsedServiceIds); err != nil {
						return fmt.Errorf("parsing --service-ids JSON: %w", err)
					}
					body["serviceIds"] = parsedServiceIds
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "env-groups",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyEnvVars, "env-vars", "", "Env vars")
	cmd.Flags().StringVar(&bodyEnvironmentId, "environment-id", "", "Environment id")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyOwnerId, "owner-id", "", "Owner id")
	cmd.Flags().StringVar(&bodySecretFiles, "secret-files", "", "Secret files")
	cmd.Flags().StringVar(&bodyServiceIds, "service-ids", "", "Service ids")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
