// Copyright 2026 Giuliano Giacaglia and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newEnvironmentsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyIpAllowList string
	var bodyName string
	var bodyNetworkIsolationEnabled bool
	var bodyProjectId string
	var bodyProtectedStatus string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a new environment belonging to the project with the provided ID.",
		Example:     "  render-pp-cli environments create --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "environments.create", "pp:method": "POST", "pp:path": "/environments"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("project-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "project-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/environments"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyIpAllowList != "" {
					var parsedIpAllowList any
					if err := json.Unmarshal([]byte(bodyIpAllowList), &parsedIpAllowList); err != nil {
						return fmt.Errorf("parsing --ip-allow-list JSON: %w", err)
					}
					body["ipAllowList"] = parsedIpAllowList
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNetworkIsolationEnabled != false {
					body["networkIsolationEnabled"] = bodyNetworkIsolationEnabled
				}
				if bodyProjectId != "" {
					body["projectId"] = bodyProjectId
				}
				if bodyProtectedStatus != "" {
					body["protectedStatus"] = bodyProtectedStatus
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "environments",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyIpAllowList, "ip-allow-list", "", "Ip allow list")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().BoolVar(&bodyNetworkIsolationEnabled, "network-isolation-enabled", false, "Indicates whether network connections across environments are allowed.")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project id")
	cmd.Flags().StringVar(&bodyProtectedStatus, "protected-status", "", "Indicates whether an environment is `unprotected` or `protected`. Only admin users can perform destructive actions...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
