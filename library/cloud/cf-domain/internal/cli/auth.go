// Copyright 2026 Danny Shmueli and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"github.com/mvanhorn/printing-press-library/library/cloud/cf-domain/internal/config"
	"github.com/spf13/cobra"
	"os"
	"os/exec"
)

func newAuthCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "auth",
		Short: "Manage authentication for Cloudflare Registrar Domains",
	}

	cmd.AddCommand(newAuthSetupCmd(flags))
	cmd.AddCommand(newAuthLoginCmd(flags))
	cmd.AddCommand(newAuthContextCmd(flags))
	cmd.AddCommand(newAuthStatusCmd(flags))
	cmd.AddCommand(newAuthSetTokenCmd(flags))
	cmd.AddCommand(newAuthLogoutCmd(flags))

	return cmd
}

func newAuthLoginCmd(_ *rootFlags) *cobra.Command {
	return &cobra.Command{
		Use:     "login",
		Short:   "Login with the official Cloudflare cf CLI OAuth flow",
		Example: "  cf-domain-pp-cli auth login",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Delegate interactive OAuth to an already-installed Cloudflare cf CLI,
			// then reuse ~/.cf/config.toml. Do not invoke npx here: auth login must
			// not silently install and execute packages from npm.
			cfPath, err := exec.LookPath("cf")
			if err != nil {
				return fmt.Errorf("Cloudflare cf CLI not found in PATH; install it first or use `cf-domain-pp-cli auth set-token <token>` / CLOUDFLARE_API_TOKEN")
			}
			login := exec.Command(cfPath, "auth", "login")
			login.Stdin = os.Stdin
			login.Stdout = cmd.OutOrStdout()
			login.Stderr = cmd.ErrOrStderr()
			return login.Run()
		},
	}
}

func newAuthContextCmd(flags *rootFlags) *cobra.Command {
	return &cobra.Command{
		Use:     "context",
		Short:   "Show selected Cloudflare account context",
		Example: "  cf-domain-pp-cli auth context",
		RunE: func(cmd *cobra.Command, args []string) error {
			accountID := config.DefaultCloudflareAccountID()
			if flags.asJSON {
				return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
					"account_id": accountID,
					"source":     "env or ~/.config/cf/config.json",
				}, flags)
			}
			if accountID == "" {
				return fmt.Errorf("no Cloudflare account context found; run `cf context set --account-id <id>`")
			}
			fmt.Fprintf(cmd.OutOrStdout(), "Account: %s\n", accountID)
			return nil
		},
	}
}

// newAuthSetupCmd prints concrete steps for getting a credential. Side-effect
// rule: print by default, --launch opt-in to open the URL, short-circuit when
// the verifier is running this in a sandboxed subprocess.
func newAuthSetupCmd(_ *rootFlags) *cobra.Command {
	var launch bool
	cmd := &cobra.Command{
		Use:     "setup",
		Short:   "Print steps for obtaining a credential (use --launch to open the URL)",
		Example: "  cf-domain-pp-cli auth setup\n  cf-domain-pp-cli auth setup --launch",
		RunE: func(cmd *cobra.Command, args []string) error {
			w := cmd.OutOrStdout()
			fmt.Fprintln(w, "No setup URL is configured for this CLI; check the API's docs.")
			fmt.Fprintln(w, "")
			fmt.Fprintln(w, "Then set:")
			fmt.Fprintln(w, "  export CLOUDFLARE_REGISTRAR_DOMAINS_BEARER_AUTH=\"<your-token>\"")
			fmt.Fprintln(w, "  cf-domain-pp-cli auth set-token <token>")
			if !launch {
				return nil
			}
			fmt.Fprintln(cmd.ErrOrStderr(), "no setup URL configured; cannot launch")
			return nil
		},
	}
	cmd.Flags().BoolVar(&launch, "launch", false, "Open the setup URL in your default browser")
	return cmd
}

func newAuthStatusCmd(flags *rootFlags) *cobra.Command {
	return &cobra.Command{
		Use:     "status",
		Short:   "Show authentication status",
		Example: "  cf-domain-pp-cli auth status",
		RunE: func(cmd *cobra.Command, args []string) error {
			cfg, err := config.Load(flags.configPath)
			if err != nil {
				return configErr(err)
			}

			w := cmd.OutOrStdout()
			header := cfg.AuthHeader()
			authed := header != ""
			// JSON envelope: {authenticated, verified, source, config}. When not
			// authenticated, write the envelope first then return authErr
			// so exit code carries the auth-failure signal.
			if flags.asJSON {
				out := map[string]any{
					"authenticated": authed,
					"verified":      false,
					"source":        cfg.AuthSource,
					"config":        cfg.Path,
				}
				if printErr := printJSONFiltered(w, out, flags); printErr != nil {
					return printErr
				}
				if !authed {
					return authErr(fmt.Errorf("no credentials configured"))
				}
				return nil
			}
			if !authed {
				fmt.Fprintln(w, red("Not authenticated"))
				fmt.Fprintln(w, "")
				fmt.Fprintln(w, "Set your token:")
				fmt.Fprintln(w, "  export CLOUDFLARE_REGISTRAR_DOMAINS_BEARER_AUTH=\"your-token-here\"")
				fmt.Fprintf(w, "  cf-domain-pp-cli auth set-token <token>\n")
				return authErr(fmt.Errorf("no credentials configured"))
			}

			fmt.Fprintln(w, green("Credentials present (not verified)"))
			fmt.Fprintf(w, "  Source: %s\n", cfg.AuthSource)
			fmt.Fprintf(w, "  Config: %s\n", cfg.Path)
			return nil
		},
	}
}

func newAuthSetTokenCmd(flags *rootFlags) *cobra.Command {
	return &cobra.Command{
		Use:     "set-token <token>",
		Short:   "Save an API token to the config file",
		Example: "  cf-domain-pp-cli auth set-token YOUR_TOKEN_HERE",
		Args:    cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			cfg, err := config.Load(flags.configPath)
			if err != nil {
				return configErr(err)
			}

			// Clear any legacy auth_header so AuthHeader() falls through to
			// the newly-saved credential. Without this, a pre-existing
			// auth_header value (common after regenerate) shadows the saved
			// token and set-token silently has no effect. Silent clear (no
			// log line): a masked-tail variant could leak token bytes through
			// scripted dogfood that captures stderr.
			cfg.AuthHeaderVal = ""
			if err := cfg.SaveTokens("", "", args[0], "", cfg.TokenExpiry); err != nil {
				return configErr(fmt.Errorf("saving token: %w", err))
			}

			// JSON envelope: {saved, config_path}.
			if flags.asJSON {
				return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
					"saved":       true,
					"config_path": cfg.Path,
				}, flags)
			}
			fmt.Fprintf(cmd.OutOrStdout(), "Token saved to %s\n", cfg.Path)
			return nil
		},
	}
}

func newAuthLogoutCmd(flags *rootFlags) *cobra.Command {
	return &cobra.Command{
		Use:     "logout",
		Short:   "Clear stored credentials",
		Example: "  cf-domain-pp-cli auth logout",
		RunE: func(cmd *cobra.Command, args []string) error {
			cfg, err := config.Load(flags.configPath)
			if err != nil {
				return configErr(err)
			}

			if err := cfg.ClearTokens(); err != nil {
				return configErr(fmt.Errorf("clearing tokens: %w", err))
			}

			// Identify which (if any) external auth source is still active so the
			// JSON envelope and the human prose can both surface it.
			activeAuthSource := ""
			if activeAuthSource == "" && os.Getenv("CLOUDFLARE_API_TOKEN") != "" {
				activeAuthSource = "CLOUDFLARE_API_TOKEN env var"
			}
			if activeAuthSource == "" && os.Getenv("CLOUDFLARE_REGISTRAR_DOMAINS_BEARER_AUTH") != "" {
				activeAuthSource = "CLOUDFLARE_REGISTRAR_DOMAINS_BEARER_AUTH env var"
			}
			if activeAuthSource == "" && config.HasCloudflareOAuthToken() {
				activeAuthSource = "cf OAuth token in ~/.cf/config.toml"
			}

			// JSON envelope: {cleared: true, note?: "<auth source> is still active"}.
			if flags.asJSON {
				out := map[string]any{"cleared": true}
				if activeAuthSource != "" {
					out["note"] = activeAuthSource + " is still active"
				}
				return printJSONFiltered(cmd.OutOrStdout(), out, flags)
			}

			if activeAuthSource != "" {
				fmt.Fprintf(cmd.OutOrStdout(), "Config cleared. Note: %s is still active.\n", activeAuthSource)
				return nil
			}
			fmt.Fprintln(cmd.OutOrStdout(), "Logged out. Credentials cleared.")
			return nil
		},
	}
}
