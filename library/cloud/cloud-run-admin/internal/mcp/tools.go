// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/cloud/cloud-run-admin/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/cloud/cloud-run-admin/internal/client"
	"github.com/mvanhorn/printing-press-library/library/cloud/cloud-run-admin/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/cloud/cloud-run-admin/internal/config"
	"github.com/mvanhorn/printing-press-library/library/cloud/cloud-run-admin/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/cloud/cloud-run-admin/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("cloud-run-admin-jobs_create",
			mcplib.WithDescription("Creates a Job. Required: parent. Optional: jobId, validateOnly, annotations (plus 23 more). Returns the new GoogleLongrunningOperation."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The location and project in which this Job should be created. Format:...")),
			mcplib.WithString("jobId", mcplib.Description("Required. The unique identifier for the Job. The name of the job becomes {parent}/jobs/{job_id}.")),
			mcplib.WithString("validateOnly", mcplib.Description("Indicates that the request should be validated and default values populated, without persisting the request or...")),
			mcplib.WithString("annotations", mcplib.Description("KRM-style annotations for the resource. Unstructured key value map that may be set by external tools to store and...")),
			mcplib.WithString("binaryAuthorization", mcplib.Description("Settings for Binary Authorization feature.")),
			mcplib.WithString("client", mcplib.Description("Arbitrary identifier for the API client.")),
			mcplib.WithString("clientVersion", mcplib.Description("Arbitrary version identifier for the API client.")),
			mcplib.WithString("conditions", mcplib.Description("Output only. The Conditions of all other associated sub-resources. They contain additional diagnostics information...")),
			mcplib.WithString("createTime", mcplib.Description("Output only. The creation time.")),
			mcplib.WithString("creator", mcplib.Description("Output only. Email address of the authenticated creator.")),
			mcplib.WithString("deleteTime", mcplib.Description("Output only. The deletion time.")),
			mcplib.WithString("etag", mcplib.Description("Output only. A system-generated fingerprint for this version of the resource. May be used to...")),
			mcplib.WithString("executionCount", mcplib.Description("Output only. Number of executions created for this job.")),
			mcplib.WithString("expireTime", mcplib.Description("Output only. For a deleted resource, the time after which it will be permamently deleted.")),
			mcplib.WithString("generation", mcplib.Description("Output only. A number that monotonically increases every time the user modifies the desired state.")),
			mcplib.WithString("labels", mcplib.Description("KRM-style labels for the resource. User-provided labels are shared with Google's billing system, so they can be used...")),
			mcplib.WithString("lastModifier", mcplib.Description("Output only. Email address of the last authenticated modifier.")),
			mcplib.WithString("latestCreatedExecution", mcplib.Description("Reference to an Execution. Use /Executions.GetExecution with the given name to get full execution including the...")),
			mcplib.WithString("launchStage", mcplib.Description("The launch stage as defined by [Google Cloud Platform Launch Stages](https://cloud.")),
			mcplib.WithString("name", mcplib.Description("The fully qualified name of this Job. Format: projects/{project}/locations/{location}/jobs/{job}")),
			mcplib.WithString("observedGeneration", mcplib.Description("Output only. The generation of this Job. See comments in `reconciling` for additional information on reconciliation...")),
			mcplib.WithString("reconciling", mcplib.Description("Output only. Returns true if the Job is currently being acted upon by the system to bring it into the desired state....")),
			mcplib.WithString("satisfiesPzs", mcplib.Description("Output only. Reserved for future use.")),
			mcplib.WithString("template", mcplib.Description("ExecutionTemplate describes the data an execution should have when created from a template.")),
			mcplib.WithString("terminalCondition", mcplib.Description("Defines a status condition for a resource.")),
			mcplib.WithString("uid", mcplib.Description("Output only. Server assigned unique identifier for the Execution. The value is a UUID4 string and guaranteed to...")),
			mcplib.WithString("updateTime", mcplib.Description("Output only. The last-modified time.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{parent}/jobs", []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "jobId", WireName: "jobId", Location: "query"}, {PublicName: "validateOnly", WireName: "validateOnly", Location: "query"}, {PublicName: "annotations", WireName: "annotations", Location: "body"}, {PublicName: "binaryAuthorization", WireName: "binaryAuthorization", Location: "body"}, {PublicName: "client", WireName: "client", Location: "body"}, {PublicName: "clientVersion", WireName: "clientVersion", Location: "body"}, {PublicName: "conditions", WireName: "conditions", Location: "body"}, {PublicName: "createTime", WireName: "createTime", Location: "body"}, {PublicName: "creator", WireName: "creator", Location: "body"}, {PublicName: "deleteTime", WireName: "deleteTime", Location: "body"}, {PublicName: "etag", WireName: "etag", Location: "body"}, {PublicName: "executionCount", WireName: "executionCount", Location: "body"}, {PublicName: "expireTime", WireName: "expireTime", Location: "body"}, {PublicName: "generation", WireName: "generation", Location: "body"}, {PublicName: "labels", WireName: "labels", Location: "body"}, {PublicName: "lastModifier", WireName: "lastModifier", Location: "body"}, {PublicName: "latestCreatedExecution", WireName: "latestCreatedExecution", Location: "body"}, {PublicName: "launchStage", WireName: "launchStage", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "observedGeneration", WireName: "observedGeneration", Location: "body"}, {PublicName: "reconciling", WireName: "reconciling", Location: "body"}, {PublicName: "satisfiesPzs", WireName: "satisfiesPzs", Location: "body"}, {PublicName: "template", WireName: "template", Location: "body"}, {PublicName: "terminalCondition", WireName: "terminalCondition", Location: "body"}, {PublicName: "uid", WireName: "uid", Location: "body"}, {PublicName: "updateTime", WireName: "updateTime", Location: "body"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("cloud-run-admin-jobs_list",
			mcplib.WithDescription("Lists Jobs. Required: parent. Optional: pageSize, pageToken, showDeleted. Returns the GoogleCloudRunV2ListJobsResponse."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The location and project to list resources on. Format: projects/{project}/locations/{location}, where...")),
			mcplib.WithString("pageSize", mcplib.Description("Maximum number of Jobs to return in this call.")),
			mcplib.WithString("pageToken", mcplib.Description("A page token received from a previous call to ListJobs. All other parameters must match.")),
			mcplib.WithString("showDeleted", mcplib.Description("If true, returns deleted (but unexpired) resources along with active ones.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{parent}/jobs", []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}, {PublicName: "showDeleted", WireName: "showDeleted", Location: "query"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("cloud-run-admin-jobs_run",
			mcplib.WithDescription("Triggers creation of a new Execution of this Job. Required: name. Optional: etag, validateOnly. Returns the new GoogleLongrunningOperation."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Required. The full name of the Job. Format: projects/{project}/locations/{location}/jobs/{job}, where {project} can...")),
			mcplib.WithString("etag", mcplib.Description("A system-generated fingerprint for this version of the resource. May be used to detect modification conflict during...")),
			mcplib.WithString("validateOnly", mcplib.Description("Indicates that the request should be validated without actually deleting any resources.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{name}:run", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "etag", WireName: "etag", Location: "body"}, {PublicName: "validateOnly", WireName: "validateOnly", Location: "body"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("cloud-run-admin-jobs_executions_list",
			mcplib.WithDescription("Lists Executions from a Job. Required: parent. Optional: pageSize, pageToken, showDeleted. Returns the GoogleCloudRunV2ListExecutionsResponse."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The Execution from which the Executions should be listed. To list all Executions across Jobs, use '-'...")),
			mcplib.WithString("pageSize", mcplib.Description("Maximum number of Executions to return in this call.")),
			mcplib.WithString("pageToken", mcplib.Description("A page token received from a previous call to ListExecutions. All other parameters must match.")),
			mcplib.WithString("showDeleted", mcplib.Description("If true, returns deleted (but unexpired) resources along with active ones.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{parent}/executions", []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}, {PublicName: "showDeleted", WireName: "showDeleted", Location: "query"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("executions_tasks_list",
			mcplib.WithDescription("Lists Tasks from an Execution of a Job. Required: parent. Optional: pageSize, pageToken, showDeleted. Returns the GoogleCloudRunV2ListTasksResponse."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The Execution from which the Tasks should be listed. To list all Tasks across Executions of a Job, use '-'...")),
			mcplib.WithString("pageSize", mcplib.Description("Maximum number of Tasks to return in this call.")),
			mcplib.WithString("pageToken", mcplib.Description("A page token received from a previous call to ListTasks. All other parameters must match.")),
			mcplib.WithString("showDeleted", mcplib.Description("If true, returns deleted (but unexpired) resources along with active ones.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{parent}/tasks", []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}, {PublicName: "showDeleted", WireName: "showDeleted", Location: "query"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("operations_list",
			mcplib.WithDescription("Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns `UNIMPLEMENTED`. Required: name. Optional: filter, pageSize, pageToken."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Required. To query for all of the operations for a project.")),
			mcplib.WithString("filter", mcplib.Description("Optional. A filter for matching the completed or in-progress operations. The supported formats of *filter* are: To...")),
			mcplib.WithString("pageSize", mcplib.Description("The maximum number of records that should be returned. Requested page size cannot exceed 100. If not set or set to...")),
			mcplib.WithString("pageToken", mcplib.Description("Token identifying which result to start with, which is returned by a previous list call.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{name}/operations", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("operations_wait",
			mcplib.WithDescription("Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns `google.rpc.Code.UNIMPLEMENTED`. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done. Required: name. Optional: timeout."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("The name of the operation resource to wait on.")),
			mcplib.WithString("timeout", mcplib.Description("The maximum duration to wait before timing out. If left blank, the wait will be at most the time permitted by the...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{name}:wait", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "timeout", WireName: "timeout", Location: "body"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("services_create",
			mcplib.WithDescription("Creates a new Service in a given project and location. Required: parent. Optional: serviceId, validateOnly, annotations (plus 28 more). Returns the new GoogleLongrunningOperation."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The location and project in which this service should be created. Format:...")),
			mcplib.WithString("serviceId", mcplib.Description("Required. The unique identifier for the Service. It must begin with letter, and cannot end with hyphen; must contain...")),
			mcplib.WithString("validateOnly", mcplib.Description("Indicates that the request should be validated and default values populated, without persisting the request or...")),
			mcplib.WithString("annotations", mcplib.Description("Unstructured key value map that may be set by external tools to store and arbitrary metadata. They are not queryable...")),
			mcplib.WithString("binaryAuthorization", mcplib.Description("Settings for Binary Authorization feature.")),
			mcplib.WithString("client", mcplib.Description("Arbitrary identifier for the API client.")),
			mcplib.WithString("clientVersion", mcplib.Description("Arbitrary version identifier for the API client.")),
			mcplib.WithString("conditions", mcplib.Description("Output only. The Conditions of all other associated sub-resources. They contain additional diagnostics information...")),
			mcplib.WithString("createTime", mcplib.Description("Output only. The creation time.")),
			mcplib.WithString("creator", mcplib.Description("Output only. Email address of the authenticated creator.")),
			mcplib.WithString("deleteTime", mcplib.Description("Output only. The deletion time.")),
			mcplib.WithString("description", mcplib.Description("User-provided description of the Service. This field currently has a 512-character limit.")),
			mcplib.WithString("etag", mcplib.Description("Output only. A system-generated fingerprint for this version of the resource. May be used to...")),
			mcplib.WithString("expireTime", mcplib.Description("Output only. For a deleted resource, the time after which it will be permamently deleted.")),
			mcplib.WithString("generation", mcplib.Description("Output only. A number that monotonically increases every time the user modifies the desired state. Please note that...")),
			mcplib.WithString("ingress", mcplib.Description("Provides the ingress settings for this Service. On output, returns the currently observed ingress settings, or...")),
			mcplib.WithString("labels", mcplib.Description("Map of string keys and values that can be used to organize and categorize objects. User-provided labels are shared...")),
			mcplib.WithString("lastModifier", mcplib.Description("Output only. Email address of the last authenticated modifier.")),
			mcplib.WithString("latestCreatedRevision", mcplib.Description("Output only. Name of the last created revision. See comments in `reconciling` for additional information on...")),
			mcplib.WithString("latestReadyRevision", mcplib.Description("Output only. Name of the latest revision that is serving traffic. See comments in `reconciling` for additional...")),
			mcplib.WithString("launchStage", mcplib.Description("The launch stage as defined by [Google Cloud Platform Launch Stages](https://cloud.")),
			mcplib.WithString("name", mcplib.Description("The fully qualified name of this Service. In CreateServiceRequest, this field is ignored, and instead composed from...")),
			mcplib.WithString("observedGeneration", mcplib.Description("Output only. The generation of this Service currently serving traffic. See comments in `reconciling` for additional...")),
			mcplib.WithString("reconciling", mcplib.Description("Output only. Returns true if the Service is currently being acted upon by the system to bring it into the desired...")),
			mcplib.WithString("satisfiesPzs", mcplib.Description("Output only. Reserved for future use.")),
			mcplib.WithString("template", mcplib.Description("RevisionTemplate describes the data a revision should have when created from a template.")),
			mcplib.WithString("terminalCondition", mcplib.Description("Defines a status condition for a resource.")),
			mcplib.WithString("traffic", mcplib.Description("Specifies how to distribute traffic over a collection of Revisions belonging to the Service. If traffic is empty or...")),
			mcplib.WithString("trafficStatuses", mcplib.Description("Output only. Detailed status information for corresponding traffic targets. See comments in `reconciling` for...")),
			mcplib.WithString("uid", mcplib.Description("Output only. Server assigned unique identifier for the trigger. The value is a UUID4 string and guaranteed to remain...")),
			mcplib.WithString("updateTime", mcplib.Description("Output only. The last-modified time.")),
			mcplib.WithString("uri", mcplib.Description("Output only. The main URI in which this Service is serving traffic.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{parent}/services", []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "serviceId", WireName: "serviceId", Location: "query"}, {PublicName: "validateOnly", WireName: "validateOnly", Location: "query"}, {PublicName: "annotations", WireName: "annotations", Location: "body"}, {PublicName: "binaryAuthorization", WireName: "binaryAuthorization", Location: "body"}, {PublicName: "client", WireName: "client", Location: "body"}, {PublicName: "clientVersion", WireName: "clientVersion", Location: "body"}, {PublicName: "conditions", WireName: "conditions", Location: "body"}, {PublicName: "createTime", WireName: "createTime", Location: "body"}, {PublicName: "creator", WireName: "creator", Location: "body"}, {PublicName: "deleteTime", WireName: "deleteTime", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "etag", WireName: "etag", Location: "body"}, {PublicName: "expireTime", WireName: "expireTime", Location: "body"}, {PublicName: "generation", WireName: "generation", Location: "body"}, {PublicName: "ingress", WireName: "ingress", Location: "body"}, {PublicName: "labels", WireName: "labels", Location: "body"}, {PublicName: "lastModifier", WireName: "lastModifier", Location: "body"}, {PublicName: "latestCreatedRevision", WireName: "latestCreatedRevision", Location: "body"}, {PublicName: "latestReadyRevision", WireName: "latestReadyRevision", Location: "body"}, {PublicName: "launchStage", WireName: "launchStage", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "observedGeneration", WireName: "observedGeneration", Location: "body"}, {PublicName: "reconciling", WireName: "reconciling", Location: "body"}, {PublicName: "satisfiesPzs", WireName: "satisfiesPzs", Location: "body"}, {PublicName: "template", WireName: "template", Location: "body"}, {PublicName: "terminalCondition", WireName: "terminalCondition", Location: "body"}, {PublicName: "traffic", WireName: "traffic", Location: "body"}, {PublicName: "trafficStatuses", WireName: "trafficStatuses", Location: "body"}, {PublicName: "uid", WireName: "uid", Location: "body"}, {PublicName: "updateTime", WireName: "updateTime", Location: "body"}, {PublicName: "uri", WireName: "uri", Location: "body"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("services_get-iam-policy",
			mcplib.WithDescription("Gets the IAM Access Control policy currently in effect for the given Cloud Run Service. This result does not include any inherited policies. Required: resource. Optional: options.requestedPolicyVersion. Returns the GoogleIamV1Policy."),
			mcplib.WithString("resource", mcplib.Required(), mcplib.Description("REQUIRED: The resource for which the policy is being requested. See [Resource...")),
			mcplib.WithString("options.requestedPolicyVersion", mcplib.Description("Optional. The maximum policy version that will be used to format the policy. Valid values are 0, 1, and 3. Requests...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{resource}:getIamPolicy", []mcpParamBinding{{PublicName: "resource", WireName: "resource", Location: "path"}, {PublicName: "options.requestedPolicyVersion", WireName: "options.requestedPolicyVersion", Location: "query"}}, []string{"resource"}),
	)
	s.AddTool(
		mcplib.NewTool("services_list",
			mcplib.WithDescription("Lists Services. Required: parent. Optional: pageSize, pageToken, showDeleted. Returns the GoogleCloudRunV2ListServicesResponse."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The location and project to list resources on. Location must be a valid Google Cloud region, and cannot be...")),
			mcplib.WithString("pageSize", mcplib.Description("Maximum number of Services to return in this call.")),
			mcplib.WithString("pageToken", mcplib.Description("A page token received from a previous call to ListServices. All other parameters must match.")),
			mcplib.WithString("showDeleted", mcplib.Description("If true, returns deleted (but unexpired) resources along with active ones.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{parent}/services", []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}, {PublicName: "showDeleted", WireName: "showDeleted", Location: "query"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("services_patch",
			mcplib.WithDescription("Updates a Service. Required: name. Optional: allowMissing, validateOnly, annotations (plus 27 more). Returns the updated GoogleLongrunningOperation."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("The fully qualified name of this Service. In CreateServiceRequest, this field is ignored, and instead composed from...")),
			mcplib.WithString("allowMissing", mcplib.Description("This field is currently not used by Cloud Run; setting it does not have any effect.")),
			mcplib.WithString("validateOnly", mcplib.Description("Indicates that the request should be validated and default values populated, without persisting the request or...")),
			mcplib.WithString("annotations", mcplib.Description("Unstructured key value map that may be set by external tools to store and arbitrary metadata. They are not queryable...")),
			mcplib.WithString("binaryAuthorization", mcplib.Description("Settings for Binary Authorization feature.")),
			mcplib.WithString("client", mcplib.Description("Arbitrary identifier for the API client.")),
			mcplib.WithString("clientVersion", mcplib.Description("Arbitrary version identifier for the API client.")),
			mcplib.WithString("conditions", mcplib.Description("Output only. The Conditions of all other associated sub-resources. They contain additional diagnostics information...")),
			mcplib.WithString("createTime", mcplib.Description("Output only. The creation time.")),
			mcplib.WithString("creator", mcplib.Description("Output only. Email address of the authenticated creator.")),
			mcplib.WithString("deleteTime", mcplib.Description("Output only. The deletion time.")),
			mcplib.WithString("description", mcplib.Description("User-provided description of the Service. This field currently has a 512-character limit.")),
			mcplib.WithString("etag", mcplib.Description("Output only. A system-generated fingerprint for this version of the resource. May be used to...")),
			mcplib.WithString("expireTime", mcplib.Description("Output only. For a deleted resource, the time after which it will be permamently deleted.")),
			mcplib.WithString("generation", mcplib.Description("Output only. A number that monotonically increases every time the user modifies the desired state. Please note that...")),
			mcplib.WithString("ingress", mcplib.Description("Provides the ingress settings for this Service. On output, returns the currently observed ingress settings, or...")),
			mcplib.WithString("labels", mcplib.Description("Map of string keys and values that can be used to organize and categorize objects. User-provided labels are shared...")),
			mcplib.WithString("lastModifier", mcplib.Description("Output only. Email address of the last authenticated modifier.")),
			mcplib.WithString("latestCreatedRevision", mcplib.Description("Output only. Name of the last created revision. See comments in `reconciling` for additional information on...")),
			mcplib.WithString("latestReadyRevision", mcplib.Description("Output only. Name of the latest revision that is serving traffic. See comments in `reconciling` for additional...")),
			mcplib.WithString("launchStage", mcplib.Description("The launch stage as defined by [Google Cloud Platform Launch Stages](https://cloud.")),
			mcplib.WithString("observedGeneration", mcplib.Description("Output only. The generation of this Service currently serving traffic. See comments in `reconciling` for additional...")),
			mcplib.WithString("reconciling", mcplib.Description("Output only. Returns true if the Service is currently being acted upon by the system to bring it into the desired...")),
			mcplib.WithString("satisfiesPzs", mcplib.Description("Output only. Reserved for future use.")),
			mcplib.WithString("template", mcplib.Description("RevisionTemplate describes the data a revision should have when created from a template.")),
			mcplib.WithString("terminalCondition", mcplib.Description("Defines a status condition for a resource.")),
			mcplib.WithString("traffic", mcplib.Description("Specifies how to distribute traffic over a collection of Revisions belonging to the Service. If traffic is empty or...")),
			mcplib.WithString("trafficStatuses", mcplib.Description("Output only. Detailed status information for corresponding traffic targets. See comments in `reconciling` for...")),
			mcplib.WithString("uid", mcplib.Description("Output only. Server assigned unique identifier for the trigger. The value is a UUID4 string and guaranteed to remain...")),
			mcplib.WithString("updateTime", mcplib.Description("Output only. The last-modified time.")),
			mcplib.WithString("uri", mcplib.Description("Output only. The main URI in which this Service is serving traffic.")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/v2/{name}", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "allowMissing", WireName: "allowMissing", Location: "query"}, {PublicName: "validateOnly", WireName: "validateOnly", Location: "query"}, {PublicName: "annotations", WireName: "annotations", Location: "body"}, {PublicName: "binaryAuthorization", WireName: "binaryAuthorization", Location: "body"}, {PublicName: "client", WireName: "client", Location: "body"}, {PublicName: "clientVersion", WireName: "clientVersion", Location: "body"}, {PublicName: "conditions", WireName: "conditions", Location: "body"}, {PublicName: "createTime", WireName: "createTime", Location: "body"}, {PublicName: "creator", WireName: "creator", Location: "body"}, {PublicName: "deleteTime", WireName: "deleteTime", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "etag", WireName: "etag", Location: "body"}, {PublicName: "expireTime", WireName: "expireTime", Location: "body"}, {PublicName: "generation", WireName: "generation", Location: "body"}, {PublicName: "ingress", WireName: "ingress", Location: "body"}, {PublicName: "labels", WireName: "labels", Location: "body"}, {PublicName: "lastModifier", WireName: "lastModifier", Location: "body"}, {PublicName: "latestCreatedRevision", WireName: "latestCreatedRevision", Location: "body"}, {PublicName: "latestReadyRevision", WireName: "latestReadyRevision", Location: "body"}, {PublicName: "launchStage", WireName: "launchStage", Location: "body"}, {PublicName: "observedGeneration", WireName: "observedGeneration", Location: "body"}, {PublicName: "reconciling", WireName: "reconciling", Location: "body"}, {PublicName: "satisfiesPzs", WireName: "satisfiesPzs", Location: "body"}, {PublicName: "template", WireName: "template", Location: "body"}, {PublicName: "terminalCondition", WireName: "terminalCondition", Location: "body"}, {PublicName: "traffic", WireName: "traffic", Location: "body"}, {PublicName: "trafficStatuses", WireName: "trafficStatuses", Location: "body"}, {PublicName: "uid", WireName: "uid", Location: "body"}, {PublicName: "updateTime", WireName: "updateTime", Location: "body"}, {PublicName: "uri", WireName: "uri", Location: "body"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("services_set-iam-policy",
			mcplib.WithDescription("Sets the IAM Access control policy for the specified Service. Overwrites any existing policy. Required: resource. Optional: policy, updateMask. Returns the new GoogleIamV1Policy."),
			mcplib.WithString("resource", mcplib.Required(), mcplib.Description("REQUIRED: The resource for which the policy is being specified. See [Resource...")),
			mcplib.WithString("policy", mcplib.Description("An Identity and Access Management (IAM) policy, which specifies access controls for Google Cloud resources. A...")),
			mcplib.WithString("updateMask", mcplib.Description("OPTIONAL: A FieldMask specifying which fields of the policy to modify. Only the fields in the mask will be modified....")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{resource}:setIamPolicy", []mcpParamBinding{{PublicName: "resource", WireName: "resource", Location: "path"}, {PublicName: "policy", WireName: "policy", Location: "body"}, {PublicName: "updateMask", WireName: "updateMask", Location: "body"}}, []string{"resource"}),
	)
	s.AddTool(
		mcplib.NewTool("services_test-iam-permissions",
			mcplib.WithDescription("Returns permissions that a caller has on the specified Project. There are no permissions required for making this API call. Required: resource. Optional: permissions."),
			mcplib.WithString("resource", mcplib.Required(), mcplib.Description("REQUIRED: The resource for which the policy detail is being requested. See [Resource...")),
			mcplib.WithString("permissions", mcplib.Description("The set of permissions to check for the `resource`. Permissions with wildcards (such as `*` or `storage.*`) are not...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{resource}:testIamPermissions", []mcpParamBinding{{PublicName: "resource", WireName: "resource", Location: "path"}, {PublicName: "permissions", WireName: "permissions", Location: "body"}}, []string{"resource"}),
	)
	s.AddTool(
		mcplib.NewTool("services_revisions_delete",
			mcplib.WithDescription("Deletes a Revision. Required: name. Optional: etag, validateOnly. Returns the GoogleLongrunningOperation. Destructive."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Required. The name of the Revision to delete. Format: projects/{project}/locations/{location}/services/{service}/revi...")),
			mcplib.WithString("etag", mcplib.Description("A system-generated fingerprint for this version of the resource. This may be used to detect modification conflict...")),
			mcplib.WithString("validateOnly", mcplib.Description("Indicates that the request should be validated without actually deleting any resources.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v2/{name}", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "etag", WireName: "etag", Location: "query"}, {PublicName: "validateOnly", WireName: "validateOnly", Location: "query"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("services_revisions_get",
			mcplib.WithDescription("Gets information about a Revision. Required: name. Returns the GoogleCloudRunV2Revision."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Required. The full name of the Revision. Format: projects/{project}/locations/{location}/services/{service}/revisions...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{name}", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("services_revisions_list",
			mcplib.WithDescription("Lists Revisions from a given Service, or from a given location. Required: parent. Optional: pageSize, pageToken, showDeleted. Returns the GoogleCloudRunV2ListRevisionsResponse."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The Service from which the Revisions should be listed. To list all Revisions across Services, use '-'...")),
			mcplib.WithString("pageSize", mcplib.Description("Maximum number of revisions to return in this call.")),
			mcplib.WithString("pageToken", mcplib.Description("A page token received from a previous call to ListRevisions. All other parameters must match.")),
			mcplib.WithString("showDeleted", mcplib.Description("If true, returns deleted (but unexpired) resources along with active ones.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{parent}/revisions", []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}, {PublicName: "showDeleted", WireName: "showDeleted", Location: "query"}}, []string{"parent"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT only). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export CLOUD_RUN_ADMIN_OAUTH2C=<your-key>" +
					"\n      Run 'cloud-run-admin-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export CLOUD_RUN_ADMIN_OAUTH2C=<your-key>" +
					"\n      Run 'cloud-run-admin-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export CLOUD_RUN_ADMIN_OAUTH2C=<your-key>" +
					"\n      Run 'cloud-run-admin-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "cloud-run-admin-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "cloud-run-admin-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenWithContext(ctx, dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	// Block write operations
	upper := strings.ToUpper(strings.TrimSpace(query))
	for _, prefix := range []string{"INSERT", "UPDATE", "DELETE", "DROP", "ALTER", "CREATE"} {
		if strings.HasPrefix(upper, prefix) {
			return mcplib.NewToolResultError("only SELECT queries are allowed"), nil
		}
	}

	db, err := store.OpenWithContext(ctx, dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "cloud-run-admin",
		"description": "Deploy and manage user provided container images that scale automatically based on incoming requests. The Cloud Run...",
		"archetype":   "communication",
		"tool_count":  16,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion cloud-run-admin-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "CLOUD_RUN_ADMIN_OAUTH2C",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "cloud-run-admin-jobs",
				"description": "Manage cloud run admin jobs",
				"endpoints":   []string{"create", "list", "run"},
				"searchable":  true,
			},
			{
				"name":        "executions",
				"description": "Manage executions",
				"endpoints":   []string{},
				"searchable":  true,
			},
			{
				"name":        "operations",
				"description": "Manage operations",
				"endpoints":   []string{"list", "wait"},
				"searchable":  true,
			},
			{
				"name":        "services",
				"description": "Manage services",
				"endpoints":   []string{"create", "get-iam-policy", "list", "patch", "set-iam-policy", "test-iam-permissions"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass pagetoken parameter for subsequent pages.",
			"Control page size with the pagesize parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Intent-aware command finder", "command": "which", "description": "Finds the Cloud Run command that matches an operator's intent, such as listing services, inspecting revisions, or...", "rationale": "This compensates for the large Cloud Run resource tree and gives agents a deterministic way to pick the right...", "via": "mcp-command-mirror"},
			{"name": "Local Cloud Run inventory", "command": "sync", "description": "Syncs Cloud Run resources into a local SQLite store so services, jobs, revisions, executions, tasks, and operations...", "rationale": "The official CLI is excellent at live operations, but it does not provide a dedicated local data layer for repeated...", "via": "mcp-command-mirror"},
			{"name": "Offline resource search", "command": "search", "description": "Searches synced Cloud Run inventory locally or falls back to live API calls depending on the data-source mode.", "rationale": "Cloud Run debugging often starts with partial names or fragments; local full-text search reduces repeated API calls...", "via": "mcp-command-mirror"},
			{"name": "Cloud Run analytics", "command": "analytics", "description": "Runs local analysis against synced Cloud Run data for inventory and status-oriented questions.", "rationale": "The Admin API exposes raw resources; operators still need compact summaries to spot stale revisions, failed...", "via": "mcp-command-mirror"},
			{"name": "Compound Cloud Run workflows", "command": "workflow", "description": "Provides compound workflows that combine multiple Cloud Run Admin API operations into one operator-facing flow.", "rationale": "Services, jobs, executions, revisions, and operations are related during rollouts and job runs; workflow commands...", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Intent-aware command finder", "insight": "This compensates for the large Cloud Run resource tree and gives agents a deterministic way to pick the right command before calling the API."},
			{"topic": "Local Cloud Run inventory", "insight": "The official CLI is excellent at live operations, but it does not provide a dedicated local data layer for repeated analysis."},
			{"topic": "Offline resource search", "insight": "Cloud Run debugging often starts with partial names or fragments; local full-text search reduces repeated API calls and context size."},
			{"topic": "Cloud Run analytics", "insight": "The Admin API exposes raw resources; operators still need compact summaries to spot stale revisions, failed executions, or inventory shape."},
			{"topic": "Compound Cloud Run workflows", "insight": "Services, jobs, executions, revisions, and operations are related during rollouts and job runs; workflow commands keep those checks together."},
			{"topic": "Message search", "insight": "Use the search tool on synced data rather than paginating through message history. Message APIs often have aggressive rate limits."},
			{"topic": "Channel health", "insight": "When analyzing channel activity, use the channel-health command or sql aggregation on synced messages. Don't iterate individual messages via API."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
