// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCloudRunAdminJobsListCmd(flags *rootFlags) *cobra.Command {
	var flagPageSize int
	var flagPageToken string
	var flagShowDeleted bool
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list <parent>",
		Aliases:     []string{"get"},
		Short:       "Lists Jobs.",
		Example:     "  cloud-run-admin-pp-cli cloud-run-admin-jobs list example-value",
		Annotations: map[string]string{"pp:endpoint": "cloud-run-admin-jobs.list", "pp:method": "GET", "pp:path": "/v2/{parent}/jobs", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/{parent}/jobs"
			path = replacePathParam(path, "parent", args[0])
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "cloud-run-admin-jobs", path, map[string]string{
				"pageSize":    fmt.Sprintf("%v", flagPageSize),
				"pageToken":   fmt.Sprintf("%v", flagPageToken),
				"showDeleted": fmt.Sprintf("%v", flagShowDeleted),
			}, nil, flagAll, "pagetoken", "nextPageToken", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Maximum number of Jobs to return in this call.")
	cmd.Flags().StringVar(&flagPageToken, "page-token", "", "A page token received from a previous call to ListJobs. All other parameters must match.")
	cmd.Flags().BoolVar(&flagShowDeleted, "show-deleted", false, "If true, returns deleted (but unexpired) resources along with active ones.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
