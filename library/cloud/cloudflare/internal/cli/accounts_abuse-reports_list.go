// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsAbuseReportsListCmd(flags *rootFlags) *cobra.Command {
	var flagPage string
	var flagPerPage int
	var flagSort string
	var flagDomain string
	var flagCreatedBefore string
	var flagCreatedAfter string
	var flagStatus string
	var flagType string
	var flagMitigationStatus string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list <account_id>",
		Short:       "List the abuse reports for a given account",
		Example:     "  cloudflare-pp-cli accounts abuse-reports list 023e105f4ecef8ad9ca31a8372d0c353",
		Annotations: map[string]string{"pp:endpoint": "abuse-reports.list", "pp:method": "GET", "pp:path": "/accounts/{account_id}/abuse-reports", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"accepted", "in_review"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStatus, "status", allowedStatus)
				}
			}
			if cmd.Flags().Changed("type") {
				allowedType := []string{"PHISH", "GEN", "THREAT", "DMCA", "EMER", "TM", "REG_WHO", "NCSEI", "NETWORK"}
				validType := false
				for _, v := range allowedType {
					if flagType == v {
						validType = true
						break
					}
				}
				if !validType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagType, "type", allowedType)
				}
			}
			if cmd.Flags().Changed("mitigation-status") {
				allowedMitigationStatus := []string{"pending", "active", "in_review", "cancelled", "removed"}
				validMitigationStatus := false
				for _, v := range allowedMitigationStatus {
					if flagMitigationStatus == v {
						validMitigationStatus = true
						break
					}
				}
				if !validMitigationStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagMitigationStatus, "mitigation-status", allowedMitigationStatus)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/abuse-reports"
			path = replacePathParam(path, "account_id", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "abuse-reports", path, map[string]string{
				"page":              formatCLIParamValue(flagPage),
				"per_page":          formatCLIParamValue(flagPerPage),
				"sort":              formatCLIParamValue(flagSort),
				"domain":            formatCLIParamValue(flagDomain),
				"created_before":    formatCLIParamValue(flagCreatedBefore),
				"created_after":     formatCLIParamValue(flagCreatedAfter),
				"status":            formatCLIParamValue(flagStatus),
				"type":              formatCLIParamValue(flagType),
				"mitigation_status": formatCLIParamValue(flagMitigationStatus),
			}, nil, flagAll, "page", "page", "per_page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagPage, "page", "", "Where in pagination to start listing abuse reports")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 0, "How many abuse reports per page to list")
	cmd.Flags().StringVar(&flagSort, "sort", "", "A property to sort by, followed by the order (id, cdate, domain, type, status)")
	cmd.Flags().StringVar(&flagDomain, "domain", "", "Filter by domain name related to the abuse report")
	cmd.Flags().StringVar(&flagCreatedBefore, "created-before", "", "Returns reports created before the specified date")
	cmd.Flags().StringVar(&flagCreatedAfter, "created-after", "", "Returns reports created after the specified date")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Filter by the status of the report. (one of: accepted, in_review)")
	cmd.Flags().StringVar(&flagType, "type", "", "Filter by the type of the report. (one of: PHISH, GEN, THREAT, DMCA, EMER, TM, REG_WHO, NCSEI, NETWORK)")
	cmd.Flags().StringVar(&flagMitigationStatus, "mitigation-status", "", "Filter reports that have any mitigations in the given status. (one of: pending, active, in_review, cancelled, removed)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
