// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/cloud/cloudflare/internal/cliutil"
	"github.com/spf13/cobra"
)

func newZonesWaitingRoomsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAdditionalRoutes string
	var bodyCookieAttributesSamesite string
	var bodyCookieAttributesSecure string
	var bodyCookieSuffix string
	var bodyCustomPageHtml string
	var bodyDefaultTemplateLanguage string
	var bodyDescription string
	var bodyDisableSessionRenewal bool
	var bodyEnabledOriginCommands string
	var bodyHost string
	var bodyJsonResponseEnabled bool
	var bodyName string
	var bodyNewUsersPerMinute int
	var bodyPath string
	var bodyQueueAll bool
	var bodyQueueingMethod string
	var bodyQueueingStatusCode int
	var bodySessionDuration int
	var bodySuspended bool
	var bodyTotalActiveUsers int
	var bodyTurnstileAction string
	var bodyTurnstileMode string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create <zone_id>",
		Short:       "Creates a new waiting room.",
		Example:     "  cloudflare-pp-cli zones waiting-rooms create 023e105f4ecef8ad9ca31a8372d0c353 --host shop.example.com",
		Annotations: map[string]string{"pp:endpoint": "waiting-rooms.create", "pp:method": "POST", "pp:path": "/zones/{zone_id}/waiting_rooms"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("host") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "host")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("new-users-per-minute") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "new-users-per-minute")
				}
				if !cmd.Flags().Changed("total-active-users") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "total-active-users")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/zones/{zone_id}/waiting_rooms"
			path = replacePathParam(path, "zone_id", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAdditionalRoutes != "" {
					var parsedAdditionalRoutes any
					if err := json.Unmarshal([]byte(bodyAdditionalRoutes), &parsedAdditionalRoutes); err != nil {
						return fmt.Errorf("parsing --additional-routes JSON: %w", err)
					}
					body["additional_routes"] = parsedAdditionalRoutes
				}
				{
					nestedCookieAttributes := map[string]any{}
					if bodyCookieAttributesSamesite != "" {
						nestedCookieAttributes["samesite"] = bodyCookieAttributesSamesite
					}
					if bodyCookieAttributesSecure != "" {
						nestedCookieAttributes["secure"] = bodyCookieAttributesSecure
					}
					if len(nestedCookieAttributes) > 0 {
						body["cookie_attributes"] = nestedCookieAttributes
					}
				}
				if bodyCookieSuffix != "" {
					body["cookie_suffix"] = bodyCookieSuffix
				}
				if bodyCustomPageHtml != "" {
					body["custom_page_html"] = bodyCustomPageHtml
				}
				if bodyDefaultTemplateLanguage != "" {
					body["default_template_language"] = bodyDefaultTemplateLanguage
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if cmd.Flags().Changed("disable-session-renewal") {
					body["disable_session_renewal"] = bodyDisableSessionRenewal
				}
				if bodyEnabledOriginCommands != "" {
					body["enabled_origin_commands"] = cliutil.SplitCSV(bodyEnabledOriginCommands)
				}
				if bodyHost != "" {
					body["host"] = bodyHost
				}
				if cmd.Flags().Changed("json-response-enabled") {
					body["json_response_enabled"] = bodyJsonResponseEnabled
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNewUsersPerMinute != 0 {
					body["new_users_per_minute"] = bodyNewUsersPerMinute
				}
				if bodyPath != "" {
					body["path"] = bodyPath
				}
				if cmd.Flags().Changed("queue-all") {
					body["queue_all"] = bodyQueueAll
				}
				if bodyQueueingMethod != "" {
					body["queueing_method"] = bodyQueueingMethod
				}
				if bodyQueueingStatusCode != 0 {
					body["queueing_status_code"] = bodyQueueingStatusCode
				}
				if bodySessionDuration != 0 {
					body["session_duration"] = bodySessionDuration
				}
				if cmd.Flags().Changed("suspended") {
					body["suspended"] = bodySuspended
				}
				if bodyTotalActiveUsers != 0 {
					body["total_active_users"] = bodyTotalActiveUsers
				}
				if bodyTurnstileAction != "" {
					body["turnstile_action"] = bodyTurnstileAction
				}
				if bodyTurnstileMode != "" {
					body["turnstile_mode"] = bodyTurnstileMode
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "waiting-rooms", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "waiting-rooms", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "waiting-rooms", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "waiting-rooms", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "waiting-rooms", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "waiting-rooms",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "waiting-rooms", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "waiting-rooms", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAdditionalRoutes, "additional-routes", "", "Only available for the Waiting Room Advanced subscription.")
	cmd.Flags().StringVar(&bodyCookieAttributesSamesite, "cookie-attributes-samesite", "", "Configures the SameSite attribute on the waiting room cookie.")
	cmd.Flags().StringVar(&bodyCookieAttributesSecure, "cookie-attributes-secure", "", "Configures the Secure attribute on the waiting room cookie.")
	cmd.Flags().StringVar(&bodyCookieSuffix, "cookie-suffix", "", "Appends a '_' + a custom suffix to the end of Cloudflare Waiting Room's cookie name(__cf_waitingroom).")
	cmd.Flags().StringVar(&bodyCustomPageHtml, "custom-page-html", "", "Only available for the Waiting Room Advanced subscription.")
	cmd.Flags().StringVar(&bodyDefaultTemplateLanguage, "default-template-language", "en-US", "The language of the default page template.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A note that you can use to add more details about the waiting room.")
	cmd.Flags().BoolVar(&bodyDisableSessionRenewal, "disable-session-renewal", false, "Only available for the Waiting Room Advanced subscription. Disables automatic renewal of session cookies.")
	cmd.Flags().StringVar(&bodyEnabledOriginCommands, "enabled-origin-commands", "[]", "A list of enabled origin commands.")
	cmd.Flags().StringVar(&bodyHost, "host", "", "The host name to which the waiting room will be applied (no wildcards).")
	cmd.Flags().BoolVar(&bodyJsonResponseEnabled, "json-response-enabled", false, "Only available for the Waiting Room Advanced subscription.")
	cmd.Flags().StringVar(&bodyName, "name", "", "A unique name to identify the waiting room. Only alphanumeric characters, hyphens and underscores are allowed.")
	cmd.Flags().IntVar(&bodyNewUsersPerMinute, "new-users-per-minute", 0, "Sets the number of new users that will be let into the route every minute.")
	cmd.Flags().StringVar(&bodyPath, "path", "/", "Sets the path within the host to enable the waiting room on. The waiting room will be enabled for all subpaths as well.")
	cmd.Flags().BoolVar(&bodyQueueAll, "queue-all", false, "If queue_all is `true`, all the traffic that is coming to a route will be sent to the waiting room.")
	cmd.Flags().StringVar(&bodyQueueingMethod, "queueing-method", "fifo", "Sets the queueing method used by the waiting room.")
	cmd.Flags().IntVar(&bodyQueueingStatusCode, "queueing-status-code", 200, "HTTP status code returned to a user while in the queue.")
	cmd.Flags().IntVar(&bodySessionDuration, "session-duration", 5, "Lifetime of a cookie (in minutes) set by Cloudflare for users who get access to the route.")
	cmd.Flags().BoolVar(&bodySuspended, "suspended", false, "Suspends or allows traffic going to the waiting room. If set to `true`, the traffic will not go to the waiting room.")
	cmd.Flags().IntVar(&bodyTotalActiveUsers, "total-active-users", 0, "Sets the total number of active user sessions on the route at a point in time.")
	cmd.Flags().StringVar(&bodyTurnstileAction, "turnstile-action", "log", "Which action to take when a bot is detected using Turnstile.")
	cmd.Flags().StringVar(&bodyTurnstileMode, "turnstile-mode", "invisible", "Which Turnstile widget type to use for detecting bot traffic. See [the Turnstile documentation](https://developers.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
