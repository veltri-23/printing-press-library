// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newZonesFirewallIpAccessRulesForAZoneListIpAccessRulesCmd(flags *rootFlags) *cobra.Command {
	var flagMode string
	var flagConfigurationTarget string
	var flagConfigurationValue string
	var flagNotes string
	var flagMatch string
	var flagPage string
	var flagPerPage float64
	var flagOrder string
	var flagDirection string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "ip-access-rules-for-a-zone-list-ip-access-rules <zone_id>",
		Aliases:     []string{"get"},
		Short:       "Fetches IP Access rules of a zone. You can filter the results using several optional parameters.",
		Example:     "  cloudflare-pp-cli zones firewall ip-access-rules-for-a-zone-list-ip-access-rules 023e105f4ecef8ad9ca31a8372d0c353",
		Annotations: map[string]string{"pp:endpoint": "firewall.ip-access-rules-for-a-zone-list-ip-access-rules", "pp:method": "GET", "pp:path": "/zones/{zone_id}/firewall/access_rules/rules", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("mode") {
				allowedMode := []string{"block", "challenge", "whitelist", "js_challenge", "managed_challenge"}
				validMode := false
				for _, v := range allowedMode {
					if flagMode == v {
						validMode = true
						break
					}
				}
				if !validMode {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagMode, "mode", allowedMode)
				}
			}
			if cmd.Flags().Changed("configuration-target") {
				allowedConfigurationTarget := []string{"ip", "ip_range", "asn", "country"}
				validConfigurationTarget := false
				for _, v := range allowedConfigurationTarget {
					if flagConfigurationTarget == v {
						validConfigurationTarget = true
						break
					}
				}
				if !validConfigurationTarget {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagConfigurationTarget, "configuration-target", allowedConfigurationTarget)
				}
			}
			if cmd.Flags().Changed("match") {
				allowedMatch := []string{"any", "all"}
				validMatch := false
				for _, v := range allowedMatch {
					if flagMatch == v {
						validMatch = true
						break
					}
				}
				if !validMatch {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagMatch, "match", allowedMatch)
				}
			}
			if cmd.Flags().Changed("order") {
				allowedOrder := []string{"configuration.target", "configuration.value", "mode"}
				validOrder := false
				for _, v := range allowedOrder {
					if flagOrder == v {
						validOrder = true
						break
					}
				}
				if !validOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrder, "order", allowedOrder)
				}
			}
			if cmd.Flags().Changed("direction") {
				allowedDirection := []string{"asc", "desc"}
				validDirection := false
				for _, v := range allowedDirection {
					if flagDirection == v {
						validDirection = true
						break
					}
				}
				if !validDirection {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagDirection, "direction", allowedDirection)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/zones/{zone_id}/firewall/access_rules/rules"
			path = replacePathParam(path, "zone_id", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "firewall", path, map[string]string{
				"mode":                 formatCLIParamValue(flagMode),
				"configuration.target": formatCLIParamValue(flagConfigurationTarget),
				"configuration.value":  formatCLIParamValue(flagConfigurationValue),
				"notes":                formatCLIParamValue(flagNotes),
				"match":                formatCLIParamValue(flagMatch),
				"page":                 formatCLIParamValue(flagPage),
				"per_page":             formatCLIParamValue(flagPerPage),
				"order":                formatCLIParamValue(flagOrder),
				"direction":            formatCLIParamValue(flagDirection),
			}, nil, flagAll, "page", "page", "per_page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagMode, "mode", "", "Mode (one of: block, challenge, whitelist, js_challenge, managed_challenge)")
	cmd.Flags().StringVar(&flagConfigurationTarget, "configuration-target", "", "Configuration target (one of: ip, ip_range, asn, country)")
	cmd.Flags().StringVar(&flagConfigurationValue, "configuration-value", "", "Configuration value")
	cmd.Flags().StringVar(&flagNotes, "notes", "", "Notes")
	cmd.Flags().StringVar(&flagMatch, "match", "all", "Match (one of: any, all)")
	cmd.Flags().StringVar(&flagPage, "page", "", "Page")
	cmd.Flags().Float64Var(&flagPerPage, "per-page", 0.0, "Per page")
	cmd.Flags().StringVar(&flagOrder, "order", "", "Order (one of: configuration.target, configuration.value, mode)")
	cmd.Flags().StringVar(&flagDirection, "direction", "", "Direction (one of: asc, desc)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
