// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsMagicGreTunnelsUpdateGreTunnelCmd(flags *rootFlags) *cobra.Command {
	var bodyAutomaticReturnRouting bool
	var bodyCloudflareGreEndpoint string
	var bodyCustomerGreEndpoint string
	var bodyDescription string
	var bodyHealthCheckDirection string
	var bodyHealthCheckEnabled bool
	var bodyHealthCheckRate string
	var bodyHealthCheckTarget string
	var bodyHealthCheckType string
	var bodyInterfaceAddress string
	var bodyInterfaceAddress6 string
	var bodyMtu int
	var bodyName string
	var bodyTtl int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "gre-tunnels-update-gre-tunnel <gre_tunnel_id> <account_id>",
		Short:       "Updates a specific GRE tunnel. Use `?",
		Example:     "  cloudflare-pp-cli accounts magic gre-tunnels-update-gre-tunnel 023e105f4ecef8ad9ca31a8372d0c353 023e105f4ecef8ad9ca31a8372d0c353 --cloudflare-gre-endpoint 203.0.113.1",
		Annotations: map[string]string{"pp:endpoint": "magic.gre-tunnels-update-gre-tunnel", "pp:method": "PUT", "pp:path": "/accounts/{account_id}/magic/gre_tunnels/{gre_tunnel_id}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("cloudflare-gre-endpoint") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "cloudflare-gre-endpoint")
				}
				if !cmd.Flags().Changed("customer-gre-endpoint") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "customer-gre-endpoint")
				}
				if !cmd.Flags().Changed("interface-address") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "interface-address")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/magic/gre_tunnels/{gre_tunnel_id}"
			path = replacePathParam(path, "gre_tunnel_id", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("account_id is required\nUsage: %s <%s>", cmd.CommandPath(), "account_id"))
			}
			path = replacePathParam(path, "account_id", args[1])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("automatic-return-routing") {
					body["automatic_return_routing"] = bodyAutomaticReturnRouting
				}
				if bodyCloudflareGreEndpoint != "" {
					body["cloudflare_gre_endpoint"] = bodyCloudflareGreEndpoint
				}
				if bodyCustomerGreEndpoint != "" {
					body["customer_gre_endpoint"] = bodyCustomerGreEndpoint
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				{
					nestedHealthCheck := map[string]any{}
					if bodyHealthCheckDirection != "" {
						nestedHealthCheck["direction"] = bodyHealthCheckDirection
					}
					if cmd.Flags().Changed("health-check-enabled") {
						nestedHealthCheck["enabled"] = bodyHealthCheckEnabled
					}
					if bodyHealthCheckRate != "" {
						nestedHealthCheck["rate"] = bodyHealthCheckRate
					}
					if bodyHealthCheckTarget != "" {
						nestedHealthCheck["target"] = bodyHealthCheckTarget
					}
					if bodyHealthCheckType != "" {
						nestedHealthCheck["type"] = bodyHealthCheckType
					}
					if len(nestedHealthCheck) > 0 {
						body["health_check"] = nestedHealthCheck
					}
				}
				if bodyInterfaceAddress != "" {
					body["interface_address"] = bodyInterfaceAddress
				}
				if bodyInterfaceAddress6 != "" {
					body["interface_address6"] = bodyInterfaceAddress6
				}
				if bodyMtu != 0 {
					body["mtu"] = bodyMtu
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyTtl != 0 {
					body["ttl"] = bodyTtl
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "magic", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "magic", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "magic", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "magic", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "magic", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "magic",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "magic", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "magic", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAutomaticReturnRouting, "automatic-return-routing", false, "True if automatic stateful return routing should be enabled for a tunnel, false otherwise.")
	cmd.Flags().StringVar(&bodyCloudflareGreEndpoint, "cloudflare-gre-endpoint", "", "The IP address assigned to the Cloudflare side of the GRE tunnel.")
	cmd.Flags().StringVar(&bodyCustomerGreEndpoint, "customer-gre-endpoint", "", "The IP address assigned to the customer side of the GRE tunnel.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "An optional description of the GRE tunnel.")
	cmd.Flags().StringVar(&bodyHealthCheckDirection, "health-check-direction", "", "The direction of the flow of the healthcheck.")
	cmd.Flags().BoolVar(&bodyHealthCheckEnabled, "health-check-enabled", false, "Determines whether to run healthchecks for a tunnel.")
	cmd.Flags().StringVar(&bodyHealthCheckRate, "health-check-rate", "", "How frequent the health check is run. The default value is `mid`.")
	cmd.Flags().StringVar(&bodyHealthCheckTarget, "health-check-target", "", "The destination address in a request type health check.")
	cmd.Flags().StringVar(&bodyHealthCheckType, "health-check-type", "", "The type of healthcheck to run, reply or request. The default value is `reply`.")
	cmd.Flags().StringVar(&bodyInterfaceAddress, "interface-address", "", "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one for each side of the tunnel.")
	cmd.Flags().StringVar(&bodyInterfaceAddress6, "interface-address6", "", "A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space with the address being the first IP of the subnet")
	cmd.Flags().IntVar(&bodyMtu, "mtu", 1476, "Maximum Transmission Unit (MTU) in bytes for the GRE tunnel. The minimum value is 576.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the tunnel.")
	cmd.Flags().IntVar(&bodyTtl, "ttl", 64, "Time To Live (TTL) in number of hops of the GRE tunnel.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
