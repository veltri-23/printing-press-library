// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/cloud/cloudflare/internal/cliutil"
	"github.com/spf13/cobra"
)

func newAccountsAccessPoliciesCreateAnReusablePolicyCmd(flags *rootFlags) *cobra.Command {
	var bodyApprovalGroups string
	var bodyApprovalRequired bool
	var bodyConnectionRulesRdpAllowedClipboardLocalToRemoteFormats string
	var bodyConnectionRulesRdpAllowedClipboardRemoteToLocalFormats string
	var bodyDecision string
	var bodyExclude string
	var bodyInclude string
	var bodyIsolationRequired bool
	var bodyMfaConfigAllowedAuthenticators string
	var bodyMfaConfigMfaDisabled bool
	var bodyMfaConfigSessionDuration string
	var bodyName string
	var bodyPurposeJustificationPrompt string
	var bodyPurposeJustificationRequired bool
	var bodyRequire string
	var bodySessionDuration string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "policies-create-an-reusable-policy <account_id>",
		Short:       "Creates a new Access reusable policy.",
		Example:     "  cloudflare-pp-cli accounts access policies-create-an-reusable-policy 023e105f4ecef8ad9ca31a8372d0c353 --decision allow",
		Annotations: map[string]string{"pp:endpoint": "access.policies-create-an-reusable-policy", "pp:method": "POST", "pp:path": "/accounts/{account_id}/access/policies"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("decision") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "decision")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/access/policies"
			path = replacePathParam(path, "account_id", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyApprovalGroups != "" {
					var parsedApprovalGroups any
					if err := json.Unmarshal([]byte(bodyApprovalGroups), &parsedApprovalGroups); err != nil {
						return fmt.Errorf("parsing --approval-groups JSON: %w", err)
					}
					body["approval_groups"] = parsedApprovalGroups
				}
				if cmd.Flags().Changed("approval-required") {
					body["approval_required"] = bodyApprovalRequired
				}
				{
					nestedConnectionRules := map[string]any{}
					{
						nestedConnectionRulesRdp := map[string]any{}
						if bodyConnectionRulesRdpAllowedClipboardLocalToRemoteFormats != "" {
							nestedConnectionRulesRdp["allowed_clipboard_local_to_remote_formats"] = cliutil.SplitCSV(bodyConnectionRulesRdpAllowedClipboardLocalToRemoteFormats)
						}
						if bodyConnectionRulesRdpAllowedClipboardRemoteToLocalFormats != "" {
							nestedConnectionRulesRdp["allowed_clipboard_remote_to_local_formats"] = cliutil.SplitCSV(bodyConnectionRulesRdpAllowedClipboardRemoteToLocalFormats)
						}
						if len(nestedConnectionRulesRdp) > 0 {
							nestedConnectionRules["rdp"] = nestedConnectionRulesRdp
						}
					}
					if len(nestedConnectionRules) > 0 {
						body["connection_rules"] = nestedConnectionRules
					}
				}
				if bodyDecision != "" {
					body["decision"] = bodyDecision
				}
				if bodyExclude != "" {
					var parsedExclude any
					if err := json.Unmarshal([]byte(bodyExclude), &parsedExclude); err != nil {
						return fmt.Errorf("parsing --exclude JSON: %w", err)
					}
					body["exclude"] = parsedExclude
				}
				if bodyInclude != "" {
					var parsedInclude any
					if err := json.Unmarshal([]byte(bodyInclude), &parsedInclude); err != nil {
						return fmt.Errorf("parsing --include JSON: %w", err)
					}
					body["include"] = parsedInclude
				}
				if cmd.Flags().Changed("isolation-required") {
					body["isolation_required"] = bodyIsolationRequired
				}
				{
					nestedMfaConfig := map[string]any{}
					if bodyMfaConfigAllowedAuthenticators != "" {
						nestedMfaConfig["allowed_authenticators"] = cliutil.SplitCSV(bodyMfaConfigAllowedAuthenticators)
					}
					if cmd.Flags().Changed("mfa-config-mfa-disabled") {
						nestedMfaConfig["mfa_disabled"] = bodyMfaConfigMfaDisabled
					}
					if bodyMfaConfigSessionDuration != "" {
						nestedMfaConfig["session_duration"] = bodyMfaConfigSessionDuration
					}
					if len(nestedMfaConfig) > 0 {
						body["mfa_config"] = nestedMfaConfig
					}
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPurposeJustificationPrompt != "" {
					body["purpose_justification_prompt"] = bodyPurposeJustificationPrompt
				}
				if cmd.Flags().Changed("purpose-justification-required") {
					body["purpose_justification_required"] = bodyPurposeJustificationRequired
				}
				if bodyRequire != "" {
					var parsedRequire any
					if err := json.Unmarshal([]byte(bodyRequire), &parsedRequire); err != nil {
						return fmt.Errorf("parsing --require JSON: %w", err)
					}
					body["require"] = parsedRequire
				}
				if bodySessionDuration != "" {
					body["session_duration"] = bodySessionDuration
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "access", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "access", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "access", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "access", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "access", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "access",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "access", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "access", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyApprovalGroups, "approval-groups", "", "Administrators who can approve a temporary authentication request.")
	cmd.Flags().BoolVar(&bodyApprovalRequired, "approval-required", false, "Requires the user to request access from an administrator at the start of each session.")
	cmd.Flags().StringVar(&bodyConnectionRulesRdpAllowedClipboardLocalToRemoteFormats, "connection-rules-rdp-allowed-clipboard-local-to-remote-formats", "", "Clipboard formats allowed when copying from local machine to remote RDP session.")
	cmd.Flags().StringVar(&bodyConnectionRulesRdpAllowedClipboardRemoteToLocalFormats, "connection-rules-rdp-allowed-clipboard-remote-to-local-formats", "", "Clipboard formats allowed when copying from remote RDP session to local machine.")
	cmd.Flags().StringVar(&bodyDecision, "decision", "", "The action Access will take if a user matches this policy.")
	cmd.Flags().StringVar(&bodyExclude, "exclude", "[]", "Rules evaluated with a NOT logical operator. To match the policy, a user cannot meet any of the Exclude rules.")
	cmd.Flags().StringVar(&bodyInclude, "include", "[]", "Rules evaluated with an OR logical operator. A user needs to meet only one of the Include rules.")
	cmd.Flags().BoolVar(&bodyIsolationRequired, "isolation-required", false, "Require this application to be served in an isolated browser for users matching this policy.")
	cmd.Flags().StringVar(&bodyMfaConfigAllowedAuthenticators, "mfa-config-allowed-authenticators", "", "Lists the MFA methods that users can authenticate with.")
	cmd.Flags().BoolVar(&bodyMfaConfigMfaDisabled, "mfa-config-mfa-disabled", false, "Indicates whether to disable MFA for this resource. This option is available at the application and policy level.")
	cmd.Flags().StringVar(&bodyMfaConfigSessionDuration, "mfa-config-session-duration", "", "Defines the duration of an MFA session. Must be in minutes (m) or hours (h). Minimum: 0m. Maximum: 720h (30 days).")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the Access policy.")
	cmd.Flags().StringVar(&bodyPurposeJustificationPrompt, "purpose-justification-prompt", "", "A custom message that will appear on the purpose justification screen.")
	cmd.Flags().BoolVar(&bodyPurposeJustificationRequired, "purpose-justification-required", false, "Require users to enter a justification when they log in to the application.")
	cmd.Flags().StringVar(&bodyRequire, "require", "[]", "Rules evaluated with an AND logical operator. To match the policy, a user must meet all of the Require rules.")
	cmd.Flags().StringVar(&bodySessionDuration, "session-duration", "24h", "The amount of time that tokens issued for the application will be valid. Must be in the format `300ms` or `2h45m`.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
