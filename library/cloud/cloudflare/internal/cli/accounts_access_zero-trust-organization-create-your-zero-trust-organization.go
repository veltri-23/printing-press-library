// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/cloud/cloudflare/internal/cliutil"
	"github.com/spf13/cobra"
)

func newAccountsAccessZeroTrustOrganizationCreateYourZeroTrustOrganizationCmd(flags *rootFlags) *cobra.Command {
	var bodyAllowAuthenticateViaWarp bool
	var bodyAuthDomain string
	var bodyAutoRedirectToIdentity bool
	var bodyDenyUnmatchedRequests bool
	var bodyDenyUnmatchedRequestsExemptedZoneNames string
	var bodyIsUiReadOnly bool
	var bodyLoginDesignBackgroundColor string
	var bodyLoginDesignFooterText string
	var bodyLoginDesignHeaderText string
	var bodyLoginDesignLogoPath string
	var bodyLoginDesignTextColor string
	var bodyMfaConfigAllowedAuthenticators string
	var bodyMfaConfigAmrMatchingSessionDuration string
	var bodyMfaConfigRequiredAaguids string
	var bodyMfaConfigSessionDuration string
	var bodyMfaRequiredForAllApps bool
	var bodyMfaSshPivKeyRequirementsPinPolicy string
	var bodyMfaSshPivKeyRequirementsRequireFipsDevice bool
	var bodyMfaSshPivKeyRequirementsSshKeySize string
	var bodyMfaSshPivKeyRequirementsSshKeyType string
	var bodyMfaSshPivKeyRequirementsTouchPolicy string
	var bodyName string
	var bodySessionDuration string
	var bodyUiReadOnlyToggleReason string
	var bodyUserSeatExpirationInactiveTime string
	var bodyWarpAuthSessionDuration string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "zero-trust-organization-create-your-zero-trust-organization <account_id>",
		Short:       "Sets up a Zero Trust organization for your account.",
		Example:     "  cloudflare-pp-cli accounts access zero-trust-organization-create-your-zero-trust-organization 023e105f4ecef8ad9ca31a8372d0c353 --auth-domain test.cloudflareaccess.com",
		Annotations: map[string]string{"pp:endpoint": "access.zero-trust-organization-create-your-zero-trust-organization", "pp:method": "POST", "pp:path": "/accounts/{account_id}/access/organizations"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("auth-domain") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "auth-domain")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/access/organizations"
			path = replacePathParam(path, "account_id", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("allow-authenticate-via-warp") {
					body["allow_authenticate_via_warp"] = bodyAllowAuthenticateViaWarp
				}
				if bodyAuthDomain != "" {
					body["auth_domain"] = bodyAuthDomain
				}
				if cmd.Flags().Changed("auto-redirect-to-identity") {
					body["auto_redirect_to_identity"] = bodyAutoRedirectToIdentity
				}
				if cmd.Flags().Changed("deny-unmatched-requests") {
					body["deny_unmatched_requests"] = bodyDenyUnmatchedRequests
				}
				if bodyDenyUnmatchedRequestsExemptedZoneNames != "" {
					body["deny_unmatched_requests_exempted_zone_names"] = cliutil.SplitCSV(bodyDenyUnmatchedRequestsExemptedZoneNames)
				}
				if cmd.Flags().Changed("is-ui-read-only") {
					body["is_ui_read_only"] = bodyIsUiReadOnly
				}
				{
					nestedLoginDesign := map[string]any{}
					if bodyLoginDesignBackgroundColor != "" {
						nestedLoginDesign["background_color"] = bodyLoginDesignBackgroundColor
					}
					if bodyLoginDesignFooterText != "" {
						nestedLoginDesign["footer_text"] = bodyLoginDesignFooterText
					}
					if bodyLoginDesignHeaderText != "" {
						nestedLoginDesign["header_text"] = bodyLoginDesignHeaderText
					}
					if bodyLoginDesignLogoPath != "" {
						nestedLoginDesign["logo_path"] = bodyLoginDesignLogoPath
					}
					if bodyLoginDesignTextColor != "" {
						nestedLoginDesign["text_color"] = bodyLoginDesignTextColor
					}
					if len(nestedLoginDesign) > 0 {
						body["login_design"] = nestedLoginDesign
					}
				}
				{
					nestedMfaConfig := map[string]any{}
					if bodyMfaConfigAllowedAuthenticators != "" {
						nestedMfaConfig["allowed_authenticators"] = cliutil.SplitCSV(bodyMfaConfigAllowedAuthenticators)
					}
					if bodyMfaConfigAmrMatchingSessionDuration != "" {
						nestedMfaConfig["amr_matching_session_duration"] = bodyMfaConfigAmrMatchingSessionDuration
					}
					if bodyMfaConfigRequiredAaguids != "" {
						nestedMfaConfig["required_aaguids"] = bodyMfaConfigRequiredAaguids
					}
					if bodyMfaConfigSessionDuration != "" {
						nestedMfaConfig["session_duration"] = bodyMfaConfigSessionDuration
					}
					if len(nestedMfaConfig) > 0 {
						body["mfa_config"] = nestedMfaConfig
					}
				}
				if cmd.Flags().Changed("mfa-required-for-all-apps") {
					body["mfa_required_for_all_apps"] = bodyMfaRequiredForAllApps
				}
				{
					nestedMfaSshPivKeyRequirements := map[string]any{}
					if bodyMfaSshPivKeyRequirementsPinPolicy != "" {
						nestedMfaSshPivKeyRequirements["pin_policy"] = bodyMfaSshPivKeyRequirementsPinPolicy
					}
					if cmd.Flags().Changed("mfa-ssh-piv-key-requirements-require-fips-device") {
						nestedMfaSshPivKeyRequirements["require_fips_device"] = bodyMfaSshPivKeyRequirementsRequireFipsDevice
					}
					if bodyMfaSshPivKeyRequirementsSshKeySize != "" {
						var parsedMfaSshPivKeyRequirementsSshKeySize any
						if err := json.Unmarshal([]byte(bodyMfaSshPivKeyRequirementsSshKeySize), &parsedMfaSshPivKeyRequirementsSshKeySize); err != nil {
							return fmt.Errorf("parsing --mfa-ssh-piv-key-requirements-ssh-key-size JSON: %w", err)
						}
						nestedMfaSshPivKeyRequirements["ssh_key_size"] = parsedMfaSshPivKeyRequirementsSshKeySize
					}
					if bodyMfaSshPivKeyRequirementsSshKeyType != "" {
						nestedMfaSshPivKeyRequirements["ssh_key_type"] = cliutil.SplitCSV(bodyMfaSshPivKeyRequirementsSshKeyType)
					}
					if bodyMfaSshPivKeyRequirementsTouchPolicy != "" {
						nestedMfaSshPivKeyRequirements["touch_policy"] = bodyMfaSshPivKeyRequirementsTouchPolicy
					}
					if len(nestedMfaSshPivKeyRequirements) > 0 {
						body["mfa_ssh_piv_key_requirements"] = nestedMfaSshPivKeyRequirements
					}
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodySessionDuration != "" {
					body["session_duration"] = bodySessionDuration
				}
				if bodyUiReadOnlyToggleReason != "" {
					body["ui_read_only_toggle_reason"] = bodyUiReadOnlyToggleReason
				}
				if bodyUserSeatExpirationInactiveTime != "" {
					body["user_seat_expiration_inactive_time"] = bodyUserSeatExpirationInactiveTime
				}
				if bodyWarpAuthSessionDuration != "" {
					body["warp_auth_session_duration"] = bodyWarpAuthSessionDuration
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "access", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "access", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "access", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "access", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "access", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "access",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "access", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "access", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAllowAuthenticateViaWarp, "allow-authenticate-via-warp", false, "When set to true, users can authenticate via WARP for any application in your organization.")
	cmd.Flags().StringVar(&bodyAuthDomain, "auth-domain", "", "The unique subdomain assigned to your Zero Trust organization.")
	cmd.Flags().BoolVar(&bodyAutoRedirectToIdentity, "auto-redirect-to-identity", false, "When set to `true`, users skip the identity provider selection step during login.")
	cmd.Flags().BoolVar(&bodyDenyUnmatchedRequests, "deny-unmatched-requests", false, "Determines whether to deny all requests to Cloudflare-protected resources that lack an associated Access application.")
	cmd.Flags().StringVar(&bodyDenyUnmatchedRequestsExemptedZoneNames, "deny-unmatched-requests-exempted-zone-names", "", "Contains zone names to exempt from the `deny_unmatched_requests` feature.")
	cmd.Flags().BoolVar(&bodyIsUiReadOnly, "is-ui-read-only", false, "Lock all settings as Read-Only in the Dashboard, regardless of user permission.")
	cmd.Flags().StringVar(&bodyLoginDesignBackgroundColor, "login-design-background-color", "", "The background color on your login page.")
	cmd.Flags().StringVar(&bodyLoginDesignFooterText, "login-design-footer-text", "", "The text at the bottom of your login page.")
	cmd.Flags().StringVar(&bodyLoginDesignHeaderText, "login-design-header-text", "", "The text at the top of your login page.")
	cmd.Flags().StringVar(&bodyLoginDesignLogoPath, "login-design-logo-path", "", "The URL of the logo on your login page.")
	cmd.Flags().StringVar(&bodyLoginDesignTextColor, "login-design-text-color", "", "The text color on your login page.")
	cmd.Flags().StringVar(&bodyMfaConfigAllowedAuthenticators, "mfa-config-allowed-authenticators", "", "Lists the MFA methods that users can authenticate with.")
	cmd.Flags().StringVar(&bodyMfaConfigAmrMatchingSessionDuration, "mfa-config-amr-matching-session-duration", "", "Allows a user to skip MFA via Authentication Method Reference (AMR)")
	cmd.Flags().StringVar(&bodyMfaConfigRequiredAaguids, "mfa-config-required-aaguids", "", "Specifies a Cloudflare List of required FIDO2 authenticator device AAGUIDs.")
	cmd.Flags().StringVar(&bodyMfaConfigSessionDuration, "mfa-config-session-duration", "", "Defines the duration of an MFA session. Must be in minutes (m) or hours (h). Minimum: 0m. Maximum: 720h (30 days).")
	cmd.Flags().BoolVar(&bodyMfaRequiredForAllApps, "mfa-required-for-all-apps", false, "Determines whether global MFA settings apply to applications by default.")
	cmd.Flags().StringVar(&bodyMfaSshPivKeyRequirementsPinPolicy, "mfa-ssh-piv-key-requirements-pin-policy", "", "Defines when a PIN is required to use the SSH key.")
	cmd.Flags().BoolVar(&bodyMfaSshPivKeyRequirementsRequireFipsDevice, "mfa-ssh-piv-key-requirements-require-fips-device", false, "Requires the SSH PIV key to be stored on a FIPS 140-2 Level 1 or higher validated device.")
	cmd.Flags().StringVar(&bodyMfaSshPivKeyRequirementsSshKeySize, "mfa-ssh-piv-key-requirements-ssh-key-size", "", "Specifies the allowed SSH key sizes in bits. Valid sizes depend on key type.")
	cmd.Flags().StringVar(&bodyMfaSshPivKeyRequirementsSshKeyType, "mfa-ssh-piv-key-requirements-ssh-key-type", "", "Specifies the allowed SSH key types. Valid values are `ecdsa`, `ed25519`, and `rsa`.")
	cmd.Flags().StringVar(&bodyMfaSshPivKeyRequirementsTouchPolicy, "mfa-ssh-piv-key-requirements-touch-policy", "", "Defines when physical touch is required to use the SSH key.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of your Zero Trust organization.")
	cmd.Flags().StringVar(&bodySessionDuration, "session-duration", "", "The amount of time that tokens issued for applications will be valid. Must be in the format `300ms` or `2h45m`.")
	cmd.Flags().StringVar(&bodyUiReadOnlyToggleReason, "ui-read-only-toggle-reason", "", "A description of the reason why the UI read only field is being toggled.")
	cmd.Flags().StringVar(&bodyUserSeatExpirationInactiveTime, "user-seat-expiration-inactive-time", "", "The amount of time a user seat is inactive before it expires.")
	cmd.Flags().StringVar(&bodyWarpAuthSessionDuration, "warp-auth-session-duration", "", "The amount of time that tokens issued for applications will be valid. Must be in the format `30m` or `2h45m`.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
