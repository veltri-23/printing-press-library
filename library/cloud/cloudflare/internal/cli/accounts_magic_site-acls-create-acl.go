// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/cloud/cloudflare/internal/cliutil"
	"github.com/spf13/cobra"
)

func newAccountsMagicSiteAclsCreateAclCmd(flags *rootFlags) *cobra.Command {
	var bodyDescription string
	var bodyForwardLocally bool
	var bodyLan1LanId string
	var bodyLan1LanName string
	var bodyLan1PortRanges string
	var bodyLan1Ports string
	var bodyLan1Subnets string
	var bodyLan2LanId string
	var bodyLan2LanName string
	var bodyLan2PortRanges string
	var bodyLan2Ports string
	var bodyLan2Subnets string
	var bodyName string
	var bodyProtocols string
	var bodyUnidirectional bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "site-acls-create-acl <account_id> <site_id>",
		Short:       "Creates a new Site ACL.",
		Example:     "  cloudflare-pp-cli accounts magic site-acls-create-acl 023e105f4ecef8ad9ca31a8372d0c353 023e105f4ecef8ad9ca31a8372d0c353 --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "magic.site-acls-create-acl", "pp:method": "POST", "pp:path": "/accounts/{account_id}/magic/sites/{site_id}/acls"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("lan-1-lan-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "lan-1-lan-id")
				}
				if !cmd.Flags().Changed("lan-2-lan-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "lan-2-lan-id")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/magic/sites/{site_id}/acls"
			path = replacePathParam(path, "account_id", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("site_id is required\nUsage: %s <%s>", cmd.CommandPath(), "site_id"))
			}
			path = replacePathParam(path, "site_id", args[1])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if cmd.Flags().Changed("forward-locally") {
					body["forward_locally"] = bodyForwardLocally
				}
				{
					nestedLan1 := map[string]any{}
					if bodyLan1LanId != "" {
						nestedLan1["lan_id"] = bodyLan1LanId
					}
					if bodyLan1LanName != "" {
						nestedLan1["lan_name"] = bodyLan1LanName
					}
					if bodyLan1PortRanges != "" {
						nestedLan1["port_ranges"] = cliutil.SplitCSV(bodyLan1PortRanges)
					}
					if bodyLan1Ports != "" {
						var parsedLan1Ports any
						if err := json.Unmarshal([]byte(bodyLan1Ports), &parsedLan1Ports); err != nil {
							return fmt.Errorf("parsing --lan-1-ports JSON: %w", err)
						}
						nestedLan1["ports"] = parsedLan1Ports
					}
					if bodyLan1Subnets != "" {
						var parsedLan1Subnets any
						if err := json.Unmarshal([]byte(bodyLan1Subnets), &parsedLan1Subnets); err != nil {
							return fmt.Errorf("parsing --lan-1-subnets JSON: %w", err)
						}
						nestedLan1["subnets"] = parsedLan1Subnets
					}
					if len(nestedLan1) > 0 {
						body["lan_1"] = nestedLan1
					}
				}
				{
					nestedLan2 := map[string]any{}
					if bodyLan2LanId != "" {
						nestedLan2["lan_id"] = bodyLan2LanId
					}
					if bodyLan2LanName != "" {
						nestedLan2["lan_name"] = bodyLan2LanName
					}
					if bodyLan2PortRanges != "" {
						nestedLan2["port_ranges"] = cliutil.SplitCSV(bodyLan2PortRanges)
					}
					if bodyLan2Ports != "" {
						var parsedLan2Ports any
						if err := json.Unmarshal([]byte(bodyLan2Ports), &parsedLan2Ports); err != nil {
							return fmt.Errorf("parsing --lan-2-ports JSON: %w", err)
						}
						nestedLan2["ports"] = parsedLan2Ports
					}
					if bodyLan2Subnets != "" {
						var parsedLan2Subnets any
						if err := json.Unmarshal([]byte(bodyLan2Subnets), &parsedLan2Subnets); err != nil {
							return fmt.Errorf("parsing --lan-2-subnets JSON: %w", err)
						}
						nestedLan2["subnets"] = parsedLan2Subnets
					}
					if len(nestedLan2) > 0 {
						body["lan_2"] = nestedLan2
					}
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyProtocols != "" {
					body["protocols"] = cliutil.SplitCSV(bodyProtocols)
				}
				if cmd.Flags().Changed("unidirectional") {
					body["unidirectional"] = bodyUnidirectional
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "magic", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "magic", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "magic", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "magic", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "magic", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "magic",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "magic", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "magic", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description for the ACL.")
	cmd.Flags().BoolVar(&bodyForwardLocally, "forward-locally", false, "The desired forwarding action for this ACL policy. If set to 'false', the policy will forward traffic to Cloudflare.")
	cmd.Flags().StringVar(&bodyLan1LanId, "lan-1-lan-id", "", "The identifier for the LAN you want to create an ACL policy with.")
	cmd.Flags().StringVar(&bodyLan1LanName, "lan-1-lan-name", "", "The name of the LAN based on the provided lan_id.")
	cmd.Flags().StringVar(&bodyLan1PortRanges, "lan-1-port-ranges", "", "Array of port ranges on the provided LAN that will be included in the ACL.")
	cmd.Flags().StringVar(&bodyLan1Ports, "lan-1-ports", "", "Array of ports on the provided LAN that will be included in the ACL.")
	cmd.Flags().StringVar(&bodyLan1Subnets, "lan-1-subnets", "", "Array of subnet IPs within the LAN that will be included in the ACL.")
	cmd.Flags().StringVar(&bodyLan2LanId, "lan-2-lan-id", "", "The identifier for the LAN you want to create an ACL policy with.")
	cmd.Flags().StringVar(&bodyLan2LanName, "lan-2-lan-name", "", "The name of the LAN based on the provided lan_id.")
	cmd.Flags().StringVar(&bodyLan2PortRanges, "lan-2-port-ranges", "", "Array of port ranges on the provided LAN that will be included in the ACL.")
	cmd.Flags().StringVar(&bodyLan2Ports, "lan-2-ports", "", "Array of ports on the provided LAN that will be included in the ACL.")
	cmd.Flags().StringVar(&bodyLan2Subnets, "lan-2-subnets", "", "Array of subnet IPs within the LAN that will be included in the ACL.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the ACL.")
	cmd.Flags().StringVar(&bodyProtocols, "protocols", "", "Protocols")
	cmd.Flags().BoolVar(&bodyUnidirectional, "unidirectional", false, "The desired traffic direction for this ACL policy. If set to 'false', the policy will allow bidirectional traffic.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
