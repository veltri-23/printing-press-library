// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsAiWorkersPostRunCfDeepgramNova3Cmd(flags *rootFlags) *cobra.Command {
	var flagQueueRequest string
	var flagTags string
	var bodyAudioBody string
	var bodyAudioContentType string
	var bodyChannels float64
	var bodyCustomIntent string
	var bodyCustomIntentMode string
	var bodyCustomTopic string
	var bodyCustomTopicMode string
	var bodyDetectEntities bool
	var bodyDetectLanguage bool
	var bodyDiarize bool
	var bodyDictation bool
	var bodyEncoding string
	var bodyEndpointing string
	var bodyExtra string
	var bodyFillerWords bool
	var bodyInterimResults bool
	var bodyKeyterm string
	var bodyKeywords string
	var bodyLanguage string
	var bodyMeasurements bool
	var bodyMipOptOut bool
	var bodyMode string
	var bodyMultichannel bool
	var bodyNumerals bool
	var bodyParagraphs bool
	var bodyProfanityFilter bool
	var bodyPunctuate bool
	var bodyRedact string
	var bodyReplace string
	var bodySearch string
	var bodySentiment bool
	var bodySmartFormat bool
	var bodyTopics bool
	var bodyUttSplit float64
	var bodyUtteranceEndMs bool
	var bodyUtterances bool
	var bodyVadEvents bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "workers-post-run-cf-deepgram-nova-3 <account_id>",
		Short:       "Runs inference on the @cf/deepgram/nova-3 model.",
		Example:     "  cloudflare-pp-cli accounts ai workers-post-run-cf-deepgram-nova-3 023e105f4ecef8ad9ca31a8372d0c353",
		Annotations: map[string]string{"pp:endpoint": "ai.workers-post-run-cf-deepgram-nova-3", "pp:method": "POST", "pp:path": "/accounts/{account_id}/ai/run/@cf/deepgram/nova-3"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("audio-body") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "audio-body")
				}
				if !cmd.Flags().Changed("audio-content-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "audio-content-type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/ai/run/@cf/deepgram/nova-3"
			path = replacePathParam(path, "account_id", args[0])
			params := map[string]string{}
			if flagQueueRequest != "" {
				params["queueRequest"] = formatCLIParamValue(flagQueueRequest)
			}
			if flagTags != "" {
				params["tags"] = formatCLIParamValue(flagTags)
			}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAudio := map[string]any{}
					if bodyAudioBody != "" {
						var parsedAudioBody any
						if err := json.Unmarshal([]byte(bodyAudioBody), &parsedAudioBody); err != nil {
							return fmt.Errorf("parsing --audio-body JSON: %w", err)
						}
						nestedAudio["body"] = parsedAudioBody
					}
					if bodyAudioContentType != "" {
						nestedAudio["contentType"] = bodyAudioContentType
					}
					if len(nestedAudio) > 0 {
						body["audio"] = nestedAudio
					}
				}
				if bodyChannels != 0.0 {
					body["channels"] = bodyChannels
				}
				if bodyCustomIntent != "" {
					body["custom_intent"] = bodyCustomIntent
				}
				if bodyCustomIntentMode != "" {
					body["custom_intent_mode"] = bodyCustomIntentMode
				}
				if bodyCustomTopic != "" {
					body["custom_topic"] = bodyCustomTopic
				}
				if bodyCustomTopicMode != "" {
					body["custom_topic_mode"] = bodyCustomTopicMode
				}
				if cmd.Flags().Changed("detect-entities") {
					body["detect_entities"] = bodyDetectEntities
				}
				if cmd.Flags().Changed("detect-language") {
					body["detect_language"] = bodyDetectLanguage
				}
				if cmd.Flags().Changed("diarize") {
					body["diarize"] = bodyDiarize
				}
				if cmd.Flags().Changed("dictation") {
					body["dictation"] = bodyDictation
				}
				if bodyEncoding != "" {
					body["encoding"] = bodyEncoding
				}
				if bodyEndpointing != "" {
					body["endpointing"] = bodyEndpointing
				}
				if bodyExtra != "" {
					body["extra"] = bodyExtra
				}
				if cmd.Flags().Changed("filler-words") {
					body["filler_words"] = bodyFillerWords
				}
				if cmd.Flags().Changed("interim-results") {
					body["interim_results"] = bodyInterimResults
				}
				if bodyKeyterm != "" {
					body["keyterm"] = bodyKeyterm
				}
				if bodyKeywords != "" {
					body["keywords"] = bodyKeywords
				}
				if bodyLanguage != "" {
					body["language"] = bodyLanguage
				}
				if cmd.Flags().Changed("measurements") {
					body["measurements"] = bodyMeasurements
				}
				if cmd.Flags().Changed("mip-opt-out") {
					body["mip_opt_out"] = bodyMipOptOut
				}
				if bodyMode != "" {
					body["mode"] = bodyMode
				}
				if cmd.Flags().Changed("multichannel") {
					body["multichannel"] = bodyMultichannel
				}
				if cmd.Flags().Changed("numerals") {
					body["numerals"] = bodyNumerals
				}
				if cmd.Flags().Changed("paragraphs") {
					body["paragraphs"] = bodyParagraphs
				}
				if cmd.Flags().Changed("profanity-filter") {
					body["profanity_filter"] = bodyProfanityFilter
				}
				if cmd.Flags().Changed("punctuate") {
					body["punctuate"] = bodyPunctuate
				}
				if bodyRedact != "" {
					body["redact"] = bodyRedact
				}
				if bodyReplace != "" {
					body["replace"] = bodyReplace
				}
				if bodySearch != "" {
					body["search"] = bodySearch
				}
				if cmd.Flags().Changed("sentiment") {
					body["sentiment"] = bodySentiment
				}
				if cmd.Flags().Changed("smart-format") {
					body["smart_format"] = bodySmartFormat
				}
				if cmd.Flags().Changed("topics") {
					body["topics"] = bodyTopics
				}
				if bodyUttSplit != 0.0 {
					body["utt_split"] = bodyUttSplit
				}
				if cmd.Flags().Changed("utterance-end-ms") {
					body["utterance_end_ms"] = bodyUtteranceEndMs
				}
				if cmd.Flags().Changed("utterances") {
					body["utterances"] = bodyUtterances
				}
				if cmd.Flags().Changed("vad-events") {
					body["vad_events"] = bodyVadEvents
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "ai", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "ai", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "ai",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagQueueRequest, "queue-request", "", "Queue request")
	cmd.Flags().StringVar(&flagTags, "tags", "", "Tags")
	cmd.Flags().StringVar(&bodyAudioBody, "audio-body", "", "Body")
	cmd.Flags().StringVar(&bodyAudioContentType, "audio-content-type", "", "Content type")
	cmd.Flags().Float64Var(&bodyChannels, "channels", 0.0, "The number of channels in the submitted audio")
	cmd.Flags().StringVar(&bodyCustomIntent, "custom-intent", "", "Custom intents you want the model to detect within your input audio if present")
	cmd.Flags().StringVar(&bodyCustomIntentMode, "custom-intent-mode", "", "Sets how the model will interpret intents submitted to the custom_intent param.")
	cmd.Flags().StringVar(&bodyCustomTopic, "custom-topic", "", "Custom topics you want the model to detect within your input audio or text if present Submit up to 100")
	cmd.Flags().StringVar(&bodyCustomTopicMode, "custom-topic-mode", "", "Sets how the model will interpret strings submitted to the custom_topic param.")
	cmd.Flags().BoolVar(&bodyDetectEntities, "detect-entities", false, "Identifies and extracts key entities from content in submitted audio")
	cmd.Flags().BoolVar(&bodyDetectLanguage, "detect-language", false, "Identifies the dominant language spoken in submitted audio")
	cmd.Flags().BoolVar(&bodyDiarize, "diarize", false, "Recognize speaker changes. Each word in the transcript will be assigned a speaker number starting at 0")
	cmd.Flags().BoolVar(&bodyDictation, "dictation", false, "Identify and extract key entities from content in submitted audio")
	cmd.Flags().StringVar(&bodyEncoding, "encoding", "", "Specify the expected encoding of your submitted audio")
	cmd.Flags().StringVar(&bodyEndpointing, "endpointing", "", "Indicates how long model will wait to detect whether a speaker has finished speaking or pauses for a significant period")
	cmd.Flags().StringVar(&bodyExtra, "extra", "", "Arbitrary key-value pairs that are attached to the API response for usage in downstream processing")
	cmd.Flags().BoolVar(&bodyFillerWords, "filler-words", false, "Filler Words can help transcribe interruptions in your audio, like 'uh' and 'um'")
	cmd.Flags().BoolVar(&bodyInterimResults, "interim-results", false, "Specifies whether the streaming endpoint should provide ongoing transcription updates as more audio is received.")
	cmd.Flags().StringVar(&bodyKeyterm, "keyterm", "", "Key term prompting can boost or suppress specialized terminology and brands.")
	cmd.Flags().StringVar(&bodyKeywords, "keywords", "", "Keywords can boost or suppress specialized terminology and brands.")
	cmd.Flags().StringVar(&bodyLanguage, "language", "", "The BCP-47 language tag that hints at the primary spoken language.")
	cmd.Flags().BoolVar(&bodyMeasurements, "measurements", false, "Spoken measurements will be converted to their corresponding abbreviations.")
	cmd.Flags().BoolVar(&bodyMipOptOut, "mip-opt-out", false, "Opts out requests from the Deepgram Model Improvement Program.")
	cmd.Flags().StringVar(&bodyMode, "mode", "", "Mode of operation for the model representing broad area of topic that will be talked about in the supplied audio")
	cmd.Flags().BoolVar(&bodyMultichannel, "multichannel", false, "Transcribe each audio channel independently.")
	cmd.Flags().BoolVar(&bodyNumerals, "numerals", false, "Numerals converts numbers from written format to numerical format.")
	cmd.Flags().BoolVar(&bodyParagraphs, "paragraphs", false, "Splits audio into paragraphs to improve transcript readability.")
	cmd.Flags().BoolVar(&bodyProfanityFilter, "profanity-filter", false, "Profanity Filter looks for recognized profanity and converts it to the nearest recognized non-profane word or removes")
	cmd.Flags().BoolVar(&bodyPunctuate, "punctuate", false, "Add punctuation and capitalization to the transcript.")
	cmd.Flags().StringVar(&bodyRedact, "redact", "", "Redaction removes sensitive information from your transcripts.")
	cmd.Flags().StringVar(&bodyReplace, "replace", "", "Search for terms or phrases in submitted audio and replaces them.")
	cmd.Flags().StringVar(&bodySearch, "search", "", "Search for terms or phrases in submitted audio.")
	cmd.Flags().BoolVar(&bodySentiment, "sentiment", false, "Recognizes the sentiment throughout a transcript or text.")
	cmd.Flags().BoolVar(&bodySmartFormat, "smart-format", false, "Apply formatting to transcript output.")
	cmd.Flags().BoolVar(&bodyTopics, "topics", false, "Detect topics throughout a transcript or text.")
	cmd.Flags().Float64Var(&bodyUttSplit, "utt-split", 0.0, "Seconds to wait before detecting a pause between words in submitted audio.")
	cmd.Flags().BoolVar(&bodyUtteranceEndMs, "utterance-end-ms", false, "Indicates how long model will wait to send an UtteranceEnd message after a word has been transcribed.")
	cmd.Flags().BoolVar(&bodyUtterances, "utterances", false, "Segments speech into meaningful semantic units.")
	cmd.Flags().BoolVar(&bodyVadEvents, "vad-events", false, "Indicates that speech has started. You'll begin receiving Speech Started messages upon speech starting.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
