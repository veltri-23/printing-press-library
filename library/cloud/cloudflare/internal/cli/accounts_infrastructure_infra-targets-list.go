// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsInfrastructureInfraTargetsListCmd(flags *rootFlags) *cobra.Command {
	var flagHostname string
	var flagHostnameContains string
	var flagVirtualNetworkId string
	var flagIpV4 string
	var flagIpV6 string
	var flagCreatedBefore string
	var flagCreatedAfter string
	var flagModifiedBefore string
	var flagModifiedAfter string
	var flagIps string
	var flagTargetIds string
	var flagIpLike string
	var flagIpv4Start string
	var flagIpv4End string
	var flagIpv6Start string
	var flagIpv6End string
	var flagPage string
	var flagPerPage int
	var flagOrder string
	var flagDirection string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "infra-targets-list <account_id>",
		Short:       "Lists and sorts an account’s targets. Filters are optional and are ANDed together.",
		Example:     "  cloudflare-pp-cli accounts infrastructure infra-targets-list 023e105f4ecef8ad9ca31a8372d0c353",
		Annotations: map[string]string{"pp:endpoint": "infrastructure.infra-targets-list", "pp:method": "GET", "pp:path": "/accounts/{account_id}/infrastructure/targets", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("order") {
				allowedOrder := []string{"hostname", "created_at"}
				validOrder := false
				for _, v := range allowedOrder {
					if flagOrder == v {
						validOrder = true
						break
					}
				}
				if !validOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrder, "order", allowedOrder)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/infrastructure/targets"
			path = replacePathParam(path, "account_id", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "infrastructure", path, map[string]string{
				"hostname":           formatCLIParamValue(flagHostname),
				"hostname_contains":  formatCLIParamValue(flagHostnameContains),
				"virtual_network_id": formatCLIParamValue(flagVirtualNetworkId),
				"ip_v4":              formatCLIParamValue(flagIpV4),
				"ip_v6":              formatCLIParamValue(flagIpV6),
				"created_before":     formatCLIParamValue(flagCreatedBefore),
				"created_after":      formatCLIParamValue(flagCreatedAfter),
				"modified_before":    formatCLIParamValue(flagModifiedBefore),
				"modified_after":     formatCLIParamValue(flagModifiedAfter),
				"ips":                formatCLIParamValue(flagIps),
				"target_ids":         formatCLIParamValue(flagTargetIds),
				"ip_like":            formatCLIParamValue(flagIpLike),
				"ipv4_start":         formatCLIParamValue(flagIpv4Start),
				"ipv4_end":           formatCLIParamValue(flagIpv4End),
				"ipv6_start":         formatCLIParamValue(flagIpv6Start),
				"ipv6_end":           formatCLIParamValue(flagIpv6End),
				"page":               formatCLIParamValue(flagPage),
				"per_page":           formatCLIParamValue(flagPerPage),
				"order":              formatCLIParamValue(flagOrder),
				"direction":          formatCLIParamValue(flagDirection),
			}, nil, flagAll, "page", "page", "per_page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagHostname, "hostname", "", "Hostname of a target")
	cmd.Flags().StringVar(&flagHostnameContains, "hostname-contains", "", "Partial match to the hostname of a target")
	cmd.Flags().StringVar(&flagVirtualNetworkId, "virtual-network-id", "", "Private virtual network identifier of the target")
	cmd.Flags().StringVar(&flagIpV4, "ip-v4", "", "IPv4 address of the target")
	cmd.Flags().StringVar(&flagIpV6, "ip-v6", "", "IPv6 address of the target")
	cmd.Flags().StringVar(&flagCreatedBefore, "created-before", "", "Date and time at which the target was created before (inclusive)")
	cmd.Flags().StringVar(&flagCreatedAfter, "created-after", "", "Date and time at which the target was created after (inclusive)")
	cmd.Flags().StringVar(&flagModifiedBefore, "modified-before", "", "Date and time at which the target was modified before (inclusive)")
	cmd.Flags().StringVar(&flagModifiedAfter, "modified-after", "", "Date and time at which the target was modified after (inclusive)")
	cmd.Flags().StringVar(&flagIps, "ips", "", "Filters for targets that have any of the following IP addresses.")
	cmd.Flags().StringVar(&flagTargetIds, "target-ids", "", "Filters for targets that have any of the following UUIDs.")
	cmd.Flags().StringVar(&flagIpLike, "ip-like", "", "Filters for targets whose IP addresses look like the specified string. Supports `*` as a wildcard character")
	cmd.Flags().StringVar(&flagIpv4Start, "ipv4-start", "", "Defines an IPv4 filter range's starting value (inclusive). Requires `ipv4_end` to be specified as well.")
	cmd.Flags().StringVar(&flagIpv4End, "ipv4-end", "", "Defines an IPv4 filter range's ending value (inclusive). Requires `ipv4_start` to be specified as well.")
	cmd.Flags().StringVar(&flagIpv6Start, "ipv6-start", "", "Defines an IPv6 filter range's starting value (inclusive). Requires `ipv6_end` to be specified as well.")
	cmd.Flags().StringVar(&flagIpv6End, "ipv6-end", "", "Defines an IPv6 filter range's ending value (inclusive). Requires `ipv6_start` to be specified as well.")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Current page in the response")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 1000, "Max amount of entries returned per page")
	cmd.Flags().StringVar(&flagOrder, "order", "", "The field to sort by. (one of: hostname, created_at)")
	cmd.Flags().StringVar(&flagDirection, "direction", "", "The sorting direction.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
