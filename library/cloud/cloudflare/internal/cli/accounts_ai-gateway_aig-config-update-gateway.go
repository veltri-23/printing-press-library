// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strconv"

	"github.com/spf13/cobra"
)

func newAccountsAiGatewayAigConfigUpdateGatewayCmd(flags *rootFlags) *cobra.Command {
	var bodyAuthentication bool
	var bodyCacheInvalidateOnUpdate string
	var bodyCacheTtl int
	var bodyCollectLogs string
	var bodyDlp string
	var bodyGuardrailsPromptP1 string
	var bodyGuardrailsPromptS1 string
	var bodyGuardrailsPromptS10 string
	var bodyGuardrailsPromptS11 string
	var bodyGuardrailsPromptS12 string
	var bodyGuardrailsPromptS13 string
	var bodyGuardrailsPromptS2 string
	var bodyGuardrailsPromptS3 string
	var bodyGuardrailsPromptS4 string
	var bodyGuardrailsPromptS5 string
	var bodyGuardrailsPromptS6 string
	var bodyGuardrailsPromptS7 string
	var bodyGuardrailsPromptS8 string
	var bodyGuardrailsPromptS9 string
	var bodyGuardrailsResponseP1 string
	var bodyGuardrailsResponseS1 string
	var bodyGuardrailsResponseS10 string
	var bodyGuardrailsResponseS11 string
	var bodyGuardrailsResponseS12 string
	var bodyGuardrailsResponseS13 string
	var bodyGuardrailsResponseS2 string
	var bodyGuardrailsResponseS3 string
	var bodyGuardrailsResponseS4 string
	var bodyGuardrailsResponseS5 string
	var bodyGuardrailsResponseS6 string
	var bodyGuardrailsResponseS7 string
	var bodyGuardrailsResponseS8 string
	var bodyGuardrailsResponseS9 string
	var bodyLogManagement int
	var bodyLogManagementStrategy string
	var bodyLogpush bool
	var bodyLogpushPublicKey string
	var bodyOtel string
	var bodyRateLimitingInterval int
	var bodyRateLimitingLimit int
	var bodyRateLimitingTechnique string
	var bodyRetryBackoff string
	var bodyRetryDelay int
	var bodyRetryMaxAttempts int
	var bodySpendLimitsEnabled bool
	var bodySpendLimitsRules string
	var bodyStoreId string
	var bodyStripeAuthorization string
	var bodyStripeUsageEvents string
	var bodyWorkersAiBillingMode string
	var bodyZdr bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "aig-config-update-gateway <account_id> <id>",
		Short:       "Updates an existing AI Gateway dataset.",
		Example:     "  cloudflare-pp-cli accounts ai-gateway aig-config-update-gateway 3ebbcb006d4d46d7bb6a8c7f14676cb0 my-gateway",
		Annotations: map[string]string{"pp:endpoint": "ai-gateway.aig-config-update-gateway", "pp:method": "PUT", "pp:path": "/accounts/{account_id}/ai-gateway/gateways/{id}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("cache-invalidate-on-update") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "cache-invalidate-on-update")
				}
				if !cmd.Flags().Changed("cache-ttl") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "cache-ttl")
				}
				if !cmd.Flags().Changed("collect-logs") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "collect-logs")
				}
				if !cmd.Flags().Changed("rate-limiting-interval") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "rate-limiting-interval")
				}
				if !cmd.Flags().Changed("rate-limiting-limit") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "rate-limiting-limit")
				}
				if !cmd.Flags().Changed("stripe-authorization") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "stripe-authorization")
				}
				if !cmd.Flags().Changed("stripe-usage-events") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "stripe-usage-events")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/ai-gateway/gateways/{id}"
			path = replacePathParam(path, "account_id", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("id is required\nUsage: %s <%s>", cmd.CommandPath(), "id"))
			}
			path = replacePathParam(path, "id", args[1])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("authentication") {
					body["authentication"] = bodyAuthentication
				}
				if bodyCacheInvalidateOnUpdate != "" {
					parsedCacheInvalidateOnUpdate, err := strconv.ParseBool(bodyCacheInvalidateOnUpdate)
					if err != nil {
						return fmt.Errorf("parsing --cache-invalidate-on-update as bool: %w", err)
					}
					body["cache_invalidate_on_update"] = parsedCacheInvalidateOnUpdate
				}
				if bodyCacheTtl != 0 {
					body["cache_ttl"] = bodyCacheTtl
				}
				if bodyCollectLogs != "" {
					parsedCollectLogs, err := strconv.ParseBool(bodyCollectLogs)
					if err != nil {
						return fmt.Errorf("parsing --collect-logs as bool: %w", err)
					}
					body["collect_logs"] = parsedCollectLogs
				}
				if bodyDlp != "" {
					body["dlp"] = bodyDlp
				}
				{
					nestedGuardrails := map[string]any{}
					{
						nestedGuardrailsPrompt := map[string]any{}
						if bodyGuardrailsPromptP1 != "" {
							nestedGuardrailsPrompt["P1"] = bodyGuardrailsPromptP1
						}
						if bodyGuardrailsPromptS1 != "" {
							nestedGuardrailsPrompt["S1"] = bodyGuardrailsPromptS1
						}
						if bodyGuardrailsPromptS10 != "" {
							nestedGuardrailsPrompt["S10"] = bodyGuardrailsPromptS10
						}
						if bodyGuardrailsPromptS11 != "" {
							nestedGuardrailsPrompt["S11"] = bodyGuardrailsPromptS11
						}
						if bodyGuardrailsPromptS12 != "" {
							nestedGuardrailsPrompt["S12"] = bodyGuardrailsPromptS12
						}
						if bodyGuardrailsPromptS13 != "" {
							nestedGuardrailsPrompt["S13"] = bodyGuardrailsPromptS13
						}
						if bodyGuardrailsPromptS2 != "" {
							nestedGuardrailsPrompt["S2"] = bodyGuardrailsPromptS2
						}
						if bodyGuardrailsPromptS3 != "" {
							nestedGuardrailsPrompt["S3"] = bodyGuardrailsPromptS3
						}
						if bodyGuardrailsPromptS4 != "" {
							nestedGuardrailsPrompt["S4"] = bodyGuardrailsPromptS4
						}
						if bodyGuardrailsPromptS5 != "" {
							nestedGuardrailsPrompt["S5"] = bodyGuardrailsPromptS5
						}
						if bodyGuardrailsPromptS6 != "" {
							nestedGuardrailsPrompt["S6"] = bodyGuardrailsPromptS6
						}
						if bodyGuardrailsPromptS7 != "" {
							nestedGuardrailsPrompt["S7"] = bodyGuardrailsPromptS7
						}
						if bodyGuardrailsPromptS8 != "" {
							nestedGuardrailsPrompt["S8"] = bodyGuardrailsPromptS8
						}
						if bodyGuardrailsPromptS9 != "" {
							nestedGuardrailsPrompt["S9"] = bodyGuardrailsPromptS9
						}
						if len(nestedGuardrailsPrompt) > 0 {
							nestedGuardrails["prompt"] = nestedGuardrailsPrompt
						}
					}
					{
						nestedGuardrailsResponse := map[string]any{}
						if bodyGuardrailsResponseP1 != "" {
							nestedGuardrailsResponse["P1"] = bodyGuardrailsResponseP1
						}
						if bodyGuardrailsResponseS1 != "" {
							nestedGuardrailsResponse["S1"] = bodyGuardrailsResponseS1
						}
						if bodyGuardrailsResponseS10 != "" {
							nestedGuardrailsResponse["S10"] = bodyGuardrailsResponseS10
						}
						if bodyGuardrailsResponseS11 != "" {
							nestedGuardrailsResponse["S11"] = bodyGuardrailsResponseS11
						}
						if bodyGuardrailsResponseS12 != "" {
							nestedGuardrailsResponse["S12"] = bodyGuardrailsResponseS12
						}
						if bodyGuardrailsResponseS13 != "" {
							nestedGuardrailsResponse["S13"] = bodyGuardrailsResponseS13
						}
						if bodyGuardrailsResponseS2 != "" {
							nestedGuardrailsResponse["S2"] = bodyGuardrailsResponseS2
						}
						if bodyGuardrailsResponseS3 != "" {
							nestedGuardrailsResponse["S3"] = bodyGuardrailsResponseS3
						}
						if bodyGuardrailsResponseS4 != "" {
							nestedGuardrailsResponse["S4"] = bodyGuardrailsResponseS4
						}
						if bodyGuardrailsResponseS5 != "" {
							nestedGuardrailsResponse["S5"] = bodyGuardrailsResponseS5
						}
						if bodyGuardrailsResponseS6 != "" {
							nestedGuardrailsResponse["S6"] = bodyGuardrailsResponseS6
						}
						if bodyGuardrailsResponseS7 != "" {
							nestedGuardrailsResponse["S7"] = bodyGuardrailsResponseS7
						}
						if bodyGuardrailsResponseS8 != "" {
							nestedGuardrailsResponse["S8"] = bodyGuardrailsResponseS8
						}
						if bodyGuardrailsResponseS9 != "" {
							nestedGuardrailsResponse["S9"] = bodyGuardrailsResponseS9
						}
						if len(nestedGuardrailsResponse) > 0 {
							nestedGuardrails["response"] = nestedGuardrailsResponse
						}
					}
					if len(nestedGuardrails) > 0 {
						body["guardrails"] = nestedGuardrails
					}
				}
				if bodyLogManagement != 0 {
					body["log_management"] = bodyLogManagement
				}
				if bodyLogManagementStrategy != "" {
					body["log_management_strategy"] = bodyLogManagementStrategy
				}
				if cmd.Flags().Changed("logpush") {
					body["logpush"] = bodyLogpush
				}
				if bodyLogpushPublicKey != "" {
					body["logpush_public_key"] = bodyLogpushPublicKey
				}
				if bodyOtel != "" {
					var parsedOtel any
					if err := json.Unmarshal([]byte(bodyOtel), &parsedOtel); err != nil {
						return fmt.Errorf("parsing --otel JSON: %w", err)
					}
					body["otel"] = parsedOtel
				}
				if bodyRateLimitingInterval != 0 {
					body["rate_limiting_interval"] = bodyRateLimitingInterval
				}
				if bodyRateLimitingLimit != 0 {
					body["rate_limiting_limit"] = bodyRateLimitingLimit
				}
				if bodyRateLimitingTechnique != "" {
					body["rate_limiting_technique"] = bodyRateLimitingTechnique
				}
				if bodyRetryBackoff != "" {
					body["retry_backoff"] = bodyRetryBackoff
				}
				if bodyRetryDelay != 0 {
					body["retry_delay"] = bodyRetryDelay
				}
				if bodyRetryMaxAttempts != 0 {
					body["retry_max_attempts"] = bodyRetryMaxAttempts
				}
				{
					nestedSpendLimits := map[string]any{}
					if cmd.Flags().Changed("spend-limits-enabled") {
						nestedSpendLimits["enabled"] = bodySpendLimitsEnabled
					}
					if bodySpendLimitsRules != "" {
						var parsedSpendLimitsRules any
						if err := json.Unmarshal([]byte(bodySpendLimitsRules), &parsedSpendLimitsRules); err != nil {
							return fmt.Errorf("parsing --spend-limits-rules JSON: %w", err)
						}
						nestedSpendLimits["rules"] = parsedSpendLimitsRules
					}
					if len(nestedSpendLimits) > 0 {
						body["spend_limits"] = nestedSpendLimits
					}
				}
				if bodyStoreId != "" {
					body["store_id"] = bodyStoreId
				}
				{
					nestedStripe := map[string]any{}
					if bodyStripeAuthorization != "" {
						nestedStripe["authorization"] = bodyStripeAuthorization
					}
					if bodyStripeUsageEvents != "" {
						var parsedStripeUsageEvents any
						if err := json.Unmarshal([]byte(bodyStripeUsageEvents), &parsedStripeUsageEvents); err != nil {
							return fmt.Errorf("parsing --stripe-usage-events JSON: %w", err)
						}
						nestedStripe["usage_events"] = parsedStripeUsageEvents
					}
					if len(nestedStripe) > 0 {
						body["stripe"] = nestedStripe
					}
				}
				if bodyWorkersAiBillingMode != "" {
					body["workers_ai_billing_mode"] = bodyWorkersAiBillingMode
				}
				if cmd.Flags().Changed("zdr") {
					body["zdr"] = bodyZdr
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "ai-gateway", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "ai-gateway", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai-gateway", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai-gateway", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai-gateway", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "ai-gateway",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai-gateway", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai-gateway", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAuthentication, "authentication", false, "Authentication")
	cmd.Flags().StringVar(&bodyCacheInvalidateOnUpdate, "cache-invalidate-on-update", "", "Cache invalidate on update")
	cmd.Flags().IntVar(&bodyCacheTtl, "cache-ttl", 0, "Cache ttl")
	cmd.Flags().StringVar(&bodyCollectLogs, "collect-logs", "", "Collect logs")
	cmd.Flags().StringVar(&bodyDlp, "dlp", "", "Dlp")
	cmd.Flags().StringVar(&bodyGuardrailsPromptP1, "guardrails-prompt-p1", "", "P1")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS1, "guardrails-prompt-s1", "", "S1")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS10, "guardrails-prompt-s10", "", "S10")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS11, "guardrails-prompt-s11", "", "S11")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS12, "guardrails-prompt-s12", "", "S12")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS13, "guardrails-prompt-s13", "", "S13")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS2, "guardrails-prompt-s2", "", "S2")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS3, "guardrails-prompt-s3", "", "S3")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS4, "guardrails-prompt-s4", "", "S4")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS5, "guardrails-prompt-s5", "", "S5")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS6, "guardrails-prompt-s6", "", "S6")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS7, "guardrails-prompt-s7", "", "S7")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS8, "guardrails-prompt-s8", "", "S8")
	cmd.Flags().StringVar(&bodyGuardrailsPromptS9, "guardrails-prompt-s9", "", "S9")
	cmd.Flags().StringVar(&bodyGuardrailsResponseP1, "guardrails-response-p1", "", "P1")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS1, "guardrails-response-s1", "", "S1")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS10, "guardrails-response-s10", "", "S10")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS11, "guardrails-response-s11", "", "S11")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS12, "guardrails-response-s12", "", "S12")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS13, "guardrails-response-s13", "", "S13")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS2, "guardrails-response-s2", "", "S2")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS3, "guardrails-response-s3", "", "S3")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS4, "guardrails-response-s4", "", "S4")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS5, "guardrails-response-s5", "", "S5")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS6, "guardrails-response-s6", "", "S6")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS7, "guardrails-response-s7", "", "S7")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS8, "guardrails-response-s8", "", "S8")
	cmd.Flags().StringVar(&bodyGuardrailsResponseS9, "guardrails-response-s9", "", "S9")
	cmd.Flags().IntVar(&bodyLogManagement, "log-management", 0, "Log management")
	cmd.Flags().StringVar(&bodyLogManagementStrategy, "log-management-strategy", "", "Log management strategy")
	cmd.Flags().BoolVar(&bodyLogpush, "logpush", false, "Logpush")
	cmd.Flags().StringVar(&bodyLogpushPublicKey, "logpush-public-key", "", "Logpush public key")
	cmd.Flags().StringVar(&bodyOtel, "otel", "", "Otel")
	cmd.Flags().IntVar(&bodyRateLimitingInterval, "rate-limiting-interval", 0, "Rate limiting interval")
	cmd.Flags().IntVar(&bodyRateLimitingLimit, "rate-limiting-limit", 0, "Rate limiting limit")
	cmd.Flags().StringVar(&bodyRateLimitingTechnique, "rate-limiting-technique", "", "Rate limiting technique")
	cmd.Flags().StringVar(&bodyRetryBackoff, "retry-backoff", "", "Backoff strategy for retry delays")
	cmd.Flags().IntVar(&bodyRetryDelay, "retry-delay", 0, "Delay between retry attempts in milliseconds (0-5000)")
	cmd.Flags().IntVar(&bodyRetryMaxAttempts, "retry-max-attempts", 0, "Maximum number of retry attempts for failed requests (1-5)")
	cmd.Flags().BoolVar(&bodySpendLimitsEnabled, "spend-limits-enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodySpendLimitsRules, "spend-limits-rules", "", "Rules")
	cmd.Flags().StringVar(&bodyStoreId, "store-id", "", "Store id")
	cmd.Flags().StringVar(&bodyStripeAuthorization, "stripe-authorization", "", "Authorization")
	cmd.Flags().StringVar(&bodyStripeUsageEvents, "stripe-usage-events", "", "Usage events")
	cmd.Flags().StringVar(&bodyWorkersAiBillingMode, "workers-ai-billing-mode", "postpaid", "Controls how Workers AI inference calls routed through this gateway are billed. Only 'postpaid' is currently supported.")
	cmd.Flags().BoolVar(&bodyZdr, "zdr", false, "Zdr")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
