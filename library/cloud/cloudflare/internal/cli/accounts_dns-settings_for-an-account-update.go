// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsDnsSettingsForAnAccountUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyEnforceDnsOnly bool
	var bodyZoneDefaultsFlattenAllCnames bool
	var bodyZoneDefaultsFoundationDns bool
	var bodyZoneDefaultsInternalDnsReferenceZoneId string
	var bodyZoneDefaultsMultiProvider bool
	var bodyZoneDefaultsNameserversType string
	var bodyZoneDefaultsNsTtl float64
	var bodyZoneDefaultsSecondaryOverrides bool
	var bodyZoneDefaultsSoaExpire float64
	var bodyZoneDefaultsSoaMinTtl float64
	var bodyZoneDefaultsSoaMname string
	var bodyZoneDefaultsSoaRefresh float64
	var bodyZoneDefaultsSoaRetry float64
	var bodyZoneDefaultsSoaRname string
	var bodyZoneDefaultsSoaTtl float64
	var bodyZoneDefaultsZoneMode string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "for-an-account-update <account_id>",
		Short:       "Update DNS settings for an account",
		Example:     "  cloudflare-pp-cli accounts dns-settings for-an-account-update 023e105f4ecef8ad9ca31a8372d0c353",
		Annotations: map[string]string{"pp:endpoint": "dns-settings.for-an-account-update", "pp:method": "PATCH", "pp:path": "/accounts/{account_id}/dns_settings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/dns_settings"
			path = replacePathParam(path, "account_id", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("enforce-dns-only") {
					body["enforce_dns_only"] = bodyEnforceDnsOnly
				}
				{
					nestedZoneDefaults := map[string]any{}
					if cmd.Flags().Changed("zone-defaults-flatten-all-cnames") {
						nestedZoneDefaults["flatten_all_cnames"] = bodyZoneDefaultsFlattenAllCnames
					}
					if cmd.Flags().Changed("zone-defaults-foundation-dns") {
						nestedZoneDefaults["foundation_dns"] = bodyZoneDefaultsFoundationDns
					}
					{
						nestedZoneDefaultsInternalDns := map[string]any{}
						if bodyZoneDefaultsInternalDnsReferenceZoneId != "" {
							nestedZoneDefaultsInternalDns["reference_zone_id"] = bodyZoneDefaultsInternalDnsReferenceZoneId
						}
						if len(nestedZoneDefaultsInternalDns) > 0 {
							nestedZoneDefaults["internal_dns"] = nestedZoneDefaultsInternalDns
						}
					}
					if cmd.Flags().Changed("zone-defaults-multi-provider") {
						nestedZoneDefaults["multi_provider"] = bodyZoneDefaultsMultiProvider
					}
					{
						nestedZoneDefaultsNameservers := map[string]any{}
						if bodyZoneDefaultsNameserversType != "" {
							nestedZoneDefaultsNameservers["type"] = bodyZoneDefaultsNameserversType
						}
						if len(nestedZoneDefaultsNameservers) > 0 {
							nestedZoneDefaults["nameservers"] = nestedZoneDefaultsNameservers
						}
					}
					if bodyZoneDefaultsNsTtl != 0.0 {
						nestedZoneDefaults["ns_ttl"] = bodyZoneDefaultsNsTtl
					}
					if cmd.Flags().Changed("zone-defaults-secondary-overrides") {
						nestedZoneDefaults["secondary_overrides"] = bodyZoneDefaultsSecondaryOverrides
					}
					{
						nestedZoneDefaultsSoa := map[string]any{}
						if bodyZoneDefaultsSoaExpire != 0.0 {
							nestedZoneDefaultsSoa["expire"] = bodyZoneDefaultsSoaExpire
						}
						if bodyZoneDefaultsSoaMinTtl != 0.0 {
							nestedZoneDefaultsSoa["min_ttl"] = bodyZoneDefaultsSoaMinTtl
						}
						if bodyZoneDefaultsSoaMname != "" {
							nestedZoneDefaultsSoa["mname"] = bodyZoneDefaultsSoaMname
						}
						if bodyZoneDefaultsSoaRefresh != 0.0 {
							nestedZoneDefaultsSoa["refresh"] = bodyZoneDefaultsSoaRefresh
						}
						if bodyZoneDefaultsSoaRetry != 0.0 {
							nestedZoneDefaultsSoa["retry"] = bodyZoneDefaultsSoaRetry
						}
						if bodyZoneDefaultsSoaRname != "" {
							nestedZoneDefaultsSoa["rname"] = bodyZoneDefaultsSoaRname
						}
						if bodyZoneDefaultsSoaTtl != 0.0 {
							nestedZoneDefaultsSoa["ttl"] = bodyZoneDefaultsSoaTtl
						}
						if len(nestedZoneDefaultsSoa) > 0 {
							nestedZoneDefaults["soa"] = nestedZoneDefaultsSoa
						}
					}
					if bodyZoneDefaultsZoneMode != "" {
						nestedZoneDefaults["zone_mode"] = bodyZoneDefaultsZoneMode
					}
					if len(nestedZoneDefaults) > 0 {
						body["zone_defaults"] = nestedZoneDefaults
					}
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "dns-settings", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "dns-settings", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "dns-settings", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "dns-settings", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "dns-settings", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "dns-settings",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "dns-settings", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "dns-settings", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyEnforceDnsOnly, "enforce-dns-only", false, "When enabled, forces all proxied DNS records in the account to behave as DNS-only at the edge")
	cmd.Flags().BoolVar(&bodyZoneDefaultsFlattenAllCnames, "zone-defaults-flatten-all-cnames", false, "Whether to flatten all CNAME records in the zone.")
	cmd.Flags().BoolVar(&bodyZoneDefaultsFoundationDns, "zone-defaults-foundation-dns", false, "Whether to enable Foundation DNS Advanced Nameservers on the zone.")
	cmd.Flags().StringVar(&bodyZoneDefaultsInternalDnsReferenceZoneId, "zone-defaults-internal-dns-reference-zone-id", "", "The ID of the zone to fallback to.")
	cmd.Flags().BoolVar(&bodyZoneDefaultsMultiProvider, "zone-defaults-multi-provider", false, "Whether to enable multi-provider DNS")
	cmd.Flags().StringVar(&bodyZoneDefaultsNameserversType, "zone-defaults-nameservers-type", "", "Nameserver type")
	cmd.Flags().Float64Var(&bodyZoneDefaultsNsTtl, "zone-defaults-ns-ttl", 0.0, "The time to live (TTL) of the zone's nameserver (NS) records.")
	cmd.Flags().BoolVar(&bodyZoneDefaultsSecondaryOverrides, "zone-defaults-secondary-overrides", false, "Allows a Secondary DNS zone to use (proxied) override records and CNAME flattening at the zone apex.")
	cmd.Flags().Float64Var(&bodyZoneDefaultsSoaExpire, "zone-defaults-soa-expire", 0.0, "Time in seconds of being unable to query the primary server after which secondary servers should stop serving the zone.")
	cmd.Flags().Float64Var(&bodyZoneDefaultsSoaMinTtl, "zone-defaults-soa-min-ttl", 0.0, "The time to live (TTL) for negative caching of records within the zone.")
	cmd.Flags().StringVar(&bodyZoneDefaultsSoaMname, "zone-defaults-soa-mname", "", "The primary nameserver, which may be used for outbound zone transfers.")
	cmd.Flags().Float64Var(&bodyZoneDefaultsSoaRefresh, "zone-defaults-soa-refresh", 0.0, "Time in seconds after which secondary servers should re-check the SOA record to see if the zone has been updated.")
	cmd.Flags().Float64Var(&bodyZoneDefaultsSoaRetry, "zone-defaults-soa-retry", 0.0, "Time in seconds after which secondary servers should retry queries after the primary server was unresponsive.")
	cmd.Flags().StringVar(&bodyZoneDefaultsSoaRname, "zone-defaults-soa-rname", "", "The email address of the zone administrator, with the first label representing the local part of the email address.")
	cmd.Flags().Float64Var(&bodyZoneDefaultsSoaTtl, "zone-defaults-soa-ttl", 0.0, "The time to live (TTL) of the SOA record itself.")
	cmd.Flags().StringVar(&bodyZoneDefaultsZoneMode, "zone-defaults-zone-mode", "", "Whether the zone mode is a regular or CDN/DNS only zone.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
