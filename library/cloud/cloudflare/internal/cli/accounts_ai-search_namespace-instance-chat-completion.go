// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsAiSearchNamespaceInstanceChatCompletionCmd(flags *rootFlags) *cobra.Command {
	var bodyAiSearchOptionsCacheCacheThreshold string
	var bodyAiSearchOptionsCacheEnabled bool
	var bodyAiSearchOptionsQueryRewriteEnabled bool
	var bodyAiSearchOptionsQueryRewriteModel string
	var bodyAiSearchOptionsQueryRewriteRewritePrompt string
	var bodyAiSearchOptionsRerankingEnabled bool
	var bodyAiSearchOptionsRerankingMatchThreshold float64
	var bodyAiSearchOptionsRerankingModel string
	var bodyAiSearchOptionsRetrievalBoostBy string
	var bodyAiSearchOptionsRetrievalContextExpansion int
	var bodyAiSearchOptionsRetrievalFilters string
	var bodyAiSearchOptionsRetrievalFusionMethod string
	var bodyAiSearchOptionsRetrievalKeywordMatchMode string
	var bodyAiSearchOptionsRetrievalMatchThreshold float64
	var bodyAiSearchOptionsRetrievalMaxNumResults int
	var bodyAiSearchOptionsRetrievalRetrievalType string
	var bodyAiSearchOptionsRetrievalReturnOnFailure bool
	var bodyMessages string
	var bodyModel string
	var bodyStream bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "namespace-instance-chat-completion <id> <account_id> <name>",
		Short:       "Performs a chat completion request against an AI Search instance",
		Example:     "  cloudflare-pp-cli accounts ai-search namespace-instance-chat-completion my-ai-search c3dc5f0b34a14ff8e1b3ec04895e1b22 my-namespace",
		Annotations: map[string]string{"pp:endpoint": "ai-search.namespace-instance-chat-completion", "pp:method": "POST", "pp:path": "/accounts/{account_id}/ai-search/namespaces/{name}/instances/{id}/chat/completions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("messages") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "messages")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/ai-search/namespaces/{name}/instances/{id}/chat/completions"
			path = replacePathParam(path, "id", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("account_id is required\nUsage: %s <%s>", cmd.CommandPath(), "account_id"))
			}
			path = replacePathParam(path, "account_id", args[1])
			if len(args) < 3 {
				return usageErr(fmt.Errorf("name is required\nUsage: %s <%s>", cmd.CommandPath(), "name"))
			}
			path = replacePathParam(path, "name", args[2])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAiSearchOptions := map[string]any{}
					{
						nestedAiSearchOptionsCache := map[string]any{}
						if bodyAiSearchOptionsCacheCacheThreshold != "" {
							nestedAiSearchOptionsCache["cache_threshold"] = bodyAiSearchOptionsCacheCacheThreshold
						}
						if cmd.Flags().Changed("ai-search-options-cache-enabled") {
							nestedAiSearchOptionsCache["enabled"] = bodyAiSearchOptionsCacheEnabled
						}
						if len(nestedAiSearchOptionsCache) > 0 {
							nestedAiSearchOptions["cache"] = nestedAiSearchOptionsCache
						}
					}
					{
						nestedAiSearchOptionsQueryRewrite := map[string]any{}
						if cmd.Flags().Changed("ai-search-options-query-rewrite-enabled") {
							nestedAiSearchOptionsQueryRewrite["enabled"] = bodyAiSearchOptionsQueryRewriteEnabled
						}
						if bodyAiSearchOptionsQueryRewriteModel != "" {
							nestedAiSearchOptionsQueryRewrite["model"] = bodyAiSearchOptionsQueryRewriteModel
						}
						if bodyAiSearchOptionsQueryRewriteRewritePrompt != "" {
							nestedAiSearchOptionsQueryRewrite["rewrite_prompt"] = bodyAiSearchOptionsQueryRewriteRewritePrompt
						}
						if len(nestedAiSearchOptionsQueryRewrite) > 0 {
							nestedAiSearchOptions["query_rewrite"] = nestedAiSearchOptionsQueryRewrite
						}
					}
					{
						nestedAiSearchOptionsReranking := map[string]any{}
						if cmd.Flags().Changed("ai-search-options-reranking-enabled") {
							nestedAiSearchOptionsReranking["enabled"] = bodyAiSearchOptionsRerankingEnabled
						}
						if bodyAiSearchOptionsRerankingMatchThreshold != 0.0 {
							nestedAiSearchOptionsReranking["match_threshold"] = bodyAiSearchOptionsRerankingMatchThreshold
						}
						if bodyAiSearchOptionsRerankingModel != "" {
							nestedAiSearchOptionsReranking["model"] = bodyAiSearchOptionsRerankingModel
						}
						if len(nestedAiSearchOptionsReranking) > 0 {
							nestedAiSearchOptions["reranking"] = nestedAiSearchOptionsReranking
						}
					}
					{
						nestedAiSearchOptionsRetrieval := map[string]any{}
						if bodyAiSearchOptionsRetrievalBoostBy != "" {
							var parsedAiSearchOptionsRetrievalBoostBy any
							if err := json.Unmarshal([]byte(bodyAiSearchOptionsRetrievalBoostBy), &parsedAiSearchOptionsRetrievalBoostBy); err != nil {
								return fmt.Errorf("parsing --ai-search-options-retrieval-boost-by JSON: %w", err)
							}
							nestedAiSearchOptionsRetrieval["boost_by"] = parsedAiSearchOptionsRetrievalBoostBy
						}
						if bodyAiSearchOptionsRetrievalContextExpansion != 0 {
							nestedAiSearchOptionsRetrieval["context_expansion"] = bodyAiSearchOptionsRetrievalContextExpansion
						}
						if bodyAiSearchOptionsRetrievalFilters != "" {
							var parsedAiSearchOptionsRetrievalFilters any
							if err := json.Unmarshal([]byte(bodyAiSearchOptionsRetrievalFilters), &parsedAiSearchOptionsRetrievalFilters); err != nil {
								return fmt.Errorf("parsing --ai-search-options-retrieval-filters JSON: %w", err)
							}
							nestedAiSearchOptionsRetrieval["filters"] = parsedAiSearchOptionsRetrievalFilters
						}
						if bodyAiSearchOptionsRetrievalFusionMethod != "" {
							nestedAiSearchOptionsRetrieval["fusion_method"] = bodyAiSearchOptionsRetrievalFusionMethod
						}
						if bodyAiSearchOptionsRetrievalKeywordMatchMode != "" {
							nestedAiSearchOptionsRetrieval["keyword_match_mode"] = bodyAiSearchOptionsRetrievalKeywordMatchMode
						}
						if bodyAiSearchOptionsRetrievalMatchThreshold != 0.0 {
							nestedAiSearchOptionsRetrieval["match_threshold"] = bodyAiSearchOptionsRetrievalMatchThreshold
						}
						if bodyAiSearchOptionsRetrievalMaxNumResults != 0 {
							nestedAiSearchOptionsRetrieval["max_num_results"] = bodyAiSearchOptionsRetrievalMaxNumResults
						}
						if bodyAiSearchOptionsRetrievalRetrievalType != "" {
							nestedAiSearchOptionsRetrieval["retrieval_type"] = bodyAiSearchOptionsRetrievalRetrievalType
						}
						if cmd.Flags().Changed("ai-search-options-retrieval-return-on-failure") {
							nestedAiSearchOptionsRetrieval["return_on_failure"] = bodyAiSearchOptionsRetrievalReturnOnFailure
						}
						if len(nestedAiSearchOptionsRetrieval) > 0 {
							nestedAiSearchOptions["retrieval"] = nestedAiSearchOptionsRetrieval
						}
					}
					if len(nestedAiSearchOptions) > 0 {
						body["ai_search_options"] = nestedAiSearchOptions
					}
				}
				if bodyMessages != "" {
					var parsedMessages any
					if err := json.Unmarshal([]byte(bodyMessages), &parsedMessages); err != nil {
						return fmt.Errorf("parsing --messages JSON: %w", err)
					}
					body["messages"] = parsedMessages
				}
				if bodyModel != "" {
					body["model"] = bodyModel
				}
				if cmd.Flags().Changed("stream") {
					body["stream"] = bodyStream
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "ai-search", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "ai-search", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai-search", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai-search", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai-search", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "ai-search",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai-search", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ai-search", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAiSearchOptionsCacheCacheThreshold, "ai-search-options-cache-cache-threshold", "", "Cache threshold")
	cmd.Flags().BoolVar(&bodyAiSearchOptionsCacheEnabled, "ai-search-options-cache-enabled", false, "Enabled")
	cmd.Flags().BoolVar(&bodyAiSearchOptionsQueryRewriteEnabled, "ai-search-options-query-rewrite-enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyAiSearchOptionsQueryRewriteModel, "ai-search-options-query-rewrite-model", "", "Model")
	cmd.Flags().StringVar(&bodyAiSearchOptionsQueryRewriteRewritePrompt, "ai-search-options-query-rewrite-rewrite-prompt", "", "Rewrite prompt")
	cmd.Flags().BoolVar(&bodyAiSearchOptionsRerankingEnabled, "ai-search-options-reranking-enabled", false, "Enabled")
	cmd.Flags().Float64Var(&bodyAiSearchOptionsRerankingMatchThreshold, "ai-search-options-reranking-match-threshold", 0.0, "Match threshold")
	cmd.Flags().StringVar(&bodyAiSearchOptionsRerankingModel, "ai-search-options-reranking-model", "", "Model")
	cmd.Flags().StringVar(&bodyAiSearchOptionsRetrievalBoostBy, "ai-search-options-retrieval-boost-by", "", "Metadata fields to boost search results by. Overrides the instance-level boost_by config.")
	cmd.Flags().IntVar(&bodyAiSearchOptionsRetrievalContextExpansion, "ai-search-options-retrieval-context-expansion", 0, "Context expansion")
	cmd.Flags().StringVar(&bodyAiSearchOptionsRetrievalFilters, "ai-search-options-retrieval-filters", "", "Filters")
	cmd.Flags().StringVar(&bodyAiSearchOptionsRetrievalFusionMethod, "ai-search-options-retrieval-fusion-method", "", "Fusion method")
	cmd.Flags().StringVar(&bodyAiSearchOptionsRetrievalKeywordMatchMode, "ai-search-options-retrieval-keyword-match-mode", "", "Controls which documents are candidates for BM25 scoring.")
	cmd.Flags().Float64Var(&bodyAiSearchOptionsRetrievalMatchThreshold, "ai-search-options-retrieval-match-threshold", 0.0, "Match threshold")
	cmd.Flags().IntVar(&bodyAiSearchOptionsRetrievalMaxNumResults, "ai-search-options-retrieval-max-num-results", 0, "Max num results")
	cmd.Flags().StringVar(&bodyAiSearchOptionsRetrievalRetrievalType, "ai-search-options-retrieval-retrieval-type", "", "Retrieval type")
	cmd.Flags().BoolVar(&bodyAiSearchOptionsRetrievalReturnOnFailure, "ai-search-options-retrieval-return-on-failure", false, "Return on failure")
	cmd.Flags().StringVar(&bodyMessages, "messages", "", "Messages")
	cmd.Flags().StringVar(&bodyModel, "model", "", "Model")
	cmd.Flags().BoolVar(&bodyStream, "stream", false, "Stream")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
