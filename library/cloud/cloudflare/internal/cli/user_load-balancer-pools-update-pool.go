// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/cloud/cloudflare/internal/cliutil"
	"github.com/spf13/cobra"
)

func newUserLoadBalancerPoolsUpdatePoolCmd(flags *rootFlags) *cobra.Command {
	var bodyCheckRegions string
	var bodyDescription string
	var bodyDisabledAt string
	var bodyEnabled bool
	var bodyLatitude float64
	var bodyLoadSheddingDefaultPercent float64
	var bodyLoadSheddingDefaultPolicy string
	var bodyLoadSheddingSessionPercent float64
	var bodyLoadSheddingSessionPolicy string
	var bodyLongitude float64
	var bodyMinimumOrigins int
	var bodyMonitor string
	var bodyMonitorGroup string
	var bodyName string
	var bodyNetworks string
	var bodyNotificationEmail string
	var bodyNotificationFilterOriginDisable bool
	var bodyNotificationFilterOriginHealthy bool
	var bodyNotificationFilterPoolDisable bool
	var bodyNotificationFilterPoolHealthy bool
	var bodyOriginSteeringPolicy string
	var bodyOrigins string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "load-balancer-pools-update-pool <pool_id>",
		Short:       "Modify a configured pool.",
		Example:     "  cloudflare-pp-cli user load-balancer-pools-update-pool 17b5962d775c646f3f9725cbc7a53df4 --name primary-dc-1",
		Annotations: map[string]string{"pp:endpoint": "user.load-balancer-pools-update-pool", "pp:method": "PUT", "pp:path": "/user/load_balancers/pools/{pool_id}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("origins") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "origins")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/user/load_balancers/pools/{pool_id}"
			path = replacePathParam(path, "pool_id", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCheckRegions != "" {
					body["check_regions"] = cliutil.SplitCSV(bodyCheckRegions)
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDisabledAt != "" {
					body["disabled_at"] = bodyDisabledAt
				}
				if cmd.Flags().Changed("enabled") {
					body["enabled"] = bodyEnabled
				}
				if bodyLatitude != 0.0 {
					body["latitude"] = bodyLatitude
				}
				{
					nestedLoadShedding := map[string]any{}
					if bodyLoadSheddingDefaultPercent != 0.0 {
						nestedLoadShedding["default_percent"] = bodyLoadSheddingDefaultPercent
					}
					if bodyLoadSheddingDefaultPolicy != "" {
						nestedLoadShedding["default_policy"] = bodyLoadSheddingDefaultPolicy
					}
					if bodyLoadSheddingSessionPercent != 0.0 {
						nestedLoadShedding["session_percent"] = bodyLoadSheddingSessionPercent
					}
					if bodyLoadSheddingSessionPolicy != "" {
						nestedLoadShedding["session_policy"] = bodyLoadSheddingSessionPolicy
					}
					if len(nestedLoadShedding) > 0 {
						body["load_shedding"] = nestedLoadShedding
					}
				}
				if bodyLongitude != 0.0 {
					body["longitude"] = bodyLongitude
				}
				if bodyMinimumOrigins != 0 {
					body["minimum_origins"] = bodyMinimumOrigins
				}
				if bodyMonitor != "" {
					body["monitor"] = bodyMonitor
				}
				if bodyMonitorGroup != "" {
					body["monitor_group"] = bodyMonitorGroup
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNetworks != "" {
					body["networks"] = cliutil.SplitCSV(bodyNetworks)
				}
				if bodyNotificationEmail != "" {
					body["notification_email"] = bodyNotificationEmail
				}
				{
					nestedNotificationFilter := map[string]any{}
					{
						nestedNotificationFilterOrigin := map[string]any{}
						if cmd.Flags().Changed("notification-filter-origin-disable") {
							nestedNotificationFilterOrigin["disable"] = bodyNotificationFilterOriginDisable
						}
						if cmd.Flags().Changed("notification-filter-origin-healthy") {
							nestedNotificationFilterOrigin["healthy"] = bodyNotificationFilterOriginHealthy
						}
						if len(nestedNotificationFilterOrigin) > 0 {
							nestedNotificationFilter["origin"] = nestedNotificationFilterOrigin
						}
					}
					{
						nestedNotificationFilterPool := map[string]any{}
						if cmd.Flags().Changed("notification-filter-pool-disable") {
							nestedNotificationFilterPool["disable"] = bodyNotificationFilterPoolDisable
						}
						if cmd.Flags().Changed("notification-filter-pool-healthy") {
							nestedNotificationFilterPool["healthy"] = bodyNotificationFilterPoolHealthy
						}
						if len(nestedNotificationFilterPool) > 0 {
							nestedNotificationFilter["pool"] = nestedNotificationFilterPool
						}
					}
					if len(nestedNotificationFilter) > 0 {
						body["notification_filter"] = nestedNotificationFilter
					}
				}
				{
					nestedOriginSteering := map[string]any{}
					if bodyOriginSteeringPolicy != "" {
						nestedOriginSteering["policy"] = bodyOriginSteeringPolicy
					}
					if len(nestedOriginSteering) > 0 {
						body["origin_steering"] = nestedOriginSteering
					}
				}
				if bodyOrigins != "" {
					var parsedOrigins any
					if err := json.Unmarshal([]byte(bodyOrigins), &parsedOrigins); err != nil {
						return fmt.Errorf("parsing --origins JSON: %w", err)
					}
					body["origins"] = parsedOrigins
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "user", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "user", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "user",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyCheckRegions, "check-regions", "", "A list of regions from which to run health checks. Null means every Cloudflare data center.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A human-readable description of the pool.")
	cmd.Flags().StringVar(&bodyDisabledAt, "disabled-at", "", "This field shows up only if the pool is disabled. This field is set with the time the pool was disabled at.")
	cmd.Flags().BoolVar(&bodyEnabled, "enabled", true, "Whether to enable (the default) or disable this pool.")
	cmd.Flags().Float64Var(&bodyLatitude, "latitude", 0.0, "The latitude of the data center containing the origins used in this pool in decimal degrees.")
	cmd.Flags().Float64Var(&bodyLoadSheddingDefaultPercent, "load-shedding-default-percent", 0.0, "The percent of traffic to shed from the pool, according to the default policy.")
	cmd.Flags().StringVar(&bodyLoadSheddingDefaultPolicy, "load-shedding-default-policy", "", "The default policy to use when load shedding. A random policy randomly sheds a given percent of requests.")
	cmd.Flags().Float64Var(&bodyLoadSheddingSessionPercent, "load-shedding-session-percent", 0.0, "The percent of existing sessions to shed from the pool, according to the session policy.")
	cmd.Flags().StringVar(&bodyLoadSheddingSessionPolicy, "load-shedding-session-policy", "", "Only the hash policy is supported for existing sessions (to avoid exponential decay).")
	cmd.Flags().Float64Var(&bodyLongitude, "longitude", 0.0, "The longitude of the data center containing the origins used in this pool in decimal degrees.")
	cmd.Flags().IntVar(&bodyMinimumOrigins, "minimum-origins", 1, "The minimum number of origins that must be healthy for this pool to serve traffic.")
	cmd.Flags().StringVar(&bodyMonitor, "monitor", "", "The ID of the Monitor to use for checking the health of origins within this pool.")
	cmd.Flags().StringVar(&bodyMonitorGroup, "monitor-group", "", "The ID of the Monitor Group to use for checking the health of origins within this pool.")
	cmd.Flags().StringVar(&bodyName, "name", "", "A short name (tag) for the pool. Only alphanumeric characters, hyphens, and underscores are allowed.")
	cmd.Flags().StringVar(&bodyNetworks, "networks", "", "List of networks where Load Balancer or Pool is enabled.")
	cmd.Flags().StringVar(&bodyNotificationEmail, "notification-email", "", "This field is now deprecated. It has been moved to Cloudflare's Centralized Notification service https://developers.")
	cmd.Flags().BoolVar(&bodyNotificationFilterOriginDisable, "notification-filter-origin-disable", false, "If set true, disable notifications for this type of resource (pool or origin).")
	cmd.Flags().BoolVar(&bodyNotificationFilterOriginHealthy, "notification-filter-origin-healthy", false, "If present, send notifications only for this health status (e.g. false for only DOWN events).")
	cmd.Flags().BoolVar(&bodyNotificationFilterPoolDisable, "notification-filter-pool-disable", false, "If set true, disable notifications for this type of resource (pool or origin).")
	cmd.Flags().BoolVar(&bodyNotificationFilterPoolHealthy, "notification-filter-pool-healthy", false, "If present, send notifications only for this health status (e.g. false for only DOWN events).")
	cmd.Flags().StringVar(&bodyOriginSteeringPolicy, "origin-steering-policy", "", "The type of origin steering policy to use. - `'random'`: Select an origin randomly.")
	cmd.Flags().StringVar(&bodyOrigins, "origins", "", "The list of origins within this pool.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
