// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsAccessAuthenticationLogsGetAuthenticationLogsCmd(flags *rootFlags) *cobra.Command {
	var flagLimit int
	var flagDirection string
	var flagSince string
	var flagUntil string
	var flagPage string
	var flagPerPage int
	var flagEmail string
	var flagEmailExact bool
	var flagUserId string
	var flagAllowedOp string
	var flagCountryCodeOp string
	var flagAppTypeOp string
	var flagAppUidOp string
	var flagRayIdOp string
	var flagEmailOp string
	var flagIdpOp string
	var flagNonIdentityOp string
	var flagUserIdOp string
	var flagFields string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "authentication-logs-get-authentication-logs <account_id>",
		Short:       "Gets a list of Access authentication audit logs for an account.",
		Example:     "  cloudflare-pp-cli accounts access authentication-logs-get-authentication-logs 023e105f4ecef8ad9ca31a8372d0c353",
		Annotations: map[string]string{"pp:endpoint": "access.authentication-logs-get-authentication-logs", "pp:method": "GET", "pp:path": "/accounts/{account_id}/access/logs/access_requests", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("direction") {
				allowedDirection := []string{"desc", "asc"}
				validDirection := false
				for _, v := range allowedDirection {
					if flagDirection == v {
						validDirection = true
						break
					}
				}
				if !validDirection {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagDirection, "direction", allowedDirection)
				}
			}
			if cmd.Flags().Changed("allowed-op") {
				allowedAllowedOp := []string{"eq", "neq"}
				validAllowedOp := false
				for _, v := range allowedAllowedOp {
					if flagAllowedOp == v {
						validAllowedOp = true
						break
					}
				}
				if !validAllowedOp {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAllowedOp, "allowed-op", allowedAllowedOp)
				}
			}
			if cmd.Flags().Changed("country-code-op") {
				allowedCountryCodeOp := []string{"eq", "neq"}
				validCountryCodeOp := false
				for _, v := range allowedCountryCodeOp {
					if flagCountryCodeOp == v {
						validCountryCodeOp = true
						break
					}
				}
				if !validCountryCodeOp {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagCountryCodeOp, "country-code-op", allowedCountryCodeOp)
				}
			}
			if cmd.Flags().Changed("app-type-op") {
				allowedAppTypeOp := []string{"eq", "neq"}
				validAppTypeOp := false
				for _, v := range allowedAppTypeOp {
					if flagAppTypeOp == v {
						validAppTypeOp = true
						break
					}
				}
				if !validAppTypeOp {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAppTypeOp, "app-type-op", allowedAppTypeOp)
				}
			}
			if cmd.Flags().Changed("app-uid-op") {
				allowedAppUidOp := []string{"eq", "neq"}
				validAppUidOp := false
				for _, v := range allowedAppUidOp {
					if flagAppUidOp == v {
						validAppUidOp = true
						break
					}
				}
				if !validAppUidOp {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAppUidOp, "app-uid-op", allowedAppUidOp)
				}
			}
			if cmd.Flags().Changed("ray-id-op") {
				allowedRayIdOp := []string{"eq", "neq"}
				validRayIdOp := false
				for _, v := range allowedRayIdOp {
					if flagRayIdOp == v {
						validRayIdOp = true
						break
					}
				}
				if !validRayIdOp {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagRayIdOp, "ray-id-op", allowedRayIdOp)
				}
			}
			if cmd.Flags().Changed("email-op") {
				allowedEmailOp := []string{"eq", "neq", "contains"}
				validEmailOp := false
				for _, v := range allowedEmailOp {
					if flagEmailOp == v {
						validEmailOp = true
						break
					}
				}
				if !validEmailOp {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagEmailOp, "email-op", allowedEmailOp)
				}
			}
			if cmd.Flags().Changed("idp-op") {
				allowedIdpOp := []string{"eq", "neq"}
				validIdpOp := false
				for _, v := range allowedIdpOp {
					if flagIdpOp == v {
						validIdpOp = true
						break
					}
				}
				if !validIdpOp {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagIdpOp, "idp-op", allowedIdpOp)
				}
			}
			if cmd.Flags().Changed("non-identity-op") {
				allowedNonIdentityOp := []string{"eq", "neq"}
				validNonIdentityOp := false
				for _, v := range allowedNonIdentityOp {
					if flagNonIdentityOp == v {
						validNonIdentityOp = true
						break
					}
				}
				if !validNonIdentityOp {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagNonIdentityOp, "non-identity-op", allowedNonIdentityOp)
				}
			}
			if cmd.Flags().Changed("user-id-op") {
				allowedUserIdOp := []string{"eq", "neq"}
				validUserIdOp := false
				for _, v := range allowedUserIdOp {
					if flagUserIdOp == v {
						validUserIdOp = true
						break
					}
				}
				if !validUserIdOp {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagUserIdOp, "user-id-op", allowedUserIdOp)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/access/logs/access_requests"
			path = replacePathParam(path, "account_id", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "access", path, map[string]string{
				"limit":          formatCLIParamValue(flagLimit),
				"direction":      formatCLIParamValue(flagDirection),
				"since":          formatCLIParamValue(flagSince),
				"until":          formatCLIParamValue(flagUntil),
				"page":           formatCLIParamValue(flagPage),
				"per_page":       formatCLIParamValue(flagPerPage),
				"email":          formatCLIParamValue(flagEmail),
				"email_exact":    formatCLIParamValue(flagEmailExact),
				"user_id":        formatCLIParamValue(flagUserId),
				"allowedOp":      formatCLIParamValue(flagAllowedOp),
				"country_codeOp": formatCLIParamValue(flagCountryCodeOp),
				"app_typeOp":     formatCLIParamValue(flagAppTypeOp),
				"app_uidOp":      formatCLIParamValue(flagAppUidOp),
				"ray_idOp":       formatCLIParamValue(flagRayIdOp),
				"emailOp":        formatCLIParamValue(flagEmailOp),
				"idpOp":          formatCLIParamValue(flagIdpOp),
				"non_identityOp": formatCLIParamValue(flagNonIdentityOp),
				"user_idOp":      formatCLIParamValue(flagUserIdOp),
				"fields":         formatCLIParamValue(flagFields),
			}, nil, flagAll, "page", "page", "limit", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagLimit, "limit", 25, "The maximum number of log entries to retrieve.")
	cmd.Flags().StringVar(&flagDirection, "direction", "desc", "The chronological sorting order for the logs. (one of: desc, asc)")
	cmd.Flags().StringVar(&flagSince, "since", "", "The earliest event timestamp to query.")
	cmd.Flags().StringVar(&flagUntil, "until", "", "The latest event timestamp to query.")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Page")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 25, "Per page")
	cmd.Flags().StringVar(&flagEmail, "email", "", "Filter by user email. Match mode is controlled by `emailOp` (preferred) or the legacy `email_exact` flag.")
	cmd.Flags().BoolVar(&flagEmailExact, "email-exact", false, "When true, `email` is matched exactly instead of substring matching.")
	cmd.Flags().StringVar(&flagUserId, "user-id", "", "Deprecated. Accepted for backward compatibility but no longer applied as a filter. Use `email` instead.")
	cmd.Flags().StringVar(&flagAllowedOp, "allowed-op", "eq", "Operator for the `allowed` filter. (one of: eq, neq)")
	cmd.Flags().StringVar(&flagCountryCodeOp, "country-code-op", "eq", "Operator for the `country_code` filter. (one of: eq, neq)")
	cmd.Flags().StringVar(&flagAppTypeOp, "app-type-op", "eq", "Operator for the `app_type` filter. (one of: eq, neq)")
	cmd.Flags().StringVar(&flagAppUidOp, "app-uid-op", "eq", "Operator for the `app_uid` filter. (one of: eq, neq)")
	cmd.Flags().StringVar(&flagRayIdOp, "ray-id-op", "eq", "Operator for the `ray_id` filter. (one of: eq, neq)")
	cmd.Flags().StringVar(&flagEmailOp, "email-op", "eq", "Operator for the `email` filter. `contains` performs a substring (case-sensitive) match. (one of: eq, neq, contains)")
	cmd.Flags().StringVar(&flagIdpOp, "idp-op", "eq", "Operator for the `idp` filter. (one of: eq, neq)")
	cmd.Flags().StringVar(&flagNonIdentityOp, "non-identity-op", "eq", "Operator for the `non_identity` filter. (one of: eq, neq)")
	cmd.Flags().StringVar(&flagUserIdOp, "user-id-op", "eq", "Deprecated. (one of: eq, neq)")
	cmd.Flags().StringVar(&flagFields, "fields", "", "Comma-separated list of fields to include in the response. When omitted, all fields are returned.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
