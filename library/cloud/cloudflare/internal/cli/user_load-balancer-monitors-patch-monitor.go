// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newUserLoadBalancerMonitorsPatchMonitorCmd(flags *rootFlags) *cobra.Command {
	var bodyAllowInsecure bool
	var bodyConsecutiveDown int
	var bodyConsecutiveUp int
	var bodyDescription string
	var bodyExpectedBody string
	var bodyExpectedCodes string
	var bodyFollowRedirects bool
	var bodyHeader string
	var bodyInterval int
	var bodyMethod string
	var bodyPath string
	var bodyPort int
	var bodyProbeZone string
	var bodyRetries int
	var bodyTimeout int
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "load-balancer-monitors-patch-monitor <monitor_id>",
		Short:       "Apply changes to an existing monitor, overwriting the supplied properties.",
		Example:     "  cloudflare-pp-cli user load-balancer-monitors-patch-monitor f1aba936b94213e5b8dca0c0dbf1f9cc",
		Annotations: map[string]string{"pp:endpoint": "user.load-balancer-monitors-patch-monitor", "pp:method": "PATCH", "pp:path": "/user/load_balancers/monitors/{monitor_id}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/user/load_balancers/monitors/{monitor_id}"
			path = replacePathParam(path, "monitor_id", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("allow-insecure") {
					body["allow_insecure"] = bodyAllowInsecure
				}
				if bodyConsecutiveDown != 0 {
					body["consecutive_down"] = bodyConsecutiveDown
				}
				if bodyConsecutiveUp != 0 {
					body["consecutive_up"] = bodyConsecutiveUp
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyExpectedBody != "" {
					body["expected_body"] = bodyExpectedBody
				}
				if bodyExpectedCodes != "" {
					body["expected_codes"] = bodyExpectedCodes
				}
				if cmd.Flags().Changed("follow-redirects") {
					body["follow_redirects"] = bodyFollowRedirects
				}
				if bodyHeader != "" {
					var parsedHeader any
					if err := json.Unmarshal([]byte(bodyHeader), &parsedHeader); err != nil {
						return fmt.Errorf("parsing --header JSON: %w", err)
					}
					body["header"] = parsedHeader
				}
				if bodyInterval != 0 {
					body["interval"] = bodyInterval
				}
				if bodyMethod != "" {
					body["method"] = bodyMethod
				}
				if bodyPath != "" {
					body["path"] = bodyPath
				}
				if bodyPort != 0 {
					body["port"] = bodyPort
				}
				if bodyProbeZone != "" {
					body["probe_zone"] = bodyProbeZone
				}
				if bodyRetries != 0 {
					body["retries"] = bodyRetries
				}
				if bodyTimeout != 0 {
					body["timeout"] = bodyTimeout
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "user", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "user", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "user",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAllowInsecure, "allow-insecure", false, "Do not validate the certificate when monitor use HTTPS.")
	cmd.Flags().IntVar(&bodyConsecutiveDown, "consecutive-down", 0, "To be marked unhealthy the monitored origin must fail this healthcheck N consecutive times.")
	cmd.Flags().IntVar(&bodyConsecutiveUp, "consecutive-up", 0, "To be marked healthy the monitored origin must pass this healthcheck N consecutive times.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Object description.")
	cmd.Flags().StringVar(&bodyExpectedBody, "expected-body", "", "A case-insensitive sub-string to look for in the response body.")
	cmd.Flags().StringVar(&bodyExpectedCodes, "expected-codes", "", "The expected HTTP response code or code range of the health check.")
	cmd.Flags().BoolVar(&bodyFollowRedirects, "follow-redirects", false, "Follow redirects if returned by the origin. This parameter is only valid for HTTP and HTTPS monitors.")
	cmd.Flags().StringVar(&bodyHeader, "header", "", "The HTTP request headers to send in the health check. It is recommended you set a Host header by default.")
	cmd.Flags().IntVar(&bodyInterval, "interval", 60, "The interval between each health check.")
	cmd.Flags().StringVar(&bodyMethod, "method", "", "The method to use for the health check.")
	cmd.Flags().StringVar(&bodyPath, "path", "", "The endpoint path you want to conduct a health check against. This parameter is only valid for HTTP and HTTPS monitors.")
	cmd.Flags().IntVar(&bodyPort, "port", 0, "The port number to connect to for the health check. Required for TCP, UDP, and SMTP checks.")
	cmd.Flags().StringVar(&bodyProbeZone, "probe-zone", "", "Assign this monitor to emulate the specified zone while probing.")
	cmd.Flags().IntVar(&bodyRetries, "retries", 2, "The number of retries to attempt in case of a timeout before marking the origin as unhealthy.")
	cmd.Flags().IntVar(&bodyTimeout, "timeout", 5, "The timeout (in seconds) before marking the health check as failed.")
	cmd.Flags().StringVar(&bodyType, "type", "http", "The protocol to use for the health check.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
