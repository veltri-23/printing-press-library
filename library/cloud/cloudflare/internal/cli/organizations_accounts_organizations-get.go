// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsAccountsOrganizationsGetCmd(flags *rootFlags) *cobra.Command {
	var flagAccountPubname string
	var flagAccountPubnameStartsWith string
	var flagAccountPubnameEndsWith string
	var flagAccountPubnameContains string
	var flagName string
	var flagNameStartsWith string
	var flagNameEndsWith string
	var flagNameContains string
	var flagOrderBy string
	var flagDirection string
	var flagPageToken string
	var flagPageSize int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "organizations-get <organization_id>",
		Aliases:     []string{"get"},
		Short:       "Retrieve a list of accounts that belong to a specific organization. (Currently in Public Beta - see https://developers.",
		Example:     "  cloudflare-pp-cli organizations accounts organizations-get a7b9c3d2e8f4g1h5i6j0k9l2m3n7o4p8",
		Annotations: map[string]string{"pp:endpoint": "accounts.organizations-get", "pp:method": "GET", "pp:path": "/organizations/{organization_id}/accounts", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("order-by") {
				allowedOrderBy := []string{"account_name"}
				validOrderBy := false
				for _, v := range allowedOrderBy {
					if flagOrderBy == v {
						validOrderBy = true
						break
					}
				}
				if !validOrderBy {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrderBy, "order-by", allowedOrderBy)
				}
			}
			if cmd.Flags().Changed("direction") {
				allowedDirection := []string{"asc", "desc"}
				validDirection := false
				for _, v := range allowedDirection {
					if flagDirection == v {
						validDirection = true
						break
					}
				}
				if !validDirection {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagDirection, "direction", allowedDirection)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/organizations/{organization_id}/accounts"
			path = replacePathParam(path, "organization_id", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "accounts", path, map[string]string{
				"account_pubname":            formatCLIParamValue(flagAccountPubname),
				"account_pubname.startsWith": formatCLIParamValue(flagAccountPubnameStartsWith),
				"account_pubname.endsWith":   formatCLIParamValue(flagAccountPubnameEndsWith),
				"account_pubname.contains":   formatCLIParamValue(flagAccountPubnameContains),
				"name":                       formatCLIParamValue(flagName),
				"name.startsWith":            formatCLIParamValue(flagNameStartsWith),
				"name.endsWith":              formatCLIParamValue(flagNameEndsWith),
				"name.contains":              formatCLIParamValue(flagNameContains),
				"order_by":                   formatCLIParamValue(flagOrderBy),
				"direction":                  formatCLIParamValue(flagDirection),
				"page_token":                 formatCLIParamValue(flagPageToken),
				"page_size":                  formatCLIParamValue(flagPageSize),
			}, nil, flagAll, "page_token", "page_token", "page_size", "nextPageToken", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAccountPubname, "account-pubname", "", "(case-insensitive) Filter the list of accounts to where the account_pubname is equal to a particular string.")
	cmd.Flags().StringVar(&flagAccountPubnameStartsWith, "account-pubname-starts-with", "", "(case-insensitive) Filter the list of accounts to where the account_pubname starts with a particular string.")
	cmd.Flags().StringVar(&flagAccountPubnameEndsWith, "account-pubname-ends-with", "", "(case-insensitive) Filter the list of accounts to where the account_pubname ends with a particular string.")
	cmd.Flags().StringVar(&flagAccountPubnameContains, "account-pubname-contains", "", "(case-insensitive) Filter the list of accounts to where the account_pubname contains a particular string.")
	cmd.Flags().StringVar(&flagName, "name", "", "(case-insensitive) Filter the list of accounts to where the name is equal to a particular string.")
	cmd.Flags().StringVar(&flagNameStartsWith, "name-starts-with", "", "(case-insensitive) Filter the list of accounts to where the name starts with a particular string.")
	cmd.Flags().StringVar(&flagNameEndsWith, "name-ends-with", "", "(case-insensitive) Filter the list of accounts to where the name ends with a particular string.")
	cmd.Flags().StringVar(&flagNameContains, "name-contains", "", "(case-insensitive) Filter the list of accounts to where the name contains a particular string.")
	cmd.Flags().StringVar(&flagOrderBy, "order-by", "", "Field to order results by. Currently supported values: `account_name`. (one of: account_name)")
	cmd.Flags().StringVar(&flagDirection, "direction", "", "Sort direction for the order_by field. Valid values: `asc`, `desc`. Defaults to `asc` when order_by is specified. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagPageToken, "page-token", "", "An opaque token returned from the last list response that when provided will retrieve the next page.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "The amount of items to return. Defaults to 10.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
