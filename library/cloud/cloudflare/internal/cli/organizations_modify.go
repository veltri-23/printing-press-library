// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsModifyCmd(flags *rootFlags) *cobra.Command {
	var bodyCreateTime string
	var bodyId string
	var bodyMetaFlagsAccountCreation string
	var bodyMetaFlagsAccountDeletion string
	var bodyMetaFlagsAccountMigration string
	var bodyMetaFlagsAccountMobility string
	var bodyMetaFlagsSubOrgCreation string
	var bodyMetaManagedBy string
	var bodyName string
	var bodyParentId string
	var bodyParentName string
	var bodyProfileBusinessAddress string
	var bodyProfileBusinessEmail string
	var bodyProfileBusinessName string
	var bodyProfileBusinessPhone string
	var bodyProfileExternalMetadata string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "modify <organization_id>",
		Aliases:     []string{"update"},
		Short:       "Modify organization. (Currently in Public Beta - see https://developers.cloudflare.com/fundamentals/organizations/)",
		Example:     "  cloudflare-pp-cli organizations modify a7b9c3d2e8f4g1h5i6j0k9l2m3n7o4p8 --create-time 2026-01-15T09:00:00Z",
		Annotations: map[string]string{"pp:endpoint": "organizations.modify", "pp:method": "PUT", "pp:path": "/organizations/{organization_id}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("create-time") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "create-time")
				}
				if !cmd.Flags().Changed("id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "id")
				}
				if !cmd.Flags().Changed("meta-flags-account-creation") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "meta-flags-account-creation")
				}
				if !cmd.Flags().Changed("meta-flags-account-deletion") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "meta-flags-account-deletion")
				}
				if !cmd.Flags().Changed("meta-flags-account-migration") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "meta-flags-account-migration")
				}
				if !cmd.Flags().Changed("meta-flags-account-mobility") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "meta-flags-account-mobility")
				}
				if !cmd.Flags().Changed("meta-flags-sub-org-creation") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "meta-flags-sub-org-creation")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("parent-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "parent-id")
				}
				if !cmd.Flags().Changed("parent-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "parent-name")
				}
				if !cmd.Flags().Changed("profile-business-address") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "profile-business-address")
				}
				if !cmd.Flags().Changed("profile-business-email") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "profile-business-email")
				}
				if !cmd.Flags().Changed("profile-business-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "profile-business-name")
				}
				if !cmd.Flags().Changed("profile-business-phone") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "profile-business-phone")
				}
				if !cmd.Flags().Changed("profile-external-metadata") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "profile-external-metadata")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/organizations/{organization_id}"
			path = replacePathParam(path, "organization_id", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCreateTime != "" {
					body["create_time"] = bodyCreateTime
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				{
					nestedMeta := map[string]any{}
					{
						nestedMetaFlags := map[string]any{}
						if bodyMetaFlagsAccountCreation != "" {
							nestedMetaFlags["account_creation"] = bodyMetaFlagsAccountCreation
						}
						if bodyMetaFlagsAccountDeletion != "" {
							nestedMetaFlags["account_deletion"] = bodyMetaFlagsAccountDeletion
						}
						if bodyMetaFlagsAccountMigration != "" {
							nestedMetaFlags["account_migration"] = bodyMetaFlagsAccountMigration
						}
						if bodyMetaFlagsAccountMobility != "" {
							nestedMetaFlags["account_mobility"] = bodyMetaFlagsAccountMobility
						}
						if bodyMetaFlagsSubOrgCreation != "" {
							nestedMetaFlags["sub_org_creation"] = bodyMetaFlagsSubOrgCreation
						}
						if len(nestedMetaFlags) > 0 {
							nestedMeta["flags"] = nestedMetaFlags
						}
					}
					if bodyMetaManagedBy != "" {
						nestedMeta["managed_by"] = bodyMetaManagedBy
					}
					if len(nestedMeta) > 0 {
						body["meta"] = nestedMeta
					}
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				{
					nestedParent := map[string]any{}
					if bodyParentId != "" {
						nestedParent["id"] = bodyParentId
					}
					if bodyParentName != "" {
						nestedParent["name"] = bodyParentName
					}
					if len(nestedParent) > 0 {
						body["parent"] = nestedParent
					}
				}
				{
					nestedProfile := map[string]any{}
					if bodyProfileBusinessAddress != "" {
						nestedProfile["business_address"] = bodyProfileBusinessAddress
					}
					if bodyProfileBusinessEmail != "" {
						nestedProfile["business_email"] = bodyProfileBusinessEmail
					}
					if bodyProfileBusinessName != "" {
						nestedProfile["business_name"] = bodyProfileBusinessName
					}
					if bodyProfileBusinessPhone != "" {
						nestedProfile["business_phone"] = bodyProfileBusinessPhone
					}
					if bodyProfileExternalMetadata != "" {
						nestedProfile["external_metadata"] = bodyProfileExternalMetadata
					}
					if len(nestedProfile) > 0 {
						body["profile"] = nestedProfile
					}
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "organizations", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "organizations", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "organizations", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "organizations", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "organizations", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "organizations",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "organizations", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "organizations", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyCreateTime, "create-time", "", "Create time")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().StringVar(&bodyMetaFlagsAccountCreation, "meta-flags-account-creation", "", "Account creation")
	cmd.Flags().StringVar(&bodyMetaFlagsAccountDeletion, "meta-flags-account-deletion", "", "Account deletion")
	cmd.Flags().StringVar(&bodyMetaFlagsAccountMigration, "meta-flags-account-migration", "", "Account migration")
	cmd.Flags().StringVar(&bodyMetaFlagsAccountMobility, "meta-flags-account-mobility", "", "Account mobility")
	cmd.Flags().StringVar(&bodyMetaFlagsSubOrgCreation, "meta-flags-sub-org-creation", "", "Sub org creation")
	cmd.Flags().StringVar(&bodyMetaManagedBy, "meta-managed-by", "", "Managed by")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyParentId, "parent-id", "", "Id")
	cmd.Flags().StringVar(&bodyParentName, "parent-name", "", "Name")
	cmd.Flags().StringVar(&bodyProfileBusinessAddress, "profile-business-address", "", "Business address")
	cmd.Flags().StringVar(&bodyProfileBusinessEmail, "profile-business-email", "", "Business email")
	cmd.Flags().StringVar(&bodyProfileBusinessName, "profile-business-name", "", "Business name")
	cmd.Flags().StringVar(&bodyProfileBusinessPhone, "profile-business-phone", "", "Business phone")
	cmd.Flags().StringVar(&bodyProfileExternalMetadata, "profile-external-metadata", "", "External metadata")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
