// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsRequestTracerAccountRequestTraceCmd(flags *rootFlags) *cobra.Command {
	var bodyBodyBase64 string
	var bodyBodyJson string
	var bodyBodyPlainText string
	var bodyContextBotScore int
	var bodyContextGeolocCity string
	var bodyContextGeolocContinent string
	var bodyContextGeolocIsEuCountry bool
	var bodyContextGeolocIsoCode string
	var bodyContextGeolocLatitude float64
	var bodyContextGeolocLongitude float64
	var bodyContextGeolocPostalCode string
	var bodyContextGeolocRegionCode string
	var bodyContextGeolocSubdivision2IsoCode string
	var bodyContextGeolocTimezone string
	var bodyContextSkipChallenge bool
	var bodyContextThreatScore int
	var bodyCookies string
	var bodyHeaders string
	var bodyMethod string
	var bodyProtocol string
	var bodySkipResponse bool
	var bodyUrl string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "account-request-trace <account_id>",
		Aliases:     []string{"create"},
		Short:       "Request Trace",
		Example:     "  cloudflare-pp-cli accounts request-tracer account-request-trace 023e105f4ecef8ad9ca31a8372d0c353 --method PUT",
		Annotations: map[string]string{"pp:endpoint": "request-tracer.account-request-trace", "pp:method": "POST", "pp:path": "/accounts/{account_id}/request-tracer/trace"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("method") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "method")
				}
				if !cmd.Flags().Changed("url") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "url")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/request-tracer/trace"
			path = replacePathParam(path, "account_id", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedBody := map[string]any{}
					if bodyBodyBase64 != "" {
						nestedBody["base64"] = bodyBodyBase64
					}
					if bodyBodyJson != "" {
						var parsedBodyJson any
						if err := json.Unmarshal([]byte(bodyBodyJson), &parsedBodyJson); err != nil {
							return fmt.Errorf("parsing --body-json JSON: %w", err)
						}
						nestedBody["json"] = parsedBodyJson
					}
					if bodyBodyPlainText != "" {
						nestedBody["plain_text"] = bodyBodyPlainText
					}
					if len(nestedBody) > 0 {
						body["body"] = nestedBody
					}
				}
				{
					nestedContext := map[string]any{}
					if bodyContextBotScore != 0 {
						nestedContext["bot_score"] = bodyContextBotScore
					}
					{
						nestedContextGeoloc := map[string]any{}
						if bodyContextGeolocCity != "" {
							nestedContextGeoloc["city"] = bodyContextGeolocCity
						}
						if bodyContextGeolocContinent != "" {
							nestedContextGeoloc["continent"] = bodyContextGeolocContinent
						}
						if cmd.Flags().Changed("context-geoloc-is-eu-country") {
							nestedContextGeoloc["is_eu_country"] = bodyContextGeolocIsEuCountry
						}
						if bodyContextGeolocIsoCode != "" {
							nestedContextGeoloc["iso_code"] = bodyContextGeolocIsoCode
						}
						if bodyContextGeolocLatitude != 0.0 {
							nestedContextGeoloc["latitude"] = bodyContextGeolocLatitude
						}
						if bodyContextGeolocLongitude != 0.0 {
							nestedContextGeoloc["longitude"] = bodyContextGeolocLongitude
						}
						if bodyContextGeolocPostalCode != "" {
							nestedContextGeoloc["postal_code"] = bodyContextGeolocPostalCode
						}
						if bodyContextGeolocRegionCode != "" {
							nestedContextGeoloc["region_code"] = bodyContextGeolocRegionCode
						}
						if bodyContextGeolocSubdivision2IsoCode != "" {
							nestedContextGeoloc["subdivision_2_iso_code"] = bodyContextGeolocSubdivision2IsoCode
						}
						if bodyContextGeolocTimezone != "" {
							nestedContextGeoloc["timezone"] = bodyContextGeolocTimezone
						}
						if len(nestedContextGeoloc) > 0 {
							nestedContext["geoloc"] = nestedContextGeoloc
						}
					}
					if cmd.Flags().Changed("context-skip-challenge") {
						nestedContext["skip_challenge"] = bodyContextSkipChallenge
					}
					if bodyContextThreatScore != 0 {
						nestedContext["threat_score"] = bodyContextThreatScore
					}
					if len(nestedContext) > 0 {
						body["context"] = nestedContext
					}
				}
				if bodyCookies != "" {
					var parsedCookies any
					if err := json.Unmarshal([]byte(bodyCookies), &parsedCookies); err != nil {
						return fmt.Errorf("parsing --cookies JSON: %w", err)
					}
					body["cookies"] = parsedCookies
				}
				if bodyHeaders != "" {
					var parsedHeaders any
					if err := json.Unmarshal([]byte(bodyHeaders), &parsedHeaders); err != nil {
						return fmt.Errorf("parsing --headers JSON: %w", err)
					}
					body["headers"] = parsedHeaders
				}
				if bodyMethod != "" {
					body["method"] = bodyMethod
				}
				if bodyProtocol != "" {
					body["protocol"] = bodyProtocol
				}
				if cmd.Flags().Changed("skip-response") {
					body["skip_response"] = bodySkipResponse
				}
				if bodyUrl != "" {
					body["url"] = bodyUrl
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "request-tracer", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "request-tracer", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "request-tracer", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "request-tracer", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "request-tracer", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "request-tracer",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "request-tracer", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "request-tracer", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyBodyBase64, "body-base64", "", "Base64 encoded request body")
	cmd.Flags().StringVar(&bodyBodyJson, "body-json", "", "Arbitrary json as request body")
	cmd.Flags().StringVar(&bodyBodyPlainText, "body-plain-text", "", "Request body as plain text")
	cmd.Flags().IntVar(&bodyContextBotScore, "context-bot-score", 0, "Bot score used for evaluating tracing request processing")
	cmd.Flags().StringVar(&bodyContextGeolocCity, "context-geoloc-city", "", "City")
	cmd.Flags().StringVar(&bodyContextGeolocContinent, "context-geoloc-continent", "", "Continent")
	cmd.Flags().BoolVar(&bodyContextGeolocIsEuCountry, "context-geoloc-is-eu-country", false, "Is eu country")
	cmd.Flags().StringVar(&bodyContextGeolocIsoCode, "context-geoloc-iso-code", "", "Iso code")
	cmd.Flags().Float64Var(&bodyContextGeolocLatitude, "context-geoloc-latitude", 0.0, "Latitude")
	cmd.Flags().Float64Var(&bodyContextGeolocLongitude, "context-geoloc-longitude", 0.0, "Longitude")
	cmd.Flags().StringVar(&bodyContextGeolocPostalCode, "context-geoloc-postal-code", "", "Postal code")
	cmd.Flags().StringVar(&bodyContextGeolocRegionCode, "context-geoloc-region-code", "", "Region code")
	cmd.Flags().StringVar(&bodyContextGeolocSubdivision2IsoCode, "context-geoloc-subdivision-2-iso-code", "", "Subdivision 2 iso code")
	cmd.Flags().StringVar(&bodyContextGeolocTimezone, "context-geoloc-timezone", "", "Timezone")
	cmd.Flags().BoolVar(&bodyContextSkipChallenge, "context-skip-challenge", false, "Whether to skip any challenges for tracing request (e.g.: captcha)")
	cmd.Flags().IntVar(&bodyContextThreatScore, "context-threat-score", 0, "Threat score used for evaluating tracing request processing")
	cmd.Flags().StringVar(&bodyCookies, "cookies", "", "Cookies added to tracing request")
	cmd.Flags().StringVar(&bodyHeaders, "headers", "", "Headers added to tracing request")
	cmd.Flags().StringVar(&bodyMethod, "method", "", "HTTP Method of tracing request")
	cmd.Flags().StringVar(&bodyProtocol, "protocol", "", "HTTP Protocol of tracing request")
	cmd.Flags().BoolVar(&bodySkipResponse, "skip-response", false, "Skip sending the request to the Origin server after all rules evaluation")
	cmd.Flags().StringVar(&bodyUrl, "url", "", "URL to which perform tracing request")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
