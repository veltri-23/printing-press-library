// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsAiWorkersPostRunCfRunwaymlStableDiffusionV15Img2imgCmd(flags *rootFlags) *cobra.Command {
	var flagQueueRequest string
	var flagTags string
	var bodyGuidance float64
	var bodyHeight int
	var bodyImage string
	var bodyImageB64 string
	var bodyMask string
	var bodyNegativePrompt string
	var bodyNumSteps int
	var bodyPrompt string
	var bodySeed int
	var bodyStrength float64
	var bodyWidth int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "workers-post-run-cf-runwayml-stable-diffusion-v1-5-img2img <account_id>",
		Short: "Runs inference on the @cf/runwayml/stable-diffusion-v1-5-img2img model.",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  cloudflare-pp-cli accounts ai workers-post-run-cf-runwayml-stable-diffusion-v1-5-img2img 023e105f4ecef8ad9ca31a8372d0c353 --prompt example-value",
		Annotations: map[string]string{"pp:endpoint": "ai.workers-post-run-cf-runwayml-stable-diffusion-v1-5-img2img", "pp:method": "POST", "pp:path": "/accounts/{account_id}/ai/run/@cf/runwayml/stable-diffusion-v1-5-img2img"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("prompt") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "prompt")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/ai/run/@cf/runwayml/stable-diffusion-v1-5-img2img"
			path = replacePathParam(path, "account_id", args[0])
			headerOverrides := map[string]string{
				"X-Printing-Press-Binary-Response": "true",
			}
			params := map[string]string{}
			if flagQueueRequest != "" {
				params["queueRequest"] = formatCLIParamValue(flagQueueRequest)
			}
			if flagTags != "" {
				params["tags"] = formatCLIParamValue(flagTags)
			}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyGuidance != 0.0 {
					body["guidance"] = bodyGuidance
				}
				if bodyHeight != 0 {
					body["height"] = bodyHeight
				}
				if bodyImage != "" {
					var parsedImage any
					if err := json.Unmarshal([]byte(bodyImage), &parsedImage); err != nil {
						return fmt.Errorf("parsing --image JSON: %w", err)
					}
					body["image"] = parsedImage
				}
				if bodyImageB64 != "" {
					body["image_b64"] = bodyImageB64
				}
				if bodyMask != "" {
					var parsedMask any
					if err := json.Unmarshal([]byte(bodyMask), &parsedMask); err != nil {
						return fmt.Errorf("parsing --mask JSON: %w", err)
					}
					body["mask"] = parsedMask
				}
				if bodyNegativePrompt != "" {
					body["negative_prompt"] = bodyNegativePrompt
				}
				if bodyNumSteps != 0 {
					body["num_steps"] = bodyNumSteps
				}
				if bodyPrompt != "" {
					body["prompt"] = bodyPrompt
				}
				if bodySeed != 0 {
					body["seed"] = bodySeed
				}
				if bodyStrength != 0.0 {
					body["strength"] = bodyStrength
				}
				if bodyWidth != 0 {
					body["width"] = bodyWidth
				}
			}
			data, statusCode, err := c.PostWithParamsAndHeaders(cmd.Context(), path, params, body, headerOverrides)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = statusCode
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>")
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().StringVar(&flagQueueRequest, "queue-request", "", "Queue request")
	cmd.Flags().StringVar(&flagTags, "tags", "", "Tags")
	cmd.Flags().Float64Var(&bodyGuidance, "guidance", 7.500000, "Controls how closely the generated image should adhere to the prompt")
	cmd.Flags().IntVar(&bodyHeight, "height", 0, "The height of the generated image in pixels")
	cmd.Flags().StringVar(&bodyImage, "image", "", "For use with img2img tasks.")
	cmd.Flags().StringVar(&bodyImageB64, "image-b64", "", "For use with img2img tasks. A base64-encoded string of the input image")
	cmd.Flags().StringVar(&bodyMask, "mask", "", "An array representing An array of integers that represent mask image data for inpainting constrained to 8-bit unsigned")
	cmd.Flags().StringVar(&bodyNegativePrompt, "negative-prompt", "", "Text describing elements to avoid in the generated image")
	cmd.Flags().IntVar(&bodyNumSteps, "num-steps", 20, "The number of diffusion steps; higher values can improve quality but take longer")
	cmd.Flags().StringVar(&bodyPrompt, "prompt", "", "A text description of the image you want to generate")
	cmd.Flags().IntVar(&bodySeed, "seed", 0, "Random seed for reproducibility of the image generation")
	cmd.Flags().Float64Var(&bodyStrength, "strength", 1.000000, "A value between 0 and 1 indicating how strongly to apply the transformation during img2img tasks")
	cmd.Flags().IntVar(&bodyWidth, "width", 0, "The width of the generated image in pixels")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
