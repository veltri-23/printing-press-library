// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/cloud/cloudflare/internal/cliutil"
	"github.com/spf13/cobra"
)

func newZonesFraudDetectionZoneUpdateSettingsCmd(flags *rootFlags) *cobra.Command {
	var bodyAuthenticationSettingsFailureCriteriaKind string
	var bodyAuthenticationSettingsFailureCriteriaStatusCodes string
	var bodyAuthenticationSettingsSuccessCriteriaKind string
	var bodyAuthenticationSettingsSuccessCriteriaStatusCodes string
	var bodyUserProfiles string
	var bodyUsernameExpressions string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "zone-update-settings <zone_id>",
		Aliases:     []string{"update"},
		Short:       "Update Fraud Detection settings for a zone.",
		Example:     "  cloudflare-pp-cli zones fraud-detection zone-update-settings 023e105f4ecef8ad9ca31a8372d0c353",
		Annotations: map[string]string{"pp:endpoint": "fraud-detection.zone-update-settings", "pp:method": "PUT", "pp:path": "/zones/{zone_id}/fraud_detection/settings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("authentication-settings-failure-criteria-kind") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "authentication-settings-failure-criteria-kind")
				}
				if !cmd.Flags().Changed("authentication-settings-success-criteria-kind") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "authentication-settings-success-criteria-kind")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/zones/{zone_id}/fraud_detection/settings"
			path = replacePathParam(path, "zone_id", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAuthenticationSettings := map[string]any{}
					{
						nestedAuthenticationSettingsFailureCriteria := map[string]any{}
						if bodyAuthenticationSettingsFailureCriteriaKind != "" {
							nestedAuthenticationSettingsFailureCriteria["kind"] = bodyAuthenticationSettingsFailureCriteriaKind
						}
						if bodyAuthenticationSettingsFailureCriteriaStatusCodes != "" {
							var parsedAuthenticationSettingsFailureCriteriaStatusCodes any
							if err := json.Unmarshal([]byte(bodyAuthenticationSettingsFailureCriteriaStatusCodes), &parsedAuthenticationSettingsFailureCriteriaStatusCodes); err != nil {
								return fmt.Errorf("parsing --authentication-settings-failure-criteria-status-codes JSON: %w", err)
							}
							nestedAuthenticationSettingsFailureCriteria["status_codes"] = parsedAuthenticationSettingsFailureCriteriaStatusCodes
						}
						if len(nestedAuthenticationSettingsFailureCriteria) > 0 {
							nestedAuthenticationSettings["failure_criteria"] = nestedAuthenticationSettingsFailureCriteria
						}
					}
					{
						nestedAuthenticationSettingsSuccessCriteria := map[string]any{}
						if bodyAuthenticationSettingsSuccessCriteriaKind != "" {
							nestedAuthenticationSettingsSuccessCriteria["kind"] = bodyAuthenticationSettingsSuccessCriteriaKind
						}
						if bodyAuthenticationSettingsSuccessCriteriaStatusCodes != "" {
							var parsedAuthenticationSettingsSuccessCriteriaStatusCodes any
							if err := json.Unmarshal([]byte(bodyAuthenticationSettingsSuccessCriteriaStatusCodes), &parsedAuthenticationSettingsSuccessCriteriaStatusCodes); err != nil {
								return fmt.Errorf("parsing --authentication-settings-success-criteria-status-codes JSON: %w", err)
							}
							nestedAuthenticationSettingsSuccessCriteria["status_codes"] = parsedAuthenticationSettingsSuccessCriteriaStatusCodes
						}
						if len(nestedAuthenticationSettingsSuccessCriteria) > 0 {
							nestedAuthenticationSettings["success_criteria"] = nestedAuthenticationSettingsSuccessCriteria
						}
					}
					if len(nestedAuthenticationSettings) > 0 {
						body["authentication_settings"] = nestedAuthenticationSettings
					}
				}
				if bodyUserProfiles != "" {
					body["user_profiles"] = bodyUserProfiles
				}
				if bodyUsernameExpressions != "" {
					body["username_expressions"] = cliutil.SplitCSV(bodyUsernameExpressions)
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "fraud-detection", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "fraud-detection", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "fraud-detection", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "fraud-detection", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "fraud-detection", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "fraud-detection",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "fraud-detection", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "fraud-detection", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAuthenticationSettingsFailureCriteriaKind, "authentication-settings-failure-criteria-kind", "", "The type of criterion. Currently only `status_code` is supported.")
	cmd.Flags().StringVar(&bodyAuthenticationSettingsFailureCriteriaStatusCodes, "authentication-settings-failure-criteria-status-codes", "", "HTTP status codes to match against the origin response. - Maximum of 10 codes per criterion.")
	cmd.Flags().StringVar(&bodyAuthenticationSettingsSuccessCriteriaKind, "authentication-settings-success-criteria-kind", "", "The type of criterion. Currently only `status_code` is supported.")
	cmd.Flags().StringVar(&bodyAuthenticationSettingsSuccessCriteriaStatusCodes, "authentication-settings-success-criteria-status-codes", "", "HTTP status codes to match against the origin response. - Maximum of 10 codes per criterion.")
	cmd.Flags().StringVar(&bodyUserProfiles, "user-profiles", "", "Whether Fraud User Profiles is enabled for the zone.")
	cmd.Flags().StringVar(&bodyUsernameExpressions, "username-expressions", "", "List of expressions to detect usernames in write HTTP requests. - Maximum of 10 expressions.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
