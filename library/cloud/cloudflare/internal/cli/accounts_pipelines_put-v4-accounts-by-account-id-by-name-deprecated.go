// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsPipelinesPutV4AccountsByAccountIdByNameDeprecatedCmd(flags *rootFlags) *cobra.Command {
	var bodyDestinationBatchMaxBytes int
	var bodyDestinationBatchMaxDurationS float64
	var bodyDestinationBatchMaxRows int
	var bodyDestinationCompressionType string
	var bodyDestinationCredentialsAccessKeyId string
	var bodyDestinationCredentialsEndpoint string
	var bodyDestinationCredentialsSecretAccessKey string
	var bodyDestinationFormat string
	var bodyDestinationPathBucket string
	var bodyDestinationPathFilename string
	var bodyDestinationPathFilepath string
	var bodyDestinationPathPrefix string
	var bodyDestinationType string
	var bodyName string
	var bodySource string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "put-v4-accounts-by-account-id-by-name-deprecated <account_id> <pipeline_name>",
		Short:       "[DEPRECATED] Update an existing pipeline. Use the new /pipelines/v1/pipelines endpoint instead.",
		Example:     "  cloudflare-pp-cli accounts pipelines put-v4-accounts-by-account-id-by-name-deprecated 0123105f4ecef8ad9ca31a8372d0c353 sample_pipeline --name sample_pipeline",
		Annotations: map[string]string{"pp:endpoint": "pipelines.put-v4-accounts-by-account-id-by-name-deprecated", "pp:method": "PUT", "pp:path": "/accounts/{account_id}/pipelines/{pipeline_name}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("destination-credentials-access-key-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "destination-credentials-access-key-id")
				}
				if !cmd.Flags().Changed("destination-credentials-endpoint") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "destination-credentials-endpoint")
				}
				if !cmd.Flags().Changed("destination-credentials-secret-access-key") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "destination-credentials-secret-access-key")
				}
				if !cmd.Flags().Changed("destination-format") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "destination-format")
				}
				if !cmd.Flags().Changed("destination-path-bucket") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "destination-path-bucket")
				}
				if !cmd.Flags().Changed("destination-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "destination-type")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("source") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/pipelines/{pipeline_name}"
			path = replacePathParam(path, "account_id", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("pipeline_name is required\nUsage: %s <%s>", cmd.CommandPath(), "pipeline_name"))
			}
			path = replacePathParam(path, "pipeline_name", args[1])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedDestination := map[string]any{}
					{
						nestedDestinationBatch := map[string]any{}
						if bodyDestinationBatchMaxBytes != 0 {
							nestedDestinationBatch["max_bytes"] = bodyDestinationBatchMaxBytes
						}
						if bodyDestinationBatchMaxDurationS != 0.0 {
							nestedDestinationBatch["max_duration_s"] = bodyDestinationBatchMaxDurationS
						}
						if bodyDestinationBatchMaxRows != 0 {
							nestedDestinationBatch["max_rows"] = bodyDestinationBatchMaxRows
						}
						if len(nestedDestinationBatch) > 0 {
							nestedDestination["batch"] = nestedDestinationBatch
						}
					}
					{
						nestedDestinationCompression := map[string]any{}
						if bodyDestinationCompressionType != "" {
							nestedDestinationCompression["type"] = bodyDestinationCompressionType
						}
						if len(nestedDestinationCompression) > 0 {
							nestedDestination["compression"] = nestedDestinationCompression
						}
					}
					{
						nestedDestinationCredentials := map[string]any{}
						if bodyDestinationCredentialsAccessKeyId != "" {
							nestedDestinationCredentials["access_key_id"] = bodyDestinationCredentialsAccessKeyId
						}
						if bodyDestinationCredentialsEndpoint != "" {
							nestedDestinationCredentials["endpoint"] = bodyDestinationCredentialsEndpoint
						}
						if bodyDestinationCredentialsSecretAccessKey != "" {
							nestedDestinationCredentials["secret_access_key"] = bodyDestinationCredentialsSecretAccessKey
						}
						if len(nestedDestinationCredentials) > 0 {
							nestedDestination["credentials"] = nestedDestinationCredentials
						}
					}
					if bodyDestinationFormat != "" {
						nestedDestination["format"] = bodyDestinationFormat
					}
					{
						nestedDestinationPath := map[string]any{}
						if bodyDestinationPathBucket != "" {
							nestedDestinationPath["bucket"] = bodyDestinationPathBucket
						}
						if bodyDestinationPathFilename != "" {
							nestedDestinationPath["filename"] = bodyDestinationPathFilename
						}
						if bodyDestinationPathFilepath != "" {
							nestedDestinationPath["filepath"] = bodyDestinationPathFilepath
						}
						if bodyDestinationPathPrefix != "" {
							nestedDestinationPath["prefix"] = bodyDestinationPathPrefix
						}
						if len(nestedDestinationPath) > 0 {
							nestedDestination["path"] = nestedDestinationPath
						}
					}
					if bodyDestinationType != "" {
						nestedDestination["type"] = bodyDestinationType
					}
					if len(nestedDestination) > 0 {
						body["destination"] = nestedDestination
					}
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodySource != "" {
					var parsedSource any
					if err := json.Unmarshal([]byte(bodySource), &parsedSource); err != nil {
						return fmt.Errorf("parsing --source JSON: %w", err)
					}
					body["source"] = parsedSource
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "pipelines", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "pipelines", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "pipelines", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "pipelines", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "pipelines", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "pipelines",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "pipelines", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "pipelines", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().IntVar(&bodyDestinationBatchMaxBytes, "destination-batch-max-bytes", 0, "Specifies rough maximum size of files.")
	cmd.Flags().Float64Var(&bodyDestinationBatchMaxDurationS, "destination-batch-max-duration-s", 0.0, "Specifies duration to wait to aggregate batches files.")
	cmd.Flags().IntVar(&bodyDestinationBatchMaxRows, "destination-batch-max-rows", 0, "Specifies rough maximum number of rows per file.")
	cmd.Flags().StringVar(&bodyDestinationCompressionType, "destination-compression-type", "", "Specifies the desired compression algorithm and format.")
	cmd.Flags().StringVar(&bodyDestinationCredentialsAccessKeyId, "destination-credentials-access-key-id", "", "Specifies the R2 Bucket Access Key Id.")
	cmd.Flags().StringVar(&bodyDestinationCredentialsEndpoint, "destination-credentials-endpoint", "", "Specifies the R2 Endpoint.")
	cmd.Flags().StringVar(&bodyDestinationCredentialsSecretAccessKey, "destination-credentials-secret-access-key", "", "Specifies the R2 Bucket Secret Access Key.")
	cmd.Flags().StringVar(&bodyDestinationFormat, "destination-format", "", "Specifies the format of data to deliver.")
	cmd.Flags().StringVar(&bodyDestinationPathBucket, "destination-path-bucket", "", "Specifies the R2 Bucket to store files.")
	cmd.Flags().StringVar(&bodyDestinationPathFilename, "destination-path-filename", "", "Specifies the name pattern to for individual data files.")
	cmd.Flags().StringVar(&bodyDestinationPathFilepath, "destination-path-filepath", "", "Specifies the name pattern for directory.")
	cmd.Flags().StringVar(&bodyDestinationPathPrefix, "destination-path-prefix", "", "Specifies the base directory within the bucket.")
	cmd.Flags().StringVar(&bodyDestinationType, "destination-type", "", "Specifies the type of destination.")
	cmd.Flags().StringVar(&bodyName, "name", "", "Defines the name of the pipeline.")
	cmd.Flags().StringVar(&bodySource, "source", "", "Source")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
