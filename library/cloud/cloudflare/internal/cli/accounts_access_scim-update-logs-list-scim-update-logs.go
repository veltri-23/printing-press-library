// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsAccessScimUpdateLogsListScimUpdateLogsCmd(flags *rootFlags) *cobra.Command {
	var flagLimit int
	var flagDirection string
	var flagSince string
	var flagUntil string
	var flagIdpId string
	var flagStatus string
	var flagResourceType string
	var flagRequestMethod string
	var flagResourceUserEmail string
	var flagResourceGroupName string
	var flagCfResourceId string
	var flagIdpResourceId string
	var flagPage string
	var flagPerPage int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "scim-update-logs-list-scim-update-logs <account_id>",
		Short:       "Lists Access SCIM update logs that maintain a record of updates made to User and Group resources synced to Cloudflare",
		Example:     "  cloudflare-pp-cli accounts access scim-update-logs-list-scim-update-logs 023e105f4ecef8ad9ca31a8372d0c353 --idp-id df7e2w5f-02b7-4d9d-af26-8d1988fca630",
		Annotations: map[string]string{"pp:endpoint": "access.scim-update-logs-list-scim-update-logs", "pp:method": "GET", "pp:path": "/accounts/{account_id}/access/logs/scim/updates", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("idp-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "idp-id")
			}
			if cmd.Flags().Changed("direction") {
				allowedDirection := []string{"desc", "asc"}
				validDirection := false
				for _, v := range allowedDirection {
					if flagDirection == v {
						validDirection = true
						break
					}
				}
				if !validDirection {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagDirection, "direction", allowedDirection)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/access/logs/scim/updates"
			path = replacePathParam(path, "account_id", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "access", path, map[string]string{
				"limit":               formatCLIParamValue(flagLimit),
				"direction":           formatCLIParamValue(flagDirection),
				"since":               formatCLIParamValue(flagSince),
				"until":               formatCLIParamValue(flagUntil),
				"idp_id":              formatCLIParamValue(flagIdpId),
				"status":              formatCLIParamValue(flagStatus),
				"resource_type":       formatCLIParamValue(flagResourceType),
				"request_method":      formatCLIParamValue(flagRequestMethod),
				"resource_user_email": formatCLIParamValue(flagResourceUserEmail),
				"resource_group_name": formatCLIParamValue(flagResourceGroupName),
				"cf_resource_id":      formatCLIParamValue(flagCfResourceId),
				"idp_resource_id":     formatCLIParamValue(flagIdpResourceId),
				"page":                formatCLIParamValue(flagPage),
				"per_page":            formatCLIParamValue(flagPerPage),
			}, nil, flagAll, "page", "page", "limit", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagLimit, "limit", 20, "Limit")
	cmd.Flags().StringVar(&flagDirection, "direction", "desc", "Direction (one of: desc, asc)")
	cmd.Flags().StringVar(&flagSince, "since", "", "Since")
	cmd.Flags().StringVar(&flagUntil, "until", "", "Until")
	cmd.Flags().StringVar(&flagIdpId, "idp-id", "", "Idp id")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Status")
	cmd.Flags().StringVar(&flagResourceType, "resource-type", "", "Resource type")
	cmd.Flags().StringVar(&flagRequestMethod, "request-method", "", "Request method")
	cmd.Flags().StringVar(&flagResourceUserEmail, "resource-user-email", "", "Resource user email")
	cmd.Flags().StringVar(&flagResourceGroupName, "resource-group-name", "", "Resource group name")
	cmd.Flags().StringVar(&flagCfResourceId, "cf-resource-id", "", "Cf resource id")
	cmd.Flags().StringVar(&flagIdpResourceId, "idp-resource-id", "", "Idp resource id")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Page")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 20, "Per page")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
