// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsAiWorkersPostRunCfDeepgramAura2EsCmd(flags *rootFlags) *cobra.Command {
	var flagQueueRequest string
	var flagTags string
	var bodyBitRate float64
	var bodyContainer string
	var bodyEncoding string
	var bodySampleRate float64
	var bodySpeaker string
	var bodyText string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "workers-post-run-cf-deepgram-aura-2-es <account_id>",
		Short:       "Runs inference on the @cf/deepgram/aura-2-es model.",
		Example:     "  cloudflare-pp-cli accounts ai workers-post-run-cf-deepgram-aura-2-es 023e105f4ecef8ad9ca31a8372d0c353",
		Annotations: map[string]string{"pp:endpoint": "ai.workers-post-run-cf-deepgram-aura-2-es", "pp:method": "POST", "pp:path": "/accounts/{account_id}/ai/run/@cf/deepgram/aura-2-es"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/ai/run/@cf/deepgram/aura-2-es"
			path = replacePathParam(path, "account_id", args[0])
			headerOverrides := map[string]string{
				"X-Printing-Press-Binary-Response": "true",
			}
			params := map[string]string{}
			if flagQueueRequest != "" {
				params["queueRequest"] = formatCLIParamValue(flagQueueRequest)
			}
			if flagTags != "" {
				params["tags"] = formatCLIParamValue(flagTags)
			}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyBitRate != 0.0 {
					body["bit_rate"] = bodyBitRate
				}
				if bodyContainer != "" {
					body["container"] = bodyContainer
				}
				if bodyEncoding != "" {
					body["encoding"] = bodyEncoding
				}
				if bodySampleRate != 0.0 {
					body["sample_rate"] = bodySampleRate
				}
				if bodySpeaker != "" {
					body["speaker"] = bodySpeaker
				}
				if bodyText != "" {
					body["text"] = bodyText
				}
			}
			data, statusCode, err := c.PostWithParamsAndHeaders(cmd.Context(), path, params, body, headerOverrides)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = statusCode
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>")
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().StringVar(&flagQueueRequest, "queue-request", "", "Queue request")
	cmd.Flags().StringVar(&flagTags, "tags", "", "Tags")
	cmd.Flags().Float64Var(&bodyBitRate, "bit-rate", 0.0, "The bitrate of the audio in bits per second.")
	cmd.Flags().StringVar(&bodyContainer, "container", "", "Container specifies the file format wrapper for the output audio. The available options depend on the encoding type..")
	cmd.Flags().StringVar(&bodyEncoding, "encoding", "", "Encoding of the output audio.")
	cmd.Flags().Float64Var(&bodySampleRate, "sample-rate", 0.0, "Sample Rate specifies the sample rate for the output audio. Based on the encoding, different sample rates are supported.")
	cmd.Flags().StringVar(&bodySpeaker, "speaker", "aquila", "Speaker used to produce the audio.")
	cmd.Flags().StringVar(&bodyText, "text", "", "The text content to be converted to speech")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
