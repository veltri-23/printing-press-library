// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsSharesListCmd(flags *rootFlags) *cobra.Command {
	var flagStatus string
	var flagKind string
	var flagTargetType string
	var flagResourceTypes string
	var flagOrder string
	var flagDirection string
	var flagPage string
	var flagPerPage int
	var flagIncludeResources bool
	var flagIncludeRecipientCounts bool
	var flagTag string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list <account_id>",
		Short:       "Lists all account shares.",
		Example:     "  cloudflare-pp-cli accounts shares list 023e105f4ecef8ad9ca31a8372d0c353",
		Annotations: map[string]string{"pp:endpoint": "shares.list", "pp:method": "GET", "pp:path": "/accounts/{account_id}/shares", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"active", "deleting", "deleted"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStatus, "status", allowedStatus)
				}
			}
			if cmd.Flags().Changed("kind") {
				allowedKind := []string{"sent", "received"}
				validKind := false
				for _, v := range allowedKind {
					if flagKind == v {
						validKind = true
						break
					}
				}
				if !validKind {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagKind, "kind", allowedKind)
				}
			}
			if cmd.Flags().Changed("target-type") {
				allowedTargetType := []string{"account", "organization"}
				validTargetType := false
				for _, v := range allowedTargetType {
					if flagTargetType == v {
						validTargetType = true
						break
					}
				}
				if !validTargetType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagTargetType, "target-type", allowedTargetType)
				}
			}
			if cmd.Flags().Changed("order") {
				allowedOrder := []string{"name", "created"}
				validOrder := false
				for _, v := range allowedOrder {
					if flagOrder == v {
						validOrder = true
						break
					}
				}
				if !validOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrder, "order", allowedOrder)
				}
			}
			if cmd.Flags().Changed("direction") {
				allowedDirection := []string{"asc", "desc"}
				validDirection := false
				for _, v := range allowedDirection {
					if flagDirection == v {
						validDirection = true
						break
					}
				}
				if !validDirection {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagDirection, "direction", allowedDirection)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/shares"
			path = replacePathParam(path, "account_id", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "shares", path, map[string]string{
				"status":                   formatCLIParamValue(flagStatus),
				"kind":                     formatCLIParamValue(flagKind),
				"target_type":              formatCLIParamValue(flagTargetType),
				"resource_types":           formatCLIParamValue(flagResourceTypes),
				"order":                    formatCLIParamValue(flagOrder),
				"direction":                formatCLIParamValue(flagDirection),
				"page":                     formatCLIParamValue(flagPage),
				"per_page":                 formatCLIParamValue(flagPerPage),
				"include_resources":        formatCLIParamValue(flagIncludeResources),
				"include_recipient_counts": formatCLIParamValue(flagIncludeRecipientCounts),
				"tag":                      formatCLIParamValue(flagTag),
			}, nil, flagAll, "page", "page", "per_page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagStatus, "status", "", "Filter shares by status. (one of: active, deleting, deleted)")
	cmd.Flags().StringVar(&flagKind, "kind", "", "Filter shares by kind. (one of: sent, received)")
	cmd.Flags().StringVar(&flagTargetType, "target-type", "", "Filter shares by target_type. (one of: account, organization)")
	cmd.Flags().StringVar(&flagResourceTypes, "resource-types", "", "Filter share resources by resource_types.")
	cmd.Flags().StringVar(&flagOrder, "order", "created", "Order shares by values in the given field. (one of: name, created)")
	cmd.Flags().StringVar(&flagDirection, "direction", "asc", "Direction to sort objects. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Page number. Defaults to `1` when `per_page` is supplied without `page`.")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 20, "Number of objects to return per page. Defaults to `20` when `page` is supplied without `per_page`.")
	cmd.Flags().BoolVar(&flagIncludeResources, "include-resources", false, "Include resources in the response.")
	cmd.Flags().BoolVar(&flagIncludeRecipientCounts, "include-recipient-counts", false, "Include recipient counts in the response.")
	cmd.Flags().StringVar(&flagTag, "tag", "", "Filter shares by tag.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
