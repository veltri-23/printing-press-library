// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/cloud/cloudflare/internal/cliutil"
	"github.com/spf13/cobra"
)

func newZonesLoadBalancersPatchCmd(flags *rootFlags) *cobra.Command {
	var bodyAdaptiveRoutingFailoverAcrossPools bool
	var bodyCountryPools string
	var bodyDefaultPools string
	var bodyDescription string
	var bodyEnabled bool
	var bodyFallbackPool string
	var bodyLocationStrategyMode string
	var bodyLocationStrategyPreferEcs string
	var bodyName string
	var bodyPopPools string
	var bodyProxied bool
	var bodyRandomSteeringDefaultWeight float64
	var bodyRandomSteeringPoolWeights string
	var bodyRegionPools string
	var bodyRules string
	var bodySessionAffinity string
	var bodySessionAffinityAttributesDrainDuration float64
	var bodySessionAffinityAttributesHeaders string
	var bodySessionAffinityAttributesRequireAllHeaders bool
	var bodySessionAffinityAttributesSamesite string
	var bodySessionAffinityAttributesSecure string
	var bodySessionAffinityAttributesZeroDowntimeFailover string
	var bodySessionAffinityTtl float64
	var bodySteeringPolicy string
	var bodyTtl float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "patch <zone_id> <load_balancer_id>",
		Short:       "Apply changes to an existing load balancer, overwriting the supplied properties.",
		Example:     "  cloudflare-pp-cli zones load-balancers patch 699d98642c564d2e855e9661899b7252 699d98642c564d2e855e9661899b7252",
		Annotations: map[string]string{"pp:endpoint": "load-balancers.patch", "pp:method": "PATCH", "pp:path": "/zones/{zone_id}/load_balancers/{load_balancer_id}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/zones/{zone_id}/load_balancers/{load_balancer_id}"
			path = replacePathParam(path, "zone_id", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("load_balancer_id is required\nUsage: %s <%s>", cmd.CommandPath(), "load_balancer_id"))
			}
			path = replacePathParam(path, "load_balancer_id", args[1])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAdaptiveRouting := map[string]any{}
					if cmd.Flags().Changed("adaptive-routing-failover-across-pools") {
						nestedAdaptiveRouting["failover_across_pools"] = bodyAdaptiveRoutingFailoverAcrossPools
					}
					if len(nestedAdaptiveRouting) > 0 {
						body["adaptive_routing"] = nestedAdaptiveRouting
					}
				}
				if bodyCountryPools != "" {
					var parsedCountryPools any
					if err := json.Unmarshal([]byte(bodyCountryPools), &parsedCountryPools); err != nil {
						return fmt.Errorf("parsing --country-pools JSON: %w", err)
					}
					body["country_pools"] = parsedCountryPools
				}
				if bodyDefaultPools != "" {
					body["default_pools"] = cliutil.SplitCSV(bodyDefaultPools)
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if cmd.Flags().Changed("enabled") {
					body["enabled"] = bodyEnabled
				}
				if bodyFallbackPool != "" {
					body["fallback_pool"] = bodyFallbackPool
				}
				{
					nestedLocationStrategy := map[string]any{}
					if bodyLocationStrategyMode != "" {
						nestedLocationStrategy["mode"] = bodyLocationStrategyMode
					}
					if bodyLocationStrategyPreferEcs != "" {
						nestedLocationStrategy["prefer_ecs"] = bodyLocationStrategyPreferEcs
					}
					if len(nestedLocationStrategy) > 0 {
						body["location_strategy"] = nestedLocationStrategy
					}
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPopPools != "" {
					var parsedPopPools any
					if err := json.Unmarshal([]byte(bodyPopPools), &parsedPopPools); err != nil {
						return fmt.Errorf("parsing --pop-pools JSON: %w", err)
					}
					body["pop_pools"] = parsedPopPools
				}
				if cmd.Flags().Changed("proxied") {
					body["proxied"] = bodyProxied
				}
				{
					nestedRandomSteering := map[string]any{}
					if bodyRandomSteeringDefaultWeight != 0.0 {
						nestedRandomSteering["default_weight"] = bodyRandomSteeringDefaultWeight
					}
					if bodyRandomSteeringPoolWeights != "" {
						var parsedRandomSteeringPoolWeights any
						if err := json.Unmarshal([]byte(bodyRandomSteeringPoolWeights), &parsedRandomSteeringPoolWeights); err != nil {
							return fmt.Errorf("parsing --random-steering-pool-weights JSON: %w", err)
						}
						nestedRandomSteering["pool_weights"] = parsedRandomSteeringPoolWeights
					}
					if len(nestedRandomSteering) > 0 {
						body["random_steering"] = nestedRandomSteering
					}
				}
				if bodyRegionPools != "" {
					var parsedRegionPools any
					if err := json.Unmarshal([]byte(bodyRegionPools), &parsedRegionPools); err != nil {
						return fmt.Errorf("parsing --region-pools JSON: %w", err)
					}
					body["region_pools"] = parsedRegionPools
				}
				if bodyRules != "" {
					var parsedRules any
					if err := json.Unmarshal([]byte(bodyRules), &parsedRules); err != nil {
						return fmt.Errorf("parsing --rules JSON: %w", err)
					}
					body["rules"] = parsedRules
				}
				if bodySessionAffinity != "" {
					body["session_affinity"] = bodySessionAffinity
				}
				{
					nestedSessionAffinityAttributes := map[string]any{}
					if bodySessionAffinityAttributesDrainDuration != 0.0 {
						nestedSessionAffinityAttributes["drain_duration"] = bodySessionAffinityAttributesDrainDuration
					}
					if bodySessionAffinityAttributesHeaders != "" {
						nestedSessionAffinityAttributes["headers"] = cliutil.SplitCSV(bodySessionAffinityAttributesHeaders)
					}
					if cmd.Flags().Changed("session-affinity-attributes-require-all-headers") {
						nestedSessionAffinityAttributes["require_all_headers"] = bodySessionAffinityAttributesRequireAllHeaders
					}
					if bodySessionAffinityAttributesSamesite != "" {
						nestedSessionAffinityAttributes["samesite"] = bodySessionAffinityAttributesSamesite
					}
					if bodySessionAffinityAttributesSecure != "" {
						nestedSessionAffinityAttributes["secure"] = bodySessionAffinityAttributesSecure
					}
					if bodySessionAffinityAttributesZeroDowntimeFailover != "" {
						nestedSessionAffinityAttributes["zero_downtime_failover"] = bodySessionAffinityAttributesZeroDowntimeFailover
					}
					if len(nestedSessionAffinityAttributes) > 0 {
						body["session_affinity_attributes"] = nestedSessionAffinityAttributes
					}
				}
				if bodySessionAffinityTtl != 0.0 {
					body["session_affinity_ttl"] = bodySessionAffinityTtl
				}
				if bodySteeringPolicy != "" {
					body["steering_policy"] = bodySteeringPolicy
				}
				if bodyTtl != 0.0 {
					body["ttl"] = bodyTtl
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "load-balancers", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "load-balancers", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "load-balancers", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "load-balancers", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "load-balancers", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "load-balancers",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "load-balancers", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "load-balancers", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAdaptiveRoutingFailoverAcrossPools, "adaptive-routing-failover-across-pools", false, "Extends zero-downtime failover of requests to healthy origins from alternate pools")
	cmd.Flags().StringVar(&bodyCountryPools, "country-pools", "", "A mapping of country codes to a list of pool IDs (ordered by their failover priority) for the given country.")
	cmd.Flags().StringVar(&bodyDefaultPools, "default-pools", "", "A list of pool IDs ordered by their failover priority.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Object description.")
	cmd.Flags().BoolVar(&bodyEnabled, "enabled", true, "Whether to enable (the default) this load balancer.")
	cmd.Flags().StringVar(&bodyFallbackPool, "fallback-pool", "", "The pool ID to use when all other pools are detected as unhealthy.")
	cmd.Flags().StringVar(&bodyLocationStrategyMode, "location-strategy-mode", "", "Determines the authoritative location when ECS is not preferred, does not exist in the request")
	cmd.Flags().StringVar(&bodyLocationStrategyPreferEcs, "location-strategy-prefer-ecs", "", "Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as the authoritative location.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The DNS hostname to associate with your Load Balancer.")
	cmd.Flags().StringVar(&bodyPopPools, "pop-pools", "", "Enterprise only: A mapping of Cloudflare PoP identifiers to a list of pool IDs (ordered by their failover priority)")
	cmd.Flags().BoolVar(&bodyProxied, "proxied", false, "Whether the hostname should be gray clouded (false) or orange clouded (true).")
	cmd.Flags().Float64Var(&bodyRandomSteeringDefaultWeight, "random-steering-default-weight", 0.0, "The default weight for pools in the load balancer that are not specified in the pool_weights map.")
	cmd.Flags().StringVar(&bodyRandomSteeringPoolWeights, "random-steering-pool-weights", "", "A mapping of pool IDs to custom weights. The weight is relative to other pools in the load balancer.")
	cmd.Flags().StringVar(&bodyRegionPools, "region-pools", "", "A mapping of region codes to a list of pool IDs (ordered by their failover priority) for the given region.")
	cmd.Flags().StringVar(&bodyRules, "rules", "", "BETA Field Not General Access: A list of rules for this load balancer to execute.")
	cmd.Flags().StringVar(&bodySessionAffinity, "session-affinity", "none", "Specifies the type of session affinity the load balancer should use unless specified as `'none'`.")
	cmd.Flags().Float64Var(&bodySessionAffinityAttributesDrainDuration, "session-affinity-attributes-drain-duration", 0.0, "Configures the drain duration in seconds. This field is only used when session affinity is enabled on the load balancer.")
	cmd.Flags().StringVar(&bodySessionAffinityAttributesHeaders, "session-affinity-attributes-headers", "", "Configures the names of HTTP headers to base session affinity on when header `session_affinity` is enabled.")
	cmd.Flags().BoolVar(&bodySessionAffinityAttributesRequireAllHeaders, "session-affinity-attributes-require-all-headers", false, "When header `session_affinity` is enabled")
	cmd.Flags().StringVar(&bodySessionAffinityAttributesSamesite, "session-affinity-attributes-samesite", "", "Configures the SameSite attribute on session affinity cookie.")
	cmd.Flags().StringVar(&bodySessionAffinityAttributesSecure, "session-affinity-attributes-secure", "", "Configures the Secure attribute on session affinity cookie.")
	cmd.Flags().StringVar(&bodySessionAffinityAttributesZeroDowntimeFailover, "session-affinity-attributes-zero-downtime-failover", "", "Configures the zero-downtime failover between origins within a pool when session affinity is enabled.")
	cmd.Flags().Float64Var(&bodySessionAffinityTtl, "session-affinity-ttl", 0.0, "Time, in seconds, until a client's session expires after being created.")
	cmd.Flags().StringVar(&bodySteeringPolicy, "steering-policy", "", "Steering Policy for this load balancer. - `'off'`: Use `default_pools`.")
	cmd.Flags().Float64Var(&bodyTtl, "ttl", 0.0, "Time to live (TTL) of the DNS entry for the IP address returned by this load balancer.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
