// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strconv"

	"github.com/spf13/cobra"
)

func newZonesSettingsPutZonesZoneIdentifierZarazConfigCmd(flags *rootFlags) *cobra.Command {
	var bodyAnalyticsDefaultPurpose string
	var bodyAnalyticsEnabled bool
	var bodyAnalyticsSessionExpTime int
	var bodyConsentButtonTextTranslationsAcceptAll string
	var bodyConsentButtonTextTranslationsConfirmMyChoices string
	var bodyConsentButtonTextTranslationsRejectAll string
	var bodyConsentCompanyEmail string
	var bodyConsentCompanyName string
	var bodyConsentCompanyStreetAddress string
	var bodyConsentConsentModalIntroHTML string
	var bodyConsentConsentModalIntroHTMLWithTranslations string
	var bodyConsentCookieName string
	var bodyConsentCustomCSS string
	var bodyConsentCustomIntroDisclaimerDismissed bool
	var bodyConsentDefaultLanguage string
	var bodyConsentEnabled string
	var bodyConsentHideModal bool
	var bodyConsentPurposes string
	var bodyConsentPurposesWithTranslations string
	var bodyConsentTcfCompliant bool
	var bodyDataLayer string
	var bodyDebugKey string
	var bodyHistoryChange bool
	var bodySettingsAutoInjectScript string
	var bodySettingsContextEnricherEscapedWorkerName string
	var bodySettingsContextEnricherWorkerTag string
	var bodySettingsCookieDomain string
	var bodySettingsEcommerce bool
	var bodySettingsEventsApiPath string
	var bodySettingsHideExternalReferer bool
	var bodySettingsHideIPAddress bool
	var bodySettingsHideQueryParams bool
	var bodySettingsHideUserAgent bool
	var bodySettingsInitPath string
	var bodySettingsInjectIframes bool
	var bodySettingsMcRootPath string
	var bodySettingsScriptPath string
	var bodySettingsTrackPath string
	var bodyTools string
	var bodyTriggers string
	var bodyVariables string
	var bodyZarazVersion int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "put-zones-zone-identifier-zaraz-config <zone_id>",
		Short:       "Updates Zaraz configuration for a zone.",
		Example:     "  cloudflare-pp-cli zones settings put-zones-zone-identifier-zaraz-config 023e105f4ecef8ad9ca31a8372d0c353 --debug-key your-token-here",
		Annotations: map[string]string{"pp:endpoint": "settings.put-zones-zone-identifier-zaraz-config", "pp:method": "PUT", "pp:path": "/zones/{zone_id}/settings/zaraz/config"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("consent-button-text-translations-accept-all") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "consent-button-text-translations-accept-all")
				}
				if !cmd.Flags().Changed("consent-button-text-translations-confirm-my-choices") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "consent-button-text-translations-confirm-my-choices")
				}
				if !cmd.Flags().Changed("consent-button-text-translations-reject-all") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "consent-button-text-translations-reject-all")
				}
				if !cmd.Flags().Changed("consent-enabled") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "consent-enabled")
				}
				if !cmd.Flags().Changed("data-layer") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-layer")
				}
				if !cmd.Flags().Changed("debug-key") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "debug-key")
				}
				if !cmd.Flags().Changed("settings-auto-inject-script") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "settings-auto-inject-script")
				}
				if !cmd.Flags().Changed("settings-context-enricher-escaped-worker-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "settings-context-enricher-escaped-worker-name")
				}
				if !cmd.Flags().Changed("settings-context-enricher-worker-tag") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "settings-context-enricher-worker-tag")
				}
				if !cmd.Flags().Changed("tools") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "tools")
				}
				if !cmd.Flags().Changed("triggers") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "triggers")
				}
				if !cmd.Flags().Changed("variables") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "variables")
				}
				if !cmd.Flags().Changed("zaraz-version") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "zaraz-version")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/zones/{zone_id}/settings/zaraz/config"
			path = replacePathParam(path, "zone_id", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAnalytics := map[string]any{}
					if bodyAnalyticsDefaultPurpose != "" {
						nestedAnalytics["defaultPurpose"] = bodyAnalyticsDefaultPurpose
					}
					if cmd.Flags().Changed("analytics-enabled") {
						nestedAnalytics["enabled"] = bodyAnalyticsEnabled
					}
					if bodyAnalyticsSessionExpTime != 0 {
						nestedAnalytics["sessionExpTime"] = bodyAnalyticsSessionExpTime
					}
					if len(nestedAnalytics) > 0 {
						body["analytics"] = nestedAnalytics
					}
				}
				{
					nestedConsent := map[string]any{}
					{
						nestedConsentButtonTextTranslations := map[string]any{}
						if bodyConsentButtonTextTranslationsAcceptAll != "" {
							var parsedConsentButtonTextTranslationsAcceptAll any
							if err := json.Unmarshal([]byte(bodyConsentButtonTextTranslationsAcceptAll), &parsedConsentButtonTextTranslationsAcceptAll); err != nil {
								return fmt.Errorf("parsing --consent-button-text-translations-accept-all JSON: %w", err)
							}
							nestedConsentButtonTextTranslations["accept_all"] = parsedConsentButtonTextTranslationsAcceptAll
						}
						if bodyConsentButtonTextTranslationsConfirmMyChoices != "" {
							var parsedConsentButtonTextTranslationsConfirmMyChoices any
							if err := json.Unmarshal([]byte(bodyConsentButtonTextTranslationsConfirmMyChoices), &parsedConsentButtonTextTranslationsConfirmMyChoices); err != nil {
								return fmt.Errorf("parsing --consent-button-text-translations-confirm-my-choices JSON: %w", err)
							}
							nestedConsentButtonTextTranslations["confirm_my_choices"] = parsedConsentButtonTextTranslationsConfirmMyChoices
						}
						if bodyConsentButtonTextTranslationsRejectAll != "" {
							var parsedConsentButtonTextTranslationsRejectAll any
							if err := json.Unmarshal([]byte(bodyConsentButtonTextTranslationsRejectAll), &parsedConsentButtonTextTranslationsRejectAll); err != nil {
								return fmt.Errorf("parsing --consent-button-text-translations-reject-all JSON: %w", err)
							}
							nestedConsentButtonTextTranslations["reject_all"] = parsedConsentButtonTextTranslationsRejectAll
						}
						if len(nestedConsentButtonTextTranslations) > 0 {
							nestedConsent["buttonTextTranslations"] = nestedConsentButtonTextTranslations
						}
					}
					if bodyConsentCompanyEmail != "" {
						nestedConsent["companyEmail"] = bodyConsentCompanyEmail
					}
					if bodyConsentCompanyName != "" {
						nestedConsent["companyName"] = bodyConsentCompanyName
					}
					if bodyConsentCompanyStreetAddress != "" {
						nestedConsent["companyStreetAddress"] = bodyConsentCompanyStreetAddress
					}
					if bodyConsentConsentModalIntroHTML != "" {
						nestedConsent["consentModalIntroHTML"] = bodyConsentConsentModalIntroHTML
					}
					if bodyConsentConsentModalIntroHTMLWithTranslations != "" {
						var parsedConsentConsentModalIntroHTMLWithTranslations any
						if err := json.Unmarshal([]byte(bodyConsentConsentModalIntroHTMLWithTranslations), &parsedConsentConsentModalIntroHTMLWithTranslations); err != nil {
							return fmt.Errorf("parsing --consent-consent-modal-intro-html-with-translations JSON: %w", err)
						}
						nestedConsent["consentModalIntroHTMLWithTranslations"] = parsedConsentConsentModalIntroHTMLWithTranslations
					}
					if bodyConsentCookieName != "" {
						nestedConsent["cookieName"] = bodyConsentCookieName
					}
					if bodyConsentCustomCSS != "" {
						nestedConsent["customCSS"] = bodyConsentCustomCSS
					}
					if cmd.Flags().Changed("consent-custom-intro-disclaimer-dismissed") {
						nestedConsent["customIntroDisclaimerDismissed"] = bodyConsentCustomIntroDisclaimerDismissed
					}
					if bodyConsentDefaultLanguage != "" {
						nestedConsent["defaultLanguage"] = bodyConsentDefaultLanguage
					}
					if bodyConsentEnabled != "" {
						parsedConsentEnabled, err := strconv.ParseBool(bodyConsentEnabled)
						if err != nil {
							return fmt.Errorf("parsing --consent-enabled as bool: %w", err)
						}
						nestedConsent["enabled"] = parsedConsentEnabled
					}
					if cmd.Flags().Changed("consent-hide-modal") {
						nestedConsent["hideModal"] = bodyConsentHideModal
					}
					if bodyConsentPurposes != "" {
						var parsedConsentPurposes any
						if err := json.Unmarshal([]byte(bodyConsentPurposes), &parsedConsentPurposes); err != nil {
							return fmt.Errorf("parsing --consent-purposes JSON: %w", err)
						}
						nestedConsent["purposes"] = parsedConsentPurposes
					}
					if bodyConsentPurposesWithTranslations != "" {
						var parsedConsentPurposesWithTranslations any
						if err := json.Unmarshal([]byte(bodyConsentPurposesWithTranslations), &parsedConsentPurposesWithTranslations); err != nil {
							return fmt.Errorf("parsing --consent-purposes-with-translations JSON: %w", err)
						}
						nestedConsent["purposesWithTranslations"] = parsedConsentPurposesWithTranslations
					}
					if cmd.Flags().Changed("consent-tcf-compliant") {
						nestedConsent["tcfCompliant"] = bodyConsentTcfCompliant
					}
					if len(nestedConsent) > 0 {
						body["consent"] = nestedConsent
					}
				}
				if bodyDataLayer != "" {
					parsedDataLayer, err := strconv.ParseBool(bodyDataLayer)
					if err != nil {
						return fmt.Errorf("parsing --data-layer as bool: %w", err)
					}
					body["dataLayer"] = parsedDataLayer
				}
				if bodyDebugKey != "" {
					body["debugKey"] = bodyDebugKey
				}
				if cmd.Flags().Changed("history-change") {
					body["historyChange"] = bodyHistoryChange
				}
				{
					nestedSettings := map[string]any{}
					if bodySettingsAutoInjectScript != "" {
						parsedSettingsAutoInjectScript, err := strconv.ParseBool(bodySettingsAutoInjectScript)
						if err != nil {
							return fmt.Errorf("parsing --settings-auto-inject-script as bool: %w", err)
						}
						nestedSettings["autoInjectScript"] = parsedSettingsAutoInjectScript
					}
					{
						nestedSettingsContextEnricher := map[string]any{}
						if bodySettingsContextEnricherEscapedWorkerName != "" {
							nestedSettingsContextEnricher["escapedWorkerName"] = bodySettingsContextEnricherEscapedWorkerName
						}
						if bodySettingsContextEnricherWorkerTag != "" {
							nestedSettingsContextEnricher["workerTag"] = bodySettingsContextEnricherWorkerTag
						}
						if len(nestedSettingsContextEnricher) > 0 {
							nestedSettings["contextEnricher"] = nestedSettingsContextEnricher
						}
					}
					if bodySettingsCookieDomain != "" {
						nestedSettings["cookieDomain"] = bodySettingsCookieDomain
					}
					if cmd.Flags().Changed("settings-ecommerce") {
						nestedSettings["ecommerce"] = bodySettingsEcommerce
					}
					if bodySettingsEventsApiPath != "" {
						nestedSettings["eventsApiPath"] = bodySettingsEventsApiPath
					}
					if cmd.Flags().Changed("settings-hide-external-referer") {
						nestedSettings["hideExternalReferer"] = bodySettingsHideExternalReferer
					}
					if cmd.Flags().Changed("settings-hide-ip-address") {
						nestedSettings["hideIPAddress"] = bodySettingsHideIPAddress
					}
					if cmd.Flags().Changed("settings-hide-query-params") {
						nestedSettings["hideQueryParams"] = bodySettingsHideQueryParams
					}
					if cmd.Flags().Changed("settings-hide-user-agent") {
						nestedSettings["hideUserAgent"] = bodySettingsHideUserAgent
					}
					if bodySettingsInitPath != "" {
						nestedSettings["initPath"] = bodySettingsInitPath
					}
					if cmd.Flags().Changed("settings-inject-iframes") {
						nestedSettings["injectIframes"] = bodySettingsInjectIframes
					}
					if bodySettingsMcRootPath != "" {
						nestedSettings["mcRootPath"] = bodySettingsMcRootPath
					}
					if bodySettingsScriptPath != "" {
						nestedSettings["scriptPath"] = bodySettingsScriptPath
					}
					if bodySettingsTrackPath != "" {
						nestedSettings["trackPath"] = bodySettingsTrackPath
					}
					if len(nestedSettings) > 0 {
						body["settings"] = nestedSettings
					}
				}
				if bodyTools != "" {
					var parsedTools any
					if err := json.Unmarshal([]byte(bodyTools), &parsedTools); err != nil {
						return fmt.Errorf("parsing --tools JSON: %w", err)
					}
					body["tools"] = parsedTools
				}
				if bodyTriggers != "" {
					var parsedTriggers any
					if err := json.Unmarshal([]byte(bodyTriggers), &parsedTriggers); err != nil {
						return fmt.Errorf("parsing --triggers JSON: %w", err)
					}
					body["triggers"] = parsedTriggers
				}
				if bodyVariables != "" {
					var parsedVariables any
					if err := json.Unmarshal([]byte(bodyVariables), &parsedVariables); err != nil {
						return fmt.Errorf("parsing --variables JSON: %w", err)
					}
					body["variables"] = parsedVariables
				}
				if bodyZarazVersion != 0 {
					body["zarazVersion"] = bodyZarazVersion
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "settings", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "settings", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "settings",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAnalyticsDefaultPurpose, "analytics-default-purpose", "", "Consent purpose assigned to Monitoring.")
	cmd.Flags().BoolVar(&bodyAnalyticsEnabled, "analytics-enabled", false, "Whether Advanced Monitoring reports are enabled.")
	cmd.Flags().IntVar(&bodyAnalyticsSessionExpTime, "analytics-session-exp-time", 0, "Session expiration time (seconds).")
	cmd.Flags().StringVar(&bodyConsentButtonTextTranslationsAcceptAll, "consent-button-text-translations-accept-all", "", "Object where keys are language codes.")
	cmd.Flags().StringVar(&bodyConsentButtonTextTranslationsConfirmMyChoices, "consent-button-text-translations-confirm-my-choices", "", "Object where keys are language codes.")
	cmd.Flags().StringVar(&bodyConsentButtonTextTranslationsRejectAll, "consent-button-text-translations-reject-all", "", "Object where keys are language codes.")
	cmd.Flags().StringVar(&bodyConsentCompanyEmail, "consent-company-email", "", "Company email")
	cmd.Flags().StringVar(&bodyConsentCompanyName, "consent-company-name", "", "Company name")
	cmd.Flags().StringVar(&bodyConsentCompanyStreetAddress, "consent-company-street-address", "", "Company street address")
	cmd.Flags().StringVar(&bodyConsentConsentModalIntroHTML, "consent-consent-modal-intro-html", "", "Consent modal intro html")
	cmd.Flags().StringVar(&bodyConsentConsentModalIntroHTMLWithTranslations, "consent-consent-modal-intro-html-with-translations", "", "Object where keys are language codes.")
	cmd.Flags().StringVar(&bodyConsentCookieName, "consent-cookie-name", "", "Cookie name")
	cmd.Flags().StringVar(&bodyConsentCustomCSS, "consent-custom-css", "", "Custom css")
	cmd.Flags().BoolVar(&bodyConsentCustomIntroDisclaimerDismissed, "consent-custom-intro-disclaimer-dismissed", false, "Custom intro disclaimer dismissed")
	cmd.Flags().StringVar(&bodyConsentDefaultLanguage, "consent-default-language", "", "Default language")
	cmd.Flags().StringVar(&bodyConsentEnabled, "consent-enabled", "", "Enabled")
	cmd.Flags().BoolVar(&bodyConsentHideModal, "consent-hide-modal", false, "Hide modal")
	cmd.Flags().StringVar(&bodyConsentPurposes, "consent-purposes", "", "Object where keys are purpose alpha-numeric IDs.")
	cmd.Flags().StringVar(&bodyConsentPurposesWithTranslations, "consent-purposes-with-translations", "", "Object where keys are purpose alpha-numeric IDs.")
	cmd.Flags().BoolVar(&bodyConsentTcfCompliant, "consent-tcf-compliant", false, "Tcf compliant")
	cmd.Flags().StringVar(&bodyDataLayer, "data-layer", "", "Data layer compatibility mode enabled.")
	cmd.Flags().StringVar(&bodyDebugKey, "debug-key", "", "The key for Zaraz debug mode.")
	cmd.Flags().BoolVar(&bodyHistoryChange, "history-change", false, "Single Page Application support enabled.")
	cmd.Flags().StringVar(&bodySettingsAutoInjectScript, "settings-auto-inject-script", "", "Automatic injection of Zaraz scripts enabled.")
	cmd.Flags().StringVar(&bodySettingsContextEnricherEscapedWorkerName, "settings-context-enricher-escaped-worker-name", "", "Escaped worker name")
	cmd.Flags().StringVar(&bodySettingsContextEnricherWorkerTag, "settings-context-enricher-worker-tag", "", "Worker tag")
	cmd.Flags().StringVar(&bodySettingsCookieDomain, "settings-cookie-domain", "", "The domain Zaraz will use for writing and reading its cookies.")
	cmd.Flags().BoolVar(&bodySettingsEcommerce, "settings-ecommerce", false, "Ecommerce API enabled.")
	cmd.Flags().StringVar(&bodySettingsEventsApiPath, "settings-events-api-path", "", "Custom endpoint for server-side track events.")
	cmd.Flags().BoolVar(&bodySettingsHideExternalReferer, "settings-hide-external-referer", false, "Hiding external referrer URL enabled.")
	cmd.Flags().BoolVar(&bodySettingsHideIPAddress, "settings-hide-ip-address", false, "Trimming IP address enabled.")
	cmd.Flags().BoolVar(&bodySettingsHideQueryParams, "settings-hide-query-params", false, "Removing URL query params enabled.")
	cmd.Flags().BoolVar(&bodySettingsHideUserAgent, "settings-hide-user-agent", false, "Removing sensitive data from User Agent string enabled.")
	cmd.Flags().StringVar(&bodySettingsInitPath, "settings-init-path", "", "Custom endpoint for Zaraz init script.")
	cmd.Flags().BoolVar(&bodySettingsInjectIframes, "settings-inject-iframes", false, "Injection of Zaraz scripts into iframes enabled.")
	cmd.Flags().StringVar(&bodySettingsMcRootPath, "settings-mc-root-path", "", "Custom path for Managed Components server functionalities.")
	cmd.Flags().StringVar(&bodySettingsScriptPath, "settings-script-path", "", "Custom endpoint for Zaraz main script.")
	cmd.Flags().StringVar(&bodySettingsTrackPath, "settings-track-path", "", "Custom endpoint for Zaraz tracking requests.")
	cmd.Flags().StringVar(&bodyTools, "tools", "", "Tools set up under Zaraz configuration")
	cmd.Flags().StringVar(&bodyTriggers, "triggers", "", "Triggers set up under Zaraz configuration")
	cmd.Flags().StringVar(&bodyVariables, "variables", "", "Variables set up under Zaraz configuration")
	cmd.Flags().IntVar(&bodyZarazVersion, "zaraz-version", 0, "Zaraz internal version of the config.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
