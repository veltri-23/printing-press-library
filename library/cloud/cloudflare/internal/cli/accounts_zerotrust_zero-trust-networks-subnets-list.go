// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsZerotrustZeroTrustNetworksSubnetsListCmd(flags *rootFlags) *cobra.Command {
	var flagName string
	var flagComment string
	var flagNetwork string
	var flagExistedAt string
	var flagAddressFamily string
	var flagIsDefaultNetwork bool
	var flagIsDeleted bool
	var flagSortOrder string
	var flagSubnetTypes string
	var flagPerPage float64
	var flagPage string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "zero-trust-networks-subnets-list <account_id>",
		Short:       "Lists and filters subnets in an account.",
		Example:     "  cloudflare-pp-cli accounts zerotrust zero-trust-networks-subnets-list 699d98642c564d2e855e9661899b7252",
		Annotations: map[string]string{"pp:endpoint": "zerotrust.zero-trust-networks-subnets-list", "pp:method": "GET", "pp:path": "/accounts/{account_id}/zerotrust/subnets", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("address-family") {
				allowedAddressFamily := []string{"v4", "v6"}
				validAddressFamily := false
				for _, v := range allowedAddressFamily {
					if flagAddressFamily == v {
						validAddressFamily = true
						break
					}
				}
				if !validAddressFamily {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAddressFamily, "address-family", allowedAddressFamily)
				}
			}
			if cmd.Flags().Changed("sort-order") {
				allowedSortOrder := []string{"asc", "desc"}
				validSortOrder := false
				for _, v := range allowedSortOrder {
					if flagSortOrder == v {
						validSortOrder = true
						break
					}
				}
				if !validSortOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSortOrder, "sort-order", allowedSortOrder)
				}
			}
			if cmd.Flags().Changed("subnet-types") {
				allowedSubnetTypes := []string{"cloudflare_source", "warp"}
				validSubnetTypes := false
				for _, v := range allowedSubnetTypes {
					if flagSubnetTypes == v {
						validSubnetTypes = true
						break
					}
				}
				if !validSubnetTypes {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSubnetTypes, "subnet-types", allowedSubnetTypes)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accounts/{account_id}/zerotrust/subnets"
			path = replacePathParam(path, "account_id", args[0])
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "zerotrust", path, map[string]string{
				"name":               formatCLIParamValue(flagName),
				"comment":            formatCLIParamValue(flagComment),
				"network":            formatCLIParamValue(flagNetwork),
				"existed_at":         formatCLIParamValue(flagExistedAt),
				"address_family":     formatCLIParamValue(flagAddressFamily),
				"is_default_network": formatCLIParamValue(flagIsDefaultNetwork),
				"is_deleted":         formatCLIParamValue(flagIsDeleted),
				"sort_order":         formatCLIParamValue(flagSortOrder),
				"subnet_types":       formatCLIParamValue(flagSubnetTypes),
				"per_page":           formatCLIParamValue(flagPerPage),
				"page":               formatCLIParamValue(flagPage),
			}, nil, flagAll, "page", "page", "per_page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagName, "name", "", "If set, only list subnets with the given name")
	cmd.Flags().StringVar(&flagComment, "comment", "", "Comment")
	cmd.Flags().StringVar(&flagNetwork, "network", "", "Network")
	cmd.Flags().StringVar(&flagExistedAt, "existed-at", "", "Existed at")
	cmd.Flags().StringVar(&flagAddressFamily, "address-family", "", "If set, only include subnets in the given address family - `v4` or `v6` (one of: v4, v6)")
	cmd.Flags().BoolVar(&flagIsDefaultNetwork, "is-default-network", false, "Is default network")
	cmd.Flags().BoolVar(&flagIsDeleted, "is-deleted", false, "Is deleted")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "", "Sort order (one of: asc, desc)")
	cmd.Flags().StringVar(&flagSubnetTypes, "subnet-types", "", "Subnet types (one of: cloudflare_source, warp)")
	cmd.Flags().Float64Var(&flagPerPage, "per-page", 0.0, "Per page")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Page")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
