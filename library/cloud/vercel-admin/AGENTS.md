# AGENTS.md — vercel-admin

This directory is generated by CLI Printing Press and then curated for the published library.

## Rules

- Treat generated endpoint wrappers as API surface, not business logic.
- Keep operator commands under `internal/cli/ops*.go` read-only unless a future change explicitly gates mutation behind `--yes`.
- Use `VERCEL_TOKEN` for auth and `VERCEL_ADMIN_BASE_URL` for mock/live verification overrides.
- Run `go test ./...`, `go vet ./...`, and `go build ./...` before publishing changes.

