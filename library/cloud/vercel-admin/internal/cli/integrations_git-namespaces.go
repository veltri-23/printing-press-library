// Copyright 2026 hnshah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIntegrationsGitNamespacesCmd(flags *rootFlags) *cobra.Command {
	var flagHost string
	var flagProvider string
	var flagViewerMetadata bool

	cmd := &cobra.Command{
		Use:         "git-namespaces",
		Short:       "Lists git namespaces for a supported provider. Supported providers are `github`, `gitlab` and `bitbucket`.",
		Example:     "  vercel-admin-pp-cli integrations git-namespaces",
		Annotations: map[string]string{"pp:endpoint": "integrations.git-namespaces", "pp:method": "GET", "pp:path": "/v1/integrations/git-namespaces", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("provider") {
				allowedProvider := []string{"github", "github-limited", "github-custom-host", "gitlab", "bitbucket"}
				validProvider := false
				for _, v := range allowedProvider {
					if flagProvider == v {
						validProvider = true
						break
					}
				}
				if !validProvider {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagProvider, "provider", allowedProvider)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/integrations/git-namespaces"
			params := map[string]string{}
			if flagHost != "" {
				params["host"] = formatCLIParamValue(flagHost)
			}
			if flagProvider != "" {
				params["provider"] = formatCLIParamValue(flagProvider)
			}
			if flagViewerMetadata != false {
				params["viewerMetadata"] = formatCLIParamValue(flagViewerMetadata)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "integrations", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagHost, "host", "", "The custom Git host if using a custom Git provider, like GitHub Enterprise Server")
	cmd.Flags().StringVar(&flagProvider, "provider", "", "Provider (one of: github, github-limited, github-custom-host, gitlab, bitbucket)")
	cmd.Flags().BoolVar(&flagViewerMetadata, "viewer-metadata", false, "When true, includes the viewer object for each namespace.")

	return cmd
}
