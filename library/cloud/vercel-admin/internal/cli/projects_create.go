// Copyright 2026 hnshah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/cloud/vercel-admin/internal/cliutil"
	"github.com/spf13/cobra"
)

func newProjectsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyBuildCommand string
	var bodyCommandForIgnoringBuildStep string
	var bodyDevCommand string
	var bodyEnableAffectedProjectsDeployments bool
	var bodyEnablePreviewFeedback bool
	var bodyEnableProductionFeedback bool
	var bodyEnvironmentVariables string
	var bodyFramework string
	var bodyGitRepositoryRepo string
	var bodyGitRepositoryType string
	var bodyInstallCommand string
	var bodyName string
	var bodyOidcTokenConfigEnabled bool
	var bodyOidcTokenConfigIssuerMode string
	var bodyOutputDirectory string
	var bodyPreviewDeploymentSuffix string
	var bodyPreviewDeploymentsDisabled bool
	var bodyPublicSource bool
	var bodyResourceConfigBuildMachineElasticLastUpdated float64
	var bodyResourceConfigBuildMachineSelection string
	var bodyResourceConfigBuildMachineType string
	var bodyResourceConfigBuildQueueConfiguration string
	var bodyResourceConfigElasticConcurrencyEnabled bool
	var bodyResourceConfigEnableFunctionsBeta bool
	var bodyResourceConfigFluid bool
	var bodyResourceConfigFunctionDefaultMemoryType string
	var bodyResourceConfigFunctionDefaultRegions string
	var bodyResourceConfigFunctionDefaultTimeout float64
	var bodyResourceConfigFunctionZeroConfigFailover string
	var bodyResourceConfigIsNSNBDisabled bool
	var bodyRootDirectory string
	var bodyServerlessFunctionRegion string
	var bodyServerlessFunctionZeroConfigFailover string
	var bodySkipGitConnectDuringLink bool
	var bodySsoProtectionDeploymentType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Allows to create a new project with the provided configuration.",
		Example:     "  vercel-admin-pp-cli projects create --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "projects.create", "pp:method": "POST", "pp:path": "/v11/projects"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("git-repository-repo") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "git-repository-repo")
				}
				if !cmd.Flags().Changed("git-repository-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "git-repository-type")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("sso-protection-deployment-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "sso-protection-deployment-type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v11/projects"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyBuildCommand != "" {
					body["buildCommand"] = bodyBuildCommand
				}
				if bodyCommandForIgnoringBuildStep != "" {
					body["commandForIgnoringBuildStep"] = bodyCommandForIgnoringBuildStep
				}
				if bodyDevCommand != "" {
					body["devCommand"] = bodyDevCommand
				}
				if cmd.Flags().Changed("enable-affected-projects-deployments") {
					body["enableAffectedProjectsDeployments"] = bodyEnableAffectedProjectsDeployments
				}
				if cmd.Flags().Changed("enable-preview-feedback") {
					body["enablePreviewFeedback"] = bodyEnablePreviewFeedback
				}
				if cmd.Flags().Changed("enable-production-feedback") {
					body["enableProductionFeedback"] = bodyEnableProductionFeedback
				}
				if bodyEnvironmentVariables != "" {
					var parsedEnvironmentVariables any
					if err := json.Unmarshal([]byte(bodyEnvironmentVariables), &parsedEnvironmentVariables); err != nil {
						return fmt.Errorf("parsing --environment-variables JSON: %w", err)
					}
					body["environmentVariables"] = parsedEnvironmentVariables
				}
				if bodyFramework != "" {
					body["framework"] = bodyFramework
				}
				{
					nestedGitRepository := map[string]any{}
					if bodyGitRepositoryRepo != "" {
						nestedGitRepository["repo"] = bodyGitRepositoryRepo
					}
					if bodyGitRepositoryType != "" {
						nestedGitRepository["type"] = bodyGitRepositoryType
					}
					if len(nestedGitRepository) > 0 {
						body["gitRepository"] = nestedGitRepository
					}
				}
				if bodyInstallCommand != "" {
					body["installCommand"] = bodyInstallCommand
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				{
					nestedOidcTokenConfig := map[string]any{}
					if cmd.Flags().Changed("oidc-token-config-enabled") {
						nestedOidcTokenConfig["enabled"] = bodyOidcTokenConfigEnabled
					}
					if bodyOidcTokenConfigIssuerMode != "" {
						nestedOidcTokenConfig["issuerMode"] = bodyOidcTokenConfigIssuerMode
					}
					if len(nestedOidcTokenConfig) > 0 {
						body["oidcTokenConfig"] = nestedOidcTokenConfig
					}
				}
				if bodyOutputDirectory != "" {
					body["outputDirectory"] = bodyOutputDirectory
				}
				if bodyPreviewDeploymentSuffix != "" {
					body["previewDeploymentSuffix"] = bodyPreviewDeploymentSuffix
				}
				if cmd.Flags().Changed("preview-deployments-disabled") {
					body["previewDeploymentsDisabled"] = bodyPreviewDeploymentsDisabled
				}
				if cmd.Flags().Changed("public-source") {
					body["publicSource"] = bodyPublicSource
				}
				{
					nestedResourceConfig := map[string]any{}
					if bodyResourceConfigBuildMachineElasticLastUpdated != 0.0 {
						nestedResourceConfig["buildMachineElasticLastUpdated"] = bodyResourceConfigBuildMachineElasticLastUpdated
					}
					if bodyResourceConfigBuildMachineSelection != "" {
						nestedResourceConfig["buildMachineSelection"] = bodyResourceConfigBuildMachineSelection
					}
					if bodyResourceConfigBuildMachineType != "" {
						nestedResourceConfig["buildMachineType"] = bodyResourceConfigBuildMachineType
					}
					{
						nestedResourceConfigBuildQueue := map[string]any{}
						if bodyResourceConfigBuildQueueConfiguration != "" {
							nestedResourceConfigBuildQueue["configuration"] = bodyResourceConfigBuildQueueConfiguration
						}
						if len(nestedResourceConfigBuildQueue) > 0 {
							nestedResourceConfig["buildQueue"] = nestedResourceConfigBuildQueue
						}
					}
					if cmd.Flags().Changed("resource-config-elastic-concurrency-enabled") {
						nestedResourceConfig["elasticConcurrencyEnabled"] = bodyResourceConfigElasticConcurrencyEnabled
					}
					if cmd.Flags().Changed("resource-config-enable-functions-beta") {
						nestedResourceConfig["enableFunctionsBeta"] = bodyResourceConfigEnableFunctionsBeta
					}
					if cmd.Flags().Changed("resource-config-fluid") {
						nestedResourceConfig["fluid"] = bodyResourceConfigFluid
					}
					if bodyResourceConfigFunctionDefaultMemoryType != "" {
						nestedResourceConfig["functionDefaultMemoryType"] = bodyResourceConfigFunctionDefaultMemoryType
					}
					if bodyResourceConfigFunctionDefaultRegions != "" {
						nestedResourceConfig["functionDefaultRegions"] = cliutil.SplitCSV(bodyResourceConfigFunctionDefaultRegions)
					}
					if bodyResourceConfigFunctionDefaultTimeout != 0.0 {
						nestedResourceConfig["functionDefaultTimeout"] = bodyResourceConfigFunctionDefaultTimeout
					}
					if bodyResourceConfigFunctionZeroConfigFailover != "" {
						nestedResourceConfig["functionZeroConfigFailover"] = bodyResourceConfigFunctionZeroConfigFailover
					}
					if cmd.Flags().Changed("resource-config-is-nsnb-disabled") {
						nestedResourceConfig["isNSNBDisabled"] = bodyResourceConfigIsNSNBDisabled
					}
					if len(nestedResourceConfig) > 0 {
						body["resourceConfig"] = nestedResourceConfig
					}
				}
				if bodyRootDirectory != "" {
					body["rootDirectory"] = bodyRootDirectory
				}
				if bodyServerlessFunctionRegion != "" {
					body["serverlessFunctionRegion"] = bodyServerlessFunctionRegion
				}
				if bodyServerlessFunctionZeroConfigFailover != "" {
					body["serverlessFunctionZeroConfigFailover"] = bodyServerlessFunctionZeroConfigFailover
				}
				if cmd.Flags().Changed("skip-git-connect-during-link") {
					body["skipGitConnectDuringLink"] = bodySkipGitConnectDuringLink
				}
				{
					nestedSsoProtection := map[string]any{}
					if bodySsoProtectionDeploymentType != "" {
						nestedSsoProtection["deploymentType"] = bodySsoProtectionDeploymentType
					}
					if len(nestedSsoProtection) > 0 {
						body["ssoProtection"] = nestedSsoProtection
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "projects", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "projects", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "projects",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyBuildCommand, "build-command", "", "The build command for this project. When `null` is used this value will be automatically detected")
	cmd.Flags().StringVar(&bodyCommandForIgnoringBuildStep, "command-for-ignoring-build-step", "", "Command for ignoring build step")
	cmd.Flags().StringVar(&bodyDevCommand, "dev-command", "", "The dev command for this project. When `null` is used this value will be automatically detected")
	cmd.Flags().BoolVar(&bodyEnableAffectedProjectsDeployments, "enable-affected-projects-deployments", false, "Opt-in to skip deployments when there are no changes to the root directory and its dependencies")
	cmd.Flags().BoolVar(&bodyEnablePreviewFeedback, "enable-preview-feedback", false, "Opt-in to preview toolbar on the project level")
	cmd.Flags().BoolVar(&bodyEnableProductionFeedback, "enable-production-feedback", false, "Opt-in to production toolbar on the project level")
	cmd.Flags().StringVar(&bodyEnvironmentVariables, "environment-variables", "", "Collection of ENV Variables the Project will use")
	cmd.Flags().StringVar(&bodyFramework, "framework", "", "The framework that is being used for this project. When `null` is used no framework is selected")
	cmd.Flags().StringVar(&bodyGitRepositoryRepo, "git-repository-repo", "", "The name of the git repository. For example: 'vercel/next.js'")
	cmd.Flags().StringVar(&bodyGitRepositoryType, "git-repository-type", "", "The Git Provider of the repository")
	cmd.Flags().StringVar(&bodyInstallCommand, "install-command", "", "The install command for this project. When `null` is used this value will be automatically detected")
	cmd.Flags().StringVar(&bodyName, "name", "", "The desired name for the project")
	cmd.Flags().BoolVar(&bodyOidcTokenConfigEnabled, "oidc-token-config-enabled", false, "Whether or not to generate OpenID Connect JSON Web Tokens.")
	cmd.Flags().StringVar(&bodyOidcTokenConfigIssuerMode, "oidc-token-config-issuer-mode", "", "team: `https://oidc.vercel.com/[team_slug]` global: `https://oidc.vercel.com`")
	cmd.Flags().StringVar(&bodyOutputDirectory, "output-directory", "", "The output directory of the project. When `null` is used this value will be automatically detected")
	cmd.Flags().StringVar(&bodyPreviewDeploymentSuffix, "preview-deployment-suffix", "", "Custom domain suffix for preview deployments. Takes precedence over team-level suffix.")
	cmd.Flags().BoolVar(&bodyPreviewDeploymentsDisabled, "preview-deployments-disabled", false, "Specifies whether preview deployments are disabled for this project.")
	cmd.Flags().BoolVar(&bodyPublicSource, "public-source", false, "Deprecated. Accepted for backwards compatibility but ignored.")
	cmd.Flags().Float64Var(&bodyResourceConfigBuildMachineElasticLastUpdated, "resource-config-build-machine-elastic-last-updated", 0.0, "Build machine elastic last updated")
	cmd.Flags().StringVar(&bodyResourceConfigBuildMachineSelection, "resource-config-build-machine-selection", "", "Build machine selection")
	cmd.Flags().StringVar(&bodyResourceConfigBuildMachineType, "resource-config-build-machine-type", "", "Build machine type")
	cmd.Flags().StringVar(&bodyResourceConfigBuildQueueConfiguration, "resource-config-build-queue-configuration", "", "Configuration")
	cmd.Flags().BoolVar(&bodyResourceConfigElasticConcurrencyEnabled, "resource-config-elastic-concurrency-enabled", false, "Elastic concurrency enabled")
	cmd.Flags().BoolVar(&bodyResourceConfigEnableFunctionsBeta, "resource-config-enable-functions-beta", false, "Enable functions beta")
	cmd.Flags().BoolVar(&bodyResourceConfigFluid, "resource-config-fluid", false, "Fluid")
	cmd.Flags().StringVar(&bodyResourceConfigFunctionDefaultMemoryType, "resource-config-function-default-memory-type", "", "Function default memory type")
	cmd.Flags().StringVar(&bodyResourceConfigFunctionDefaultRegions, "resource-config-function-default-regions", "", "The regions to deploy Vercel Functions to for this project")
	cmd.Flags().Float64Var(&bodyResourceConfigFunctionDefaultTimeout, "resource-config-function-default-timeout", 0.0, "Function default timeout")
	cmd.Flags().StringVar(&bodyResourceConfigFunctionZeroConfigFailover, "resource-config-function-zero-config-failover", "", "Specifies whether Zero Config Failover is enabled for this project.")
	cmd.Flags().BoolVar(&bodyResourceConfigIsNSNBDisabled, "resource-config-is-nsnb-disabled", false, "Is nsnbdisabled")
	cmd.Flags().StringVar(&bodyRootDirectory, "root-directory", "", "The name of a directory or relative path to the source code of your project.")
	cmd.Flags().StringVar(&bodyServerlessFunctionRegion, "serverless-function-region", "", "The region to deploy Serverless Functions in this project")
	cmd.Flags().StringVar(&bodyServerlessFunctionZeroConfigFailover, "serverless-function-zero-config-failover", "", "Specifies whether Zero Config Failover is enabled for this project.")
	cmd.Flags().BoolVar(&bodySkipGitConnectDuringLink, "skip-git-connect-during-link", false, "Opts-out of the message prompting a CLI user to connect a Git repository in `vercel link`.")
	cmd.Flags().StringVar(&bodySsoProtectionDeploymentType, "sso-protection-deployment-type", "", "Deployment type")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
