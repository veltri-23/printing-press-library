// Copyright 2026 hnshah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newRegistrarBuyDomainsCmd(flags *rootFlags) *cobra.Command {
	var bodyContactInformationAdditional string
	var bodyContactInformationAddress1 string
	var bodyContactInformationAddress2 string
	var bodyContactInformationCity string
	var bodyContactInformationCompanyName string
	var bodyContactInformationCountry string
	var bodyContactInformationEmail string
	var bodyContactInformationFax string
	var bodyContactInformationFirstName string
	var bodyContactInformationLastName string
	var bodyContactInformationPhone string
	var bodyContactInformationState string
	var bodyContactInformationZip string
	var bodyDomains string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "buy-domains",
		Aliases:     []string{"create"},
		Short:       "Buy multiple domains at once",
		Example:     "  vercel-admin-pp-cli registrar buy-domains",
		Annotations: map[string]string{"pp:endpoint": "registrar.buy-domains", "pp:method": "POST", "pp:path": "/v1/registrar/domains/buy"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("contact-information-address1") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contact-information-address1")
				}
				if !cmd.Flags().Changed("contact-information-city") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contact-information-city")
				}
				if !cmd.Flags().Changed("contact-information-country") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contact-information-country")
				}
				if !cmd.Flags().Changed("contact-information-email") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contact-information-email")
				}
				if !cmd.Flags().Changed("contact-information-first-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contact-information-first-name")
				}
				if !cmd.Flags().Changed("contact-information-last-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contact-information-last-name")
				}
				if !cmd.Flags().Changed("contact-information-phone") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contact-information-phone")
				}
				if !cmd.Flags().Changed("contact-information-state") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contact-information-state")
				}
				if !cmd.Flags().Changed("contact-information-zip") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contact-information-zip")
				}
				if !cmd.Flags().Changed("domains") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "domains")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/registrar/domains/buy"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedContactInformation := map[string]any{}
					if bodyContactInformationAdditional != "" {
						var parsedContactInformationAdditional any
						if err := json.Unmarshal([]byte(bodyContactInformationAdditional), &parsedContactInformationAdditional); err != nil {
							return fmt.Errorf("parsing --contact-information-additional JSON: %w", err)
						}
						nestedContactInformation["additional"] = parsedContactInformationAdditional
					}
					if bodyContactInformationAddress1 != "" {
						nestedContactInformation["address1"] = bodyContactInformationAddress1
					}
					if bodyContactInformationAddress2 != "" {
						nestedContactInformation["address2"] = bodyContactInformationAddress2
					}
					if bodyContactInformationCity != "" {
						nestedContactInformation["city"] = bodyContactInformationCity
					}
					if bodyContactInformationCompanyName != "" {
						nestedContactInformation["companyName"] = bodyContactInformationCompanyName
					}
					if bodyContactInformationCountry != "" {
						nestedContactInformation["country"] = bodyContactInformationCountry
					}
					if bodyContactInformationEmail != "" {
						nestedContactInformation["email"] = bodyContactInformationEmail
					}
					if bodyContactInformationFax != "" {
						nestedContactInformation["fax"] = bodyContactInformationFax
					}
					if bodyContactInformationFirstName != "" {
						nestedContactInformation["firstName"] = bodyContactInformationFirstName
					}
					if bodyContactInformationLastName != "" {
						nestedContactInformation["lastName"] = bodyContactInformationLastName
					}
					if bodyContactInformationPhone != "" {
						nestedContactInformation["phone"] = bodyContactInformationPhone
					}
					if bodyContactInformationState != "" {
						nestedContactInformation["state"] = bodyContactInformationState
					}
					if bodyContactInformationZip != "" {
						nestedContactInformation["zip"] = bodyContactInformationZip
					}
					if len(nestedContactInformation) > 0 {
						body["contactInformation"] = nestedContactInformation
					}
				}
				if bodyDomains != "" {
					var parsedDomains any
					if err := json.Unmarshal([]byte(bodyDomains), &parsedDomains); err != nil {
						return fmt.Errorf("parsing --domains JSON: %w", err)
					}
					body["domains"] = parsedDomains
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "registrar", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "registrar", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "registrar", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "registrar", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "registrar", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "registrar",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "registrar", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "registrar", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyContactInformationAdditional, "contact-information-additional", "", "Additional")
	cmd.Flags().StringVar(&bodyContactInformationAddress1, "contact-information-address1", "", "a non empty string")
	cmd.Flags().StringVar(&bodyContactInformationAddress2, "contact-information-address2", "", "a non empty string")
	cmd.Flags().StringVar(&bodyContactInformationCity, "contact-information-city", "", "a non empty string")
	cmd.Flags().StringVar(&bodyContactInformationCompanyName, "contact-information-company-name", "", "a non empty string")
	cmd.Flags().StringVar(&bodyContactInformationCountry, "contact-information-country", "", "A valid ISO 3166-1 alpha-2 country code")
	cmd.Flags().StringVar(&bodyContactInformationEmail, "contact-information-email", "", "A valid RFC 5322 email address")
	cmd.Flags().StringVar(&bodyContactInformationFax, "contact-information-fax", "", "A valid E.164 phone number")
	cmd.Flags().StringVar(&bodyContactInformationFirstName, "contact-information-first-name", "", "a non empty string")
	cmd.Flags().StringVar(&bodyContactInformationLastName, "contact-information-last-name", "", "a non empty string")
	cmd.Flags().StringVar(&bodyContactInformationPhone, "contact-information-phone", "", "A valid E.164 phone number")
	cmd.Flags().StringVar(&bodyContactInformationState, "contact-information-state", "", "a non empty string")
	cmd.Flags().StringVar(&bodyContactInformationZip, "contact-information-zip", "", "a non empty string")
	cmd.Flags().StringVar(&bodyDomains, "domains", "", "Domains")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
