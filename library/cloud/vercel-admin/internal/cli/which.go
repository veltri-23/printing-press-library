// Copyright 2026 hnshah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "access-groups create", Description: "Allows to create an access group", Group: "access-groups"},
	{Command: "access-groups delete", Description: "Allows to delete an access group", Group: "access-groups"},
	{Command: "access-groups list", Description: "List access groups for a team, project or member", Group: "access-groups"},
	{Command: "access-groups read", Description: "Allows to read an access group", Group: "access-groups"},
	{Command: "access-groups update", Description: "Allows to update an access group metadata", Group: "access-groups"},
	{Command: "access-groups members list-access-group", Description: "List members of an access group", Group: "access-groups"},
	{Command: "access-groups projects create-access-group", Description: "Allows creation of an access group project", Group: "access-groups"},
	{Command: "access-groups projects delete-access-group", Description: "Allows deletion of an access group project", Group: "access-groups"},
	{Command: "access-groups projects list-access-group", Description: "List projects of an access group", Group: "access-groups"},
	{Command: "access-groups projects read-access-group", Description: "Allows reading an access group project", Group: "access-groups"},
	{Command: "access-groups projects update-access-group", Description: "Allows update of an access group project", Group: "access-groups"},
	{Command: "aliases delete-alias", Description: "Delete an Alias with the specified ID.", Group: "aliases"},
	{Command: "aliases get-alias", Description: "Retrieves an Alias for the given host name or alias ID.", Group: "aliases"},
	{Command: "aliases list", Description: "Retrieves a list of aliases for the authenticated User or Team. When `domain` is provided, only aliases for that domain will be returned. When `projectId` is provided, it will only return the given project aliases.", Group: "aliases"},
	{Command: "aliases protection-bypass patch-url", Description: "Update the protection bypass for the alias or deployment URL (used for user access & comment access for deployments). Used as shareable links and user scoped access for Vercel Authentication and also to allow external (logged in) people to comment on previews for Preview Comments (next-live-mode).", Group: "aliases"},
	{Command: "artifacts download", Description: "Downloads a cache artifact indentified by its `hash` specified on the request path. The artifact is downloaded as an octet-stream. The client should verify the content-length header and response body.", Group: "artifacts"},
	{Command: "artifacts exists", Description: "Check that a cache artifact with the given `hash` exists. This request returns response headers only and is equivalent to a `GET` request to this endpoint where the response contains no body.", Group: "artifacts"},
	{Command: "artifacts query", Description: "Query information about an array of artifacts.", Group: "artifacts"},
	{Command: "artifacts record-events", Description: "Records an artifacts cache usage event. The body of this request is an array of cache usage events. The supported event types are `HIT` and `MISS`. The source is either `LOCAL` the cache event was on the users filesystem cache or `REMOTE` if the cache event is for a remote cache. When the event is a `HIT` the request also accepts a number `duration` which is the time taken to generate the artifact in the cache.", Group: "artifacts"},
	{Command: "artifacts status", Description: "Check the status of Remote Caching for this principal. Returns a JSON-encoded status indicating if Remote Caching is enabled, disabled, or disabled due to usage limits.", Group: "artifacts"},
	{Command: "artifacts upload", Description: "Uploads a cache artifact identified by the `hash` specified on the path. The cache artifact can then be downloaded with the provided `hash`.", Group: "artifacts"},
	{Command: "billing buy-credits", Description: "Purchases credits for a Vercel team using the default payment method on file. The purchase is charged immediately via Stripe invoice. Supported credit types are `v0`, `gateway`, and `agent`. The `amount` field specifies the number of credits to purchase and must be a positive integer. An optional `source` query parameter can be provided to identify the caller. Defaults to `api` if not specified. This is only available for Owner, Member, Developer, Security, and Billing roles for the supplied team.", Group: "billing"},
	{Command: "billing list-charges", Description: "Returns the billing charge data in FOCUS v1.3 JSONL format for a specified Vercel team, within a date range specified by `from` and `to` query parameters. Supports 1-day granularity with a maximum date range of 1 year. The response is streamed as newline-delimited JSON (JSONL) and can be optionally compressed with gzip if the `Accept-Encoding: gzip` header is provided. This is only available for Owner, Member, Developer, Security, Billing, and Enterprise Viewer roles for the supplied team.", Group: "billing"},
	{Command: "billing list-contract-commitments", Description: "Returns commitment allocations per contract period in FOCUS v1.3 JSONL format for a specified Vercel team. The response is streamed as newline-delimited JSON (JSONL). This endpoint is only applicable to Enterprise Vercel customers. An empty response is returned for non-Enterprise (Pro/Flex) customers.", Group: "billing"},
	{Command: "bulk-redirects delete-redirects", Description: "Deletes the provided redirects from the latest version of the projects' bulk redirects. Stages a new change with the new redirects and returns the alias for the new version in the response.", Group: "bulk-redirects"},
	{Command: "bulk-redirects edit-redirect", Description: "Edits a single redirect identified by its source path. Stages a new change with the modified redirect and returns the alias for the new version in the response.", Group: "bulk-redirects"},
	{Command: "bulk-redirects get-redirects", Description: "Get the version history for a project's bulk redirects", Group: "bulk-redirects"},
	{Command: "bulk-redirects get-versions", Description: "Get the version history for a project's bulk redirects", Group: "bulk-redirects"},
	{Command: "bulk-redirects restore-redirects", Description: "Restores the provided redirects in the staging version to the value in the production version. If no production version exists, removes the redirects from staging.", Group: "bulk-redirects"},
	{Command: "bulk-redirects stage-redirects", Description: "Stages new redirects for a project and returns the new version.", Group: "bulk-redirects"},
	{Command: "bulk-redirects update-version", Description: "Update a version by promoting staging to production or restoring a previous production version", Group: "bulk-redirects"},
	{Command: "certs get-by-id", Description: "Get cert by id", Group: "certs"},
	{Command: "certs issue", Description: "Issue a new cert", Group: "certs"},
	{Command: "certs remove", Description: "Remove cert", Group: "certs"},
	{Command: "certs upload", Description: "Upload a cert", Group: "certs"},
	{Command: "connect create-connector", Description: "Create a connector from type-specific configuration and optionally link it to a project during creation.", Group: "connect"},
	{Command: "connect create-connector-authorization-request", Description: "Create an authorization request for a connector and return the URL and verifier details needed to complete the flow.", Group: "connect"},
	{Command: "connect create-network", Description: "Allows to create a Secure Compute network.", Group: "connect"},
	{Command: "connect delete-network", Description: "Allows to delete a Secure Compute network.", Group: "connect"},
	{Command: "connect get-connector-token", Description: "Get an access token for a connector identified by the path parameter and scoped to the requester.", Group: "connect"},
	{Command: "connect list-networks", Description: "Allows to list Secure Compute networks.", Group: "connect"},
	{Command: "connect read-network", Description: "Allows to read a Secure Compute network.", Group: "connect"},
	{Command: "connect update-network", Description: "Allows to update a Secure Compute network.", Group: "connect"},
	{Command: "deployments create", Description: "Create a new deployment with all the required and intended data. If the deployment is not a git deployment, all files must be provided with the request, either referenced or inlined. Additionally, a deployment id can be specified to redeploy a previous deployment.", Group: "deployments"},
	{Command: "deployments delete", Description: "This API allows you to delete a deployment, either by supplying its `id` in the URL or the `url` of the deployment as a query parameter. You can obtain the ID, for example, by listing all deployments.", Group: "deployments"},
	{Command: "deployments get", Description: "List deployments under the authenticated user or team. If a deployment hasn't finished uploading (is incomplete), the `url` property will have a value of `null`.", Group: "deployments"},
	{Command: "deployments get-idorurl", Description: "Retrieves information for a deployment either by supplying its ID (`id` property) or Hostname (`url` property). Additional details will be included when the authenticated user or team is an owner of the deployment.", Group: "deployments"},
	{Command: "deployments aliases assign-alias", Description: "Creates a new alias for the deployment resolved from the given deployment or alias ID or URL. The authenticated user or team must own this deployment. If the desired alias is already assigned to another deployment, then it will be removed from the old deployment and assigned to the new one.", Group: "deployments"},
	{Command: "deployments aliases list-deployment", Description: "Retrieves all Aliases for the Deployment with the given ID. The authenticated user or team must own the deployment.", Group: "deployments"},
	{Command: "deployments cancel deployment", Description: "This endpoint allows you to cancel a deployment which is currently building, by supplying its `id` in the URL.", Group: "deployments"},
	{Command: "deployments check-runs create-deployment", Description: "Creates a new check run for a deployment.", Group: "deployments"},
	{Command: "deployments check-runs get-deployment", Description: "Return a detailed response for a single check run.", Group: "deployments"},
	{Command: "deployments check-runs list-deployment", Description: "List all check runs for a deployment.", Group: "deployments"},
	{Command: "deployments check-runs update-deployment", Description: "Update an existing check run for a deployment.", Group: "deployments"},
	{Command: "deployments checks create", Description: "Creates a new check. This endpoint must be called with an OAuth2 or it will produce a 400 error.", Group: "deployments"},
	{Command: "deployments checks get", Description: "Return a detailed response for a single check.", Group: "deployments"},
	{Command: "deployments checks get-all", Description: "List all of the checks created for a deployment.", Group: "deployments"},
	{Command: "deployments checks rerequest", Description: "Rerequest a selected check that has failed.", Group: "deployments"},
	{Command: "deployments checks update", Description: "Update an existing check. This endpoint must be called with an OAuth2 or it will produce a 400 error.", Group: "deployments"},
	{Command: "deployments events get-deployment", Description: "Get the build logs of a deployment by deployment ID and build ID. It can work as an infinite stream of logs or as a JSON endpoint depending on the input parameters.", Group: "deployments"},
	{Command: "deployments feature-flags get-deployment", Description: "Retrieve the feature flags of a deployment.", Group: "deployments"},
	{Command: "deployments files get-deployment-contents", Description: "Allows to retrieve the content of a file by supplying the file identifier and the deployment unique identifier. The response body will contain a JSON response containing the contents of the file encoded as base64.", Group: "deployments"},
	{Command: "deployments files list-deployment", Description: "Allows to retrieve the file structure of the source code of a deployment by supplying the deployment unique identifier. If the deployment was created with the Vercel CLI or the API directly with the `files` key, it will have a file tree that can be retrievable.", Group: "deployments"},
	{Command: "deployments integrations update-deployment-action", Description: "Updates the deployment integration action for the specified integration installation", Group: "deployments"},
	{Command: "domains create-or-transfer", Description: "This endpoint is used for adding a new apex domain name with Vercel for the authenticating user. Note: This endpoint is no longer used for initiating domain transfers from external registrars to Vercel. For this, please use the endpoint [Transfer-in a domain](https://vercel.com/docs/rest-api/reference/endpoints/domains-registrar/transfer-in-a-domain).", Group: "domains"},
	{Command: "domains delete", Description: "Delete a previously registered domain name from Vercel. Deleting a domain will automatically remove any associated aliases.", Group: "domains"},
	{Command: "domains get", Description: "Retrieves a list of domains registered for the authenticated user or team. By default it returns the last 20 domains if no limit is provided.", Group: "domains"},
	{Command: "domains get-domain", Description: "Get information for a single domain in an account or team.", Group: "domains"},
	{Command: "domains patch", Description: "Update or move apex domain. Note: This endpoint is no longer used for updating auto-renew or nameservers. For this, please use the endpoints [Update auto-renew for a domain](https://vercel.com/docs/rest-api/reference/endpoints/domains-registrar/update-auto-renew-for-a-domain) and [Update nameservers for a domain](https://vercel.com/docs/rest-api/reference/endpoints/domains-registrar/update-nameservers-for-a-domain).", Group: "domains"},
	{Command: "domains update-record", Description: "Updates an existing DNS record for a domain name.", Group: "domains"},
	{Command: "domains claim domain-ownership", Description: "Claim ownership of a domain for the authenticated team by verifying a TXT record. The caller must first add a TXT record to `_vercel.{domain}` (obtained from GET /domains/:domain/verification), then call this endpoint to complete the ownership transfer. If the TXT record is verified, the domain ownership will be transferred to the caller's team, even if the domain is currently owned by another user or team.", Group: "domains"},
	{Command: "domains config get-domain", Description: "Get a Domain's configuration.", Group: "domains"},
	{Command: "domains project-domains get-domain", Description: "List all project domains associated with an apex domain owned by the authenticated account.", Group: "domains"},
	{Command: "domains records create", Description: "Creates a DNS record for a domain.", Group: "domains"},
	{Command: "domains records get", Description: "Retrieves a list of DNS records created for a domain name. By default it returns 20 records if no limit is provided. The rest can be retrieved using the pagination options.", Group: "domains"},
	{Command: "domains records remove", Description: "Removes an existing DNS record from a domain name.", Group: "domains"},
	{Command: "domains verification get-domain-record", Description: "Get the TXT verification record needed to claim ownership of a domain for the authenticated team. The caller must add this TXT record to `_vercel.{domain}` in their DNS configuration, then call POST /domains/:domain/claim to complete the ownership transfer.", Group: "domains"},
	{Command: "drains create", Description: "Create a new Drain with the provided configuration.", Group: "drains"},
	{Command: "drains delete", Description: "Delete a specific Drain by passing the drain id in the URL.", Group: "drains"},
	{Command: "drains get", Description: "Allows to retrieve the list of Drains of the authenticated team.", Group: "drains"},
	{Command: "drains get-id", Description: "Get the information for a specific Drain by passing the drain id in the URL.", Group: "drains"},
	{Command: "drains test", Description: "Validate the delivery configuration of a Drain using sample events.", Group: "drains"},
	{Command: "drains update", Description: "Update the configuration of an existing drain.", Group: "drains"},
	{Command: "edge-cache dangerously-delete-by-src-images", Description: "Marks a source image as deleted, causing cache entries associated with that source image to be revalidated in the foreground on the next request. Use this method with caution because one source image can be associated with many paths and deleting the cache can cause many concurrent requests to the origin leading to cache stampede problem. This method is for advanced use cases and is not recommended; prefer using `invalidateBySrcImage` instead.", Group: "edge-cache"},
	{Command: "edge-cache dangerously-delete-by-tags", Description: "Marks a cache tag as deleted, causing cache entries associated with that tag to be revalidated in the foreground on the next request. Use this method with caution because one tag can be associated with many paths and deleting the cache can cause many concurrent requests to the origin leading to cache stampede problem. This method is for advanced use cases and is not recommended; prefer using `invalidateByTag` instead.", Group: "edge-cache"},
	{Command: "edge-cache invalidate-by-src-images", Description: "Marks a source image as stale, causing its corresponding transformed images to be revalidated in the background on the next request.", Group: "edge-cache"},
	{Command: "edge-cache invalidate-by-tags", Description: "Marks a cache tag as stale, causing cache entries associated with that tag to be revalidated in the background on the next request.", Group: "edge-cache"},
	{Command: "edge-config create", Description: "Creates an Edge Config.", Group: "edge-config"},
	{Command: "edge-config delete", Description: "Delete an Edge Config by id.", Group: "edge-config"},
	{Command: "edge-config get", Description: "Returns all Edge Configs.", Group: "edge-config"},
	{Command: "edge-config get-edgeconfig", Description: "Returns an Edge Config.", Group: "edge-config"},
	{Command: "edge-config update", Description: "Updates an Edge Config.", Group: "edge-config"},
	{Command: "edge-config backups get-edge-config", Description: "Returns backups of an Edge Config.", Group: "edge-config"},
	{Command: "edge-config backups get-edge-config-edgeconfig", Description: "Retrieves a specific version of an Edge Config from backup storage.", Group: "edge-config"},
	{Command: "edge-config item get-edge-config", Description: "Returns a specific Edge Config Item.", Group: "edge-config"},
	{Command: "edge-config items get-edge-config", Description: "Returns all items of an Edge Config.", Group: "edge-config"},
	{Command: "edge-config items patch-edge-config", Description: "Update multiple Edge Config Items in batch.", Group: "edge-config"},
	{Command: "edge-config schema delete-edge-config", Description: "Deletes the schema of existing Edge Config.", Group: "edge-config"},
	{Command: "edge-config schema get-edge-config", Description: "Returns the schema of an Edge Config.", Group: "edge-config"},
	{Command: "edge-config schema patch-edge-config", Description: "Update an Edge Config's schema.", Group: "edge-config"},
	{Command: "edge-config token create-edge-config", Description: "Adds a token to an existing Edge Config.", Group: "edge-config"},
	{Command: "edge-config token get-edge-config", Description: "Return meta data about an Edge Config token.", Group: "edge-config"},
	{Command: "edge-config tokens delete-edge-config", Description: "Deletes one or more tokens of an existing Edge Config.", Group: "edge-config"},
	{Command: "edge-config tokens get-edge-config", Description: "Returns all tokens of an Edge Config.", Group: "edge-config"},
	{Command: "env create-shared-variable", Description: "Creates shared environment variable(s) for a team.", Group: "env"},
	{Command: "env delete-shared-variable", Description: "Deletes one or many Shared Environment Variables for a given team.", Group: "env"},
	{Command: "env get-shared-var", Description: "Retrieve the decrypted value of a Shared Environment Variable by id.", Group: "env"},
	{Command: "env list-shared-variable", Description: "Lists all Shared Environment Variables for a team, taking into account optional filters.", Group: "env"},
	{Command: "env update-shared-variable", Description: "Updates a given Shared Environment Variable for a Team.", Group: "env"},
	{Command: "env unlink shared-env-variable", Description: "Disconnects a shared environment variable for a given project", Group: "env"},
	{Command: "events list-types", Description: "Returns the list of user-facing event types with descriptions.", Group: "events"},
	{Command: "events list-user", Description: "Retrieves a list of \"events\" generated by the User on Vercel. Events are generated when the User performs a particular action, such as logging in, creating a deployment, and joining a Team (just to name a few). When the `teamId` parameter is supplied, then the events that are returned will be in relation to the Team that was specified.", Group: "events"},
	{Command: "files upload", Description: "Before you create a deployment you need to upload the required files for that deployment. To do it, you need to first upload each file to this endpoint. Once that's completed, you can create a new deployment with the uploaded files. The file content must be placed inside the body of the request. In the case of a successful response you'll receive a status code 200 with an empty body.", Group: "files"},
	{Command: "installations update", Description: "This endpoint updates an integration installation.", Group: "installations"},
	{Command: "installations account get-info", Description: "Fetches the best account or user’s contact info", Group: "installations"},
	{Command: "installations billing finalize-installation", Description: "This endpoint allows the partner to mark an installation as finalized. This means you will not send any more invoices for the installation. Use this after a customer has requested uninstall and you have sent any remaining invoices. This will allow the uninstall process to proceed immediately after all invoices have been paid. <br/> Use the `credentials.access_token` we provided in the [Upsert Installation](#upsert-installation) body to authorize this request.", Group: "installations"},
	{Command: "installations billing get-invoice", Description: "Get Invoice details and status for a given invoice ID.<br/> <br/> See [Billing Events with Webhooks documentation](https://vercel.com/docs/integrations/create-integration/marketplace-api#working-with-billing-events-through-webhooks) on how to receive invoice events. This endpoint is used to retrieve the invoice details.", Group: "installations"},
	{Command: "installations billing submit-data", Description: "Sends the billing and usage data. The partner should do this at least once a day and ideally once per hour. <br/> Use the `credentials.access_token` we provided in the [Upsert Installation](#upsert-installation) body to authorize this request.", Group: "installations"},
	{Command: "installations billing submit-invoice", Description: "This endpoint allows the partner to submit an invoice to Vercel. The invoice is created in Vercel's billing system and sent to the customer. Depending on the type of billing plan, the invoice can be sent at a time of signup, at the start of the billing period, or at the end of the billing period.<br/> <br/> Use the `credentials.access_token` we provided in the [Upsert Installation](#upsert-installation) body to authorize this request. <br/> There are several limitations to the invoice submission:<br/> <br/> 1. A resource can only be billed once per the billing period and the billing plan.<br/> 2. The billing plan used to bill the resource must have been active for this resource during the billing period.<br/> 3. The billing plan used must be a subscription plan.<br/> 4. The interim usage data must be sent hourly for all types of subscriptions. See [Send subscription billing and usage data](#send-subscription-billing-and-usage-data) API on how to send interim billing and usage data.<br/> 5. If provided, `externalId` must be unique for the installation.<br/>", Group: "installations"},
	{Command: "installations billing submit-prepayment-balances", Description: "Sends the prepayment balances. The partner should do this at least once a day and ideally once per hour. <br/> Use the `credentials.access_token` we provided in the [Upsert Installation](#upsert-installation) body to authorize this request.", Group: "installations"},
	{Command: "installations billing update-invoice", Description: "This endpoint allows the partner to request a refund for an invoice to Vercel. The invoice is created using the [Submit Invoice API](#submit-invoice-api).", Group: "installations"},
	{Command: "installations events create", Description: "Partner notifies Vercel of any changes made to an Installation or a Resource. Vercel is expected to use `list-resources` and other read APIs to get the new state.<br/> <br/> `resource.updated` event should be dispatched when any state of a resource linked to Vercel is modified by the partner.<br/> `installation.updated` event should be dispatched when an installation's billing plan is changed via the provider instead of Vercel.<br/> <br/> Resource update use cases: <br/> <br/> - The user renames a database in the partner’s application. The partner should dispatch a `resource.updated` event to notify Vercel to update the resource in Vercel’s datastores.<br/> - A resource has been suspended due to a lack of use. The partner should dispatch a `resource.updated` event to notify Vercel to update the resource's status in Vercel's datastores.<br/>", Group: "installations"},
	{Command: "installations member get", Description: "Returns the member role and other information for a given member ID (\"user_id\" claim in the SSO OIDC token).", Group: "installations"},
	{Command: "installations products update-resource-secrets", Description: "This endpoint is deprecated and replaced with the endpoint [Update Resource Secrets](#update-resource-secrets). <br/> This endpoint updates the secrets of a resource. If a resource has projects connected, the connected secrets are updated with the new secrets. The old secrets may still be used by existing connected projects because they are not automatically redeployed. Redeployment is a manual action and must be completed by the user. All new project connections will use the new secrets.<br/> <br/> Use cases for this endpoint:<br/> <br/> - Resetting the credentials of a database in the partner. If the user requests the credentials to be updated in the partner’s application, the partner post the new set of secrets to Vercel, the user should redeploy their application and the expire the old credentials.<br/>", Group: "installations"},
	{Command: "installations resources create-installations-by-integration-configuration-id-by-id-experimentation-items", Description: "Create one or multiple experimentation items", Group: "installations"},
	{Command: "installations resources delete-installations-by-integration-configuration-id-by-id-experimentation-items-by-item-id", Description: "Delete an existing experimentation item", Group: "installations"},
	{Command: "installations resources delete-integration", Description: "Delete a resource owned by the selected installation ID.", Group: "installations"},
	{Command: "installations resources get-installations-by-integration-configuration-id-by-id-experimentation-edge-config", Description: "When the user enabled Edge Config syncing, then this endpoint can be used by the partner to fetch the contents of the Edge Config.", Group: "installations"},
	{Command: "installations resources get-integration", Description: "Get all resources for a given installation ID.", Group: "installations"},
	{Command: "installations resources get-integration-installations", Description: "Get a resource by its partner ID.", Group: "installations"},
	{Command: "installations resources head-installations-by-integration-configuration-id-by-id-experimentation-edge-config", Description: "When the user enabled Edge Config syncing, then this endpoint can be used by the partner to fetch the contents of the Edge Config.", Group: "installations"},
	{Command: "installations resources import", Description: "This endpoint imports (upserts) a resource to Vercel's installation. This may be needed if resources can be independently created on the partner's side and need to be synchronized to Vercel. When importing as part of the user-initiated import flow, call this endpoint before redirecting the user back to Vercel. See the [Import existing resources flow](https://vercel.com/docs/integrations/create-integration/marketplace-flows#import-existing-resources-flow) for the full contract.", Group: "installations"},
	{Command: "installations resources replace-installations-by-integration-configuration-id-by-id-experimentation-edge-config", Description: "When the user enabled Edge Config syncing, then this endpoint can be used by the partner to push their configuration data into the relevant Edge Config.", Group: "installations"},
	{Command: "installations resources update", Description: "This endpoint updates an existing resource in the installation. All parameters are optional, allowing partial updates.", Group: "installations"},
	{Command: "installations resources update-installations-by-integration-configuration-id-by-id-experimentation-items-by-item-id", Description: "Patch an existing experimentation item", Group: "installations"},
	{Command: "installations resources update-secrets-by-id", Description: "This endpoint updates the secrets of a resource. If a resource has projects connected, the connected secrets are updated with the new secrets. The old secrets may still be used by existing connected projects because they are not automatically redeployed. Redeployment is a manual action and must be completed by the user. All new project connections will use the new secrets.<br/> <br/> Use cases for this endpoint:<br/> <br/> - Resetting the credentials of a database in the partner. If the user requests the credentials to be updated in the partner’s application, the partner post the new set of secrets to Vercel, the user should redeploy their application and the expire the old credentials.<br/>", Group: "installations"},
	{Command: "integrations connect-resource-to-project", Description: "Connects an integration resource to a Vercel project. This endpoint establishes a connection between a provisioned integration resource (from storage APIs like `POST /v1/storage/stores/integration/direct`) and a specific Vercel project.", Group: "integrations"},
	{Command: "integrations create-log-drain", Description: "Creates an Integration log drain. This endpoint must be called with an OAuth2 client (integration), since log drains are tied to integrations. If it is called with a different token type it will produce a 400 error.", Group: "integrations"},
	{Command: "integrations delete-configuration", Description: "Allows to remove the configuration with the `id` provided in the parameters. The configuration and all of its resources will be removed. This includes Webhooks, LogDrains and Project Env variables.", Group: "integrations"},
	{Command: "integrations delete-log-drain", Description: "Deletes the Integration log drain with the provided `id`. When using an OAuth2 Token, the log drain can be deleted only if the integration owns it.", Group: "integrations"},
	{Command: "integrations exchange-sso-token", Description: "During the autorization process, Vercel sends the user to the provider [redirectLoginUrl](https://vercel.com/docs/integrations/create-integration/submit-integration#redirect-login-url), that includes the OAuth authorization `code` parameter. The provider then calls the SSO Token Exchange endpoint with the sent code and receives the OIDC token. They log the user in based on this token and redirects the user back to the Vercel account using deep-link parameters included the redirectLoginUrl. Providers should not persist the returned `id_token` in a database since the token will expire. See [**Authentication with SSO**](https://vercel.com/docs/integrations/create-integration/marketplace-api#authentication-with-sso) for more details.", Group: "integrations"},
	{Command: "integrations get-billing-plans", Description: "Get a list of billing plans for an integration and product.", Group: "integrations"},
	{Command: "integrations get-configuration", Description: "Allows to retrieve a the configuration with the provided id in case it exists. The authenticated user or team must be the owner of the config in order to access it.", Group: "integrations"},
	{Command: "integrations get-configuration-products", Description: "Returns products available for an integration configuration. Each product includes a `metadataSchema` field with the JSON Schema for required and optional metadata fields.", Group: "integrations"},
	{Command: "integrations get-configurations", Description: "Allows to retrieve all configurations for an authenticated integration. When the `project` view is used, configurations generated for the authorization flow will be filtered out of the results.", Group: "integrations"},
	{Command: "integrations get-log-drains", Description: "Retrieves a list of all Integration log drains that are defined for the authenticated user or team. When using an OAuth2 token, the list is limited to log drains created by the authenticated integration.", Group: "integrations"},
	{Command: "integrations git-namespaces", Description: "Lists git namespaces for a supported provider. Supported providers are `github`, `gitlab` and `bitbucket`. If the provider is not provided, it will try to obtain it from the user that authenticated the request.", Group: "integrations"},
	{Command: "integrations search-repo", Description: "Lists git repositories linked to a namespace `id` for a supported provider. A specific namespace `id` can be obtained via the `git-namespaces`  endpoint. Supported providers are `github`, `gitlab` and `bitbucket`. If the provider or namespace is not provided, it will try to obtain it from the user that authenticated the request.", Group: "integrations"},
	{Command: "log-drains create-configurable", Description: "Creates a configurable log drain. This endpoint must be called with a team AccessToken (integration OAuth2 clients are not allowed)", Group: "log-drains"},
	{Command: "log-drains delete-configurable", Description: "Deletes a Configurable Log Drain. This endpoint must be called with a team AccessToken (integration OAuth2 clients are not allowed). Only log drains owned by the authenticated team can be deleted.", Group: "log-drains"},
	{Command: "log-drains get-all", Description: "Retrieves a list of all the Log Drains owned by the account. This endpoint must be called with an account AccessToken (integration OAuth2 clients are not allowed). Only log drains owned by the authenticated account can be accessed.", Group: "log-drains"},
	{Command: "log-drains get-configurable", Description: "Retrieves a Configurable Log Drain. This endpoint must be called with a team AccessToken (integration OAuth2 clients are not allowed). Only log drains owned by the authenticated team can be accessed.", Group: "log-drains"},
	{Command: "microfrontends create-group-with-applications", Description: "Creates a microfrontends group and attaches multiple projects in a single request.", Group: "microfrontends"},
	{Command: "microfrontends get-config-for-project", Description: "Get the microfrontends config for a project by ID or name.", Group: "microfrontends"},
	{Command: "microfrontends get-groups", Description: "Get the microfrontends group IDs for a team.", Group: "microfrontends"},
	{Command: "microfrontends get-in-group", Description: "Get the microfrontends for a given group ID.", Group: "microfrontends"},
	{Command: "microfrontends config get-microfrontends", Description: "Get the microfrontends config for a deployment.", Group: "microfrontends"},
	{Command: "observability get-configuration-projects", Description: "Lists the projects that are currently configured as disabled for Observability Plus on a team.", Group: "observability"},
	{Command: "observability update-configuration-project", Description: "Updates whether Observability Plus is disabled for a single project.", Group: "observability"},
	{Command: "projects accept-transfer-request", Description: "Accept a project transfer request initated by another team. <br/> The `code` is generated using the `POST /projects/:idOrName/transfer-request` endpoint.", Group: "projects"},
	{Command: "projects create", Description: "Allows to create a new project with the provided configuration. It only requires the project `name` but more configuration can be provided to override the defaults.", Group: "projects"},
	{Command: "projects delete", Description: "Delete a specific project by passing either the project `id` or `name` in the URL.", Group: "projects"},
	{Command: "projects get", Description: "Allows to retrieve the list of projects of the authenticated user or team. The list will be paginated and the provided query parameters allow filtering the returned projects.", Group: "projects"},
	{Command: "projects get-idorname", Description: "Get the information for a specific project by passing either the project `id` or `name` in the URL.", Group: "projects"},
	{Command: "projects update", Description: "Update the fields of a project using either its `name` or `id`.", Group: "projects"},
	{Command: "projects avatar upload-project", Description: "Upload an image as the avatar of the project identified by `idOrName`. The request body is the raw bytes of a JPG, PNG, or SVG image; the `Content-Type` header must declare which. SVG payloads are sanitized and optimized server-side before storage. The final SHA-1 of the stored bytes becomes the project's `avatar` value. The actual upload pipeline (validation, sanitization, S3 write, conditional `updateProject`, and event emission) lives in the shared `@api/project-avatar-upload` helper so it can be reused by background workers.", Group: "projects"},
	{Command: "projects checks create-project", Description: "Creates a new check for a project.", Group: "projects"},
	{Command: "projects checks delete-project", Description: "Delete an existing check and all of its runs.", Group: "projects"},
	{Command: "projects checks get-project", Description: "Return a detailed response for a single check.", Group: "projects"},
	{Command: "projects checks list-project", Description: "List all checks for a project, optionally filtered by target.", Group: "projects"},
	{Command: "projects checks list-runs", Description: "List all runs associated with a given check.", Group: "projects"},
	{Command: "projects checks update-project", Description: "Update an existing check.", Group: "projects"},
	{Command: "projects custom-environments create", Description: "Creates a custom environment for the current project. Cannot be named 'Production' or 'Preview'.", Group: "projects"},
	{Command: "projects custom-environments get", Description: "Retrieve a custom environment for the project. Must not be named 'Production' or 'Preview'.", Group: "projects"},
	{Command: "projects custom-environments get-projects-by-id-or-name", Description: "Retrieve custom environments for the project. Must not be named 'Production' or 'Preview'.", Group: "projects"},
	{Command: "projects custom-environments remove", Description: "Remove a custom environment for the project. Must not be named 'Production' or 'Preview'.", Group: "projects"},
	{Command: "projects custom-environments update", Description: "Update a custom environment for the project. Must not be named 'Production' or 'Preview'.", Group: "projects"},
	{Command: "projects deployments get-runtime-logs", Description: "Returns a stream of logs for a given deployment.", Group: "projects"},
	{Command: "projects domains add-project", Description: "Add a domain to the project by passing its domain name and by specifying the project by either passing the project `id` or `name` in the URL. If the domain is not yet verified to be used on this project, the request will return `verified = false`, and the domain will need to be verified according to the `verification` challenge via `POST /projects/:idOrName/domains/:domain/verify`. If the domain already exists on the project, the request will fail with a `400` status code.", Group: "projects"},
	{Command: "projects domains get-project", Description: "Retrieve the domains associated with a given project by passing either the project `id` or `name` in the URL.", Group: "projects"},
	{Command: "projects domains get-project-projects", Description: "Get project domain by project id/name and domain name.", Group: "projects"},
	{Command: "projects domains move-project", Description: "Move one project's domain to another project. Also allows the move of all redirects pointed to that domain in the same project.", Group: "projects"},
	{Command: "projects domains remove-project", Description: "Remove a domain from a project by passing the domain name and by specifying the project by either passing the project `id` or `name` in the URL.", Group: "projects"},
	{Command: "projects domains update-project", Description: "Update a project domain's configuration, including the name, git branch and redirect of the domain.", Group: "projects"},
	{Command: "projects domains verify-project", Description: "Attempts to verify a project domain with `verified = false` by checking the correctness of the project domain's `verification` challenge.", Group: "projects"},
	{Command: "projects env batch-remove-project", Description: "Delete multiple environment variables for a given project in a single batch operation.", Group: "projects"},
	{Command: "projects env create-project", Description: "Create one or more environment variables for a project by passing its `key`, `value`, `type` and `target` and by specifying the project by either passing the project `id` or `name` in the URL. If you include `upsert=true` as a query parameter, a new environment variable will not be created if it already exists but, the existing variable's value will be updated.", Group: "projects"},
	{Command: "projects env edit-project", Description: "Edit a specific environment variable for a given project by passing the environment variable identifier and either passing the project `id` or `name` in the URL.", Group: "projects"},
	{Command: "projects env filter-project", Description: "Retrieve the environment variables for a given project by passing either the project `id` or `name` in the URL.", Group: "projects"},
	{Command: "projects env get-project", Description: "Retrieve the decrypted value of an environment variable of a project by id", Group: "projects"},
	{Command: "projects env remove-project", Description: "Delete a specific environment variable for a given project by passing the environment variable identifier and either passing the project `id` or `name` in the URL.", Group: "projects"},
	{Command: "projects feature-flags create-flag", Description: "Create a new feature flag for a project. The flag must have a unique slug within the project and specify its kind (boolean, string, number, or json).", Group: "projects"},
	{Command: "projects feature-flags create-flag-segment", Description: "Create a new feature flag segment.", Group: "projects"},
	{Command: "projects feature-flags create-sdk-key", Description: "Creates an SDK key.", Group: "projects"},
	{Command: "projects feature-flags delete-flag", Description: "Permanently delete a feature flag from the project. This action cannot be undone. Consider archiving the flag instead if you may need it in the future.", Group: "projects"},
	{Command: "projects feature-flags delete-flag-segment", Description: "Delete a feature flag segment.", Group: "projects"},
	{Command: "projects feature-flags delete-sdk-key", Description: "Deletes an SDK key.", Group: "projects"},
	{Command: "projects feature-flags get-flag", Description: "Retrieve a specific feature flag by its ID or slug.", Group: "projects"},
	{Command: "projects feature-flags get-flag-segment", Description: "Retrieve a feature flag segment by ID or slug.", Group: "projects"},
	{Command: "projects feature-flags get-flag-settings", Description: "Retrieve feature flag settings for a project.", Group: "projects"},
	{Command: "projects feature-flags get-sdk-keys", Description: "Gets all SDK keys for a project.", Group: "projects"},
	{Command: "projects feature-flags list-flag-segments", Description: "List all feature flag segments for a project.", Group: "projects"},
	{Command: "projects feature-flags list-flag-versions", Description: "Lists flag versions for a given flag.", Group: "projects"},
	{Command: "projects feature-flags list-flags", Description: "Retrieve feature flags for a project. The list can be filtered by state and supports pagination.", Group: "projects"},
	{Command: "projects feature-flags list-flags-v2", Description: "Retrieve feature flags for a project. Returns an opaque cursor for pagination.", Group: "projects"},
	{Command: "projects feature-flags update-flag", Description: "Update an existing feature flag. This endpoint supports partial updates, allowing you to modify specific properties like variants, environments, or state without providing the full flag configuration.", Group: "projects"},
	{Command: "projects feature-flags update-flag-segment", Description: "Update an existing feature flag segment.", Group: "projects"},
	{Command: "projects feature-flags update-flag-settings", Description: "Update feature flag settings for a project.", Group: "projects"},
	{Command: "projects members add-project", Description: "Adds a new member to the project.", Group: "projects"},
	{Command: "projects members get-project", Description: "Lists all members of a project.", Group: "projects"},
	{Command: "projects members remove-project", Description: "Remove a member from a specific project", Group: "projects"},
	{Command: "projects microfrontends update", Description: "Update the microfrontends settings for a project.", Group: "projects"},
	{Command: "projects pause project", Description: "Pause a project by passing its project `id` in the URL. If the project does not exist given the id then the request will fail with 400 status code. If the project disables auto assigning custom production domains and blocks the active Production Deployment then the request will return with 200 status code.", Group: "projects"},
	{Command: "projects promote list-aliases", Description: "Get a list of aliases related to the last promote request with their mapping status", Group: "projects"},
	{Command: "projects promote request", Description: "Allows users to promote a deployment to production. Note: This does NOT rebuild the deployment. If you need that, then call create-deployments endpoint.", Group: "projects"},
	{Command: "projects protection-bypass update-project", Description: "Update the deployment protection automation bypass for a project", Group: "projects"},
	{Command: "projects rollback request", Description: "Point production traffic to a previous production deployment by ID", Group: "projects"},
	{Command: "projects rollback update-projects-by-project-id-by-deployment-id-update-description", Description: "Updates the reason for a rollback, without changing the rollback status itself.", Group: "projects"},
	{Command: "projects rolling-release approve-stage", Description: "Advance a rollout to the next stage. This is only needed when rolling releases is configured to require manual approval.", Group: "projects"},
	{Command: "projects rolling-release complete", Description: "Force-complete a Rolling Release. The canary deployment will begin serving 100% of the traffic.", Group: "projects"},
	{Command: "projects rolling-release delete-config", Description: "Disable Rolling Releases for a project means that future deployments will not undergo a rolling release. Changing the config never alters a rollout that's already in-flight—it only affects the next production deployment. If you want to also stop the current rollout, call this endpoint to disable the feature, and then call either the /complete or /abort endpoint.", Group: "projects"},
	{Command: "projects rolling-release get", Description: "Return the Rolling Release for a project, regardless of whether the rollout is active, aborted, or completed. If the feature is enabled but no deployment has occurred yet, null will be returned.", Group: "projects"},
	{Command: "projects rolling-release get-billing-status", Description: "Get the Rolling Releases billing status for a project. The team level billing status is used to determine if the project can be configured for rolling releases.", Group: "projects"},
	{Command: "projects rolling-release get-config", Description: "Get the Rolling Releases configuration for a project. The project-level config is simply a template that will be used for any future rolling release, and not the configuration for any active rolling release.", Group: "projects"},
	{Command: "projects rolling-release update-config", Description: "Update (or disable) Rolling Releases for a project. When disabling with the resolve-on-disable feature flag enabled, any active rolling release document is resolved using the disableRolloutAction parameter: \"abort\" to roll back (default), or \"complete\" to promote the canary to production. When enabling or updating config, changes only affect the next production deployment and do not alter a rollout that's already in-flight. Note: Enabling Rolling Releases automatically enables skew protection on the project with the default value if it wasn't configured already.", Group: "projects"},
	{Command: "projects routes add", Description: "Add a single routing rule to a project at a specified position. Defaults to the end of the list if no position is provided. The route is enabled by default. Stages a new version with the added route.", Group: "projects"},
	{Command: "projects routes delete", Description: "Delete one or more routing rules from a project by ID. Stages a new version with the routes removed.", Group: "projects"},
	{Command: "projects routes edit", Description: "Replace a routing rule identified by its ID, or restore it from the current production version. Stages a new version with the modified route.", Group: "projects"},
	{Command: "projects routes generate", Description: "Generate a routing rule configuration from a natural language description. Returns a suggested route configuration that can be reviewed and saved.", Group: "projects"},
	{Command: "projects routes get", Description: "Get the routing rules for a project. Supports searching by name/ID/pattern, filtering by route type, and diffing staged changes against production.", Group: "projects"},
	{Command: "projects routes get-versions", Description: "Get the version history for a project's routing rules. Returns the staging version (if one exists) followed by production versions, most recent first. The staging version has `isStaging: true` and the current production version has `isLive: true`.", Group: "projects"},
	{Command: "projects routes stage", Description: "Stage routing rules for a project. Set `overwrite` to true to replace all existing rules, or omit it to merge with existing rules by ID. Returns the new staged version.", Group: "projects"},
	{Command: "projects routes update-versions", Description: "Promote staged routing rules to production, restore a previous production version, or discard staged changes. - `promote`: Publishes the staging version to production. - `restore`: Rolls back to a previous production version. - `discard`: Removes the staging version without publishing.", Group: "projects"},
	{Command: "projects shared-connect-links update-static-ips", Description: "Allows configuring Static IPs for a project", Group: "projects"},
	{Command: "projects transfer-request create-project", Description: "Initiates a project transfer request from one team to another. <br/> Returns a `code` that remains valid for 24 hours and can be used to accept the transfer request by another team using the `PUT /projects/transfer-request/:code` endpoint. <br/> Users can also accept the project transfer request using the claim URL: `https://vercel.com/claim-deployment?code=<code>&returnUrl=<returnUrl>`. <br/> The `code` parameter specifies the project transfer request code generated using this endpoint. <br/> The `returnUrl` parameter redirects users to a specific page of the application if the claim URL is invalid or expired.", Group: "projects"},
	{Command: "projects unpause project", Description: "Unpause a project by passing its project `id` in the URL. If the project does not exist given the id then the request will fail with 400 status code. If the project enables auto assigning custom production domains and unblocks the active Production Deployment then the request will return with 200 status code.", Group: "projects"},
	{Command: "registrar buy-domains", Description: "Buy multiple domains at once", Group: "registrar"},
	{Command: "registrar buy-single-domain", Description: "Buy a domain", Group: "registrar"},
	{Command: "registrar get-bulk-availability", Description: "Get availability for multiple domains. If the domains are available, they can be purchased using the [Buy a domain](https://vercel.com/docs/rest-api/reference/endpoints/domains-registrar/buy-a-domain) endpoint or the [Buy multiple domains](https://vercel.com/docs/rest-api/reference/endpoints/domains-registrar/buy-multiple-domains) endpoint.", Group: "registrar"},
	{Command: "registrar get-contact-info-schema", Description: "Some TLDs require additional contact information. Use this endpoint to get the schema for the tld-specific contact information for a domain.", Group: "registrar"},
	{Command: "registrar get-domain-auth-code", Description: "Get the auth code for a domain. This is required to transfer a domain from Vercel to another registrar.", Group: "registrar"},
	{Command: "registrar get-domain-availability", Description: "Get availability for a specific domain. If the domain is available, it can be purchased using the [Buy a domain](https://vercel.com/docs/rest-api/reference/endpoints/domains-registrar/buy-a-domain) endpoint or the [Buy multiple domains](https://vercel.com/docs/rest-api/reference/endpoints/domains-registrar/buy-multiple-domains) endpoint.", Group: "registrar"},
	{Command: "registrar get-domain-price", Description: "Get price data for a specific domain", Group: "registrar"},
	{Command: "registrar get-domain-transfer-in", Description: "Get the transfer status for a domain", Group: "registrar"},
	{Command: "registrar get-order", Description: "Get information about a domain order by its ID", Group: "registrar"},
	{Command: "registrar get-supported-tlds", Description: "Get a list of TLDs supported by Vercel", Group: "registrar"},
	{Command: "registrar get-tld", Description: "Get the metadata for a specific TLD.", Group: "registrar"},
	{Command: "registrar get-tld-price", Description: "Get price data for a specific TLD. This only reflects base prices for the given TLD. Premium domains may have different prices. Use the [Get price data for a domain](https://vercel.com/docs/rest-api/reference/endpoints/domains-registrar/get-price-data-for-a-domain) endpoint to get the price data for a specific domain.", Group: "registrar"},
	{Command: "registrar renew-domain", Description: "Renew a domain", Group: "registrar"},
	{Command: "registrar transfer-in-domain", Description: "Transfer a domain in from another registrar", Group: "registrar"},
	{Command: "registrar update-domain-auto-renew", Description: "Update the auto-renew setting for a domain", Group: "registrar"},
	{Command: "registrar update-domain-nameservers", Description: "Update the nameservers for a domain. Pass an empty array to use Vercel's default nameservers.", Group: "registrar"},
	{Command: "sandboxes create", Description: "Creates a named sandbox environment. Named sandboxes have a unique name within a project and support automatic snapshotting on shutdown.", Group: "sandboxes"},
	{Command: "sandboxes create-session-directory", Description: "Creates a new directory in a session's filesystem. By default, parent directories are created recursively if they don't exist (similar to `mkdir -p`).", Group: "sandboxes"},
	{Command: "sandboxes create-session-snapshot", Description: "Creates a point-in-time snapshot of a running session's filesystem. Snapshots can be used to quickly restore a session to a previous state or to create new sessions with pre-configured environments. The session must be running and able to accept commands for a snapshot to be created. The session will be terminated after the snapshot is created.", Group: "sandboxes"},
	{Command: "sandboxes delete-drive", Description: "Deletes a drive by project and name. Attached drives cannot be deleted. Stop or replace the session currently using the drive before retrying deletion. Drives are in private beta. Register your interest to get access: https://vercel.com/changelog/drives-for-vercel-sandbox-in-private-beta", Group: "sandboxes"},
	{Command: "sandboxes delete-sandbox", Description: "Deletes a sandbox by name. If sandboxes are currently running, they will be stopped first. This operation deletes all sandbox entities with the given name and the named sandbox metadata.", Group: "sandboxes"},
	{Command: "sandboxes delete-session-snapshot", Description: "Permanently deletes a snapshot and frees its associated storage. This action cannot be undone. After deletion, the snapshot can no longer be used to create new sessions.", Group: "sandboxes"},
	{Command: "sandboxes extend-session-timeout", Description: "Extends the maximum execution time of a running session. The session must be active and able to accept commands. The total timeout cannot exceed the maximum allowed limit for your account.", Group: "sandboxes"},
	{Command: "sandboxes get-named-sandbox", Description: "Retrieves a named sandbox by name, including its current sandbox and routes. If the sandbox is stopped and resume is true, a new sandbox will be created from the most recent snapshot.", Group: "sandboxes"},
	{Command: "sandboxes get-or-create-drive", Description: "Gets an existing drive by project and name, or creates it when it does not exist. Drives are in private beta. Register your interest to get access: https://vercel.com/changelog/drives-for-vercel-sandbox-in-private-beta", Group: "sandboxes"},
	{Command: "sandboxes get-session", Description: "Retrieves detailed information about a specific session, including its current status, resource configuration, and exposed routes.", Group: "sandboxes"},
	{Command: "sandboxes get-session-command", Description: "Retrieves the current status and details of a command executed in a session. Use the `wait` parameter to block until the command finishes execution.", Group: "sandboxes"},
	{Command: "sandboxes get-session-command-logs", Description: "Streams the output of a command in real-time using newline-delimited JSON (ND-JSON). Each entry includes the output data and stream type. Stream types include `stdout`, `stderr`, and `error` (for stream failures).", Group: "sandboxes"},
	{Command: "sandboxes get-session-snapshot", Description: "Retrieves detailed information about a specific snapshot, including its creation time, size, expiration date, and the source session it was created from.", Group: "sandboxes"},
	{Command: "sandboxes kill-session-command", Description: "Sends a signal to terminate a running command in a session. The signal can be used to gracefully stop (SIGTERM) or forcefully kill (SIGKILL) the process. The command must still be running for this operation to succeed.", Group: "sandboxes"},
	{Command: "sandboxes list", Description: "Retrieves a paginated list of named sandboxes belonging to a specific project. Results can be sorted by creation time or name, and optionally filtered by name prefix.", Group: "sandboxes"},
	{Command: "sandboxes list-drives", Description: "Retrieves a paginated list of drives belonging to a specific project. Drives are in private beta. Register your interest to get access: https://vercel.com/changelog/drives-for-vercel-sandbox-in-private-beta", Group: "sandboxes"},
	{Command: "sandboxes list-session-commands", Description: "Retrieves a list of all commands that have been executed in a session, including their current status, exit codes, and execution times, ordered from the most recent to the oldest.", Group: "sandboxes"},
	{Command: "sandboxes list-session-snapshots", Description: "Retrieves a paginated list of snapshots for a specific project.", Group: "sandboxes"},
	{Command: "sandboxes list-sessions", Description: "Retrieves a paginated list of sessions belonging to a specific sandbox. Results are sorted by creation time and paginated using an opaque cursor.", Group: "sandboxes"},
	{Command: "sandboxes read-session-file", Description: "Downloads the contents of a file from a session's filesystem. The file content is returned as a binary stream with appropriate Content-Disposition headers for file download.", Group: "sandboxes"},
	{Command: "sandboxes run-session-command", Description: "Executes a shell command inside a running session. The command runs asynchronously and returns immediately with a command ID that can be used to track its progress and retrieve its output. Optionally, use the `wait` parameter to stream the command status until completion.", Group: "sandboxes"},
	{Command: "sandboxes stop-session", Description: "Stops a running session and releases its allocated resources. All running processes within the session will be terminated. This action cannot be undone. A stopped session cannot be restarted.", Group: "sandboxes"},
	{Command: "sandboxes update-sandbox", Description: "Updates the configuration of a sandbox. Only the provided fields will be modified; omitted fields remain unchanged.", Group: "sandboxes"},
	{Command: "sandboxes update-session-network-policy", Description: "Replaces the network access policy of a running session. Use this to control which external hosts the session can communicate with. This is a full replacement. Any previously configured network rules will be overwritten.", Group: "sandboxes"},
	{Command: "sandboxes write-session-files", Description: "Uploads and extracts files to a session's filesystem. Files must be uploaded as a gzipped tarball (`.tar.gz`) with the `Content-Type` header set to `application/gzip`. The tarball contents are extracted to the session's working directory, or to a custom directory specified via the `x-cwd` header.", Group: "sandboxes"},
	{Command: "security add-bypass-ip", Description: "Create new system bypass rules", Group: "security"},
	{Command: "security get-active-attack-status", Description: "Retrieve active attack data within the last N days (default: 1 day)", Group: "security"},
	{Command: "security get-bypass-ip", Description: "Retrieve the system bypass rules configured for the specified project", Group: "security"},
	{Command: "security get-firewall-config", Description: "Retrieve the specified firewall configuration for a project. The deployed configVersion will be `active`", Group: "security"},
	{Command: "security get-firewall-events", Description: "Retrieve firewall actions for a project", Group: "security"},
	{Command: "security put-firewall-config", Description: "Set the firewall configuration to provided rules and settings. Creates or overwrite the existing firewall configuration.", Group: "security"},
	{Command: "security remove-bypass-ip", Description: "Remove system bypass rules", Group: "security"},
	{Command: "security update-attack-challenge-mode", Description: "Update the setting for determining if the project has Attack Challenge mode enabled.", Group: "security"},
	{Command: "security update-firewall-config", Description: "Process updates to modify the existing firewall config for a project", Group: "security"},
	{Command: "storage create-integration-store-direct", Description: "Creates an integration store with automatic billing plan handling. For free resources, omit `billingPlanId` to auto-discover free plans. For paid resources, provide a `billingPlanId` from the billing plans endpoint.", Group: "storage"},
	{Command: "teams create", Description: "Create a new Team under your account. You need to send a POST request with the desired Team slug, and optionally the Team name.", Group: "teams"},
	{Command: "teams delete", Description: "Delete a team under your account. You need to send a `DELETE` request with the desired team `id`. An optional array of reasons for deletion may also be sent.", Group: "teams"},
	{Command: "teams get", Description: "Get a paginated list of all the Teams the authenticated User is a member of.", Group: "teams"},
	{Command: "teams get-teamid", Description: "Get information for the Team specified by the `teamId` parameter.", Group: "teams"},
	{Command: "teams patch", Description: "Update the information of a Team specified by the `teamId` parameter. The request body should contain the information that will be updated on the Team.", Group: "teams"},
	{Command: "teams dsync-roles post-team", Description: "Update the Directory Sync role mappings for a Team. This endpoint allows updating the mapping between directory groups and team roles or access groups.", Group: "teams"},
	{Command: "teams feature-flags list-team-flag-settings", Description: "Retrieve feature flag settings for projects in a team.", Group: "teams"},
	{Command: "teams feature-flags list-team-flags", Description: "Retrieve all feature flags for a team across all projects. The list can be filtered by state and supports pagination.", Group: "teams"},
	{Command: "teams feature-flags list-team-flags-v2", Description: "Retrieve all feature flags for a team across all projects. Returns an opaque cursor for pagination.", Group: "teams"},
	{Command: "teams invites delete-team-code", Description: "Delete an active Team invite code.", Group: "teams"},
	{Command: "teams members get-team", Description: "Get a paginated list of team members for the provided team.", Group: "teams"},
	{Command: "teams members invite-user-to-team", Description: "Invite a user to join the team specified in the URL. The authenticated user needs to be an `OWNER` in order to successfully invoke this endpoint. The user to be invited must be specified by email.", Group: "teams"},
	{Command: "teams members join-team", Description: "Join a team with a provided invite code or team ID.", Group: "teams"},
	{Command: "teams members remove-team", Description: "Remove a Team Member from the Team, or dismiss a user that requested access, or leave a team.", Group: "teams"},
	{Command: "teams members update-team", Description: "Update the membership of a Team Member on the Team specified by `teamId`, such as changing the _role_ of the member, or confirming a request to join the Team for an unconfirmed member. The authenticated user must be an `OWNER` of the Team.", Group: "teams"},
	{Command: "teams microfrontends delete-group", Description: "Deletes a microfrontends group from the team associated with the group ID.", Group: "teams"},
	{Command: "teams microfrontends update-group", Description: "Updates the name (and slug) of a microfrontends group.", Group: "teams"},
	{Command: "teams request access-to-team", Description: "Request access to a team as a member. An owner has to approve the request. Only 100 users can request access to a team at the same time.", Group: "teams"},
	{Command: "teams request get-team-access", Description: "Check the status of a join request. It'll respond with a 404 if the request has been declined. If no `userId` path segment was provided, this endpoint will instead return the status of the authenticated user.", Group: "teams"},
	{Command: "user create-auth-token", Description: "Creates and returns a new authentication token for the currently authenticated User. The `bearerToken` property is only provided once, in the response body, so be sure to save it on the client for use with API requests.", Group: "user"},
	{Command: "user delete-auth-token", Description: "Invalidate an authentication token, such that it will no longer be valid for future HTTP requests.", Group: "user"},
	{Command: "user get-auth", Description: "Retrieves information related to the currently authenticated User.", Group: "user"},
	{Command: "user get-auth-token", Description: "Retrieve metadata about an authentication token belonging to the currently authenticated User.", Group: "user"},
	{Command: "user list-auth-tokens", Description: "Retrieve a list of the current User's authentication tokens.", Group: "user"},
	{Command: "user request-delete", Description: "Initiates the deletion process for the currently authenticated User, by sending a deletion confirmation email. The email contains a link that the user needs to visit in order to proceed with the deletion process.", Group: "user"},
	{Command: "webhooks create", Description: "Creates a webhook", Group: "webhooks"},
	{Command: "webhooks delete", Description: "Deletes a webhook", Group: "webhooks"},
	{Command: "webhooks get", Description: "Get a list of webhooks", Group: "webhooks"},
	{Command: "webhooks get-id", Description: "Get a webhook", Group: "webhooks"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"mcp:read-only":       "true",
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  vercel-admin-pp-cli which "stale tickets"
  vercel-admin-pp-cli which "bottleneck"
  vercel-admin-pp-cli which --limit 1 "send message"
  vercel-admin-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
