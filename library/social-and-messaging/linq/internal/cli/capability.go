// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"
)

func newCapabilityCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "capability",
		Short: "Check whether an address can use iMessage, RCS, or SMS",
		Long: `Check whether a recipient address can use iMessage-only or RCS features.

The friendly 'capability check' command calls iMessage first, then RCS, and
falls back to SMS when neither richer channel is available. It reports
structural channel support without echoing the full address.`,
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE:        parentNoSubcommandRunE(flags),
	}

	cmd.AddCommand(newCapabilityCheckCmd(flags))
	cmd.AddCommand(newCapabilityCheckImessageCmd(flags))
	cmd.AddCommand(newCapabilityCheckRcscapabilityCmd(flags))
	return cmd
}
