// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "compose preview", Description: "Build and validate the nested Linq message body for text, media, rich links, effects, decorations, replies, preferred service, and idempotency without sending.", Group: "messaging", WhyItMatters: "Agents can inspect sendability, protocol warnings, and exact JSON before a side effect."},
	{Command: "compose send", Description: "Send generic rich Linq messages to existing chats using the validated nested message body builder.", Group: "messaging", WhyItMatters: "Keeps rich message construction explicit and previewable before network side effects."},
	{Command: "typing start/stop/pulse", Description: "Start, stop, or pulse bounded iMessage typing indicators for one-to-one chats.", Group: "messaging", WhyItMatters: "Provides natural typing behavior without unbounded loops and with clear iMessage-only warnings."},
	{Command: "typing watch", Description: "Filter captured webhook/debug streams for inbound chat.typing_indicator.started and stopped events without receiving webhooks from Linq.", Group: "webhooks", WhyItMatters: "Gives operators a structural QA view of ephemeral inbound typing presence."},
	{Command: "webhooks list/show/add-event/remove-event/set-events/doctor", Description: "Manage Linq webhook subscriptions and safely update subscribed_events with catalog validation.", Group: "webhooks", WhyItMatters: "Lets operators enable typing, read, delivery, and status events without hand-rolling raw PUT bodies."},
	{Command: "capability check", Description: "Resolve an address to imessage, rcs, or sms by checking iMessage then RCS and reporting channel feature support.", Group: "capability", WhyItMatters: "Explains whether typing indicators, effects, and read receipts can work before an operator uses richer messaging features."},
	{Command: "effects list/preview", Description: "List and preview supported iMessage screen effects, bubble effects, text styles, and text animations.", Group: "messaging", WhyItMatters: "Keeps iMessage-only effects and decorations discoverable with explicit protocol warnings."},
	{Command: "attachments plan/upload/send-url/send-id/audit-url/cleanup", Description: "Plan file strategy, pre-upload larger files, send media URLs or attachment IDs, audit URLs, and clean up attachment bytes.", Group: "attachments", WhyItMatters: "Turns raw attachment endpoints into a lifecycle-aware workflow with size, HTTPS, CDN allowlist, and voice memo guidance."},
	{Command: "link-preview audit/send/metadata", Description: "Audit rich link preview constraints, send link-only preview messages, and inspect target metadata.", Group: "messaging", WhyItMatters: "Prevents invalid mixed-part preview sends and blocks links on first outbound openers."},
	{Command: "react add/remove/custom", Description: "Add or remove built-in reactions and custom emoji reactions with part-index support.", Group: "messaging", WhyItMatters: "Validates tapback types and blocks outbound stickers while preserving raw API part-index control."},
	{Command: "contact-share preflight/send", Description: "Preflight and send native iMessage Name and Photo Sharing after contact card and prior-outbound readiness checks.", Group: "messaging", WhyItMatters: "Makes contact-card sharing explicit, rate-limit-aware, and distinct from .vcf attachments."},
	{Command: "attachments delete-an", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "attachments"},
	{Command: "attachments get-metadata", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "attachments"},
	{Command: "attachments pre-upload-afile", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "attachments"},
	{Command: "capability check-imessage", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "capability"},
	{Command: "capability check-rcscapability", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "capability"},
	{Command: "chats create-anew", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats get-achat-by-id", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats list-all", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats update-achat", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats leave agroup-chat", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats location get-data", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats location request-sharing", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats messages get-from-achat", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats messages send-amessage-to-an-existing-chat", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats participants add-aparticipant-to-achat", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats participants remove-aparticipant-from-achat", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats read mark-chat-as", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats share-contact-card share-your-contact-card-with-achat", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats typing start-indicator", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats typing stop-indicator", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "chats voicememo send-avoice-memo-to-achat", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "chats"},
	{Command: "contact-card get", Description: "Retrieve active contact cards for the partner account.", Group: "contact-card"},
	{Command: "contact-card create", Description: "Create a contact card with --phone-number, --first-name, optional --last-name, and optional --image-url.", Group: "contact-card"},
	{Command: "contact-card update", Description: "Update an existing active contact card with --phone-number and changed contact-card fields.", Group: "contact-card"},
	{Command: "messages delete-amessage-from-system", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "messages"},
	{Command: "messages edit-the-content-of-amessage-part", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "messages"},
	{Command: "messages get-amessage-by-id", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "messages"},
	{Command: "messages reactions add-or-remove-areaction-to-amessage", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "messages"},
	{Command: "messages thread get-all-messages-in-athread", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "messages"},
	{Command: "phone-numbers list", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "phone-numbers"},
	{Command: "phonenumbers list-phone-numbers-deprecated", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "phonenumbers"},
	{Command: "webhook-events list-available-types", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "webhook-events"},
	{Command: "webhook-subscriptions create-anew", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "webhook-subscriptions"},
	{Command: "webhook-subscriptions delete-awebhook-subscription", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "webhook-subscriptions"},
	{Command: "webhook-subscriptions get-awebhook-subscription-by-id", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "webhook-subscriptions"},
	{Command: "webhook-subscriptions list-all", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "webhook-subscriptions"},
	{Command: "webhook-subscriptions update-awebhook-subscription", Description: "Source: https://docs.linqapp.com/api. Cross-check: github.com/linq-team/linq-go/api.md.", Group: "webhook-subscriptions"},
	{Command: "webhooks list", Description: "List webhook subscriptions and subscribed events.", Group: "webhooks"},
	{Command: "webhooks show", Description: "Show one webhook subscription and its subscribed events.", Group: "webhooks"},
	{Command: "webhooks add-event", Description: "Fetch, union, validate, and replace subscribed_events for a webhook subscription.", Group: "webhooks"},
	{Command: "webhooks remove-event", Description: "Fetch, diff, validate, and replace subscribed_events for a webhook subscription.", Group: "webhooks"},
	{Command: "webhooks set-events", Description: "Replace a webhook subscription's subscribed_events field with an explicit validated full event set.", Group: "webhooks"},
	{Command: "webhooks doctor", Description: "Compare webhook subscriptions against an expected event set and warn on drift.", Group: "webhooks"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"mcp:read-only":       "true",
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  linq-pp-cli which "stale tickets"
  linq-pp-cli which "bottleneck"
  linq-pp-cli which --limit 1 "send message"
  linq-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
