// Copyright 2026 Todd Dailey and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSubscriptionsPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyToken string
	var bodySubscription string
	var bodyUser string
	var bodyDeviceName string
	var bodySound string

	cmd := &cobra.Command{
		Use:         "subscriptions",
		Short:       "Migrate an existing user key into a subscription user key",
		Long:        "Shortcut for 'subscriptions migrate'. Migrate an existing user key into a subscription user key",
		Example:     "  pushover-pp-cli subscriptions --app-token your-token-here",
		Annotations: map[string]string{"pp:endpoint": "subscriptions.migrate", "pp:method": "POST", "pp:path": "/1/subscriptions/migrate.json"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !(cmd.Flags().Changed("app-token") || cmd.Flags().Changed("token")) && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "app-token")
			}
			if !cmd.Flags().Changed("subscription") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "subscription")
			}
			if !(cmd.Flags().Changed("user-key") || cmd.Flags().Changed("user")) && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "user-key")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/1/subscriptions/migrate.json"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyToken != "" {
				body["token"] = bodyToken
			}
			if bodySubscription != "" {
				body["subscription"] = bodySubscription
			}
			if bodyUser != "" {
				body["user"] = bodyUser
			}
			if bodyDeviceName != "" {
				body["device_name"] = bodyDeviceName
			}
			if bodySound != "" {
				body["sound"] = bodySound
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyToken, "app-token", "", "Application API token")
	cmd.Flags().StringVar(&bodyToken, "token", "", "Application API token")
	_ = cmd.Flags().MarkHidden("token")
	cmd.Flags().StringVar(&bodySubscription, "subscription", "", "Subscription code")
	cmd.Flags().StringVar(&bodyUser, "user-key", "", "Existing user key to migrate")
	cmd.Flags().StringVar(&bodyUser, "user", "", "Existing user key to migrate")
	_ = cmd.Flags().MarkHidden("user")
	cmd.Flags().StringVar(&bodyDeviceName, "device-name", "", "Optional device name limit for the subscription")
	cmd.Flags().StringVar(&bodySound, "sound", "", "Preferred default sound")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
