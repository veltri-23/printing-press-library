// Copyright 2026 Anton Sidorov aka anticodeguy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProjectsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyArchiveIn int
	var bodyCloseIn int
	var bodyCoverImage string
	var bodyCycleView bool
	var bodyDefaultAssignee string
	var bodyDefaultState string
	var bodyDescription string
	var bodyEmoji string
	var bodyEstimate string
	var bodyExternalId string
	var bodyExternalSource string
	var bodyGuestViewAllFeatures bool
	var bodyIconProp string
	var bodyIdentifier string
	var bodyIntakeView bool
	var bodyIsIssueTypeEnabled bool
	var bodyIsTimeTrackingEnabled bool
	var bodyIssueViewsView bool
	var bodyModuleView bool
	var bodyName string
	var bodyPageView bool
	var bodyProjectLead string
	var bodyTimezone string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <pk>",
		Short:       "Partially update an existing project's properties like name, description, or settings.",
		Example:     "  plane-pp-cli projects update example-value",
		Annotations: map[string]string{"pp:endpoint": "projects.update", "pp:method": "PATCH", "pp:path": "/projects/{pk}/"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/projects/{pk}/"
			path = replacePathParam(path, "pk", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyArchiveIn != 0 {
					body["archive_in"] = bodyArchiveIn
				}
				if bodyCloseIn != 0 {
					body["close_in"] = bodyCloseIn
				}
				if bodyCoverImage != "" {
					body["cover_image"] = bodyCoverImage
				}
				if cmd.Flags().Changed("cycle-view") {
					body["cycle_view"] = bodyCycleView
				}
				if bodyDefaultAssignee != "" {
					body["default_assignee"] = bodyDefaultAssignee
				}
				if bodyDefaultState != "" {
					body["default_state"] = bodyDefaultState
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyEmoji != "" {
					body["emoji"] = bodyEmoji
				}
				if bodyEstimate != "" {
					body["estimate"] = bodyEstimate
				}
				if bodyExternalId != "" {
					body["external_id"] = bodyExternalId
				}
				if bodyExternalSource != "" {
					body["external_source"] = bodyExternalSource
				}
				if cmd.Flags().Changed("guest-view-all-features") {
					body["guest_view_all_features"] = bodyGuestViewAllFeatures
				}
				if bodyIconProp != "" {
					body["icon_prop"] = bodyIconProp
				}
				if bodyIdentifier != "" {
					body["identifier"] = bodyIdentifier
				}
				if cmd.Flags().Changed("intake-view") {
					body["intake_view"] = bodyIntakeView
				}
				if cmd.Flags().Changed("is-issue-type-enabled") {
					body["is_issue_type_enabled"] = bodyIsIssueTypeEnabled
				}
				if cmd.Flags().Changed("is-time-tracking-enabled") {
					body["is_time_tracking_enabled"] = bodyIsTimeTrackingEnabled
				}
				if cmd.Flags().Changed("issue-views-view") {
					body["issue_views_view"] = bodyIssueViewsView
				}
				if cmd.Flags().Changed("module-view") {
					body["module_view"] = bodyModuleView
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if cmd.Flags().Changed("page-view") {
					body["page_view"] = bodyPageView
				}
				if bodyProjectLead != "" {
					body["project_lead"] = bodyProjectLead
				}
				if bodyTimezone != "" {
					body["timezone"] = bodyTimezone
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "projects", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "projects", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "projects",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().IntVar(&bodyArchiveIn, "archive-in", 0, "Archive in")
	cmd.Flags().IntVar(&bodyCloseIn, "close-in", 0, "Close in")
	cmd.Flags().StringVar(&bodyCoverImage, "cover-image", "", "Cover image")
	cmd.Flags().BoolVar(&bodyCycleView, "cycle-view", false, "Cycle view")
	cmd.Flags().StringVar(&bodyDefaultAssignee, "default-assignee", "", "Default assignee")
	cmd.Flags().StringVar(&bodyDefaultState, "default-state", "", "Default state")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyEmoji, "emoji", "", "Emoji")
	cmd.Flags().StringVar(&bodyEstimate, "estimate", "", "Estimate")
	cmd.Flags().StringVar(&bodyExternalId, "external-id", "", "External id")
	cmd.Flags().StringVar(&bodyExternalSource, "external-source", "", "External source")
	cmd.Flags().BoolVar(&bodyGuestViewAllFeatures, "guest-view-all-features", false, "Guest view all features")
	cmd.Flags().StringVar(&bodyIconProp, "icon-prop", "", "Icon prop")
	cmd.Flags().StringVar(&bodyIdentifier, "identifier", "", "Identifier")
	cmd.Flags().BoolVar(&bodyIntakeView, "intake-view", false, "Intake view")
	cmd.Flags().BoolVar(&bodyIsIssueTypeEnabled, "is-issue-type-enabled", false, "Is issue type enabled")
	cmd.Flags().BoolVar(&bodyIsTimeTrackingEnabled, "is-time-tracking-enabled", false, "Is time tracking enabled")
	cmd.Flags().BoolVar(&bodyIssueViewsView, "issue-views-view", false, "Issue views view")
	cmd.Flags().BoolVar(&bodyModuleView, "module-view", false, "Module view")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().BoolVar(&bodyPageView, "page-view", false, "Page view")
	cmd.Flags().StringVar(&bodyProjectLead, "project-lead", "", "Project lead")
	cmd.Flags().StringVar(&bodyTimezone, "timezone", "", "* `Africa/Abidjan` - Africa/Abidjan * `Africa/Accra` - Africa/Accra * `Africa/Addis_Ababa` - Africa/Addis_Ababa *")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
