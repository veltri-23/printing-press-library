// Copyright 2026 Hunter Veltri and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newInstanceUpdateSettingsCmd(flags *rootFlags) *cobra.Command {
	var bodyAutoRestartDevServerWhenIdle bool
	var bodyEnableCloudSync bool
	var bodyEnableConferenceRoomChat bool
	var bodyEnableEnvironments bool
	var bodyEnableExperimentalFileViewer bool
	var bodyEnableExternalObjects bool
	var bodyEnableIsolatedWorkspaces bool
	var bodyEnableIssueGraphLivenessAutoRecovery bool
	var bodyEnableIssuePlanDecompositions bool
	var bodyEnablePipelines bool
	var bodyEnableServerInfoDebugView bool
	var bodyEnableStreamlinedLeftNavigation bool
	var bodyEnableTaskWatchdogs bool
	var bodyEnableWorkspaceBranchReconcileForward bool
	var bodyIssueGraphLivenessAutoRecoveryLookbackHours int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update-settings",
		Short:       "Update experimental instance settings",
		Example:     "  paperclip-pp-cli instance update-settings",
		Annotations: map[string]string{"pp:endpoint": "instance.update-settings", "pp:method": "PATCH", "pp:path": "/api/instance/settings/experimental"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/api/instance/settings/experimental"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("auto-restart-dev-server-when-idle") {
					bodyMap["autoRestartDevServerWhenIdle"] = bodyAutoRestartDevServerWhenIdle
				}
				if cmd.Flags().Changed("enable-cloud-sync") {
					bodyMap["enableCloudSync"] = bodyEnableCloudSync
				}
				if cmd.Flags().Changed("enable-conference-room-chat") {
					bodyMap["enableConferenceRoomChat"] = bodyEnableConferenceRoomChat
				}
				if cmd.Flags().Changed("enable-environments") {
					bodyMap["enableEnvironments"] = bodyEnableEnvironments
				}
				if cmd.Flags().Changed("enable-experimental-file-viewer") {
					bodyMap["enableExperimentalFileViewer"] = bodyEnableExperimentalFileViewer
				}
				if cmd.Flags().Changed("enable-external-objects") {
					bodyMap["enableExternalObjects"] = bodyEnableExternalObjects
				}
				if cmd.Flags().Changed("enable-isolated-workspaces") {
					bodyMap["enableIsolatedWorkspaces"] = bodyEnableIsolatedWorkspaces
				}
				if cmd.Flags().Changed("enable-issue-graph-liveness-auto-recovery") {
					bodyMap["enableIssueGraphLivenessAutoRecovery"] = bodyEnableIssueGraphLivenessAutoRecovery
				}
				if cmd.Flags().Changed("enable-issue-plan-decompositions") {
					bodyMap["enableIssuePlanDecompositions"] = bodyEnableIssuePlanDecompositions
				}
				if cmd.Flags().Changed("enable-pipelines") {
					bodyMap["enablePipelines"] = bodyEnablePipelines
				}
				if cmd.Flags().Changed("enable-server-info-debug-view") {
					bodyMap["enableServerInfoDebugView"] = bodyEnableServerInfoDebugView
				}
				if cmd.Flags().Changed("enable-streamlined-left-navigation") {
					bodyMap["enableStreamlinedLeftNavigation"] = bodyEnableStreamlinedLeftNavigation
				}
				if cmd.Flags().Changed("enable-task-watchdogs") {
					bodyMap["enableTaskWatchdogs"] = bodyEnableTaskWatchdogs
				}
				if cmd.Flags().Changed("enable-workspace-branch-reconcile-forward") {
					bodyMap["enableWorkspaceBranchReconcileForward"] = bodyEnableWorkspaceBranchReconcileForward
				}
				if bodyIssueGraphLivenessAutoRecoveryLookbackHours != 0 {
					bodyMap["issueGraphLivenessAutoRecoveryLookbackHours"] = bodyIssueGraphLivenessAutoRecoveryLookbackHours
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "instance", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "instance", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "instance", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "instance", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "instance", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "instance",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "instance", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "instance", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAutoRestartDevServerWhenIdle, "auto-restart-dev-server-when-idle", false, "Auto restart dev server when idle")
	cmd.Flags().BoolVar(&bodyEnableCloudSync, "enable-cloud-sync", false, "Enable cloud sync")
	cmd.Flags().BoolVar(&bodyEnableConferenceRoomChat, "enable-conference-room-chat", false, "Enable conference room chat")
	cmd.Flags().BoolVar(&bodyEnableEnvironments, "enable-environments", false, "Enable environments")
	cmd.Flags().BoolVar(&bodyEnableExperimentalFileViewer, "enable-experimental-file-viewer", false, "Enable experimental file viewer")
	cmd.Flags().BoolVar(&bodyEnableExternalObjects, "enable-external-objects", false, "Enable external objects")
	cmd.Flags().BoolVar(&bodyEnableIsolatedWorkspaces, "enable-isolated-workspaces", false, "Enable isolated workspaces")
	cmd.Flags().BoolVar(&bodyEnableIssueGraphLivenessAutoRecovery, "enable-issue-graph-liveness-auto-recovery", false, "Enable issue graph liveness auto recovery")
	cmd.Flags().BoolVar(&bodyEnableIssuePlanDecompositions, "enable-issue-plan-decompositions", false, "Enable issue plan decompositions")
	cmd.Flags().BoolVar(&bodyEnablePipelines, "enable-pipelines", false, "Enable pipelines")
	cmd.Flags().BoolVar(&bodyEnableServerInfoDebugView, "enable-server-info-debug-view", false, "Enable server info debug view")
	cmd.Flags().BoolVar(&bodyEnableStreamlinedLeftNavigation, "enable-streamlined-left-navigation", false, "Enable streamlined left navigation")
	cmd.Flags().BoolVar(&bodyEnableTaskWatchdogs, "enable-task-watchdogs", false, "Enable task watchdogs")
	cmd.Flags().BoolVar(&bodyEnableWorkspaceBranchReconcileForward, "enable-workspace-branch-reconcile-forward", false, "Enable workspace branch reconcile forward")
	cmd.Flags().IntVar(&bodyIssueGraphLivenessAutoRecoveryLookbackHours, "issue-graph-liveness-auto-recovery-lookback-hours", 0, "Issue graph liveness auto recovery lookback hours")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
