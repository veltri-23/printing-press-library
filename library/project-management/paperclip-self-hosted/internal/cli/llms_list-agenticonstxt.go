// Copyright 2026 Hunter Veltri and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"

	"github.com/spf13/cobra"
)

func newLlmsListAgenticonstxtCmd(flags *rootFlags) *cobra.Command {

	cmd := &cobra.Command{
		Use:         "list-agenticonstxt",
		Short:       "Get agent icon names as plain text",
		Example:     "  paperclip-pp-cli llms list-agenticonstxt",
		Annotations: map[string]string{"pp:endpoint": "llms.list-agenticonstxt", "pp:method": "GET", "pp:path": "/api/llms/agent-icons.txt", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// /api/llms/agent-icons.txt returns plain text, not JSON.
			// Bypass the JSON-guarded resolver; emit verbatim text or a JSON
			// envelope when --json is set so agent-mode output stays parseable.
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			data, err := c.GetWithHeaders(cmd.Context(), "/api/llms/agent-icons.txt", nil, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				wrapped, werr := json.Marshal(map[string]any{"content": string(data)})
				if werr != nil {
					return werr
				}
				data = wrapped
			}
			_, err = cmd.OutOrStdout().Write(data)
			if err == nil && len(data) > 0 && data[len(data)-1] != '\n' {
				_, _ = cmd.OutOrStdout().Write([]byte{'\n'})
			}
			return err
		},
	}

	return cmd
}
