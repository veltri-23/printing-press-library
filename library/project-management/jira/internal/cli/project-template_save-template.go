// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProjectTemplateSaveTemplateCmd(flags *rootFlags) *cobra.Command {
	var bodyTemplateDescription string
	var bodyTemplateFromProjectRequestProjectId int
	var bodyTemplateFromProjectRequestTemplateGenerationOptionsEnableScreenDelegatedAdminSupport bool
	var bodyTemplateFromProjectRequestTemplateGenerationOptionsEnableWorkflowDelegatedAdminSupport bool
	var bodyTemplateFromProjectRequestTemplateType string
	var bodyTemplateName string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "save-template",
		Short:       "Save custom template This API endpoint allows you to save a customised template ***Note: Custom Templates are only...",
		Example:     "  jira-pp-cli project-template save-template",
		Annotations: map[string]string{"pp:endpoint": "project-template.save-template", "pp:method": "POST", "pp:path": "/rest/api/3/project-template/save-template"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/project-template/save-template"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyTemplateDescription != "" {
					body["templateDescription"] = bodyTemplateDescription
				}
				{
					nestedTemplateFromProjectRequest := map[string]any{}
					if bodyTemplateFromProjectRequestProjectId != 0 {
						nestedTemplateFromProjectRequest["projectId"] = bodyTemplateFromProjectRequestProjectId
					}
					{
						nestedTemplateFromProjectRequestTemplateGenerationOptions := map[string]any{}
						if bodyTemplateFromProjectRequestTemplateGenerationOptionsEnableScreenDelegatedAdminSupport != false {
							nestedTemplateFromProjectRequestTemplateGenerationOptions["enableScreenDelegatedAdminSupport"] = bodyTemplateFromProjectRequestTemplateGenerationOptionsEnableScreenDelegatedAdminSupport
						}
						if bodyTemplateFromProjectRequestTemplateGenerationOptionsEnableWorkflowDelegatedAdminSupport != false {
							nestedTemplateFromProjectRequestTemplateGenerationOptions["enableWorkflowDelegatedAdminSupport"] = bodyTemplateFromProjectRequestTemplateGenerationOptionsEnableWorkflowDelegatedAdminSupport
						}
						if len(nestedTemplateFromProjectRequestTemplateGenerationOptions) > 0 {
							nestedTemplateFromProjectRequest["templateGenerationOptions"] = nestedTemplateFromProjectRequestTemplateGenerationOptions
						}
					}
					if bodyTemplateFromProjectRequestTemplateType != "" {
						nestedTemplateFromProjectRequest["templateType"] = bodyTemplateFromProjectRequestTemplateType
					}
					if len(nestedTemplateFromProjectRequest) > 0 {
						body["templateFromProjectRequest"] = nestedTemplateFromProjectRequest
					}
				}
				if bodyTemplateName != "" {
					body["templateName"] = bodyTemplateName
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "project-template",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyTemplateDescription, "template-description", "", "The description of the template")
	cmd.Flags().IntVar(&bodyTemplateFromProjectRequestProjectId, "template-from-project-request-project-id", 0, "The ID of the target project")
	cmd.Flags().BoolVar(&bodyTemplateFromProjectRequestTemplateGenerationOptionsEnableScreenDelegatedAdminSupport, "template-from-project-request-template-generation-options-enable-screen-delegated-admin-support", false, "Enable screen delegated admin support for the template. This means screen and associated schemes will be copied...")
	cmd.Flags().BoolVar(&bodyTemplateFromProjectRequestTemplateGenerationOptionsEnableWorkflowDelegatedAdminSupport, "template-from-project-request-template-generation-options-enable-workflow-delegated-admin-support", false, "Enable workflow delegated admin support for the template. This means workflows and workflow schemes will be copied...")
	cmd.Flags().StringVar(&bodyTemplateFromProjectRequestTemplateType, "template-from-project-request-template-type", "", "The type of the template: LIVE | SNAPSHOT")
	cmd.Flags().StringVar(&bodyTemplateName, "template-name", "", "The name of the template")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
