// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newLicenseGetApproximateApplicationCountCmd(flags *rootFlags) *cobra.Command {
	var flagApplicationKey string

	cmd := &cobra.Command{
		Use:         "get-approximate-application-count",
		Aliases:     []string{"get"},
		Short:       "Returns the total approximate number of user accounts for a single Jira license. Note that this information is...",
		Example:     "  jira-pp-cli license get-approximate-application-count",
		Annotations: map[string]string{"pp:endpoint": "license.get-approximate-application-count", "pp:method": "GET", "pp:path": "/rest/api/3/license/approximateLicenseCount/product/{applicationKey}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("application-key") {
				allowedApplicationKey := []string{"jira-core", "jira-product-discovery", "jira-software", "jira-servicedesk"}
				validApplicationKey := false
				for _, v := range allowedApplicationKey {
					if flagApplicationKey == v {
						validApplicationKey = true
						break
					}
				}
				if !validApplicationKey {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "application-key", flagApplicationKey, allowedApplicationKey)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/license/approximateLicenseCount/product/{applicationKey}"
			path = replacePathParam(path, "applicationKey", fmt.Sprintf("%v", flagApplicationKey))
			params := map[string]string{}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "license", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagApplicationKey, "application-key", "jira-core", "The ID of the application, represents a specific version of Jira. (one of: jira-core, jira-product-discovery, jira-software, jira-servicedesk)")

	return cmd
}
