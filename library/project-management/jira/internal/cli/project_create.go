// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProjectCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAssigneeType string
	var bodyAvatarId int
	var bodyCategoryId int
	var bodyDescription string
	var bodyFieldConfigurationScheme int
	var bodyFieldScheme int
	var bodyIssueSecurityScheme int
	var bodyIssueTypeScheme int
	var bodyIssueTypeScreenScheme int
	var bodyKey string
	var bodyLead string
	var bodyLeadAccountId string
	var bodyName string
	var bodyNotificationScheme int
	var bodyPermissionScheme int
	var bodyProjectTemplateKey string
	var bodyProjectTypeKey string
	var bodyUrl string
	var bodyWorkflowScheme int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Creates a project based on a project type template, as shown in the following table: | Project Type Key | Project...",
		Example:     "  jira-pp-cli project create --key your-token-here",
		Annotations: map[string]string{"pp:endpoint": "project.create", "pp:method": "POST", "pp:path": "/rest/api/3/project"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("key") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "key")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/project"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAssigneeType != "" {
					body["assigneeType"] = bodyAssigneeType
				}
				if bodyAvatarId != 0 {
					body["avatarId"] = bodyAvatarId
				}
				if bodyCategoryId != 0 {
					body["categoryId"] = bodyCategoryId
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyFieldConfigurationScheme != 0 {
					body["fieldConfigurationScheme"] = bodyFieldConfigurationScheme
				}
				if bodyFieldScheme != 0 {
					body["fieldScheme"] = bodyFieldScheme
				}
				if bodyIssueSecurityScheme != 0 {
					body["issueSecurityScheme"] = bodyIssueSecurityScheme
				}
				if bodyIssueTypeScheme != 0 {
					body["issueTypeScheme"] = bodyIssueTypeScheme
				}
				if bodyIssueTypeScreenScheme != 0 {
					body["issueTypeScreenScheme"] = bodyIssueTypeScreenScheme
				}
				if bodyKey != "" {
					body["key"] = bodyKey
				}
				if bodyLead != "" {
					body["lead"] = bodyLead
				}
				if bodyLeadAccountId != "" {
					body["leadAccountId"] = bodyLeadAccountId
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNotificationScheme != 0 {
					body["notificationScheme"] = bodyNotificationScheme
				}
				if bodyPermissionScheme != 0 {
					body["permissionScheme"] = bodyPermissionScheme
				}
				if bodyProjectTemplateKey != "" {
					body["projectTemplateKey"] = bodyProjectTemplateKey
				}
				if bodyProjectTypeKey != "" {
					body["projectTypeKey"] = bodyProjectTypeKey
				}
				if bodyUrl != "" {
					body["url"] = bodyUrl
				}
				if bodyWorkflowScheme != 0 {
					body["workflowScheme"] = bodyWorkflowScheme
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "project",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAssigneeType, "assignee-type", "", "The default assignee when creating issues for this project.")
	cmd.Flags().IntVar(&bodyAvatarId, "avatar-id", 0, "An integer value for the project's avatar.")
	cmd.Flags().IntVar(&bodyCategoryId, "category-id", 0, "The ID of the project's category. A complete list of category IDs is found using the [Get all project...")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A brief description of the project.")
	cmd.Flags().IntVar(&bodyFieldConfigurationScheme, "field-configuration-scheme", 0, "Deprecated use [fieldScheme](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-field-schemes/#api...")
	cmd.Flags().IntVar(&bodyFieldScheme, "field-scheme", 0, "The ID of the field scheme for the project. Use the [Get field schemes](#api-rest-api-3-config-fieldschemes-get)...")
	cmd.Flags().IntVar(&bodyIssueSecurityScheme, "issue-security-scheme", 0, "The ID of the issue security scheme for the project, which enables you to control who can and cannot view issues....")
	cmd.Flags().IntVar(&bodyIssueTypeScheme, "issue-type-scheme", 0, "The ID of the issue type scheme for the project. Use the [Get all issue type...")
	cmd.Flags().IntVar(&bodyIssueTypeScreenScheme, "issue-type-screen-scheme", 0, "The ID of the issue type screen scheme for the project. Use the [Get all issue type screen...")
	cmd.Flags().StringVar(&bodyKey, "key", "", "Project keys must be unique and start with an uppercase letter followed by one or more uppercase alphanumeric...")
	cmd.Flags().StringVar(&bodyLead, "lead", "", "This parameter is deprecated because of privacy changes. Use `leadAccountId` instead. See the [migration...")
	cmd.Flags().StringVar(&bodyLeadAccountId, "lead-account-id", "", "The account ID of the project lead. Either `lead` or `leadAccountId` must be set when creating a project. Cannot be...")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the project.")
	cmd.Flags().IntVar(&bodyNotificationScheme, "notification-scheme", 0, "The ID of the notification scheme for the project. Use the [Get notification...")
	cmd.Flags().IntVar(&bodyPermissionScheme, "permission-scheme", 0, "The ID of the permission scheme for the project. Use the [Get all permission...")
	cmd.Flags().StringVar(&bodyProjectTemplateKey, "project-template-key", "", "A predefined configuration for a project. The type of the `projectTemplateKey` must match with the type of the...")
	cmd.Flags().StringVar(&bodyProjectTypeKey, "project-type-key", "", "The [project type](https://confluence.atlassian.com/x/GwiiLQ#Jiraapplicationsoverview-Productfeaturesandprojecttypes)...")
	cmd.Flags().StringVar(&bodyUrl, "url", "", "A link to information about this project, such as project documentation")
	cmd.Flags().IntVar(&bodyWorkflowScheme, "workflow-scheme", 0, "The ID of the workflow scheme for the project. Use the [Get all workflow...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
