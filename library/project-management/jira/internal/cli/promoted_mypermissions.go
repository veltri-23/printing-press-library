// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMypermissionsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagProjectKey string
	var flagProjectId string
	var flagIssueKey string
	var flagIssueId string
	var flagPermissions string
	var flagProjectUuid string
	var flagProjectConfigurationUuid string
	var flagCommentId string

	cmd := &cobra.Command{
		Use:         "mypermissions",
		Short:       "Returns a list of permissions indicating which permissions the user has. Details of the user's permissions can be...",
		Long:        "Shortcut for 'mypermissions get-my-permissions'. Returns a list of permissions indicating which permissions the user has. Details of the user's permissions can be...",
		Example:     "  jira-pp-cli mypermissions",
		Annotations: map[string]string{"pp:endpoint": "mypermissions.get-my-permissions", "pp:method": "GET", "pp:path": "/rest/api/3/mypermissions", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/mypermissions"
			params := map[string]string{}
			if flagProjectKey != "" {
				params["projectKey"] = fmt.Sprintf("%v", flagProjectKey)
			}
			if flagProjectId != "" {
				params["projectId"] = fmt.Sprintf("%v", flagProjectId)
			}
			if flagIssueKey != "" {
				params["issueKey"] = fmt.Sprintf("%v", flagIssueKey)
			}
			if flagIssueId != "" {
				params["issueId"] = fmt.Sprintf("%v", flagIssueId)
			}
			if flagPermissions != "" {
				params["permissions"] = fmt.Sprintf("%v", flagPermissions)
			}
			if flagProjectUuid != "" {
				params["projectUuid"] = fmt.Sprintf("%v", flagProjectUuid)
			}
			if flagProjectConfigurationUuid != "" {
				params["projectConfigurationUuid"] = fmt.Sprintf("%v", flagProjectConfigurationUuid)
			}
			if flagCommentId != "" {
				params["commentId"] = fmt.Sprintf("%v", flagCommentId)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "mypermissions", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagProjectKey, "project-key", "", "The key of project. Ignored if `projectId` is provided.")
	cmd.Flags().StringVar(&flagProjectId, "project-id", "", "The ID of project.")
	cmd.Flags().StringVar(&flagIssueKey, "issue-key", "", "The key of the issue. Ignored if `issueId` is provided.")
	cmd.Flags().StringVar(&flagIssueId, "issue-id", "", "The ID of the issue.")
	cmd.Flags().StringVar(&flagPermissions, "permissions", "", "A list of permission keys. (Required) This parameter accepts a comma-separated list. To get the list of available...")
	cmd.Flags().StringVar(&flagProjectUuid, "project-uuid", "", "Project uuid")
	cmd.Flags().StringVar(&flagProjectConfigurationUuid, "project-configuration-uuid", "", "Project configuration uuid")
	cmd.Flags().StringVar(&flagCommentId, "comment-id", "", "The ID of the comment.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
