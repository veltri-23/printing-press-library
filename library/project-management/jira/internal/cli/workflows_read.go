// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newWorkflowsReadCmd(flags *rootFlags) *cobra.Command {
	var bodyProjectAndIssueTypes string
	var bodyWorkflowIds string
	var bodyWorkflowNames string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "read",
		Short:       "Returns a list of workflows and related statuses by providing workflow names, workflow IDs, or project and issue...",
		Example:     "  jira-pp-cli workflows read",
		Annotations: map[string]string{"pp:endpoint": "workflows.read", "pp:method": "POST", "pp:path": "/rest/api/3/workflows"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/workflows"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyProjectAndIssueTypes != "" {
					var parsedProjectAndIssueTypes any
					if err := json.Unmarshal([]byte(bodyProjectAndIssueTypes), &parsedProjectAndIssueTypes); err != nil {
						return fmt.Errorf("parsing --project-and-issue-types JSON: %w", err)
					}
					body["projectAndIssueTypes"] = parsedProjectAndIssueTypes
				}
				if bodyWorkflowIds != "" {
					var parsedWorkflowIds any
					if err := json.Unmarshal([]byte(bodyWorkflowIds), &parsedWorkflowIds); err != nil {
						return fmt.Errorf("parsing --workflow-ids JSON: %w", err)
					}
					body["workflowIds"] = parsedWorkflowIds
				}
				if bodyWorkflowNames != "" {
					var parsedWorkflowNames any
					if err := json.Unmarshal([]byte(bodyWorkflowNames), &parsedWorkflowNames); err != nil {
						return fmt.Errorf("parsing --workflow-names JSON: %w", err)
					}
					body["workflowNames"] = parsedWorkflowNames
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "workflows",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyProjectAndIssueTypes, "project-and-issue-types", "", "The list of projects and issue types to query.")
	cmd.Flags().StringVar(&bodyWorkflowIds, "workflow-ids", "", "The list of workflow IDs to query.")
	cmd.Flags().StringVar(&bodyWorkflowNames, "workflow-names", "", "The list of workflow names to query.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
