// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGroupuserpickerPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagQuery string
	var flagMaxResults int
	var flagShowAvatar bool
	var flagFieldId string
	var flagProjectId string
	var flagIssueTypeId string
	var flagAvatarSize string
	var flagCaseInsensitive bool
	var flagExcludeConnectAddons bool
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "groupuserpicker",
		Short:       "Returns a list of users and groups matching a string. The string is used: * for users, to find a case-insensitive...",
		Long:        "Shortcut for 'groupuserpicker find-users-and-groups'. Returns a list of users and groups matching a string. The string is used: * for users, to find a case-insensitive...",
		Example:     "  jira-pp-cli groupuserpicker --query example-value",
		Annotations: map[string]string{"pp:endpoint": "groupuserpicker.find-users-and-groups", "pp:method": "GET", "pp:path": "/rest/api/3/groupuserpicker", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("query") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "query")
			}
			if cmd.Flags().Changed("avatar-size") {
				allowedAvatarSize := []string{"xsmall", "xsmall@2x", "xsmall@3x", "small", "small@2x", "small@3x", "medium", "medium@2x", "medium@3x", "large", "large@2x", "large@3x", "xlarge", "xlarge@2x", "xlarge@3x", "xxlarge", "xxlarge@2x", "xxlarge@3x", "xxxlarge", "xxxlarge@2x", "xxxlarge@3x"}
				validAvatarSize := false
				for _, v := range allowedAvatarSize {
					if flagAvatarSize == v {
						validAvatarSize = true
						break
					}
				}
				if !validAvatarSize {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "avatar-size", flagAvatarSize, allowedAvatarSize)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/groupuserpicker"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "groupuserpicker", path, map[string]string{
				"query":                fmt.Sprintf("%v", flagQuery),
				"maxResults":           fmt.Sprintf("%v", flagMaxResults),
				"showAvatar":           fmt.Sprintf("%v", flagShowAvatar),
				"fieldId":              fmt.Sprintf("%v", flagFieldId),
				"projectId":            fmt.Sprintf("%v", flagProjectId),
				"issueTypeId":          fmt.Sprintf("%v", flagIssueTypeId),
				"avatarSize":           fmt.Sprintf("%v", flagAvatarSize),
				"caseInsensitive":      fmt.Sprintf("%v", flagCaseInsensitive),
				"excludeConnectAddons": fmt.Sprintf("%v", flagExcludeConnectAddons),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagQuery, "query", "", "The search string.")
	cmd.Flags().IntVar(&flagMaxResults, "max-results", 50, "The maximum number of items to return in each list.")
	cmd.Flags().BoolVar(&flagShowAvatar, "show-avatar", false, "Whether the user avatar should be returned. If an invalid value is provided, the default value is used.")
	cmd.Flags().StringVar(&flagFieldId, "field-id", "", "The custom field ID of the field this request is for.")
	cmd.Flags().StringVar(&flagProjectId, "project-id", "", "The ID of a project that returned users and groups must have permission to view. To include multiple projects,...")
	cmd.Flags().StringVar(&flagIssueTypeId, "issue-type-id", "", "The ID of an issue type that returned users and groups must have permission to view. To include multiple issue...")
	cmd.Flags().StringVar(&flagAvatarSize, "avatar-size", "xsmall", "The size of the avatar to return. If an invalid value is provided, the default value is used. (one of: xsmall, xsmall@2x, xsmall@3x, small, small@2x, small@3x, medium, medium@2x, medium@3x, large, large@2x, large@3x, xlarge, xlarge@2x, xlarge@3x, xxlarge, xxlarge@2x, xxlarge@3x, xxxlarge, xxxlarge@2x, xxxlarge@3x)")
	cmd.Flags().BoolVar(&flagCaseInsensitive, "case-insensitive", false, "Whether the search for groups should be case insensitive.")
	cmd.Flags().BoolVar(&flagExcludeConnectAddons, "exclude-connect-addons", false, "Whether Connect app users and groups should be excluded from the search results. If an invalid value is provided,...")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
