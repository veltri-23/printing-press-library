// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newProjectTemplateLiveTemplateCmd(flags *rootFlags) *cobra.Command {
	var flagProjectId string
	var flagTemplateKey string

	cmd := &cobra.Command{
		Use:         "live-template",
		Aliases:     []string{"list"},
		Short:       "Get custom template This API endpoint allows you to get a live custom project template details by either templateKey...",
		Example:     "  jira-pp-cli project-template live-template",
		Annotations: map[string]string{"pp:endpoint": "project-template.live-template", "pp:method": "GET", "pp:path": "/rest/api/3/project-template/live-template", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/project-template/live-template"
			params := map[string]string{}
			if flagProjectId != "" {
				params["projectId"] = fmt.Sprintf("%v", flagProjectId)
			}
			if flagTemplateKey != "" {
				params["templateKey"] = fmt.Sprintf("%v", flagTemplateKey)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "project-template", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagProjectId, "project-id", "", "optional - The {@link String} containing the project key linked to the custom template to retrieve")
	cmd.Flags().StringVar(&flagTemplateKey, "template-key", "", "optional - The {@link String} containing the key of the custom template to retrieve")

	return cmd
}
