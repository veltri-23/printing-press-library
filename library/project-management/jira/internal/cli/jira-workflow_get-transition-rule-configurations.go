// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newJiraWorkflowGetTransitionRuleConfigurationsCmd(flags *rootFlags) *cobra.Command {
	var flagStartAt int
	var flagMaxResults int
	var flagTypes string
	var flagKeys string
	var flagWorkflowNames string
	var flagWithTags string
	var flagDraft bool
	var flagExpand string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-transition-rule-configurations",
		Short:       "Returns a [paginated](#pagination) list of workflows with transition rules. The workflows can be filtered to return...",
		Example:     "  jira-pp-cli jira-workflow get-transition-rule-configurations --types example-value",
		Annotations: map[string]string{"pp:endpoint": "jira-workflow.get-transition-rule-configurations", "pp:method": "GET", "pp:path": "/rest/api/3/workflow/rule/config", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("types") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "types")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/workflow/rule/config"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "jira-workflow", path, map[string]string{
				"startAt":       fmt.Sprintf("%v", flagStartAt),
				"maxResults":    fmt.Sprintf("%v", flagMaxResults),
				"types":         fmt.Sprintf("%v", flagTypes),
				"keys":          fmt.Sprintf("%v", flagKeys),
				"workflowNames": fmt.Sprintf("%v", flagWorkflowNames),
				"withTags":      fmt.Sprintf("%v", flagWithTags),
				"draft":         fmt.Sprintf("%v", flagDraft),
				"expand":        fmt.Sprintf("%v", flagExpand),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagStartAt, "start-at", 0, "The index of the first item to return in a page of results (page offset).")
	cmd.Flags().IntVar(&flagMaxResults, "max-results", 10, "The maximum number of items to return per page.")
	cmd.Flags().StringVar(&flagTypes, "types", "", "The types of the transition rules to return.")
	cmd.Flags().StringVar(&flagKeys, "keys", "", "The transition rule class keys, as defined in the Connect or the Forge app descriptor, of the transition rules to...")
	cmd.Flags().StringVar(&flagWorkflowNames, "workflow-names", "", "The list of workflow names to filter by.")
	cmd.Flags().StringVar(&flagWithTags, "with-tags", "", "The list of `tags` to filter by.")
	cmd.Flags().BoolVar(&flagDraft, "draft", false, "**Deprecated:** Whether draft or published workflows are returned. If not provided, both workflow types are...")
	cmd.Flags().StringVar(&flagExpand, "expand", "", "Use [expand](#expansion) to include additional information in the response. This parameter accepts `transition`,...")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
