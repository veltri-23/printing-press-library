// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIssueEditmetaGetEditIssueMetaCmd(flags *rootFlags) *cobra.Command {
	var flagOverrideScreenSecurity bool
	var flagOverrideEditableFlag bool

	cmd := &cobra.Command{
		Use:         "get-edit-issue-meta <issueIdOrKey>",
		Aliases:     []string{"get"},
		Short:       "Returns the edit screen fields for an issue that are visible to and editable by the user. Use the information to...",
		Example:     "  jira-pp-cli issue editmeta get-edit-issue-meta your-token-here",
		Annotations: map[string]string{"pp:endpoint": "editmeta.get-edit-issue-meta", "pp:method": "GET", "pp:path": "/rest/api/3/issue/{issueIdOrKey}/editmeta", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/issue/{issueIdOrKey}/editmeta"
			path = replacePathParam(path, "issueIdOrKey", args[0])
			params := map[string]string{}
			if flagOverrideScreenSecurity != false {
				params["overrideScreenSecurity"] = fmt.Sprintf("%v", flagOverrideScreenSecurity)
			}
			if flagOverrideEditableFlag != false {
				params["overrideEditableFlag"] = fmt.Sprintf("%v", flagOverrideEditableFlag)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "editmeta", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagOverrideScreenSecurity, "override-screen-security", false, "Whether hidden fields are returned. Available to Connect and Forge app users with *Administer Jira* [global...")
	cmd.Flags().BoolVar(&flagOverrideEditableFlag, "override-editable-flag", false, "Whether non-editable fields are returned. Available to Connect and Forge app users with *Administer Jira* [global...")

	return cmd
}
