// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIssueWorklogBulkDeleteCmd(flags *rootFlags) *cobra.Command {
	var flagAdjustEstimate string
	var flagOverrideEditableFlag bool
	var bodyIds string

	cmd := &cobra.Command{
		Use:         "bulk-delete <issueIdOrKey>",
		Short:       "Deletes a list of worklogs from an issue. This is an experimental API with limitations: * You can't delete more than...",
		Example:     "  jira-pp-cli issue worklog bulk-delete your-token-here",
		Annotations: map[string]string{"pp:endpoint": "worklog.bulk-delete", "pp:method": "DELETE", "pp:path": "/rest/api/3/issue/{issueIdOrKey}/worklog"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("adjust-estimate") {
				allowedAdjustEstimate := []string{"leave", "auto"}
				validAdjustEstimate := false
				for _, v := range allowedAdjustEstimate {
					if flagAdjustEstimate == v {
						validAdjustEstimate = true
						break
					}
				}
				if !validAdjustEstimate {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "adjust-estimate", flagAdjustEstimate, allowedAdjustEstimate)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/issue/{issueIdOrKey}/worklog"
			path = replacePathParam(path, "issueIdOrKey", args[0])
			data, statusCode, err := c.Delete(path)
			if err != nil {
				return classifyDeleteError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "delete",
					"resource": "worklog",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAdjustEstimate, "adjust-estimate", "auto", "Defines how to update the issue's time estimate, the options are: * `leave` Leaves the estimate unchanged. * `auto`... (one of: leave, auto)")
	cmd.Flags().BoolVar(&flagOverrideEditableFlag, "override-editable-flag", false, "Whether the work log entries should be removed to the issue even if the issue is not editable, because...")
	cmd.Flags().StringVar(&bodyIds, "ids", "", "A list of worklog IDs.")

	return cmd
}
