// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newUserCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyApplicationKeys string
	var bodyDisplayName string
	var bodyEmailAddress string
	var bodyKey string
	var bodyName string
	var bodyPassword string
	var bodyProducts string
	var bodySelf string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Creates a user. This resource is retained for legacy compatibility. As soon as a more suitable alternative is...",
		Example:     "  jira-pp-cli user create --email-address user@example.com",
		Annotations: map[string]string{"pp:endpoint": "user.create", "pp:method": "POST", "pp:path": "/rest/api/3/user"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("email-address") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "email-address")
				}
				if !cmd.Flags().Changed("products") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "products")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/user"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyApplicationKeys != "" {
					var parsedApplicationKeys any
					if err := json.Unmarshal([]byte(bodyApplicationKeys), &parsedApplicationKeys); err != nil {
						return fmt.Errorf("parsing --application-keys JSON: %w", err)
					}
					body["applicationKeys"] = parsedApplicationKeys
				}
				if bodyDisplayName != "" {
					body["displayName"] = bodyDisplayName
				}
				if bodyEmailAddress != "" {
					body["emailAddress"] = bodyEmailAddress
				}
				if bodyKey != "" {
					body["key"] = bodyKey
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPassword != "" {
					body["password"] = bodyPassword
				}
				if bodyProducts != "" {
					var parsedProducts any
					if err := json.Unmarshal([]byte(bodyProducts), &parsedProducts); err != nil {
						return fmt.Errorf("parsing --products JSON: %w", err)
					}
					body["products"] = parsedProducts
				}
				if bodySelf != "" {
					body["self"] = bodySelf
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "user",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyApplicationKeys, "application-keys", "", "Deprecated, do not use.")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "This property is no longer available. If the user has an Atlassian account, their display name is not changed. If...")
	cmd.Flags().StringVar(&bodyEmailAddress, "email-address", "", "The email address for the user.")
	cmd.Flags().StringVar(&bodyKey, "key", "", "This property is no longer available. See the [migration guide](https://developer.atlassian.com/cloud/jira/platform/d...")
	cmd.Flags().StringVar(&bodyName, "name", "", "This property is no longer available. See the [migration guide](https://developer.atlassian.com/cloud/jira/platform/d...")
	cmd.Flags().StringVar(&bodyPassword, "password", "", "This property is no longer available. If the user has an Atlassian account, their password is not changed. If the...")
	cmd.Flags().StringVar(&bodyProducts, "products", "", "Products the new user has access to. Valid products are: jira-core, jira-servicedesk, jira-product-discovery,...")
	cmd.Flags().StringVar(&bodySelf, "self", "", "The URL of the user.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
