// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newPriorityschemeCreatePrioritySchemeCmd(flags *rootFlags) *cobra.Command {
	var bodyDefaultPriorityId int
	var bodyDescription string
	var bodyMappingsIn string
	var bodyMappingsOut string
	var bodyName string
	var bodyPriorityIds string
	var bodyProjectIds string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-priority-scheme",
		Aliases:     []string{"create"},
		Short:       "Creates a new priority scheme. **[Permissions](#permissions) required:** *Administer Jira* [global...",
		Example:     "  jira-pp-cli priorityscheme create-priority-scheme --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "priorityscheme.create-priority-scheme", "pp:method": "POST", "pp:path": "/rest/api/3/priorityscheme"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("default-priority-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "default-priority-id")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("priority-ids") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "priority-ids")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/priorityscheme"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyDefaultPriorityId != 0 {
					body["defaultPriorityId"] = bodyDefaultPriorityId
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				{
					nestedMappings := map[string]any{}
					if bodyMappingsIn != "" {
						var parsedMappingsIn any
						if err := json.Unmarshal([]byte(bodyMappingsIn), &parsedMappingsIn); err != nil {
							return fmt.Errorf("parsing --mappings-in JSON: %w", err)
						}
						nestedMappings["in"] = parsedMappingsIn
					}
					if bodyMappingsOut != "" {
						var parsedMappingsOut any
						if err := json.Unmarshal([]byte(bodyMappingsOut), &parsedMappingsOut); err != nil {
							return fmt.Errorf("parsing --mappings-out JSON: %w", err)
						}
						nestedMappings["out"] = parsedMappingsOut
					}
					if len(nestedMappings) > 0 {
						body["mappings"] = nestedMappings
					}
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPriorityIds != "" {
					var parsedPriorityIds any
					if err := json.Unmarshal([]byte(bodyPriorityIds), &parsedPriorityIds); err != nil {
						return fmt.Errorf("parsing --priority-ids JSON: %w", err)
					}
					body["priorityIds"] = parsedPriorityIds
				}
				if bodyProjectIds != "" {
					var parsedProjectIds any
					if err := json.Unmarshal([]byte(bodyProjectIds), &parsedProjectIds); err != nil {
						return fmt.Errorf("parsing --project-ids JSON: %w", err)
					}
					body["projectIds"] = parsedProjectIds
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "priorityscheme",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyDefaultPriorityId, "default-priority-id", 0, "The ID of the default priority for the priority scheme.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "The description of the priority scheme.")
	cmd.Flags().StringVar(&bodyMappingsIn, "mappings-in", "", "The mapping of priorities for issues being migrated **into** this priority scheme. Key is the old priority ID, value...")
	cmd.Flags().StringVar(&bodyMappingsOut, "mappings-out", "", "The mapping of priorities for issues being migrated **out of** this priority scheme. Key is the old priority ID...")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the priority scheme. Must be unique.")
	cmd.Flags().StringVar(&bodyPriorityIds, "priority-ids", "", "The IDs of priorities in the scheme.")
	cmd.Flags().StringVar(&bodyProjectIds, "project-ids", "", "The IDs of projects that will use the priority scheme.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
