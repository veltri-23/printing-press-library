// Copyright 2026 Nikica Jokic and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newConfigGetFieldAssociationSchemesCmd(flags *rootFlags) *cobra.Command {
	var flagProjectId string
	var flagQuery string
	var flagStartAt int
	var flagMaxResults int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-field-association-schemes",
		Aliases:     []string{"list"},
		Short:       "REST endpoint for retrieving a paginated list of field association schemes with optional filtering. This endpoint...",
		Example:     "  jira-pp-cli config get-field-association-schemes",
		Annotations: map[string]string{"pp:endpoint": "config.get-field-association-schemes", "pp:method": "GET", "pp:path": "/rest/api/3/config/fieldschemes", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/api/3/config/fieldschemes"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "config", path, map[string]string{
				"projectId":  fmt.Sprintf("%v", flagProjectId),
				"query":      fmt.Sprintf("%v", flagQuery),
				"startAt":    fmt.Sprintf("%v", flagStartAt),
				"maxResults": fmt.Sprintf("%v", flagMaxResults),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagProjectId, "project-id", "", "(optional) List of project IDs to filter schemes by. If not provided, schemes from all projects are returned.")
	cmd.Flags().StringVar(&flagQuery, "query", "", "(optional) Text filter for scheme name or description matching (case-insensitive). If not provided, no text...")
	cmd.Flags().IntVar(&flagStartAt, "start-at", 0, "Zero-based index of the first item to return (default: 0)")
	cmd.Flags().IntVar(&flagMaxResults, "max-results", 50, "Maximum number of items to return per page (default: 50, max: 100)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
