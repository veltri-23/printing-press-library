// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAdminSettingsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAvatars string
	var bodyDefaultTokenDuration string
	var bodyDefaultTokenLength int
	var bodyDefaultUserChangeEmail bool
	var bodyDefaultUserChangeName bool
	var bodyDefaultUserChangeUsername bool
	var bodyEventRetention string
	var bodyFlagsFlowsRefreshOthers bool
	var bodyFlagsPoliciesBufferedAccessView bool
	var bodyFooterLinks string
	var bodyGdprCompliance bool
	var bodyImpersonation bool
	var bodyImpersonationRequireReason bool
	var bodyReputationLowerLimit int
	var bodyReputationUpperLimit int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "settings-update",
		Short:       "Settings view",
		Example:     "  authentik-pp-cli admin settings-update",
		Annotations: map[string]string{"pp:endpoint": "admin.settings-update", "pp:method": "PUT", "pp:path": "/admin/settings/"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("flags-flows-refresh-others") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "flags-flows-refresh-others")
				}
				if !cmd.Flags().Changed("flags-policies-buffered-access-view") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "flags-policies-buffered-access-view")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/admin/settings/"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAvatars != "" {
					body["avatars"] = bodyAvatars
				}
				if bodyDefaultTokenDuration != "" {
					body["default_token_duration"] = bodyDefaultTokenDuration
				}
				if bodyDefaultTokenLength != 0 {
					body["default_token_length"] = bodyDefaultTokenLength
				}
				if cmd.Flags().Changed("default-user-change-email") {
					body["default_user_change_email"] = bodyDefaultUserChangeEmail
				}
				if cmd.Flags().Changed("default-user-change-name") {
					body["default_user_change_name"] = bodyDefaultUserChangeName
				}
				if cmd.Flags().Changed("default-user-change-username") {
					body["default_user_change_username"] = bodyDefaultUserChangeUsername
				}
				if bodyEventRetention != "" {
					body["event_retention"] = bodyEventRetention
				}
				{
					nestedFlags := map[string]any{}
					if cmd.Flags().Changed("flags-flows-refresh-others") {
						nestedFlags["flows_refresh_others"] = bodyFlagsFlowsRefreshOthers
					}
					if cmd.Flags().Changed("flags-policies-buffered-access-view") {
						nestedFlags["policies_buffered_access_view"] = bodyFlagsPoliciesBufferedAccessView
					}
					if len(nestedFlags) > 0 {
						body["flags"] = nestedFlags
					}
				}
				if bodyFooterLinks != "" {
					body["footer_links"] = bodyFooterLinks
				}
				if cmd.Flags().Changed("gdpr-compliance") {
					body["gdpr_compliance"] = bodyGdprCompliance
				}
				if cmd.Flags().Changed("impersonation") {
					body["impersonation"] = bodyImpersonation
				}
				if cmd.Flags().Changed("impersonation-require-reason") {
					body["impersonation_require_reason"] = bodyImpersonationRequireReason
				}
				if bodyReputationLowerLimit != 0 {
					body["reputation_lower_limit"] = bodyReputationLowerLimit
				}
				if bodyReputationUpperLimit != 0 {
					body["reputation_upper_limit"] = bodyReputationUpperLimit
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "admin", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "admin", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "admin",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAvatars, "avatars", "", "Configure how authentik should show avatars for users.")
	cmd.Flags().StringVar(&bodyDefaultTokenDuration, "default-token-duration", "", "Default token duration")
	cmd.Flags().IntVar(&bodyDefaultTokenLength, "default-token-length", 0, "Default token length")
	cmd.Flags().BoolVar(&bodyDefaultUserChangeEmail, "default-user-change-email", false, "Enable the ability for users to change their email address.")
	cmd.Flags().BoolVar(&bodyDefaultUserChangeName, "default-user-change-name", false, "Enable the ability for users to change their name.")
	cmd.Flags().BoolVar(&bodyDefaultUserChangeUsername, "default-user-change-username", false, "Enable the ability for users to change their username.")
	cmd.Flags().StringVar(&bodyEventRetention, "event-retention", "", "Events will be deleted after this duration.(Format: weeks=3;days=2;hours=3,seconds=2).")
	cmd.Flags().BoolVar(&bodyFlagsFlowsRefreshOthers, "flags-flows-refresh-others", false, "Flows refresh others")
	cmd.Flags().BoolVar(&bodyFlagsPoliciesBufferedAccessView, "flags-policies-buffered-access-view", false, "Policies buffered access view")
	cmd.Flags().StringVar(&bodyFooterLinks, "footer-links", "", "Footer links")
	cmd.Flags().BoolVar(&bodyGdprCompliance, "gdpr-compliance", false, "When enabled, all the events caused by a user will be deleted upon the user's deletion.")
	cmd.Flags().BoolVar(&bodyImpersonation, "impersonation", false, "Globally enable/disable impersonation.")
	cmd.Flags().BoolVar(&bodyImpersonationRequireReason, "impersonation-require-reason", false, "Require administrators to provide a reason for impersonating a user.")
	cmd.Flags().IntVar(&bodyReputationLowerLimit, "reputation-lower-limit", 0, "Reputation cannot decrease lower than this value. Zero or negative.")
	cmd.Flags().IntVar(&bodyReputationUpperLimit, "reputation-upper-limit", 0, "Reputation cannot increase higher than this value. Zero or positive.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
