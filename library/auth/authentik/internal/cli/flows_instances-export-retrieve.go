// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newFlowsInstancesExportRetrieveCmd(flags *rootFlags) *cobra.Command {

	cmd := &cobra.Command{
		Use:         "instances-export-retrieve <slug>",
		Short:       "Export flow to .yaml file",
		Example:     "  authentik-pp-cli flows instances-export-retrieve example-value",
		Annotations: map[string]string{"pp:endpoint": "flows.instances-export-retrieve", "pp:method": "GET", "pp:path": "/flows/instances/{slug}/export/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/flows/instances/{slug}/export/"
			path = replacePathParam(path, "slug", args[0])
			headerOverrides := map[string]string{
				"X-Printing-Press-Binary-Response": "true",
			}
			params := map[string]string{}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "flows", false, path, params, headerOverrides, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = prov
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>")
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}

	return cmd
}
