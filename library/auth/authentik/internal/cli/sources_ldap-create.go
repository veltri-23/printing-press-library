// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSourcesLdapCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAdditionalGroupDn string
	var bodyAdditionalUserDn string
	var bodyAuthenticationFlow string
	var bodyBaseDn string
	var bodyBindCn string
	var bodyBindPassword string
	var bodyClientCertificate string
	var bodyDeleteNotFoundObjects bool
	var bodyEnabled bool
	var bodyEnrollmentFlow string
	var bodyGroupMembershipField string
	var bodyGroupObjectFilter string
	var bodyGroupPropertyMappings string
	var bodyLookupGroupsFromUser bool
	var bodyName string
	var bodyObjectUniquenessField string
	var bodyPasswordLoginUpdateInternalPassword bool
	var bodyPeerCertificate string
	var bodyPolicyEngineMode string
	var bodyServerUri string
	var bodySlug string
	var bodySni bool
	var bodyStartTls bool
	var bodySyncGroups bool
	var bodySyncParentGroup string
	var bodySyncUsers bool
	var bodySyncUsersPassword bool
	var bodyUserMatchingMode string
	var bodyUserMembershipAttribute string
	var bodyUserObjectFilter string
	var bodyUserPathTemplate string
	var bodyUserPropertyMappings string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "ldap-create",
		Short:       "LDAP Source Viewset",
		Example:     "  authentik-pp-cli sources ldap-create --base-dn example-value",
		Annotations: map[string]string{"pp:endpoint": "sources.ldap-create", "pp:method": "POST", "pp:path": "/sources/ldap/"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("base-dn") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "base-dn")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("server-uri") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "server-uri")
				}
				if !cmd.Flags().Changed("slug") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "slug")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/sources/ldap/"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAdditionalGroupDn != "" {
					body["additional_group_dn"] = bodyAdditionalGroupDn
				}
				if bodyAdditionalUserDn != "" {
					body["additional_user_dn"] = bodyAdditionalUserDn
				}
				if bodyAuthenticationFlow != "" {
					body["authentication_flow"] = bodyAuthenticationFlow
				}
				if bodyBaseDn != "" {
					body["base_dn"] = bodyBaseDn
				}
				if bodyBindCn != "" {
					body["bind_cn"] = bodyBindCn
				}
				if bodyBindPassword != "" {
					body["bind_password"] = bodyBindPassword
				}
				if bodyClientCertificate != "" {
					body["client_certificate"] = bodyClientCertificate
				}
				if cmd.Flags().Changed("delete-not-found-objects") {
					body["delete_not_found_objects"] = bodyDeleteNotFoundObjects
				}
				if cmd.Flags().Changed("enabled") {
					body["enabled"] = bodyEnabled
				}
				if bodyEnrollmentFlow != "" {
					body["enrollment_flow"] = bodyEnrollmentFlow
				}
				if bodyGroupMembershipField != "" {
					body["group_membership_field"] = bodyGroupMembershipField
				}
				if bodyGroupObjectFilter != "" {
					body["group_object_filter"] = bodyGroupObjectFilter
				}
				if bodyGroupPropertyMappings != "" {
					var parsedGroupPropertyMappings any
					if err := json.Unmarshal([]byte(bodyGroupPropertyMappings), &parsedGroupPropertyMappings); err != nil {
						return fmt.Errorf("parsing --group-property-mappings JSON: %w", err)
					}
					body["group_property_mappings"] = parsedGroupPropertyMappings
				}
				if cmd.Flags().Changed("lookup-groups-from-user") {
					body["lookup_groups_from_user"] = bodyLookupGroupsFromUser
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyObjectUniquenessField != "" {
					body["object_uniqueness_field"] = bodyObjectUniquenessField
				}
				if cmd.Flags().Changed("password-login-update-internal-password") {
					body["password_login_update_internal_password"] = bodyPasswordLoginUpdateInternalPassword
				}
				if bodyPeerCertificate != "" {
					body["peer_certificate"] = bodyPeerCertificate
				}
				if bodyPolicyEngineMode != "" {
					body["policy_engine_mode"] = bodyPolicyEngineMode
				}
				if bodyServerUri != "" {
					body["server_uri"] = bodyServerUri
				}
				if bodySlug != "" {
					body["slug"] = bodySlug
				}
				if cmd.Flags().Changed("sni") {
					body["sni"] = bodySni
				}
				if cmd.Flags().Changed("start-tls") {
					body["start_tls"] = bodyStartTls
				}
				if cmd.Flags().Changed("sync-groups") {
					body["sync_groups"] = bodySyncGroups
				}
				if bodySyncParentGroup != "" {
					body["sync_parent_group"] = bodySyncParentGroup
				}
				if cmd.Flags().Changed("sync-users") {
					body["sync_users"] = bodySyncUsers
				}
				if cmd.Flags().Changed("sync-users-password") {
					body["sync_users_password"] = bodySyncUsersPassword
				}
				if bodyUserMatchingMode != "" {
					body["user_matching_mode"] = bodyUserMatchingMode
				}
				if bodyUserMembershipAttribute != "" {
					body["user_membership_attribute"] = bodyUserMembershipAttribute
				}
				if bodyUserObjectFilter != "" {
					body["user_object_filter"] = bodyUserObjectFilter
				}
				if bodyUserPathTemplate != "" {
					body["user_path_template"] = bodyUserPathTemplate
				}
				if bodyUserPropertyMappings != "" {
					var parsedUserPropertyMappings any
					if err := json.Unmarshal([]byte(bodyUserPropertyMappings), &parsedUserPropertyMappings); err != nil {
						return fmt.Errorf("parsing --user-property-mappings JSON: %w", err)
					}
					body["user_property_mappings"] = parsedUserPropertyMappings
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "sources", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "sources", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sources", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sources", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sources", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "sources",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sources", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sources", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAdditionalGroupDn, "additional-group-dn", "", "Prepended to Base DN for Group-queries.")
	cmd.Flags().StringVar(&bodyAdditionalUserDn, "additional-user-dn", "", "Prepended to Base DN for User-queries.")
	cmd.Flags().StringVar(&bodyAuthenticationFlow, "authentication-flow", "", "Flow to use when authenticating existing users.")
	cmd.Flags().StringVar(&bodyBaseDn, "base-dn", "", "Base dn")
	cmd.Flags().StringVar(&bodyBindCn, "bind-cn", "", "Bind cn")
	cmd.Flags().StringVar(&bodyBindPassword, "bind-password", "", "Bind password")
	cmd.Flags().StringVar(&bodyClientCertificate, "client-certificate", "", "Client certificate to authenticate against the LDAP Server's Certificate.")
	cmd.Flags().BoolVar(&bodyDeleteNotFoundObjects, "delete-not-found-objects", false, "Delete authentik users and groups which were previously supplied by this source, but are now missing from it.")
	cmd.Flags().BoolVar(&bodyEnabled, "enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyEnrollmentFlow, "enrollment-flow", "", "Flow to use when enrolling new users.")
	cmd.Flags().StringVar(&bodyGroupMembershipField, "group-membership-field", "", "Field which contains members of a group.")
	cmd.Flags().StringVar(&bodyGroupObjectFilter, "group-object-filter", "", "Consider Objects matching this filter to be Groups.")
	cmd.Flags().StringVar(&bodyGroupPropertyMappings, "group-property-mappings", "", "Group property mappings")
	cmd.Flags().BoolVar(&bodyLookupGroupsFromUser, "lookup-groups-from-user", false, "Lookup group membership based on a user attribute instead of a group attribute.")
	cmd.Flags().StringVar(&bodyName, "name", "", "Source's display Name.")
	cmd.Flags().StringVar(&bodyObjectUniquenessField, "object-uniqueness-field", "", "Field which contains a unique Identifier.")
	cmd.Flags().BoolVar(&bodyPasswordLoginUpdateInternalPassword, "password-login-update-internal-password", false, "Update internal authentik password when login succeeds with LDAP")
	cmd.Flags().StringVar(&bodyPeerCertificate, "peer-certificate", "", "Optionally verify the LDAP Server's Certificate against the CA Chain in this keypair.")
	cmd.Flags().StringVar(&bodyPolicyEngineMode, "policy-engine-mode", "", "Policy engine mode")
	cmd.Flags().StringVar(&bodyServerUri, "server-uri", "", "Server uri")
	cmd.Flags().StringVar(&bodySlug, "slug", "", "Internal source name, used in URLs.")
	cmd.Flags().BoolVar(&bodySni, "sni", false, "Sni")
	cmd.Flags().BoolVar(&bodyStartTls, "start-tls", false, "Start tls")
	cmd.Flags().BoolVar(&bodySyncGroups, "sync-groups", false, "Sync groups")
	cmd.Flags().StringVar(&bodySyncParentGroup, "sync-parent-group", "", "Sync parent group")
	cmd.Flags().BoolVar(&bodySyncUsers, "sync-users", false, "Sync users")
	cmd.Flags().BoolVar(&bodySyncUsersPassword, "sync-users-password", false, "When a user changes their password, sync it back to LDAP. This can only be enabled on a single LDAP source.")
	cmd.Flags().StringVar(&bodyUserMatchingMode, "user-matching-mode", "", "How the source determines if an existing user should be authenticated or a new user enrolled.")
	cmd.Flags().StringVar(&bodyUserMembershipAttribute, "user-membership-attribute", "", "Attribute which matches the value of `group_membership_field`.")
	cmd.Flags().StringVar(&bodyUserObjectFilter, "user-object-filter", "", "Consider Objects matching this filter to be Users.")
	cmd.Flags().StringVar(&bodyUserPathTemplate, "user-path-template", "", "User path template")
	cmd.Flags().StringVar(&bodyUserPropertyMappings, "user-property-mappings", "", "User property mappings")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
