// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCoreBrandsListCmd(flags *rootFlags) *cobra.Command {
	var flagBrandUuid string
	var flagBrandingDefaultFlowBackground string
	var flagBrandingFavicon string
	var flagBrandingLogo string
	var flagBrandingTitle string
	var flagClientCertificates string
	var flagDefault bool
	var flagDomain string
	var flagFlowAuthentication string
	var flagFlowDeviceCode string
	var flagFlowInvalidation string
	var flagFlowRecovery string
	var flagFlowUnenrollment string
	var flagFlowUserSettings string
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagSearch string
	var flagWebCertificate string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "brands-list",
		Short:       "Brand Viewset",
		Example:     "  authentik-pp-cli core brands-list",
		Annotations: map[string]string{"pp:endpoint": "core.brands-list", "pp:method": "GET", "pp:path": "/core/brands/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/core/brands/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "core", path, map[string]string{
				"brand_uuid":                       fmt.Sprintf("%v", flagBrandUuid),
				"branding_default_flow_background": fmt.Sprintf("%v", flagBrandingDefaultFlowBackground),
				"branding_favicon":                 fmt.Sprintf("%v", flagBrandingFavicon),
				"branding_logo":                    fmt.Sprintf("%v", flagBrandingLogo),
				"branding_title":                   fmt.Sprintf("%v", flagBrandingTitle),
				"client_certificates":              fmt.Sprintf("%v", flagClientCertificates),
				"default":                          fmt.Sprintf("%v", flagDefault),
				"domain":                           fmt.Sprintf("%v", flagDomain),
				"flow_authentication":              fmt.Sprintf("%v", flagFlowAuthentication),
				"flow_device_code":                 fmt.Sprintf("%v", flagFlowDeviceCode),
				"flow_invalidation":                fmt.Sprintf("%v", flagFlowInvalidation),
				"flow_recovery":                    fmt.Sprintf("%v", flagFlowRecovery),
				"flow_unenrollment":                fmt.Sprintf("%v", flagFlowUnenrollment),
				"flow_user_settings":               fmt.Sprintf("%v", flagFlowUserSettings),
				"ordering":                         fmt.Sprintf("%v", flagOrdering),
				"page":                             fmt.Sprintf("%v", flagPage),
				"page_size":                        fmt.Sprintf("%v", flagPageSize),
				"search":                           fmt.Sprintf("%v", flagSearch),
				"web_certificate":                  fmt.Sprintf("%v", flagWebCertificate),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagBrandUuid, "brand-uuid", "", "Brand uuid")
	cmd.Flags().StringVar(&flagBrandingDefaultFlowBackground, "branding-default-flow-background", "", "Branding default flow background")
	cmd.Flags().StringVar(&flagBrandingFavicon, "branding-favicon", "", "Branding favicon")
	cmd.Flags().StringVar(&flagBrandingLogo, "branding-logo", "", "Branding logo")
	cmd.Flags().StringVar(&flagBrandingTitle, "branding-title", "", "Branding title")
	cmd.Flags().StringVar(&flagClientCertificates, "client-certificates", "", "Client certificates")
	cmd.Flags().BoolVar(&flagDefault, "default", false, "Default")
	cmd.Flags().StringVar(&flagDomain, "domain", "", "Domain")
	cmd.Flags().StringVar(&flagFlowAuthentication, "flow-authentication", "", "Flow authentication")
	cmd.Flags().StringVar(&flagFlowDeviceCode, "flow-device-code", "", "Flow device code")
	cmd.Flags().StringVar(&flagFlowInvalidation, "flow-invalidation", "", "Flow invalidation")
	cmd.Flags().StringVar(&flagFlowRecovery, "flow-recovery", "", "Flow recovery")
	cmd.Flags().StringVar(&flagFlowUnenrollment, "flow-unenrollment", "", "Flow unenrollment")
	cmd.Flags().StringVar(&flagFlowUserSettings, "flow-user-settings", "", "Flow user settings")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagWebCertificate, "web-certificate", "", "Web certificate")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
