// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSourcesLdapListCmd(flags *rootFlags) *cobra.Command {
	var flagAdditionalGroupDn string
	var flagAdditionalUserDn string
	var flagBaseDn string
	var flagBindCn string
	var flagClientCertificate string
	var flagDeleteNotFoundObjects bool
	var flagEnabled bool
	var flagGroupMembershipField string
	var flagGroupObjectFilter string
	var flagGroupPropertyMappings string
	var flagLookupGroupsFromUser bool
	var flagName string
	var flagObjectUniquenessField string
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagPasswordLoginUpdateInternalPassword bool
	var flagPbmUuid string
	var flagPeerCertificate string
	var flagSearch string
	var flagServerUri string
	var flagSlug string
	var flagSni bool
	var flagStartTls bool
	var flagSyncGroups bool
	var flagSyncParentGroup string
	var flagSyncUsers bool
	var flagSyncUsersPassword bool
	var flagUserMembershipAttribute string
	var flagUserObjectFilter string
	var flagUserPropertyMappings string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "ldap-list",
		Short:       "LDAP Source Viewset",
		Example:     "  authentik-pp-cli sources ldap-list",
		Annotations: map[string]string{"pp:endpoint": "sources.ldap-list", "pp:method": "GET", "pp:path": "/sources/ldap/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/sources/ldap/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "sources", path, map[string]string{
				"additional_group_dn":      fmt.Sprintf("%v", flagAdditionalGroupDn),
				"additional_user_dn":       fmt.Sprintf("%v", flagAdditionalUserDn),
				"base_dn":                  fmt.Sprintf("%v", flagBaseDn),
				"bind_cn":                  fmt.Sprintf("%v", flagBindCn),
				"client_certificate":       fmt.Sprintf("%v", flagClientCertificate),
				"delete_not_found_objects": fmt.Sprintf("%v", flagDeleteNotFoundObjects),
				"enabled":                  fmt.Sprintf("%v", flagEnabled),
				"group_membership_field":   fmt.Sprintf("%v", flagGroupMembershipField),
				"group_object_filter":      fmt.Sprintf("%v", flagGroupObjectFilter),
				"group_property_mappings":  fmt.Sprintf("%v", flagGroupPropertyMappings),
				"lookup_groups_from_user":  fmt.Sprintf("%v", flagLookupGroupsFromUser),
				"name":                     fmt.Sprintf("%v", flagName),
				"object_uniqueness_field":  fmt.Sprintf("%v", flagObjectUniquenessField),
				"ordering":                 fmt.Sprintf("%v", flagOrdering),
				"page":                     fmt.Sprintf("%v", flagPage),
				"page_size":                fmt.Sprintf("%v", flagPageSize),
				"password_login_update_internal_password": fmt.Sprintf("%v", flagPasswordLoginUpdateInternalPassword),
				"pbm_uuid":                  fmt.Sprintf("%v", flagPbmUuid),
				"peer_certificate":          fmt.Sprintf("%v", flagPeerCertificate),
				"search":                    fmt.Sprintf("%v", flagSearch),
				"server_uri":                fmt.Sprintf("%v", flagServerUri),
				"slug":                      fmt.Sprintf("%v", flagSlug),
				"sni":                       fmt.Sprintf("%v", flagSni),
				"start_tls":                 fmt.Sprintf("%v", flagStartTls),
				"sync_groups":               fmt.Sprintf("%v", flagSyncGroups),
				"sync_parent_group":         fmt.Sprintf("%v", flagSyncParentGroup),
				"sync_users":                fmt.Sprintf("%v", flagSyncUsers),
				"sync_users_password":       fmt.Sprintf("%v", flagSyncUsersPassword),
				"user_membership_attribute": fmt.Sprintf("%v", flagUserMembershipAttribute),
				"user_object_filter":        fmt.Sprintf("%v", flagUserObjectFilter),
				"user_property_mappings":    fmt.Sprintf("%v", flagUserPropertyMappings),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAdditionalGroupDn, "additional-group-dn", "", "Additional group dn")
	cmd.Flags().StringVar(&flagAdditionalUserDn, "additional-user-dn", "", "Additional user dn")
	cmd.Flags().StringVar(&flagBaseDn, "base-dn", "", "Base dn")
	cmd.Flags().StringVar(&flagBindCn, "bind-cn", "", "Bind cn")
	cmd.Flags().StringVar(&flagClientCertificate, "client-certificate", "", "Client certificate")
	cmd.Flags().BoolVar(&flagDeleteNotFoundObjects, "delete-not-found-objects", false, "Delete not found objects")
	cmd.Flags().BoolVar(&flagEnabled, "enabled", false, "Enabled")
	cmd.Flags().StringVar(&flagGroupMembershipField, "group-membership-field", "", "Group membership field")
	cmd.Flags().StringVar(&flagGroupObjectFilter, "group-object-filter", "", "Group object filter")
	cmd.Flags().StringVar(&flagGroupPropertyMappings, "group-property-mappings", "", "Group property mappings")
	cmd.Flags().BoolVar(&flagLookupGroupsFromUser, "lookup-groups-from-user", false, "Lookup groups from user")
	cmd.Flags().StringVar(&flagName, "name", "", "Name")
	cmd.Flags().StringVar(&flagObjectUniquenessField, "object-uniqueness-field", "", "Object uniqueness field")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().BoolVar(&flagPasswordLoginUpdateInternalPassword, "password-login-update-internal-password", false, "Password login update internal password")
	cmd.Flags().StringVar(&flagPbmUuid, "pbm-uuid", "", "Pbm uuid")
	cmd.Flags().StringVar(&flagPeerCertificate, "peer-certificate", "", "Peer certificate")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagServerUri, "server-uri", "", "Server uri")
	cmd.Flags().StringVar(&flagSlug, "slug", "", "Slug")
	cmd.Flags().BoolVar(&flagSni, "sni", false, "Sni")
	cmd.Flags().BoolVar(&flagStartTls, "start-tls", false, "Start tls")
	cmd.Flags().BoolVar(&flagSyncGroups, "sync-groups", false, "Sync groups")
	cmd.Flags().StringVar(&flagSyncParentGroup, "sync-parent-group", "", "Sync parent group")
	cmd.Flags().BoolVar(&flagSyncUsers, "sync-users", false, "Sync users")
	cmd.Flags().BoolVar(&flagSyncUsersPassword, "sync-users-password", false, "Sync users password")
	cmd.Flags().StringVar(&flagUserMembershipAttribute, "user-membership-attribute", "", "User membership attribute")
	cmd.Flags().StringVar(&flagUserObjectFilter, "user-object-filter", "", "User object filter")
	cmd.Flags().StringVar(&flagUserPropertyMappings, "user-property-mappings", "", "User property mappings")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
