// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newStagesAuthenticatorEmailListCmd(flags *rootFlags) *cobra.Command {
	var flagConfigureFlow string
	var flagFriendlyName string
	var flagFromAddress string
	var flagHost string
	var flagName string
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagPassword string
	var flagPort int
	var flagSearch string
	var flagStageUuid string
	var flagSubject string
	var flagTemplate string
	var flagTimeout int
	var flagTokenExpiry string
	var flagUseGlobalSettings bool
	var flagUseSsl bool
	var flagUseTls bool
	var flagUsername string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "authenticator-email-list",
		Short:       "AuthenticatorEmailStage Viewset",
		Example:     "  authentik-pp-cli stages authenticator-email-list",
		Annotations: map[string]string{"pp:endpoint": "stages.authenticator-email-list", "pp:method": "GET", "pp:path": "/stages/authenticator/email/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/stages/authenticator/email/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "stages", path, map[string]string{
				"configure_flow":      fmt.Sprintf("%v", flagConfigureFlow),
				"friendly_name":       fmt.Sprintf("%v", flagFriendlyName),
				"from_address":        fmt.Sprintf("%v", flagFromAddress),
				"host":                fmt.Sprintf("%v", flagHost),
				"name":                fmt.Sprintf("%v", flagName),
				"ordering":            fmt.Sprintf("%v", flagOrdering),
				"page":                fmt.Sprintf("%v", flagPage),
				"page_size":           fmt.Sprintf("%v", flagPageSize),
				"password":            fmt.Sprintf("%v", flagPassword),
				"port":                fmt.Sprintf("%v", flagPort),
				"search":              fmt.Sprintf("%v", flagSearch),
				"stage_uuid":          fmt.Sprintf("%v", flagStageUuid),
				"subject":             fmt.Sprintf("%v", flagSubject),
				"template":            fmt.Sprintf("%v", flagTemplate),
				"timeout":             fmt.Sprintf("%v", flagTimeout),
				"token_expiry":        fmt.Sprintf("%v", flagTokenExpiry),
				"use_global_settings": fmt.Sprintf("%v", flagUseGlobalSettings),
				"use_ssl":             fmt.Sprintf("%v", flagUseSsl),
				"use_tls":             fmt.Sprintf("%v", flagUseTls),
				"username":            fmt.Sprintf("%v", flagUsername),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagConfigureFlow, "configure-flow", "", "Configure flow")
	cmd.Flags().StringVar(&flagFriendlyName, "friendly-name", "", "Friendly name")
	cmd.Flags().StringVar(&flagFromAddress, "from-address", "", "From address")
	cmd.Flags().StringVar(&flagHost, "host", "", "Host")
	cmd.Flags().StringVar(&flagName, "name", "", "Name")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().StringVar(&flagPassword, "password", "", "Password")
	cmd.Flags().IntVar(&flagPort, "port", 0, "Port")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagStageUuid, "stage-uuid", "", "Stage uuid")
	cmd.Flags().StringVar(&flagSubject, "subject", "", "Subject")
	cmd.Flags().StringVar(&flagTemplate, "template", "", "Template")
	cmd.Flags().IntVar(&flagTimeout, "timeout", 0, "Timeout")
	cmd.Flags().StringVar(&flagTokenExpiry, "token-expiry", "", "Token expiry")
	cmd.Flags().BoolVar(&flagUseGlobalSettings, "use-global-settings", false, "Use global settings")
	cmd.Flags().BoolVar(&flagUseSsl, "use-ssl", false, "Use ssl")
	cmd.Flags().BoolVar(&flagUseTls, "use-tls", false, "Use tls")
	cmd.Flags().StringVar(&flagUsername, "username", "", "Username")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
