// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCoreUsersListCmd(flags *rootFlags) *cobra.Command {
	var flagAttributes string
	var flagDateJoined string
	var flagDateJoinedGt string
	var flagDateJoinedLt string
	var flagEmail string
	var flagGroupsByName string
	var flagGroupsByPk string
	var flagIncludeGroups bool
	var flagIsActive bool
	var flagIsSuperuser bool
	var flagLastUpdated string
	var flagLastUpdatedGt string
	var flagLastUpdatedLt string
	var flagName string
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagPath string
	var flagPathStartswith string
	var flagSearch string
	var flagType string
	var flagUsername string
	var flagUuid string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "users-list",
		Short:       "User Viewset",
		Example:     "  authentik-pp-cli core users-list",
		Annotations: map[string]string{"pp:endpoint": "core.users-list", "pp:method": "GET", "pp:path": "/core/users/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/core/users/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "core", path, map[string]string{
				"attributes":       fmt.Sprintf("%v", flagAttributes),
				"date_joined":      fmt.Sprintf("%v", flagDateJoined),
				"date_joined__gt":  fmt.Sprintf("%v", flagDateJoinedGt),
				"date_joined__lt":  fmt.Sprintf("%v", flagDateJoinedLt),
				"email":            fmt.Sprintf("%v", flagEmail),
				"groups_by_name":   fmt.Sprintf("%v", flagGroupsByName),
				"groups_by_pk":     fmt.Sprintf("%v", flagGroupsByPk),
				"include_groups":   fmt.Sprintf("%v", flagIncludeGroups),
				"is_active":        fmt.Sprintf("%v", flagIsActive),
				"is_superuser":     fmt.Sprintf("%v", flagIsSuperuser),
				"last_updated":     fmt.Sprintf("%v", flagLastUpdated),
				"last_updated__gt": fmt.Sprintf("%v", flagLastUpdatedGt),
				"last_updated__lt": fmt.Sprintf("%v", flagLastUpdatedLt),
				"name":             fmt.Sprintf("%v", flagName),
				"ordering":         fmt.Sprintf("%v", flagOrdering),
				"page":             fmt.Sprintf("%v", flagPage),
				"page_size":        fmt.Sprintf("%v", flagPageSize),
				"path":             fmt.Sprintf("%v", flagPath),
				"path_startswith":  fmt.Sprintf("%v", flagPathStartswith),
				"search":           fmt.Sprintf("%v", flagSearch),
				"type":             fmt.Sprintf("%v", flagType),
				"username":         fmt.Sprintf("%v", flagUsername),
				"uuid":             fmt.Sprintf("%v", flagUuid),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAttributes, "attributes", "", "Attributes")
	cmd.Flags().StringVar(&flagDateJoined, "date-joined", "", "Date joined")
	cmd.Flags().StringVar(&flagDateJoinedGt, "date-joined-gt", "", "Date joined gt")
	cmd.Flags().StringVar(&flagDateJoinedLt, "date-joined-lt", "", "Date joined lt")
	cmd.Flags().StringVar(&flagEmail, "email", "", "Email")
	cmd.Flags().StringVar(&flagGroupsByName, "groups-by-name", "", "Groups by name")
	cmd.Flags().StringVar(&flagGroupsByPk, "groups-by-pk", "", "Groups by pk")
	cmd.Flags().BoolVar(&flagIncludeGroups, "include-groups", true, "Include groups")
	cmd.Flags().BoolVar(&flagIsActive, "is-active", false, "Is active")
	cmd.Flags().BoolVar(&flagIsSuperuser, "is-superuser", false, "Is superuser")
	cmd.Flags().StringVar(&flagLastUpdated, "last-updated", "", "Last updated")
	cmd.Flags().StringVar(&flagLastUpdatedGt, "last-updated-gt", "", "Last updated gt")
	cmd.Flags().StringVar(&flagLastUpdatedLt, "last-updated-lt", "", "Last updated lt")
	cmd.Flags().StringVar(&flagName, "name", "", "Name")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().StringVar(&flagPath, "path", "", "Path")
	cmd.Flags().StringVar(&flagPathStartswith, "path-startswith", "", "Path startswith")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagType, "type", "", "Type")
	cmd.Flags().StringVar(&flagUsername, "username", "", "Username")
	cmd.Flags().StringVar(&flagUuid, "uuid", "", "Uuid")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
