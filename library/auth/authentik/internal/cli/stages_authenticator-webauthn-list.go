// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newStagesAuthenticatorWebauthnListCmd(flags *rootFlags) *cobra.Command {
	var flagAuthenticatorAttachment string
	var flagConfigureFlow string
	var flagDeviceTypeRestrictions string
	var flagFriendlyName string
	var flagMaxAttempts int
	var flagName string
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagResidentKeyRequirement string
	var flagSearch string
	var flagStageUuid string
	var flagUserVerification string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "authenticator-webauthn-list",
		Short:       "AuthenticatorWebAuthnStage Viewset",
		Example:     "  authentik-pp-cli stages authenticator-webauthn-list",
		Annotations: map[string]string{"pp:endpoint": "stages.authenticator-webauthn-list", "pp:method": "GET", "pp:path": "/stages/authenticator/webauthn/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("authenticator-attachment") {
				allowedAuthenticatorAttachment := []string{"cross-platform", "platform"}
				validAuthenticatorAttachment := false
				for _, v := range allowedAuthenticatorAttachment {
					if flagAuthenticatorAttachment == v {
						validAuthenticatorAttachment = true
						break
					}
				}
				if !validAuthenticatorAttachment {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "authenticator-attachment", flagAuthenticatorAttachment, allowedAuthenticatorAttachment)
				}
			}
			if cmd.Flags().Changed("resident-key-requirement") {
				allowedResidentKeyRequirement := []string{"discouraged", "preferred", "required"}
				validResidentKeyRequirement := false
				for _, v := range allowedResidentKeyRequirement {
					if flagResidentKeyRequirement == v {
						validResidentKeyRequirement = true
						break
					}
				}
				if !validResidentKeyRequirement {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "resident-key-requirement", flagResidentKeyRequirement, allowedResidentKeyRequirement)
				}
			}
			if cmd.Flags().Changed("user-verification") {
				allowedUserVerification := []string{"discouraged", "preferred", "required"}
				validUserVerification := false
				for _, v := range allowedUserVerification {
					if flagUserVerification == v {
						validUserVerification = true
						break
					}
				}
				if !validUserVerification {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "user-verification", flagUserVerification, allowedUserVerification)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/stages/authenticator/webauthn/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "stages", path, map[string]string{
				"authenticator_attachment": fmt.Sprintf("%v", flagAuthenticatorAttachment),
				"configure_flow":           fmt.Sprintf("%v", flagConfigureFlow),
				"device_type_restrictions": fmt.Sprintf("%v", flagDeviceTypeRestrictions),
				"friendly_name":            fmt.Sprintf("%v", flagFriendlyName),
				"max_attempts":             fmt.Sprintf("%v", flagMaxAttempts),
				"name":                     fmt.Sprintf("%v", flagName),
				"ordering":                 fmt.Sprintf("%v", flagOrdering),
				"page":                     fmt.Sprintf("%v", flagPage),
				"page_size":                fmt.Sprintf("%v", flagPageSize),
				"resident_key_requirement": fmt.Sprintf("%v", flagResidentKeyRequirement),
				"search":                   fmt.Sprintf("%v", flagSearch),
				"stage_uuid":               fmt.Sprintf("%v", flagStageUuid),
				"user_verification":        fmt.Sprintf("%v", flagUserVerification),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAuthenticatorAttachment, "authenticator-attachment", "", "Authenticator attachment (one of: cross-platform, platform)")
	cmd.Flags().StringVar(&flagConfigureFlow, "configure-flow", "", "Configure flow")
	cmd.Flags().StringVar(&flagDeviceTypeRestrictions, "device-type-restrictions", "", "Device type restrictions")
	cmd.Flags().StringVar(&flagFriendlyName, "friendly-name", "", "Friendly name")
	cmd.Flags().IntVar(&flagMaxAttempts, "max-attempts", 0, "Max attempts")
	cmd.Flags().StringVar(&flagName, "name", "", "Name")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().StringVar(&flagResidentKeyRequirement, "resident-key-requirement", "", "Resident key requirement (one of: discouraged, preferred, required)")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagStageUuid, "stage-uuid", "", "Stage uuid")
	cmd.Flags().StringVar(&flagUserVerification, "user-verification", "", "User verification (one of: discouraged, preferred, required)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
