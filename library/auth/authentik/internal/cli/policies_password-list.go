// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newPoliciesPasswordListCmd(flags *rootFlags) *cobra.Command {
	var flagAmountDigits int
	var flagAmountLowercase int
	var flagAmountSymbols int
	var flagAmountUppercase int
	var flagCheckHaveIBeenPwned bool
	var flagCheckStaticRules bool
	var flagCheckZxcvbn bool
	var flagCreated string
	var flagErrorMessage string
	var flagExecutionLogging bool
	var flagHibpAllowedCount int
	var flagLastUpdated string
	var flagLengthMin int
	var flagName string
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagPasswordField string
	var flagPolicyUuid string
	var flagSearch string
	var flagSymbolCharset string
	var flagZxcvbnScoreThreshold int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "password-list",
		Short:       "Password Policy Viewset",
		Example:     "  authentik-pp-cli policies password-list",
		Annotations: map[string]string{"pp:endpoint": "policies.password-list", "pp:method": "GET", "pp:path": "/policies/password/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/policies/password/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "policies", path, map[string]string{
				"amount_digits":           fmt.Sprintf("%v", flagAmountDigits),
				"amount_lowercase":        fmt.Sprintf("%v", flagAmountLowercase),
				"amount_symbols":          fmt.Sprintf("%v", flagAmountSymbols),
				"amount_uppercase":        fmt.Sprintf("%v", flagAmountUppercase),
				"check_have_i_been_pwned": fmt.Sprintf("%v", flagCheckHaveIBeenPwned),
				"check_static_rules":      fmt.Sprintf("%v", flagCheckStaticRules),
				"check_zxcvbn":            fmt.Sprintf("%v", flagCheckZxcvbn),
				"created":                 fmt.Sprintf("%v", flagCreated),
				"error_message":           fmt.Sprintf("%v", flagErrorMessage),
				"execution_logging":       fmt.Sprintf("%v", flagExecutionLogging),
				"hibp_allowed_count":      fmt.Sprintf("%v", flagHibpAllowedCount),
				"last_updated":            fmt.Sprintf("%v", flagLastUpdated),
				"length_min":              fmt.Sprintf("%v", flagLengthMin),
				"name":                    fmt.Sprintf("%v", flagName),
				"ordering":                fmt.Sprintf("%v", flagOrdering),
				"page":                    fmt.Sprintf("%v", flagPage),
				"page_size":               fmt.Sprintf("%v", flagPageSize),
				"password_field":          fmt.Sprintf("%v", flagPasswordField),
				"policy_uuid":             fmt.Sprintf("%v", flagPolicyUuid),
				"search":                  fmt.Sprintf("%v", flagSearch),
				"symbol_charset":          fmt.Sprintf("%v", flagSymbolCharset),
				"zxcvbn_score_threshold":  fmt.Sprintf("%v", flagZxcvbnScoreThreshold),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagAmountDigits, "amount-digits", 0, "Amount digits")
	cmd.Flags().IntVar(&flagAmountLowercase, "amount-lowercase", 0, "Amount lowercase")
	cmd.Flags().IntVar(&flagAmountSymbols, "amount-symbols", 0, "Amount symbols")
	cmd.Flags().IntVar(&flagAmountUppercase, "amount-uppercase", 0, "Amount uppercase")
	cmd.Flags().BoolVar(&flagCheckHaveIBeenPwned, "check-have-i-been-pwned", false, "Check have i been pwned")
	cmd.Flags().BoolVar(&flagCheckStaticRules, "check-static-rules", false, "Check static rules")
	cmd.Flags().BoolVar(&flagCheckZxcvbn, "check-zxcvbn", false, "Check zxcvbn")
	cmd.Flags().StringVar(&flagCreated, "created", "", "Created")
	cmd.Flags().StringVar(&flagErrorMessage, "error-message", "", "Error message")
	cmd.Flags().BoolVar(&flagExecutionLogging, "execution-logging", false, "Execution logging")
	cmd.Flags().IntVar(&flagHibpAllowedCount, "hibp-allowed-count", 0, "Hibp allowed count")
	cmd.Flags().StringVar(&flagLastUpdated, "last-updated", "", "Last updated")
	cmd.Flags().IntVar(&flagLengthMin, "length-min", 0, "Length min")
	cmd.Flags().StringVar(&flagName, "name", "", "Name")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().StringVar(&flagPasswordField, "password-field", "", "Password field")
	cmd.Flags().StringVar(&flagPolicyUuid, "policy-uuid", "", "Policy uuid")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagSymbolCharset, "symbol-charset", "", "Symbol charset")
	cmd.Flags().IntVar(&flagZxcvbnScoreThreshold, "zxcvbn-score-threshold", 0, "Zxcvbn score threshold")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
