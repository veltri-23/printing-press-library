// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newProvidersLdapListCmd(flags *rootFlags) *cobra.Command {
	var flagApplicationIsnull bool
	var flagAuthorizationFlowSlugIexact string
	var flagBaseDnIexact string
	var flagCertificateKpUuidIexact string
	var flagCertificateNameIexact string
	var flagGidStartNumberIexact int
	var flagNameIexact string
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagSearch string
	var flagTlsServerNameIexact string
	var flagUidStartNumberIexact int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "ldap-list",
		Short:       "LDAPProvider Viewset",
		Example:     "  authentik-pp-cli providers ldap-list",
		Annotations: map[string]string{"pp:endpoint": "providers.ldap-list", "pp:method": "GET", "pp:path": "/providers/ldap/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/providers/ldap/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "providers", path, map[string]string{
				"application__isnull":              fmt.Sprintf("%v", flagApplicationIsnull),
				"authorization_flow__slug__iexact": fmt.Sprintf("%v", flagAuthorizationFlowSlugIexact),
				"base_dn__iexact":                  fmt.Sprintf("%v", flagBaseDnIexact),
				"certificate__kp_uuid__iexact":     fmt.Sprintf("%v", flagCertificateKpUuidIexact),
				"certificate__name__iexact":        fmt.Sprintf("%v", flagCertificateNameIexact),
				"gid_start_number__iexact":         fmt.Sprintf("%v", flagGidStartNumberIexact),
				"name__iexact":                     fmt.Sprintf("%v", flagNameIexact),
				"ordering":                         fmt.Sprintf("%v", flagOrdering),
				"page":                             fmt.Sprintf("%v", flagPage),
				"page_size":                        fmt.Sprintf("%v", flagPageSize),
				"search":                           fmt.Sprintf("%v", flagSearch),
				"tls_server_name__iexact":          fmt.Sprintf("%v", flagTlsServerNameIexact),
				"uid_start_number__iexact":         fmt.Sprintf("%v", flagUidStartNumberIexact),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagApplicationIsnull, "application-isnull", false, "Application isnull")
	cmd.Flags().StringVar(&flagAuthorizationFlowSlugIexact, "authorization-flow-slug-iexact", "", "Authorization flow slug iexact")
	cmd.Flags().StringVar(&flagBaseDnIexact, "base-dn-iexact", "", "Base dn iexact")
	cmd.Flags().StringVar(&flagCertificateKpUuidIexact, "certificate-kp-uuid-iexact", "", "Certificate kp uuid iexact")
	cmd.Flags().StringVar(&flagCertificateNameIexact, "certificate-name-iexact", "", "Certificate name iexact")
	cmd.Flags().IntVar(&flagGidStartNumberIexact, "gid-start-number-iexact", 0, "Gid start number iexact")
	cmd.Flags().StringVar(&flagNameIexact, "name-iexact", "", "Name iexact")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagTlsServerNameIexact, "tls-server-name-iexact", "", "Tls server name iexact")
	cmd.Flags().IntVar(&flagUidStartNumberIexact, "uid-start-number-iexact", 0, "Uid start number iexact")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
