// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newFlowsBindingsListCmd(flags *rootFlags) *cobra.Command {
	var flagEvaluateOnPlan bool
	var flagFsbUuid string
	var flagInvalidResponseAction string
	var flagOrder int
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagPbmUuid string
	var flagPolicies string
	var flagPolicyEngineMode string
	var flagReEvaluatePolicies bool
	var flagSearch string
	var flagStage string
	var flagTarget string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "bindings-list",
		Aliases:     []string{"list"},
		Short:       "FlowStageBinding Viewset",
		Example:     "  authentik-pp-cli flows bindings-list",
		Annotations: map[string]string{"pp:endpoint": "flows.bindings-list", "pp:method": "GET", "pp:path": "/flows/bindings/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("invalid-response-action") {
				allowedInvalidResponseAction := []string{"restart", "restart_with_context", "retry"}
				validInvalidResponseAction := false
				for _, v := range allowedInvalidResponseAction {
					if flagInvalidResponseAction == v {
						validInvalidResponseAction = true
						break
					}
				}
				if !validInvalidResponseAction {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "invalid-response-action", flagInvalidResponseAction, allowedInvalidResponseAction)
				}
			}
			if cmd.Flags().Changed("policy-engine-mode") {
				allowedPolicyEngineMode := []string{"all", "any"}
				validPolicyEngineMode := false
				for _, v := range allowedPolicyEngineMode {
					if flagPolicyEngineMode == v {
						validPolicyEngineMode = true
						break
					}
				}
				if !validPolicyEngineMode {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "policy-engine-mode", flagPolicyEngineMode, allowedPolicyEngineMode)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/flows/bindings/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "flows", path, map[string]string{
				"evaluate_on_plan":        fmt.Sprintf("%v", flagEvaluateOnPlan),
				"fsb_uuid":                fmt.Sprintf("%v", flagFsbUuid),
				"invalid_response_action": fmt.Sprintf("%v", flagInvalidResponseAction),
				"order":                   fmt.Sprintf("%v", flagOrder),
				"ordering":                fmt.Sprintf("%v", flagOrdering),
				"page":                    fmt.Sprintf("%v", flagPage),
				"page_size":               fmt.Sprintf("%v", flagPageSize),
				"pbm_uuid":                fmt.Sprintf("%v", flagPbmUuid),
				"policies":                fmt.Sprintf("%v", flagPolicies),
				"policy_engine_mode":      fmt.Sprintf("%v", flagPolicyEngineMode),
				"re_evaluate_policies":    fmt.Sprintf("%v", flagReEvaluatePolicies),
				"search":                  fmt.Sprintf("%v", flagSearch),
				"stage":                   fmt.Sprintf("%v", flagStage),
				"target":                  fmt.Sprintf("%v", flagTarget),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagEvaluateOnPlan, "evaluate-on-plan", false, "Evaluate on plan")
	cmd.Flags().StringVar(&flagFsbUuid, "fsb-uuid", "", "Fsb uuid")
	cmd.Flags().StringVar(&flagInvalidResponseAction, "invalid-response-action", "", "Configure how the flow executor should handle an invalid response to a challenge. (one of: restart, restart_with_context, retry)")
	cmd.Flags().IntVar(&flagOrder, "order", 0, "Order")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().StringVar(&flagPbmUuid, "pbm-uuid", "", "Pbm uuid")
	cmd.Flags().StringVar(&flagPolicies, "policies", "", "Policies")
	cmd.Flags().StringVar(&flagPolicyEngineMode, "policy-engine-mode", "", "Policy engine mode (one of: all, any)")
	cmd.Flags().BoolVar(&flagReEvaluatePolicies, "re-evaluate-policies", false, "Re evaluate policies")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagStage, "stage", "", "Stage")
	cmd.Flags().StringVar(&flagTarget, "target", "", "Target")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
