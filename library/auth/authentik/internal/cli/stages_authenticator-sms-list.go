// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newStagesAuthenticatorSmsListCmd(flags *rootFlags) *cobra.Command {
	var flagAccountSid string
	var flagAuth string
	var flagAuthPassword string
	var flagAuthType string
	var flagConfigureFlow string
	var flagFriendlyName string
	var flagFromNumber string
	var flagMapping string
	var flagName string
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagProvider string
	var flagSearch string
	var flagStageUuid string
	var flagVerifyOnly bool
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "authenticator-sms-list",
		Short:       "AuthenticatorSMSStage Viewset",
		Example:     "  authentik-pp-cli stages authenticator-sms-list",
		Annotations: map[string]string{"pp:endpoint": "stages.authenticator-sms-list", "pp:method": "GET", "pp:path": "/stages/authenticator/sms/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("auth-type") {
				allowedAuthType := []string{"basic", "bearer"}
				validAuthType := false
				for _, v := range allowedAuthType {
					if flagAuthType == v {
						validAuthType = true
						break
					}
				}
				if !validAuthType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "auth-type", flagAuthType, allowedAuthType)
				}
			}
			if cmd.Flags().Changed("provider") {
				allowedProvider := []string{"generic", "twilio"}
				validProvider := false
				for _, v := range allowedProvider {
					if flagProvider == v {
						validProvider = true
						break
					}
				}
				if !validProvider {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "provider", flagProvider, allowedProvider)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/stages/authenticator/sms/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "stages", path, map[string]string{
				"account_sid":    fmt.Sprintf("%v", flagAccountSid),
				"auth":           fmt.Sprintf("%v", flagAuth),
				"auth_password":  fmt.Sprintf("%v", flagAuthPassword),
				"auth_type":      fmt.Sprintf("%v", flagAuthType),
				"configure_flow": fmt.Sprintf("%v", flagConfigureFlow),
				"friendly_name":  fmt.Sprintf("%v", flagFriendlyName),
				"from_number":    fmt.Sprintf("%v", flagFromNumber),
				"mapping":        fmt.Sprintf("%v", flagMapping),
				"name":           fmt.Sprintf("%v", flagName),
				"ordering":       fmt.Sprintf("%v", flagOrdering),
				"page":           fmt.Sprintf("%v", flagPage),
				"page_size":      fmt.Sprintf("%v", flagPageSize),
				"provider":       fmt.Sprintf("%v", flagProvider),
				"search":         fmt.Sprintf("%v", flagSearch),
				"stage_uuid":     fmt.Sprintf("%v", flagStageUuid),
				"verify_only":    fmt.Sprintf("%v", flagVerifyOnly),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAccountSid, "account-sid", "", "Account sid")
	cmd.Flags().StringVar(&flagAuth, "auth", "", "Auth")
	cmd.Flags().StringVar(&flagAuthPassword, "auth-password", "", "Auth password")
	cmd.Flags().StringVar(&flagAuthType, "auth-type", "", "Auth type (one of: basic, bearer)")
	cmd.Flags().StringVar(&flagConfigureFlow, "configure-flow", "", "Configure flow")
	cmd.Flags().StringVar(&flagFriendlyName, "friendly-name", "", "Friendly name")
	cmd.Flags().StringVar(&flagFromNumber, "from-number", "", "From number")
	cmd.Flags().StringVar(&flagMapping, "mapping", "", "Mapping")
	cmd.Flags().StringVar(&flagName, "name", "", "Name")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().StringVar(&flagProvider, "provider", "", "Provider (one of: generic, twilio)")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagStageUuid, "stage-uuid", "", "Stage uuid")
	cmd.Flags().BoolVar(&flagVerifyOnly, "verify-only", false, "Verify only")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
