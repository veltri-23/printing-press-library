// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newProvidersOauth2ListCmd(flags *rootFlags) *cobra.Command {
	var flagAccessCodeValidity string
	var flagAccessTokenValidity string
	var flagApplication string
	var flagAuthorizationFlow string
	var flagClientId string
	var flagClientType string
	var flagIncludeClaimsInIdToken bool
	var flagIssuerMode string
	var flagName string
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagPropertyMappings string
	var flagRefreshTokenValidity string
	var flagSearch string
	var flagSigningKey string
	var flagSubMode string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "oauth2-list",
		Short:       "OAuth2Provider Viewset",
		Example:     "  authentik-pp-cli providers oauth2-list",
		Annotations: map[string]string{"pp:endpoint": "providers.oauth2-list", "pp:method": "GET", "pp:path": "/providers/oauth2/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("client-type") {
				allowedClientType := []string{"confidential", "public"}
				validClientType := false
				for _, v := range allowedClientType {
					if flagClientType == v {
						validClientType = true
						break
					}
				}
				if !validClientType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "client-type", flagClientType, allowedClientType)
				}
			}
			if cmd.Flags().Changed("issuer-mode") {
				allowedIssuerMode := []string{"global", "per_provider"}
				validIssuerMode := false
				for _, v := range allowedIssuerMode {
					if flagIssuerMode == v {
						validIssuerMode = true
						break
					}
				}
				if !validIssuerMode {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "issuer-mode", flagIssuerMode, allowedIssuerMode)
				}
			}
			if cmd.Flags().Changed("sub-mode") {
				allowedSubMode := []string{"hashed_user_id", "user_email", "user_id", "user_upn", "user_username", "user_uuid"}
				validSubMode := false
				for _, v := range allowedSubMode {
					if flagSubMode == v {
						validSubMode = true
						break
					}
				}
				if !validSubMode {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sub-mode", flagSubMode, allowedSubMode)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/providers/oauth2/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "providers", path, map[string]string{
				"access_code_validity":       fmt.Sprintf("%v", flagAccessCodeValidity),
				"access_token_validity":      fmt.Sprintf("%v", flagAccessTokenValidity),
				"application":                fmt.Sprintf("%v", flagApplication),
				"authorization_flow":         fmt.Sprintf("%v", flagAuthorizationFlow),
				"client_id":                  fmt.Sprintf("%v", flagClientId),
				"client_type":                fmt.Sprintf("%v", flagClientType),
				"include_claims_in_id_token": fmt.Sprintf("%v", flagIncludeClaimsInIdToken),
				"issuer_mode":                fmt.Sprintf("%v", flagIssuerMode),
				"name":                       fmt.Sprintf("%v", flagName),
				"ordering":                   fmt.Sprintf("%v", flagOrdering),
				"page":                       fmt.Sprintf("%v", flagPage),
				"page_size":                  fmt.Sprintf("%v", flagPageSize),
				"property_mappings":          fmt.Sprintf("%v", flagPropertyMappings),
				"refresh_token_validity":     fmt.Sprintf("%v", flagRefreshTokenValidity),
				"search":                     fmt.Sprintf("%v", flagSearch),
				"signing_key":                fmt.Sprintf("%v", flagSigningKey),
				"sub_mode":                   fmt.Sprintf("%v", flagSubMode),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAccessCodeValidity, "access-code-validity", "", "Access code validity")
	cmd.Flags().StringVar(&flagAccessTokenValidity, "access-token-validity", "", "Access token validity")
	cmd.Flags().StringVar(&flagApplication, "application", "", "Application")
	cmd.Flags().StringVar(&flagAuthorizationFlow, "authorization-flow", "", "Authorization flow")
	cmd.Flags().StringVar(&flagClientId, "client-id", "", "Client id")
	cmd.Flags().StringVar(&flagClientType, "client-type", "", "Confidential clients are capable of maintaining the confidentiality of their credentials. Public clients are incapable (one of: confidential, public)")
	cmd.Flags().BoolVar(&flagIncludeClaimsInIdToken, "include-claims-in-id-token", false, "Include claims in id token")
	cmd.Flags().StringVar(&flagIssuerMode, "issuer-mode", "", "Configure how the issuer field of the ID Token should be filled. (one of: global, per_provider)")
	cmd.Flags().StringVar(&flagName, "name", "", "Name")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().StringVar(&flagPropertyMappings, "property-mappings", "", "Property mappings")
	cmd.Flags().StringVar(&flagRefreshTokenValidity, "refresh-token-validity", "", "Refresh token validity")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagSigningKey, "signing-key", "", "Signing key")
	cmd.Flags().StringVar(&flagSubMode, "sub-mode", "", "Configure what data should be used as unique User Identifier. For most cases, the default should be fine. (one of: hashed_user_id, user_email, user_id, user_upn, user_username, user_uuid)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
