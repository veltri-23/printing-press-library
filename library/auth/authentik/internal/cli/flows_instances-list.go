// Copyright 2026 carlf01. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newFlowsInstancesListCmd(flags *rootFlags) *cobra.Command {
	var flagDeniedAction string
	var flagDesignation string
	var flagFlowUuid string
	var flagName string
	var flagOrdering string
	var flagPage string
	var flagPageSize int
	var flagSearch string
	var flagSlug string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "instances-list",
		Short:       "Flow Viewset",
		Example:     "  authentik-pp-cli flows instances-list",
		Annotations: map[string]string{"pp:endpoint": "flows.instances-list", "pp:method": "GET", "pp:path": "/flows/instances/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("denied-action") {
				allowedDeniedAction := []string{"continue", "message", "message_continue"}
				validDeniedAction := false
				for _, v := range allowedDeniedAction {
					if flagDeniedAction == v {
						validDeniedAction = true
						break
					}
				}
				if !validDeniedAction {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "denied-action", flagDeniedAction, allowedDeniedAction)
				}
			}
			if cmd.Flags().Changed("designation") {
				allowedDesignation := []string{"authentication", "authorization", "enrollment", "invalidation", "recovery", "stage_configuration", "unenrollment"}
				validDesignation := false
				for _, v := range allowedDesignation {
					if flagDesignation == v {
						validDesignation = true
						break
					}
				}
				if !validDesignation {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "designation", flagDesignation, allowedDesignation)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/flows/instances/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "flows", path, map[string]string{
				"denied_action": fmt.Sprintf("%v", flagDeniedAction),
				"designation":   fmt.Sprintf("%v", flagDesignation),
				"flow_uuid":     fmt.Sprintf("%v", flagFlowUuid),
				"name":          fmt.Sprintf("%v", flagName),
				"ordering":      fmt.Sprintf("%v", flagOrdering),
				"page":          fmt.Sprintf("%v", flagPage),
				"page_size":     fmt.Sprintf("%v", flagPageSize),
				"search":        fmt.Sprintf("%v", flagSearch),
				"slug":          fmt.Sprintf("%v", flagSlug),
			}, nil, flagAll, "page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDeniedAction, "denied-action", "", "Configure what should happen when a flow denies access to a user. (one of: continue, message, message_continue)")
	cmd.Flags().StringVar(&flagDesignation, "designation", "", "Decides what this Flow is used for. (one of: authentication, authorization, enrollment, invalidation, recovery, stage_configuration, unenrollment)")
	cmd.Flags().StringVar(&flagFlowUuid, "flow-uuid", "", "Flow uuid")
	cmd.Flags().StringVar(&flagName, "name", "", "Name")
	cmd.Flags().StringVar(&flagOrdering, "ordering", "", "Which field to use when ordering the results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "A page number within the paginated result set.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results to return per page.")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search term.")
	cmd.Flags().StringVar(&flagSlug, "slug", "", "Slug")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
