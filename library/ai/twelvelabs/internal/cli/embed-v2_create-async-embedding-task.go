// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/ai/twelvelabs/internal/cliutil"
	"github.com/spf13/cobra"
)

func newEmbedV2CreateAsyncEmbeddingTaskCmd(flags *rootFlags) *cobra.Command {
	var bodyAudioEmbeddingOption string
	var bodyAudioEmbeddingScope string
	var bodyAudioEndSec float64
	var bodyAudioMediaSourceAssetId string
	var bodyAudioMediaSourceBase64String string
	var bodyAudioMediaSourceUrl string
	var bodyAudioSegmentationStrategy string
	var bodyAudioStartSec float64
	var bodyInputType string
	var bodyModelName string
	var bodyVideoEmbeddingOption string
	var bodyVideoEmbeddingScope string
	var bodyVideoEndSec float64
	var bodyVideoMediaSourceAssetId string
	var bodyVideoMediaSourceBase64String string
	var bodyVideoMediaSourceUrl string
	var bodyVideoSegmentation string
	var bodyVideoStartSec float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-async-embedding-task",
		Aliases:     []string{"create"},
		Short:       "This endpoint creates embeddings for audio and video content asynchronously.",
		Example:     "  twelvelabs-pp-cli embed-v2 create-async-embedding-task --input-type audio",
		Annotations: map[string]string{"pp:endpoint": "embed-v2.create-async-embedding-task", "pp:method": "POST", "pp:path": "/embed-v2/tasks"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("audio-segmentation-strategy") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "audio-segmentation-strategy")
				}
				if !cmd.Flags().Changed("input-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "input-type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/embed-v2/tasks"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAudio := map[string]any{}
					if bodyAudioEmbeddingOption != "" {
						nestedAudio["embedding_option"] = cliutil.SplitCSV(bodyAudioEmbeddingOption)
					}
					if bodyAudioEmbeddingScope != "" {
						nestedAudio["embedding_scope"] = cliutil.SplitCSV(bodyAudioEmbeddingScope)
					}
					if bodyAudioEndSec != 0.0 {
						nestedAudio["end_sec"] = bodyAudioEndSec
					}
					{
						nestedAudioMediaSource := map[string]any{}
						if bodyAudioMediaSourceAssetId != "" {
							nestedAudioMediaSource["asset_id"] = bodyAudioMediaSourceAssetId
						}
						if bodyAudioMediaSourceBase64String != "" {
							nestedAudioMediaSource["base64_string"] = bodyAudioMediaSourceBase64String
						}
						if bodyAudioMediaSourceUrl != "" {
							nestedAudioMediaSource["url"] = bodyAudioMediaSourceUrl
						}
						if len(nestedAudioMediaSource) > 0 {
							nestedAudio["media_source"] = nestedAudioMediaSource
						}
					}
					{
						nestedAudioSegmentation := map[string]any{}
						if bodyAudioSegmentationStrategy != "" {
							nestedAudioSegmentation["strategy"] = bodyAudioSegmentationStrategy
						}
						if len(nestedAudioSegmentation) > 0 {
							nestedAudio["segmentation"] = nestedAudioSegmentation
						}
					}
					if bodyAudioStartSec != 0.0 {
						nestedAudio["start_sec"] = bodyAudioStartSec
					}
					if len(nestedAudio) > 0 {
						body["audio"] = nestedAudio
					}
				}
				if bodyInputType != "" {
					body["input_type"] = bodyInputType
				}
				if bodyModelName != "" {
					body["model_name"] = bodyModelName
				}
				{
					nestedVideo := map[string]any{}
					if bodyVideoEmbeddingOption != "" {
						nestedVideo["embedding_option"] = cliutil.SplitCSV(bodyVideoEmbeddingOption)
					}
					if bodyVideoEmbeddingScope != "" {
						nestedVideo["embedding_scope"] = cliutil.SplitCSV(bodyVideoEmbeddingScope)
					}
					if bodyVideoEndSec != 0.0 {
						nestedVideo["end_sec"] = bodyVideoEndSec
					}
					{
						nestedVideoMediaSource := map[string]any{}
						if bodyVideoMediaSourceAssetId != "" {
							nestedVideoMediaSource["asset_id"] = bodyVideoMediaSourceAssetId
						}
						if bodyVideoMediaSourceBase64String != "" {
							nestedVideoMediaSource["base64_string"] = bodyVideoMediaSourceBase64String
						}
						if bodyVideoMediaSourceUrl != "" {
							nestedVideoMediaSource["url"] = bodyVideoMediaSourceUrl
						}
						if len(nestedVideoMediaSource) > 0 {
							nestedVideo["media_source"] = nestedVideoMediaSource
						}
					}
					if bodyVideoSegmentation != "" {
						var parsedVideoSegmentation any
						if err := json.Unmarshal([]byte(bodyVideoSegmentation), &parsedVideoSegmentation); err != nil {
							return fmt.Errorf("parsing --video-segmentation JSON: %w", err)
						}
						nestedVideo["segmentation"] = parsedVideoSegmentation
					}
					if bodyVideoStartSec != 0.0 {
						nestedVideo["start_sec"] = bodyVideoStartSec
					}
					if len(nestedVideo) > 0 {
						body["video"] = nestedVideo
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "embed-v2", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "embed-v2", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "embed-v2", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "embed-v2", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "embed-v2", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "embed-v2",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "embed-v2", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "embed-v2", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAudioEmbeddingOption, "audio-embedding-option", "", "The types of embeddings you wish to generate.")
	cmd.Flags().StringVar(&bodyAudioEmbeddingScope, "audio-embedding-scope", "", "The scope for which you wish to generate embeddings.")
	cmd.Flags().Float64Var(&bodyAudioEndSec, "audio-end-sec", 0.0, "The end time in seconds for processing the audio file.")
	cmd.Flags().StringVar(&bodyAudioMediaSourceAssetId, "audio-media-source-asset-id", "", "The unique identifier of an asset from a [direct](/v1.3/api-reference/upload-content) or [multipart](/v1.")
	cmd.Flags().StringVar(&bodyAudioMediaSourceBase64String, "audio-media-source-base64-string", "", "The base64-encoded media data.")
	cmd.Flags().StringVar(&bodyAudioMediaSourceUrl, "audio-media-source-url", "", "The publicly accessible URL of the media file. Use direct links to raw media files.")
	cmd.Flags().StringVar(&bodyAudioSegmentationStrategy, "audio-segmentation-strategy", "", "Strategy")
	cmd.Flags().Float64Var(&bodyAudioStartSec, "audio-start-sec", 0.0, "The start time in seconds for processing the audio file.")
	cmd.Flags().StringVar(&bodyInputType, "input-type", "", "The type of content for the embeddings. **Values**: - `audio`: Audio files - `video`: Video content")
	cmd.Flags().StringVar(&bodyModelName, "model-name", "marengo3.0", "The model you wish to use. Only `'marengo3.0'` is supported.")
	cmd.Flags().StringVar(&bodyVideoEmbeddingOption, "video-embedding-option", "", "The types of embeddings to generate for the video.")
	cmd.Flags().StringVar(&bodyVideoEmbeddingScope, "video-embedding-scope", "", "The scope for which you wish to generate embeddings.")
	cmd.Flags().Float64Var(&bodyVideoEndSec, "video-end-sec", 0.0, "The end time in seconds for processing the video file.")
	cmd.Flags().StringVar(&bodyVideoMediaSourceAssetId, "video-media-source-asset-id", "", "The unique identifier of an asset from a [direct](/v1.3/api-reference/upload-content) or [multipart](/v1.")
	cmd.Flags().StringVar(&bodyVideoMediaSourceBase64String, "video-media-source-base64-string", "", "The base64-encoded media data.")
	cmd.Flags().StringVar(&bodyVideoMediaSourceUrl, "video-media-source-url", "", "The publicly accessible URL of the media file. Use direct links to raw media files.")
	cmd.Flags().StringVar(&bodyVideoSegmentation, "video-segmentation", "", "Specifies how the platform divides the video into segments.")
	cmd.Flags().Float64Var(&bodyVideoStartSec, "video-start-sec", 0.0, "The start time in seconds for processing the video file.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
