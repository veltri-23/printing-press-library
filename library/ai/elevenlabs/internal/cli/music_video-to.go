// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMusicVideoToCmd(flags *rootFlags) *cobra.Command {
	var flagOutputFormat string
	var bodyDescription string
	var bodySignWithC2pa bool
	var bodyTags string
	var bodyVideos string

	cmd := &cobra.Command{
		Use:         "video-to",
		Short:       "Generate background music from one or more video files. Videos are combined in order. Optional description and style...",
		Example:     "  elevenlabs-pp-cli music video-to",
		Annotations: map[string]string{"pp:endpoint": "music.video-to", "pp:method": "POST", "pp:path": "/v1/music/video-to-music"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("output-format") {
				allowedOutputFormat := []string{"mp3_22050_32", "mp3_24000_48", "mp3_44100_32", "mp3_44100_64", "mp3_44100_96", "mp3_44100_128", "mp3_44100_192", "pcm_8000", "pcm_16000", "pcm_22050", "pcm_24000", "pcm_32000", "pcm_44100", "pcm_48000", "ulaw_8000", "alaw_8000", "opus_48000_32", "opus_48000_64", "opus_48000_96", "opus_48000_128", "opus_48000_192"}
				validOutputFormat := false
				for _, v := range allowedOutputFormat {
					if flagOutputFormat == v {
						validOutputFormat = true
						break
					}
				}
				if !validOutputFormat {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "output-format", flagOutputFormat, allowedOutputFormat)
				}
			}
			if !cmd.Flags().Changed("videos") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "videos")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/music/video-to-music"
			params := map[string]string{}
			if flagOutputFormat != "" {
				params["output_format"] = fmt.Sprintf("%v", flagOutputFormat)
			}
			fields := map[string]string{}
			fileFields := map[string]string{}
			if bodyDescription != "" {
				fields["description"] = bodyDescription
			}
			if bodySignWithC2pa != false {
				fields["sign_with_c2pa"] = fmt.Sprintf("%v", bodySignWithC2pa)
			}
			if bodyTags != "" {
				if !json.Valid([]byte(bodyTags)) {
					return fmt.Errorf("parsing --tags JSON: invalid JSON")
				}
				fields["tags"] = bodyTags
			}
			if bodyVideos != "" {
				if !json.Valid([]byte(bodyVideos)) {
					return fmt.Errorf("parsing --videos JSON: invalid JSON")
				}
				fields["videos"] = bodyVideos
			}

			data, statusCode, err := c.PostMultipartWithParams(path, params, fields, fileFields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = statusCode
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>")
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().StringVar(&flagOutputFormat, "output-format", "mp3_44100_128", "Output format of the generated audio. Formatted as codec_sample_rate_bitrate. So an mp3 with 22.05kHz sample rate at... (one of: mp3_22050_32, mp3_24000_48, mp3_44100_32, mp3_44100_64, mp3_44100_96, mp3_44100_128, mp3_44100_192, pcm_8000, pcm_16000, pcm_22050, pcm_24000, pcm_32000, pcm_44100, pcm_48000, ulaw_8000, alaw_8000, opus_48000_32, opus_48000_64, opus_48000_96, opus_48000_128, opus_48000_192)")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Optional text description of the music you want. A maximum of 1000 characters is allowed.")
	cmd.Flags().BoolVar(&bodySignWithC2pa, "sign-with-c2pa", false, "Whether to sign the generated song with C2PA. Applicable only for mp3 files.")
	cmd.Flags().StringVar(&bodyTags, "tags", "[]", "Optional list of style tags (e.g. ['upbeat', 'cinematic']). A maximum of 10 tags is allowed.")
	cmd.Flags().StringVar(&bodyVideos, "videos", "", "One or more video files sent via FormData array (multipart/form-data). They will be combined into one codec in...")

	return cmd
}
