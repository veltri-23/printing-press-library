// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newStudioAddProjectCmd(flags *rootFlags) *cobra.Command {
	var bodyAcxVolumeNormalization bool
	var bodyApplyTextNormalization string
	var bodyAuthor string
	var bodyAutoAssignVoices string
	var bodyAutoConvert bool
	var bodyCallbackUrl string
	var bodyContentType string
	var bodyCreatePublishingRead string
	var bodyDefaultModelId string
	var bodyDefaultParagraphVoiceId string
	var bodyDefaultTitleVoiceId string
	var bodyDescription string
	var bodyFiction string
	var bodyFromContentJson string
	var bodyFromDocument string
	var bodyFromUrl string
	var bodyGenres string
	var bodyIsbnNumber string
	var bodyLanguage string
	var bodyMatureContent string
	var bodyName string
	var bodyOriginalPublicationDate string
	var bodyPronunciationDictionaryLocators string
	var bodyQualityPreset string
	var bodySourceType string
	var bodyTargetAudience string
	var bodyTitle string
	var bodyVoiceSettings string
	var bodyVolumeNormalization bool

	cmd := &cobra.Command{
		Use:         "add-project",
		Short:       "Creates a new Studio project, it can be either initialized as blank, from a document or from a URL.",
		Example:     "  elevenlabs-pp-cli studio add-project --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "studio.add-project", "pp:method": "POST", "pp:path": "/v1/studio/projects"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "name")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/studio/projects"
			params := map[string]string{}
			fields := map[string]string{}
			fileFields := map[string]string{}
			if bodyAcxVolumeNormalization != false {
				fields["acx_volume_normalization"] = fmt.Sprintf("%v", bodyAcxVolumeNormalization)
			}
			if bodyApplyTextNormalization != "" {
				fields["apply_text_normalization"] = bodyApplyTextNormalization
			}
			if bodyAuthor != "" {
				fields["author"] = bodyAuthor
			}
			if bodyAutoAssignVoices != "" {
				if !json.Valid([]byte(bodyAutoAssignVoices)) {
					return fmt.Errorf("parsing --auto-assign-voices JSON: invalid JSON")
				}
				fields["auto_assign_voices"] = bodyAutoAssignVoices
			}
			if bodyAutoConvert != false {
				fields["auto_convert"] = fmt.Sprintf("%v", bodyAutoConvert)
			}
			if bodyCallbackUrl != "" {
				fields["callback_url"] = bodyCallbackUrl
			}
			if bodyContentType != "" {
				fields["content_type"] = bodyContentType
			}
			if bodyCreatePublishingRead != "" {
				fields["create_publishing_read"] = bodyCreatePublishingRead
			}
			if bodyDefaultModelId != "" {
				fields["default_model_id"] = bodyDefaultModelId
			}
			if bodyDefaultParagraphVoiceId != "" {
				fields["default_paragraph_voice_id"] = bodyDefaultParagraphVoiceId
			}
			if bodyDefaultTitleVoiceId != "" {
				fields["default_title_voice_id"] = bodyDefaultTitleVoiceId
			}
			if bodyDescription != "" {
				fields["description"] = bodyDescription
			}
			if bodyFiction != "" {
				fields["fiction"] = bodyFiction
			}
			if bodyFromContentJson != "" {
				fields["from_content_json"] = bodyFromContentJson
			}
			if bodyFromDocument != "" {
				// PATCH: from_document is a file upload (.epub/.pdf/.txt), not a text field.
				fileFields["from_document"] = bodyFromDocument
			}
			if bodyFromUrl != "" {
				fields["from_url"] = bodyFromUrl
			}
			if bodyGenres != "" {
				if !json.Valid([]byte(bodyGenres)) {
					return fmt.Errorf("parsing --genres JSON: invalid JSON")
				}
				fields["genres"] = bodyGenres
			}
			if bodyIsbnNumber != "" {
				fields["isbn_number"] = bodyIsbnNumber
			}
			if bodyLanguage != "" {
				fields["language"] = bodyLanguage
			}
			if bodyMatureContent != "" {
				fields["mature_content"] = bodyMatureContent
			}
			if bodyName != "" {
				fields["name"] = bodyName
			}
			if bodyOriginalPublicationDate != "" {
				fields["original_publication_date"] = bodyOriginalPublicationDate
			}
			if bodyPronunciationDictionaryLocators != "" {
				if !json.Valid([]byte(bodyPronunciationDictionaryLocators)) {
					return fmt.Errorf("parsing --pronunciation-dictionary-locators JSON: invalid JSON")
				}
				fields["pronunciation_dictionary_locators"] = bodyPronunciationDictionaryLocators
			}
			if bodyQualityPreset != "" {
				fields["quality_preset"] = bodyQualityPreset
			}
			if bodySourceType != "" {
				fields["source_type"] = bodySourceType
			}
			if bodyTargetAudience != "" {
				fields["target_audience"] = bodyTargetAudience
			}
			if bodyTitle != "" {
				fields["title"] = bodyTitle
			}
			if bodyVoiceSettings != "" {
				if !json.Valid([]byte(bodyVoiceSettings)) {
					return fmt.Errorf("parsing --voice-settings JSON: invalid JSON")
				}
				fields["voice_settings"] = bodyVoiceSettings
			}
			if bodyVolumeNormalization != false {
				fields["volume_normalization"] = fmt.Sprintf("%v", bodyVolumeNormalization)
			}

			data, statusCode, err := c.PostMultipartWithParams(path, params, fields, fileFields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "studio",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAcxVolumeNormalization, "acx-volume-normalization", false, "[Deprecated] When the Studio project is downloaded, should the returned audio have postprocessing in order to make...")
	cmd.Flags().StringVar(&bodyApplyTextNormalization, "apply-text-normalization", "", "This parameter controls text normalization with four modes: 'auto', 'on', 'apply_english' and 'off'. When set to...")
	cmd.Flags().StringVar(&bodyAuthor, "author", "", "An optional name of the author of the Studio project, this will be added as metadata to the mp3 file on Studio...")
	cmd.Flags().StringVar(&bodyAutoAssignVoices, "auto-assign-voices", "false", "[Alpha Feature] Whether automatically assign voices to phrases in the create Project.")
	cmd.Flags().BoolVar(&bodyAutoConvert, "auto-convert", false, "Whether to auto convert the Studio project to audio or not.")
	cmd.Flags().StringVar(&bodyCallbackUrl, "callback-url", "", "A url that will be called by our service when the Studio project is converted. Request will contain a json blob...")
	cmd.Flags().StringVar(&bodyContentType, "content-type", "", "An optional content type of the Studio project.")
	cmd.Flags().StringVar(&bodyCreatePublishingRead, "create-publishing-read", "false", "If true, creates a corresponding read for direct publishing in draft state")
	cmd.Flags().StringVar(&bodyDefaultModelId, "default-model-id", "", "The ID of the model to be used for this Studio project, you can query GET /v1/models to list all available models.")
	cmd.Flags().StringVar(&bodyDefaultParagraphVoiceId, "default-paragraph-voice-id", "", "The voice_id that corresponds to the default voice used for new paragraphs.")
	cmd.Flags().StringVar(&bodyDefaultTitleVoiceId, "default-title-voice-id", "", "The voice_id that corresponds to the default voice used for new titles.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "An optional description of the Studio project.")
	cmd.Flags().StringVar(&bodyFiction, "fiction", "", "An optional specification of whether the content of this Studio project is fiction.")
	cmd.Flags().StringVar(&bodyFromContentJson, "from-content-json", "", "An optional content to initialize the Studio project with. If this is set, 'from_url' and 'from_document' must be...")
	cmd.Flags().StringVar(&bodyFromDocument, "from-document", "", "An optional .epub, .pdf, .txt or similar file can be provided. If provided, we will initialize the Studio project...")
	cmd.Flags().StringVar(&bodyFromUrl, "from-url", "", "An optional URL from which we will extract content to initialize the Studio project. If this is set, 'from_url' and...")
	cmd.Flags().StringVar(&bodyGenres, "genres", "", "An optional list of genres associated with the Studio project.")
	cmd.Flags().StringVar(&bodyIsbnNumber, "isbn-number", "", "An optional ISBN number of the Studio project you want to create, this will be added as metadata to the mp3 file on...")
	cmd.Flags().StringVar(&bodyLanguage, "language", "", "An optional language of the Studio project. Two-letter language code (ISO 639-1).")
	cmd.Flags().StringVar(&bodyMatureContent, "mature-content", "false", "An optional specification of whether this Studio project contains mature content.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the Studio project, used for identification only.")
	cmd.Flags().StringVar(&bodyOriginalPublicationDate, "original-publication-date", "", "An optional original publication date of the Studio project, in the format YYYY-MM-DD or YYYY.")
	cmd.Flags().StringVar(&bodyPronunciationDictionaryLocators, "pronunciation-dictionary-locators", "", "A list of pronunciation dictionary locators (pronunciation_dictionary_id, version_id) encoded as a list of JSON...")
	cmd.Flags().StringVar(&bodyQualityPreset, "quality-preset", "standard", "Output quality of the generated audio. Must be one of: 'standard' - standard output format, 128kbps with 44.1kHz...")
	cmd.Flags().StringVar(&bodySourceType, "source-type", "", "The type of Studio project to create.")
	cmd.Flags().StringVar(&bodyTargetAudience, "target-audience", "", "An optional target audience of the Studio project.")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "An optional name of the author of the Studio project, this will be added as metadata to the mp3 file on Studio...")
	cmd.Flags().StringVar(&bodyVoiceSettings, "voice-settings", "", "Optional voice settings overrides for the project, encoded as a list of JSON strings. Example: ['{'voice_id':...")
	cmd.Flags().BoolVar(&bodyVolumeNormalization, "volume-normalization", false, "When the Studio project is downloaded, should the returned audio have postprocessing in order to make it compliant...")

	return cmd
}
