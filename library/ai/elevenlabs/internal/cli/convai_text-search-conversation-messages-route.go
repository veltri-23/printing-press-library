// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newConvaiTextSearchConversationMessagesRouteCmd(flags *rootFlags) *cobra.Command {
	var flagTextQuery string
	var flagAgentId string
	var flagCallSuccessful string
	var flagCallStartBeforeUnix string
	var flagCallStartAfterUnix string
	var flagCallDurationMinSecs string
	var flagCallDurationMaxSecs string
	var flagRatingMax string
	var flagRatingMin string
	var flagHasFeedbackComment string
	var flagUserId string
	var flagEvaluationParams string
	var flagDataCollectionParams string
	var flagToolNames string
	var flagToolNamesSuccessful string
	var flagToolNamesErrored string
	var flagMainLanguages string
	var flagPageSize int
	var flagSummaryMode string
	var flagConversationInitiationSource string
	var flagBranchId string
	var flagSortBy string
	var flagCursor string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "text-search-conversation-messages-route",
		Short:       "Search through conversation transcript messages by full-text and fuzzy search",
		Example:     "  elevenlabs-pp-cli convai text-search-conversation-messages-route --text-query example-value",
		Annotations: map[string]string{"pp:endpoint": "convai.text-search-conversation-messages-route", "pp:method": "GET", "pp:path": "/v1/convai/conversations/messages/text-search", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("text-query") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "text-query")
			}
			if cmd.Flags().Changed("summary-mode") {
				allowedSummaryMode := []string{"exclude", "include"}
				validSummaryMode := false
				for _, v := range allowedSummaryMode {
					if flagSummaryMode == v {
						validSummaryMode = true
						break
					}
				}
				if !validSummaryMode {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "summary-mode", flagSummaryMode, allowedSummaryMode)
				}
			}
			if cmd.Flags().Changed("sort-by") {
				allowedSortBy := []string{"search_score", "created_at"}
				validSortBy := false
				for _, v := range allowedSortBy {
					if flagSortBy == v {
						validSortBy = true
						break
					}
				}
				if !validSortBy {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-by", flagSortBy, allowedSortBy)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/convai/conversations/messages/text-search"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "convai", path, map[string]string{
				"text_query":                     fmt.Sprintf("%v", flagTextQuery),
				"agent_id":                       fmt.Sprintf("%v", flagAgentId),
				"call_successful":                fmt.Sprintf("%v", flagCallSuccessful),
				"call_start_before_unix":         fmt.Sprintf("%v", flagCallStartBeforeUnix),
				"call_start_after_unix":          fmt.Sprintf("%v", flagCallStartAfterUnix),
				"call_duration_min_secs":         fmt.Sprintf("%v", flagCallDurationMinSecs),
				"call_duration_max_secs":         fmt.Sprintf("%v", flagCallDurationMaxSecs),
				"rating_max":                     fmt.Sprintf("%v", flagRatingMax),
				"rating_min":                     fmt.Sprintf("%v", flagRatingMin),
				"has_feedback_comment":           fmt.Sprintf("%v", flagHasFeedbackComment),
				"user_id":                        fmt.Sprintf("%v", flagUserId),
				"evaluation_params":              fmt.Sprintf("%v", flagEvaluationParams),
				"data_collection_params":         fmt.Sprintf("%v", flagDataCollectionParams),
				"tool_names":                     fmt.Sprintf("%v", flagToolNames),
				"tool_names_successful":          fmt.Sprintf("%v", flagToolNamesSuccessful),
				"tool_names_errored":             fmt.Sprintf("%v", flagToolNamesErrored),
				"main_languages":                 fmt.Sprintf("%v", flagMainLanguages),
				"page_size":                      fmt.Sprintf("%v", flagPageSize),
				"summary_mode":                   fmt.Sprintf("%v", flagSummaryMode),
				"conversation_initiation_source": fmt.Sprintf("%v", flagConversationInitiationSource),
				"branch_id":                      fmt.Sprintf("%v", flagBranchId),
				"sort_by":                        fmt.Sprintf("%v", flagSortBy),
				"cursor":                         fmt.Sprintf("%v", flagCursor),
			}, nil, flagAll, "cursor", "", "has_more")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagTextQuery, "text-query", "", "The search query text for full-text and fuzzy matching")
	cmd.Flags().StringVar(&flagAgentId, "agent-id", "", "Agent id (agent_…) or speech engine external id (seng_), resolved to the same underlying resource.")
	cmd.Flags().StringVar(&flagCallSuccessful, "call-successful", "", "The result of the success evaluation")
	cmd.Flags().StringVar(&flagCallStartBeforeUnix, "call-start-before-unix", "", "Unix timestamp (in seconds) to filter conversations up to this start date.")
	cmd.Flags().StringVar(&flagCallStartAfterUnix, "call-start-after-unix", "", "Unix timestamp (in seconds) to filter conversations after to this start date.")
	cmd.Flags().StringVar(&flagCallDurationMinSecs, "call-duration-min-secs", "", "Minimum call duration in seconds.")
	cmd.Flags().StringVar(&flagCallDurationMaxSecs, "call-duration-max-secs", "", "Maximum call duration in seconds.")
	cmd.Flags().StringVar(&flagRatingMax, "rating-max", "", "Maximum overall rating (1-5).")
	cmd.Flags().StringVar(&flagRatingMin, "rating-min", "", "Minimum overall rating (1-5).")
	cmd.Flags().StringVar(&flagHasFeedbackComment, "has-feedback-comment", "", "Filter conversations with user feedback comments.")
	cmd.Flags().StringVar(&flagUserId, "user-id", "", "Filter conversations by the user ID who initiated them.")
	cmd.Flags().StringVar(&flagEvaluationParams, "evaluation-params", "", "Evaluation filters. Repeat param. Format: criteria_id:result. Example: eval=value_framing:success")
	cmd.Flags().StringVar(&flagDataCollectionParams, "data-collection-params", "", "Data collection filters. Repeat param. Format: id:op:value where op is one of...")
	cmd.Flags().StringVar(&flagToolNames, "tool-names", "", "Filter conversations by tool names used during the call.")
	cmd.Flags().StringVar(&flagToolNamesSuccessful, "tool-names-successful", "", "Filter conversations by tool names that had successful calls.")
	cmd.Flags().StringVar(&flagToolNamesErrored, "tool-names-errored", "", "Filter conversations by tool names that had errored calls.")
	cmd.Flags().StringVar(&flagMainLanguages, "main-languages", "", "Filter conversations by detected main language (language code).")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 20, "Number of results per page. Max 50.")
	cmd.Flags().StringVar(&flagSummaryMode, "summary-mode", "exclude", "Whether to include transcript summaries in the response. (one of: exclude, include)")
	cmd.Flags().StringVar(&flagConversationInitiationSource, "conversation-initiation-source", "", "Conversation initiation source")
	cmd.Flags().StringVar(&flagBranchId, "branch-id", "", "Filter conversations by branch ID.")
	cmd.Flags().StringVar(&flagSortBy, "sort-by", "search_score", "Sort order for search results. 'search_score' sorts by search score, 'created_at' sorts by conversation start time. (one of: search_score, created_at)")
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "Used for fetching next page. Cursor is returned in the response.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
