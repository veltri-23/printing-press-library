// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAudioNativeCreateProjectCmd(flags *rootFlags) *cobra.Command {
	var bodyApplyTextNormalization string
	var bodyAuthor string
	var bodyAutoConvert bool
	var bodyBackgroundColor string
	var bodyFile string
	var bodyImage string
	var bodyModelId string
	var bodyName string
	var bodyPronunciationDictionaryLocators string
	var bodySessionization int
	var bodySmall bool
	var bodyTextColor string
	var bodyTitle string
	var bodyVoiceId string

	cmd := &cobra.Command{
		Use:         "create-project",
		Aliases:     []string{"create"},
		Short:       "Creates Audio Native enabled project, optionally starts conversion and returns project ID and embeddable HTML snippet.",
		Example:     "  elevenlabs-pp-cli audio-native create-project --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "audio-native.create-project", "pp:method": "POST", "pp:path": "/v1/audio-native"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "name")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/audio-native"
			params := map[string]string{}
			fields := map[string]string{}
			fileFields := map[string]string{}
			if bodyApplyTextNormalization != "" {
				fields["apply_text_normalization"] = bodyApplyTextNormalization
			}
			if bodyAuthor != "" {
				fields["author"] = bodyAuthor
			}
			if bodyAutoConvert != false {
				fields["auto_convert"] = fmt.Sprintf("%v", bodyAutoConvert)
			}
			if bodyBackgroundColor != "" {
				fields["background_color"] = bodyBackgroundColor
			}
			if bodyFile != "" {
				fileFields["file"] = bodyFile
			}
			if bodyImage != "" {
				fields["image"] = bodyImage
			}
			if bodyModelId != "" {
				fields["model_id"] = bodyModelId
			}
			if bodyName != "" {
				fields["name"] = bodyName
			}
			if bodyPronunciationDictionaryLocators != "" {
				if !json.Valid([]byte(bodyPronunciationDictionaryLocators)) {
					return fmt.Errorf("parsing --pronunciation-dictionary-locators JSON: invalid JSON")
				}
				fields["pronunciation_dictionary_locators"] = bodyPronunciationDictionaryLocators
			}
			if bodySessionization != 0 {
				fields["sessionization"] = fmt.Sprintf("%v", bodySessionization)
			}
			if bodySmall != false {
				fields["small"] = fmt.Sprintf("%v", bodySmall)
			}
			if bodyTextColor != "" {
				fields["text_color"] = bodyTextColor
			}
			if bodyTitle != "" {
				fields["title"] = bodyTitle
			}
			if bodyVoiceId != "" {
				fields["voice_id"] = bodyVoiceId
			}

			data, statusCode, err := c.PostMultipartWithParams(path, params, fields, fileFields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "audio-native",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyApplyTextNormalization, "apply-text-normalization", "", "This parameter controls text normalization with four modes: 'auto', 'on', 'apply_english' and 'off'. When set to...")
	cmd.Flags().StringVar(&bodyAuthor, "author", "", "Author used in the player and inserted at the start of the uploaded article. If not provided, the default author set...")
	cmd.Flags().BoolVar(&bodyAutoConvert, "auto-convert", false, "Whether to auto convert the project to audio or not.")
	cmd.Flags().StringVar(&bodyBackgroundColor, "background-color", "", "Background color used in the player. If not provided, default background color set in the Player settings is used.")
	cmd.Flags().StringVar(&bodyFile, "file", "", "Either txt or HTML input file containing the article content. HTML should be formatted as follows...")
	cmd.Flags().StringVar(&bodyImage, "image", "", "(Deprecated) Image URL used in the player. If not provided, default image set in the Player settings is used.")
	cmd.Flags().StringVar(&bodyModelId, "model-id", "", "TTS Model ID used in the player. If not provided, default model ID set in the Player settings is used.")
	cmd.Flags().StringVar(&bodyName, "name", "", "Project name.")
	cmd.Flags().StringVar(&bodyPronunciationDictionaryLocators, "pronunciation-dictionary-locators", "", "A list of pronunciation dictionary locators (pronunciation_dictionary_id, version_id) encoded as a list of JSON...")
	cmd.Flags().IntVar(&bodySessionization, "sessionization", 0, "(Deprecated) Specifies for how many minutes to persist the session across page reloads. If not provided, default...")
	cmd.Flags().BoolVar(&bodySmall, "small", false, "(Deprecated) Whether to use small player or not. If not provided, default value set in the Player settings is used.")
	cmd.Flags().StringVar(&bodyTextColor, "text-color", "", "Text color used in the player. If not provided, default text color set in the Player settings is used.")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Title used in the player and inserted at the top of the uploaded article. If not provided, the default title set in...")
	cmd.Flags().StringVar(&bodyVoiceId, "voice-id", "", "Voice ID used to voice the content. If not provided, default voice ID set in the Player settings is used.")

	return cmd
}
