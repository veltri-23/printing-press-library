// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSoundGenerationPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagOutputFormat string
	var bodyDurationSeconds string
	var bodyLoop bool
	var bodyModelId string
	var bodyPromptInfluence string
	var bodyText string

	cmd := &cobra.Command{
		Use:         "sound-generation",
		Short:       "Turn text into sound effects for your videos, voice-overs or video games using the most advanced sound effects...",
		Long:        "Shortcut for 'sound-generation sound_generation'. Turn text into sound effects for your videos, voice-overs or video games using the most advanced sound effects...",
		Example:     "  elevenlabs-pp-cli sound-generation --text example-value",
		Annotations: map[string]string{"pp:endpoint": "sound-generation.sound_generation", "pp:method": "POST", "pp:path": "/v1/sound-generation"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("text") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "text")
			}
			if cmd.Flags().Changed("output-format") {
				allowedOutputFormat := []string{"mp3_22050_32", "mp3_24000_48", "mp3_44100_32", "mp3_44100_64", "mp3_44100_96", "mp3_44100_128", "mp3_44100_192", "pcm_8000", "pcm_16000", "pcm_22050", "pcm_24000", "pcm_32000", "pcm_44100", "pcm_48000", "ulaw_8000", "alaw_8000", "opus_48000_32", "opus_48000_64", "opus_48000_96", "opus_48000_128", "opus_48000_192"}
				validOutputFormat := false
				for _, v := range allowedOutputFormat {
					if flagOutputFormat == v {
						validOutputFormat = true
						break
					}
				}
				if !validOutputFormat {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "output-format", flagOutputFormat, allowedOutputFormat)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/sound-generation"
			params := map[string]string{}
			if flagOutputFormat != "" {
				params["output_format"] = fmt.Sprintf("%v", flagOutputFormat)
			}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyDurationSeconds != "" {
				body["duration_seconds"] = bodyDurationSeconds
			}
			if bodyLoop != false {
				body["loop"] = bodyLoop
			}
			if bodyModelId != "" {
				body["model_id"] = bodyModelId
			}
			if bodyPromptInfluence != "" {
				body["prompt_influence"] = bodyPromptInfluence
			}
			if bodyText != "" {
				body["text"] = bodyText
			}
			data, _, err := c.PostWithParams(path, params, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = prov
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>")
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().StringVar(&flagOutputFormat, "output-format", "mp3_44100_128", "Output format of the generated audio. Formatted as codec_sample_rate_bitrate. So an mp3 with 22.05kHz sample rate at... (one of: mp3_22050_32, mp3_24000_48, mp3_44100_32, mp3_44100_64, mp3_44100_96, mp3_44100_128, mp3_44100_192, pcm_8000, pcm_16000, pcm_22050, pcm_24000, pcm_32000, pcm_44100, pcm_48000, ulaw_8000, alaw_8000, opus_48000_32, opus_48000_64, opus_48000_96, opus_48000_128, opus_48000_192)")
	cmd.Flags().StringVar(&bodyDurationSeconds, "duration-seconds", "", "The duration of the sound which will be generated in seconds. Must be at least 0.5 and at most 30. If set to None we...")
	cmd.Flags().BoolVar(&bodyLoop, "loop", false, "Whether to create a sound effect that loops smoothly. Only available for the 'eleven_text_to_sound_v2 model'.")
	cmd.Flags().StringVar(&bodyModelId, "model-id", "eleven_text_to_sound_v2", "The model ID to use for the sound generation.")
	cmd.Flags().StringVar(&bodyPromptInfluence, "prompt-influence", "0.3", "A higher prompt influence makes your generation follow the prompt more closely while also making generations less...")
	cmd.Flags().StringVar(&bodyText, "text", "", "The text that will get converted into a sound effect.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
