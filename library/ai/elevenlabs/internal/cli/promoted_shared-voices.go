// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSharedVoicesPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagPageSize int
	var flagCategory string
	var flagGender string
	var flagAge string
	var flagAccent string
	var flagLanguage string
	var flagLocale string
	var flagSearch string
	var flagUseCases string
	var flagDescriptives string
	var flagFeatured bool
	var flagMinNoticePeriodDays string
	var flagIncludeCustomRates string
	var flagIncludeLiveModerated string
	var flagReaderAppEnabled bool
	var flagOwnerId string
	var flagSort string
	var flagPage string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "shared-voices",
		Short:       "Retrieves a list of shared voices.",
		Long:        "Shortcut for 'shared-voices get-library-voices'. Retrieves a list of shared voices.",
		Example:     "  elevenlabs-pp-cli shared-voices",
		Annotations: map[string]string{"pp:endpoint": "shared-voices.get-library-voices", "pp:method": "GET", "pp:path": "/v1/shared-voices", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("category") {
				allowedCategory := []string{"professional", "famous", "high_quality"}
				validCategory := false
				for _, v := range allowedCategory {
					if flagCategory == v {
						validCategory = true
						break
					}
				}
				if !validCategory {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "category", flagCategory, allowedCategory)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/shared-voices"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "shared-voices", path, map[string]string{
				"page_size":              fmt.Sprintf("%v", flagPageSize),
				"category":               fmt.Sprintf("%v", flagCategory),
				"gender":                 fmt.Sprintf("%v", flagGender),
				"age":                    fmt.Sprintf("%v", flagAge),
				"accent":                 fmt.Sprintf("%v", flagAccent),
				"language":               fmt.Sprintf("%v", flagLanguage),
				"locale":                 fmt.Sprintf("%v", flagLocale),
				"search":                 fmt.Sprintf("%v", flagSearch),
				"use_cases":              fmt.Sprintf("%v", flagUseCases),
				"descriptives":           fmt.Sprintf("%v", flagDescriptives),
				"featured":               fmt.Sprintf("%v", flagFeatured),
				"min_notice_period_days": fmt.Sprintf("%v", flagMinNoticePeriodDays),
				"include_custom_rates":   fmt.Sprintf("%v", flagIncludeCustomRates),
				"include_live_moderated": fmt.Sprintf("%v", flagIncludeLiveModerated),
				"reader_app_enabled":     fmt.Sprintf("%v", flagReaderAppEnabled),
				"owner_id":               fmt.Sprintf("%v", flagOwnerId),
				"sort":                   fmt.Sprintf("%v", flagSort),
				"page":                   fmt.Sprintf("%v", flagPage),
			}, nil, flagAll, "", "", "has_more")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagPageSize, "page-size", 30, "How many shared voices to return at maximum. Can not exceed 100, defaults to 30.")
	cmd.Flags().StringVar(&flagCategory, "category", "", "Voice category used for filtering (one of: professional, famous, high_quality)")
	cmd.Flags().StringVar(&flagGender, "gender", "", "Gender used for filtering")
	cmd.Flags().StringVar(&flagAge, "age", "", "Age used for filtering")
	cmd.Flags().StringVar(&flagAccent, "accent", "", "Accent used for filtering")
	cmd.Flags().StringVar(&flagLanguage, "language", "", "Language used for filtering")
	cmd.Flags().StringVar(&flagLocale, "locale", "", "Locale used for filtering")
	cmd.Flags().StringVar(&flagSearch, "search", "", "Search term used for filtering")
	cmd.Flags().StringVar(&flagUseCases, "use-cases", "", "Use-case used for filtering")
	cmd.Flags().StringVar(&flagDescriptives, "descriptives", "", "Search term used for filtering")
	cmd.Flags().BoolVar(&flagFeatured, "featured", false, "Filter featured voices")
	cmd.Flags().StringVar(&flagMinNoticePeriodDays, "min-notice-period-days", "", "Filter voices with a minimum notice period of the given number of days.")
	cmd.Flags().StringVar(&flagIncludeCustomRates, "include-custom-rates", "", "Include/exclude voices with custom rates")
	cmd.Flags().StringVar(&flagIncludeLiveModerated, "include-live-moderated", "", "Include/exclude voices that are live moderated")
	cmd.Flags().BoolVar(&flagReaderAppEnabled, "reader-app-enabled", false, "Filter voices that are enabled for the reader app")
	cmd.Flags().StringVar(&flagOwnerId, "owner-id", "", "Filter voices by public owner ID")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort criteria")
	cmd.Flags().StringVar(&flagPage, "page", "0", "Page")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
