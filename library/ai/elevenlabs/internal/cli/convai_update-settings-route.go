// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newConvaiUpdateSettingsRouteCmd(flags *rootFlags) *cobra.Command {
	var bodyCanUseMcpServers bool
	var bodyConversationEmbeddingRetentionDays string
	var bodyConversationInitiationClientDataWebhook string
	var bodyDefaultLivekitStack string
	var bodyRagRetentionPeriodDays int
	var bodyWebhooksEvents string
	var bodyWebhooksPostCallWebhookId string
	var bodyWebhooksSendAudio string
	var bodyWebhooksTranscriptFormat string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update-settings-route",
		Short:       "Update Convai settings for the workspace",
		Example:     "  elevenlabs-pp-cli convai update-settings-route",
		Annotations: map[string]string{"pp:endpoint": "convai.update-settings-route", "pp:method": "PATCH", "pp:path": "/v1/convai/settings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/convai/settings"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCanUseMcpServers != false {
					body["can_use_mcp_servers"] = bodyCanUseMcpServers
				}
				if bodyConversationEmbeddingRetentionDays != "" {
					body["conversation_embedding_retention_days"] = bodyConversationEmbeddingRetentionDays
				}
				if bodyConversationInitiationClientDataWebhook != "" {
					body["conversation_initiation_client_data_webhook"] = bodyConversationInitiationClientDataWebhook
				}
				if bodyDefaultLivekitStack != "" {
					body["default_livekit_stack"] = bodyDefaultLivekitStack
				}
				if bodyRagRetentionPeriodDays != 0 {
					body["rag_retention_period_days"] = bodyRagRetentionPeriodDays
				}
				{
					nestedWebhooks := map[string]any{}
					if bodyWebhooksEvents != "" {
						var parsedWebhooksEvents any
						if err := json.Unmarshal([]byte(bodyWebhooksEvents), &parsedWebhooksEvents); err != nil {
							return fmt.Errorf("parsing --webhooks-events JSON: %w", err)
						}
						nestedWebhooks["events"] = parsedWebhooksEvents
					}
					if bodyWebhooksPostCallWebhookId != "" {
						nestedWebhooks["post_call_webhook_id"] = bodyWebhooksPostCallWebhookId
					}
					if bodyWebhooksSendAudio != "" {
						nestedWebhooks["send_audio"] = bodyWebhooksSendAudio
					}
					if bodyWebhooksTranscriptFormat != "" {
						nestedWebhooks["transcript_format"] = bodyWebhooksTranscriptFormat
					}
					if len(nestedWebhooks) > 0 {
						body["webhooks"] = nestedWebhooks
					}
				}
			}
			data, statusCode, err := c.PatchWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "convai",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyCanUseMcpServers, "can-use-mcp-servers", false, "Whether the workspace can use MCP servers")
	cmd.Flags().StringVar(&bodyConversationEmbeddingRetentionDays, "conversation-embedding-retention-days", "", "Days to retain conversation embeddings. None means use the system default (30 days).")
	cmd.Flags().StringVar(&bodyConversationInitiationClientDataWebhook, "conversation-initiation-client-data-webhook", "", "Conversation initiation client data webhook")
	cmd.Flags().StringVar(&bodyDefaultLivekitStack, "default-livekit-stack", "standard", "Default livekit stack")
	cmd.Flags().IntVar(&bodyRagRetentionPeriodDays, "rag-retention-period-days", 10, "Rag retention period days")
	cmd.Flags().StringVar(&bodyWebhooksEvents, "webhooks-events", "", "List of event types to send via webhook. Options: transcript, audio, call_initiation_failure.")
	cmd.Flags().StringVar(&bodyWebhooksPostCallWebhookId, "webhooks-post-call-webhook-id", "", "Post call webhook id")
	cmd.Flags().StringVar(&bodyWebhooksSendAudio, "webhooks-send-audio", "", "DEPRECATED: Use 'events' field instead. Whether to send audio data with post-call webhooks for ConvAI conversations")
	cmd.Flags().StringVar(&bodyWebhooksTranscriptFormat, "webhooks-transcript-format", "", "Format for transcript webhooks.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
