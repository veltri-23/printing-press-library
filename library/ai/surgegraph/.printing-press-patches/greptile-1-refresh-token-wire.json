{
  "schema_version": 2,
  "id": "greptile-1-refresh-token-wire",
  "applied_at": "2026-05-13",
  "base_run_id": "20260512-171718",
  "base_printing_press_version": "4.5.0",
  "summary": "Wire refreshAccessToken() to oauth.Refresh + persist the new tokens via Config.SaveTokens.",
  "reason": "Generator emitted an empty stub returning nil, so an expired access token silently fell through to authHeader() and every subsequent request returned 401 with no diagnostic. oauth.Refresh() existed but was never called.",
  "files": [
    "internal/client/client.go"
  ]
}
