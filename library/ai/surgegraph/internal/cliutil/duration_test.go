// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cliutil

import (
	"testing"
	"time"
)

func TestParseDayDuration(t *testing.T) {
	t.Parallel()
	cases := []struct {
		in   string
		want time.Duration
		ok   bool
	}{
		{"7d", 7 * 24 * time.Hour, true},
		{"30d", 30 * 24 * time.Hour, true},
		{"1d", 24 * time.Hour, true},
		{"168h", 168 * time.Hour, true},
		{"30m", 30 * time.Minute, true},
		{"1h30m", 90 * time.Minute, true},
		{"500ms", 500 * time.Millisecond, true},
		// failure cases
		{"", 0, false},
		{"7days", 0, false},
		{"7 d", 0, false},
		{"abc", 0, false},
	}
	for _, c := range cases {
		got, err := ParseDayDuration(c.in)
		if c.ok {
			if err != nil {
				t.Errorf("ParseDayDuration(%q): unexpected error: %v", c.in, err)
				continue
			}
			if got != c.want {
				t.Errorf("ParseDayDuration(%q) = %v, want %v", c.in, got, c.want)
			}
		} else {
			if err == nil {
				t.Errorf("ParseDayDuration(%q): expected error, got %v", c.in, got)
			}
		}
	}
}
