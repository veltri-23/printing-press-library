// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGetAiVisibilityCitationDomainPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyDomain string
	var bodyEndDate string
	var bodyLimit float64
	var bodyModelId string
	var bodyProjectId string
	var bodyStartDate string

	cmd := &cobra.Command{
		Use:         "get-ai-visibility-citation-domain",
		Short:       "Drill into citations for a specific domain. Returns page-level URLs cited from that domain, which engines cited each...",
		Long:        "Shortcut for 'get-ai-visibility-citation-domain get_ai_visibility_citation_domain'. Drill into citations for a specific domain. Returns page-level URLs cited from that domain, which engines cited each...",
		Example:     "  surgegraph-pp-cli get-ai-visibility-citation-domain --domain example-value",
		Annotations: map[string]string{"pp:endpoint": "get-ai-visibility-citation-domain.get_ai_visibility_citation_domain", "pp:method": "POST", "pp:path": "/v1/get_ai_visibility_citation_domain"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("domain") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "domain")
			}
			if !cmd.Flags().Changed("end-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "end-date")
			}
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("start-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "start-date")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/get_ai_visibility_citation_domain"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyDomain != "" {
				body["domain"] = bodyDomain
			}
			if bodyEndDate != "" {
				body["endDate"] = bodyEndDate
			}
			if bodyLimit != 0.0 {
				body["limit"] = bodyLimit
			}
			if bodyModelId != "" {
				body["modelId"] = bodyModelId
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodyStartDate != "" {
				body["startDate"] = bodyStartDate
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyDomain, "domain", "", "Domain to drill into (e.g. 'reddit.com')")
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "End date (ISO 8601)")
	cmd.Flags().Float64Var(&bodyLimit, "limit", 20.000000, "Max pages returned (default 20)")
	cmd.Flags().StringVar(&bodyModelId, "model-id", "", "Filter to a specific engine. One of: chatgpt, gemini, google-ai-mode, google-ai-overview, perplexity....")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "Start date (ISO 8601)")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
