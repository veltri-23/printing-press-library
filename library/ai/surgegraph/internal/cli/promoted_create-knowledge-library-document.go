// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCreateKnowledgeLibraryDocumentPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyContent string
	var bodyCreditChoice string
	var bodyLibraryId string
	var bodyName string
	var bodyProjectId string

	cmd := &cobra.Command{
		Use:         "create-knowledge-library-document",
		Short:       "Create a text document to a knowledge library. The document content will be indexed for use as AI writing context....",
		Long:        "Shortcut for 'create-knowledge-library-document create_knowledge_library_document'. Create a text document to a knowledge library. The document content will be indexed for use as AI writing context....",
		Example:     "  surgegraph-pp-cli create-knowledge-library-document --content example-value",
		Annotations: map[string]string{"pp:endpoint": "create-knowledge-library-document.create_knowledge_library_document", "pp:method": "POST", "pp:path": "/v1/create_knowledge_library_document"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("content") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "content")
			}
			if !cmd.Flags().Changed("library-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "library-id")
			}
			if !cmd.Flags().Changed("name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "name")
			}
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/create_knowledge_library_document"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyContent != "" {
				body["content"] = bodyContent
			}
			if bodyCreditChoice != "" {
				body["creditChoice"] = bodyCreditChoice
			}
			if bodyLibraryId != "" {
				body["libraryId"] = bodyLibraryId
			}
			if bodyName != "" {
				body["name"] = bodyName
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyContent, "content", "", "Document text content")
	cmd.Flags().StringVar(&bodyCreditChoice, "credit-choice", "", "How to pay for indexing: 'credit' deducts the surcharge from credits (default), 'byok' uses the project OpenAI key")
	cmd.Flags().StringVar(&bodyLibraryId, "library-id", "", "Knowledge library ID to add the document to")
	cmd.Flags().StringVar(&bodyName, "name", "", "Document name")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
