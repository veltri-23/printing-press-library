// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newDeleteApiKeyPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyKeyId int

	cmd := &cobra.Command{
		Use:         "delete-api-key",
		Short:       "Delete an API key from the organization. Use get_openai_keys / get_gemini_keys / get_anthropic_keys to find the...",
		Long:        "Shortcut for 'delete-api-key delete_api_key'. Delete an API key from the organization. Use get_openai_keys / get_gemini_keys / get_anthropic_keys to find the...",
		Example:     "  surgegraph-pp-cli delete-api-key",
		Annotations: map[string]string{"pp:endpoint": "delete-api-key.delete_api_key", "pp:method": "POST", "pp:path": "/v1/delete_api_key"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("key-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "key-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/delete_api_key"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyKeyId != 0 {
				body["keyId"] = bodyKeyId
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyKeyId, "key-id", 0, "Numeric API key ID")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
