// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGetAiVisibilityResponseStructurePromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyBrandName string
	var bodyEndDate string
	var bodyModelId string
	var bodyProjectId string
	var bodyStartDate string

	cmd := &cobra.Command{
		Use:         "get-ai-visibility-response-structure",
		Short:       "Retrieve response structure analysis for a specific brand in AI Visibility tracking. Returns per-engine response...",
		Long:        "Shortcut for 'get-ai-visibility-response-structure get_ai_visibility_response_structure'. Retrieve response structure analysis for a specific brand in AI Visibility tracking. Returns per-engine response...",
		Example:     "  surgegraph-pp-cli get-ai-visibility-response-structure --brand-name example-resource",
		Annotations: map[string]string{"pp:endpoint": "get-ai-visibility-response-structure.get_ai_visibility_response_structure", "pp:method": "POST", "pp:path": "/v1/get_ai_visibility_response_structure"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("brand-name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "brand-name")
			}
			if !cmd.Flags().Changed("end-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "end-date")
			}
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("start-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "start-date")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/get_ai_visibility_response_structure"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyBrandName != "" {
				body["brandName"] = bodyBrandName
			}
			if bodyEndDate != "" {
				body["endDate"] = bodyEndDate
			}
			if bodyModelId != "" {
				body["modelId"] = bodyModelId
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodyStartDate != "" {
				body["startDate"] = bodyStartDate
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyBrandName, "brand-name", "", "Brand name to analyze")
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "End date (ISO 8601)")
	cmd.Flags().StringVar(&bodyModelId, "model-id", "", "Filter to a specific engine. One of: chatgpt, gemini, google-ai-mode, google-ai-overview, perplexity....")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "Start date (ISO 8601)")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
