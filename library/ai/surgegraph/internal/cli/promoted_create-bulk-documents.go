// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCreateBulkDocumentsPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyArticleType string
	var bodyArticles string
	var bodyIntent string
	var bodyJobName string
	var bodyLanguageCode string
	var bodyLocationCode float64
	var bodyModel string
	var bodyProjectId string
	var bodyWebhookAcknowledged bool

	cmd := &cobra.Command{
		Use:         "create-bulk-documents",
		Short:       "Bulk create multiple AI-written documents in a single batch (max 50). Each article needs a prompt; all other...",
		Long:        "Shortcut for 'create-bulk-documents create_bulk_documents'. Bulk create multiple AI-written documents in a single batch (max 50). Each article needs a prompt; all other...",
		Example:     "  surgegraph-pp-cli create-bulk-documents --article-type example-value",
		Annotations: map[string]string{"pp:endpoint": "create-bulk-documents.create_bulk_documents", "pp:method": "POST", "pp:path": "/v1/create_bulk_documents"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("articles") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "articles")
			}
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("webhook-acknowledged") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "webhook-acknowledged")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/create_bulk_documents"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyArticleType != "" {
				body["articleType"] = bodyArticleType
			}
			if bodyArticles != "" {
				var parsedArticles any
				if err := json.Unmarshal([]byte(bodyArticles), &parsedArticles); err != nil {
					return fmt.Errorf("parsing --articles JSON: %w", err)
				}
				body["articles"] = parsedArticles
			}
			if bodyIntent != "" {
				body["intent"] = bodyIntent
			}
			if bodyJobName != "" {
				body["jobName"] = bodyJobName
			}
			if bodyLanguageCode != "" {
				body["languageCode"] = bodyLanguageCode
			}
			if bodyLocationCode != 0.0 {
				body["locationCode"] = bodyLocationCode
			}
			if bodyModel != "" {
				body["model"] = bodyModel
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodyWebhookAcknowledged != false {
				body["webhookAcknowledged"] = bodyWebhookAcknowledged
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyArticleType, "article-type", "general", "Article type for all articles")
	cmd.Flags().StringVar(&bodyArticles, "articles", "", "Array of articles to create (1-50)")
	cmd.Flags().StringVar(&bodyIntent, "intent", "informational", "Search intent")
	cmd.Flags().StringVar(&bodyJobName, "job-name", "", "Batch job name (default: auto-generated with timestamp)")
	cmd.Flags().StringVar(&bodyLanguageCode, "language-code", "en", "Language code (default: en). Use get_languages to discover valid values.")
	cmd.Flags().Float64Var(&bodyLocationCode, "location-code", 2840.000000, "Location code (default: 2840 for US)")
	cmd.Flags().StringVar(&bodyModel, "model", "gpt-5.4", "AI model ID for all articles")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().BoolVar(&bodyWebhookAcknowledged, "webhook-acknowledged", false, "REQUIRED for OpenAI (gpt-*) models. Set to true ONLY after the user has explicitly confirmed they have added...")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
