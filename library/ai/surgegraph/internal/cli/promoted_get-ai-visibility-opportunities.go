// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGetAiVisibilityOpportunitiesPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyEndDate string
	var bodyLimit float64
	var bodyPriority string
	var bodyProjectId string
	var bodyStartDate string
	var bodyType string

	cmd := &cobra.Command{
		Use:         "get-ai-visibility-opportunities",
		Short:       "Retrieve AI Visibility optimization opportunities for a project. Returns stats by priority and category, plus a list...",
		Long:        "Shortcut for 'get-ai-visibility-opportunities get_ai_visibility_opportunities'. Retrieve AI Visibility optimization opportunities for a project. Returns stats by priority and category, plus a list...",
		Example:     "  surgegraph-pp-cli get-ai-visibility-opportunities --end-date 2026-01-15",
		Annotations: map[string]string{"pp:endpoint": "get-ai-visibility-opportunities.get_ai_visibility_opportunities", "pp:method": "POST", "pp:path": "/v1/get_ai_visibility_opportunities"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("end-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "end-date")
			}
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("start-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "start-date")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/get_ai_visibility_opportunities"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyEndDate != "" {
				body["endDate"] = bodyEndDate
			}
			if bodyLimit != 0.0 {
				body["limit"] = bodyLimit
			}
			if bodyPriority != "" {
				body["priority"] = bodyPriority
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodyStartDate != "" {
				body["startDate"] = bodyStartDate
			}
			if bodyType != "" {
				body["type"] = bodyType
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "End date (ISO 8601)")
	cmd.Flags().Float64Var(&bodyLimit, "limit", 20.000000, "Max opportunities returned (default 20)")
	cmd.Flags().StringVar(&bodyPriority, "priority", "", "Filter by priority")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "Start date (ISO 8601)")
	cmd.Flags().StringVar(&bodyType, "type", "", "Filter by opportunity type")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
