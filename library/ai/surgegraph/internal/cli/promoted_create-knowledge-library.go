// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCreateKnowledgeLibraryPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyDescription string
	var bodyName string
	var bodyProjectId string

	cmd := &cobra.Command{
		Use:         "create-knowledge-library",
		Short:       "Create a new knowledge library in a project. Knowledge libraries store documents that can be used as context for AI...",
		Long:        "Shortcut for 'create-knowledge-library create_knowledge_library'. Create a new knowledge library in a project. Knowledge libraries store documents that can be used as context for AI...",
		Example:     "  surgegraph-pp-cli create-knowledge-library --description example-value",
		Annotations: map[string]string{"pp:endpoint": "create-knowledge-library.create_knowledge_library", "pp:method": "POST", "pp:path": "/v1/create_knowledge_library"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "name")
			}
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/create_knowledge_library"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyDescription != "" {
				body["description"] = bodyDescription
			}
			if bodyName != "" {
				body["name"] = bodyName
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Library description")
	cmd.Flags().StringVar(&bodyName, "name", "", "Library name")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
