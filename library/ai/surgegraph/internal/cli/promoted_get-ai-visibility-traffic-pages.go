// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGetAiVisibilityTrafficPagesPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyEndDate string
	var bodyPage float64
	var bodyPageSize float64
	var bodyProjectId string
	var bodySearch string
	var bodySortBy string
	var bodySortDir string
	var bodyStartDate string

	cmd := &cobra.Command{
		Use:         "get-ai-visibility-traffic-pages",
		Short:       "Retrieve paginated list of pages with AI traffic data. Returns per-page AI/human visits, CTR, crawl errors, top...",
		Long:        "Shortcut for 'get-ai-visibility-traffic-pages get_ai_visibility_traffic_pages'. Retrieve paginated list of pages with AI traffic data. Returns per-page AI/human visits, CTR, crawl errors, top...",
		Example:     "  surgegraph-pp-cli get-ai-visibility-traffic-pages --end-date 2026-01-15",
		Annotations: map[string]string{"pp:endpoint": "get-ai-visibility-traffic-pages.get_ai_visibility_traffic_pages", "pp:method": "POST", "pp:path": "/v1/get_ai_visibility_traffic_pages"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("end-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "end-date")
			}
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("start-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "start-date")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/get_ai_visibility_traffic_pages"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyEndDate != "" {
				body["endDate"] = bodyEndDate
			}
			if bodyPage != 0.0 {
				body["page"] = bodyPage
			}
			if bodyPageSize != 0.0 {
				body["pageSize"] = bodyPageSize
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodySearch != "" {
				body["search"] = bodySearch
			}
			if bodySortBy != "" {
				body["sortBy"] = bodySortBy
			}
			if bodySortDir != "" {
				body["sortDir"] = bodySortDir
			}
			if bodyStartDate != "" {
				body["startDate"] = bodyStartDate
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "End date (ISO 8601)")
	cmd.Flags().Float64Var(&bodyPage, "page", 0.000000, "Page number (default 0)")
	cmd.Flags().Float64Var(&bodyPageSize, "page-size", 15.000000, "Page size (default 15, max 50)")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().StringVar(&bodySearch, "search", "", "URL substring search")
	cmd.Flags().StringVar(&bodySortBy, "sort-by", "", "Column to sort by")
	cmd.Flags().StringVar(&bodySortDir, "sort-dir", "desc", "Sort direction (default desc)")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "Start date (ISO 8601)")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
