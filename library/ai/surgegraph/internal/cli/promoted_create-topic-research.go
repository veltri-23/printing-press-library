// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCreateTopicResearchPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyLanguageCode string
	var bodyLocationCode float64
	var bodyProjectId string
	var bodyTopic string

	cmd := &cobra.Command{
		Use:         "create-topic-research",
		Short:       "Start a topic research for topic coverage analysis. Analyzes a specific topic to discover subtopics, related...",
		Long:        "Shortcut for 'create-topic-research create_topic_research'. Start a topic research for topic coverage analysis. Analyzes a specific topic to discover subtopics, related...",
		Example:     "  surgegraph-pp-cli create-topic-research --language-code example-value",
		Annotations: map[string]string{"pp:endpoint": "create-topic-research.create_topic_research", "pp:method": "POST", "pp:path": "/v1/create_topic_research"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("topic") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "topic")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/create_topic_research"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyLanguageCode != "" {
				body["languageCode"] = bodyLanguageCode
			}
			if bodyLocationCode != 0.0 {
				body["locationCode"] = bodyLocationCode
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodyTopic != "" {
				body["topic"] = bodyTopic
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyLanguageCode, "language-code", "en", "Language code (default: en). Use get_languages to discover valid values.")
	cmd.Flags().Float64Var(&bodyLocationCode, "location-code", 2840.000000, "Location code (default: 2840 for US). Use get_locations")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().StringVar(&bodyTopic, "topic", "", "Topic to research (e.g. 'content marketing strategy')")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
