// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGetAiVisibilityPromptDetailPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyDate string
	var bodyProjectId string
	var bodyPromptId string

	cmd := &cobra.Command{
		Use:         "get-ai-visibility-prompt-detail",
		Short:       "Deep dive into a single prompt on a specific date. Returns full brand performance (visibility, position, share,...",
		Long:        "Shortcut for 'get-ai-visibility-prompt-detail get_ai_visibility_prompt_detail'. Deep dive into a single prompt on a specific date. Returns full brand performance (visibility, position, share,...",
		Example:     "  surgegraph-pp-cli get-ai-visibility-prompt-detail --date 2026-01-15",
		Annotations: map[string]string{"pp:endpoint": "get-ai-visibility-prompt-detail.get_ai_visibility_prompt_detail", "pp:method": "POST", "pp:path": "/v1/get_ai_visibility_prompt_detail"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "date")
			}
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("prompt-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "prompt-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/get_ai_visibility_prompt_detail"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyDate != "" {
				body["date"] = bodyDate
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodyPromptId != "" {
				body["promptId"] = bodyPromptId
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyDate, "date", "", "Date to query (ISO 8601 date, e.g. 2026-04-10)")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().StringVar(&bodyPromptId, "prompt-id", "", "Prompt ID (from get_ai_visibility_prompts)")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
