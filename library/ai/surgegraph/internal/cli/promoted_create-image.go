// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCreateImagePromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAspectRatio string
	var bodyImageTypeId string
	var bodyIsFeaturedImage bool
	var bodyNegativePrompt string
	var bodyProjectId string
	var bodyPrompt string
	var bodyStyleId string
	var bodyTextSensitive bool

	cmd := &cobra.Command{
		Use:         "create-image",
		Short:       "Generate an AI image and store it in the Content Vision gallery. Use get_content_vision_settings first to see...",
		Long:        "Shortcut for 'create-image create_image'. Generate an AI image and store it in the Content Vision gallery. Use get_content_vision_settings first to see...",
		Example:     "  surgegraph-pp-cli create-image --aspect-ratio example-value",
		Annotations: map[string]string{"pp:endpoint": "create-image.create_image", "pp:method": "POST", "pp:path": "/v1/create_image"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("image-type-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "image-type-id")
			}
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("prompt") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "prompt")
			}
			if !cmd.Flags().Changed("style-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "style-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/create_image"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyAspectRatio != "" {
				body["aspectRatio"] = bodyAspectRatio
			}
			if bodyImageTypeId != "" {
				body["imageTypeId"] = bodyImageTypeId
			}
			if bodyIsFeaturedImage != false {
				body["isFeaturedImage"] = bodyIsFeaturedImage
			}
			if bodyNegativePrompt != "" {
				body["negativePrompt"] = bodyNegativePrompt
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodyPrompt != "" {
				body["prompt"] = bodyPrompt
			}
			if bodyStyleId != "" {
				body["styleId"] = bodyStyleId
			}
			if bodyTextSensitive != false {
				body["textSensitive"] = bodyTextSensitive
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAspectRatio, "aspect-ratio", "4:3", "Image aspect ratio (default: 4:3 landscape)")
	cmd.Flags().StringVar(&bodyImageTypeId, "image-type-id", "", "Image type ID (e.g. infographic, photography). Use get_content_vision_settings to see available types")
	cmd.Flags().BoolVar(&bodyIsFeaturedImage, "is-featured-image", false, "Whether this is a featured/hero image")
	cmd.Flags().StringVar(&bodyNegativePrompt, "negative-prompt", "", "What NOT to include in the image")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().StringVar(&bodyPrompt, "prompt", "", "Image generation prompt describing what to create")
	cmd.Flags().StringVar(&bodyStyleId, "style-id", "", "Style ID for the image. Use get_content_vision_settings to see available styles per type")
	cmd.Flags().BoolVar(&bodyTextSensitive, "text-sensitive", false, "Whether to be text-sensitive in generation")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
