// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCreateOptimizedDocumentPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyIntent string
	var bodyLanguageCode string
	var bodyLocationCode float64
	var bodyModel string
	var bodyProjectId string
	var bodyPrompt string
	var bodyTitle string
	var bodyUrl string
	var bodyWpPostId float64

	cmd := &cobra.Command{
		Use:         "create-optimized-document",
		Short:       "Create a content-optimized document from an existing WordPress post. The system fetches the post content via...",
		Long:        "Shortcut for 'create-optimized-document create_optimized_document'. Create a content-optimized document from an existing WordPress post. The system fetches the post content via...",
		Example:     "  surgegraph-pp-cli create-optimized-document --intent example-value",
		Annotations: map[string]string{"pp:endpoint": "create-optimized-document.create_optimized_document", "pp:method": "POST", "pp:path": "/v1/create_optimized_document"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("prompt") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "prompt")
			}
			if !cmd.Flags().Changed("wp-post-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "wp-post-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/create_optimized_document"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyIntent != "" {
				body["intent"] = bodyIntent
			}
			if bodyLanguageCode != "" {
				body["languageCode"] = bodyLanguageCode
			}
			if bodyLocationCode != 0.0 {
				body["locationCode"] = bodyLocationCode
			}
			if bodyModel != "" {
				body["model"] = bodyModel
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodyPrompt != "" {
				body["prompt"] = bodyPrompt
			}
			if bodyTitle != "" {
				body["title"] = bodyTitle
			}
			if bodyUrl != "" {
				body["url"] = bodyUrl
			}
			if bodyWpPostId != 0.0 {
				body["wpPostId"] = bodyWpPostId
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyIntent, "intent", "informational", "Search intent")
	cmd.Flags().StringVar(&bodyLanguageCode, "language-code", "en", "Language code (default: en). Use get_languages to discover valid values.")
	cmd.Flags().Float64Var(&bodyLocationCode, "location-code", 2840.000000, "Location code (default: 2840 for US)")
	cmd.Flags().StringVar(&bodyModel, "model", "gpt-5-mini", "AI model ID")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().StringVar(&bodyPrompt, "prompt", "", "Optimization target keyword/topic")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Custom title (optional)")
	cmd.Flags().StringVar(&bodyUrl, "url", "", "URL of the content to optimize. If omitted, the URL is retrieved from the WordPress post.")
	cmd.Flags().Float64Var(&bodyWpPostId, "wp-post-id", 0.0, "WordPress post ID for the content being optimized")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
