// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGetAiVisibilityEmergingTopicsPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyEndDate string
	var bodyLimit float64
	var bodyProjectId string
	var bodySelectedModelIds string
	var bodyStartDate string

	cmd := &cobra.Command{
		Use:         "get-ai-visibility-emerging-topics",
		Short:       "Retrieve emerging topics from AI Visibility tracking. Returns topics whose mention frequency is rising across AI...",
		Long:        "Shortcut for 'get-ai-visibility-emerging-topics get_ai_visibility_emerging_topics'. Retrieve emerging topics from AI Visibility tracking. Returns topics whose mention frequency is rising across AI...",
		Example:     "  surgegraph-pp-cli get-ai-visibility-emerging-topics --end-date 2026-01-15",
		Annotations: map[string]string{"pp:endpoint": "get-ai-visibility-emerging-topics.get_ai_visibility_emerging_topics", "pp:method": "POST", "pp:path": "/v1/get_ai_visibility_emerging_topics"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("end-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "end-date")
			}
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("start-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "start-date")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/get_ai_visibility_emerging_topics"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyEndDate != "" {
				body["endDate"] = bodyEndDate
			}
			if bodyLimit != 0.0 {
				body["limit"] = bodyLimit
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodySelectedModelIds != "" {
				var parsedSelectedModelIds any
				if err := json.Unmarshal([]byte(bodySelectedModelIds), &parsedSelectedModelIds); err != nil {
					return fmt.Errorf("parsing --selected-model-ids JSON: %w", err)
				}
				body["selectedModelIds"] = parsedSelectedModelIds
			}
			if bodyStartDate != "" {
				body["startDate"] = bodyStartDate
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "End date (ISO 8601)")
	cmd.Flags().Float64Var(&bodyLimit, "limit", 10.000000, "Max emerging topics returned (default 10)")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().StringVar(&bodySelectedModelIds, "selected-model-ids", "", "Filter to specific engine IDs")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "Start date (ISO 8601)")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
