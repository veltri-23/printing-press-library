// Copyright 2026 SurgeGraph Team and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCreateAiVisibilityPromptPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyIntent string
	var bodyProjectId string
	var bodyPrompt string
	var bodyTags string
	var bodyTopicName string

	cmd := &cobra.Command{
		Use:         "create-ai-visibility-prompt",
		Short:       "Create a new prompt to AI Visibility tracking. Prompts are tracked across AI answer engines to monitor brand...",
		Long:        "Shortcut for 'create-ai-visibility-prompt create_ai_visibility_prompt'. Create a new prompt to AI Visibility tracking. Prompts are tracked across AI answer engines to monitor brand...",
		Example:     "  surgegraph-pp-cli create-ai-visibility-prompt --intent example-value",
		Annotations: map[string]string{"pp:endpoint": "create-ai-visibility-prompt.create_ai_visibility_prompt", "pp:method": "POST", "pp:path": "/v1/create_ai_visibility_prompt"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("project-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "project-id")
			}
			if !cmd.Flags().Changed("prompt") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "prompt")
			}
			if !cmd.Flags().Changed("topic-name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "topic-name")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/create_ai_visibility_prompt"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyIntent != "" {
				body["intent"] = bodyIntent
			}
			if bodyProjectId != "" {
				body["projectId"] = bodyProjectId
			}
			if bodyPrompt != "" {
				body["prompt"] = bodyPrompt
			}
			if bodyTags != "" {
				var parsedTags any
				if err := json.Unmarshal([]byte(bodyTags), &parsedTags); err != nil {
					return fmt.Errorf("parsing --tags JSON: %w", err)
				}
				body["tags"] = parsedTags
			}
			if bodyTopicName != "" {
				body["topicName"] = bodyTopicName
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyIntent, "intent", "commercial", "Prompt intent")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project ID")
	cmd.Flags().StringVar(&bodyPrompt, "prompt", "", "The prompt text to track (e.g. 'What is the best SEO tool?')")
	cmd.Flags().StringVar(&bodyTags, "tags", "[]", "Tag names to assign. Tags will be created if they do not exist. Tag names are lowercased on write and lookup is...")
	cmd.Flags().StringVar(&bodyTopicName, "topic-name", "", "Topic name to associate the prompt with. Will be created if it does not exist.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
