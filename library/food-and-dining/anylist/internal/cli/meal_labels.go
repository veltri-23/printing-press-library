// Copyright 2026 Jeeves and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newMealLabelsCmd(flags *rootFlags) *cobra.Command {
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "labels",
		Short:       "List meal plan labels (Breakfast, Lunch, Dinner, Snack, etc.)",
		Example:     "  anylist-pp-cli meal labels",
		Annotations: map[string]string{"pp:endpoint": "meal.labels", "pp:method": "POST", "pp:path": "/data/user-data/get"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if stdinBody {
				if _, err := readStdinJSONMap(); err != nil {
					return err
				}
			}
			if dryRunOK(flags) {
				return nil
			}
			_, st, err := openLocalStore(flags)
			if err != nil {
				return err
			}
			defer st.Close()

			labels, err := st.GetCalendarLabels()
			if err != nil {
				return fmt.Errorf("reading meal labels: %w", err)
			}
			if flags.asJSON {
				return printJSONFiltered(cmd.OutOrStdout(), labels, flags)
			}
			tw := newTabWriter(cmd.OutOrStdout())
			fmt.Fprintln(tw, "NAME\tCOLOR")
			for _, l := range labels {
				fmt.Fprintf(tw, "%s\t%s\n", l.Name, l.HexColor)
			}
			return tw.Flush()
		},
	}
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
