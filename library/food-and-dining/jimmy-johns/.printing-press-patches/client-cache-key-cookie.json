{
  "schema_version": 2,
  "id": "client-cache-key-cookie",
  "applied_at": "2026-05-11",
  "base_run_id": "20260511-034611",
  "base_printing_press_version": "4.2.2",
  "files": [
    "internal/client/client.go"
  ],
  "summary": "Hash cfg.Headers[\"Cookie\"] into the response cache key alongside AuthHeader() bearer tokens.",
  "reason": "AuthHeader() returns \"\" for the documented import-cookies flow, so two distinct sessions that share a config path produced an identical cache key — meaning authenticated GET responses (account, rewards, order) could leak across sessions for the cache TTL. Found by codex review."
}
