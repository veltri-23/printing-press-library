// Copyright 2026 Matt Van Horn and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"
)

func newOrdersCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "orders",
		Short: "Order history, detail, validation, and tracking - the core ordering data path",
	}

	cmd.AddCommand(newOrdersCancelCmd(flags))
	cmd.AddCommand(newOrdersListCmd(flags))
	cmd.AddCommand(newOrdersShowCmd(flags))
	cmd.AddCommand(newOrdersTrackCmd(flags))
	cmd.AddCommand(newOrdersValidateCmd(flags))
	// PATCH: Local cart planning and chromedp checkout commands for agent-safe
	// order reuse flows.
	cmd.AddCommand(newOrdersPlanCmd(flags))
	cmd.AddCommand(newOrdersPlaceCmd(flags))
	return cmd
}
