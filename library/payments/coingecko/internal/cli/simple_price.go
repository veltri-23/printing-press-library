// Copyright 2026 Hiten Shah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSimplePriceCmd(flags *rootFlags) *cobra.Command {
	var flagIds string
	var flagVsCurrencies string
	var flagIncludeMarketCap string
	var flagInclude24hrVol string
	var flagInclude24hrChange string
	var flagIncludeLastUpdatedAt string

	cmd := &cobra.Command{
		Use:         "price",
		Aliases:     []string{"list"},
		Short:       "Get price of coins",
		Example:     "  coingecko-pp-cli simple price",
		Annotations: map[string]string{"pp:endpoint": "simple.price", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("ids") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "ids")
			}
			if !cmd.Flags().Changed("vs-currencies") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "vs-currencies")
			}
			if cmd.Flags().Changed("include-market-cap") {
				allowedIncludeMarketCap := []string{"true", "false"}
				validIncludeMarketCap := false
				for _, v := range allowedIncludeMarketCap {
					if flagIncludeMarketCap == v {
						validIncludeMarketCap = true
						break
					}
				}
				if !validIncludeMarketCap {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "include-market-cap", flagIncludeMarketCap, allowedIncludeMarketCap)
				}
			}
			if cmd.Flags().Changed("include-24hr-vol") {
				allowedInclude24hrVol := []string{"true", "false"}
				validInclude24hrVol := false
				for _, v := range allowedInclude24hrVol {
					if flagInclude24hrVol == v {
						validInclude24hrVol = true
						break
					}
				}
				if !validInclude24hrVol {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "include-24hr-vol", flagInclude24hrVol, allowedInclude24hrVol)
				}
			}
			if cmd.Flags().Changed("include-24hr-change") {
				allowedInclude24hrChange := []string{"true", "false"}
				validInclude24hrChange := false
				for _, v := range allowedInclude24hrChange {
					if flagInclude24hrChange == v {
						validInclude24hrChange = true
						break
					}
				}
				if !validInclude24hrChange {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "include-24hr-change", flagInclude24hrChange, allowedInclude24hrChange)
				}
			}
			if cmd.Flags().Changed("include-last-updated-at") {
				allowedIncludeLastUpdatedAt := []string{"true", "false"}
				validIncludeLastUpdatedAt := false
				for _, v := range allowedIncludeLastUpdatedAt {
					if flagIncludeLastUpdatedAt == v {
						validIncludeLastUpdatedAt = true
						break
					}
				}
				if !validIncludeLastUpdatedAt {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "include-last-updated-at", flagIncludeLastUpdatedAt, allowedIncludeLastUpdatedAt)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/simple/price"
			params := map[string]string{}
			if flagIds != "" {
				params["ids"] = fmt.Sprintf("%v", flagIds)
			}
			if flagVsCurrencies != "" {
				params["vs_currencies"] = fmt.Sprintf("%v", flagVsCurrencies)
			}
			if flagIncludeMarketCap != "" {
				params["include_market_cap"] = fmt.Sprintf("%v", flagIncludeMarketCap)
			}
			if flagInclude24hrVol != "" {
				params["include_24hr_vol"] = fmt.Sprintf("%v", flagInclude24hrVol)
			}
			if flagInclude24hrChange != "" {
				params["include_24hr_change"] = fmt.Sprintf("%v", flagInclude24hrChange)
			}
			if flagIncludeLastUpdatedAt != "" {
				params["include_last_updated_at"] = fmt.Sprintf("%v", flagIncludeLastUpdatedAt)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "simple", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagIds, "ids", "", "Comma-separated coin ids (e.g. bitcoin,ethereum)")
	cmd.Flags().StringVar(&flagVsCurrencies, "vs-currencies", "", "Comma-separated target currencies (e.g. usd,eur)")
	cmd.Flags().StringVar(&flagIncludeMarketCap, "include-market-cap", "", "Include market cap (one of: true, false)")
	cmd.Flags().StringVar(&flagInclude24hrVol, "include-24hr-vol", "", "Include 24hr vol (one of: true, false)")
	cmd.Flags().StringVar(&flagInclude24hrChange, "include-24hr-change", "", "Include 24hr change (one of: true, false)")
	cmd.Flags().StringVar(&flagIncludeLastUpdatedAt, "include-last-updated-at", "", "Include last updated at (one of: true, false)")

	return cmd
}
