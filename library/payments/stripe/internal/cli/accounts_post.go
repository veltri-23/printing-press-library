// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsPostCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountToken string
	var bodyBusinessProfile string
	var bodyBusinessType string
	var bodyCapabilities string
	var bodyCompany string
	var bodyController string
	var bodyCountry string
	var bodyDefaultCurrency string
	var bodyDocuments string
	var bodyEmail string
	var bodyExpand string
	var bodyExternalAccount string
	var bodyGroups string
	var bodyIndividual string
	var bodyMetadata string
	var bodySettings string
	var bodyTosAcceptance string
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post",
		Aliases:     []string{"create"},
		Short:       "<p>With <a href='/docs/connect'>Connect</a>, you can create Stripe accounts for your users. To do this, you’ll...",
		Example:     "  stripe-pp-cli accounts post",
		Annotations: map[string]string{"pp:endpoint": "accounts.post", "pp:method": "POST", "pp:path": "/v1/accounts"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/accounts"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccountToken != "" {
					body["account_token"] = bodyAccountToken
				}
				if bodyBusinessProfile != "" {
					var parsedBusinessProfile any
					if err := json.Unmarshal([]byte(bodyBusinessProfile), &parsedBusinessProfile); err != nil {
						return fmt.Errorf("parsing --business-profile JSON: %w", err)
					}
					body["business_profile"] = parsedBusinessProfile
				}
				if bodyBusinessType != "" {
					body["business_type"] = bodyBusinessType
				}
				if bodyCapabilities != "" {
					var parsedCapabilities any
					if err := json.Unmarshal([]byte(bodyCapabilities), &parsedCapabilities); err != nil {
						return fmt.Errorf("parsing --capabilities JSON: %w", err)
					}
					body["capabilities"] = parsedCapabilities
				}
				if bodyCompany != "" {
					var parsedCompany any
					if err := json.Unmarshal([]byte(bodyCompany), &parsedCompany); err != nil {
						return fmt.Errorf("parsing --company JSON: %w", err)
					}
					body["company"] = parsedCompany
				}
				if bodyController != "" {
					var parsedController any
					if err := json.Unmarshal([]byte(bodyController), &parsedController); err != nil {
						return fmt.Errorf("parsing --controller JSON: %w", err)
					}
					body["controller"] = parsedController
				}
				if bodyCountry != "" {
					body["country"] = bodyCountry
				}
				if bodyDefaultCurrency != "" {
					body["default_currency"] = bodyDefaultCurrency
				}
				if bodyDocuments != "" {
					var parsedDocuments any
					if err := json.Unmarshal([]byte(bodyDocuments), &parsedDocuments); err != nil {
						return fmt.Errorf("parsing --documents JSON: %w", err)
					}
					body["documents"] = parsedDocuments
				}
				if bodyEmail != "" {
					body["email"] = bodyEmail
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyExternalAccount != "" {
					body["external_account"] = bodyExternalAccount
				}
				if bodyGroups != "" {
					var parsedGroups any
					if err := json.Unmarshal([]byte(bodyGroups), &parsedGroups); err != nil {
						return fmt.Errorf("parsing --groups JSON: %w", err)
					}
					body["groups"] = parsedGroups
				}
				if bodyIndividual != "" {
					var parsedIndividual any
					if err := json.Unmarshal([]byte(bodyIndividual), &parsedIndividual); err != nil {
						return fmt.Errorf("parsing --individual JSON: %w", err)
					}
					body["individual"] = parsedIndividual
				}
				if bodyMetadata != "" {
					body["metadata"] = bodyMetadata
				}
				if bodySettings != "" {
					var parsedSettings any
					if err := json.Unmarshal([]byte(bodySettings), &parsedSettings); err != nil {
						return fmt.Errorf("parsing --settings JSON: %w", err)
					}
					body["settings"] = parsedSettings
				}
				if bodyTosAcceptance != "" {
					var parsedTosAcceptance any
					if err := json.Unmarshal([]byte(bodyTosAcceptance), &parsedTosAcceptance); err != nil {
						return fmt.Errorf("parsing --tos-acceptance JSON: %w", err)
					}
					body["tos_acceptance"] = parsedTosAcceptance
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "accounts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAccountToken, "account-token", "", "An [account token](https://api.stripe.com#create_account_token), used to securely provide details to the account.")
	cmd.Flags().StringVar(&bodyBusinessProfile, "business-profile", "", "Business information about the account.")
	cmd.Flags().StringVar(&bodyBusinessType, "business-type", "", "The business type. Once you create an [Account Link](/api/account_links) or [Account...")
	cmd.Flags().StringVar(&bodyCapabilities, "capabilities", "", "Each key of the dictionary represents a capability, and each capability maps to its settings (for example, whether...")
	cmd.Flags().StringVar(&bodyCompany, "company", "", "Information about the company or business. This field is available for any `business_type`. Once you create an...")
	cmd.Flags().StringVar(&bodyController, "controller", "", "A hash of configuration describing the account controller's attributes.")
	cmd.Flags().StringVar(&bodyCountry, "country", "", "The country in which the account holder resides, or in which the business is legally established. This should be an...")
	cmd.Flags().StringVar(&bodyDefaultCurrency, "default-currency", "", "Three-letter ISO currency code representing the default currency for the account. This must be a currency that...")
	cmd.Flags().StringVar(&bodyDocuments, "documents", "", "Documents that may be submitted to satisfy various informational requests.")
	cmd.Flags().StringVar(&bodyEmail, "email", "", "The email address of the account holder. This is only to make the account easier to identify to you. If...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyExternalAccount, "external-account", "", "A card or bank account to attach to the account for receiving [payouts](/connect/bank-debit-card-payouts) (you...")
	cmd.Flags().StringVar(&bodyGroups, "groups", "", "A hash of account group type to tokens. These are account groups this account should be added to.")
	cmd.Flags().StringVar(&bodyIndividual, "individual", "", "Information about the person represented by the account. This field is null unless `business_type` is set to...")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodySettings, "settings", "", "Options for customizing how the account functions within Stripe.")
	cmd.Flags().StringVar(&bodyTosAcceptance, "tos-acceptance", "", "Details on the account's acceptance of the [Stripe Services Agreement](/connect/updating-accounts#tos-acceptance)....")
	cmd.Flags().StringVar(&bodyType, "type", "", "The `type` parameter is deprecated. Use [`controller`](/api/accounts/create#create_account-controller) instead to...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
