// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSetupIntentsPostIntentCmd(flags *rootFlags) *cobra.Command {
	var bodyAttachToSelf bool
	var bodyCustomer string
	var bodyCustomerAccount string
	var bodyDescription string
	var bodyExcludedPaymentMethodTypes string
	var bodyExpand string
	var bodyFlowDirections string
	var bodyMetadata string
	var bodyPaymentMethod string
	var bodyPaymentMethodConfiguration string
	var bodyPaymentMethodData string
	var bodyPaymentMethodOptions string
	var bodyPaymentMethodTypes string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-intent <intent>",
		Short:       "<p>Updates a SetupIntent object.</p>",
		Example:     "  stripe-pp-cli setup-intents post-intent example-value",
		Annotations: map[string]string{"pp:endpoint": "setup-intents.post-intent", "pp:method": "POST", "pp:path": "/v1/setup_intents/{intent}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/setup_intents/{intent}"
			path = replacePathParam(path, "intent", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAttachToSelf != false {
					body["attach_to_self"] = bodyAttachToSelf
				}
				if bodyCustomer != "" {
					body["customer"] = bodyCustomer
				}
				if bodyCustomerAccount != "" {
					body["customer_account"] = bodyCustomerAccount
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyExcludedPaymentMethodTypes != "" {
					body["excluded_payment_method_types"] = bodyExcludedPaymentMethodTypes
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyFlowDirections != "" {
					var parsedFlowDirections any
					if err := json.Unmarshal([]byte(bodyFlowDirections), &parsedFlowDirections); err != nil {
						return fmt.Errorf("parsing --flow-directions JSON: %w", err)
					}
					body["flow_directions"] = parsedFlowDirections
				}
				if bodyMetadata != "" {
					body["metadata"] = bodyMetadata
				}
				if bodyPaymentMethod != "" {
					body["payment_method"] = bodyPaymentMethod
				}
				if bodyPaymentMethodConfiguration != "" {
					body["payment_method_configuration"] = bodyPaymentMethodConfiguration
				}
				if bodyPaymentMethodData != "" {
					var parsedPaymentMethodData any
					if err := json.Unmarshal([]byte(bodyPaymentMethodData), &parsedPaymentMethodData); err != nil {
						return fmt.Errorf("parsing --payment-method-data JSON: %w", err)
					}
					body["payment_method_data"] = parsedPaymentMethodData
				}
				if bodyPaymentMethodOptions != "" {
					var parsedPaymentMethodOptions any
					if err := json.Unmarshal([]byte(bodyPaymentMethodOptions), &parsedPaymentMethodOptions); err != nil {
						return fmt.Errorf("parsing --payment-method-options JSON: %w", err)
					}
					body["payment_method_options"] = parsedPaymentMethodOptions
				}
				if bodyPaymentMethodTypes != "" {
					var parsedPaymentMethodTypes any
					if err := json.Unmarshal([]byte(bodyPaymentMethodTypes), &parsedPaymentMethodTypes); err != nil {
						return fmt.Errorf("parsing --payment-method-types JSON: %w", err)
					}
					body["payment_method_types"] = parsedPaymentMethodTypes
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "setup-intents",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAttachToSelf, "attach-to-self", false, "If present, the SetupIntent's payment method will be attached to the in-context Stripe Account. It can only be used...")
	cmd.Flags().StringVar(&bodyCustomer, "customer", "", "ID of the Customer this SetupIntent belongs to, if one exists. If present, the SetupIntent's payment method will be...")
	cmd.Flags().StringVar(&bodyCustomerAccount, "customer-account", "", "ID of the Account this SetupIntent belongs to, if one exists. If present, the SetupIntent's payment method will be...")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "An arbitrary string attached to the object. Often useful for displaying to users.")
	cmd.Flags().StringVar(&bodyExcludedPaymentMethodTypes, "excluded-payment-method-types", "", "The list of payment method types to exclude from use with this SetupIntent.")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyFlowDirections, "flow-directions", "", "Indicates the directions of money movement for which this payment method is intended to be used. Include `inbound`...")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyPaymentMethod, "payment-method", "", "ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent. To unset...")
	cmd.Flags().StringVar(&bodyPaymentMethodConfiguration, "payment-method-configuration", "", "The ID of the [payment method configuration](https://docs.stripe.com/api/payment_method_configurations) to use with...")
	cmd.Flags().StringVar(&bodyPaymentMethodData, "payment-method-data", "", "When included, this hash creates a PaymentMethod that is set as the...")
	cmd.Flags().StringVar(&bodyPaymentMethodOptions, "payment-method-options", "", "Payment method-specific configuration for this SetupIntent.")
	cmd.Flags().StringVar(&bodyPaymentMethodTypes, "payment-method-types", "", "The list of payment method types (for example, card) that this SetupIntent can set up. If you don't provide this,...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
