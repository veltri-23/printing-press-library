// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTestHelpersPostIssuingAuthorizationsCmd(flags *rootFlags) *cobra.Command {
	var bodyAmount int
	var bodyAmountDetails string
	var bodyAuthorizationMethod string
	var bodyCard string
	var bodyCurrency string
	var bodyExpand string
	var bodyFleet string
	var bodyFraudDisputabilityLikelihood string
	var bodyFuel string
	var bodyIsAmountControllable bool
	var bodyMerchantAmount int
	var bodyMerchantCurrency string
	var bodyMerchantData string
	var bodyNetworkData string
	var bodyRiskAssessment string
	var bodyVerificationData string
	var bodyWallet string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-issuing-authorizations",
		Short:       "<p>Create a test-mode authorization.</p>",
		Example:     "  stripe-pp-cli test-helpers post-issuing-authorizations --card example-value",
		Annotations: map[string]string{"pp:endpoint": "test-helpers.post-issuing-authorizations", "pp:method": "POST", "pp:path": "/v1/test_helpers/issuing/authorizations"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("card") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "card")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/test_helpers/issuing/authorizations"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAmount != 0 {
					body["amount"] = bodyAmount
				}
				if bodyAmountDetails != "" {
					var parsedAmountDetails any
					if err := json.Unmarshal([]byte(bodyAmountDetails), &parsedAmountDetails); err != nil {
						return fmt.Errorf("parsing --amount-details JSON: %w", err)
					}
					body["amount_details"] = parsedAmountDetails
				}
				if bodyAuthorizationMethod != "" {
					body["authorization_method"] = bodyAuthorizationMethod
				}
				if bodyCard != "" {
					body["card"] = bodyCard
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyFleet != "" {
					var parsedFleet any
					if err := json.Unmarshal([]byte(bodyFleet), &parsedFleet); err != nil {
						return fmt.Errorf("parsing --fleet JSON: %w", err)
					}
					body["fleet"] = parsedFleet
				}
				if bodyFraudDisputabilityLikelihood != "" {
					body["fraud_disputability_likelihood"] = bodyFraudDisputabilityLikelihood
				}
				if bodyFuel != "" {
					var parsedFuel any
					if err := json.Unmarshal([]byte(bodyFuel), &parsedFuel); err != nil {
						return fmt.Errorf("parsing --fuel JSON: %w", err)
					}
					body["fuel"] = parsedFuel
				}
				if bodyIsAmountControllable != false {
					body["is_amount_controllable"] = bodyIsAmountControllable
				}
				if bodyMerchantAmount != 0 {
					body["merchant_amount"] = bodyMerchantAmount
				}
				if bodyMerchantCurrency != "" {
					body["merchant_currency"] = bodyMerchantCurrency
				}
				if bodyMerchantData != "" {
					var parsedMerchantData any
					if err := json.Unmarshal([]byte(bodyMerchantData), &parsedMerchantData); err != nil {
						return fmt.Errorf("parsing --merchant-data JSON: %w", err)
					}
					body["merchant_data"] = parsedMerchantData
				}
				if bodyNetworkData != "" {
					var parsedNetworkData any
					if err := json.Unmarshal([]byte(bodyNetworkData), &parsedNetworkData); err != nil {
						return fmt.Errorf("parsing --network-data JSON: %w", err)
					}
					body["network_data"] = parsedNetworkData
				}
				if bodyRiskAssessment != "" {
					var parsedRiskAssessment any
					if err := json.Unmarshal([]byte(bodyRiskAssessment), &parsedRiskAssessment); err != nil {
						return fmt.Errorf("parsing --risk-assessment JSON: %w", err)
					}
					body["risk_assessment"] = parsedRiskAssessment
				}
				if bodyVerificationData != "" {
					var parsedVerificationData any
					if err := json.Unmarshal([]byte(bodyVerificationData), &parsedVerificationData); err != nil {
						return fmt.Errorf("parsing --verification-data JSON: %w", err)
					}
					body["verification_data"] = parsedVerificationData
				}
				if bodyWallet != "" {
					body["wallet"] = bodyWallet
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "test-helpers",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyAmount, "amount", 0, "The total amount to attempt to authorize. This amount is in the provided currency, or defaults to the card's...")
	cmd.Flags().StringVar(&bodyAmountDetails, "amount-details", "", "Detailed breakdown of amount components. These amounts are denominated in `currency` and in the [smallest currency...")
	cmd.Flags().StringVar(&bodyAuthorizationMethod, "authorization-method", "", "How the card details were provided. Defaults to online.")
	cmd.Flags().StringVar(&bodyCard, "card", "", "Card associated with this authorization.")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "The currency of the authorization. If not provided, defaults to the currency of the card. Three-letter [ISO currency...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyFleet, "fleet", "", "Fleet-specific information for authorizations using Fleet cards.")
	cmd.Flags().StringVar(&bodyFraudDisputabilityLikelihood, "fraud-disputability-likelihood", "", "Probability that this transaction can be disputed in the event of fraud. Assessed by comparing the characteristics...")
	cmd.Flags().StringVar(&bodyFuel, "fuel", "", "Information about fuel that was purchased with this transaction.")
	cmd.Flags().BoolVar(&bodyIsAmountControllable, "is-amount-controllable", false, "If set `true`, you may provide [amount](https://docs.stripe.com/api/issuing/authorizations/approve#approve_issuing_au...")
	cmd.Flags().IntVar(&bodyMerchantAmount, "merchant-amount", 0, "The total amount to attempt to authorize. This amount is in the provided merchant currency, and in the [smallest...")
	cmd.Flags().StringVar(&bodyMerchantCurrency, "merchant-currency", "", "The currency of the authorization. If not provided, defaults to the currency of the card. Three-letter [ISO currency...")
	cmd.Flags().StringVar(&bodyMerchantData, "merchant-data", "", "Details about the seller (grocery store, e-commerce website, etc.) where the card authorization happened.")
	cmd.Flags().StringVar(&bodyNetworkData, "network-data", "", "Details about the authorization, such as identifiers, set by the card network.")
	cmd.Flags().StringVar(&bodyRiskAssessment, "risk-assessment", "", "Stripe’s assessment of the fraud risk for this authorization.")
	cmd.Flags().StringVar(&bodyVerificationData, "verification-data", "", "Verifications that Stripe performed on information that the cardholder provided to the merchant.")
	cmd.Flags().StringVar(&bodyWallet, "wallet", "", "The digital wallet used for this transaction. One of `apple_pay`, `google_pay`, or `samsung_pay`. Will populate as...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
