// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCustomersPaymentMethodsGetCustomersCustomerCmd(flags *rootFlags) *cobra.Command {
	var flagAllowRedisplay string
	var flagEndingBefore string
	var flagExpand string
	var flagLimit int
	var flagStartingAfter string
	var flagType string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-customers-customer <customer>",
		Aliases:     []string{"get"},
		Short:       "<p>Returns a list of PaymentMethods for a given Customer</p>",
		Example:     "  stripe-pp-cli customers payment-methods get-customers-customer example-value",
		Annotations: map[string]string{"pp:endpoint": "payment-methods.get-customers-customer", "pp:method": "GET", "pp:path": "/v1/customers/{customer}/payment_methods", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("allow-redisplay") {
				allowedAllowRedisplay := []string{"always", "limited", "unspecified"}
				validAllowRedisplay := false
				for _, v := range allowedAllowRedisplay {
					if flagAllowRedisplay == v {
						validAllowRedisplay = true
						break
					}
				}
				if !validAllowRedisplay {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "allow-redisplay", flagAllowRedisplay, allowedAllowRedisplay)
				}
			}
			if cmd.Flags().Changed("type") {
				allowedType := []string{"acss_debit", "affirm", "afterpay_clearpay", "alipay", "alma", "amazon_pay", "au_becs_debit", "bacs_debit", "bancontact", "billie", "blik", "boleto", "card", "cashapp", "crypto", "custom", "customer_balance", "eps", "fpx", "giropay", "grabpay", "ideal", "kakao_pay", "klarna", "konbini", "kr_card", "link", "mb_way", "mobilepay", "multibanco", "naver_pay", "nz_bank_account", "oxxo", "p24", "pay_by_bank", "payco", "paynow", "paypal", "payto", "pix", "promptpay", "revolut_pay", "samsung_pay", "satispay", "sepa_debit", "sofort", "sunbit", "swish", "twint", "upi", "us_bank_account", "wechat_pay", "zip"}
				validType := false
				for _, v := range allowedType {
					if flagType == v {
						validType = true
						break
					}
				}
				if !validType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "type", flagType, allowedType)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/customers/{customer}/payment_methods"
			if len(args) < 2 {
				return usageErr(fmt.Errorf("customer is required\nUsage: %s <%s>", cmd.CommandPath(), "customer"))
			}
			path = replacePathParam(path, "customer", args[1])
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "payment-methods", path, map[string]string{
				"allow_redisplay": fmt.Sprintf("%v", flagAllowRedisplay),
				"ending_before":   fmt.Sprintf("%v", flagEndingBefore),
				"expand":          fmt.Sprintf("%v", flagExpand),
				"limit":           fmt.Sprintf("%v", flagLimit),
				"starting_after":  fmt.Sprintf("%v", flagStartingAfter),
				"type":            fmt.Sprintf("%v", flagType),
			}, nil, flagAll, "", "", "has_more")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAllowRedisplay, "allow-redisplay", "", "This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe... (one of: always, limited, unspecified)")
	cmd.Flags().StringVar(&flagEndingBefore, "ending-before", "", "A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance,...")
	cmd.Flags().StringVar(&flagExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.")
	cmd.Flags().StringVar(&flagStartingAfter, "starting-after", "", "A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance,...")
	cmd.Flags().StringVar(&flagType, "type", "", "An optional filter on the list, based on the object `type` field. Without the filter, the list includes all current... (one of: acss_debit, affirm, afterpay_clearpay, alipay, alma, amazon_pay, au_becs_debit, bacs_debit, bancontact, billie, blik, boleto, card, cashapp, crypto, custom, customer_balance, eps, fpx, giropay, grabpay, ideal, kakao_pay, klarna, konbini, kr_card, link, mb_way, mobilepay, multibanco, naver_pay, nz_bank_account, oxxo, p24, pay_by_bank, payco, paynow, paypal, payto, pix, promptpay, revolut_pay, samsung_pay, satispay, sepa_debit, sofort, sunbit, swish, twint, upi, us_bank_account, wechat_pay, zip)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
