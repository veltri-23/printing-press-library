// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newPaymentLinksPostPaymentlinksCmd(flags *rootFlags) *cobra.Command {
	var bodyActive bool
	var bodyAfterCompletion string
	var bodyAllowPromotionCodes bool
	var bodyAutomaticTax string
	var bodyBillingAddressCollection string
	var bodyCustomFields string
	var bodyCustomText string
	var bodyCustomerCreation string
	var bodyExpand string
	var bodyInactiveMessage string
	var bodyInvoiceCreation string
	var bodyLineItems string
	var bodyMetadata string
	var bodyNameCollection string
	var bodyOptionalItems string
	var bodyPaymentIntentData string
	var bodyPaymentMethodCollection string
	var bodyPaymentMethodTypes string
	var bodyPhoneNumberCollection string
	var bodyRestrictions string
	var bodyShippingAddressCollection string
	var bodySubmitType string
	var bodySubscriptionData string
	var bodyTaxIdCollection string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-paymentlinks <payment_link>",
		Short:       "<p>Updates a payment link.</p>",
		Example:     "  stripe-pp-cli payment-links post-paymentlinks https://example.com/resource",
		Annotations: map[string]string{"pp:endpoint": "payment-links.post-paymentlinks", "pp:method": "POST", "pp:path": "/v1/payment_links/{payment_link}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/payment_links/{payment_link}"
			path = replacePathParam(path, "payment_link", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyActive != false {
					body["active"] = bodyActive
				}
				if bodyAfterCompletion != "" {
					var parsedAfterCompletion any
					if err := json.Unmarshal([]byte(bodyAfterCompletion), &parsedAfterCompletion); err != nil {
						return fmt.Errorf("parsing --after-completion JSON: %w", err)
					}
					body["after_completion"] = parsedAfterCompletion
				}
				if bodyAllowPromotionCodes != false {
					body["allow_promotion_codes"] = bodyAllowPromotionCodes
				}
				if bodyAutomaticTax != "" {
					var parsedAutomaticTax any
					if err := json.Unmarshal([]byte(bodyAutomaticTax), &parsedAutomaticTax); err != nil {
						return fmt.Errorf("parsing --automatic-tax JSON: %w", err)
					}
					body["automatic_tax"] = parsedAutomaticTax
				}
				if bodyBillingAddressCollection != "" {
					body["billing_address_collection"] = bodyBillingAddressCollection
				}
				if bodyCustomFields != "" {
					body["custom_fields"] = bodyCustomFields
				}
				if bodyCustomText != "" {
					var parsedCustomText any
					if err := json.Unmarshal([]byte(bodyCustomText), &parsedCustomText); err != nil {
						return fmt.Errorf("parsing --custom-text JSON: %w", err)
					}
					body["custom_text"] = parsedCustomText
				}
				if bodyCustomerCreation != "" {
					body["customer_creation"] = bodyCustomerCreation
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyInactiveMessage != "" {
					body["inactive_message"] = bodyInactiveMessage
				}
				if bodyInvoiceCreation != "" {
					var parsedInvoiceCreation any
					if err := json.Unmarshal([]byte(bodyInvoiceCreation), &parsedInvoiceCreation); err != nil {
						return fmt.Errorf("parsing --invoice-creation JSON: %w", err)
					}
					body["invoice_creation"] = parsedInvoiceCreation
				}
				if bodyLineItems != "" {
					var parsedLineItems any
					if err := json.Unmarshal([]byte(bodyLineItems), &parsedLineItems); err != nil {
						return fmt.Errorf("parsing --line-items JSON: %w", err)
					}
					body["line_items"] = parsedLineItems
				}
				if bodyMetadata != "" {
					var parsedMetadata any
					if err := json.Unmarshal([]byte(bodyMetadata), &parsedMetadata); err != nil {
						return fmt.Errorf("parsing --metadata JSON: %w", err)
					}
					body["metadata"] = parsedMetadata
				}
				if bodyNameCollection != "" {
					body["name_collection"] = bodyNameCollection
				}
				if bodyOptionalItems != "" {
					body["optional_items"] = bodyOptionalItems
				}
				if bodyPaymentIntentData != "" {
					var parsedPaymentIntentData any
					if err := json.Unmarshal([]byte(bodyPaymentIntentData), &parsedPaymentIntentData); err != nil {
						return fmt.Errorf("parsing --payment-intent-data JSON: %w", err)
					}
					body["payment_intent_data"] = parsedPaymentIntentData
				}
				if bodyPaymentMethodCollection != "" {
					body["payment_method_collection"] = bodyPaymentMethodCollection
				}
				if bodyPaymentMethodTypes != "" {
					body["payment_method_types"] = bodyPaymentMethodTypes
				}
				if bodyPhoneNumberCollection != "" {
					var parsedPhoneNumberCollection any
					if err := json.Unmarshal([]byte(bodyPhoneNumberCollection), &parsedPhoneNumberCollection); err != nil {
						return fmt.Errorf("parsing --phone-number-collection JSON: %w", err)
					}
					body["phone_number_collection"] = parsedPhoneNumberCollection
				}
				if bodyRestrictions != "" {
					body["restrictions"] = bodyRestrictions
				}
				if bodyShippingAddressCollection != "" {
					body["shipping_address_collection"] = bodyShippingAddressCollection
				}
				if bodySubmitType != "" {
					body["submit_type"] = bodySubmitType
				}
				if bodySubscriptionData != "" {
					var parsedSubscriptionData any
					if err := json.Unmarshal([]byte(bodySubscriptionData), &parsedSubscriptionData); err != nil {
						return fmt.Errorf("parsing --subscription-data JSON: %w", err)
					}
					body["subscription_data"] = parsedSubscriptionData
				}
				if bodyTaxIdCollection != "" {
					var parsedTaxIdCollection any
					if err := json.Unmarshal([]byte(bodyTaxIdCollection), &parsedTaxIdCollection); err != nil {
						return fmt.Errorf("parsing --tax-id-collection JSON: %w", err)
					}
					body["tax_id_collection"] = parsedTaxIdCollection
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "payment-links",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyActive, "active", false, "Whether the payment link's `url` is active. If `false`, customers visiting the URL will be shown a page saying that...")
	cmd.Flags().StringVar(&bodyAfterCompletion, "after-completion", "", "Behavior after the purchase is complete.")
	cmd.Flags().BoolVar(&bodyAllowPromotionCodes, "allow-promotion-codes", false, "Enables user redeemable promotion codes.")
	cmd.Flags().StringVar(&bodyAutomaticTax, "automatic-tax", "", "Configuration for automatic tax collection.")
	cmd.Flags().StringVar(&bodyBillingAddressCollection, "billing-address-collection", "", "Configuration for collecting the customer's billing address. Defaults to `auto`.")
	cmd.Flags().StringVar(&bodyCustomFields, "custom-fields", "", "Collect additional information from your customer using custom fields. Up to 3 fields are supported. You can't set...")
	cmd.Flags().StringVar(&bodyCustomText, "custom-text", "", "Display additional text for your customers using custom text. You can't set this parameter if `ui_mode` is `custom`.")
	cmd.Flags().StringVar(&bodyCustomerCreation, "customer-creation", "", "Configures whether [checkout sessions](https://docs.stripe.com/api/checkout/sessions) created by this payment link...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyInactiveMessage, "inactive-message", "", "The custom message to be displayed to a customer when a payment link is no longer active.")
	cmd.Flags().StringVar(&bodyInvoiceCreation, "invoice-creation", "", "Generate a post-purchase Invoice for one-time payments.")
	cmd.Flags().StringVar(&bodyLineItems, "line-items", "", "The line items representing what is being sold. Each line item represents an item being sold. Up to 20 line items...")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyNameCollection, "name-collection", "", "Controls settings applied for collecting the customer's name.")
	cmd.Flags().StringVar(&bodyOptionalItems, "optional-items", "", "A list of optional items the customer can add to their order at checkout. Use this parameter to pass one-time or...")
	cmd.Flags().StringVar(&bodyPaymentIntentData, "payment-intent-data", "", "A subset of parameters to be passed to PaymentIntent creation for Checkout Sessions in `payment` mode.")
	cmd.Flags().StringVar(&bodyPaymentMethodCollection, "payment-method-collection", "", "Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a...")
	cmd.Flags().StringVar(&bodyPaymentMethodTypes, "payment-method-types", "", "The list of payment method types that customers can use. Pass an empty string to enable dynamic payment methods that...")
	cmd.Flags().StringVar(&bodyPhoneNumberCollection, "phone-number-collection", "", "Controls phone number collection settings during checkout. We recommend that you review your privacy policy and...")
	cmd.Flags().StringVar(&bodyRestrictions, "restrictions", "", "Settings that restrict the usage of a payment link.")
	cmd.Flags().StringVar(&bodyShippingAddressCollection, "shipping-address-collection", "", "Configuration for collecting the customer's shipping address.")
	cmd.Flags().StringVar(&bodySubmitType, "submit-type", "", "Describes the type of transaction being performed in order to customize relevant text on the page, such as the...")
	cmd.Flags().StringVar(&bodySubscriptionData, "subscription-data", "", "When creating a subscription, the specified configuration data will be used. There must be at least one line item...")
	cmd.Flags().StringVar(&bodyTaxIdCollection, "tax-id-collection", "", "Controls tax ID collection during checkout.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
