// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTerminalPostLocationsLocationCmd(flags *rootFlags) *cobra.Command {
	var bodyAddress string
	var bodyAddressKana string
	var bodyAddressKanji string
	var bodyConfigurationOverrides string
	var bodyDisplayName string
	var bodyDisplayNameKana string
	var bodyDisplayNameKanji string
	var bodyExpand string
	var bodyMetadata string
	var bodyPhone string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-locations-location <location>",
		Short:       "<p>Updates a <code>Location</code> object by setting the values of the parameters passed. Any parameters not...",
		Example:     "  stripe-pp-cli terminal post-locations-location example-value",
		Annotations: map[string]string{"pp:endpoint": "terminal.post-locations-location", "pp:method": "POST", "pp:path": "/v1/terminal/locations/{location}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/terminal/locations/{location}"
			path = replacePathParam(path, "location", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAddress != "" {
					var parsedAddress any
					if err := json.Unmarshal([]byte(bodyAddress), &parsedAddress); err != nil {
						return fmt.Errorf("parsing --address JSON: %w", err)
					}
					body["address"] = parsedAddress
				}
				if bodyAddressKana != "" {
					var parsedAddressKana any
					if err := json.Unmarshal([]byte(bodyAddressKana), &parsedAddressKana); err != nil {
						return fmt.Errorf("parsing --address-kana JSON: %w", err)
					}
					body["address_kana"] = parsedAddressKana
				}
				if bodyAddressKanji != "" {
					var parsedAddressKanji any
					if err := json.Unmarshal([]byte(bodyAddressKanji), &parsedAddressKanji); err != nil {
						return fmt.Errorf("parsing --address-kanji JSON: %w", err)
					}
					body["address_kanji"] = parsedAddressKanji
				}
				if bodyConfigurationOverrides != "" {
					body["configuration_overrides"] = bodyConfigurationOverrides
				}
				if bodyDisplayName != "" {
					body["display_name"] = bodyDisplayName
				}
				if bodyDisplayNameKana != "" {
					body["display_name_kana"] = bodyDisplayNameKana
				}
				if bodyDisplayNameKanji != "" {
					body["display_name_kanji"] = bodyDisplayNameKanji
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyMetadata != "" {
					body["metadata"] = bodyMetadata
				}
				if bodyPhone != "" {
					body["phone"] = bodyPhone
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "terminal",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAddress, "address", "", "The full address of the location. You can't change the location's `country`. If you need to modify the `country`...")
	cmd.Flags().StringVar(&bodyAddressKana, "address-kana", "", "The Kana variation of the full address of the location (Japan only).")
	cmd.Flags().StringVar(&bodyAddressKanji, "address-kanji", "", "The Kanji variation of the full address of the location (Japan only).")
	cmd.Flags().StringVar(&bodyConfigurationOverrides, "configuration-overrides", "", "The ID of a configuration that will be used to customize all readers in this location.")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "A name for the location.")
	cmd.Flags().StringVar(&bodyDisplayNameKana, "display-name-kana", "", "The Kana variation of the name for the location (Japan only).")
	cmd.Flags().StringVar(&bodyDisplayNameKanji, "display-name-kanji", "", "The Kanji variation of the name for the location (Japan only).")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyPhone, "phone", "", "The phone number for the location.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
