// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTreasuryPostFinancialAccountsFinancialAccountFeaturesCmd(flags *rootFlags) *cobra.Command {
	var bodyCardIssuing string
	var bodyDepositInsurance string
	var bodyExpand string
	var bodyFinancialAddresses string
	var bodyInboundTransfers string
	var bodyIntraStripeFlows string
	var bodyOutboundPayments string
	var bodyOutboundTransfers string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-financial-accounts-financial-account-features <financial_account>",
		Short:       "<p>Updates the Features associated with a FinancialAccount.</p>",
		Example:     "  stripe-pp-cli treasury post-financial-accounts-financial-account-features example-value",
		Annotations: map[string]string{"pp:endpoint": "treasury.post-financial-accounts-financial-account-features", "pp:method": "POST", "pp:path": "/v1/treasury/financial_accounts/{financial_account}/features"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/treasury/financial_accounts/{financial_account}/features"
			path = replacePathParam(path, "financial_account", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCardIssuing != "" {
					var parsedCardIssuing any
					if err := json.Unmarshal([]byte(bodyCardIssuing), &parsedCardIssuing); err != nil {
						return fmt.Errorf("parsing --card-issuing JSON: %w", err)
					}
					body["card_issuing"] = parsedCardIssuing
				}
				if bodyDepositInsurance != "" {
					var parsedDepositInsurance any
					if err := json.Unmarshal([]byte(bodyDepositInsurance), &parsedDepositInsurance); err != nil {
						return fmt.Errorf("parsing --deposit-insurance JSON: %w", err)
					}
					body["deposit_insurance"] = parsedDepositInsurance
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyFinancialAddresses != "" {
					var parsedFinancialAddresses any
					if err := json.Unmarshal([]byte(bodyFinancialAddresses), &parsedFinancialAddresses); err != nil {
						return fmt.Errorf("parsing --financial-addresses JSON: %w", err)
					}
					body["financial_addresses"] = parsedFinancialAddresses
				}
				if bodyInboundTransfers != "" {
					var parsedInboundTransfers any
					if err := json.Unmarshal([]byte(bodyInboundTransfers), &parsedInboundTransfers); err != nil {
						return fmt.Errorf("parsing --inbound-transfers JSON: %w", err)
					}
					body["inbound_transfers"] = parsedInboundTransfers
				}
				if bodyIntraStripeFlows != "" {
					var parsedIntraStripeFlows any
					if err := json.Unmarshal([]byte(bodyIntraStripeFlows), &parsedIntraStripeFlows); err != nil {
						return fmt.Errorf("parsing --intra-stripe-flows JSON: %w", err)
					}
					body["intra_stripe_flows"] = parsedIntraStripeFlows
				}
				if bodyOutboundPayments != "" {
					var parsedOutboundPayments any
					if err := json.Unmarshal([]byte(bodyOutboundPayments), &parsedOutboundPayments); err != nil {
						return fmt.Errorf("parsing --outbound-payments JSON: %w", err)
					}
					body["outbound_payments"] = parsedOutboundPayments
				}
				if bodyOutboundTransfers != "" {
					var parsedOutboundTransfers any
					if err := json.Unmarshal([]byte(bodyOutboundTransfers), &parsedOutboundTransfers); err != nil {
						return fmt.Errorf("parsing --outbound-transfers JSON: %w", err)
					}
					body["outbound_transfers"] = parsedOutboundTransfers
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "treasury",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCardIssuing, "card-issuing", "", "Encodes the FinancialAccount's ability to be used with the Issuing product, including attaching cards to and drawing...")
	cmd.Flags().StringVar(&bodyDepositInsurance, "deposit-insurance", "", "Represents whether this FinancialAccount is eligible for deposit insurance. Various factors determine the insurance...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyFinancialAddresses, "financial-addresses", "", "Contains Features that add FinancialAddresses to the FinancialAccount.")
	cmd.Flags().StringVar(&bodyInboundTransfers, "inbound-transfers", "", "Contains settings related to adding funds to a FinancialAccount from another Account with the same owner.")
	cmd.Flags().StringVar(&bodyIntraStripeFlows, "intra-stripe-flows", "", "Represents the ability for the FinancialAccount to send money to, or receive money from other FinancialAccounts (for...")
	cmd.Flags().StringVar(&bodyOutboundPayments, "outbound-payments", "", "Includes Features related to initiating money movement out of the FinancialAccount to someone else's bucket of money.")
	cmd.Flags().StringVar(&bodyOutboundTransfers, "outbound-transfers", "", "Contains a Feature and settings related to moving money out of the FinancialAccount into another Account with the...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
