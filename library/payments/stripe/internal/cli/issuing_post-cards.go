// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newIssuingPostCardsCmd(flags *rootFlags) *cobra.Command {
	var bodyCardholder string
	var bodyCurrency string
	var bodyExpMonth int
	var bodyExpYear int
	var bodyExpand string
	var bodyFinancialAccount string
	var bodyLifecycleControls string
	var bodyMetadata string
	var bodyPersonalizationDesign string
	var bodyPin string
	var bodyReplacementFor string
	var bodyReplacementReason string
	var bodySecondLine string
	var bodyShipping string
	var bodySpendingControls string
	var bodyStatus string
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-cards",
		Short:       "<p>Creates an Issuing <code>Card</code> object.</p>",
		Example:     "  stripe-pp-cli issuing post-cards --currency example-value",
		Annotations: map[string]string{"pp:endpoint": "issuing.post-cards", "pp:method": "POST", "pp:path": "/v1/issuing/cards"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("currency") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "currency")
				}
				if !cmd.Flags().Changed("type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/issuing/cards"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCardholder != "" {
					body["cardholder"] = bodyCardholder
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				if bodyExpMonth != 0 {
					body["exp_month"] = bodyExpMonth
				}
				if bodyExpYear != 0 {
					body["exp_year"] = bodyExpYear
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyFinancialAccount != "" {
					body["financial_account"] = bodyFinancialAccount
				}
				if bodyLifecycleControls != "" {
					var parsedLifecycleControls any
					if err := json.Unmarshal([]byte(bodyLifecycleControls), &parsedLifecycleControls); err != nil {
						return fmt.Errorf("parsing --lifecycle-controls JSON: %w", err)
					}
					body["lifecycle_controls"] = parsedLifecycleControls
				}
				if bodyMetadata != "" {
					var parsedMetadata any
					if err := json.Unmarshal([]byte(bodyMetadata), &parsedMetadata); err != nil {
						return fmt.Errorf("parsing --metadata JSON: %w", err)
					}
					body["metadata"] = parsedMetadata
				}
				if bodyPersonalizationDesign != "" {
					body["personalization_design"] = bodyPersonalizationDesign
				}
				if bodyPin != "" {
					var parsedPin any
					if err := json.Unmarshal([]byte(bodyPin), &parsedPin); err != nil {
						return fmt.Errorf("parsing --pin JSON: %w", err)
					}
					body["pin"] = parsedPin
				}
				if bodyReplacementFor != "" {
					body["replacement_for"] = bodyReplacementFor
				}
				if bodyReplacementReason != "" {
					body["replacement_reason"] = bodyReplacementReason
				}
				if bodySecondLine != "" {
					body["second_line"] = bodySecondLine
				}
				if bodyShipping != "" {
					var parsedShipping any
					if err := json.Unmarshal([]byte(bodyShipping), &parsedShipping); err != nil {
						return fmt.Errorf("parsing --shipping JSON: %w", err)
					}
					body["shipping"] = parsedShipping
				}
				if bodySpendingControls != "" {
					var parsedSpendingControls any
					if err := json.Unmarshal([]byte(bodySpendingControls), &parsedSpendingControls); err != nil {
						return fmt.Errorf("parsing --spending-controls JSON: %w", err)
					}
					body["spending_controls"] = parsedSpendingControls
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "issuing",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCardholder, "cardholder", "", "The [Cardholder](https://docs.stripe.com/api#issuing_cardholder_object) object with which the card will be associated.")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "The currency for the card.")
	cmd.Flags().IntVar(&bodyExpMonth, "exp-month", 0, "The desired expiration month (1-12) for this card if [specifying a custom expiration...")
	cmd.Flags().IntVar(&bodyExpYear, "exp-year", 0, "The desired 4-digit expiration year for this card if [specifying a custom expiration...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyFinancialAccount, "financial-account", "", "The new financial account ID the card will be associated with. This field allows a card to be reassigned to a...")
	cmd.Flags().StringVar(&bodyLifecycleControls, "lifecycle-controls", "", "Rules that control the lifecycle of this card, such as automatic cancellation. Refer to our...")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyPersonalizationDesign, "personalization-design", "", "The personalization design object belonging to this card.")
	cmd.Flags().StringVar(&bodyPin, "pin", "", "The desired PIN for this card.")
	cmd.Flags().StringVar(&bodyReplacementFor, "replacement-for", "", "The card this is meant to be a replacement for (if any).")
	cmd.Flags().StringVar(&bodyReplacementReason, "replacement-reason", "", "If `replacement_for` is specified, this should indicate why that card is being replaced.")
	cmd.Flags().StringVar(&bodySecondLine, "second-line", "", "The second line to print on the card. Max length: 24 characters.")
	cmd.Flags().StringVar(&bodyShipping, "shipping", "", "The address where the card will be shipped.")
	cmd.Flags().StringVar(&bodySpendingControls, "spending-controls", "", "Rules that control spending for this card. Refer to our [documentation](https://docs.stripe.com/issuing/controls/spen...")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Whether authorizations can be approved on this card. May be blocked from activating cards depending on past-due...")
	cmd.Flags().StringVar(&bodyType, "type", "", "The type of card to issue. Possible values are `physical` or `virtual`.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
