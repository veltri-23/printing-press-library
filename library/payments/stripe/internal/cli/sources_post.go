// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSourcesPostCmd(flags *rootFlags) *cobra.Command {
	var bodyAmount int
	var bodyCurrency string
	var bodyCustomer string
	var bodyExpand string
	var bodyFlow string
	var bodyMandate string
	var bodyMetadata string
	var bodyOriginalSource string
	var bodyOwner string
	var bodyReceiver string
	var bodyRedirect string
	var bodySourceOrder string
	var bodyStatementDescriptor string
	var bodyToken string
	var bodyType string
	var bodyUsage string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post",
		Aliases:     []string{"create"},
		Short:       "<p>Creates a new source object.</p>",
		Example:     "  stripe-pp-cli sources post",
		Annotations: map[string]string{"pp:endpoint": "sources.post", "pp:method": "POST", "pp:path": "/v1/sources"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/sources"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAmount != 0 {
					body["amount"] = bodyAmount
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				if bodyCustomer != "" {
					body["customer"] = bodyCustomer
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyFlow != "" {
					body["flow"] = bodyFlow
				}
				if bodyMandate != "" {
					var parsedMandate any
					if err := json.Unmarshal([]byte(bodyMandate), &parsedMandate); err != nil {
						return fmt.Errorf("parsing --mandate JSON: %w", err)
					}
					body["mandate"] = parsedMandate
				}
				if bodyMetadata != "" {
					var parsedMetadata any
					if err := json.Unmarshal([]byte(bodyMetadata), &parsedMetadata); err != nil {
						return fmt.Errorf("parsing --metadata JSON: %w", err)
					}
					body["metadata"] = parsedMetadata
				}
				if bodyOriginalSource != "" {
					body["original_source"] = bodyOriginalSource
				}
				if bodyOwner != "" {
					var parsedOwner any
					if err := json.Unmarshal([]byte(bodyOwner), &parsedOwner); err != nil {
						return fmt.Errorf("parsing --owner JSON: %w", err)
					}
					body["owner"] = parsedOwner
				}
				if bodyReceiver != "" {
					var parsedReceiver any
					if err := json.Unmarshal([]byte(bodyReceiver), &parsedReceiver); err != nil {
						return fmt.Errorf("parsing --receiver JSON: %w", err)
					}
					body["receiver"] = parsedReceiver
				}
				if bodyRedirect != "" {
					var parsedRedirect any
					if err := json.Unmarshal([]byte(bodyRedirect), &parsedRedirect); err != nil {
						return fmt.Errorf("parsing --redirect JSON: %w", err)
					}
					body["redirect"] = parsedRedirect
				}
				if bodySourceOrder != "" {
					var parsedSourceOrder any
					if err := json.Unmarshal([]byte(bodySourceOrder), &parsedSourceOrder); err != nil {
						return fmt.Errorf("parsing --source-order JSON: %w", err)
					}
					body["source_order"] = parsedSourceOrder
				}
				if bodyStatementDescriptor != "" {
					body["statement_descriptor"] = bodyStatementDescriptor
				}
				if bodyToken != "" {
					body["token"] = bodyToken
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
				if bodyUsage != "" {
					body["usage"] = bodyUsage
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "sources",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyAmount, "amount", 0, "Amount associated with the source. This is the amount for which the source will be chargeable once ready. Required...")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) associated with the source. This is the...")
	cmd.Flags().StringVar(&bodyCustomer, "customer", "", "The `Customer` to whom the original source is attached to. Must be set when the original source is not a `Source`...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyFlow, "flow", "", "The authentication `flow` of the source to create. `flow` is one of `redirect`, `receiver`, `code_verification`,...")
	cmd.Flags().StringVar(&bodyMandate, "mandate", "", "Information about a mandate possibility attached to a source object (generally for bank debits) as well as its...")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Metadata")
	cmd.Flags().StringVar(&bodyOriginalSource, "original-source", "", "The source to share.")
	cmd.Flags().StringVar(&bodyOwner, "owner", "", "Information about the owner of the payment instrument that may be used or required by particular source types.")
	cmd.Flags().StringVar(&bodyReceiver, "receiver", "", "Optional parameters for the receiver flow. Can be set only if the source is a receiver (`flow` is `receiver`).")
	cmd.Flags().StringVar(&bodyRedirect, "redirect", "", "Parameters required for the redirect flow. Required if the source is authenticated by a redirect (`flow` is `redirect`).")
	cmd.Flags().StringVar(&bodySourceOrder, "source-order", "", "Information about the items and shipping associated with the source. Required for transactional credit (for example...")
	cmd.Flags().StringVar(&bodyStatementDescriptor, "statement-descriptor", "", "An arbitrary string to be displayed on your customer's statement. As an example, if your website is `RunClub` and...")
	cmd.Flags().StringVar(&bodyToken, "token", "", "An optional token used to create the source. When passed, token properties will override source parameters.")
	cmd.Flags().StringVar(&bodyType, "type", "", "The `type` of the source to create. Required unless `customer` and `original_source` are specified (see the [Cloning...")
	cmd.Flags().StringVar(&bodyUsage, "usage", "", "Usage")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
