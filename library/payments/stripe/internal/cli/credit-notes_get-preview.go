// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCreditNotesGetPreviewCmd(flags *rootFlags) *cobra.Command {
	var flagAmount int
	var flagCreditAmount int
	var flagEffectiveAt int
	var flagEmailType string
	var flagExpand string
	var flagInvoice string
	var flagLines string
	var flagMemo string
	var flagMetadata string
	var flagOutOfBandAmount int
	var flagReason string
	var flagRefundAmount int
	var flagRefunds string
	var flagShippingCost string

	cmd := &cobra.Command{
		Use:         "get-preview",
		Short:       "<p>Get a preview of a credit note without creating it.</p>",
		Example:     "  stripe-pp-cli credit-notes get-preview --invoice example-value",
		Annotations: map[string]string{"pp:endpoint": "credit-notes.get-preview", "pp:method": "GET", "pp:path": "/v1/credit_notes/preview", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("invoice") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "invoice")
			}
			if cmd.Flags().Changed("email-type") {
				allowedEmailType := []string{"credit_note", "none"}
				validEmailType := false
				for _, v := range allowedEmailType {
					if flagEmailType == v {
						validEmailType = true
						break
					}
				}
				if !validEmailType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "email-type", flagEmailType, allowedEmailType)
				}
			}
			if cmd.Flags().Changed("reason") {
				allowedReason := []string{"duplicate", "fraudulent", "order_change", "product_unsatisfactory"}
				validReason := false
				for _, v := range allowedReason {
					if flagReason == v {
						validReason = true
						break
					}
				}
				if !validReason {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "reason", flagReason, allowedReason)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/credit_notes/preview"
			params := map[string]string{}
			if flagAmount != 0 {
				params["amount"] = fmt.Sprintf("%v", flagAmount)
			}
			if flagCreditAmount != 0 {
				params["credit_amount"] = fmt.Sprintf("%v", flagCreditAmount)
			}
			if flagEffectiveAt != 0 {
				params["effective_at"] = fmt.Sprintf("%v", flagEffectiveAt)
			}
			if flagEmailType != "" {
				params["email_type"] = fmt.Sprintf("%v", flagEmailType)
			}
			if flagExpand != "" {
				params["expand"] = fmt.Sprintf("%v", flagExpand)
			}
			if flagInvoice != "" {
				params["invoice"] = fmt.Sprintf("%v", flagInvoice)
			}
			if flagLines != "" {
				params["lines"] = fmt.Sprintf("%v", flagLines)
			}
			if flagMemo != "" {
				params["memo"] = fmt.Sprintf("%v", flagMemo)
			}
			if flagMetadata != "" {
				params["metadata"] = fmt.Sprintf("%v", flagMetadata)
			}
			if flagOutOfBandAmount != 0 {
				params["out_of_band_amount"] = fmt.Sprintf("%v", flagOutOfBandAmount)
			}
			if flagReason != "" {
				params["reason"] = fmt.Sprintf("%v", flagReason)
			}
			if flagRefundAmount != 0 {
				params["refund_amount"] = fmt.Sprintf("%v", flagRefundAmount)
			}
			if flagRefunds != "" {
				params["refunds"] = fmt.Sprintf("%v", flagRefunds)
			}
			if flagShippingCost != "" {
				params["shipping_cost"] = fmt.Sprintf("%v", flagShippingCost)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "credit-notes", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagAmount, "amount", 0, "The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`,...")
	cmd.Flags().IntVar(&flagCreditAmount, "credit-amount", 0, "The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which...")
	cmd.Flags().IntVar(&flagEffectiveAt, "effective-at", 0, "The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces...")
	cmd.Flags().StringVar(&flagEmailType, "email-type", "", "Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`. (one of: credit_note, none)")
	cmd.Flags().StringVar(&flagExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&flagInvoice, "invoice", "", "ID of the invoice.")
	cmd.Flags().StringVar(&flagLines, "lines", "", "Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.")
	cmd.Flags().StringVar(&flagMemo, "memo", "", "The credit note's memo appears on the credit note PDF.")
	cmd.Flags().StringVar(&flagMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().IntVar(&flagOutOfBandAmount, "out-of-band-amount", 0, "The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.")
	cmd.Flags().StringVar(&flagReason, "reason", "", "Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory` (one of: duplicate, fraudulent, order_change, product_unsatisfactory)")
	cmd.Flags().IntVar(&flagRefundAmount, "refund-amount", 0, "The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be...")
	cmd.Flags().StringVar(&flagRefunds, "refunds", "", "Refunds to link to this credit note.")
	cmd.Flags().StringVar(&flagShippingCost, "shipping-cost", "", "When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note....")

	return cmd
}
