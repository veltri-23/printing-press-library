// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTerminalPostConfigurationsConfigurationCmd(flags *rootFlags) *cobra.Command {
	var bodyBbposWisepad3 string
	var bodyBbposWiseposE string
	var bodyCellular string
	var bodyExpand string
	var bodyName string
	var bodyOffline string
	var bodyRebootWindow string
	var bodyStripeS700 string
	var bodyStripeS710 string
	var bodyTipping string
	var bodyVerifoneP400 string
	var bodyWifi string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-configurations-configuration <configuration>",
		Short:       "<p>Updates a new <code>Configuration</code> object.</p>",
		Example:     "  stripe-pp-cli terminal post-configurations-configuration example-value",
		Annotations: map[string]string{"pp:endpoint": "terminal.post-configurations-configuration", "pp:method": "POST", "pp:path": "/v1/terminal/configurations/{configuration}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/terminal/configurations/{configuration}"
			path = replacePathParam(path, "configuration", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyBbposWisepad3 != "" {
					body["bbpos_wisepad3"] = bodyBbposWisepad3
				}
				if bodyBbposWiseposE != "" {
					body["bbpos_wisepos_e"] = bodyBbposWiseposE
				}
				if bodyCellular != "" {
					body["cellular"] = bodyCellular
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyOffline != "" {
					body["offline"] = bodyOffline
				}
				if bodyRebootWindow != "" {
					body["reboot_window"] = bodyRebootWindow
				}
				if bodyStripeS700 != "" {
					body["stripe_s700"] = bodyStripeS700
				}
				if bodyStripeS710 != "" {
					body["stripe_s710"] = bodyStripeS710
				}
				if bodyTipping != "" {
					body["tipping"] = bodyTipping
				}
				if bodyVerifoneP400 != "" {
					body["verifone_p400"] = bodyVerifoneP400
				}
				if bodyWifi != "" {
					body["wifi"] = bodyWifi
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "terminal",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyBbposWisepad3, "bbpos-wisepad3", "", "An object containing device type specific settings for BBPOS WisePad 3 readers.")
	cmd.Flags().StringVar(&bodyBbposWiseposE, "bbpos-wisepos-e", "", "An object containing device type specific settings for BBPOS WisePOS E readers.")
	cmd.Flags().StringVar(&bodyCellular, "cellular", "", "Configuration for cellular connectivity.")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name of the configuration")
	cmd.Flags().StringVar(&bodyOffline, "offline", "", "Configurations for collecting transactions offline.")
	cmd.Flags().StringVar(&bodyRebootWindow, "reboot-window", "", "Reboot time settings for readers. that support customized reboot time configuration.")
	cmd.Flags().StringVar(&bodyStripeS700, "stripe-s700", "", "An object containing device type specific settings for Stripe S700 readers.")
	cmd.Flags().StringVar(&bodyStripeS710, "stripe-s710", "", "An object containing device type specific settings for Stripe S710 readers.")
	cmd.Flags().StringVar(&bodyTipping, "tipping", "", "Tipping configurations for readers that support on-reader tips.")
	cmd.Flags().StringVar(&bodyVerifoneP400, "verifone-p400", "", "An object containing device type specific settings for Verifone P400 readers.")
	cmd.Flags().StringVar(&bodyWifi, "wifi", "", "Configurations for connecting to a WiFi network.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
