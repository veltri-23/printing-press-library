// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newInvoicesPostCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountTaxIds string
	var bodyApplicationFeeAmount int
	var bodyAutoAdvance bool
	var bodyAutomaticTax string
	var bodyAutomaticallyFinalizesAt int
	var bodyCollectionMethod string
	var bodyCurrency string
	var bodyCustomFields string
	var bodyCustomer string
	var bodyCustomerAccount string
	var bodyDaysUntilDue int
	var bodyDefaultPaymentMethod string
	var bodyDefaultSource string
	var bodyDefaultTaxRates string
	var bodyDescription string
	var bodyDiscounts string
	var bodyDueDate int
	var bodyEffectiveAt int
	var bodyExpand string
	var bodyFooter string
	var bodyFromInvoice string
	var bodyIssuer string
	var bodyMetadata string
	var bodyNumber string
	var bodyOnBehalfOf string
	var bodyPaymentSettings string
	var bodyPendingInvoiceItemsBehavior string
	var bodyRendering string
	var bodyShippingCost string
	var bodyShippingDetails string
	var bodyStatementDescriptor string
	var bodySubscription string
	var bodyTransferData string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post",
		Aliases:     []string{"create"},
		Short:       "<p>This endpoint creates a draft invoice for a given customer. The invoice remains a draft until you <a...",
		Example:     "  stripe-pp-cli invoices post",
		Annotations: map[string]string{"pp:endpoint": "invoices.post", "pp:method": "POST", "pp:path": "/v1/invoices"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/invoices"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccountTaxIds != "" {
					body["account_tax_ids"] = bodyAccountTaxIds
				}
				if bodyApplicationFeeAmount != 0 {
					body["application_fee_amount"] = bodyApplicationFeeAmount
				}
				if bodyAutoAdvance != false {
					body["auto_advance"] = bodyAutoAdvance
				}
				if bodyAutomaticTax != "" {
					var parsedAutomaticTax any
					if err := json.Unmarshal([]byte(bodyAutomaticTax), &parsedAutomaticTax); err != nil {
						return fmt.Errorf("parsing --automatic-tax JSON: %w", err)
					}
					body["automatic_tax"] = parsedAutomaticTax
				}
				if bodyAutomaticallyFinalizesAt != 0 {
					body["automatically_finalizes_at"] = bodyAutomaticallyFinalizesAt
				}
				if bodyCollectionMethod != "" {
					body["collection_method"] = bodyCollectionMethod
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				if bodyCustomFields != "" {
					body["custom_fields"] = bodyCustomFields
				}
				if bodyCustomer != "" {
					body["customer"] = bodyCustomer
				}
				if bodyCustomerAccount != "" {
					body["customer_account"] = bodyCustomerAccount
				}
				if bodyDaysUntilDue != 0 {
					body["days_until_due"] = bodyDaysUntilDue
				}
				if bodyDefaultPaymentMethod != "" {
					body["default_payment_method"] = bodyDefaultPaymentMethod
				}
				if bodyDefaultSource != "" {
					body["default_source"] = bodyDefaultSource
				}
				if bodyDefaultTaxRates != "" {
					var parsedDefaultTaxRates any
					if err := json.Unmarshal([]byte(bodyDefaultTaxRates), &parsedDefaultTaxRates); err != nil {
						return fmt.Errorf("parsing --default-tax-rates JSON: %w", err)
					}
					body["default_tax_rates"] = parsedDefaultTaxRates
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDiscounts != "" {
					body["discounts"] = bodyDiscounts
				}
				if bodyDueDate != 0 {
					body["due_date"] = bodyDueDate
				}
				if bodyEffectiveAt != 0 {
					body["effective_at"] = bodyEffectiveAt
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyFooter != "" {
					body["footer"] = bodyFooter
				}
				if bodyFromInvoice != "" {
					var parsedFromInvoice any
					if err := json.Unmarshal([]byte(bodyFromInvoice), &parsedFromInvoice); err != nil {
						return fmt.Errorf("parsing --from-invoice JSON: %w", err)
					}
					body["from_invoice"] = parsedFromInvoice
				}
				if bodyIssuer != "" {
					var parsedIssuer any
					if err := json.Unmarshal([]byte(bodyIssuer), &parsedIssuer); err != nil {
						return fmt.Errorf("parsing --issuer JSON: %w", err)
					}
					body["issuer"] = parsedIssuer
				}
				if bodyMetadata != "" {
					body["metadata"] = bodyMetadata
				}
				if bodyNumber != "" {
					body["number"] = bodyNumber
				}
				if bodyOnBehalfOf != "" {
					body["on_behalf_of"] = bodyOnBehalfOf
				}
				if bodyPaymentSettings != "" {
					var parsedPaymentSettings any
					if err := json.Unmarshal([]byte(bodyPaymentSettings), &parsedPaymentSettings); err != nil {
						return fmt.Errorf("parsing --payment-settings JSON: %w", err)
					}
					body["payment_settings"] = parsedPaymentSettings
				}
				if bodyPendingInvoiceItemsBehavior != "" {
					body["pending_invoice_items_behavior"] = bodyPendingInvoiceItemsBehavior
				}
				if bodyRendering != "" {
					var parsedRendering any
					if err := json.Unmarshal([]byte(bodyRendering), &parsedRendering); err != nil {
						return fmt.Errorf("parsing --rendering JSON: %w", err)
					}
					body["rendering"] = parsedRendering
				}
				if bodyShippingCost != "" {
					var parsedShippingCost any
					if err := json.Unmarshal([]byte(bodyShippingCost), &parsedShippingCost); err != nil {
						return fmt.Errorf("parsing --shipping-cost JSON: %w", err)
					}
					body["shipping_cost"] = parsedShippingCost
				}
				if bodyShippingDetails != "" {
					var parsedShippingDetails any
					if err := json.Unmarshal([]byte(bodyShippingDetails), &parsedShippingDetails); err != nil {
						return fmt.Errorf("parsing --shipping-details JSON: %w", err)
					}
					body["shipping_details"] = parsedShippingDetails
				}
				if bodyStatementDescriptor != "" {
					body["statement_descriptor"] = bodyStatementDescriptor
				}
				if bodySubscription != "" {
					body["subscription"] = bodySubscription
				}
				if bodyTransferData != "" {
					var parsedTransferData any
					if err := json.Unmarshal([]byte(bodyTransferData), &parsedTransferData); err != nil {
						return fmt.Errorf("parsing --transfer-data JSON: %w", err)
					}
					body["transfer_data"] = parsedTransferData
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "invoices",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAccountTaxIds, "account-tax-ids", "", "The account tax IDs associated with the invoice. Only editable when the invoice is a draft.")
	cmd.Flags().IntVar(&bodyApplicationFeeAmount, "application-fee-amount", 0, "A fee in cents (or local equivalent) that will be applied to the invoice and transferred to the application owner's...")
	cmd.Flags().BoolVar(&bodyAutoAdvance, "auto-advance", false, "Controls whether Stripe performs [automatic collection](https://docs.stripe.com/invoicing/integration/automatic-advan...")
	cmd.Flags().StringVar(&bodyAutomaticTax, "automatic-tax", "", "Settings for automatic tax lookup for this invoice.")
	cmd.Flags().IntVar(&bodyAutomaticallyFinalizesAt, "automatically-finalizes-at", 0, "The time when this invoice should be scheduled to finalize (up to 5 years in the future). The invoice is finalized...")
	cmd.Flags().StringVar(&bodyCollectionMethod, "collection-method", "", "Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this...")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "The currency to create this invoice in. Defaults to that of `customer` if not specified.")
	cmd.Flags().StringVar(&bodyCustomFields, "custom-fields", "", "A list of up to 4 custom fields to be displayed on the invoice.")
	cmd.Flags().StringVar(&bodyCustomer, "customer", "", "The ID of the customer to bill.")
	cmd.Flags().StringVar(&bodyCustomerAccount, "customer-account", "", "The ID of the account to bill.")
	cmd.Flags().IntVar(&bodyDaysUntilDue, "days-until-due", 0, "The number of days from when the invoice is created until it is due. Valid only for invoices where...")
	cmd.Flags().StringVar(&bodyDefaultPaymentMethod, "default-payment-method", "", "ID of the default payment method for the invoice. It must belong to the customer associated with the invoice. If not...")
	cmd.Flags().StringVar(&bodyDefaultSource, "default-source", "", "ID of the default payment source for the invoice. It must belong to the customer associated with the invoice and be...")
	cmd.Flags().StringVar(&bodyDefaultTaxRates, "default-tax-rates", "", "The tax rates that will apply to any line item that does not have `tax_rates` set.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "An arbitrary string attached to the object. Often useful for displaying to users. Referenced as 'memo' in the Dashboard.")
	cmd.Flags().StringVar(&bodyDiscounts, "discounts", "", "The coupons and promotion codes to redeem into discounts for the invoice. If not specified, inherits the discount...")
	cmd.Flags().IntVar(&bodyDueDate, "due-date", 0, "The date on which payment for this invoice is due. Valid only for invoices where `collection_method=send_invoice`.")
	cmd.Flags().IntVar(&bodyEffectiveAt, "effective-at", 0, "The date when this invoice is in effect. Same as `finalized_at` unless overwritten. When defined, this value...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyFooter, "footer", "", "Footer to be displayed on the invoice.")
	cmd.Flags().StringVar(&bodyFromInvoice, "from-invoice", "", "Revise an existing invoice. The new invoice will be created in `status=draft`. See the [revision...")
	cmd.Flags().StringVar(&bodyIssuer, "issuer", "", "The connected account that issues the invoice. The invoice is presented with the branding and support information of...")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyNumber, "number", "", "Set the number for this invoice. If no number is present then a number will be assigned automatically when the...")
	cmd.Flags().StringVar(&bodyOnBehalfOf, "on-behalf-of", "", "The account (if any) for which the funds of the invoice payment are intended. If set, the invoice will be presented...")
	cmd.Flags().StringVar(&bodyPaymentSettings, "payment-settings", "", "Configuration settings for the PaymentIntent that is generated when the invoice is finalized.")
	cmd.Flags().StringVar(&bodyPendingInvoiceItemsBehavior, "pending-invoice-items-behavior", "", "How to handle pending invoice items on invoice creation. Defaults to `exclude` if the parameter is omitted.")
	cmd.Flags().StringVar(&bodyRendering, "rendering", "", "The rendering-related settings that control how the invoice is displayed on customer-facing surfaces such as PDF and...")
	cmd.Flags().StringVar(&bodyShippingCost, "shipping-cost", "", "Settings for the cost of shipping for this invoice.")
	cmd.Flags().StringVar(&bodyShippingDetails, "shipping-details", "", "Shipping details for the invoice. The Invoice PDF will use the `shipping_details` value if it is set, otherwise the...")
	cmd.Flags().StringVar(&bodyStatementDescriptor, "statement-descriptor", "", "Extra information about a charge for the customer's credit card statement. It must contain at least one letter. If...")
	cmd.Flags().StringVar(&bodySubscription, "subscription", "", "The ID of the subscription to invoice, if any. If set, the created invoice will only include pending invoice items...")
	cmd.Flags().StringVar(&bodyTransferData, "transfer-data", "", "If specified, the funds from the invoice will be transferred to the destination and the ID of the resulting transfer...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
