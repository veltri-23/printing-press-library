// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newInvoicesGetCmd(flags *rootFlags) *cobra.Command {
	var flagCollectionMethod string
	var flagCreated string
	var flagCustomer string
	var flagCustomerAccount string
	var flagDueDate string
	var flagEndingBefore string
	var flagExpand string
	var flagLimit int
	var flagStartingAfter string
	var flagStatus string
	var flagSubscription string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get",
		Aliases:     []string{"list"},
		Short:       "<p>You can list all invoices, or list the invoices for a specific customer. The invoices are returned sorted by...",
		Example:     "  stripe-pp-cli invoices get",
		Annotations: map[string]string{"pp:endpoint": "invoices.get", "pp:method": "GET", "pp:path": "/v1/invoices", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("collection-method") {
				allowedCollectionMethod := []string{"charge_automatically", "send_invoice"}
				validCollectionMethod := false
				for _, v := range allowedCollectionMethod {
					if flagCollectionMethod == v {
						validCollectionMethod = true
						break
					}
				}
				if !validCollectionMethod {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "collection-method", flagCollectionMethod, allowedCollectionMethod)
				}
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"draft", "open", "paid", "uncollectible", "void"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "status", flagStatus, allowedStatus)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/invoices"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "invoices", path, map[string]string{
				"collection_method": fmt.Sprintf("%v", flagCollectionMethod),
				"created":           fmt.Sprintf("%v", flagCreated),
				"customer":          fmt.Sprintf("%v", flagCustomer),
				"customer_account":  fmt.Sprintf("%v", flagCustomerAccount),
				"due_date":          fmt.Sprintf("%v", flagDueDate),
				"ending_before":     fmt.Sprintf("%v", flagEndingBefore),
				"expand":            fmt.Sprintf("%v", flagExpand),
				"limit":             fmt.Sprintf("%v", flagLimit),
				"starting_after":    fmt.Sprintf("%v", flagStartingAfter),
				"status":            fmt.Sprintf("%v", flagStatus),
				"subscription":      fmt.Sprintf("%v", flagSubscription),
			}, nil, flagAll, "", "", "has_more")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagCollectionMethod, "collection-method", "", "The collection method of the invoice to retrieve. Either `charge_automatically` or `send_invoice`. (one of: charge_automatically, send_invoice)")
	cmd.Flags().StringVar(&flagCreated, "created", "", "Only return invoices that were created during the given date interval.")
	cmd.Flags().StringVar(&flagCustomer, "customer", "", "Only return invoices for the customer specified by this customer ID.")
	cmd.Flags().StringVar(&flagCustomerAccount, "customer-account", "", "Only return invoices for the account representing the customer specified by this account ID.")
	cmd.Flags().StringVar(&flagDueDate, "due-date", "", "Due date")
	cmd.Flags().StringVar(&flagEndingBefore, "ending-before", "", "A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance,...")
	cmd.Flags().StringVar(&flagExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.")
	cmd.Flags().StringVar(&flagStartingAfter, "starting-after", "", "A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance,...")
	cmd.Flags().StringVar(&flagStatus, "status", "", "The status of the invoice, one of `draft`, `open`, `paid`, `uncollectible`, or `void`. [Learn... (one of: draft, open, paid, uncollectible, void)")
	cmd.Flags().StringVar(&flagSubscription, "subscription", "", "Only return invoices for the subscription specified by this subscription ID.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
