// Copyright 2026 Chris Rodriguez and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newQuotesPostQuoteCmd(flags *rootFlags) *cobra.Command {
	var bodyApplicationFeeAmount string
	var bodyApplicationFeePercent string
	var bodyAutomaticTax string
	var bodyCollectionMethod string
	var bodyCustomer string
	var bodyCustomerAccount string
	var bodyDefaultTaxRates string
	var bodyDescription string
	var bodyDiscounts string
	var bodyExpand string
	var bodyExpiresAt int
	var bodyFooter string
	var bodyHeader string
	var bodyInvoiceSettings string
	var bodyLineItems string
	var bodyMetadata string
	var bodyOnBehalfOf string
	var bodySubscriptionData string
	var bodyTransferData string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-quote <quote>",
		Short:       "<p>A quote models prices and services for a customer.</p>",
		Example:     "  stripe-pp-cli quotes post-quote example-value",
		Annotations: map[string]string{"pp:endpoint": "quotes.post-quote", "pp:method": "POST", "pp:path": "/v1/quotes/{quote}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/quotes/{quote}"
			path = replacePathParam(path, "quote", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyApplicationFeeAmount != "" {
					body["application_fee_amount"] = bodyApplicationFeeAmount
				}
				if bodyApplicationFeePercent != "" {
					body["application_fee_percent"] = bodyApplicationFeePercent
				}
				if bodyAutomaticTax != "" {
					var parsedAutomaticTax any
					if err := json.Unmarshal([]byte(bodyAutomaticTax), &parsedAutomaticTax); err != nil {
						return fmt.Errorf("parsing --automatic-tax JSON: %w", err)
					}
					body["automatic_tax"] = parsedAutomaticTax
				}
				if bodyCollectionMethod != "" {
					body["collection_method"] = bodyCollectionMethod
				}
				if bodyCustomer != "" {
					body["customer"] = bodyCustomer
				}
				if bodyCustomerAccount != "" {
					body["customer_account"] = bodyCustomerAccount
				}
				if bodyDefaultTaxRates != "" {
					body["default_tax_rates"] = bodyDefaultTaxRates
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDiscounts != "" {
					body["discounts"] = bodyDiscounts
				}
				if bodyExpand != "" {
					var parsedExpand any
					if err := json.Unmarshal([]byte(bodyExpand), &parsedExpand); err != nil {
						return fmt.Errorf("parsing --expand JSON: %w", err)
					}
					body["expand"] = parsedExpand
				}
				if bodyExpiresAt != 0 {
					body["expires_at"] = bodyExpiresAt
				}
				if bodyFooter != "" {
					body["footer"] = bodyFooter
				}
				if bodyHeader != "" {
					body["header"] = bodyHeader
				}
				if bodyInvoiceSettings != "" {
					var parsedInvoiceSettings any
					if err := json.Unmarshal([]byte(bodyInvoiceSettings), &parsedInvoiceSettings); err != nil {
						return fmt.Errorf("parsing --invoice-settings JSON: %w", err)
					}
					body["invoice_settings"] = parsedInvoiceSettings
				}
				if bodyLineItems != "" {
					var parsedLineItems any
					if err := json.Unmarshal([]byte(bodyLineItems), &parsedLineItems); err != nil {
						return fmt.Errorf("parsing --line-items JSON: %w", err)
					}
					body["line_items"] = parsedLineItems
				}
				if bodyMetadata != "" {
					var parsedMetadata any
					if err := json.Unmarshal([]byte(bodyMetadata), &parsedMetadata); err != nil {
						return fmt.Errorf("parsing --metadata JSON: %w", err)
					}
					body["metadata"] = parsedMetadata
				}
				if bodyOnBehalfOf != "" {
					body["on_behalf_of"] = bodyOnBehalfOf
				}
				if bodySubscriptionData != "" {
					var parsedSubscriptionData any
					if err := json.Unmarshal([]byte(bodySubscriptionData), &parsedSubscriptionData); err != nil {
						return fmt.Errorf("parsing --subscription-data JSON: %w", err)
					}
					body["subscription_data"] = parsedSubscriptionData
				}
				if bodyTransferData != "" {
					body["transfer_data"] = bodyTransferData
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "quotes",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyApplicationFeeAmount, "application-fee-amount", "", "The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to...")
	cmd.Flags().StringVar(&bodyApplicationFeePercent, "application-fee-percent", "", "A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the...")
	cmd.Flags().StringVar(&bodyAutomaticTax, "automatic-tax", "", "Settings for automatic tax lookup for this quote and resulting invoices and subscriptions.")
	cmd.Flags().StringVar(&bodyCollectionMethod, "collection-method", "", "Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay invoices...")
	cmd.Flags().StringVar(&bodyCustomer, "customer", "", "The customer for which this quote belongs to. A customer is required before finalizing the quote. Once specified, it...")
	cmd.Flags().StringVar(&bodyCustomerAccount, "customer-account", "", "The account for which this quote belongs to. A customer or account is required before finalizing the quote. Once...")
	cmd.Flags().StringVar(&bodyDefaultTaxRates, "default-tax-rates", "", "The tax rates that will apply to any line item that does not have `tax_rates` set.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A description that will be displayed on the quote PDF.")
	cmd.Flags().StringVar(&bodyDiscounts, "discounts", "", "The discounts applied to the quote.")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().IntVar(&bodyExpiresAt, "expires-at", 0, "A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the...")
	cmd.Flags().StringVar(&bodyFooter, "footer", "", "A footer that will be displayed on the quote PDF.")
	cmd.Flags().StringVar(&bodyHeader, "header", "", "A header that will be displayed on the quote PDF.")
	cmd.Flags().StringVar(&bodyInvoiceSettings, "invoice-settings", "", "All invoices will be billed using the specified settings.")
	cmd.Flags().StringVar(&bodyLineItems, "line-items", "", "A list of line items the customer is being quoted for. Each line item includes information about the product, the...")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyOnBehalfOf, "on-behalf-of", "", "The account on behalf of which to charge.")
	cmd.Flags().StringVar(&bodySubscriptionData, "subscription-data", "", "When creating a subscription or subscription schedule, the specified configuration data will be used. There must be...")
	cmd.Flags().StringVar(&bodyTransferData, "transfer-data", "", "The data with which to automatically create a Transfer for each of the invoices.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
