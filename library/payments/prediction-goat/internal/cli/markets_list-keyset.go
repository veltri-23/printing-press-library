// Copyright 2026 mvanhorn. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMarketsListKeysetCmd(flags *rootFlags) *cobra.Command {
	var flagLimit int
	var flagOrder string
	var flagAscending bool
	var flagAfterCursor string
	var flagOffset string
	var flagId string
	var flagSlug string
	var flagClosed bool
	var flagDecimalized bool
	var flagClobTokenIds string
	var flagConditionIds string
	var flagQuestionIds string
	var flagMarketMakerAddress string
	var flagLiquidityNumMin float64
	var flagLiquidityNumMax float64
	var flagVolumeNumMin float64
	var flagVolumeNumMax float64
	var flagStartDateMin string
	var flagStartDateMax string
	var flagEndDateMin string
	var flagEndDateMax string
	var flagTagId string
	var flagRelatedTags bool
	var flagTagMatch string
	var flagCyom bool
	var flagRfqEnabled bool
	var flagUmaResolutionStatus string
	var flagGameId string
	var flagSportsMarketTypes string
	var flagIncludeTag bool
	var flagLocale string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list-keyset",
		Short:       "Returns markets using cursor-based (keyset) pagination for stable, efficient paging through large result sets. Use...",
		Example:     "  prediction-goat-pp-cli markets list-keyset",
		Annotations: map[string]string{"pp:endpoint": "markets.list-keyset", "pp:method": "GET", "pp:path": "/markets/keyset", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/markets/keyset"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "markets", path, map[string]string{
				"limit":                 fmt.Sprintf("%v", flagLimit),
				"order":                 fmt.Sprintf("%v", flagOrder),
				"ascending":             fmt.Sprintf("%v", flagAscending),
				"after_cursor":          fmt.Sprintf("%v", flagAfterCursor),
				"offset":                fmt.Sprintf("%v", flagOffset),
				"id":                    fmt.Sprintf("%v", flagId),
				"slug":                  fmt.Sprintf("%v", flagSlug),
				"closed":                fmt.Sprintf("%v", flagClosed),
				"decimalized":           fmt.Sprintf("%v", flagDecimalized),
				"clob_token_ids":        fmt.Sprintf("%v", flagClobTokenIds),
				"condition_ids":         fmt.Sprintf("%v", flagConditionIds),
				"question_ids":          fmt.Sprintf("%v", flagQuestionIds),
				"market_maker_address":  fmt.Sprintf("%v", flagMarketMakerAddress),
				"liquidity_num_min":     fmt.Sprintf("%v", flagLiquidityNumMin),
				"liquidity_num_max":     fmt.Sprintf("%v", flagLiquidityNumMax),
				"volume_num_min":        fmt.Sprintf("%v", flagVolumeNumMin),
				"volume_num_max":        fmt.Sprintf("%v", flagVolumeNumMax),
				"start_date_min":        fmt.Sprintf("%v", flagStartDateMin),
				"start_date_max":        fmt.Sprintf("%v", flagStartDateMax),
				"end_date_min":          fmt.Sprintf("%v", flagEndDateMin),
				"end_date_max":          fmt.Sprintf("%v", flagEndDateMax),
				"tag_id":                fmt.Sprintf("%v", flagTagId),
				"related_tags":          fmt.Sprintf("%v", flagRelatedTags),
				"tag_match":             fmt.Sprintf("%v", flagTagMatch),
				"cyom":                  fmt.Sprintf("%v", flagCyom),
				"rfq_enabled":           fmt.Sprintf("%v", flagRfqEnabled),
				"uma_resolution_status": fmt.Sprintf("%v", flagUmaResolutionStatus),
				"game_id":               fmt.Sprintf("%v", flagGameId),
				"sports_market_types":   fmt.Sprintf("%v", flagSportsMarketTypes),
				"include_tag":           fmt.Sprintf("%v", flagIncludeTag),
				"locale":                fmt.Sprintf("%v", flagLocale),
			}, nil, flagAll, "offset", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagLimit, "limit", 20, "Maximum number of results to return (max 100)")
	cmd.Flags().StringVar(&flagOrder, "order", "", "Comma-separated list of JSON field names to order by, e.g. volume_num,liquidity_num")
	cmd.Flags().BoolVar(&flagAscending, "ascending", true, "Sort direction. Only used when order is set.")
	cmd.Flags().StringVar(&flagAfterCursor, "after-cursor", "", "Opaque cursor token from a previous response's next_cursor")
	cmd.Flags().StringVar(&flagOffset, "offset", "", "Not allowed. Returns 422 if provided.")
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagSlug, "slug", "", "Slug")
	cmd.Flags().BoolVar(&flagClosed, "closed", false, "Closed")
	cmd.Flags().BoolVar(&flagDecimalized, "decimalized", false, "Decimalized")
	cmd.Flags().StringVar(&flagClobTokenIds, "clob-token-ids", "", "Clob token ids")
	cmd.Flags().StringVar(&flagConditionIds, "condition-ids", "", "Condition ids")
	cmd.Flags().StringVar(&flagQuestionIds, "question-ids", "", "Question ids")
	cmd.Flags().StringVar(&flagMarketMakerAddress, "market-maker-address", "", "Market maker address")
	cmd.Flags().Float64Var(&flagLiquidityNumMin, "liquidity-num-min", 0.0, "Liquidity num min")
	cmd.Flags().Float64Var(&flagLiquidityNumMax, "liquidity-num-max", 0.0, "Liquidity num max")
	cmd.Flags().Float64Var(&flagVolumeNumMin, "volume-num-min", 0.0, "Volume num min")
	cmd.Flags().Float64Var(&flagVolumeNumMax, "volume-num-max", 0.0, "Volume num max")
	cmd.Flags().StringVar(&flagStartDateMin, "start-date-min", "", "Start date min")
	cmd.Flags().StringVar(&flagStartDateMax, "start-date-max", "", "Start date max")
	cmd.Flags().StringVar(&flagEndDateMin, "end-date-min", "", "End date min")
	cmd.Flags().StringVar(&flagEndDateMax, "end-date-max", "", "End date max")
	cmd.Flags().StringVar(&flagTagId, "tag-id", "", "Tag id")
	cmd.Flags().BoolVar(&flagRelatedTags, "related-tags", false, "Related tags")
	cmd.Flags().StringVar(&flagTagMatch, "tag-match", "", "Tag match")
	cmd.Flags().BoolVar(&flagCyom, "cyom", false, "Cyom")
	cmd.Flags().BoolVar(&flagRfqEnabled, "rfq-enabled", false, "Rfq enabled")
	cmd.Flags().StringVar(&flagUmaResolutionStatus, "uma-resolution-status", "", "Uma resolution status")
	cmd.Flags().StringVar(&flagGameId, "game-id", "", "Game id")
	cmd.Flags().StringVar(&flagSportsMarketTypes, "sports-market-types", "", "Sports market types")
	cmd.Flags().BoolVar(&flagIncludeTag, "include-tag", false, "When true, includes Tags relation on each market")
	cmd.Flags().StringVar(&flagLocale, "locale", "", "Locale")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
