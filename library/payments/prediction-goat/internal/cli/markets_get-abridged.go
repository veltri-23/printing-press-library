// Copyright 2026 mvanhorn. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newMarketsGetAbridgedCmd(flags *rootFlags) *cobra.Command {
	var bodyClobTokenIds string
	var bodyClosed bool
	var bodyConditionIds string
	var bodyCyom bool
	var bodyEndDateMax string
	var bodyEndDateMin string
	var bodyGameId string
	var bodyId string
	var bodyIncludeTags bool
	var bodyLiquidityNumMax float64
	var bodyLiquidityNumMin float64
	var bodyMarketMakerAddress string
	var bodyQuestionIds string
	var bodyRelatedTags bool
	var bodyRewardsMinSize float64
	var bodySlug string
	var bodySportsMarketTypes string
	var bodyStartDateMax string
	var bodyStartDateMin string
	var bodyTagId int
	var bodyUmaResolutionStatus string
	var bodyVolumeNumMax float64
	var bodyVolumeNumMin float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "get-abridged",
		Aliases:     []string{"create"},
		Short:       "Query abridged markets by information filters",
		Example:     "  prediction-goat-pp-cli markets get-abridged",
		Annotations: map[string]string{"pp:endpoint": "markets.get-abridged", "pp:method": "POST", "pp:path": "/markets/abridged"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/markets/abridged"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyClobTokenIds != "" {
					var parsedClobTokenIds any
					if err := json.Unmarshal([]byte(bodyClobTokenIds), &parsedClobTokenIds); err != nil {
						return fmt.Errorf("parsing --clob-token-ids JSON: %w", err)
					}
					body["clobTokenIds"] = parsedClobTokenIds
				}
				if cmd.Flags().Changed("closed") {
					body["closed"] = bodyClosed
				}
				if bodyConditionIds != "" {
					var parsedConditionIds any
					if err := json.Unmarshal([]byte(bodyConditionIds), &parsedConditionIds); err != nil {
						return fmt.Errorf("parsing --condition-ids JSON: %w", err)
					}
					body["conditionIds"] = parsedConditionIds
				}
				if cmd.Flags().Changed("cyom") {
					body["cyom"] = bodyCyom
				}
				if bodyEndDateMax != "" {
					body["endDateMax"] = bodyEndDateMax
				}
				if bodyEndDateMin != "" {
					body["endDateMin"] = bodyEndDateMin
				}
				if bodyGameId != "" {
					body["gameId"] = bodyGameId
				}
				if bodyId != "" {
					var parsedId any
					if err := json.Unmarshal([]byte(bodyId), &parsedId); err != nil {
						return fmt.Errorf("parsing --id JSON: %w", err)
					}
					body["id"] = parsedId
				}
				if cmd.Flags().Changed("include-tags") {
					body["includeTags"] = bodyIncludeTags
				}
				if bodyLiquidityNumMax != 0.0 {
					body["liquidityNumMax"] = bodyLiquidityNumMax
				}
				if bodyLiquidityNumMin != 0.0 {
					body["liquidityNumMin"] = bodyLiquidityNumMin
				}
				if bodyMarketMakerAddress != "" {
					var parsedMarketMakerAddress any
					if err := json.Unmarshal([]byte(bodyMarketMakerAddress), &parsedMarketMakerAddress); err != nil {
						return fmt.Errorf("parsing --market-maker-address JSON: %w", err)
					}
					body["marketMakerAddress"] = parsedMarketMakerAddress
				}
				if bodyQuestionIds != "" {
					var parsedQuestionIds any
					if err := json.Unmarshal([]byte(bodyQuestionIds), &parsedQuestionIds); err != nil {
						return fmt.Errorf("parsing --question-ids JSON: %w", err)
					}
					body["questionIds"] = parsedQuestionIds
				}
				if cmd.Flags().Changed("related-tags") {
					body["relatedTags"] = bodyRelatedTags
				}
				if bodyRewardsMinSize != 0.0 {
					body["rewardsMinSize"] = bodyRewardsMinSize
				}
				if bodySlug != "" {
					var parsedSlug any
					if err := json.Unmarshal([]byte(bodySlug), &parsedSlug); err != nil {
						return fmt.Errorf("parsing --slug JSON: %w", err)
					}
					body["slug"] = parsedSlug
				}
				if bodySportsMarketTypes != "" {
					var parsedSportsMarketTypes any
					if err := json.Unmarshal([]byte(bodySportsMarketTypes), &parsedSportsMarketTypes); err != nil {
						return fmt.Errorf("parsing --sports-market-types JSON: %w", err)
					}
					body["sportsMarketTypes"] = parsedSportsMarketTypes
				}
				if bodyStartDateMax != "" {
					body["startDateMax"] = bodyStartDateMax
				}
				if bodyStartDateMin != "" {
					body["startDateMin"] = bodyStartDateMin
				}
				if bodyTagId != 0 {
					body["tagId"] = bodyTagId
				}
				if bodyUmaResolutionStatus != "" {
					body["umaResolutionStatus"] = bodyUmaResolutionStatus
				}
				if bodyVolumeNumMax != 0.0 {
					body["volumeNumMax"] = bodyVolumeNumMax
				}
				if bodyVolumeNumMin != 0.0 {
					body["volumeNumMin"] = bodyVolumeNumMin
				}
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "markets", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "markets", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "markets", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "markets", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "markets",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "markets", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "markets", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyClobTokenIds, "clob-token-ids", "", "Clob token ids")
	cmd.Flags().BoolVar(&bodyClosed, "closed", false, "Closed")
	cmd.Flags().StringVar(&bodyConditionIds, "condition-ids", "", "Condition ids")
	cmd.Flags().BoolVar(&bodyCyom, "cyom", false, "Cyom")
	cmd.Flags().StringVar(&bodyEndDateMax, "end-date-max", "", "End date max")
	cmd.Flags().StringVar(&bodyEndDateMin, "end-date-min", "", "End date min")
	cmd.Flags().StringVar(&bodyGameId, "game-id", "", "Game id")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().BoolVar(&bodyIncludeTags, "include-tags", false, "Include tags")
	cmd.Flags().Float64Var(&bodyLiquidityNumMax, "liquidity-num-max", 0.0, "Liquidity num max")
	cmd.Flags().Float64Var(&bodyLiquidityNumMin, "liquidity-num-min", 0.0, "Liquidity num min")
	cmd.Flags().StringVar(&bodyMarketMakerAddress, "market-maker-address", "", "Market maker address")
	cmd.Flags().StringVar(&bodyQuestionIds, "question-ids", "", "Question ids")
	cmd.Flags().BoolVar(&bodyRelatedTags, "related-tags", false, "Related tags")
	cmd.Flags().Float64Var(&bodyRewardsMinSize, "rewards-min-size", 0.0, "Rewards min size")
	cmd.Flags().StringVar(&bodySlug, "slug", "", "Slug")
	cmd.Flags().StringVar(&bodySportsMarketTypes, "sports-market-types", "", "Sports market types")
	cmd.Flags().StringVar(&bodyStartDateMax, "start-date-max", "", "Start date max")
	cmd.Flags().StringVar(&bodyStartDateMin, "start-date-min", "", "Start date min")
	cmd.Flags().IntVar(&bodyTagId, "tag-id", 0, "Tag id")
	cmd.Flags().StringVar(&bodyUmaResolutionStatus, "uma-resolution-status", "", "Uma resolution status")
	cmd.Flags().Float64Var(&bodyVolumeNumMax, "volume-num-max", 0.0, "Volume num max")
	cmd.Flags().Float64Var(&bodyVolumeNumMin, "volume-num-min", 0.0, "Volume num min")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
