// Copyright 2026 Martin Kessler and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/payments/qbo/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/payments/qbo/internal/client"
	"github.com/mvanhorn/printing-press-library/library/payments/qbo/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/payments/qbo/internal/config"
	"github.com/mvanhorn/printing-press-library/library/payments/qbo/internal/mcp/cobratree"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("accounts_create",
			mcplib.WithDescription("Create or update an account. Required: account. Returns the new AccountResponse."),
			mcplib.WithString("account", mcplib.Required(), mcplib.Description("Account payload")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/account", false, false, nil, []mcpParamBinding{{PublicName: "account", WireName: "account", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("accounts_get",
			mcplib.WithDescription("Get an account by ID. Required: id. Returns the AccountResponse."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Account ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/account/{id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("bills_create",
			mcplib.WithDescription("Create or update a bill. Required: bill. Returns the new BillResponse."),
			mcplib.WithString("bill", mcplib.Required(), mcplib.Description("Bill payload")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/bill", false, false, nil, []mcpParamBinding{{PublicName: "bill", WireName: "bill", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("bills_get",
			mcplib.WithDescription("Get a bill by ID. Required: id. Returns the BillResponse."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Bill ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/bill/{id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("cdc_sync",
			mcplib.WithDescription("Query changed entities since a timestamp. Required: entities, changedSince. Returns the CDCResponse."),
			mcplib.WithString("entities", mcplib.Required(), mcplib.Description("Comma-separated list of entities to check")),
			mcplib.WithString("changedSince", mcplib.Required(), mcplib.Description("Timestamp in ISO-8601 format")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/cdc", true, false, nil, []mcpParamBinding{{PublicName: "entities", WireName: "entities", Location: "query"}, {PublicName: "changedSince", WireName: "changedSince", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("customers_create",
			mcplib.WithDescription("Create or update a customer. Required: customer. Returns the new CustomerResponse."),
			mcplib.WithString("customer", mcplib.Required(), mcplib.Description("Customer payload")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/customer", false, false, nil, []mcpParamBinding{{PublicName: "customer", WireName: "customer", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("customers_get",
			mcplib.WithDescription("Get a customer by ID. Required: id. Returns the CustomerResponse."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Customer ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/customer/{id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("invoices_create",
			mcplib.WithDescription("Create or update an invoice. Required: invoice. Returns the new InvoiceResponse."),
			mcplib.WithString("invoice", mcplib.Required(), mcplib.Description("Invoice payload")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/invoice", false, false, nil, []mcpParamBinding{{PublicName: "invoice", WireName: "invoice", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("invoices_get",
			mcplib.WithDescription("Get an invoice by ID. Required: id. Returns the InvoiceResponse."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Invoice ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/invoice/{id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("journal_entries_create",
			mcplib.WithDescription("Create or update a journal entry. Required: journalentry. Returns the new JournalEntryResponse."),
			mcplib.WithString("journalentry", mcplib.Required(), mcplib.Description("Journal Entry payload")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/journalentry", false, false, nil, []mcpParamBinding{{PublicName: "journalentry", WireName: "journalentry", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("journal_entries_get",
			mcplib.WithDescription("Get a journal entry by ID. Required: id. Returns the JournalEntryResponse."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Journal Entry ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/journalentry/{id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("payments_create",
			mcplib.WithDescription("Create or update a payment. Required: payment. Returns the new PaymentResponse."),
			mcplib.WithString("payment", mcplib.Required(), mcplib.Description("Payment payload")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/payment", false, false, nil, []mcpParamBinding{{PublicName: "payment", WireName: "payment", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("payments_get",
			mcplib.WithDescription("Get a payment by ID. Required: id. Returns the PaymentResponse."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Payment ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/payment/{id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("purchases_create",
			mcplib.WithDescription("Create or update a purchase. Required: purchase. Returns the new PurchaseResponse."),
			mcplib.WithString("purchase", mcplib.Required(), mcplib.Description("Purchase payload")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/purchase", false, false, nil, []mcpParamBinding{{PublicName: "purchase", WireName: "purchase", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("purchases_get",
			mcplib.WithDescription("Get a purchase by ID. Required: id. Returns the PurchaseResponse."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Purchase ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/purchase/{id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("query_execute",
			mcplib.WithDescription("Run raw QuickBooks SQL query. Required: query. Returns the QueryResponse."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("The QBO SQL query string")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/query", true, false, nil, []mcpParamBinding{{PublicName: "query", WireName: "query", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("vendors_create",
			mcplib.WithDescription("Create or update a vendor. Required: vendor. Returns the new VendorResponse."),
			mcplib.WithString("vendor", mcplib.Required(), mcplib.Description("Vendor payload")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/vendor", false, false, nil, []mcpParamBinding{{PublicName: "vendor", WireName: "vendor", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("vendors_get",
			mcplib.WithDescription("Get a vendor by ID. Required: id. Returns the VendorResponse."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Vendor ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/vendor/{id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export QBO_CLIENT_ID=<your-key>" +
					"\n      Run 'qbo-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export QBO_CLIENT_ID=<your-key>" +
					"\n      Run 'qbo-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export QBO_CLIENT_ID=<your-key>" +
					"\n      Run 'qbo-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "qbo-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "qbo-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "qbo",
		"description": "Interact with QuickBooks Online directly.",
		"archetype":   "payments",
		"tool_count":  18,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion qbo-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "QBO_CLIENT_ID",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
				{
					"name":        "QBO_CLIENT_SECRET",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
				{
					"name":        "QBO_REALM_ID",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "accounts",
				"description": "Manage accounts",
				"endpoints":   []string{"create", "get"},
				"searchable":  true,
			},
			{
				"name":        "bills",
				"description": "Manage vendor bills",
				"endpoints":   []string{"create", "get"},
				"searchable":  true,
			},
			{
				"name":        "cdc",
				"description": "Change Data Capture (CDC) endpoints for incremental syncs",
				"endpoints":   []string{"sync"},
				"searchable":  true,
			},
			{
				"name":        "customers",
				"description": "Manage customers",
				"endpoints":   []string{"create", "get"},
				"searchable":  true,
			},
			{
				"name":        "invoices",
				"description": "Manage invoices",
				"endpoints":   []string{"create", "get"},
				"searchable":  true,
			},
			{
				"name":        "journal_entries",
				"description": "Manage journal entries",
				"endpoints":   []string{"create", "get"},
				"searchable":  true,
			},
			{
				"name":        "payments",
				"description": "Manage customer payments",
				"endpoints":   []string{"create", "get"},
				"searchable":  true,
			},
			{
				"name":        "purchases",
				"description": "Manage purchases (Expenses)",
				"endpoints":   []string{"create", "get"},
				"searchable":  true,
			},
			{
				"name":        "query",
				"description": "Run raw SQL queries against QBO (limitations apply; sync first to query SQLite locally)",
				"endpoints":   []string{"execute"},
				"searchable":  true,
			},
			{
				"name":        "vendors",
				"description": "Manage vendors",
				"endpoints":   []string{"create", "get"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use changedSince for incremental fetches (filter by modification time).",
		},
		"playbook": []map[string]string{
			{"topic": "Financial data", "insight": "Always use read-only operations for financial queries. Never use create/update tools for payment data without explicit user confirmation."},
			{"topic": "Reconciliation", "insight": "For reconciliation tasks, sync first then use sql for cross-referencing. API pagination over financial records is slow and rate-limited."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
