version: 2
project_name: qbo-pp-cli
changelog:
  disable: true
builds:
  - id: qbo-pp-cli
    main: ./cmd/qbo-pp-cli
    binary: qbo-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/payments/qbo/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: qbo-pp-mcp
    main: ./cmd/qbo-pp-mcp
    binary: qbo-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: qbo-pp-cli
    repository:
      owner: kesslerio
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Interact with QuickBooks Online directly."
    install: |
      bin.install "qbo-pp-cli"
      bin.install "qbo-pp-mcp"
