// Copyright 2026 Count and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"strings"

	"github.com/spf13/cobra"
)

var version = "2026.6.2"

// Execute runs the CLI.
func Execute() error {
	rootCmd := &cobra.Command{
		Use:          "monarch-money-pp-cli",
		Short:        "CLI for Monarch Money personal finance data",
		SilenceUsage: true,
		Version:      version,
	}
	rootCmd.SetVersionTemplate("monarch-money-pp-cli {{ .Version }}\n")
	rootCmd.AddCommand(newLoginCmd())
	rootCmd.AddCommand(newStatusCmd())
	rootCmd.AddCommand(newAccountsCmd())
	rootCmd.AddCommand(newTagsCmd())
	rootCmd.AddCommand(newTransactionsCmd())
	rootCmd.AddCommand(newCashflowCmd())
	rootCmd.AddCommand(newQueryCmd())
	rootCmd.AddCommand(newDoctorCmd())
	rootCmd.AddCommand(newVersionCliCmd())

	return rootCmd.Execute()
}

// ExitCode extracts exit code from an error (always 1 for now).
func ExitCode(err error) int {
	return 1
}

func newVersionCliCmd() *cobra.Command {
	return &cobra.Command{
		Use:   "version",
		Short: "Print version",
		Run: func(cmd *cobra.Command, args []string) {
			fmt.Printf("monarch-money-pp-cli %s\n", version)
		},
	}
}

// suggestFlag is a placeholder for flag suggestion support.
var _ = strings.Contains
