// Copyright 2026 salmonumbrella and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"math"
	"testing"
	"time"
)

// PATCH: Cover local-only transcendence helper behavior added outside the generator.

func TestNormalizeLocalPayee(t *testing.T) {
	got := normalizeLocalPayee("  ACME   Billing\tLLC  ")
	if got != "acme billing llc" {
		t.Fatalf("normalizeLocalPayee = %q, want %q", got, "acme billing llc")
	}
}

func TestInferLocalCadenceMonthly(t *testing.T) {
	dates := []time.Time{
		mustDate(t, "2026-01-03"),
		mustDate(t, "2026-02-02"),
		mustDate(t, "2026-03-04"),
		mustDate(t, "2026-04-03"),
	}
	cadence := inferLocalCadence(dates)
	if cadence != "monthly" {
		t.Fatalf("inferLocalCadence = %q, want monthly", cadence)
	}
}

func TestStddevPct(t *testing.T) {
	got := localStddevPct([]float64{10, 10, 11, 9})
	if math.Abs(got-0.070710678) > 0.000001 {
		t.Fatalf("localStddevPct = %.9f", got)
	}
}

func mustDate(t *testing.T, s string) time.Time {
	t.Helper()
	d, err := parseLocalDate(s)
	if err != nil {
		t.Fatalf("parseLocalDate(%q): %v", s, err)
	}
	return d
}
