// Copyright 2026 Jeff DeBolt and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

var version = "2026.6.1"

// Execute runs the CLI.
func Execute() error {
	rootCmd := &cobra.Command{
		Use:          "qbo-pp-cli",
		Short:        "CLI for qbo",
		SilenceUsage: true,
		Version:      version,
	}
	rootCmd.SetVersionTemplate("qbo-pp-cli {{ .Version }}\n")
	rootCmd.AddCommand(newStatusCmd())
	rootCmd.AddCommand(newQueryCmd())
	rootCmd.AddCommand(newAccountsCmd())
	rootCmd.AddCommand(newAuthCmd())
	rootCmd.AddCommand(newBillsCmd())
	rootCmd.AddCommand(newCompanyCmd())
	rootCmd.AddCommand(newCustomersCmd())
	rootCmd.AddCommand(newInvoicesCmd())
	rootCmd.AddCommand(newItemsCmd())
	rootCmd.AddCommand(newPaymentsCmd())
	rootCmd.AddCommand(newRawCmd())
	rootCmd.AddCommand(newReportsCmd())
	rootCmd.AddCommand(newVendorsCmd())
	rootCmd.AddCommand(newDoctorCmd())
	rootCmd.AddCommand(newVersionCliCmd())

	return rootCmd.Execute()
}

// ExitCode extracts exit code from an error (always 1 for now).
func ExitCode(err error) int {
	return 1
}

func newVersionCliCmd() *cobra.Command {
	return &cobra.Command{
		Use:   "version",
		Short: "Print version",
		Run: func(cmd *cobra.Command, args []string) {
			fmt.Printf("qbo-pp-cli %s\n", version)
		},
	}
}
