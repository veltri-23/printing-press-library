# xero scope and safety research

This first candidate is fixture-only/read-only.

Provider live OAuth work is explicitly deferred until auth fallback, refresh, diagnostics, timeout, and mutation-boundary tests are implemented.
