// Copyright 2026 bricenice17 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/commerce/doordash/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/commerce/doordash/internal/client"
	"github.com/mvanhorn/printing-press-library/library/commerce/doordash/internal/config"
	"github.com/mvanhorn/printing-press-library/library/commerce/doordash/internal/mcp/cobratree"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("graphql_create_add_cart_item",
			mcplib.WithDescription("POST /graphql/addCartItem. Required: operationName, query, variables. Optional: operation. Returns the new addCartItem."),
			mcplib.WithString("operation", mcplib.Description("")),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("variables", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql/addCartItem", []mcpParamBinding{{PublicName: "operation", WireName: "operation", Location: "query"}, {PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("graphql_create_autocomplete_facet_feed",
			mcplib.WithDescription("POST /graphql/autocompleteFacetFeed. Required: operationName, query, variables. Optional: operation. Returns the new autocompleteFacetFeed."),
			mcplib.WithString("operation", mcplib.Description("")),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("variables", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql/autocompleteFacetFeed", []mcpParamBinding{{PublicName: "operation", WireName: "operation", Location: "query"}, {PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("graphql_create_checkout",
			mcplib.WithDescription("POST /graphql/checkout. Required: operationName, query, variables. Optional: operation. Returns the new checkout."),
			mcplib.WithString("operation", mcplib.Description("")),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("variables", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql/checkout", []mcpParamBinding{{PublicName: "operation", WireName: "operation", Location: "query"}, {PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("graphql_create_convenience_search_query",
			mcplib.WithDescription("POST /graphql/convenienceSearchQuery. Required: operationName, query, variables. Optional: operation. Returns the new convenienceSearchQuery."),
			mcplib.WithString("operation", mcplib.Description("")),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("variables", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql/convenienceSearchQuery", []mcpParamBinding{{PublicName: "operation", WireName: "operation", Location: "query"}, {PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}}, []string{}),
	)
	// PATCH: Do not expose createOrderFromCart through the generic MCP API handler.
	// The endpoint can charge a saved payment method; agents must use the Cobra
	// mirror of the guarded CLI commands so validateOrderPlacementGate runs.
	s.AddTool(
		mcplib.NewTool("graphql_create_item_page",
			mcplib.WithDescription("POST /graphql/itemPage. Required: operationName, query, variables. Optional: operation. Returns the new itemPage."),
			mcplib.WithString("operation", mcplib.Description("")),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("variables", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql/itemPage", []mcpParamBinding{{PublicName: "operation", WireName: "operation", Location: "query"}, {PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("graphql_create_list_carts",
			mcplib.WithDescription("POST /graphql/listCarts. Required: operationName, query, variables. Optional: operation. Returns the new listCarts."),
			mcplib.WithString("operation", mcplib.Description("")),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("variables", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql/listCarts", []mcpParamBinding{{PublicName: "operation", WireName: "operation", Location: "query"}, {PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("graphql_create_storepage_feed",
			mcplib.WithDescription("POST /graphql/storepageFeed. Required: operationName, query, variables. Optional: operation. Returns the new storepageFeed."),
			mcplib.WithString("operation", mcplib.Description("")),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("variables", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql/storepageFeed", []mcpParamBinding{{PublicName: "operation", WireName: "operation", Location: "query"}, {PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}}, []string{}),
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// PATCH(greptile PR #784): MCP clients pass GraphQL variables through a string
// schema, but the GraphQL HTTP body must contain variables as a JSON object.
func normalizeMCPBodyValue(wireName string, v any) (any, error) {
	if wireName != "variables" {
		return v, nil
	}
	s, ok := v.(string)
	if !ok {
		return v, nil
	}
	var parsed map[string]any
	if err := json.Unmarshal([]byte(s), &parsed); err != nil {
		return nil, fmt.Errorf("parsing variables JSON: %w", err)
	}
	return parsed, nil
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyValue, err := normalizeMCPBodyValue(binding.WireName, v)
				if err != nil {
					return mcplib.NewToolResultError(err.Error()), nil
				}
				bodyArgs[binding.WireName] = bodyValue
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyValue, err := normalizeMCPBodyValue(k, v)
				if err != nil {
					return mcplib.NewToolResultError(err.Error()), nil
				}
				bodyArgs[k] = bodyValue
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      Run 'doordash-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      Run 'doordash-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "doordash-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 2)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "doordash-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "doordash",
		"description": "Discovered API spec for doordash",
		"archetype":   "generic",
		"tool_count":  8,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion doordash-pp-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "graphql",
				"description": "Operations on autocompleteFacetFeed",
				"endpoints":   []string{"create_addCartItem", "create_autocompleteFacetFeed", "create_checkout", "create_convenienceSearchQuery", "create_itemPage", "create_listCarts", "create_storepageFeed"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
