// Copyright 2026 Matt Van Horn and contributors. Licensed under Apache-2.0. See LICENSE.
// Originally generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press).
// Hand-edited 2026-04-30: marked experimental + hidden because eBay step-ups
// auth on /bfl/placebid and the cookie-based bid flow can't complete.

package cli

import (
	"github.com/spf13/cobra"
)

func newBidCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:    "bid",
		Short:  "[experimental] Place bids on auction listings (currently broken: eBay step-up auth)",
		Hidden: true,
		PersistentPreRun: func(cmd *cobra.Command, args []string) {
			experimentalWarning(cmd, "bid")
		},
	}

	cmd.AddCommand(newBidConfirmCmd(flags))
	cmd.AddCommand(newBidModuleCmd(flags))
	cmd.AddCommand(newBidTriskCmd(flags))
	return cmd
}
