// Copyright 2026 Bheem Reddy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/commerce/gumroad/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/commerce/gumroad/internal/client"
	"github.com/mvanhorn/printing-press-library/library/commerce/gumroad/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/commerce/gumroad/internal/config"
	"github.com/mvanhorn/printing-press-library/library/commerce/gumroad/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/commerce/gumroad/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("earnings_get",
			mcplib.WithDescription("Retrieve an annual earnings breakdown for the authenticated user. Requires view_tax_data scope. Required: year. Returns the SuccessEnvelope."),
			mcplib.WithString("year", mcplib.Required(), mcplib.Description("Four-digit tax year.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/earnings", []mcpParamBinding{{PublicName: "year", WireName: "year", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_abort",
			mcplib.WithDescription("Cancel a multipart upload started by /files/presign. Requires edit_products scope. Required: key, upload_id. Returns the new SuccessEnvelope."),
			mcplib.WithString("key", mcplib.Required(), mcplib.Description("Key")),
			mcplib.WithString("upload_id", mcplib.Required(), mcplib.Description("Upload id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/files/abort", []mcpParamBinding{{PublicName: "key", WireName: "key", Location: "body"}, {PublicName: "upload_id", WireName: "upload_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_complete",
			mcplib.WithDescription("Finalize a multipart upload started by /files/presign. Requires edit_products scope. Required: key, parts, upload_id. Returns the new SuccessEnvelope."),
			mcplib.WithString("key", mcplib.Required(), mcplib.Description("Key")),
			mcplib.WithString("parts", mcplib.Required(), mcplib.Description("Parts")),
			mcplib.WithString("upload_id", mcplib.Required(), mcplib.Description("Upload id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/files/complete", []mcpParamBinding{{PublicName: "key", WireName: "key", Location: "body"}, {PublicName: "parts", WireName: "parts", Location: "body"}, {PublicName: "upload_id", WireName: "upload_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_presign",
			mcplib.WithDescription("Start a multipart upload and return presigned URLs for each part. Requires edit_products scope. Required: file_size, filename. Returns the new SuccessEnvelope."),
			mcplib.WithString("file_size", mcplib.Required(), mcplib.Description("File size in bytes, max 20 GB.")),
			mcplib.WithString("filename", mcplib.Required(), mcplib.Description("Filename")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/files/presign", []mcpParamBinding{{PublicName: "file_size", WireName: "file_size", Location: "body"}, {PublicName: "filename", WireName: "filename", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("licenses_decrement-uses-count",
			mcplib.WithDescription("Decrement the uses count of a license. Requires edit_products scope. Required: license_key, product_id. Returns the updated SuccessEnvelope."),
			mcplib.WithString("license_key", mcplib.Required(), mcplib.Description("License key")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/licenses/decrement_uses_count", []mcpParamBinding{{PublicName: "license_key", WireName: "license_key", Location: "body"}, {PublicName: "product_id", WireName: "product_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("licenses_disable",
			mcplib.WithDescription("Disable a license. Requires edit_products scope. Required: license_key, product_id. Returns the updated SuccessEnvelope."),
			mcplib.WithString("license_key", mcplib.Required(), mcplib.Description("License key")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/licenses/disable", []mcpParamBinding{{PublicName: "license_key", WireName: "license_key", Location: "body"}, {PublicName: "product_id", WireName: "product_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("licenses_enable",
			mcplib.WithDescription("Enable a license. Requires edit_products scope. Required: license_key, product_id. Returns the updated SuccessEnvelope."),
			mcplib.WithString("license_key", mcplib.Required(), mcplib.Description("License key")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/licenses/enable", []mcpParamBinding{{PublicName: "license_key", WireName: "license_key", Location: "body"}, {PublicName: "product_id", WireName: "product_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("licenses_rotate",
			mcplib.WithDescription("Rotate a license key. The old key will no longer be valid. Requires edit_products scope. Required: license_key, product_id. Returns the updated SuccessEnvelope."),
			mcplib.WithString("license_key", mcplib.Required(), mcplib.Description("License key")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/licenses/rotate", []mcpParamBinding{{PublicName: "license_key", WireName: "license_key", Location: "body"}, {PublicName: "product_id", WireName: "product_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("licenses_verify",
			mcplib.WithDescription("Verify a license key. Required: license_key, product_id. Optional: increment_uses_count. Returns the new SuccessEnvelope. (public)"),
			mcplib.WithString("increment_uses_count", mcplib.Description("Increment uses count")),
			mcplib.WithString("license_key", mcplib.Required(), mcplib.Description("License key")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/licenses/verify", []mcpParamBinding{{PublicName: "increment_uses_count", WireName: "increment_uses_count", Location: "body"}, {PublicName: "license_key", WireName: "license_key", Location: "body"}, {PublicName: "product_id", WireName: "product_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("payouts_get",
			mcplib.WithDescription("Retrieve details of a payout. Requires view_payouts scope. Required: id. Optional: include_sales, include_transactions. Returns the SuccessEnvelope."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("include_sales", mcplib.Description("Include sales")),
			mcplib.WithString("include_transactions", mcplib.Description("Include transactions")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/payouts/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "include_sales", WireName: "include_sales", Location: "query"}, {PublicName: "include_transactions", WireName: "include_transactions", Location: "query"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("payouts_get-upcoming",
			mcplib.WithDescription("Retrieve upcoming payouts. Requires view_payouts scope. Optional: include_sales, include_transactions. Returns the SuccessEnvelope."),
			mcplib.WithString("include_sales", mcplib.Description("Include sales")),
			mcplib.WithString("include_transactions", mcplib.Description("Include transactions")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/payouts/upcoming", []mcpParamBinding{{PublicName: "include_sales", WireName: "include_sales", Location: "query"}, {PublicName: "include_transactions", WireName: "include_transactions", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("payouts_list",
			mcplib.WithDescription("Retrieve payouts for the authenticated user. Requires view_payouts scope. Optional: after, before, page_key (plus 1 more). Returns the SuccessEnvelope."),
			mcplib.WithString("after", mcplib.Description("Return records after this date.")),
			mcplib.WithString("before", mcplib.Description("Return records before this date.")),
			mcplib.WithString("page_key", mcplib.Description("Opaque pagination cursor returned as next_page_key.")),
			mcplib.WithString("include_upcoming", mcplib.Description("Set false to exclude the upcoming payout.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/payouts", []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "page_key", WireName: "page_key", Location: "query"}, {PublicName: "include_upcoming", WireName: "include_upcoming", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("products_create",
			mcplib.WithDescription("Create a new product as a draft. Requires edit_products or account scope. Optional: cover_ids, custom_permalink, custom_receipt (plus 19 more). Returns the new SuccessEnvelope."),
			mcplib.WithString("cover_ids", mcplib.Description("Cover ids")),
			mcplib.WithString("custom_permalink", mcplib.Description("Custom permalink")),
			mcplib.WithString("custom_receipt", mcplib.Description("Custom receipt")),
			mcplib.WithString("custom_summary", mcplib.Description("Custom summary")),
			mcplib.WithString("customizable_price", mcplib.Description("Customizable price")),
			mcplib.WithString("description", mcplib.Description("HTML description.")),
			mcplib.WithString("display_product_reviews", mcplib.Description("Display product reviews")),
			mcplib.WithString("files", mcplib.Description("Files to attach. Use file_url values returned by /files/complete.")),
			mcplib.WithString("has_same_rich_content_for_all_variants", mcplib.Description("Has same rich content for all variants")),
			mcplib.WithString("is_adult", mcplib.Description("Is adult")),
			mcplib.WithString("max_purchase_count", mcplib.Description("Max purchase count")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("native_type", mcplib.Description("Native type")),
			mcplib.WithString("price", mcplib.Description("Price in the smallest currency unit, for example cents.")),
			mcplib.WithString("price_currency_type", mcplib.Description("ISO currency code.")),
			mcplib.WithString("quantity_enabled", mcplib.Description("Quantity enabled")),
			mcplib.WithString("rich_content", mcplib.Description("Rich content")),
			mcplib.WithString("should_show_sales_count", mcplib.Description("Should show sales count")),
			mcplib.WithString("subscription_duration", mcplib.Description("Subscription duration")),
			mcplib.WithString("suggested_price_cents", mcplib.Description("Suggested price cents")),
			mcplib.WithString("tags", mcplib.Description("Tags")),
			mcplib.WithString("taxonomy_id", mcplib.Description("Taxonomy id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/products", []mcpParamBinding{{PublicName: "cover_ids", WireName: "cover_ids", Location: "body"}, {PublicName: "custom_permalink", WireName: "custom_permalink", Location: "body"}, {PublicName: "custom_receipt", WireName: "custom_receipt", Location: "body"}, {PublicName: "custom_summary", WireName: "custom_summary", Location: "body"}, {PublicName: "customizable_price", WireName: "customizable_price", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "display_product_reviews", WireName: "display_product_reviews", Location: "body"}, {PublicName: "files", WireName: "files", Location: "body"}, {PublicName: "has_same_rich_content_for_all_variants", WireName: "has_same_rich_content_for_all_variants", Location: "body"}, {PublicName: "is_adult", WireName: "is_adult", Location: "body"}, {PublicName: "max_purchase_count", WireName: "max_purchase_count", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "native_type", WireName: "native_type", Location: "body"}, {PublicName: "price", WireName: "price", Location: "body"}, {PublicName: "price_currency_type", WireName: "price_currency_type", Location: "body"}, {PublicName: "quantity_enabled", WireName: "quantity_enabled", Location: "body"}, {PublicName: "rich_content", WireName: "rich_content", Location: "body"}, {PublicName: "should_show_sales_count", WireName: "should_show_sales_count", Location: "body"}, {PublicName: "subscription_duration", WireName: "subscription_duration", Location: "body"}, {PublicName: "suggested_price_cents", WireName: "suggested_price_cents", Location: "body"}, {PublicName: "tags", WireName: "tags", Location: "body"}, {PublicName: "taxonomy_id", WireName: "taxonomy_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("products_delete",
			mcplib.WithDescription("Permanently delete a product. Required: id. Returns the SuccessEnvelope. Destructive."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/products/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_get",
			mcplib.WithDescription("Retrieve details of a product. Required: id. Returns the SuccessEnvelope."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_list",
			mcplib.WithDescription("Retrieve all existing products for the authenticated user. Optional: page_key. Returns the SuccessEnvelope."),
			mcplib.WithString("page_key", mcplib.Description("Opaque pagination cursor returned as next_page_key.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products", []mcpParamBinding{{PublicName: "page_key", WireName: "page_key", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("products_update",
			mcplib.WithDescription("Update an existing product. Collection fields such as files, tags, and rich_content replace the full collection. Required: id. Optional: cover_ids, custom_permalink, custom_receipt (plus 19 more). Returns the updated SuccessEnvelope."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("cover_ids", mcplib.Description("Cover ids")),
			mcplib.WithString("custom_permalink", mcplib.Description("Custom permalink")),
			mcplib.WithString("custom_receipt", mcplib.Description("Custom receipt")),
			mcplib.WithString("custom_summary", mcplib.Description("Custom summary")),
			mcplib.WithString("customizable_price", mcplib.Description("Customizable price")),
			mcplib.WithString("description", mcplib.Description("HTML description.")),
			mcplib.WithString("display_product_reviews", mcplib.Description("Display product reviews")),
			mcplib.WithString("files", mcplib.Description("Files to attach. Use file_url values returned by /files/complete.")),
			mcplib.WithString("has_same_rich_content_for_all_variants", mcplib.Description("Has same rich content for all variants")),
			mcplib.WithString("is_adult", mcplib.Description("Is adult")),
			mcplib.WithString("max_purchase_count", mcplib.Description("Max purchase count")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("native_type", mcplib.Description("Native type")),
			mcplib.WithString("price", mcplib.Description("Price in the smallest currency unit, for example cents.")),
			mcplib.WithString("price_currency_type", mcplib.Description("ISO currency code.")),
			mcplib.WithString("quantity_enabled", mcplib.Description("Quantity enabled")),
			mcplib.WithString("rich_content", mcplib.Description("Rich content")),
			mcplib.WithString("should_show_sales_count", mcplib.Description("Should show sales count")),
			mcplib.WithString("subscription_duration", mcplib.Description("Subscription duration")),
			mcplib.WithString("suggested_price_cents", mcplib.Description("Suggested price cents")),
			mcplib.WithString("tags", mcplib.Description("Tags")),
			mcplib.WithString("taxonomy_id", mcplib.Description("Taxonomy id")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/products/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "cover_ids", WireName: "cover_ids", Location: "body"}, {PublicName: "custom_permalink", WireName: "custom_permalink", Location: "body"}, {PublicName: "custom_receipt", WireName: "custom_receipt", Location: "body"}, {PublicName: "custom_summary", WireName: "custom_summary", Location: "body"}, {PublicName: "customizable_price", WireName: "customizable_price", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "display_product_reviews", WireName: "display_product_reviews", Location: "body"}, {PublicName: "files", WireName: "files", Location: "body"}, {PublicName: "has_same_rich_content_for_all_variants", WireName: "has_same_rich_content_for_all_variants", Location: "body"}, {PublicName: "is_adult", WireName: "is_adult", Location: "body"}, {PublicName: "max_purchase_count", WireName: "max_purchase_count", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "native_type", WireName: "native_type", Location: "body"}, {PublicName: "price", WireName: "price", Location: "body"}, {PublicName: "price_currency_type", WireName: "price_currency_type", Location: "body"}, {PublicName: "quantity_enabled", WireName: "quantity_enabled", Location: "body"}, {PublicName: "rich_content", WireName: "rich_content", Location: "body"}, {PublicName: "should_show_sales_count", WireName: "should_show_sales_count", Location: "body"}, {PublicName: "subscription_duration", WireName: "subscription_duration", Location: "body"}, {PublicName: "suggested_price_cents", WireName: "suggested_price_cents", Location: "body"}, {PublicName: "tags", WireName: "tags", Location: "body"}, {PublicName: "taxonomy_id", WireName: "taxonomy_id", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_covers_create",
			mcplib.WithDescription("Add a cover to a product from a publicly accessible URL. Requires edit_products scope. Required: product_id, url. Returns the new SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("url", mcplib.Required(), mcplib.Description("Url")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/products/{product_id}/covers", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "url", WireName: "url", Location: "body"}}, []string{"product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_covers_delete",
			mcplib.WithDescription("Delete a cover from a product. Requires edit_products scope. Required: product_id, id. Returns the SuccessEnvelope. Destructive."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/products/{product_id}/covers/{id}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}}, []string{"product_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_custom-fields_create",
			mcplib.WithDescription("Create a new custom field for a product. Required: product_id, name. Optional: required, type. Returns the new SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithString("required", mcplib.Description("Required")),
			mcplib.WithString("type", mcplib.Description("Type")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/products/{product_id}/custom_fields", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "required", WireName: "required", Location: "body"}, {PublicName: "type", WireName: "type", Location: "body"}}, []string{"product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_custom-fields_delete",
			mcplib.WithDescription("Permanently delete a product custom field. Required: product_id, name. Returns the SuccessEnvelope. Destructive."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/products/{product_id}/custom_fields/{name}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "path"}}, []string{"product_id", "name"}),
	)
	s.AddTool(
		mcplib.NewTool("products_custom-fields_list",
			mcplib.WithDescription("Retrieve all existing custom fields for a product. Required: product_id. Returns the SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products/{product_id}/custom_fields", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}}, []string{"product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_custom-fields_update",
			mcplib.WithDescription("Edit an existing product custom field. Required: product_id, name. Optional: required, type. Returns the updated SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithString("required", mcplib.Description("Required")),
			mcplib.WithString("type", mcplib.Description("Type")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/products/{product_id}/custom_fields/{name}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "required", WireName: "required", Location: "body"}, {PublicName: "type", WireName: "type", Location: "body"}}, []string{"product_id", "name"}),
	)
	s.AddTool(
		mcplib.NewTool("products_disable_product",
			mcplib.WithDescription("Disable an existing product. Required: id. Returns the updated SuccessEnvelope."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/products/{id}/disable", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_enable_product",
			mcplib.WithDescription("Enable an existing product. Required: id. Returns the updated SuccessEnvelope."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/products/{id}/enable", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_offer-codes_create",
			mcplib.WithDescription("Create a new offer code for a product. Required: product_id, name. Optional: amount_cents, max_purchase_count, percent_off (plus 1 more). Returns the new SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("amount_cents", mcplib.Description("Amount cents")),
			mcplib.WithString("max_purchase_count", mcplib.Description("Max purchase count")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithString("percent_off", mcplib.Description("Percent off")),
			mcplib.WithString("universal", mcplib.Description("Universal")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/products/{product_id}/offer_codes", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "amount_cents", WireName: "amount_cents", Location: "body"}, {PublicName: "max_purchase_count", WireName: "max_purchase_count", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "percent_off", WireName: "percent_off", Location: "body"}, {PublicName: "universal", WireName: "universal", Location: "body"}}, []string{"product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_offer-codes_delete",
			mcplib.WithDescription("Permanently delete a product offer code. Required: product_id, id. Returns the SuccessEnvelope. Destructive."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/products/{product_id}/offer_codes/{id}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}}, []string{"product_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_offer-codes_get",
			mcplib.WithDescription("Retrieve details of a specific offer code of a product. Required: product_id, id. Returns the SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products/{product_id}/offer_codes/{id}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}}, []string{"product_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_offer-codes_list",
			mcplib.WithDescription("Retrieve all existing offer codes for a product. Required: product_id. Returns the SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products/{product_id}/offer_codes", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}}, []string{"product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_offer-codes_update",
			mcplib.WithDescription("Edit an existing product offer code. Required: product_id, id. Optional: amount_cents, max_purchase_count, name (plus 1 more). Returns the updated SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("amount_cents", mcplib.Description("Amount cents")),
			mcplib.WithString("max_purchase_count", mcplib.Description("Max purchase count")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("percent_off", mcplib.Description("Percent off")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/products/{product_id}/offer_codes/{id}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "amount_cents", WireName: "amount_cents", Location: "body"}, {PublicName: "max_purchase_count", WireName: "max_purchase_count", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "percent_off", WireName: "percent_off", Location: "body"}}, []string{"product_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_subscribers_list",
			mcplib.WithDescription("Retrieve active subscribers for one product. Requires view_sales scope. Required: product_id. Optional: email, page_key. Returns the SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("email", mcplib.Description("Email")),
			mcplib.WithString("page_key", mcplib.Description("Opaque pagination cursor returned as next_page_key.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products/{product_id}/subscribers", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "email", WireName: "email", Location: "query"}, {PublicName: "page_key", WireName: "page_key", Location: "query"}}, []string{"product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_variant-categories_create-variant",
			mcplib.WithDescription("Create a new variant of a product. Required: product_id, variant_category_id, name. Optional: max_purchase_count, price_difference_cents. Returns the new SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("variant_category_id", mcplib.Required(), mcplib.Description("Variant category id")),
			mcplib.WithString("max_purchase_count", mcplib.Description("Max purchase count")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithString("price_difference_cents", mcplib.Description("Price difference cents")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/products/{product_id}/variant_categories/{variant_category_id}/variants", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "variant_category_id", WireName: "variant_category_id", Location: "path"}, {PublicName: "max_purchase_count", WireName: "max_purchase_count", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "price_difference_cents", WireName: "price_difference_cents", Location: "body"}}, []string{"product_id", "variant_category_id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_variant-categories_create-variant-category",
			mcplib.WithDescription("Create a new variant category on a product. Required: product_id, title. Returns the new SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("title", mcplib.Required(), mcplib.Description("Title")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/products/{product_id}/variant_categories", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "title", WireName: "title", Location: "body"}}, []string{"product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_variant-categories_delete-variant",
			mcplib.WithDescription("Permanently delete a variant of a product. Required: product_id, variant_category_id, id. Returns the SuccessEnvelope. Destructive."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("variant_category_id", mcplib.Required(), mcplib.Description("Variant category id")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/products/{product_id}/variant_categories/{variant_category_id}/variants/{id}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "variant_category_id", WireName: "variant_category_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}}, []string{"product_id", "variant_category_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_variant-categories_delete-variant-category",
			mcplib.WithDescription("Permanently delete a variant category of a product. Required: product_id, id. Returns the SuccessEnvelope. Destructive."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/products/{product_id}/variant_categories/{id}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}}, []string{"product_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_variant-categories_get-variant",
			mcplib.WithDescription("Retrieve details of a variant of a product. Required: product_id, variant_category_id, id. Returns the SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("variant_category_id", mcplib.Required(), mcplib.Description("Variant category id")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products/{product_id}/variant_categories/{variant_category_id}/variants/{id}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "variant_category_id", WireName: "variant_category_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}}, []string{"product_id", "variant_category_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_variant-categories_get-variant-category",
			mcplib.WithDescription("Retrieve details of a variant category of a product. Required: product_id, id. Returns the SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products/{product_id}/variant_categories/{id}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}}, []string{"product_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_variant-categories_list",
			mcplib.WithDescription("Retrieve all existing variant categories of a product. Required: product_id. Returns the SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products/{product_id}/variant_categories", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}}, []string{"product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_variant-categories_list-variants",
			mcplib.WithDescription("Retrieve all existing variants in a variant category. Required: product_id, variant_category_id. Returns the SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("variant_category_id", mcplib.Required(), mcplib.Description("Variant category id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products/{product_id}/variant_categories/{variant_category_id}/variants", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "variant_category_id", WireName: "variant_category_id", Location: "path"}}, []string{"product_id", "variant_category_id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_variant-categories_update-variant",
			mcplib.WithDescription("Edit a variant of an existing product. Required: product_id, variant_category_id, id. Optional: max_purchase_count, name, price_difference_cents. Returns the updated SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("variant_category_id", mcplib.Required(), mcplib.Description("Variant category id")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("max_purchase_count", mcplib.Description("Max purchase count")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("price_difference_cents", mcplib.Description("Price difference cents")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/products/{product_id}/variant_categories/{variant_category_id}/variants/{id}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "variant_category_id", WireName: "variant_category_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "max_purchase_count", WireName: "max_purchase_count", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "price_difference_cents", WireName: "price_difference_cents", Location: "body"}}, []string{"product_id", "variant_category_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("products_variant-categories_update-variant-category",
			mcplib.WithDescription("Edit a variant category of an existing product. Required: product_id, id. Optional: title. Returns the updated SuccessEnvelope."),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("title", mcplib.Description("Title")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/products/{product_id}/variant_categories/{id}", []mcpParamBinding{{PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "title", WireName: "title", Location: "body"}}, []string{"product_id", "id"}),
	)
	s.AddTool(
		mcplib.NewTool("resource-subscriptions_create",
			mcplib.WithDescription("Subscribe to a resource webhook. Requires view_sales scope. Required: post_url, resource_name. Returns the updated SuccessEnvelope."),
			mcplib.WithString("post_url", mcplib.Required(), mcplib.Description("Post url")),
			mcplib.WithString("resource_name", mcplib.Required(), mcplib.Description("Resource name")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/resource_subscriptions", []mcpParamBinding{{PublicName: "post_url", WireName: "post_url", Location: "body"}, {PublicName: "resource_name", WireName: "resource_name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("resource-subscriptions_delete",
			mcplib.WithDescription("Unsubscribe from a resource. Required: id. Returns the SuccessEnvelope. Destructive."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/resource_subscriptions/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("resource-subscriptions_list",
			mcplib.WithDescription("Show active subscriptions for the input resource. Requires view_sales scope. Optional: resource_name. Returns the SuccessEnvelope."),
			mcplib.WithString("resource_name", mcplib.Description("Resource name")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/resource_subscriptions", []mcpParamBinding{{PublicName: "resource_name", WireName: "resource_name", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sales_get",
			mcplib.WithDescription("Retrieve details of a sale. Requires view_sales scope. Required: id. Returns the SuccessEnvelope."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/sales/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("sales_list",
			mcplib.WithDescription("Retrieve successful sales by the authenticated user. Requires view_sales scope. Optional: after, before, product_id (plus 5 more). Returns the SuccessEnvelope."),
			mcplib.WithString("after", mcplib.Description("Return records after this date.")),
			mcplib.WithString("before", mcplib.Description("Return records before this date.")),
			mcplib.WithString("product_id", mcplib.Description("Product id")),
			mcplib.WithString("email", mcplib.Description("Email")),
			mcplib.WithString("order_id", mcplib.Description("Order id")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("license_key", mcplib.Description("License key")),
			mcplib.WithString("page_key", mcplib.Description("Opaque pagination cursor returned as next_page_key.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/sales", []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "product_id", WireName: "product_id", Location: "query"}, {PublicName: "email", WireName: "email", Location: "query"}, {PublicName: "order_id", WireName: "order_id", Location: "query"}, {PublicName: "name", WireName: "name", Location: "query"}, {PublicName: "license_key", WireName: "license_key", Location: "query"}, {PublicName: "page_key", WireName: "page_key", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sales_mark-as-shipped_mark-sale-as-shipped",
			mcplib.WithDescription("Mark a sale as shipped. Requires mark_sales_as_shipped scope. Required: id. Optional: tracking_url. Returns the updated SuccessEnvelope."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("tracking_url", mcplib.Description("Tracking url")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/sales/{id}/mark_as_shipped", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "tracking_url", WireName: "tracking_url", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("sales_refund_sale",
			mcplib.WithDescription("Refund a sale. Requires refund_sales or edit_sales scope. Required: id. Optional: amount_cents, comment. Returns the updated SuccessEnvelope."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("amount_cents", mcplib.Description("Amount cents")),
			mcplib.WithString("comment", mcplib.Description("Comment")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/sales/{id}/refund", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "amount_cents", WireName: "amount_cents", Location: "body"}, {PublicName: "comment", WireName: "comment", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("sales_resend-receipt_resend_receipt",
			mcplib.WithDescription("Resend the purchase receipt to the customer's email. Requires edit_sales scope. Required: id. Returns the new SuccessEnvelope."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/sales/{id}/resend_receipt", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_get",
			mcplib.WithDescription("Retrieve details of a subscriber. Requires view_sales scope. Required: id. Returns the SuccessEnvelope."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/subscribers/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("tax-forms_list",
			mcplib.WithDescription("Retrieve 1099 tax forms for the authenticated user. Requires view_tax_data scope. Optional: year. Returns the SuccessEnvelope."),
			mcplib.WithString("year", mcplib.Description("Optional four-digit tax year.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/tax_forms", []mcpParamBinding{{PublicName: "year", WireName: "year", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("user_get",
			mcplib.WithDescription("Retrieve the authenticated user's data. Returns the SuccessEnvelope."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/user", []mcpParamBinding{}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export GUMROAD_ACCESS_TOKEN=<your-key>" +
					"\n      Run 'gumroad-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export GUMROAD_ACCESS_TOKEN=<your-key>" +
					"\n      Run 'gumroad-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export GUMROAD_ACCESS_TOKEN=<your-key>" +
					"\n      Run 'gumroad-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "gumroad-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "gumroad-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "gumroad",
		"description": "Gumroad OAuth 2.0 API for products, sales, subscribers, licenses, payouts, tax data, and resource subscriptions.",
		"archetype":   "payments",
		"tool_count":  52,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion gumroad-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "GUMROAD_ACCESS_TOKEN",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "earnings",
				"description": "Manage earnings",
				"endpoints":   []string{"get"},
			},
			{
				"name":        "files",
				"description": "Manage files",
				"endpoints":   []string{"abort", "complete", "presign"},
				"searchable":  true,
			},
			{
				"name":        "licenses",
				"description": "Manage licenses",
				"endpoints":   []string{"decrement-uses-count", "disable", "enable", "rotate", "verify"},
				"searchable":  true,
			},
			{
				"name":        "payouts",
				"description": "Manage payouts",
				"endpoints":   []string{"get", "get-upcoming", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "products",
				"description": "Manage products",
				"endpoints":   []string{"create", "delete", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "resource-subscriptions",
				"description": "Manage resource subscriptions",
				"endpoints":   []string{"create", "delete", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "sales",
				"description": "Manage sales",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "subscribers",
				"description": "Manage subscribers",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
			{
				"name":        "tax-forms",
				"description": "Manage tax forms",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "user",
				"description": "Manage user",
				"endpoints":   []string{"get"},
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Seller Snapshot Sync", "command": "sync --resources products,sales,subscribers,payouts --latest-only --json", "description": "Refresh a local SQLite snapshot of the seller-facing Gumroad resources that agents inspect most often.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Cross-Resource Search", "command": "search", "description": "Search locally synced products, sales, subscribers, payouts, and tax records through one command.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Local Gumroad Analytics", "command": "analytics", "description": "Group and count synced Gumroad records locally for fast summaries without additional API traffic.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Polling Tail for Operational Events", "command": "tail", "description": "Poll selected Gumroad resources and emit NDJSON changes for scripts or agent monitors.", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Seller Snapshot Sync", "insight": ""},
			{"topic": "Cross-Resource Search", "insight": ""},
			{"topic": "Local Gumroad Analytics", "insight": ""},
			{"topic": "Polling Tail for Operational Events", "insight": ""},
			{"topic": "Financial data", "insight": "Always use read-only operations for financial queries. Never use create/update tools for payment data without explicit user confirmation."},
			{"topic": "Reconciliation", "insight": "For reconciliation tasks, sync first then use sql for cross-referencing. API pagination over financial records is slow and rate-limited."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
