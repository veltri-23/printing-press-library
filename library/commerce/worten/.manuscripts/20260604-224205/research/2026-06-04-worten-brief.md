# Worten Brief

Run `20260604-224205` packages a focused Worten retailer CLI derived from the working housebuy operator surface.

The CLI's differentiated layer is the normalized command set on top of the raw endpoint tree: `resolve`, `product`, `buyer`, `stock`, `specs`, `suggest`, `search`, and `snapshot`.

The publish contract for this run depends on four novel commands being explicit in the manifest: canonical product resolution, normalized buyer view, persistent snapshots, and normalized stock context.

The source tree was promoted with English-only user-facing command examples and validator-visible normalized Cobra commands so publish-time static verification matches the real CLI surface.
