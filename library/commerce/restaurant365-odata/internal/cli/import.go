// Copyright 2026 dhilip-subramanian. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newImportCmd(_ *rootFlags) *cobra.Command {
	var inputFile string
	var dryRun bool
	var batchSize int

	cmd := &cobra.Command{
		Use:    "import <resource>",
		Short:  "Disabled for Restaurant365 OData read-only views",
		Hidden: true,
		Long: `Import data from a JSONL file by issuing POST requests for each record.
Each line must be a valid JSON object. Failed records are logged to stderr
but do not stop the import.`,
		Example: `  # Import from a JSONL file
  restaurant365-odata-pp-cli import <resource> --input data.jsonl

  # Dry-run to preview without sending
  restaurant365-odata-pp-cli import <resource> --input data.jsonl --dry-run

  # Import from stdin
  cat data.jsonl | restaurant365-odata-pp-cli import <resource> --input -`,
		Args: cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			return usageErr(fmt.Errorf("Restaurant365 OData exposes read-only reporting views; import is disabled"))
		},
	}

	cmd.Flags().StringVarP(&inputFile, "input", "i", "", "Input JSONL file path (use - for stdin)")
	_ = cmd.MarkFlagRequired("input")
	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview import without sending requests")
	cmd.Flags().IntVar(&batchSize, "batch-size", 1, "Records per batch (future: batch API support)")

	return cmd
}
