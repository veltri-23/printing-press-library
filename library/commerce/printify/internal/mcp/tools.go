// Copyright 2026 horknfbr and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/commerce/printify/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/commerce/printify/internal/client"
	"github.com/mvanhorn/printing-press-library/library/commerce/printify/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/commerce/printify/internal/config"
	"github.com/mvanhorn/printing-press-library/library/commerce/printify/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/commerce/printify/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-alist-of-all-print-providers-that-fulfill-orders-for-aspecific-blueprint",
			mcplib.WithDescription("Retrieve a list of all print providers that fulfill orders for a specific blueprint. Required: blueprint_id. Returns array of PrintProviderSimple."),
			mcplib.WithString("blueprint_id", mcplib.Required(), mcplib.Description("Blueprint id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/blueprints/{blueprint_id}/print_providers.json", true, false, nil, []mcpParamBinding{{PublicName: "blueprint_id", WireName: "blueprint_id", Location: "path"}}, []string{"blueprint_id"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-alist-of-available-print-providers",
			mcplib.WithDescription("Retrieves the list of blueprints in the catalog to explore from. Returns array of PrintProvider."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/print_providers.json", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-alist-of-variants-of-ablueprint-from-aspecific-print-provider",
			mcplib.WithDescription("Retrieves the list of of variants options for the Print Provider and Blueprint. Those form the set of options available for customization Product (Blueprint) on particular manufacturer (Print Provider). Required: blueprint_id, print_provider_id. Optional: show-out-of-stock. Returns the VariantsResponse."),
			mcplib.WithString("blueprint_id", mcplib.Required(), mcplib.Description("Blueprint id")),
			mcplib.WithString("print_provider_id", mcplib.Required(), mcplib.Description("Print provider id")),
			mcplib.WithString("show-out-of-stock", mcplib.Description("Depending on the value, it shows all variants or only those not out of stock.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/blueprints/{blueprint_id}/print_providers/{print_provider_id}/variants.json", true, false, nil, []mcpParamBinding{{PublicName: "blueprint_id", WireName: "blueprint_id", Location: "path"}, {PublicName: "print_provider_id", WireName: "print_provider_id", Location: "path"}, {PublicName: "show-out-of-stock", WireName: "show-out-of-stock", Location: "query"}}, []string{"blueprint_id", "print_provider_id"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-aspecific-blueprint",
			mcplib.WithDescription("Retrieves the list of blueprints in the catalog to explore from. Required: blueprint_id. Returns the Blueprint."),
			mcplib.WithString("blueprint_id", mcplib.Required(), mcplib.Description("Blueprint id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/blueprints/{blueprint_id}.json", true, false, nil, []mcpParamBinding{{PublicName: "blueprint_id", WireName: "blueprint_id", Location: "path"}}, []string{"blueprint_id"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-aspecific-print-provider",
			mcplib.WithDescription("Retrieves the list of blueprints in the catalog to explore from. Required: print_provider_id. Returns the PrintProvider."),
			mcplib.WithString("print_provider_id", mcplib.Required(), mcplib.Description("Print provider id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/print_providers/{print_provider_id}.json", true, false, nil, []mcpParamBinding{{PublicName: "print_provider_id", WireName: "print_provider_id", Location: "path"}}, []string{"print_provider_id"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-available-shipping-list-information",
			mcplib.WithDescription("Retrieves the list of print providers avilable for the Blueprint. Required: blueprint_id, print_provider_id. Returns array of ShippingMethodItem."),
			mcplib.WithString("blueprint_id", mcplib.Required(), mcplib.Description("Blueprint id")),
			mcplib.WithString("print_provider_id", mcplib.Required(), mcplib.Description("Print provider id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/catalog/blueprints/{blueprint_id}/print_providers/{print_provider_id}/shipping.json", true, false, nil, []mcpParamBinding{{PublicName: "blueprint_id", WireName: "blueprint_id", Location: "path"}, {PublicName: "print_provider_id", WireName: "print_provider_id", Location: "path"}}, []string{"blueprint_id", "print_provider_id"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-economy-shipping-method-information",
			mcplib.WithDescription("Retrieves the list of print providers available for the Blueprint. Required: blueprint_id."),
			mcplib.WithString("blueprint_id", mcplib.Required(), mcplib.Description("Blueprint id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/blueprints/{blueprint_id}/print_providers/shipping/economy.json", true, true, map[string]string{"Accept": "application/octet-stream"}, []mcpParamBinding{{PublicName: "blueprint_id", WireName: "blueprint_id", Location: "path"}}, []string{"blueprint_id"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-express-shipping-method-information",
			mcplib.WithDescription("Retrieves the list of print providers available for the Blueprint. Required: blueprint_id."),
			mcplib.WithString("blueprint_id", mcplib.Required(), mcplib.Description("Blueprint id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/blueprints/{blueprint_id}/print_providers/shipping/express.json", true, true, map[string]string{"Accept": "application/octet-stream"}, []mcpParamBinding{{PublicName: "blueprint_id", WireName: "blueprint_id", Location: "path"}}, []string{"blueprint_id"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-priority-shipping-method-information",
			mcplib.WithDescription("Retrieves the list of print providers available for the Blueprint. Required: blueprint_id."),
			mcplib.WithString("blueprint_id", mcplib.Required(), mcplib.Description("Blueprint id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/blueprints/{blueprint_id}/print_providers/shipping/priority.json", true, true, map[string]string{"Accept": "application/octet-stream"}, []mcpParamBinding{{PublicName: "blueprint_id", WireName: "blueprint_id", Location: "path"}}, []string{"blueprint_id"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-shipping-information",
			mcplib.WithDescription("Retrieves the list of print providers avilable for the Blueprint. Required: blueprint_id. Returns the ShippingResponse."),
			mcplib.WithString("blueprint_id", mcplib.Required(), mcplib.Description("Blueprint id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/blueprints/{blueprint_id}/print_providers/shipping.json", true, false, nil, []mcpParamBinding{{PublicName: "blueprint_id", WireName: "blueprint_id", Location: "path"}}, []string{"blueprint_id"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieve-specific-shipping-method-information",
			mcplib.WithDescription("Retrieves the list of print providers avilable for the Blueprint. Required: blueprint_id. Returns array of VariantShippingItem."),
			mcplib.WithString("blueprint_id", mcplib.Required(), mcplib.Description("Blueprint id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/blueprints/{blueprint_id}/print_providers/shipping/standard.json", true, false, nil, []mcpParamBinding{{PublicName: "blueprint_id", WireName: "blueprint_id", Location: "path"}}, []string{"blueprint_id"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_retrieves-list-of-blueprints-in-the",
			mcplib.WithDescription("Retrieves the list of blueprints in the catalog to explore from. Returns array of Blueprint."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/catalog/blueprints.json", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("shops_connection-json_disconnect-ashop",
			mcplib.WithDescription("Disconnect a sales-channel shop connection from Printify after confirming the target shop ID. Required: shop_id. Destructive."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/shops/{shop_id}/connection.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}}, []string{"shop_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_orders_calculate-the-shipping-cost-of-an",
			mcplib.WithDescription("Calculate the shipping cost of an order. Required: shop_id, address_to, line_items. Returns the new ShippingCosts."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("address_to", mcplib.Required(), mcplib.Description("Address to")),
			mcplib.WithString("line_items", mcplib.Required(), mcplib.Description("Line items")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/orders/shipping.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "address_to", WireName: "address_to", Location: "body"}, {PublicName: "line_items", WireName: "line_items", Location: "body"}}, []string{"shop_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_orders_cancel-an",
			mcplib.WithDescription("Cancel an order. Required: shop_id, order_id. Returns the new Order."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("order_id", mcplib.Required(), mcplib.Description("Order id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/orders/{order_id}/cancel.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "order_id", WireName: "order_id", Location: "path"}}, []string{"shop_id", "order_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_orders_get-details-by-id",
			mcplib.WithDescription("Get order details by ID. Required: shop_id, order_id. Returns the Order."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("order_id", mcplib.Required(), mcplib.Description("Order id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/shops/{shop_id}/orders/{order_id}.json", true, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "order_id", WireName: "order_id", Location: "path"}}, []string{"shop_id", "order_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_orders_send-an-existing-to-production",
			mcplib.WithDescription("Send an existing order to production. Required: shop_id, order_id. Returns the new OrderCreated."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("order_id", mcplib.Required(), mcplib.Description("Order id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/orders/{order_id}/send_to_production.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "order_id", WireName: "order_id", Location: "path"}}, []string{"shop_id", "order_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_orders_submit-aprintify-express",
			mcplib.WithDescription("Submit a Printify Express order. Required: shop_id, address_to, line_items. Optional: external_id, label, send_shipping_notification (plus 1 more). Returns array of ExpressOrderItem."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("address_to", mcplib.Required(), mcplib.Description("Address to")),
			mcplib.WithString("external_id", mcplib.Description("External id")),
			mcplib.WithString("label", mcplib.Description("Label")),
			mcplib.WithString("line_items", mcplib.Required(), mcplib.Description("Line items")),
			mcplib.WithBoolean("send_shipping_notification", mcplib.Description("Send shipping notification")),
			mcplib.WithNumber("shipping_method", mcplib.Description("Shipping method")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/orders/express.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "address_to", WireName: "address_to", Location: "body"}, {PublicName: "external_id", WireName: "external_id", Location: "body"}, {PublicName: "label", WireName: "label", Location: "body"}, {PublicName: "line_items", WireName: "line_items", Location: "body"}, {PublicName: "send_shipping_notification", WireName: "send_shipping_notification", Location: "body"}, {PublicName: "shipping_method", WireName: "shipping_method", Location: "body"}}, []string{"shop_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_orders-json_retrieve-alist-of-orders",
			mcplib.WithDescription("Retrieve a list of orders. Required: shop_id. Optional: limit, page, status (plus 1 more). Returns the OrdersJsonRetrieveAlistOfOrdersResponse."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("limit", mcplib.Description("Results per page (default: 10, maximum: 10)")),
			mcplib.WithString("page", mcplib.Description("Paginate through list of results")),
			mcplib.WithString("status", mcplib.Description("Filter results by order status")),
			mcplib.WithString("sku", mcplib.Description("Filter results by product SKU - response will contain only those orders that have at least one product with passed SKU")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/shops/{shop_id}/orders.json", true, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "status", WireName: "status", Location: "query"}, {PublicName: "sku", WireName: "sku", Location: "query"}}, []string{"shop_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_orders-json_submit-an-order",
			mcplib.WithDescription("Submit an order. Required: shop_id, address_to, line_items. Optional: external_id, is_economy_shipping, is_printify_express (plus 3 more). Returns the new OrderCreated."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("address_to", mcplib.Required(), mcplib.Description("Address to")),
			mcplib.WithString("external_id", mcplib.Description("External id")),
			mcplib.WithBoolean("is_economy_shipping", mcplib.Description("Is economy shipping")),
			mcplib.WithBoolean("is_printify_express", mcplib.Description("Is printify express")),
			mcplib.WithString("label", mcplib.Description("Label")),
			mcplib.WithString("line_items", mcplib.Required(), mcplib.Description("Line items")),
			mcplib.WithBoolean("send_shipping_notification", mcplib.Description("Send shipping notification")),
			mcplib.WithNumber("shipping_method", mcplib.Description("Shipping method")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/orders.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "address_to", WireName: "address_to", Location: "body"}, {PublicName: "external_id", WireName: "external_id", Location: "body"}, {PublicName: "is_economy_shipping", WireName: "is_economy_shipping", Location: "body"}, {PublicName: "is_printify_express", WireName: "is_printify_express", Location: "body"}, {PublicName: "label", WireName: "label", Location: "body"}, {PublicName: "line_items", WireName: "line_items", Location: "body"}, {PublicName: "send_shipping_notification", WireName: "send_shipping_notification", Location: "body"}, {PublicName: "shipping_method", WireName: "shipping_method", Location: "body"}}, []string{"shop_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_products_delete-aproduct",
			mcplib.WithDescription("Delete a product. Required: shop_id, product_id. Destructive."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/shops/{shop_id}/products/{product_id}.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "product_id", WireName: "product_id", Location: "path"}}, []string{"shop_id", "product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_products_notify-that-aproduct-has-been-unpublished",
			mcplib.WithDescription("Notify that a product has been unpublished. Required: shop_id, product_id. Optional: limit, page."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("limit", mcplib.Description("Results per page (default: 10, maximum: 50)")),
			mcplib.WithString("page", mcplib.Description("Paginate through list of results")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/products/{product_id}/unpublish.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{"shop_id", "product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_products_publish-aproduct",
			mcplib.WithDescription("Publish a product. Required: shop_id, product_id. Optional: limit, page, description (plus 6 more)."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("limit", mcplib.Description("Results per page (default: 10, maximum: 50)")),
			mcplib.WithString("page", mcplib.Description("Paginate through list of results")),
			mcplib.WithBoolean("description", mcplib.Description("Description")),
			mcplib.WithBoolean("images", mcplib.Description("Images")),
			mcplib.WithBoolean("keyFeatures", mcplib.Description("Key features")),
			mcplib.WithBoolean("shipping_template", mcplib.Description("Shipping template")),
			mcplib.WithBoolean("tags", mcplib.Description("Tags")),
			mcplib.WithBoolean("title", mcplib.Description("Title")),
			mcplib.WithBoolean("variants", mcplib.Description("Variants")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/products/{product_id}/publish.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "images", WireName: "images", Location: "body"}, {PublicName: "keyFeatures", WireName: "keyFeatures", Location: "body"}, {PublicName: "shipping_template", WireName: "shipping_template", Location: "body"}, {PublicName: "tags", WireName: "tags", Location: "body"}, {PublicName: "title", WireName: "title", Location: "body"}, {PublicName: "variants", WireName: "variants", Location: "body"}}, []string{"shop_id", "product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_products_retrieve-aproduct",
			mcplib.WithDescription("Retrieve a product. Required: shop_id, product_id. Returns the Product."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/shops/{shop_id}/products/{product_id}.json", true, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "product_id", WireName: "product_id", Location: "path"}}, []string{"shop_id", "product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_products_retrieve-gpsr-information",
			mcplib.WithDescription("Retrieve product GPSR information. Required: shop_id, product_id. Returns array of GpsrInfo."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/shops/{shop_id}/products/{product_id}/gpsr.json", true, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "product_id", WireName: "product_id", Location: "path"}}, []string{"shop_id", "product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_products_set-publish-status-to-failed",
			mcplib.WithDescription("Set product publish status to failed. Required: shop_id, product_id. Optional: limit, page, reason."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("limit", mcplib.Description("Results per page (default: 10, maximum: 50)")),
			mcplib.WithString("page", mcplib.Description("Paginate through list of results")),
			mcplib.WithString("reason", mcplib.Description("Reason")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/products/{product_id}/publishing_failed.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "reason", WireName: "reason", Location: "body"}}, []string{"shop_id", "product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_products_set-publish-status-to-succeeded",
			mcplib.WithDescription("Set product publish status to succeeded. Required: shop_id, product_id. Optional: limit, page, external."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("limit", mcplib.Description("Results per page (default: 10, maximum: 50)")),
			mcplib.WithString("page", mcplib.Description("Paginate through list of results")),
			mcplib.WithString("external", mcplib.Description("External")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/products/{product_id}/publishing_succeeded.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "external", WireName: "external", Location: "body"}}, []string{"shop_id", "product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_products_update-aproduct",
			mcplib.WithDescription("Update a product. Required: shop_id, product_id. Optional: blueprint_id, description, print_areas (plus 4 more). Returns the updated Product."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("product_id", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithNumber("blueprint_id", mcplib.Description("Blueprint id")),
			mcplib.WithString("description", mcplib.Description("Description")),
			mcplib.WithString("print_areas", mcplib.Description("Print areas")),
			mcplib.WithNumber("print_provider_id", mcplib.Description("Print provider id")),
			mcplib.WithString("safety_information", mcplib.Description("Safety information")),
			mcplib.WithString("title", mcplib.Description("Title")),
			mcplib.WithString("variants", mcplib.Description("Variants")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v1/shops/{shop_id}/products/{product_id}.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "product_id", WireName: "product_id", Location: "path"}, {PublicName: "blueprint_id", WireName: "blueprint_id", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "print_areas", WireName: "print_areas", Location: "body"}, {PublicName: "print_provider_id", WireName: "print_provider_id", Location: "body"}, {PublicName: "safety_information", WireName: "safety_information", Location: "body"}, {PublicName: "title", WireName: "title", Location: "body"}, {PublicName: "variants", WireName: "variants", Location: "body"}}, []string{"shop_id", "product_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_products-json_create-anew-product",
			mcplib.WithDescription("Create a new product. Required: shop_id, blueprint_id, print_areas, print_provider_id, title, variants. Optional: description, safety_information. Returns the new Product."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithNumber("blueprint_id", mcplib.Required(), mcplib.Description("Blueprint id")),
			mcplib.WithString("description", mcplib.Description("Description")),
			mcplib.WithString("print_areas", mcplib.Required(), mcplib.Description("Print areas")),
			mcplib.WithNumber("print_provider_id", mcplib.Required(), mcplib.Description("Print provider id")),
			mcplib.WithString("safety_information", mcplib.Description("Safety information")),
			mcplib.WithString("title", mcplib.Required(), mcplib.Description("Title")),
			mcplib.WithString("variants", mcplib.Required(), mcplib.Description("Variants")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/products.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "blueprint_id", WireName: "blueprint_id", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "print_areas", WireName: "print_areas", Location: "body"}, {PublicName: "print_provider_id", WireName: "print_provider_id", Location: "body"}, {PublicName: "safety_information", WireName: "safety_information", Location: "body"}, {PublicName: "title", WireName: "title", Location: "body"}, {PublicName: "variants", WireName: "variants", Location: "body"}}, []string{"shop_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_products-json_retrieve-alist-of-products",
			mcplib.WithDescription("Retrieve a list of products. Required: shop_id. Optional: limit, page. Returns the ProductsJsonRetrieveAlistOfProductsResponse."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("limit", mcplib.Description("Results per page (default: 10, maximum: 50)")),
			mcplib.WithString("page", mcplib.Description("Paginate through list of results")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/shops/{shop_id}/products.json", true, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{"shop_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_webhooks_delete-awebhook",
			mcplib.WithDescription("Delete a webhook. Required: shop_id, webhook_id. Optional: host. Returns the WebhookDeleted. Destructive."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("webhook_id", mcplib.Required(), mcplib.Description("Webhook id")),
			mcplib.WithString("host", mcplib.Description("Host works as a safeguard: the deletion will only be performed if the host matches the webhook's host")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/shops/{shop_id}/webhooks/{webhook_id}.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "webhook_id", WireName: "webhook_id", Location: "path"}, {PublicName: "host", WireName: "host", Location: "query"}}, []string{"shop_id", "webhook_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_webhooks_modify-awebhook",
			mcplib.WithDescription("Modify a webhook. Required: shop_id, webhook_id. Optional: url. Returns the updated Webhook."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("webhook_id", mcplib.Required(), mcplib.Description("Webhook id")),
			mcplib.WithString("url", mcplib.Description("Url")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v1/shops/{shop_id}/webhooks/{webhook_id}.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "webhook_id", WireName: "webhook_id", Location: "path"}, {PublicName: "url", WireName: "url", Location: "body"}}, []string{"shop_id", "webhook_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_webhooks_simulate",
			mcplib.WithDescription("Trigger a Printify webhook simulation for a specific shop and webhook subscription. Required: shop_id, webhook_id."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("shop_id")),
			mcplib.WithString("webhook_id", mcplib.Required(), mcplib.Description("webhook_id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/webhooks/{webhook_id}/simulate", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "webhook_id", WireName: "webhook_id", Location: "path"}}, []string{"shop_id", "webhook_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_webhooks-json_create-anew-webhook",
			mcplib.WithDescription("Create a new webhook. Required: shop_id. Optional: topic, url. Returns the new Webhook."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("topic", mcplib.Description("Topic")),
			mcplib.WithString("url", mcplib.Description("Url")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/shops/{shop_id}/webhooks.json", false, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "topic", WireName: "topic", Location: "body"}, {PublicName: "url", WireName: "url", Location: "body"}}, []string{"shop_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops_webhooks-json_retrieve-alist-of-webhooks",
			mcplib.WithDescription("Retrieve a list of webhooks. Required: shop_id. Optional: limit, page. Returns array of Webhook."),
			mcplib.WithString("shop_id", mcplib.Required(), mcplib.Description("Shop id")),
			mcplib.WithString("limit", mcplib.Description("Results per page (default: 10, maximum: 100)")),
			mcplib.WithString("page", mcplib.Description("Paginate through list of results")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/shops/{shop_id}/webhooks.json", true, false, nil, []mcpParamBinding{{PublicName: "shop_id", WireName: "shop_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{"shop_id"}),
	)
	s.AddTool(
		mcplib.NewTool("shops-json_retrieve-list-of-shops-in-aprintify-account",
			mcplib.WithDescription("This will return the list of available merchant shops (IDs and titles). Returns array of Shop."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/shops.json", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("uploads_an-image",
			mcplib.WithDescription("Upload image content to Printify and return the reusable upload ID for product manifests."),
			mcplib.WithString("body_json", mcplib.Description("Request body as a JSON object string (this endpoint accepts a polymorphic schema: oneOf/anyOf)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/uploads/images.json", false, false, nil, []mcpParamBinding{{PublicName: "body_json", WireName: "body_json", Location: "body_json"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("uploads_retrieve-an-uploaded-image-by-id",
			mcplib.WithDescription("Retrieve an uploaded image by id. Required: image_id. Returns the Upload."),
			mcplib.WithString("image_id", mcplib.Required(), mcplib.Description("Image id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/uploads/{image_id}.json", true, false, nil, []mcpParamBinding{{PublicName: "image_id", WireName: "image_id", Location: "path"}}, []string{"image_id"}),
	)
	s.AddTool(
		mcplib.NewTool("uploads_archive-json_archive-an-uploaded-image",
			mcplib.WithDescription("Archive a Printify uploaded image by ID when it is no longer used by product drafts. Required: image_id."),
			mcplib.WithString("image_id", mcplib.Required(), mcplib.Description("Image id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/uploads/{image_id}/archive.json", false, false, nil, []mcpParamBinding{{PublicName: "image_id", WireName: "image_id", Location: "path"}}, []string{"image_id"}),
	)
	s.AddTool(
		mcplib.NewTool("uploads-json_retrieve-alist-of-uploaded-images",
			mcplib.WithDescription("Retrieve a list of uploaded images. Optional: limit, page. Returns the UploadsJsonRetrieveAlistOfUploadedImagesResponse."),
			mcplib.WithString("limit", mcplib.Description("Results per page (default: 10, maximum: 100)")),
			mcplib.WithString("page", mcplib.Description("Paginate through list of results")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/uploads.json", true, false, nil, []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		// bodyJSONOverride holds an opaque body for endpoints whose request
		// schema is a oneOf/anyOf and was wired with a single body_json input.
		// When set, it replaces the bodyArgs map for POST/PUT/PATCH.
		var bodyJSONOverride json.RawMessage
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			case "body_json":
				if s, ok := v.(string); ok && s != "" {
					var parsed any
					if err := json.Unmarshal([]byte(s), &parsed); err != nil {
						return mcplib.NewToolResultError("body_json must be a valid JSON object: " + err.Error()), nil
					}
					if _, isMap := parsed.(map[string]any); !isMap {
						return mcplib.NewToolResultError(fmt.Sprintf("body_json must be a JSON object, got JSON %T", parsed)), nil
					}
					bodyJSONOverride = json.RawMessage(s)
				}
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(bodyJSONOverride) > 0 {
				if len(headers) > 0 {
					if readOnly {
						data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyJSONOverride, headers)
					} else {
						data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyJSONOverride, headers)
					}
					break
				}
				if readOnly {
					data, _, err = c.PostQueryWithParams(ctx, path, params, bodyJSONOverride)
				} else {
					data, _, err = c.PostWithParams(ctx, path, params, bodyJSONOverride)
				}
				break
			}
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(bodyJSONOverride) > 0 {
				if len(headers) > 0 {
					data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyJSONOverride, headers)
					break
				}
				data, _, err = c.PutWithParams(ctx, path, params, bodyJSONOverride)
				break
			}
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(bodyJSONOverride) > 0 {
				if len(headers) > 0 {
					data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyJSONOverride, headers)
					break
				}
				data, _, err = c.PatchWithParams(ctx, path, params, bodyJSONOverride)
				break
			}
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export PRINTIFY_BEARER_AUTH=<your-key>" +
					"\n      Run 'printify-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export PRINTIFY_BEARER_AUTH=<your-key>" +
					"\n      Run 'printify-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export PRINTIFY_BEARER_AUTH=<your-key>" +
					"\n      Run 'printify-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "printify-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "printify-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "printify",
		"description": "Create and audit Printify products with manifest-driven image uploads, personalization checks, and local proofing.",
		"archetype":   "generic",
		"tool_count":  40,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion printify-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "PRINTIFY_BEARER_AUTH",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "catalog",
				"description": "Browse the Printify catalog including blueprints, print providers, product variants, and shipping information.",
				"endpoints":   []string{"retrieve-alist-of-all-print-providers-that-fulfill-orders-for-aspecific-blueprint", "retrieve-alist-of-available-print-providers", "retrieve-alist-of-variants-of-ablueprint-from-aspecific-print-provider", "retrieve-aspecific-blueprint", "retrieve-aspecific-print-provider", "retrieve-available-shipping-list-information", "retrieve-economy-shipping-method-information", "retrieve-express-shipping-method-information", "retrieve-priority-shipping-method-information", "retrieve-shipping-information", "retrieve-specific-shipping-method-information", "retrieves-list-of-blueprints-in-the"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "shops",
				"description": "Manage Printify shops and shop connections. Retrieve shop information and disconnect shops from your account.",
				"endpoints":   []string{},
			},
			{
				"name":        "shops-json",
				"description": "Manage shops json",
				"endpoints":   []string{"retrieve-list-of-shops-in-aprintify-account"},
				"syncable":    true,
			},
			{
				"name":        "uploads",
				"description": "Upload and manage images and assets.",
				"endpoints":   []string{"an-image", "retrieve-an-uploaded-image-by-id"},
				"searchable":  true,
			},
			{
				"name":        "uploads-json",
				"description": "Manage uploads json",
				"endpoints":   []string{"retrieve-alist-of-uploaded-images"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass page parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Personalization Coverage Audit", "command": "personalization-audit", "description": "Audit a product for documented personalization placeholder fields, missing text/image setup", "rationale": "Requires correlating Printify product print areas, placeholder images", "via": "mcp-command-mirror"},
			{"name": "Placement Matrix", "command": "placement-matrix", "description": "Show variant, print-area, placeholder, uploaded image, x/y/scale/angle, and missing-placement rows for a product.", "rationale": "Requires joining products, variants, print areas, placeholder image IDs", "via": "mcp-command-mirror"},
			{"name": "Catalog Margin Matrix", "command": "catalog-margin-matrix", "description": "Join catalog variants and shipping data to estimate per-variant cost and margin at a target retail price.", "rationale": "Requires combining blueprint, provider, variant", "via": "mcp-command-mirror"},
			{"name": "Product Manifest Drift", "command": "product-drift", "description": "Compare an intended product manifest against the current Printify product payload after create or update.", "rationale": "Requires deterministic field-level comparison across manifests, product variants", "via": "mcp-command-mirror"},
			{"name": "Personalized Batch Manifest Compiler", "command": "personalization-batch", "description": "Expand a reusable product manifest and CSV rows into per-product manifests using documented image and text placeholder", "rationale": "Requires a Printify-aware manifest transform that preserves placeholder image/text semantics without guessing", "via": "mcp-command-mirror"},
			{"name": "Asset Reuse Map", "command": "asset-reuse", "description": "List uploaded images, where each is used, unused uploads, and products sharing the same artwork.", "rationale": "Requires a local join from upload inventory to product print-area image IDs across the account.", "via": "mcp-command-mirror"},
			{"name": "Fulfillment Risk Scan", "command": "fulfillment-risk", "description": "Flag open orders tied to risky product, variant, publish, or shipment states.", "rationale": "Requires joining orders, line items, products, product variants, publish status", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Personalization Coverage Audit", "insight": "Requires correlating Printify product print areas, placeholder images, and text metadata in one local view instead of reading raw nested payloads."},
			{"topic": "Placement Matrix", "insight": "Requires joining products, variants, print areas, placeholder image IDs, and uploads that the API returns through separate structures."},
			{"topic": "Catalog Margin Matrix", "insight": "Requires combining blueprint, provider, variant, and shipping tables that are expensive for an agent to reconcile manually."},
			{"topic": "Product Manifest Drift", "insight": "Requires deterministic field-level comparison across manifests, product variants, and print areas rather than a single API call."},
			{"topic": "Personalized Batch Manifest Compiler", "insight": "Requires a Printify-aware manifest transform that preserves placeholder image/text semantics without guessing undocumented UI settings."},
			{"topic": "Asset Reuse Map", "insight": "Requires a local join from upload inventory to product print-area image IDs across the account."},
			{"topic": "Fulfillment Risk Scan", "insight": "Requires joining orders, line items, products, product variants, publish status, and shipments into one operational check."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
