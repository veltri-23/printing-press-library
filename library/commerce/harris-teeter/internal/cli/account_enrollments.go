// Copyright 2026 Jonathan Moss and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAccountEnrollmentsCmd(flags *rootFlags) *cobra.Command {
	var flagOptionWithEnrollmentsOnly bool
	var flagProjections string

	cmd := &cobra.Command{
		Use:         "enrollments",
		Short:       "List membership enrollments and benefits for the current account.",
		Example:     "  harris-teeter-pp-cli account enrollments",
		Annotations: map[string]string{"pp:endpoint": "account.enrollments", "pp:method": "GET", "pp:path": "/atlas/v1/membership/v1/enrollments", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/atlas/v1/membership/v1/enrollments"
			params := map[string]string{}
			if flagOptionWithEnrollmentsOnly != false {
				params["option.withEnrollmentsOnly"] = fmt.Sprintf("%v", flagOptionWithEnrollmentsOnly)
			}
			if flagProjections != "" {
				params["projections"] = fmt.Sprintf("%v", flagProjections)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "account", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagOptionWithEnrollmentsOnly, "enrollments-only", true, "Return only active enrollments.")
	cmd.Flags().StringVar(&flagProjections, "projections", "benefits", "Enrollment response projection.")

	return cmd
}
