// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newConsolidationsDeleteShipmentCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountNumber string
	var bodyConsolidationKey string
	var bodyShipmentKey string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "delete-shipment",
		Short:       "Remove a shipment from a consolidation",
		Example:     "  fedex-pp-cli consolidations delete_shipment",
		Annotations: map[string]string{"pp:endpoint": "consolidations.delete_shipment"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("account-number") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "account-number")
				}
				if !cmd.Flags().Changed("consolidation-key") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "consolidation-key")
				}
				if !cmd.Flags().Changed("shipment-key") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "shipment-key")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ship/v1/consolidations/shipments/delete"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccountNumber != "" {
					var parsedAccountNumber any
					if err := json.Unmarshal([]byte(bodyAccountNumber), &parsedAccountNumber); err != nil {
						return fmt.Errorf("parsing --account-number JSON: %w", err)
					}
					body["accountNumber"] = parsedAccountNumber
				}
				if bodyConsolidationKey != "" {
					var parsedConsolidationKey any
					if err := json.Unmarshal([]byte(bodyConsolidationKey), &parsedConsolidationKey); err != nil {
						return fmt.Errorf("parsing --consolidation-key JSON: %w", err)
					}
					body["consolidationKey"] = parsedConsolidationKey
				}
				if bodyShipmentKey != "" {
					var parsedShipmentKey any
					if err := json.Unmarshal([]byte(bodyShipmentKey), &parsedShipmentKey); err != nil {
						return fmt.Errorf("parsing --shipment-key JSON: %w", err)
					}
					body["shipmentKey"] = parsedShipmentKey
				}
			}
			data, statusCode, err := c.Put(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "consolidations",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAccountNumber, "account-number", "", "")
	cmd.Flags().StringVar(&bodyConsolidationKey, "consolidation-key", "", "")
	cmd.Flags().StringVar(&bodyShipmentKey, "shipment-key", "", "")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
