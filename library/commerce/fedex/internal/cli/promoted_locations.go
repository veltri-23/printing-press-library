// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newLocationsPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyLocationsSummaryRequestedAddress string
	var bodyLocationsSummaryGeographicCoordinates string
	var bodySearchPageDetails string
	var bodyLocationAttributesToReturn string
	var bodyDistance string
	var bodySortDetail string

	cmd := &cobra.Command{
		Use:         "locations",
		Short:       "Find FedEx locations near an address or postal code",
		Long:        "Shortcut for 'locations find'. Find FedEx locations near an address or postal code",
		Example:     "  fedex-pp-cli locations",
		Annotations: map[string]string{"pp:endpoint": "locations.find", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/location/v1/locations"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyLocationsSummaryRequestedAddress != "" {
				var parsedLocationsSummaryRequestedAddress any
				if err := json.Unmarshal([]byte(bodyLocationsSummaryRequestedAddress), &parsedLocationsSummaryRequestedAddress); err != nil {
					return fmt.Errorf("parsing --locations-summary-requested-address JSON: %w", err)
				}
				body["locationsSummaryRequestedAddress"] = parsedLocationsSummaryRequestedAddress
			}
			if bodyLocationsSummaryGeographicCoordinates != "" {
				var parsedLocationsSummaryGeographicCoordinates any
				if err := json.Unmarshal([]byte(bodyLocationsSummaryGeographicCoordinates), &parsedLocationsSummaryGeographicCoordinates); err != nil {
					return fmt.Errorf("parsing --locations-summary-geographic-coordinates JSON: %w", err)
				}
				body["locationsSummaryGeographicCoordinates"] = parsedLocationsSummaryGeographicCoordinates
			}
			if bodySearchPageDetails != "" {
				var parsedSearchPageDetails any
				if err := json.Unmarshal([]byte(bodySearchPageDetails), &parsedSearchPageDetails); err != nil {
					return fmt.Errorf("parsing --search-page-details JSON: %w", err)
				}
				body["searchPageDetails"] = parsedSearchPageDetails
			}
			if bodyLocationAttributesToReturn != "" {
				var parsedLocationAttributesToReturn any
				if err := json.Unmarshal([]byte(bodyLocationAttributesToReturn), &parsedLocationAttributesToReturn); err != nil {
					return fmt.Errorf("parsing --location-attributes-to-return JSON: %w", err)
				}
				body["locationAttributesToReturn"] = parsedLocationAttributesToReturn
			}
			if bodyDistance != "" {
				var parsedDistance any
				if err := json.Unmarshal([]byte(bodyDistance), &parsedDistance); err != nil {
					return fmt.Errorf("parsing --distance JSON: %w", err)
				}
				body["distance"] = parsedDistance
			}
			if bodySortDetail != "" {
				var parsedSortDetail any
				if err := json.Unmarshal([]byte(bodySortDetail), &parsedSortDetail); err != nil {
					return fmt.Errorf("parsing --sort-detail JSON: %w", err)
				}
				body["sortDetail"] = parsedSortDetail
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyLocationsSummaryRequestedAddress, "locations-summary-requested-address", "", "Search by address")
	cmd.Flags().StringVar(&bodyLocationsSummaryGeographicCoordinates, "locations-summary-geographic-coordinates", "", "Search by lat/long")
	cmd.Flags().StringVar(&bodySearchPageDetails, "search-page-details", "", "{resultsPerPage, pageNumber}")
	cmd.Flags().StringVar(&bodyLocationAttributesToReturn, "location-attributes-to-return", "", "Array of attribute filters (PACKAGE_PICKUP, COPY_AND_PRINT, NOTARY_SERVICES, etc.)")
	cmd.Flags().StringVar(&bodyDistance, "distance", "", "{value, units (KM/MI)}")
	cmd.Flags().StringVar(&bodySortDetail, "sort-detail", "", "")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
