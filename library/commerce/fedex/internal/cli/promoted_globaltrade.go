// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGlobaltradePromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyRequestedShipment string
	var bodyRegulatoryControlParameter string

	cmd := &cobra.Command{
		Use:         "globaltrade",
		Short:       "Get regulatory details (HS codes, harmonized system, country restrictions)",
		Long:        "Shortcut for 'globaltrade regulatory'. Get regulatory details (HS codes, harmonized system, country restrictions)",
		Example:     "  fedex-pp-cli globaltrade",
		Annotations: map[string]string{"pp:endpoint": "globaltrade.regulatory"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("requested-shipment") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "requested-shipment")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/globaltrade/v1/shipments/regulatorydetails/retrieve"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyRequestedShipment != "" {
				var parsedRequestedShipment any
				if err := json.Unmarshal([]byte(bodyRequestedShipment), &parsedRequestedShipment); err != nil {
					return fmt.Errorf("parsing --requested-shipment JSON: %w", err)
				}
				body["requestedShipment"] = parsedRequestedShipment
			}
			if bodyRegulatoryControlParameter != "" {
				var parsedRegulatoryControlParameter any
				if err := json.Unmarshal([]byte(bodyRegulatoryControlParameter), &parsedRegulatoryControlParameter); err != nil {
					return fmt.Errorf("parsing --regulatory-control-parameter JSON: %w", err)
				}
				body["regulatoryControlParameter"] = parsedRegulatoryControlParameter
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyRequestedShipment, "requested-shipment", "", "")
	cmd.Flags().StringVar(&bodyRegulatoryControlParameter, "regulatory-control-parameter", "", "")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
