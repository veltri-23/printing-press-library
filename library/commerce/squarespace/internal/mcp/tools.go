// Copyright 2026 Zayd and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/commerce/squarespace/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/commerce/squarespace/internal/client"
	"github.com/mvanhorn/printing-press-library/library/commerce/squarespace/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/commerce/squarespace/internal/config"
	"github.com/mvanhorn/printing-press-library/library/commerce/squarespace/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/commerce/squarespace/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("1-0_adjust-inventory-stock-levels",
			mcplib.WithDescription("Adjusts stock quantities for product variants. Stock quantities can be added or subtracted, set with a given number, or marked as 'unlimited'. All quantity information is stored in InventoryItems. Optional: decrementOperations, incrementOperations, setFiniteOperations (plus 1 more)."),
			mcplib.WithString("decrementOperations", mcplib.Description("Optional; array of objects. Subtracts stock quantity for InventoryItems.")),
			mcplib.WithString("incrementOperations", mcplib.Description("Optional; array of objects. Increments stock quantity for InventoryItems.")),
			mcplib.WithString("setFiniteOperations", mcplib.Description("Optional; array of objects. Sets the exact stock quantity for InventoryItems.")),
			mcplib.WithString("setUnlimitedOperations", mcplib.Description("Optional; array of InventoryItem ids. Marks stock quantity as 'unlimited' for the given InventoryItems.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/1.0/commerce/inventory/adjustments", []mcpParamBinding{{PublicName: "decrementOperations", WireName: "decrementOperations", Location: "body"}, {PublicName: "incrementOperations", WireName: "incrementOperations", Location: "body"}, {PublicName: "setFiniteOperations", WireName: "setFiniteOperations", Location: "body"}, {PublicName: "setUnlimitedOperations", WireName: "setUnlimitedOperations", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_create-order",
			mcplib.WithDescription("Creates an order using information from a third-party sales channel. A successful request creates an Order resource. Required: channelName, createdOn, externalOrderReference, fulfillments, grandTotal, lineItems, priceTaxInterpretation. Optional: billingAddress, customerEmail, discountLines (plus 10 more). Returns the new Order."),
			mcplib.WithString("billingAddress", mcplib.Description("Customer's shipping address.")),
			mcplib.WithString("channelName", mcplib.Required(), mcplib.Description("Name of third-party sales channel.")),
			mcplib.WithString("createdOn", mcplib.Required(), mcplib.Description("ISO 8601 UTC date and time string. Represents the date and time when the order was placed through the third-party...")),
			mcplib.WithString("customerEmail", mcplib.Description("Email address provided at checkout on the third-party sales channel.")),
			mcplib.WithString("discountLines", mcplib.Description("Array of discount line items. Describes the promotions redeemed during checkout.")),
			mcplib.WithString("discountTotal", mcplib.Description("A monetary amount with currency code and decimal value")),
			mcplib.WithString("externalOrderReference", mcplib.Required(), mcplib.Description("Order reference identifier used by the third-party sales channel.")),
			mcplib.WithString("fulfilledOn", mcplib.Description("ISO 8601 UTC date and time string. Represents the moment the order was fulfilled. Required if fulfillmentStatus is...")),
			mcplib.WithString("fulfillmentStatus", mcplib.Description("Current fulfillment status of the order. Value may be PENDING or FULFILLED.")),
			mcplib.WithString("fulfillments", mcplib.Required(), mcplib.Description("Array of shipping fulfillments; up to 100 entries. Describes shipment information for the order.")),
			mcplib.WithString("grandTotal", mcplib.Required(), mcplib.Description("A monetary amount with currency code and decimal value")),
			mcplib.WithString("inventoryBehavior", mcplib.Description("Indicates whether to deduct stock quantity for the product variant upon Order creation. Values may be DEDUCT or...")),
			mcplib.WithString("lineItems", mcplib.Required(), mcplib.Description("Array of purchased line items; cannot be empty.")),
			mcplib.WithString("priceTaxInterpretation", mcplib.Required(), mcplib.Description("Indicates that lineItems.unitPricePaid includes tax. Values may be EXCLUSIVE or INCLUSIVE.")),
			mcplib.WithString("shippingAddress", mcplib.Description("Customer's shipping address.")),
			mcplib.WithString("shippingLines", mcplib.Description("Array of shipping line items. Describes the shipping options chosen at checkout. Currently accepts only one shipping...")),
			mcplib.WithString("shippingTotal", mcplib.Description("A monetary amount with currency code and decimal value")),
			mcplib.WithString("shopperFulfillmentNotificationBehavior", mcplib.Description("Indicates whether to send a fulfillment notification email to the customer. Value may be SEND or SKIP.")),
			mcplib.WithString("subtotal", mcplib.Description("A monetary amount with currency code and decimal value")),
			mcplib.WithString("taxTotal", mcplib.Description("A monetary amount with currency code and decimal value")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/1.0/commerce/orders", []mcpParamBinding{{PublicName: "billingAddress", WireName: "billingAddress", Location: "body"}, {PublicName: "channelName", WireName: "channelName", Location: "body"}, {PublicName: "createdOn", WireName: "createdOn", Location: "body"}, {PublicName: "customerEmail", WireName: "customerEmail", Location: "body"}, {PublicName: "discountLines", WireName: "discountLines", Location: "body"}, {PublicName: "discountTotal", WireName: "discountTotal", Location: "body"}, {PublicName: "externalOrderReference", WireName: "externalOrderReference", Location: "body"}, {PublicName: "fulfilledOn", WireName: "fulfilledOn", Location: "body"}, {PublicName: "fulfillmentStatus", WireName: "fulfillmentStatus", Location: "body"}, {PublicName: "fulfillments", WireName: "fulfillments", Location: "body"}, {PublicName: "grandTotal", WireName: "grandTotal", Location: "body"}, {PublicName: "inventoryBehavior", WireName: "inventoryBehavior", Location: "body"}, {PublicName: "lineItems", WireName: "lineItems", Location: "body"}, {PublicName: "priceTaxInterpretation", WireName: "priceTaxInterpretation", Location: "body"}, {PublicName: "shippingAddress", WireName: "shippingAddress", Location: "body"}, {PublicName: "shippingLines", WireName: "shippingLines", Location: "body"}, {PublicName: "shippingTotal", WireName: "shippingTotal", Location: "body"}, {PublicName: "shopperFulfillmentNotificationBehavior", WireName: "shopperFulfillmentNotificationBehavior", Location: "body"}, {PublicName: "subtotal", WireName: "subtotal", Location: "body"}, {PublicName: "taxTotal", WireName: "taxTotal", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_create-webhook-subscription",
			mcplib.WithDescription("Creates a webhook subscription. A successful request returns the created Webhook Subscription resource. Required: endpointUrl. Optional: topics."),
			mcplib.WithString("endpointUrl", mcplib.Required(), mcplib.Description("HTTPS URL to POST webhook notifications.")),
			mcplib.WithString("topics", mcplib.Description("List of event topics that trigger a webhook notification.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/1.0/webhook_subscriptions", []mcpParamBinding{{PublicName: "endpointUrl", WireName: "endpointUrl", Location: "body"}, {PublicName: "topics", WireName: "topics", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_delete-webhook-subscription",
			mcplib.WithDescription("Deletes a webhook subscription. Required: subscriptionId. Destructive."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Specifies the Webhook Subscription to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/1.0/webhook_subscriptions/{subscriptionId}", []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_fulfill-order",
			mcplib.WithDescription("Updates the status of a specific order to fulfilled, with options to include shipment information and send a customer notification. Required: id. Optional: shipments, shouldSendNotification."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Specifies the Order to update.")),
			mcplib.WithString("shipments", mcplib.Description("Array of shipment data.")),
			mcplib.WithString("shouldSendNotification", mcplib.Description("Indicates whether the customer should receive an email notification about the added shipments.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/1.0/commerce/orders/{id}/fulfillments", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "shipments", WireName: "shipments", Location: "body"}, {PublicName: "shouldSendNotification", WireName: "shouldSendNotification", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-documents-by-id",
			mcplib.WithDescription("Retrieves information for specific transaction Documents. The response contains up to 50 Documents. Multiple Documents can be retrieved by providing a comma-separated list of Document ids. Required: documentIds. Returns the TransactionListResponse."),
			mcplib.WithString("documentIds", mcplib.Required(), mcplib.Description("Specifies the Documents to retrieve. Multiple Documents can be retrieved by providing a comma-separated list of...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/commerce/transactions/{documentIds}", []mcpParamBinding{{PublicName: "documentIds", WireName: "documentIds", Location: "path"}}, []string{"documentIds"}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-documents-by-updated-on",
			mcplib.WithDescription("Retrieves all financial transactions for orders and donations. Per order and donation, the response groups transactions into a Document, contains up to 50 Documents ordered by their modification date (modifiedOn), and supports dynamic cursors for pagination. Optional: cursor, modifiedAfter, modifiedBefore (plus 1 more). Returns the PaginatedTransactionListResponse."),
			mcplib.WithString("cursor", mcplib.Description("Identifies where the next page of results should begin. Should be the value of pagination.nextPageCursor from a...")),
			mcplib.WithString("modifiedAfter", mcplib.Description("Time-boxes the request to Documents that were modified after a given ISO 8601 UTC date and time string...")),
			mcplib.WithString("modifiedBefore", mcplib.Description("Time-boxes the request to Documents that were modified before a given ISO 8601 UTC date and time string...")),
			mcplib.WithString("orderId", mcplib.Description("Order id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/commerce/transactions", []mcpParamBinding{{PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "modifiedAfter", WireName: "modifiedAfter", Location: "query"}, {PublicName: "modifiedBefore", WireName: "modifiedBefore", Location: "query"}, {PublicName: "orderId", WireName: "orderId", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-inventory-items",
			mcplib.WithDescription("Retrieves real-time stock information for all product variants. Stock information is stored in an InventoryItem for each product variant. The response contains up to 50 InventoryItems and supports dynamic cursors for pagination. Optional: cursor. Returns the PaginatedInventoryItemListResponse."),
			mcplib.WithString("cursor", mcplib.Description("Identifies where the next page of results should begin. Should be the value of pagination.nextPageCursor from a...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/commerce/inventory", []mcpParamBinding{{PublicName: "cursor", WireName: "cursor", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-member-profile",
			mcplib.WithDescription("Retrieves basic details about the Squarespace member who owns the provided OAuth token. Returns the MemberProfile."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/authorization/member", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-order",
			mcplib.WithDescription("Retrieves information for a specific order. The response contains order information in an Order. Required: id. Returns the Order."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Specifies the Order to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/commerce/orders/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-orders",
			mcplib.WithDescription("Retrieves information about all orders. Orders can be filtered by Customer ID and date ranges. The response contains order information in an Order, up to 50 Orders ordered by their modification date (modifiedOn), and supports dynamic cursors for pagination. Optional: customerId, modifiedAfter, modifiedBefore (plus 2 more). Returns the OrderListResponse."),
			mcplib.WithString("customerId", mcplib.Description("Used to filter Orders by Customer ID.")),
			mcplib.WithString("modifiedAfter", mcplib.Description("Time-boxes the request to Orders that were modified after a given ISO 8601 UTC date and time string...")),
			mcplib.WithString("modifiedBefore", mcplib.Description("Time-boxes the request to Orders that were modified before a given ISO 8601 UTC date and time string...")),
			mcplib.WithString("cursor", mcplib.Description("Identifies where the next page of results should begin. Should be the value of pagination.nextPageCursor from a...")),
			mcplib.WithString("fulfillmentStatus", mcplib.Description("Used to filter Orders by fulfillment status. Values may be PENDING, FULFILLED, or CANCELED.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/commerce/orders", []mcpParamBinding{{PublicName: "customerId", WireName: "customerId", Location: "query"}, {PublicName: "modifiedAfter", WireName: "modifiedAfter", Location: "query"}, {PublicName: "modifiedBefore", WireName: "modifiedBefore", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "fulfillmentStatus", WireName: "fulfillmentStatus", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-profiles",
			mcplib.WithDescription("Retrieves all profiles; profiles can be filtered and sorted. The response contains up to 50 Profiles and supports dynamic cursors for pagination. Optional: sortDirection, sortField, filter (plus 2 more). Returns the PaginatedProfileListResponse."),
			mcplib.WithString("sortDirection", mcplib.Description("Identifies the sort direction of the result list; asc for ascending or dsc for descending. If not specified, the...")),
			mcplib.WithString("sortField", mcplib.Description("Identifies the sort field of the result list. Values include: createdOn, id, email, or lastName. If not specified,...")),
			mcplib.WithString("filter", mcplib.Description("Semicolon separated list used to filter profile results. Values include: isCustomer and/or hasAccount, or email. The...")),
			mcplib.WithString("email", mcplib.Description("Email address filter (URL-encoded, case insensitive).")),
			mcplib.WithString("cursor", mcplib.Description("Identifies where the next page of results should begin. Should be the value of pagination.nextPageCursor from a...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/profiles", []mcpParamBinding{{PublicName: "sortDirection", WireName: "sortDirection", Location: "query"}, {PublicName: "sortField", WireName: "sortField", Location: "query"}, {PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "email", WireName: "email", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-specific-inventory-items",
			mcplib.WithDescription("Retrieves real-time stock information for specific product variants. Stock information is stored in an InventoryItem and up to 50 InventoryItems can be retrieved per request. Required: variantIdCsvs. Returns the InventoryItemListResponse."),
			mcplib.WithString("variantIdCsvs", mcplib.Required(), mcplib.Description("Specifies the InventoryItems to retrieve. Multiple InventoryItems can be retrieved by providing a comma-separated...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/commerce/inventory/{variantIdCsvs}", []mcpParamBinding{{PublicName: "variantIdCsvs", WireName: "variantIdCsvs", Location: "path"}}, []string{"variantIdCsvs"}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-specific-profiles",
			mcplib.WithDescription("Retrieves information for specific profiles. The response contains a list of up to 50 Profiles. Multiple Profiles can be retrieved by providing a comma-separated list of profile ids. Required: profileIdCsvs. Returns the ProfileListResponse."),
			mcplib.WithString("profileIdCsvs", mcplib.Required(), mcplib.Description("Specifies the profiles to retrieve. Multiple Profiles can be retrieved by providing a comma-separated list of...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/profiles/{profileIdCsvs}", []mcpParamBinding{{PublicName: "profileIdCsvs", WireName: "profileIdCsvs", Location: "path"}}, []string{"profileIdCsvs"}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-store-pages",
			mcplib.WithDescription("Retrieves information for all store pages on the website. The response supports dynamic cursors for pagination. Optional: cursor. Returns the PaginatedStorePagesResponse."),
			mcplib.WithString("cursor", mcplib.Description("Cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/commerce/store_pages", []mcpParamBinding{{PublicName: "cursor", WireName: "cursor", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-webhook-subscription",
			mcplib.WithDescription("Retrieves information for a specific webhook subscription. Required: subscriptionId. Returns the ExternalWebhookSubscriptionResponse."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Specifies the Webhook Subscription to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/webhook_subscriptions/{subscriptionId}", []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-webhook-subscriptions",
			mcplib.WithDescription("Retrieves information for all webhook subscriptions. The response contains up to 25 Webhook Subscriptions. Returns the ExternalWebhookSubscriptionListResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/webhook_subscriptions", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_get-website-profile",
			mcplib.WithDescription("Retrieves basic details about the website that owns the provided API key or OAuth token. Returns the WebsiteProfile."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/1.0/authorization/website", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_rotate-subscription-secret",
			mcplib.WithDescription("Rotates a webhook subscription's secret. The previous secret for a subscription is no longer valid after a new one is generated. Required: subscriptionId. Returns the new ExternalRotateSecretResponse."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Specifies the Webhook Subscription to update.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/1.0/webhook_subscriptions/{subscriptionId}/actions/rotateSecret", []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_send-test-notification-for-webhook-subscription",
			mcplib.WithDescription("Sends a notification to a subscribed webhook endpoint for testing purposes. This is a one-time notification, and will not be retried if it fails or times out. Required: subscriptionId, topic. Returns the new ExternalTestNotificationResponse."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Specifies the Webhook Subscription to send a notification to.")),
			mcplib.WithString("topic", mcplib.Required(), mcplib.Description("Required; Squarespace event topic that triggers a webhook notification. Possible values include:...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/1.0/webhook_subscriptions/{subscriptionId}/actions/sendTestNotification", []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}, {PublicName: "topic", WireName: "topic", Location: "body"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("1-0_update-webhook-subscription",
			mcplib.WithDescription("Updates information for a webhook subscription. A successful request returns the updated Webhook Subscription resource. Required: subscriptionId. Optional: endpointUrl, topics."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Specifies the Webhook Subscription to update.")),
			mcplib.WithString("endpointUrl", mcplib.Description("Optional; HTTPS URL to receive webhook notifications.")),
			mcplib.WithString("topics", mcplib.Description("Optional; list of event topics that trigger a webhook notification. Possible values include: extension.uninstall,...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/1.0/webhook_subscriptions/{subscriptionId}", []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}, {PublicName: "endpointUrl", WireName: "endpointUrl", Location: "body"}, {PublicName: "topics", WireName: "topics", Location: "body"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_associate-product-variant-image",
			mcplib.WithDescription("Assigns a product image to a product variant. Specifying imageId of null will delete the association. Required: productId, variantId, imageId."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("variantId", mcplib.Required(), mcplib.Description("Variant id")),
			mcplib.WithString("imageId", mcplib.Required(), mcplib.Description("Optional; HTTPS URL to receive webhook notifications.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/commerce/products/{productId}/variants/{variantId}/image", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}, {PublicName: "variantId", WireName: "variantId", Location: "path"}, {PublicName: "imageId", WireName: "imageId", Location: "body"}}, []string{"productId", "variantId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_create-product",
			mcplib.WithDescription("Creates a product with appropriate subresources based on product type. Returns the new ProductV2."),
			mcplib.WithString("body_json", mcplib.Required(), mcplib.Description("Request body as a JSON object string (this endpoint accepts a polymorphic schema: oneOf/anyOf)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/commerce/products", []mcpParamBinding{{PublicName: "body_json", WireName: "body_json", Location: "body_json"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_create-product-variant",
			mcplib.WithDescription("Creates a variant of a product. Creating a variant for a physical or service product requires that the product already has at least one attribute. Required: productId, pricing, sku. Optional: attributes, shippingMeasurements, stock. Returns the new ProductVariantV2."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("attributes", mcplib.Description("Specifies attribute-value pairs for the variant. 100 char limit per key and per value with no more than six...")),
			mcplib.WithString("pricing", mcplib.Required(), mcplib.Description("Present for DIGITAL products only. Digital products don't have variants, so pricing is at the product level.")),
			mcplib.WithString("shippingMeasurements", mcplib.Description("Measurements of the variant when it's shipped. Present for PHYSICAL products only.")),
			mcplib.WithString("sku", mcplib.Required(), mcplib.Description("Merchant-defined code that identifies the product variant. Value must be unique among variants. Leading and trailing...")),
			mcplib.WithString("stock", mcplib.Description("Available stock for the variant. Present for PHYSICAL products only.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/commerce/products/{productId}/variants", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}, {PublicName: "attributes", WireName: "attributes", Location: "body"}, {PublicName: "pricing", WireName: "pricing", Location: "body"}, {PublicName: "shippingMeasurements", WireName: "shippingMeasurements", Location: "body"}, {PublicName: "sku", WireName: "sku", Location: "body"}, {PublicName: "stock", WireName: "stock", Location: "body"}}, []string{"productId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_delete-product",
			mcplib.WithDescription("Delete specified product. Required: productId. Destructive."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v2/commerce/products/{productId}", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}}, []string{"productId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_delete-product-image",
			mcplib.WithDescription("Delete one product image. Required: productId, imageId. Destructive."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("imageId", mcplib.Required(), mcplib.Description("Image id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v2/commerce/products/{productId}/images/{imageId}", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}, {PublicName: "imageId", WireName: "imageId", Location: "path"}}, []string{"productId", "imageId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_delete-product-variant",
			mcplib.WithDescription("Delete one product variant. Required: productId, variantId. Destructive."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("variantId", mcplib.Required(), mcplib.Description("Variant id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v2/commerce/products/{productId}/variants/{variantId}", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}, {PublicName: "variantId", WireName: "variantId", Location: "path"}}, []string{"productId", "variantId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_get-product-image-processing-status",
			mcplib.WithDescription("Retrieves the processing status for an uploaded product image. A successful request indicates that a ProductImage is processing, ready, or in an error state. Required: productId, imageId. Returns the ProductImageProcessingStatus."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("imageId", mcplib.Required(), mcplib.Description("Image id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/commerce/products/{productId}/images/{imageId}/status", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}, {PublicName: "imageId", WireName: "imageId", Location: "path"}}, []string{"productId", "imageId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_get-products",
			mcplib.WithDescription("Retrieves information for products; products can be filtered within a date range and by product type. The response contains product information in a Product, up to 50 Products ordered by their modification date (modifiedOn), and supports dynamic cursors for pagination. Optional: modifiedAfter, modifiedBefore, cursor (plus 2 more). Returns the PaginatedProductListResponseV2."),
			mcplib.WithString("modifiedAfter", mcplib.Description("Modified after")),
			mcplib.WithString("modifiedBefore", mcplib.Description("Modified before")),
			mcplib.WithString("cursor", mcplib.Description("Cursor")),
			mcplib.WithString("query", mcplib.Description("Query")),
			mcplib.WithString("type", mcplib.Description("Type")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/commerce/products", []mcpParamBinding{{PublicName: "modifiedAfter", WireName: "modifiedAfter", Location: "query"}, {PublicName: "modifiedBefore", WireName: "modifiedBefore", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "query", WireName: "query", Location: "query"}, {PublicName: "type", WireName: "type", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_get-specific-products",
			mcplib.WithDescription("Retrieves information for specific products, including information for any variants or images. Up to 50 products can be retrieved per request. Required: productIdCsvs. Returns the ProductListResponseV2."),
			mcplib.WithString("productIdCsvs", mcplib.Required(), mcplib.Description("Product id csvs")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/commerce/products/{productIdCsvs}", []mcpParamBinding{{PublicName: "productIdCsvs", WireName: "productIdCsvs", Location: "path"}}, []string{"productIdCsvs"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_update-product",
			mcplib.WithDescription("Updates information for a product. The endpoint supports partial updates. Required: productId. Optional: description, isVisible, name (plus 5 more). Returns the new ProductV2."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("description", mcplib.Description("Optional; HTTPS URL to receive webhook notifications.")),
			mcplib.WithString("isVisible", mcplib.Description("Is visible")),
			mcplib.WithString("name", mcplib.Description("Optional; HTTPS URL to receive webhook notifications.")),
			mcplib.WithString("pricing", mcplib.Description("Pricing data for the product.")),
			mcplib.WithString("productAttributeNames", mcplib.Description("Optional; list of event topics that trigger a webhook notification. Possible values include: extension.uninstall,...")),
			mcplib.WithString("seoData", mcplib.Description("Options for search engine optimization.")),
			mcplib.WithString("tags", mcplib.Description("Optional; list of event topics that trigger a webhook notification. Possible values include: extension.uninstall,...")),
			mcplib.WithString("urlSlug", mcplib.Description("Optional; HTTPS URL to receive webhook notifications.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/commerce/products/{productId}", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "isVisible", WireName: "isVisible", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "pricing", WireName: "pricing", Location: "body"}, {PublicName: "productAttributeNames", WireName: "productAttributeNames", Location: "body"}, {PublicName: "seoData", WireName: "seoData", Location: "body"}, {PublicName: "tags", WireName: "tags", Location: "body"}, {PublicName: "urlSlug", WireName: "urlSlug", Location: "body"}}, []string{"productId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_update-product-image",
			mcplib.WithDescription("Updates information for a product image. Currently, the endpoint only supports updates to the alt text for an image. Required: productId, imageId. Optional: altText. Returns the new ProductImage."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("imageId", mcplib.Required(), mcplib.Description("Image id")),
			mcplib.WithString("altText", mcplib.Description("Optional; HTTPS URL to receive webhook notifications.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/commerce/products/{productId}/images/{imageId}", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}, {PublicName: "imageId", WireName: "imageId", Location: "path"}, {PublicName: "altText", WireName: "altText", Location: "body"}}, []string{"productId", "imageId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_update-product-image-order",
			mcplib.WithDescription("Updates the ordering of a product image on the product details page. Required: productId, imageId. Optional: afterImageId."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("imageId", mcplib.Required(), mcplib.Description("Image id")),
			mcplib.WithString("afterImageId", mcplib.Description("Optional; HTTPS URL to receive webhook notifications.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/commerce/products/{productId}/images/{imageId}/order", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}, {PublicName: "imageId", WireName: "imageId", Location: "path"}, {PublicName: "afterImageId", WireName: "afterImageId", Location: "body"}}, []string{"productId", "imageId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_update-product-variant",
			mcplib.WithDescription("Updates a variant of a product. The endpoint supports partial updates. Required: productId, variantId. Optional: attributes, pricing, shippingMeasurements (plus 1 more). Returns the new ProductVariantV2."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithString("variantId", mcplib.Required(), mcplib.Description("Variant id")),
			mcplib.WithString("attributes", mcplib.Description("Specifies attribute-value pairs for the variant. Supported for PHYSICAL products only.")),
			mcplib.WithString("pricing", mcplib.Description("Pricing data for the product.")),
			mcplib.WithString("shippingMeasurements", mcplib.Description("Measurements of the variant when it's shipped. Supported for PHYSICAL products only.")),
			mcplib.WithString("sku", mcplib.Description("Optional; HTTPS URL to receive webhook notifications.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/commerce/products/{productId}/variants/{variantId}", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}, {PublicName: "variantId", WireName: "variantId", Location: "path"}, {PublicName: "attributes", WireName: "attributes", Location: "body"}, {PublicName: "pricing", WireName: "pricing", Location: "body"}, {PublicName: "shippingMeasurements", WireName: "shippingMeasurements", Location: "body"}, {PublicName: "sku", WireName: "sku", Location: "body"}}, []string{"productId", "variantId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce_upload-product-image",
			mcplib.WithDescription("Uploads an image for a product. Uploading an image doesn't set the product's featured image. A successful request creates a ProductImage subresource of the Product. Required: productId. Returns the new UploadProductImageResponse."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/commerce/products/{productId}/images", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}}, []string{"productId"}),
	)
	s.AddTool(
		mcplib.NewTool("commerce-analytics_get-transactions-summaries",
			mcplib.WithDescription("Retrieve transaction summaries grouped by contact. Required: contactIds, groupBy. Returns the new RetrieveTransactionsSummariesResponse."),
			mcplib.WithString("contactIds", mcplib.Required(), mcplib.Description("Contact ids")),
			mcplib.WithString("groupBy", mcplib.Required(), mcplib.Description("Field to group results by, should be a contactId")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/analytics/transaction-summaries", []mcpParamBinding{{PublicName: "contactIds", WireName: "contactIds", Location: "body"}, {PublicName: "groupBy", WireName: "groupBy", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_create",
			mcplib.WithDescription("Creates a new contact. Requires OAuth website scope WEBSITE_CONTACTS or WEBSITE_PROFILES (read/write). Required: firstName, lastName, locale, primaryEmail. Returns the new CreateContactResponse."),
			mcplib.WithString("firstName", mcplib.Required(), mcplib.Description("Contact's first name")),
			mcplib.WithString("lastName", mcplib.Required(), mcplib.Description("Contact's last name")),
			mcplib.WithString("locale", mcplib.Required(), mcplib.Description("Contact's locale")),
			mcplib.WithString("primaryEmail", mcplib.Required(), mcplib.Description("Primary email supplied when creating a contact.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/contacts", []mcpParamBinding{{PublicName: "firstName", WireName: "firstName", Location: "body"}, {PublicName: "lastName", WireName: "lastName", Location: "body"}, {PublicName: "locale", WireName: "locale", Location: "body"}, {PublicName: "primaryEmail", WireName: "primaryEmail", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_delete",
			mcplib.WithDescription("Deletes the contact for the given contact ID. Requires OAuth website scope WEBSITE_CONTACTS or WEBSITE_PROFILES (read/write). Required: contactId. Destructive."),
			mcplib.WithString("contactId", mcplib.Required(), mcplib.Description("The contact's ID.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/contacts/{contactId}", []mcpParamBinding{{PublicName: "contactId", WireName: "contactId", Location: "path"}}, []string{"contactId"}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_get",
			mcplib.WithDescription("Returns a paginated list of contacts for the website. Requires OAuth website scope WEBSITE_CONTACTS_READ, WEBSITE_CONTACTS, WEBSITE_PROFILES_READ, or WEBSITE_PROFILES. Required: paginationParameters."),
			mcplib.WithString("paginationParameters", mcplib.Required(), mcplib.Description("Pagination parameters")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/contacts", []mcpParamBinding{{PublicName: "paginationParameters", WireName: "paginationParameters", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_get-contactid",
			mcplib.WithDescription("Returns the contact for the given contact ID. Requires OAuth website scope WEBSITE_CONTACTS_READ, WEBSITE_CONTACTS, WEBSITE_PROFILES_READ, or WEBSITE_PROFILES. Required: contactId."),
			mcplib.WithString("contactId", mcplib.Required(), mcplib.Description("The contact's ID.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/contacts/{contactId}", []mcpParamBinding{{PublicName: "contactId", WireName: "contactId", Location: "path"}}, []string{"contactId"}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_patch",
			mcplib.WithDescription("Updates a contact using JSON merge patch. Requires OAuth website scope WEBSITE_CONTACTS or WEBSITE_PROFILES (read/write). Required: contactId. Optional: firstName, lastName, locale (plus 1 more). Returns the updated PatchContactResponse."),
			mcplib.WithString("contactId", mcplib.Required(), mcplib.Description("The contact's ID.")),
			mcplib.WithString("firstName", mcplib.Description("The contact's first name.")),
			mcplib.WithString("lastName", mcplib.Description("The contact's last name.")),
			mcplib.WithString("locale", mcplib.Description("The contact's locale.")),
			mcplib.WithString("primaryEmail", mcplib.Description("Primary email fields for an update request.")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/v1/contacts/{contactId}", []mcpParamBinding{{PublicName: "contactId", WireName: "contactId", Location: "path"}, {PublicName: "firstName", WireName: "firstName", Location: "body"}, {PublicName: "lastName", WireName: "lastName", Location: "body"}, {PublicName: "locale", WireName: "locale", Location: "body"}, {PublicName: "primaryEmail", WireName: "primaryEmail", Location: "body"}}, []string{"contactId"}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_query",
			mcplib.WithDescription("Returns a paginated list of contacts matching filters and sort options in the request body. Requires OAuth website scope WEBSITE_CONTACTS_READ, WEBSITE_CONTACTS, WEBSITE_PROFILES_READ, or WEBSITE_PROFILES. Optional: acceptsMarketingWithDate, cursor, donationAmount (plus 11 more)."),
			mcplib.WithString("acceptsMarketingWithDate", mcplib.Description("Contact's marketing settings with joined on date")),
			mcplib.WithString("cursor", mcplib.Description("The cursor for the next page of results. Use the value of `pagination.nextPageCursor` from the previous response.")),
			mcplib.WithString("donationAmount", mcplib.Description("Object for filtering by a min and max integer")),
			mcplib.WithString("donationCount", mcplib.Description("Object for filtering by a min and max integer")),
			mcplib.WithString("firstDonationOn", mcplib.Description("Object for filtering by before and after dates")),
			mcplib.WithString("firstOrderOn", mcplib.Description("Object for filtering by before and after dates")),
			mcplib.WithString("lastDonationOn", mcplib.Description("Object for filtering by before and after dates")),
			mcplib.WithString("lastOrderOn", mcplib.Description("Object for filtering by before and after dates")),
			mcplib.WithString("orderAmount", mcplib.Description("Object for filtering by a min and max integer")),
			mcplib.WithString("orderCount", mcplib.Description("Object for filtering by a min and max integer")),
			mcplib.WithString("pageSize", mcplib.Description("The number of contacts to return per request.")),
			mcplib.WithString("searchString", mcplib.Description("Search string for Name and Email address")),
			mcplib.WithString("sortDirection", mcplib.Description("Direction to sort results, ASCENDING or DESCENDING")),
			mcplib.WithString("sortField", mcplib.Description("Field to sort results by")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/contacts/query", []mcpParamBinding{{PublicName: "acceptsMarketingWithDate", WireName: "acceptsMarketingWithDate", Location: "body"}, {PublicName: "cursor", WireName: "cursor", Location: "body"}, {PublicName: "donationAmount", WireName: "donationAmount", Location: "body"}, {PublicName: "donationCount", WireName: "donationCount", Location: "body"}, {PublicName: "firstDonationOn", WireName: "firstDonationOn", Location: "body"}, {PublicName: "firstOrderOn", WireName: "firstOrderOn", Location: "body"}, {PublicName: "lastDonationOn", WireName: "lastDonationOn", Location: "body"}, {PublicName: "lastOrderOn", WireName: "lastOrderOn", Location: "body"}, {PublicName: "orderAmount", WireName: "orderAmount", Location: "body"}, {PublicName: "orderCount", WireName: "orderCount", Location: "body"}, {PublicName: "pageSize", WireName: "pageSize", Location: "body"}, {PublicName: "searchString", WireName: "searchString", Location: "body"}, {PublicName: "sortDirection", WireName: "sortDirection", Location: "body"}, {PublicName: "sortField", WireName: "sortField", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_address-book_create-entry",
			mcplib.WithDescription("Creates a new address book entry for the contact. If this is the contact's only address book entry after creation, it becomes the default shipping address even when defaultShipping is false. Requires OAuth website scope WEBSITE_CONTACTS or WEBSITE_PROFILES (read/write). Required: contactId, address. Optional: defaultShipping. Returns the new CreateAddressBookEntryResponse."),
			mcplib.WithString("contactId", mcplib.Required(), mcplib.Description("The contact's ID.")),
			mcplib.WithString("address", mcplib.Required(), mcplib.Description("Address properties for a contact.")),
			mcplib.WithString("defaultShipping", mcplib.Description("Whether this entry should be the default shipping address. If this is the only address book entry for the contact...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/contacts/{contactId}/address-book", []mcpParamBinding{{PublicName: "contactId", WireName: "contactId", Location: "path"}, {PublicName: "address", WireName: "address", Location: "body"}, {PublicName: "defaultShipping", WireName: "defaultShipping", Location: "body"}}, []string{"contactId"}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_address-book_delete-entry",
			mcplib.WithDescription("Deletes the address book entry for the contact. If the deleted entry was the default shipping address, another entry is assigned as the default. Requires OAuth website scope WEBSITE_CONTACTS or WEBSITE_PROFILES (read/write). Required: contactId, addressBookEntryId. Destructive."),
			mcplib.WithString("contactId", mcplib.Required(), mcplib.Description("The contact's ID.")),
			mcplib.WithString("addressBookEntryId", mcplib.Required(), mcplib.Description("The address book entry's ID.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/contacts/{contactId}/address-book/{addressBookEntryId}", []mcpParamBinding{{PublicName: "contactId", WireName: "contactId", Location: "path"}, {PublicName: "addressBookEntryId", WireName: "addressBookEntryId", Location: "path"}}, []string{"contactId", "addressBookEntryId"}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_address-book_get",
			mcplib.WithDescription("Returns paginated addresses for the contact. When there is at least one entry, exactly one is the default shipping address (defaultShippingAddressId and each entry's defaultShipping reflect this). The default shipping address (when present) is always included on the first page of results. Requires OAuth website scope WEBSITE_CONTACTS_READ, WEBSITE_CONTACTS, WEBSITE_PROFILES_READ, or WEBSITE_PROFILES. Required: contactId, paginationParameters."),
			mcplib.WithString("contactId", mcplib.Required(), mcplib.Description("The contact's ID.")),
			mcplib.WithString("paginationParameters", mcplib.Required(), mcplib.Description("Pagination parameters")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/contacts/{contactId}/address-book", []mcpParamBinding{{PublicName: "contactId", WireName: "contactId", Location: "path"}, {PublicName: "paginationParameters", WireName: "paginationParameters", Location: "query"}}, []string{"contactId"}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_address-book_get-entry",
			mcplib.WithDescription("Returns a single address book entry for the contact. Requires OAuth website scope WEBSITE_CONTACTS_READ, WEBSITE_CONTACTS, WEBSITE_PROFILES_READ, or WEBSITE_PROFILES. Required: contactId, addressBookEntryId."),
			mcplib.WithString("contactId", mcplib.Required(), mcplib.Description("The contact's ID.")),
			mcplib.WithString("addressBookEntryId", mcplib.Required(), mcplib.Description("The address book entry's ID.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/contacts/{contactId}/address-book/{addressBookEntryId}", []mcpParamBinding{{PublicName: "contactId", WireName: "contactId", Location: "path"}, {PublicName: "addressBookEntryId", WireName: "addressBookEntryId", Location: "path"}}, []string{"contactId", "addressBookEntryId"}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_address-book_update-entry",
			mcplib.WithDescription("Replaces an address book entry for the contact with a full JSON body. When updating the entry that is currently the default, setting 'defaultShipping' from true to false has no effect. Requires OAuth website scope WEBSITE_CONTACTS or WEBSITE_PROFILES (read/write). Required: contactId, addressBookEntryId, address. Optional: defaultShipping. Returns the updated UpdateAddressBookEntryResponse."),
			mcplib.WithString("contactId", mcplib.Required(), mcplib.Description("The contact's ID.")),
			mcplib.WithString("addressBookEntryId", mcplib.Required(), mcplib.Description("The address book entry's ID.")),
			mcplib.WithString("address", mcplib.Required(), mcplib.Description("Address properties for a contact.")),
			mcplib.WithString("defaultShipping", mcplib.Description("Whether this entry should be the default shipping address. When updating the entry that is currently the default,...")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/v1/contacts/{contactId}/address-book/{addressBookEntryId}", []mcpParamBinding{{PublicName: "contactId", WireName: "contactId", Location: "path"}, {PublicName: "addressBookEntryId", WireName: "addressBookEntryId", Location: "path"}, {PublicName: "address", WireName: "address", Location: "body"}, {PublicName: "defaultShipping", WireName: "defaultShipping", Location: "body"}}, []string{"contactId", "addressBookEntryId"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		// bodyJSONOverride holds an opaque body for endpoints whose request
		// schema is a oneOf/anyOf and was wired with a single body_json input.
		// When set, it replaces the marshaled bodyArgs map for POST/PUT/PATCH.
		var bodyJSONOverride json.RawMessage
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			case "body_json":
				if s, ok := v.(string); ok && s != "" {
					var parsed any
					if err := json.Unmarshal([]byte(s), &parsed); err != nil {
						return mcplib.NewToolResultError("body_json must be a valid JSON object: " + err.Error()), nil
					}
					if _, isMap := parsed.(map[string]any); !isMap {
						return mcplib.NewToolResultError(fmt.Sprintf("body_json must be a JSON object, got JSON %T", parsed)), nil
					}
					bodyJSONOverride = json.RawMessage(s)
				}
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			if len(bodyJSONOverride) > 0 {
				data, _, err = c.Post(path, bodyJSONOverride)
				break
			}
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			if len(bodyJSONOverride) > 0 {
				data, _, err = c.Put(path, bodyJSONOverride)
				break
			}
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			if len(bodyJSONOverride) > 0 {
				data, _, err = c.Patch(path, bodyJSONOverride)
				break
			}
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export COMMERCE_AUTHORIZATION=<your-key>" +
					"\n      Run 'squarespace-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export COMMERCE_AUTHORIZATION=<your-key>" +
					"\n      Run 'squarespace-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export COMMERCE_AUTHORIZATION=<your-key>" +
					"\n      Run 'squarespace-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "squarespace-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "squarespace-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "squarespace",
		"description": "Printing Press CLI for Squarespace.",
		"archetype":   "crm",
		"tool_count":  47,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion squarespace-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "COMMERCE_AUTHORIZATION",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "1-0",
				"description": "Manage 1 0",
				"endpoints":   []string{"adjust-inventory-stock-levels", "create-order", "create-webhook-subscription", "delete-webhook-subscription", "fulfill-order", "get-documents-by-id", "get-documents-by-updated-on", "get-inventory-items", "get-member-profile", "get-order", "get-orders", "get-profiles", "get-specific-inventory-items", "get-specific-profiles", "get-store-pages", "get-webhook-subscription", "get-webhook-subscriptions", "get-website-profile", "rotate-subscription-secret", "send-test-notification-for-webhook-subscription", "update-webhook-subscription"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "commerce",
				"description": "Manage commerce",
				"endpoints":   []string{"associate-product-variant-image", "create-product", "create-product-variant", "delete-product", "delete-product-image", "delete-product-variant", "get-product-image-processing-status", "get-products", "get-specific-products", "update-product", "update-product-image", "update-product-image-order", "update-product-variant", "upload-product-image"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "commerce-analytics",
				"description": "Manage commerce analytics",
				"endpoints":   []string{"get-transactions-summaries"},
				"searchable":  true,
			},
			{
				"name":        "contacts",
				"description": "Manage customer contacts and address book entries for a website: create, read, update, delete, and query contacts;...",
				"endpoints":   []string{"create", "delete", "get", "get-contactid", "patch", "query"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass cursor parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		"playbook": []map[string]string{
			{"topic": "Contact lookup", "insight": "Use search for finding contacts by name/email. List endpoints return unsorted results and require pagination for large datasets."},
			{"topic": "Activity tracking", "insight": "When checking deal activity, sync first and query locally. CRM APIs often throttle activity-log endpoints heavily."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
