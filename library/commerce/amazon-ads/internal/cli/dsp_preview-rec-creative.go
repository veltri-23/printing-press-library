// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/commerce/amazon-ads/internal/cliutil"
	"github.com/spf13/cobra"
)

func newDspPreviewRecCreativeCmd(flags *rootFlags) *cobra.Command {
	var bodyCreativeId string
	var bodyCreativeModelContentBackground string
	var bodyCreativeModelMarketplace string
	var bodyCreativeModelThirdPartyClickTrackers string
	var bodyCreativeModelThirdPartyTrackers string
	var bodyPreviewConfigurationAllowedFormat string
	var bodyPreviewConfigurationAllowedSizeHeight float64
	var bodyPreviewConfigurationAllowedSizeWidth float64
	var bodyPreviewConfigurationAssociatedProductAsin string
	var bodyPreviewConfigurationAssociatedProductProductTitle string
	var bodyPreviewConfigurationIsOnAmazon bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "preview-rec-creative",
		Short:       "Preview a Responsive eCommerce Creative(REC).",
		Example:     "  amazon-ads-pp-cli dsp preview-rec-creative",
		Annotations: map[string]string{"pp:endpoint": "dsp.preview-rec-creative", "pp:method": "POST", "pp:path": "/dsp/creatives/rec/preview"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("creative-model-marketplace") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "creative-model-marketplace")
				}
				if !cmd.Flags().Changed("preview-configuration-allowed-format") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "preview-configuration-allowed-format")
				}
				if !cmd.Flags().Changed("preview-configuration-allowed-size-height") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "preview-configuration-allowed-size-height")
				}
				if !cmd.Flags().Changed("preview-configuration-allowed-size-width") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "preview-configuration-allowed-size-width")
				}
				if !cmd.Flags().Changed("preview-configuration-associated-product-asin") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "preview-configuration-associated-product-asin")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/dsp/creatives/rec/preview"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCreativeId != "" {
					body["creativeId"] = bodyCreativeId
				}
				{
					nestedCreativeModel := map[string]any{}
					{
						nestedCreativeModelContent := map[string]any{}
						if bodyCreativeModelContentBackground != "" {
							var parsedCreativeModelContentBackground any
							if err := json.Unmarshal([]byte(bodyCreativeModelContentBackground), &parsedCreativeModelContentBackground); err != nil {
								return fmt.Errorf("parsing --creative-model-content-background JSON: %w", err)
							}
							nestedCreativeModelContent["background"] = parsedCreativeModelContentBackground
						}
						if len(nestedCreativeModelContent) > 0 {
							nestedCreativeModel["content"] = nestedCreativeModelContent
						}
					}
					if bodyCreativeModelMarketplace != "" {
						nestedCreativeModel["marketplace"] = bodyCreativeModelMarketplace
					}
					if bodyCreativeModelThirdPartyClickTrackers != "" {
						nestedCreativeModel["thirdPartyClickTrackers"] = cliutil.SplitCSV(bodyCreativeModelThirdPartyClickTrackers)
					}
					if bodyCreativeModelThirdPartyTrackers != "" {
						var parsedCreativeModelThirdPartyTrackers any
						if err := json.Unmarshal([]byte(bodyCreativeModelThirdPartyTrackers), &parsedCreativeModelThirdPartyTrackers); err != nil {
							return fmt.Errorf("parsing --creative-model-third-party-trackers JSON: %w", err)
						}
						nestedCreativeModel["thirdPartyTrackers"] = parsedCreativeModelThirdPartyTrackers
					}
					if len(nestedCreativeModel) > 0 {
						body["creativeModel"] = nestedCreativeModel
					}
				}
				{
					nestedPreviewConfiguration := map[string]any{}
					if bodyPreviewConfigurationAllowedFormat != "" {
						nestedPreviewConfiguration["allowedFormat"] = bodyPreviewConfigurationAllowedFormat
					}
					{
						nestedPreviewConfigurationAllowedSize := map[string]any{}
						if bodyPreviewConfigurationAllowedSizeHeight != 0.0 {
							nestedPreviewConfigurationAllowedSize["height"] = bodyPreviewConfigurationAllowedSizeHeight
						}
						if bodyPreviewConfigurationAllowedSizeWidth != 0.0 {
							nestedPreviewConfigurationAllowedSize["width"] = bodyPreviewConfigurationAllowedSizeWidth
						}
						if len(nestedPreviewConfigurationAllowedSize) > 0 {
							nestedPreviewConfiguration["allowedSize"] = nestedPreviewConfigurationAllowedSize
						}
					}
					{
						nestedPreviewConfigurationAssociatedProduct := map[string]any{}
						if bodyPreviewConfigurationAssociatedProductAsin != "" {
							nestedPreviewConfigurationAssociatedProduct["asin"] = bodyPreviewConfigurationAssociatedProductAsin
						}
						if bodyPreviewConfigurationAssociatedProductProductTitle != "" {
							nestedPreviewConfigurationAssociatedProduct["productTitle"] = bodyPreviewConfigurationAssociatedProductProductTitle
						}
						if len(nestedPreviewConfigurationAssociatedProduct) > 0 {
							nestedPreviewConfiguration["associatedProduct"] = nestedPreviewConfigurationAssociatedProduct
						}
					}
					if cmd.Flags().Changed("preview-configuration-is-on-amazon") {
						nestedPreviewConfiguration["isOnAmazon"] = bodyPreviewConfigurationIsOnAmazon
					}
					if len(nestedPreviewConfiguration) > 0 {
						body["previewConfiguration"] = nestedPreviewConfiguration
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "dsp", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "dsp", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "dsp", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "dsp", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "dsp", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "dsp",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "dsp", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "dsp", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyCreativeId, "creative-id", "", "The identifier of the creative.")
	cmd.Flags().StringVar(&bodyCreativeModelContentBackground, "creative-model-content-background", "", "Creative customization field for displaying custom images.")
	cmd.Flags().StringVar(&bodyCreativeModelMarketplace, "creative-model-marketplace", "", "Marketplace where this creative is applicable.")
	cmd.Flags().StringVar(&bodyCreativeModelThirdPartyClickTrackers, "creative-model-third-party-click-trackers", "", "This URL is pinged when the creative is clicked. The URL can contain macros.")
	cmd.Flags().StringVar(&bodyCreativeModelThirdPartyTrackers, "creative-model-third-party-trackers", "", "Third party trackers")
	cmd.Flags().StringVar(&bodyPreviewConfigurationAllowedFormat, "preview-configuration-allowed-format", "", "Formats configured to display for the creative.")
	cmd.Flags().Float64Var(&bodyPreviewConfigurationAllowedSizeHeight, "preview-configuration-allowed-size-height", 0.0, "The creative height in pixels.")
	cmd.Flags().Float64Var(&bodyPreviewConfigurationAllowedSizeWidth, "preview-configuration-allowed-size-width", 0.0, "The creative width in pixels.")
	cmd.Flags().StringVar(&bodyPreviewConfigurationAssociatedProductAsin, "preview-configuration-associated-product-asin", "", "Amazon standard identification number.")
	cmd.Flags().StringVar(&bodyPreviewConfigurationAssociatedProductProductTitle, "preview-configuration-associated-product-product-title", "", "Retail product title.")
	cmd.Flags().BoolVar(&bodyPreviewConfigurationIsOnAmazon, "preview-configuration-is-on-amazon", false, "If it's true, shows preview in amazon websites, else shows in other websites")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
