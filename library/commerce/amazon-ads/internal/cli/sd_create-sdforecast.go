// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSdCreateSdforecastCmd(flags *rootFlags) *cobra.Command {
	var bodyAdGroupAdGroupId string
	var bodyAdGroupBidOptimization string
	var bodyAdGroupCampaignId string
	var bodyAdGroupCreativeType string
	var bodyAdGroupDefaultBid float64
	var bodyAdGroupName string
	var bodyAdGroupState string
	var bodyAdGroupTactic string
	var bodyCampaignBudget float64
	var bodyCampaignBudgetType string
	var bodyCampaignCampaignId string
	var bodyCampaignCostType string
	var bodyCampaignDeliveryProfile string
	var bodyCampaignEndDate string
	var bodyCampaignName string
	var bodyCampaignPortfolioId string
	var bodyCampaignStartDate string
	var bodyCampaignState string
	var bodyCampaignTactic string
	var bodyNegativeTargetingClauses string
	var bodyProductAds string
	var bodyTargetingClauses string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-sdforecast",
		Short:       "Returns forecasts for a given ad group specified in SD forecast request.",
		Example:     "  amazon-ads-pp-cli sd create-sdforecast",
		Annotations: map[string]string{"pp:endpoint": "sd.create-sdforecast", "pp:method": "POST", "pp:path": "/sd/forecasts"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("product-ads") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "product-ads")
				}
				if !cmd.Flags().Changed("targeting-clauses") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "targeting-clauses")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/sd/forecasts"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAdGroup := map[string]any{}
					if bodyAdGroupAdGroupId != "" {
						nestedAdGroup["adGroupId"] = bodyAdGroupAdGroupId
					}
					if bodyAdGroupBidOptimization != "" {
						nestedAdGroup["bidOptimization"] = bodyAdGroupBidOptimization
					}
					if bodyAdGroupCampaignId != "" {
						nestedAdGroup["campaignId"] = bodyAdGroupCampaignId
					}
					if bodyAdGroupCreativeType != "" {
						nestedAdGroup["creativeType"] = bodyAdGroupCreativeType
					}
					if bodyAdGroupDefaultBid != 0.0 {
						nestedAdGroup["defaultBid"] = bodyAdGroupDefaultBid
					}
					if bodyAdGroupName != "" {
						nestedAdGroup["name"] = bodyAdGroupName
					}
					if bodyAdGroupState != "" {
						nestedAdGroup["state"] = bodyAdGroupState
					}
					if bodyAdGroupTactic != "" {
						nestedAdGroup["tactic"] = bodyAdGroupTactic
					}
					if len(nestedAdGroup) > 0 {
						body["adGroup"] = nestedAdGroup
					}
				}
				{
					nestedCampaign := map[string]any{}
					if bodyCampaignBudget != 0.0 {
						nestedCampaign["budget"] = bodyCampaignBudget
					}
					if bodyCampaignBudgetType != "" {
						nestedCampaign["budgetType"] = bodyCampaignBudgetType
					}
					if bodyCampaignCampaignId != "" {
						nestedCampaign["campaignId"] = bodyCampaignCampaignId
					}
					if bodyCampaignCostType != "" {
						nestedCampaign["costType"] = bodyCampaignCostType
					}
					if bodyCampaignDeliveryProfile != "" {
						nestedCampaign["deliveryProfile"] = bodyCampaignDeliveryProfile
					}
					if bodyCampaignEndDate != "" {
						nestedCampaign["endDate"] = bodyCampaignEndDate
					}
					if bodyCampaignName != "" {
						nestedCampaign["name"] = bodyCampaignName
					}
					if bodyCampaignPortfolioId != "" {
						nestedCampaign["portfolioId"] = bodyCampaignPortfolioId
					}
					if bodyCampaignStartDate != "" {
						nestedCampaign["startDate"] = bodyCampaignStartDate
					}
					if bodyCampaignState != "" {
						nestedCampaign["state"] = bodyCampaignState
					}
					if bodyCampaignTactic != "" {
						nestedCampaign["tactic"] = bodyCampaignTactic
					}
					if len(nestedCampaign) > 0 {
						body["campaign"] = nestedCampaign
					}
				}
				if bodyNegativeTargetingClauses != "" {
					var parsedNegativeTargetingClauses any
					if err := json.Unmarshal([]byte(bodyNegativeTargetingClauses), &parsedNegativeTargetingClauses); err != nil {
						return fmt.Errorf("parsing --negative-targeting-clauses JSON: %w", err)
					}
					body["negativeTargetingClauses"] = parsedNegativeTargetingClauses
				}
				if bodyProductAds != "" {
					var parsedProductAds any
					if err := json.Unmarshal([]byte(bodyProductAds), &parsedProductAds); err != nil {
						return fmt.Errorf("parsing --product-ads JSON: %w", err)
					}
					body["productAds"] = parsedProductAds
				}
				if bodyTargetingClauses != "" {
					var parsedTargetingClauses any
					if err := json.Unmarshal([]byte(bodyTargetingClauses), &parsedTargetingClauses); err != nil {
						return fmt.Errorf("parsing --targeting-clauses JSON: %w", err)
					}
					body["targetingClauses"] = parsedTargetingClauses
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "sd", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "sd", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sd", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sd", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sd", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "sd",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sd", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sd", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAdGroupAdGroupId, "ad-group-ad-group-id", "", "The identifier of the ad group.")
	cmd.Flags().StringVar(&bodyAdGroupBidOptimization, "ad-group-bid-optimization", "", "Bid Optimization for the Adgroup. Default behavior is to optimize for clicks.")
	cmd.Flags().StringVar(&bodyAdGroupCampaignId, "ad-group-campaign-id", "", "The identifier of the campaign.")
	cmd.Flags().StringVar(&bodyAdGroupCreativeType, "ad-group-creative-type", "", "The type of the associated creative. If the field is empty or null, a default value of IMAGE will be used.")
	cmd.Flags().Float64Var(&bodyAdGroupDefaultBid, "ad-group-default-bid", 0.0, "The amount of the default bid associated with the ad group. Used if no bid is specified.")
	cmd.Flags().StringVar(&bodyAdGroupName, "ad-group-name", "", "The name of the ad group.")
	cmd.Flags().StringVar(&bodyAdGroupState, "ad-group-state", "", "The state of the ad group.")
	cmd.Flags().StringVar(&bodyAdGroupTactic, "ad-group-tactic", "", "The advertising tactic associated with the campaign.")
	cmd.Flags().Float64Var(&bodyCampaignBudget, "campaign-budget", 0.0, "The amount of the budget.")
	cmd.Flags().StringVar(&bodyCampaignBudgetType, "campaign-budget-type", "", "The time period over which the amount specified in the `budget` property is allocated.")
	cmd.Flags().StringVar(&bodyCampaignCampaignId, "campaign-campaign-id", "", "The identifier of the campaign.")
	cmd.Flags().StringVar(&bodyCampaignCostType, "campaign-cost-type", "", "Determines how the campaign will bid and charge.")
	cmd.Flags().StringVar(&bodyCampaignDeliveryProfile, "campaign-delivery-profile", "", "Delivery profile")
	cmd.Flags().StringVar(&bodyCampaignEndDate, "campaign-end-date", "", "The YYYYMMDD end date of the campaign.")
	cmd.Flags().StringVar(&bodyCampaignName, "campaign-name", "", "The name of the campaign.")
	cmd.Flags().StringVar(&bodyCampaignPortfolioId, "campaign-portfolio-id", "", "Identifier of the portfolio that will be associated with the campaign.")
	cmd.Flags().StringVar(&bodyCampaignStartDate, "campaign-start-date", "", "The YYYYMMDD start date of the campaign. The date must be today or in the future.")
	cmd.Flags().StringVar(&bodyCampaignState, "campaign-state", "", "The state of the campaign.")
	cmd.Flags().StringVar(&bodyCampaignTactic, "campaign-tactic", "", "The advertising tactic associated with the campaign.")
	cmd.Flags().StringVar(&bodyNegativeTargetingClauses, "negative-targeting-clauses", "", "A list of SD negative targeting clauses.")
	cmd.Flags().StringVar(&bodyProductAds, "product-ads", "", "Product ads")
	cmd.Flags().StringVar(&bodyTargetingClauses, "targeting-clauses", "", "A list of SD targeting clauses.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
