// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAmazonAdsProfilesProfilesListCmd(flags *rootFlags) *cobra.Command {
	var flagApiProgram string
	var flagAccessLevel string
	var flagProfileTypeFilter string
	var flagValidPaymentMethodFilter string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Note that this operation does not return a response unless the current account has created at least one campaign using",
		Example:     "  amazon-ads-pp-cli amazon-ads-profiles-profiles list",
		Annotations: map[string]string{"pp:endpoint": "amazon-ads-profiles-profiles.list", "pp:method": "GET", "pp:path": "/v2/profiles", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("api-program") {
				allowedApiProgram := []string{"billing", "campaign", "paymentMethod", "store", "report", "account", "posts"}
				validApiProgram := false
				for _, v := range allowedApiProgram {
					if flagApiProgram == v {
						validApiProgram = true
						break
					}
				}
				if !validApiProgram {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagApiProgram, "api-program", allowedApiProgram)
				}
			}
			if cmd.Flags().Changed("access-level") {
				allowedAccessLevel := []string{"edit", "view"}
				validAccessLevel := false
				for _, v := range allowedAccessLevel {
					if flagAccessLevel == v {
						validAccessLevel = true
						break
					}
				}
				if !validAccessLevel {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAccessLevel, "access-level", allowedAccessLevel)
				}
			}
			if cmd.Flags().Changed("profile-type-filter") {
				allowedProfileTypeFilter := []string{"seller", "vendor", "agency"}
				validProfileTypeFilter := false
				for _, v := range allowedProfileTypeFilter {
					if flagProfileTypeFilter == v {
						validProfileTypeFilter = true
						break
					}
				}
				if !validProfileTypeFilter {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagProfileTypeFilter, "profile-type-filter", allowedProfileTypeFilter)
				}
			}
			if cmd.Flags().Changed("valid-payment-method-filter") {
				allowedValidPaymentMethodFilter := []string{"true", "false"}
				validValidPaymentMethodFilter := false
				for _, v := range allowedValidPaymentMethodFilter {
					if flagValidPaymentMethodFilter == v {
						validValidPaymentMethodFilter = true
						break
					}
				}
				if !validValidPaymentMethodFilter {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagValidPaymentMethodFilter, "valid-payment-method-filter", allowedValidPaymentMethodFilter)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/profiles"
			params := map[string]string{}
			if flagApiProgram != "" {
				params["apiProgram"] = fmt.Sprintf("%v", flagApiProgram)
			}
			if flagAccessLevel != "" {
				params["accessLevel"] = fmt.Sprintf("%v", flagAccessLevel)
			}
			if flagProfileTypeFilter != "" {
				params["profileTypeFilter"] = fmt.Sprintf("%v", flagProfileTypeFilter)
			}
			if flagValidPaymentMethodFilter != "" {
				params["validPaymentMethodFilter"] = fmt.Sprintf("%v", flagValidPaymentMethodFilter)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "amazon-ads-profiles-profiles", true, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagApiProgram, "api-program", "", "Filters response to include profiles that have permissions for the specified Advertising API program only. (one of: billing, campaign, paymentMethod, store, report, account, posts)")
	cmd.Flags().StringVar(&flagAccessLevel, "access-level", "", "Filters response to include profiles that have specified permissions for the specified Advertising API program only. (one of: edit, view)")
	cmd.Flags().StringVar(&flagProfileTypeFilter, "profile-type-filter", "", "Filters response to include profiles that are of the specified types in the comma-delimited list. Default is all types. (one of: seller, vendor, agency)")
	cmd.Flags().StringVar(&flagValidPaymentMethodFilter, "valid-payment-method-filter", "", "Filter response to include profiles that have valid payment methods. Default is to include all profiles. (one of: true, false)")

	return cmd
}
