// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSbListNegativeKeywordsCmd(flags *rootFlags) *cobra.Command {
	var flagStartIndex int
	var flagCount int
	var flagMatchTypeFilter string
	var flagKeywordText string
	var flagStateFilter string
	var flagCampaignIdFilter string
	var flagAdGroupIdFilter string
	var flagKeywordIdFilter string
	var flagCreativeType string

	cmd := &cobra.Command{
		Use:         "list-negative-keywords",
		Short:       "Gets an array of negative keywords, filtered by optional criteria.",
		Example:     "  amazon-ads-pp-cli sb list-negative-keywords",
		Annotations: map[string]string{"pp:endpoint": "sb.list-negative-keywords", "pp:method": "GET", "pp:path": "/sb/negativeKeywords", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("match-type-filter") {
				allowedMatchTypeFilter := []string{"negativeExact", "negativePhrase"}
				validMatchTypeFilter := false
				for _, v := range allowedMatchTypeFilter {
					if flagMatchTypeFilter == v {
						validMatchTypeFilter = true
						break
					}
				}
				if !validMatchTypeFilter {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagMatchTypeFilter, "match-type-filter", allowedMatchTypeFilter)
				}
			}
			if cmd.Flags().Changed("state-filter") {
				allowedStateFilter := []string{"enabled", "paused", "archived"}
				validStateFilter := false
				for _, v := range allowedStateFilter {
					if flagStateFilter == v {
						validStateFilter = true
						break
					}
				}
				if !validStateFilter {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStateFilter, "state-filter", allowedStateFilter)
				}
			}
			if cmd.Flags().Changed("creative-type") {
				allowedCreativeType := []string{"productCollection", "video"}
				validCreativeType := false
				for _, v := range allowedCreativeType {
					if flagCreativeType == v {
						validCreativeType = true
						break
					}
				}
				if !validCreativeType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagCreativeType, "creative-type", allowedCreativeType)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/sb/negativeKeywords"
			params := map[string]string{}
			if flagStartIndex != 0 {
				params["startIndex"] = fmt.Sprintf("%v", flagStartIndex)
			}
			if flagCount != 0 {
				params["count"] = fmt.Sprintf("%v", flagCount)
			}
			if flagMatchTypeFilter != "" {
				params["matchTypeFilter"] = fmt.Sprintf("%v", flagMatchTypeFilter)
			}
			if flagKeywordText != "" {
				params["keywordText"] = fmt.Sprintf("%v", flagKeywordText)
			}
			if flagStateFilter != "" {
				params["stateFilter"] = fmt.Sprintf("%v", flagStateFilter)
			}
			if flagCampaignIdFilter != "" {
				params["campaignIdFilter"] = fmt.Sprintf("%v", flagCampaignIdFilter)
			}
			if flagAdGroupIdFilter != "" {
				params["adGroupIdFilter"] = fmt.Sprintf("%v", flagAdGroupIdFilter)
			}
			if flagKeywordIdFilter != "" {
				params["keywordIdFilter"] = fmt.Sprintf("%v", flagKeywordIdFilter)
			}
			if flagCreativeType != "" {
				params["creativeType"] = fmt.Sprintf("%v", flagCreativeType)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "sb", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagStartIndex, "start-index", 0, "Sets a zero-based offset into the requested set of negative keywords.")
	cmd.Flags().IntVar(&flagCount, "count", 0, "Sets the number of negative keywords in the returned array.")
	cmd.Flags().StringVar(&flagMatchTypeFilter, "match-type-filter", "", "The returned array is filtered to include only negative keywords with `matchType` set to one of the values in the (one of: negativeExact, negativePhrase)")
	cmd.Flags().StringVar(&flagKeywordText, "keyword-text", "", "The returned array includes only negative keywords with the specified text.")
	cmd.Flags().StringVar(&flagStateFilter, "state-filter", "", "The returned array includes only negative keywords with `state` set to the specified value. (one of: enabled, paused, archived)")
	cmd.Flags().StringVar(&flagCampaignIdFilter, "campaign-id-filter", "", "The returned array includes only negative keywords associated with campaigns matching those specified by identifier in")
	cmd.Flags().StringVar(&flagAdGroupIdFilter, "ad-group-id-filter", "", "The returned array includes only negative keywords associated with ad groups matching those specified by identifier in")
	cmd.Flags().StringVar(&flagKeywordIdFilter, "keyword-id-filter", "", "The returned array includes only negative keywords with identifiers matching those specified in the comma-delimited")
	cmd.Flags().StringVar(&flagCreativeType, "creative-type", "", "Filter by the type of creative the campaign is associated with. (one of: productCollection, video)")

	return cmd
}
