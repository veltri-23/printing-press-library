// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/commerce/amazon-ads/internal/cliutil"
	"github.com/spf13/cobra"
)

func newMeasurementDspCreateDspaudienceResearchStudyCmd(flags *rootFlags) *cobra.Command {
	var bodyAdvertiserId string
	var bodyAudienceTargetingGroupExcludedAudienceGroups string
	var bodyAudienceTargetingGroupIncludedAudienceGroups string
	var bodyBrandName string
	var bodyComment string
	var bodyCreateDate string
	var bodyEndDate string
	var bodyExternalReferenceId string
	var bodyLastUpdatedDate string
	var bodyName string
	var bodyPeerNames string
	var bodyProductCategory string
	var bodyReviewDate string
	var bodyStartDate string
	var bodyStatus string
	var bodyStatusReasons string
	var bodyStudyResultStatus string
	var bodySurveyId string
	var bodyVendorProductId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-dspaudience-research-study",
		Short:       "Create new DSP AUDIENCE_RESEARCH study. **Requires one of these permissions**: []",
		Example:     "  amazon-ads-pp-cli measurement-dsp create-dspaudience-research-study",
		Annotations: map[string]string{"pp:endpoint": "measurement-dsp.create-dspaudience-research-study", "pp:method": "POST", "pp:path": "/dsp/measurement/studies/audienceResearch"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/dsp/measurement/studies/audienceResearch"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAdvertiserId != "" {
					body["advertiserId"] = bodyAdvertiserId
				}
				{
					nestedAudienceTargetingGroup := map[string]any{}
					if bodyAudienceTargetingGroupExcludedAudienceGroups != "" {
						var parsedAudienceTargetingGroupExcludedAudienceGroups any
						if err := json.Unmarshal([]byte(bodyAudienceTargetingGroupExcludedAudienceGroups), &parsedAudienceTargetingGroupExcludedAudienceGroups); err != nil {
							return fmt.Errorf("parsing --audience-targeting-group-excluded-audience-groups JSON: %w", err)
						}
						nestedAudienceTargetingGroup["excludedAudienceGroups"] = parsedAudienceTargetingGroupExcludedAudienceGroups
					}
					if bodyAudienceTargetingGroupIncludedAudienceGroups != "" {
						var parsedAudienceTargetingGroupIncludedAudienceGroups any
						if err := json.Unmarshal([]byte(bodyAudienceTargetingGroupIncludedAudienceGroups), &parsedAudienceTargetingGroupIncludedAudienceGroups); err != nil {
							return fmt.Errorf("parsing --audience-targeting-group-included-audience-groups JSON: %w", err)
						}
						nestedAudienceTargetingGroup["includedAudienceGroups"] = parsedAudienceTargetingGroupIncludedAudienceGroups
					}
					if len(nestedAudienceTargetingGroup) > 0 {
						body["audienceTargetingGroup"] = nestedAudienceTargetingGroup
					}
				}
				if bodyBrandName != "" {
					body["brandName"] = bodyBrandName
				}
				if bodyComment != "" {
					body["comment"] = bodyComment
				}
				if bodyCreateDate != "" {
					body["createDate"] = bodyCreateDate
				}
				if bodyEndDate != "" {
					body["endDate"] = bodyEndDate
				}
				if bodyExternalReferenceId != "" {
					body["externalReferenceId"] = bodyExternalReferenceId
				}
				if bodyLastUpdatedDate != "" {
					body["lastUpdatedDate"] = bodyLastUpdatedDate
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPeerNames != "" {
					body["peerNames"] = cliutil.SplitCSV(bodyPeerNames)
				}
				if bodyProductCategory != "" {
					body["productCategory"] = bodyProductCategory
				}
				if bodyReviewDate != "" {
					body["reviewDate"] = bodyReviewDate
				}
				if bodyStartDate != "" {
					body["startDate"] = bodyStartDate
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyStatusReasons != "" {
					body["statusReasons"] = cliutil.SplitCSV(bodyStatusReasons)
				}
				if bodyStudyResultStatus != "" {
					body["studyResultStatus"] = bodyStudyResultStatus
				}
				if bodySurveyId != "" {
					body["surveyId"] = bodySurveyId
				}
				if bodyVendorProductId != "" {
					body["vendorProductId"] = bodyVendorProductId
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "measurement-dsp", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "measurement-dsp", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "measurement-dsp", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "measurement-dsp", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "measurement-dsp", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "measurement-dsp",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "measurement-dsp", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "measurement-dsp", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAdvertiserId, "advertiser-id", "", "The associated advertiser identifier. Immutable field.")
	cmd.Flags().StringVar(&bodyAudienceTargetingGroupExcludedAudienceGroups, "audience-targeting-group-excluded-audience-groups", "", "List of audience groups to be excluded from the targeted audience.")
	cmd.Flags().StringVar(&bodyAudienceTargetingGroupIncludedAudienceGroups, "audience-targeting-group-included-audience-groups", "", "List of audience groups to be included in the targeted audience.")
	cmd.Flags().StringVar(&bodyBrandName, "brand-name", "", "The study brand name.")
	cmd.Flags().StringVar(&bodyComment, "comment", "", "The approver's comment on why the study is approved/rejected.")
	cmd.Flags().StringVar(&bodyCreateDate, "create-date", "", "The study creation date in ISO format (YYYY-MM-DDThh:mm:ssTZD). Timezone is UTC.")
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "The study end date in ISO format (YYYY-MM-DDThh:mm:ssTZD). Timezone is UTC.")
	cmd.Flags().StringVar(&bodyExternalReferenceId, "external-reference-id", "", "Optional field.")
	cmd.Flags().StringVar(&bodyLastUpdatedDate, "last-updated-date", "", "The study last updated date in ISO format (YYYY-MM-DDThh:mm:ssTZD). Timezone is UTC.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The study name.")
	cmd.Flags().StringVar(&bodyPeerNames, "peer-names", "", "A list of peer names for the study brand.")
	cmd.Flags().StringVar(&bodyProductCategory, "product-category", "", "The study product category.")
	cmd.Flags().StringVar(&bodyReviewDate, "review-date", "", "The study review date in ISO format (YYYY-MM-DDThh:mm:ssTZD). Timezone is UTC.")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "The study start date in ISO format (YYYY-MM-DDThh:mm:ssTZD). Timezone is UTC.")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "The study status. Only required if study is in DRAFT as part of study creation/update.")
	cmd.Flags().StringVar(&bodyStatusReasons, "status-reasons", "", "List of reasons for study status.")
	cmd.Flags().StringVar(&bodyStudyResultStatus, "study-result-status", "", "The status of result of the study.")
	cmd.Flags().StringVar(&bodySurveyId, "survey-id", "", "The study survey canonical identifier.")
	cmd.Flags().StringVar(&bodyVendorProductId, "vendor-product-id", "", "Associated vendor product canonical identifier.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
