// Copyright 2026 Trevin Chow and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/commerce/yahoo-finance/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/commerce/yahoo-finance/internal/client"
	"github.com/mvanhorn/printing-press-library/library/commerce/yahoo-finance/internal/config"
	"github.com/mvanhorn/printing-press-library/library/commerce/yahoo-finance/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/commerce/yahoo-finance/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("autocomplete_get",
			mcplib.WithDescription("Autocomplete symbols and company names. Required: query. Optional: lang (default: en), region (default: US). Returns the AutocompleteResult."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Prefix query")),
			mcplib.WithString("lang", mcplib.Description("Language code")),
			mcplib.WithString("region", mcplib.Description("Region code")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v6/finance/autocomplete", true, false, nil, []mcpParamBinding{{PublicName: "query", WireName: "query", Location: "query"}, {PublicName: "lang", WireName: "lang", Location: "query"}, {PublicName: "region", WireName: "region", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("chart_get",
			mcplib.WithDescription("Historical price chart data for a symbol. Required: symbol. Optional: interval (default: 1d), range (default: 1mo), period1 (plus 3 more). Returns the Chart."),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("Ticker symbol")),
			mcplib.WithString("interval", mcplib.Description("Bar interval: 1m, 2m, 5m, 15m, 30m, 60m, 90m, 1h, 1d, 5d, 1wk, 1mo, 3mo")),
			mcplib.WithString("range", mcplib.Description("Date range: 1d, 5d, 1mo, 3mo, 6mo, 1y, 2y, 5y, 10y, ytd, max")),
			mcplib.WithNumber("period1", mcplib.Description("Start unix timestamp (overrides range)")),
			mcplib.WithNumber("period2", mcplib.Description("End unix timestamp")),
			mcplib.WithString("events", mcplib.Description("Events to include: div, split, earn")),
			mcplib.WithBoolean("includePrePost", mcplib.Description("Include pre/post-market data")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v8/finance/chart/{symbol}", true, false, nil, []mcpParamBinding{{PublicName: "symbol", WireName: "symbol", Location: "path"}, {PublicName: "interval", WireName: "interval", Location: "query"}, {PublicName: "range", WireName: "range", Location: "query"}, {PublicName: "period1", WireName: "period1", Location: "query"}, {PublicName: "period2", WireName: "period2", Location: "query"}, {PublicName: "events", WireName: "events", Location: "query"}, {PublicName: "includePrePost", WireName: "includePrePost", Location: "query"}}, []string{"symbol"}),
	)
	s.AddTool(
		mcplib.NewTool("fundamentals_timeseries",
			mcplib.WithDescription("Fundamentals time series (EPS, revenue, margin, cash flow, etc.). Required: symbol, type. Optional: period1 (default: 0), period2. Returns the TimeSeries."),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("Ticker symbol")),
			mcplib.WithString("type", mcplib.Required(), mcplib.Description("Comma-separated fundamentals keys (prefix annual/quarterly/trailing)")),
			mcplib.WithNumber("period1", mcplib.Description("Start unix timestamp")),
			mcplib.WithNumber("period2", mcplib.Description("End unix timestamp (default now)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/ws/fundamentals/v1/finance/timeseries/{symbol}", true, false, nil, []mcpParamBinding{{PublicName: "symbol", WireName: "symbol", Location: "path"}, {PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "period1", WireName: "period1", Location: "query"}, {PublicName: "period2", WireName: "period2", Location: "query"}}, []string{"symbol"}),
	)
	s.AddTool(
		mcplib.NewTool("insights_get",
			mcplib.WithDescription("Insights for a symbol: technical events, valuation, research reports. Required: symbol. Returns the Insights."),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("Ticker symbol")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/ws/insights/v3/finance/insights", true, false, nil, []mcpParamBinding{{PublicName: "symbol", WireName: "symbol", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("lookup_query",
			mcplib.WithDescription("Search for symbols, news, and funds matching a query. Required: query. Optional: quotesCount (default: 10), newsCount (default: 5), region (default: US). Returns the SearchResult."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (ticker or company name)")),
			mcplib.WithNumber("quotesCount", mcplib.Description("Max quote results")),
			mcplib.WithNumber("newsCount", mcplib.Description("Max news results")),
			mcplib.WithString("region", mcplib.Description("Market region")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/finance/search", true, false, nil, []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "quotesCount", WireName: "quotesCount", Location: "query"}, {PublicName: "newsCount", WireName: "newsCount", Location: "query"}, {PublicName: "region", WireName: "region", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("options_get",
			mcplib.WithDescription("Options chain for a symbol (calls and puts). Required: symbol. Optional: date. Returns the OptionChain."),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("Underlying ticker symbol")),
			mcplib.WithNumber("date", mcplib.Description("Expiration unix epoch (omit for nearest expiration)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v7/finance/options/{symbol}", true, false, nil, []mcpParamBinding{{PublicName: "symbol", WireName: "symbol", Location: "path"}, {PublicName: "date", WireName: "date", Location: "query"}}, []string{"symbol"}),
	)
	s.AddTool(
		mcplib.NewTool("quote_list",
			mcplib.WithDescription("Get current quotes for one or more symbols. Required: symbols. Optional: fields, region (default: US). Returns array of Quote."),
			mcplib.WithString("symbols", mcplib.Required(), mcplib.Description("Comma-separated ticker symbols")),
			mcplib.WithString("fields", mcplib.Description("Comma-separated response fields filter")),
			mcplib.WithString("region", mcplib.Description("Market region")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v7/finance/quote", true, false, nil, []mcpParamBinding{{PublicName: "symbols", WireName: "symbols", Location: "query"}, {PublicName: "fields", WireName: "fields", Location: "query"}, {PublicName: "region", WireName: "region", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("quote_summary",
			mcplib.WithDescription("Deep quote summary including price, fundamentals, ownership, and filings. Required: symbol. Optional: modules. Returns the QuoteSummary."),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("Ticker symbol")),
			mcplib.WithString("modules", mcplib.Description("Comma-separated quoteSummary modules: assetProfile, summaryProfile, summaryDetail, price, financialData")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v10/finance/quoteSummary/{symbol}", true, false, nil, []mcpParamBinding{{PublicName: "symbol", WireName: "symbol", Location: "path"}, {PublicName: "modules", WireName: "modules", Location: "query"}}, []string{"symbol"}),
	)
	s.AddTool(
		mcplib.NewTool("recommendations_bysymbol",
			mcplib.WithDescription("Symbols that share recommendations with the given symbol. Required: symbol. Returns the Recommendations."),
			mcplib.WithString("symbol", mcplib.Required(), mcplib.Description("Ticker symbol")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v6/finance/recommendationsbysymbol/{symbol}", true, false, nil, []mcpParamBinding{{PublicName: "symbol", WireName: "symbol", Location: "path"}}, []string{"symbol"}),
	)
	s.AddTool(
		mcplib.NewTool("screener_predefined",
			mcplib.WithDescription("Run a predefined screener by ID. Required: scrIds (default: day_gainers). Optional: count (default: 25), region (default: US). Returns array of ScreenerQuote."),
			mcplib.WithString("scrIds", mcplib.Required(), mcplib.Description("Predefined screener: day_gainers, day_losers, most_actives, undervalued_growth_stocks, aggressive_small_caps")),
			mcplib.WithNumber("count", mcplib.Description("Max results")),
			mcplib.WithString("region", mcplib.Description("Market region")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/finance/screener/predefined/saved", true, false, nil, []mcpParamBinding{{PublicName: "scrIds", WireName: "scrIds", Location: "query"}, {PublicName: "count", WireName: "count", Location: "query"}, {PublicName: "region", WireName: "region", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("trending_list",
			mcplib.WithDescription("Top trending symbols in a region right now. Required: region (default: US). Optional: count (default: 10). Returns array of TrendingSymbol."),
			mcplib.WithString("region", mcplib.Required(), mcplib.Description("Region code: US, GB, FR, DE, HK, IN, AU, CA, BR, SG, TW, IT, ES, NL")),
			mcplib.WithNumber("count", mcplib.Description("Max trending results")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/finance/trending/{region}", true, false, nil, []mcpParamBinding{{PublicName: "region", WireName: "region", Location: "path"}, {PublicName: "count", WireName: "count", Location: "query"}}, []string{"region"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      Run 'yahoo-finance-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      Run 'yahoo-finance-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "yahoo-finance-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "yahoo-finance-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "yahoo-finance",
		"description": "Every Yahoo Finance endpoint plus a SQLite portfolio, covered-call screener",
		"archetype":   "generic",
		"tool_count":  11,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion yahoo-finance-pp-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "autocomplete",
				"description": "Legacy autocomplete (faster than search)",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
			{
				"name":        "chart",
				"description": "Historical OHLCV price data",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
			{
				"name":        "fundamentals",
				"description": "Time series of fundamentals (quarterly/annual)",
				"endpoints":   []string{"timeseries"},
				"searchable":  true,
			},
			{
				"name":        "insights",
				"description": "Company insights, valuation, and technical events",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
			{
				"name":        "lookup",
				"description": "Symbol search and lookup",
				"endpoints":   []string{"query"},
				"searchable":  true,
			},
			{
				"name":        "options",
				"description": "Options chains for equities and ETFs",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
			{
				"name":        "quote",
				"description": "Real-time quotes and quote summaries",
				"endpoints":   []string{"list", "summary"},
				"searchable":  true,
			},
			{
				"name":        "recommendations",
				"description": "Symbols related by analyst recommendation",
				"endpoints":   []string{"bysymbol"},
				"searchable":  true,
			},
			{
				"name":        "screener",
				"description": "Predefined and custom stock screeners",
				"endpoints":   []string{"predefined"},
				"searchable":  true,
			},
			{
				"name":        "trending",
				"description": "Trending symbols by region",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the count parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Portfolio performance tracker", "command": "portfolio perf", "description": "Track YTD / 1Y / all-time returns on your holdings with cost basis, dividends, and current price baked in.", "rationale": "Requires joining local cost-basis state with live quotes and historical dividends — no Yahoo endpoint provides this", "via": "mcp-command-mirror"},
			{"name": "Morning digest across watchlist", "command": "digest", "description": "One command: overnight news + biggest movers + earnings today + ex-div dates for everything on your watchlist.", "rationale": "Aggregates data across quote, news, calendarEvents", "via": "mcp-command-mirror"},
			{"name": "Dividend income + yield-on-cost", "command": "portfolio dividends", "description": "See total dividend income for the year, per-holding breakdown, and yield on cost.", "rationale": "Joins local portfolio lots with cached dividend history.", "via": "mcp-command-mirror"},
			{"name": "Options moneyness + DTE filter", "command": "options moneyness", "description": "Filter an options chain to ATM/OTM/ITM contracts within a days-to-expiration window.", "rationale": "Yahoo's options endpoint returns the full chain; computing moneyness against spot and DTE requires local processing.", "via": "mcp-command-mirror"},
			{"name": "Local SQL screener over synced fundamentals", "command": "screen-local", "description": "Run arbitrary P/E, P/B, yield, margin, and growth filters against the data you've synced locally.", "rationale": "Yahoo's remote screener is limited to 12 predefined IDs. Arbitrary filters require a local fundamentals index.", "via": "mcp-command-mirror"},
			{"name": "Chrome cookie import fallback", "command": "auth login --chrome", "description": "Import your live Chrome session cookies when Yahoo's crumb handshake is blocked from your IP.", "rationale": "Works around residential and cloud IP rate limits that kill raw API access.", "via": "mcp-command-mirror"},
			{"name": "Insider net-buying signal screener", "command": "insiders net-buying", "description": "Surface companies where insiders are net buyers in the last N days, filtered to your watchlist.", "rationale": "Joins insider transactions × watchlist membership and computes buy/sell ratios — needs local state.", "via": "mcp-command-mirror"},
			{"name": "Total-return comparator", "command": "compare", "description": "Multi-symbol price-plus-dividend total return ranked over a range.", "rationale": "Joins cached history × dividends to compute total return (price appreciation + reinvested dividends)", "via": "mcp-command-mirror"},
			{"name": "Covered-call screener over holdings", "command": "options covered-calls", "description": "Scan your holdings for covered-call candidates by annualized yield and DTE.", "rationale": "Joins portfolio_lots (shares >=100) × options chains × spot quote.", "via": "mcp-command-mirror"},
			{"name": "Watchlist correlation matrix", "command": "watchlist correlate", "description": "Pairwise Pearson correlation across the symbols in a named watchlist over a date range.", "rationale": "Pulls cached daily closes for every symbol in the watchlist", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Portfolio performance tracker", "insight": "Requires joining local cost-basis state with live quotes and historical dividends — no Yahoo endpoint provides this because cost basis only exists on the user's machine."},
			{"topic": "Morning digest across watchlist", "insight": "Aggregates data across quote, news, calendarEvents, and dailyGainers filtered by local watchlist — no single API call returns this view."},
			{"topic": "Dividend income + yield-on-cost", "insight": "Joins local portfolio lots with cached dividend history. Yield-on-cost requires the original cost basis the user typed in."},
			{"topic": "Options moneyness + DTE filter", "insight": "Yahoo's options endpoint returns the full chain; computing moneyness against spot and DTE requires local processing."},
			{"topic": "Local SQL screener over synced fundamentals", "insight": "Yahoo's remote screener is limited to 12 predefined IDs. Arbitrary filters require a local fundamentals index."},
			{"topic": "Chrome cookie import fallback", "insight": "Works around residential and cloud IP rate limits that kill raw API access. No other Yahoo Finance tool offers this fallback."},
			{"topic": "Insider net-buying signal screener", "insight": "Joins insider transactions × watchlist membership and computes buy/sell ratios — needs local state."},
			{"topic": "Total-return comparator", "insight": "Joins cached history × dividends to compute total return (price appreciation + reinvested dividends) — single-shot from local data."},
			{"topic": "Covered-call screener over holdings", "insight": "Joins portfolio_lots (shares >=100) × options chains × spot quote. Computes annualized yield = (call_bid / spot) * (365/DTE)."},
			{"topic": "Watchlist correlation matrix", "insight": "Pulls cached daily closes for every symbol in the watchlist, computes pairwise correlation locally — portfolio-theory diversification check."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
