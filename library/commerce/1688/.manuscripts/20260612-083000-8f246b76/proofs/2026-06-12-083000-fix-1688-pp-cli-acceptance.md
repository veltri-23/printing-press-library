# 1688-pp-cli Acceptance Report

  Level: Full Dogfood (live, binary-owned matrix)
  Tests: 40/40 passed
  Gate: PASS

## Fixes applied during dogfood
- 10 commands failed the `error_path` probe (`__printing_press_invalid__` → expected non-zero exit). For 1688, an arbitrary keyword/offer-ID is a *valid* query that returns zero results (search even returns ~60 fallback recommendations), so exit 0 is correct. Added `pp:no-error-path-probe: "true"` to search, sync, find, offers, factory-find, repurchase-top, region-spread, compare, supplier-report, watch, drift — the documented annotation for "HTTP 200 + empty success envelope on unknown input, indistinguishable from valid-empty." Re-ran: 40/40 pass.

## Behavioral correctness (live, 2026-06-12)
All read commands return exit 0 with valid JSON; live search returns rich offers; sync persists + snapshots; transcendence commands compute correctly over the local store; doctor reachable. No flagship feature returns wrong output.

## Printing Press issues (retro candidates)
- Generated `sync` stub correctly instructed hand-authoring for signed-mtop surfaces.
- Generated `offers` HTML page-mode endpoint extracted nothing (detail edge not __NEXT_DATA__); replaced with store-backed lookup.
- Generated `offers` example used a UUID placeholder (1688 offer IDs are numeric).
- error_path probe treats "API returns recommendations for any keyword" as needing a non-zero exit; the `pp:no-error-path-probe` annotation is the correct escape hatch (worked as designed).
