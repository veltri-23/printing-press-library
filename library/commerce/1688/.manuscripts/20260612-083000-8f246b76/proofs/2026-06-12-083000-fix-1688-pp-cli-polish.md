# 1688-pp-cli Polish

The forked `printing-press-polish` skill ran partially (applied a gosec `#nosec G501`
annotation on the md5 import — md5 is 1688's required signature algorithm, not a
security primitive — plus errcheck fixes on `io.Copy`) before the forked execution
hit a transient `API Error: Overloaded`. The main remaining lever was completed in
the parent session:

- **Dead code removed:** the 6 orphaned generated helpers (formatCLIParamValue,
  printProvenance, replacePathParam, unwrapSingleKeyArray, wantsHumanTable,
  wrapWithProvenance) and their now-unused imports (bytes, net/url). They were
  orphaned when the generated `offers` HTML endpoint command was replaced with a
  store-backed command.

## Scorecard delta
- Before polish: 68/100 (Grade B) — dead_code 0/5, insight 4/10, type_fidelity 2/5
- After:         74/100 (Grade B) — dead_code 5/5, insight 10/10, type_fidelity 4/5

## Shipcheck after polish
All 6 legs PASS; verify PASS; dogfood PASS; scorecard 74. Remaining:
- path_validity 0/10 — structural; this CLI's data surface is a hand-built signed
  mtop client, not generated endpoint commands, so there are no generated paths to
  validate. Inherent to the reverse-engineered-API design, not a fixable gap.

## Pre-publish hygiene (per user instruction: no the work organization trace)
- Added canonical `.gitignore` (binaries, build/, *.db, *.err) mirroring the
  published-library pattern.
- Removed all compiled binaries + build/ from the working tree (Go debug paths
  embed the `<path> username — confirmed via strings probe).
- Source/docs scan: ZERO the work organization / developer / pp-workspace / @developer
  strings; author headers use the personal name "Hamza Qazi" only.
- Remaining publish-time gate (Phase 6 / publish skill): verify commit identity =
  personal GitHub (qazmataz noreply), NOT dev@example.com.
