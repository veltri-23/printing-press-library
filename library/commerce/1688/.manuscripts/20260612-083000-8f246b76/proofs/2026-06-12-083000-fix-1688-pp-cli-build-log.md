# 1688-pp-cli Build Log

Manifest transcendence rows: 7 planned, 7 built. Phase 3 will not pass until all 7 ship.

## Built
- **Signed mtop client** (`internal/mtop/client.go`): token bootstrap (cookie jar) -> md5 sign -> signed GET, with adaptive limiter + typed RateLimitError, token TTL cache, retry on token expiry. Parses `data.data.OFFER.items[]` (cellType smart_ui_offer) into a normalized `Offer` (price, MOQ, supplier+memberId+loginId(decoded), province/city, transactions, sales volume, repurchase rate, factory flags, 深度验厂 tags, trade-service scores, image, ad flag, URL).
- **Store extension** (`internal/store/store_1688.go`): `offer_snapshots` table (drift), `FindOffers` (LIKE substring for CJK, since FTS does not tokenize Chinese), `OffersByKeyword`, `OffersBySupplier`. Offers/suppliers persist into the generic resources table.
- **Core commands**: `search` (live, persists), `sync` (multi-page live persist, dogfood-curtailed), `find` (offline LIKE), `offers <id>` (store-backed single-offer lookup — replaces the generated HTML page-mode endpoint, which returned empty because detail.1688.com is not __NEXT_DATA__ and throttles direct fetches to a stub).
- **Transcendence (7/7)**: `factory-find`, `repurchase-top`, `drift`, `compare`, `supplier-report`, `watch`, `region-spread` — all hand-coded, all verified live.

## Verified live (2026-06-12)
search SUCCESS (total 2000, rich offers); sync persisted 20; factory-find ranked verified factories top (深度验厂 + 41% reorder); repurchase-top sorted with min-tx floor; region-spread grouped by province (median price); compare side-by-side; supplier-report aggregated (avg reorder 31%, badges, trade scores); drift diffed snapshots; find (LIKE) matched 硅胶; doctor reachable. All 10 commands `--dry-run` exit 0.

## Deferred / gaps
- **Search-by-image** (AliPrice): stub, not built — needs image upload + a different mtop endpoint. Documented in README ## Known Gaps.
- **Offer-detail HTML extraction**: detail.1688.com throttles non-browser requests to a stub and isn't a Next.js page, so `offers <id>` is store-backed (the robust mtop search gateway is the data source) rather than scraping the detail page.

## Generator notes (retro candidates)
- Generated `sync` stub explicitly instructed hand-authoring (signed-mtop surface does not fit the generic spec-driven sync template) — correct.
- Generated `offers` HTML page-mode endpoint extracted nothing (detail edge not __NEXT_DATA__); replaced with a store-backed command.
- Generated `offers` example used a UUID placeholder; real 1688 offer IDs are numeric (fixed in the hand-authored replacement).
