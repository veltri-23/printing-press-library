# 1688-pp-cli Shipcheck

## Verdict: ship

Shipcheck umbrella: **PASS (6/6 legs)**.

| Leg | Result |
|-----|--------|
| verify | PASS (data_pipeline true, pass_rate 100%, 0 critical) |
| validate-narrative | PASS (all README/SKILL example commands resolve + dry-run) |
| dogfood | PASS |
| workflow-verify | PASS |
| verify-skill | PASS (SKILL flags/commands all exist in source) |
| scorecard | PASS — **68/100 Grade B** |

## Fixes applied during shipcheck
1. **verify FAIL → PASS:** data-pipeline check ran `sync` framework-style (`--full`/`--resources`, no keyword) → exit 2 ("sync crashed"). Added `--full`/`--resources` flags and made a no-keyword invocation a clean no-op (1688 sync is keyword-driven, not resource-walking). Now `sync --full` exits 0; data_pipeline true.
2. **mtop tests:** added `internal/mtop/client_test.go` (table-driven: parsePercent/parseFloat/parseInt/firstImage/sortType/hasServiceTag/decodeOffer). Cleared the "pure-logic package with no tests" issue; type_fidelity 2/5 → 4/5.
3. **find CJK:** switched offline search from FTS (doesn't tokenize Chinese) to LIKE substring.
4. **offers detail:** replaced the empty-returning generated HTML page-mode endpoint with a store-backed lookup (detail.1688.com throttles direct fetches to a stub).
5. **login_id:** URL-decoded `loginIdOfUtf8`.

## Remaining gaps (non-blocking; for Phase 5.5 polish)
- **dead_code 0/5:** 6 generated framework helpers (formatCLIParamValue, printProvenance, replacePathParam, unwrapSingleKeyArray, wantsHumanTable, wrapWithProvenance) became orphaned when the generated `offers` endpoint command was replaced with a store-backed command. Polish removes dead code.
- **path_validity 0/10:** structural — this CLI's data surface is a hand-built signed mtop client, not generated endpoint commands, so there are no generated paths to validate. Inherent to the reverse-engineered-API design.
- **Scorecard live-sample probe (informational, not a leg):** the token-match heuristic does not fit store-backed analytical commands (drift/region-spread/supplier-report emit offer-IDs/provinces/memberIds, not the query keyword) and ran against an empty default store; `watch` is a live call that can exceed the 10s probe timeout. All commands verified working live by hand.

## Behavioral correctness (manual live verification, 2026-06-12)
search SUCCESS (2000 total); sync persisted 20/page; factory-find ranked verified factories top; repurchase-top sorted by 回头率 with min-tx floor; compare side-by-side; supplier-report aggregated; drift diffed snapshots; region-spread grouped by province; find (LIKE) matched 硅胶; offers store-backed; doctor reachable.
