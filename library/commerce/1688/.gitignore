# Compiled binaries at the CLI root (release builds come from CI; never committed)
/1688-pp-cli
/1688-pp-mcp
/1688-pp-cli-dogfood
/build/
# Local SQLite stores / caches
*.db
*.db-shm
*.db-wal
# Transient, path-leaking run logs
*.err
dogfood-results.json
publish-live-gate.json
