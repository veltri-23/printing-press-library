version: 2
project_name: amazon-seller-pp-cli
changelog:
  disable: true
builds:
  - id: amazon-seller-pp-cli
    main: ./cmd/amazon-seller-pp-cli
    binary: amazon-seller-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/commerce/amazon-seller/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: amazon-seller-pp-mcp
    main: ./cmd/amazon-seller-pp-mcp
    binary: amazon-seller-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: amazon-seller-pp-cli
    repository:
      owner: user
      name: homebrew-tap
    homepage: "https://github.com/user/amazon-seller-pp-cli"
    description: "Run Amazon seller workflows from the terminal: FBA inventory, orders, sales reports, listings, catalog lookup, and account operations."
    install: |
      bin.install "amazon-seller-pp-cli"
      bin.install "amazon-seller-pp-mcp"
